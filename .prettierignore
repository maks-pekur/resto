node_modules
.pnpm-store
dist
build
out
.next
.nx
coverage
playwright-report
test-results
pnpm-lock.yaml
*.generated.ts
generated
.env*

# Emitted by `pnpm exec nx run api:openapi:emit`. Round-tripping through
# prettier rewrites anchors/spacing so the CI drift check would fail on
# every push (RES-87).
docs/api/openapi.yaml
