---
phase: 06-qr-menu-customer
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - apps/qr-menu/package.json
  - apps/qr-menu/src/api/types.ts
  - apps/qr-menu/src/api/client.ts
  - apps/qr-menu/src/App.tsx
  - apps/qr-menu/src/components/MenuView.tsx
  - apps/qr-menu/src/components/MenuItemCard.tsx
  - apps/qr-menu/src/components/ItemDetail.tsx
  - apps/qr-menu/src/styles.css
  - apps/qr-menu/test/menu-view.spec.tsx
autonomous: true
requirements: [QRM-01, QRM-02, QRM-03, QRM-04, QRM-05]
must_haves:
  truths:
    - 'qr-menu imports menu wire types from @resto/api-client/public (its duplicate api/types.ts is deleted)'
    - 'The existing display render path (categories + item card with photo + price) still works after the api/types.ts deletion and re-import from @resto/api-client/public (QRM-02 carry-over verified, not assumed)'
    - 'Guest sees a branded header (logo + display name; brand primaryColor injected as --resto-accent)'
    - 'Item detail renders description/allergens/photo + modifier groups resolved from menu.modifierGroups'
    - 'Selecting modifiers updates a live price using @resto/cart parseMinorUnits/formatMinorUnits'
    - 'Add-to-cart writes a CartLineItem (with selected modifiers) into @resto/cart'
  artifacts:
    - path: 'apps/qr-menu/src/components/ItemDetail.tsx'
      provides: 'modifier selection + live price + add-to-cart'
      contains: 'useCartStore'
    - path: 'apps/qr-menu/package.json'
      provides: '@resto/cart + @resto/api-client workspace deps'
      contains: '@resto/cart'
  key_links:
    - from: 'apps/qr-menu/src/components/ItemDetail.tsx'
      to: '@resto/cart'
      via: 'useCartStore.getState().addItem'
      pattern: 'addItem'
    - from: 'apps/qr-menu/src/components/MenuView.tsx'
      to: '@resto/api-client/public'
      via: 'MenuDto type import'
      pattern: '@resto/api-client/public'
---

<objective>
Switch qr-menu from its duplicated `api/types.ts` to `@resto/api-client/public`, add the branded header (QRM-01), and extend `ItemDetail` into a real ordering surface: render modifier groups (QRM-03), compute a live price on selection (QRM-04), and add the configured item to the shared `@resto/cart` (QRM-05). Categories/items/photos/prices (QRM-02) already render display-only and MUST be re-verified intact after the type swap (the existing menu-view render test is re-pointed and asserts the render path still works).

Purpose: This is the core ordering build-out on the in-venue surface. It depends on `@resto/cart` (Wave 1, plan 01) for the store + minor-units helpers and on the api-client wire type (plan 02) so qr-menu inherits the menu shape (incl. forthcoming isStopListed) from a single source.
Output: qr-menu renders branded header + item detail with working modifiers, live price, and add-to-cart, and the existing menu display render still passes. Cart drawer/table/stop-list/i18n land in plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-qr-menu-customer/06-CONTEXT.md
@.planning/phases/06-qr-menu-customer/06-RESEARCH.md
@.planning/phases/06-qr-menu-customer/06-PATTERNS.md

<read_first>

- apps/qr-menu/src/App.tsx — routing (parsePath/navigate\*) to extend; pass resolved groups to ItemDetail
- apps/qr-menu/src/components/MenuView.tsx — category loop + where to add branded header + cart trigger prop
- apps/qr-menu/src/components/MenuItemCard.tsx — item card to extend (isStopListed wired in plan 04)
- apps/qr-menu/src/components/ItemDetail.tsx — skeleton to extend with modifiers + live price + add-to-cart
- apps/qr-menu/src/api/types.ts — duplicate wire types to DELETE
- apps/qr-menu/test/menu-view.spec.tsx — existing render-path test; imports MenuDto from ../src/api/types (must be re-pointed when api/types.ts is deleted); this is the spec that proves QRM-02 still renders
- apps/qr-menu/src/styles.css — :root --resto-\* tokens; add header/modifier/price classes here
- apps/website/components/menu/item-modal.tsx — LOGIC reference for modifier selection / toggleOption / handleAddToCart (NOT its shadcn/Tailwind styling)
- packages/api-client/src/public.ts — the barrel qr-menu imports from
  </read_first>

<interfaces>
From @resto/api-client/public (after plan 02): MenuDto, MenuItemDto, MenuModifierGroupDto, MenuModifierOptionDto, MenuBrandDto, LocalizedText, MenuPhotoDto. MenuItemDto carries modifierGroupIds: readonly string[] and (post plan 02) isStopListed: boolean. MenuModifierOptionDto.priceDelta is a decimal string (can be negative). MenuModifierGroupDto has minSelectable/maxSelectable/isRequired + options.
From @resto/cart: useCartStore, CartModifier, CartLineItem, parseMinorUnits, formatMinorUnits.
qr-menu i18n: `localized(field)` (no locale arg) + `t(key)` from '../i18n'.
Brand theme injection point: document.documentElement.style.setProperty('--resto-accent', brand.theme.primaryColor) — --resto-accent already exists in styles.css :root.
Existing render-path test (apps/qr-menu/test/menu-view.spec.tsx): renders <MenuView menu={buildMenu()} onSelectItem={...} /> and asserts the level-1 heading, the level-2 category heading (Pizza), and an item button (Margherita). buildMenu() types its fixture as MenuDto imported from ../src/api/types — that import must move to @resto/api-client/public.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch qr-menu to @resto/api-client/public types (and verify the display render still works)</name>
  <files>apps/qr-menu/package.json, apps/qr-menu/src/api/types.ts, apps/qr-menu/src/api/client.ts, apps/qr-menu/src/App.tsx, apps/qr-menu/src/components/MenuView.tsx, apps/qr-menu/src/components/MenuItemCard.tsx, apps/qr-menu/src/components/ItemDetail.tsx, apps/qr-menu/test/menu-view.spec.tsx</files>
  <action>
    Add `"@resto/cart": "workspace:*"` and `"@resto/api-client": "workspace:*"` to apps/qr-menu/package.json dependencies; run pnpm install.
    Delete apps/qr-menu/src/api/types.ts. In every file that imported from '../api/types' / './types' (api/client.ts, App.tsx, MenuView.tsx, MenuItemCard.tsx, ItemDetail.tsx, the test/menu-view.spec.tsx, and any others found by grep), replace the import source with '@resto/api-client/public', keeping the imported type names identical (MenuDto, MenuItemDto, etc.). These remain `import type` where they already were. In test/menu-view.spec.tsx specifically, change `import type { MenuDto } from '../src/api/types';` to `import type { MenuDto } from '@resto/api-client/public';` and reconcile the buildMenu() fixture with the api-client MenuDto shape if any field name/optionality differs (the fixture must still typecheck — adjust only fixture fields, NOT the existing assertions). Do NOT touch the tenant-header logic in api/client.ts (VITE_TENANT_SLUG → X-Tenant-Slug) other than its type import source.
    QRM-02 carry-over guard: after re-pointing, confirm the menu-view render test still asserts the display path. Extend menu-view.spec.tsx's "renders categories and item names" test (or add one assertion to it) so it proves an item card renders its PRICE and, when a photo is present, its image: give the buildMenu() Margherita fixture a non-null `imageUrl` (or one `photos` entry) and a `basePrice`, then additionally assert the rendered card shows the formatted price text (e.g. the `12.50`/currency string MenuItemCard renders) and an `<img>` (e.g. `screen.getByRole('img')`). Do NOT remove the existing category/item-name/empty-state/onSelectItem assertions. This makes QRM-02 (categories + item card with photo + price) a verified render, not a passive carry-over.
    Grep-verify zero remaining '../api/types' or './types' references under apps/qr-menu (src AND test) before finishing.
    No code comments.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm install >/dev/null 2>&1; test ! -f apps/qr-menu/src/api/types.ts && grep -q '"@resto/api-client"' apps/qr-menu/package.json && grep -q '"@resto/cart"' apps/qr-menu/package.json && [ "$(grep -rn --include='*.ts' --include='*.tsx' "api/types" apps/qr-menu/src apps/qr-menu/test | grep -v '^#' | wc -l | tr -d ' ')" = "0" ] && grep -q "@resto/api-client/public" apps/qr-menu/test/menu-view.spec.tsx && grep -qE "getByRole\('img'\)|toBeGreaterThan|basePrice|12\.50" apps/qr-menu/test/menu-view.spec.tsx && pnpm nx typecheck qr-menu && pnpm nx test qr-menu</automated>
  </verify>
  <acceptance_criteria>
    - api/types.ts deleted; all src AND test imports point at @resto/api-client/public; zero 'api/types' references remain.
    - qr-menu depends on @resto/api-client and @resto/cart (workspace:*).
    - api/client.ts tenant-header logic unchanged.
    - QRM-02 render verified: menu-view.spec.tsx (re-pointed) still passes AND now asserts an item card renders its price and a photo `<img>` (categories + item card with photo + price), proving the display path survived the api/types.ts deletion.
    - `pnpm nx typecheck qr-menu` and `pnpm nx test qr-menu` pass.
  </acceptance_criteria>
  <done>qr-menu consumes the shared wire types with no duplicate, the existing menu display render still works (categories + item card with photo + price), and it is ready to inherit isStopListed.</done>
</task>

<task type="auto">
  <name>Task 2: Branded header (QRM-01) + accent injection + cart trigger prop</name>
  <files>apps/qr-menu/src/App.tsx, apps/qr-menu/src/components/MenuView.tsx, apps/qr-menu/src/styles.css</files>
  <action>
    QRM-01 branded header. In App.tsx, add a useEffect (after the existing effects) that, when the menu is loaded (state.kind === 'ready') and `state.menu.brand?.theme?.primaryColor` is present, calls `document.documentElement.style.setProperty('--resto-accent', state.menu.brand.theme.primaryColor)`. Depend on state.
    In MenuView.tsx, render a `<header className="menu__header">` at the top of the menu content: when `menu.brand` is present, show `menu.brand.theme?.logoUrl` as an `<img className="menu__logo" alt={menu.brand.displayName}>` (only if logoUrl truthy) and `menu.brand.displayName` as the heading; fall back to the existing `t('menu.title')` only when brand is absent. Keep the existing category/section loop verbatim (the menu-view render test from Task 1 must continue to pass — the level-1 heading + category/item assertions still hold).
    Add an `onOpenCart: () => void` prop to MenuView Props and render a cart trigger button (plain `<button className="cart-trigger">`) that calls it — the drawer itself ships in plan 04; for now App.tsx passes a no-op or local open-state stub that plan 04 will replace. (Document in the SUMMARY that the trigger is wired to a stub pending plan 04.) Note: adding a required onOpenCart prop will require the menu-view.spec.tsx render calls to pass it (or make it optional) — keep the test green.
    Add `.menu__header`, `.menu__logo`, `.menu__brand`, `.cart-trigger` classes to styles.css using var(--resto-*) tokens only (no hardcoded hex). Brand text is rendered as React text children (auto-escaped) — never via innerHTML (XSS mitigation T-06-05).
    No code comments.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -q "menu__header" apps/qr-menu/src/components/MenuView.tsx && grep -q "menu__header" apps/qr-menu/src/styles.css && grep -q "setProperty('--resto-accent'" apps/qr-menu/src/App.tsx && grep -q "onOpenCart" apps/qr-menu/src/components/MenuView.tsx && pnpm nx typecheck qr-menu && pnpm nx test qr-menu</automated>
  </verify>
  <acceptance_criteria>
    - Branded header renders logo (when present) + displayName; accent color injected as --resto-accent.
    - MenuView exposes onOpenCart; cart-trigger button present.
    - New CSS classes use --resto-* tokens; no innerHTML used for brand/menu text.
    - menu-view.spec.tsx still passes (existing display assertions intact after header addition).
  </acceptance_criteria>
  <done>The menu shows a branded header themed by the tenant's primary color, with a cart trigger placeholder, and the existing render test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: ItemDetail modifiers + live price + add-to-cart (QRM-03/04/05)</name>
  <files>apps/qr-menu/src/App.tsx, apps/qr-menu/src/components/ItemDetail.tsx, apps/qr-menu/src/styles.css</files>
  <action>
    In App.tsx item route render, pre-resolve the item's modifier groups and pass them as a prop:
    `const groups = state.menu.modifierGroups.filter((g) => item.modifierGroupIds.includes(g.id));`
    then `return <ItemDetail item={item} groups={groups} onBack={navigateToMenu} />;` (filter+verify guards against stale modifierGroupIds — Pitfall 4).
    Extend ItemDetail (logic reference: apps/website/components/menu/item-modal.tsx, plain HTML + qr-menu classes — NO shadcn Dialog/Sheet):
    - Props gains `readonly groups: readonly MenuModifierGroupDto[];`.
    - State: selectedSizeId (default = item.sizes.find(s => s.isDefault)?.id ?? item.sizes[0]?.id ?? null) and selectedOptions: Map<groupId, Set<optionId>>.
    - Render each group with its options; respect minSelectable/maxSelectable (single-select group with maxSelectable===1 behaves as radio, otherwise checkbox-style toggle). Implement toggleOption mirroring item-modal.tsx (pure logic).
    - livePrice via useMemo: base = parseMinorUnits(effective size price or item.basePrice); for each group, for each chosen option add parseMinorUnits(opt.priceDelta); return formatMinorUnits(base). Import parseMinorUnits/formatMinorUnits from @resto/cart (decimal-safe — do NOT use parseFloat). Display the live price.
    - Render description/allergens/photo from the existing item fields (QRM-03) using localized() for localized text; keep the existing ItemDetail scaffold (`<main className="item">`, onBack).
    - handleAddToCart: build CartModifier[] from chosen options (optionId, name: localized(opt.name), priceDelta: opt.priceDelta, amount: 1), then `useCartStore.getState().addItem({ itemId: item.id, sizeId: effectiveSizeId, name: localized(item.name), unitPrice: livePrice, currency: item.currency, modifiers })`, then onBack(). Wire it to an "Add to cart" button.
    Add `.item-modifiers`, `.item-modifier-option`, `.item-live-price`, `.item-add-btn` classes to styles.css (var(--resto-*) tokens). Add i18n keys used (e.g. item.addToCart) to en.json/ru.json if missing — full switcher build is plan 04, but any new t() keys referenced here must exist.
    No code comments.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -q "groups:" apps/qr-menu/src/components/ItemDetail.tsx && grep -q "parseMinorUnits" apps/qr-menu/src/components/ItemDetail.tsx && grep -q "useCartStore" apps/qr-menu/src/components/ItemDetail.tsx && grep -q "modifierGroupIds.includes" apps/qr-menu/src/App.tsx && pnpm nx typecheck qr-menu && pnpm nx test qr-menu</automated>
  </verify>
  <acceptance_criteria>
    - App passes pre-resolved, verified groups to ItemDetail.
    - ItemDetail renders modifier groups, computes live price with @resto/cart minor-units helpers, and adds a configured CartLineItem to the store.
    - New CSS + any referenced i18n keys exist; `pnpm nx typecheck qr-menu` + `pnpm nx test qr-menu` pass.
  </acceptance_criteria>
  <done>A guest can open an item, pick modifiers, see the price update live, and add it to the shared cart.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                        |
| ------------------------------ | ---------------------------------------------------------------------------------- |
| /v1/menu response → DOM render | Menu text (names, descriptions, brand) is server-provided and rendered into the UI |
| guest selections → @resto/cart | Modifier/size selections become a client-side CartLineItem                         |

## STRIDE Threat Register

| Threat ID | Category        | Component                          | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | --------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-05   | Tampering (XSS) | branded header + item text render  | mitigate    | All menu/brand text rendered as React text children (auto-escaped); brand.theme.logoUrl used only as img src; no dangerouslySetInnerHTML anywhere             |
| T-06-06   | Spoofing        | tenant resolution in api/client.ts | accept      | X-Tenant-Slug derives from VITE_TENANT_SLUG (trusted build-time env, baked per deploy), never from guest input — type-import swap leaves this logic untouched |
| T-06-07   | Tampering       | live price computed client-side    | accept      | Price is display-only; Phase 7 re-prices server-side from /v1/menu — a tampered livePrice never becomes the charged amount                                    |

</threat_model>

<verification>
- `pnpm nx typecheck qr-menu` + `pnpm nx test qr-menu` pass.
- No '../api/types' references remain (src or test); all menu types come from @resto/api-client/public.
- QRM-02 display render verified: menu-view.spec.tsx (re-pointed) asserts categories + item card with photo + price after the api/types.ts deletion.
- Branded header renders; ItemDetail shows modifiers, live price, and add-to-cart wired to @resto/cart.
</verification>

<success_criteria>

- QRM-01 branded header, QRM-02 display render verified intact post type-swap, QRM-03 item detail w/ modifier groups, QRM-04 live price, QRM-05 add-to-cart all functional in qr-menu.
- qr-menu uses the shared wire types and shared cart store exclusively.
  </success_criteria>

<output>
Create `.planning/phases/06-qr-menu-customer/06-03-SUMMARY.md` when done. Note the cart-trigger stub pending plan 04.
</output>
