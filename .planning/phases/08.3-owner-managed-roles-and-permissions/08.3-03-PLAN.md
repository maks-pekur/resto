---
phase: 08.3-owner-managed-roles-and-permissions
plan: 03
type: execute
wave: 3
depends_on: ['08.3-01', '08.3-02']
files_modified:
  - apps/api/src/contexts/identity/application/assign-role.service.ts
  - apps/api/src/contexts/identity/application/list-members.service.ts
  - apps/api/src/contexts/identity/application/effective-permissions.ts
  - apps/api/src/contexts/identity/application/preset-roles.ts
  - apps/api/src/contexts/identity/application/seed-preset-roles.service.ts
  - apps/api/src/contexts/identity/application/dto.ts
  - apps/api/src/contexts/identity/interfaces/http/member-roles.controller.ts
  - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts
  - apps/api/src/contexts/identity/identity-http.module.ts
  - apps/api/src/contexts/tenancy/application/provision-tenant.service.ts
autonomous: true
requirements: [RBAC-04, RBAC-06]
user_setup: []

must_haves:
  truths:
    - "Owner can assign a custom role to a member via POST /v1/roles/:roleSlug/assign; effective permissions reflect it on the member's next request (D-02, SC#2)"
    - 'An assigner cannot assign a role whose effective permissions are NOT a subset of their own effective set (assignment-subset guard, D-06)'
    - 'An assigner cannot assign a role bearing a NON_DELEGATABLE permission, even the owner (D-04 on the assign path)'
    - 'A member cannot assign a role to themselves (self-escalation / self-assignment blocked) (D-06)'
    - 'The assignment payload accepts exactly one role slug (array rejected); a system-role assignment is rejected (D-05, Pitfall 3)'
    - "beforeUpdateMemberRole hook is a definitive backstop: it resolves the target role's permission set and throws ForbiddenException if containsNonDelegatable matches, rejecting a direct BA update-member-role call that bypasses assign-role.service (T-083-17); afterUpdateMemberRole now records actorUserId (D-16)"
    - 'Provisioning a new tenant seeds exactly 3 preset custom roles (Manager, Cashier-FoH, Kitchen) into organization_role (D-07)'
  artifacts:
    - path: 'apps/api/src/contexts/identity/application/assign-role.service.ts'
      provides: 'assignment-subset + NON_DELEGATABLE + self-escalation guard, then auth.api.updateMemberRole (D-02/D-06)'
    - path: 'apps/api/src/contexts/identity/application/effective-permissions.ts'
      provides: 'actor effective-set computation (system role ∪ custom role) reused by the guard'
    - path: 'apps/api/src/contexts/identity/application/preset-roles.ts'
      provides: 'the 3 preset permission sets (Manager/Cashier-FoH/Kitchen)'
      contains: 'manager'
    - path: 'apps/api/src/contexts/identity/interfaces/http/member-roles.controller.ts'
      provides: 'POST /v1/roles/:roleSlug/assign + GET /v1/roles/members (assignment surface, D-10)'
      exports: ['MemberRolesController']
  key_links:
    - from: 'assign-role.service.ts'
      to: 'effective-permissions.ts + containsNonDelegatable'
      via: 'subset check target ⊆ actor, and non-delegatable reject, before updateMemberRole'
      pattern: 'isSubsetOf|containsNonDelegatable'
    - from: 'provision-tenant.service.ts'
      to: 'seed-preset-roles.service.ts'
      via: 'seed 3 preset roles at tenant creation'
      pattern: 'seedPresetRoles|SeedPresetRoles'
    - from: 'auth.config.ts'
      to: 'beforeUpdateMemberRole hook'
      via: 'defense-in-depth backstop + actorUserId capture in afterUpdateMemberRole'
      pattern: 'beforeUpdateMemberRole'
---

<objective>
Close the privilege-escalation gap better-auth leaves open on the ASSIGNMENT path (CTO C-1, verified: BA's `updateMemberRole` gates only on `{ member: ['update'] }` with zero permission-subset check) and ship the minimal assignment surface so a role can actually be attached to a human end-to-end (D-10 — without it the feature dead-ends). Also seed the 3 preset roles at tenant provisioning (D-07) so the owner's first experience is "duplicate & tweak" rather than a blank 20-checkbox grid.

The assignment-subset + NON_DELEGATABLE + self-escalation guard lives primarily in the RestOS `assign-role.service` (the guaranteed path), with `beforeUpdateMemberRole` as a defense-in-depth backstop.

Purpose: Owner assigns roles safely; no actor can assign a role stronger than they hold or escalate themselves; presets make the feature usable on day one.
Output: assign-role + list-members + effective-permissions + preset definitions + seed service + MemberRolesController + beforeUpdateMemberRole hook + provisioning seed wire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-CONTEXT.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-01-SUMMARY.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified contracts. -->

BA assignment API (via roleApi bridge from plan 02):
updateMemberRole({ body: { memberId: string, role: string, organizationId }, headers }) → unknown
NOTE (C-1, verified): BA checks only { member: ['update'] } + creator-role protection; NO subset check. RestOS owns it.
NOTE (Pitfall 4): BA blocks sole-owner self-demotion natively (YOU_CANNOT_LEAVE_THE_ORGANIZATION_WITHOUT_AN_OWNER) — inherited for free.

BA hook signatures (types.d.mts:506/520):
beforeUpdateMemberRole(data: { member, newRole: string, user, organization }) → void | { data: { role } }
afterUpdateMemberRole(data) — already wired at auth.config.ts:232-258 emitting identity.role_changed.v1 (missing actorUserId — fix here)

Permission checker (reuse): PERMISSION_CHECKER = Symbol('PermissionChecker'); hasPermission(principal, required: Permission, headers) → boolean
Domain: PERMISSIONS_STATEMENT, SYSTEM_ROLES, NON_DELEGATABLE, containsNonDelegatable (all @resto/domain, plan 01).
Errors from plan 02: InsufficientPermissionsToMintError, RoleNotFoundError. Add new: SelfRoleAssignmentError, AssignmentExceedsAuthorityError (this plan).
roleApi bridge: apps/api/.../better-auth/role-api.bridge.ts (plan 02).
Provisioning: apps/api/src/contexts/tenancy/application/provision-tenant.service.ts (creation hook; add preset seeding step).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: effective-permissions helper + assign-role service (subset + NON_DELEGATABLE + self-escalation guard)</name>
  <read_first>
    - apps/api/src/contexts/identity/application/bootstrap-owner.service.ts (analog — service shape, AUTH_TOKEN/AUTH_DRIZZLE inject, buildEnvelope)
    - apps/api/src/contexts/identity/infrastructure/better-auth/permission-checker.adapter.ts (lines 36-58 — hasPermission call shape; effective-set probing)
    - apps/api/src/contexts/identity/application/ports/permission-checker.port.ts (PERMISSION_CHECKER token + hasPermission signature)
    - apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts (plan 02 — roleApi.listOrgRoles/getOrgRole/updateMemberRole)
    - apps/api/src/contexts/identity/domain/errors.ts (plan 02 — extend with SelfRoleAssignmentError, AssignmentExceedsAuthorityError)
    - apps/api/src/contexts/identity/interfaces/http/error-mapping.ts (plan 02 — map the two new errors)
    - packages/domain/src/rbac/non-delegatable.ts + system-roles.ts (plan 01)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md → section "assign-role.service.ts"
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-06 (subset algorithm + effective-set computation), Example 5, Pitfall 3, Pitfall 4
  </read_first>
  <behavior>
    - assign-role rejects with AssignmentExceedsAuthorityError when target role has a (resource, action) the actor's effective set lacks
    - assign-role rejects with InsufficientPermissionsToMintError when the target role contains any NON_DELEGATABLE pair
    - assign-role rejects with SelfRoleAssignmentError when actorUserId maps to the target memberId (self-assignment)
    - assign-role rejects an array-valued role (single slug only) and rejects assigning a system-role slug (owner/admin/staff) — D-05, Pitfall 3
    - assign-role rejects RoleNotFoundError for an unknown slug or an archived role
    - On success it calls roleApi.updateMemberRole exactly once with the ALS organizationId
    - effective-permissions computes actor's set = union of SYSTEM_ROLES[baseRole] and the actor's custom role permission (read from organization_role); owner resolves to the full owner set
  </behavior>
  <action>
    Create `apps/api/src/contexts/identity/application/effective-permissions.ts`: a pure-ish helper `computeEffectivePermissions(memberRoleCsv, customRoleLookup)` and `isSubsetOf(target, actorEffective)` returning whether every (resource, action) in `target` exists in `actorEffective`. Effective set = union of system-role perms from `SYSTEM_ROLES` for each system slug in the CSV, plus the `permission` of any custom role slug (resolved via the caller-supplied lookup that reads `organization_role`). Prefer the in-process approach from RESEARCH §D-06 ("Alternative (cheaper)") over N `hasPermission` round-trips.

    Extend `apps/api/src/contexts/identity/domain/errors.ts` with `SelfRoleAssignmentError()` (`'role.self_assignment'`) and `AssignmentExceedsAuthorityError()` (`'role.assignment_exceeds_authority'`), and map both in `error-mapping.ts` → `ForbiddenException`.

    Create `assign-role.service.ts` (`AssignRoleService`, `.execute({ organizationId, actorUserId, targetMemberId, roleSlug, headers })`): (1) reject array `roleSlug` and reject system-role slugs (assignment of system roles is out of scope — D-05, Pitfall 3); (2) `SelfRoleAssignmentError` if the actor's own member maps to `targetMemberId`; (3) resolve the target role's permission via `roleApi.listOrgRoles`/`getOrgRole` — `RoleNotFoundError` if missing or archived; (4) `containsNonDelegatable(targetPerms)` → `InsufficientPermissionsToMintError`; (5) compute actor effective set (effective-permissions helper) and `isSubsetOf(targetPerms, actorEffective)` — else `AssignmentExceedsAuthorityError`; (6) call `roleApi(auth).updateMemberRole({ body: { memberId: targetMemberId, role: roleSlug, organizationId }, headers })`. The existing `afterUpdateMemberRole` hook emits `identity.role_changed.v1` — do not double-emit here.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/unit/identity --run 2>&1 | grep -iE "assign|subset|effective|escalat" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Unit spec: assigning a role with `{ billing: ['update'] }` throws `InsufficientPermissionsToMintError`; updateMemberRole not called
    - Unit spec: an actor whose effective set lacks `menu:delete` assigning a role that has `menu:delete` throws `AssignmentExceedsAuthorityError`; updateMemberRole not called
    - Unit spec: actor assigning to their own member throws `SelfRoleAssignmentError`
    - Unit spec: array-valued roleSlug rejected; system-role slug ('staff') rejected
    - Unit spec: valid subset assignment calls `roleApi.updateMemberRole` exactly once with the ALS organizationId
    - `isSubsetOf` unit test: `{ menu: ['read'] } ⊆ { menu: ['read','update'], order: ['read'] }` true; `{ menu: ['delete'] } ⊆ { menu: ['read'] }` false
    - identity unit suite exits 0
  </acceptance_criteria>
  <done>Assignment path enforces subset + NON_DELEGATABLE + self-escalation before touching BA; effective-set helper unit-covered.</done>
</task>

<task type="auto">
  <name>Task 2: MemberRolesController + list-members service + beforeUpdateMemberRole hook + actorUserId in audit</name>
  <read_first>
    - apps/api/src/contexts/payments/interfaces/http/refunds.controller.ts (analog — owner-only @Permissions + wrapWith)
    - apps/api/src/contexts/identity/application/list-my-brands.service.ts (analog for list-members.service.ts)
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (lines 28-31 AfterUpdateMemberRoleHook type; lines 231-260 the existing hook — add beforeUpdateMemberRole sibling + capture actorUserId in afterUpdateMemberRole)
    - apps/api/src/contexts/identity/identity-http.module.ts (file being modified — register controller + services)
    - apps/api/src/contexts/identity/application/dto.ts (add AssignRoleInputDto)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md → sections "member-roles.controller.ts", "list-members.service.ts", "auth.config.ts" (Change 2 + Change 3)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-10, §D-16 (actorUserId note), Example 3
  </read_first>
  <action>
    Add `AssignRoleInputDto` to `dto.ts`: `{ memberId: z.string().uuid(), role: z.string().min(1).max(64) }` (single string, NOT array — Pitfall 3). Derive type via `z.infer`.

    Create `list-members.service.ts` (`ListMembersService`): returns active members for the ALS-resolved organization with `{ id, email, role }` for the assignment surface (D-10). Read via `AuthDrizzle` (member ⨝ user) or BA `listMembers`, filtered to the ALS organizationId. Include each member's current role slug for the picker's current-value display.

    Create `member-roles.controller.ts` (`MemberRolesController`, `@ApiTags('identity')`, `@BrandNeutral()`, `@Controller('v1/roles')`): `@Post(':roleSlug/assign')` `@RequiresTenantContext()` `@Permissions({ ac: ['update'] })` `@HttpCode(200)` assignRole → `wrap(() => assignRoleService.execute({ organizationId: <ALS>, actorUserId: operator.userId, targetMemberId: input.memberId, roleSlug: input.role, headers }))`; and `@Get('members')` `@Permissions({ ac: ['read'] })` listMembers. organizationId ALWAYS from `requireTenantContext()`.

    In `auth.config.ts`: add the `beforeUpdateMemberRole` hook alongside the existing `afterUpdateMemberRole` as a DEFINITIVE defense-in-depth backstop against a direct call to BA's `/organization/update-member-role` that bypasses the RestOS `assign-role.service` (threat T-083-17). The hook MUST NOT be a no-op: it MUST (1) resolve the target role's permission set from `newRole` — look up the custom role's `permission` via `organization_role` (AuthDrizzle / `roleApi.getOrgRole`) for a custom slug, or `SYSTEM_ROLES[newRole]` for a system slug; (2) call `containsNonDelegatable(targetPermission)`; (3) if it returns true, `throw new ForbiddenException({ code: 'role.insufficient_permissions', message })` to REJECT the mutation BEFORE BA writes it. Return void ONLY when the target permission set contains no NON_DELEGATABLE pair. (The primary subset + self-escalation guard remains in `assign-role.service`; this hook is the backstop that ALSO fires when someone calls BA's endpoint directly.) Add the `BeforeUpdateMemberRoleHook` / `BeforeUpdateMemberRoleData` types mirroring the existing `AfterUpdateMemberRoleHook` types (lines 28-31). Update the existing `afterUpdateMemberRole` emit to populate `actorUserId` on `IdentityRoleChangedV1` (currently omitted — RESEARCH §D-16), capturing the acting session user (same WeakMap-stash pattern used for sign-out, or the actor available in the hook/session context).

    Wire `MemberRolesController`, `AssignRoleService`, `ListMembersService` into `identity-http.module.ts` (controllers + providers).

  </action>
  <verify>
    <automated>pnpm --filter @resto/api exec tsc --noEmit -p tsconfig.json 2>&1 | tail -10 && pnpm --filter @resto/api build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `member-roles.controller.ts` declares `@Post(':roleSlug/assign')` and `@Get('members')`, both `@Permissions({ ac: [...] })`, `@BrandNeutral()`, ALS-pinned
    - `auth.config.ts` contains a `beforeUpdateMemberRole` key inside `organizationHooks` and a `BeforeUpdateMemberRoleData` type
    - Unit spec: `beforeUpdateMemberRole` REJECTS (throws `ForbiddenException`, code `role.insufficient_permissions`) when `newRole` resolves to a permission set containing a NON_DELEGATABLE pair (e.g. a slug whose perms include `billing:['update']` or `ac:['create']`); it returns void for a delegatable target role. The assertion proves the hook actually rejects the payload, NOT merely that the `beforeUpdateMemberRole` key exists in `organizationHooks`.
    - `afterUpdateMemberRole` emit now includes `actorUserId` in the IdentityRoleChangedV1 payload
    - `identity-http.module.ts` registers MemberRolesController + AssignRoleService + ListMembersService
    - `tsc --noEmit` exits 0; `pnpm --filter @resto/api build` exits 0
  </acceptance_criteria>
  <done>Assignment endpoint + members list live and wired; beforeUpdateMemberRole backstop present; assignment audit now records who acted.</done>
</task>

<task type="auto">
  <name>Task 3: Seed 3 preset roles at tenant provisioning (D-07)</name>
  <read_first>
    - apps/api/src/contexts/tenancy/application/provision-tenant.service.ts (file being modified — add a preset-seed step after member creation)
    - apps/api/src/contexts/identity/application/create-role.service.ts (plan 02 — reuse the same auth.api.createOrgRole path / roleApi bridge for seeding)
    - apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts (plan 02)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-07 (preset permission table), Open Question #2 (seed at provisioning — recommended)
    - packages/domain/src/rbac/permissions.ts (plan 01 — valid resource/action vocabulary for the presets)
    - packages/domain/CLAUDE.md → RBAC rules (presets are custom tenant roles, NOT system roles; must not use reserved slugs)
  </read_first>
  <action>
    Create `apps/api/src/contexts/identity/application/preset-roles.ts` exporting the 3 preset definitions with RU/EN display names and permission sets from RESEARCH §D-07:
    - Manager / Менеджер: `menu:['read','create','update','delete']`, `order:['read','update-status']`, `staff:['invite']`, `reports:['read']`, `brand:['read','update']`, `settings:['update']`.
    - Cashier-FoH / Кассир-зал: `order:['read','update-status']`, `menu:['read']`, `brand:['read']`.
    - Kitchen / Кухня: `order:['read','update-status']`, `brand:['read']`.
    None contain any NON_DELEGATABLE pair (invariant — assert in a unit test using `containsNonDelegatable`).

    Create `seed-preset-roles.service.ts` (`SeedPresetRolesService.execute({ organizationId, headers })`): iterate the 3 presets and call `roleApi(auth).createOrgRole` for each (they are tenant-owned custom roles in `organization_role`, not system roles). Idempotent: skip a preset whose slug already exists for the org (catch BA's name-taken error or pre-check via listOrgRoles).

    Wire into `provision-tenant.service.ts`: after the owner member is created, invoke `SeedPresetRolesService.execute({ organizationId: <new tenant id>, headers })`. This runs server-side within provisioning (no client involvement). If seeding fails, log at warn and continue — presets are convenience, not a provisioning blocker (the owner can still author roles). Register `SeedPresetRolesService` in whichever module provides ProvisionTenantService's dependencies (tenancy module imports identity where needed — follow existing cross-module provider wiring).

  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/unit --run 2>&1 | grep -iE "preset|seed|provision" | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `preset-roles.ts` exports 3 presets; unit test asserts `containsNonDelegatable(preset.permission) === false` for all three
    - Unit test: `SeedPresetRolesService.execute` issues 3 `createOrgRole` calls for a fresh org; issues 0 for presets already present (idempotent)
    - `provision-tenant.service.ts` invokes preset seeding after owner-member creation
    - Preset slugs are not reserved system slugs (owner/admin/staff)
    - `pnpm --filter @resto/api test apps/api/test/unit --run` exits 0
  </acceptance_criteria>
  <done>New tenants provision with Manager/Cashier-FoH/Kitchen preset roles; seeding is idempotent and non-blocking; presets carry no non-delegatable perms.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| browser → MemberRolesController /assign            | Untrusted memberId + role slug cross here; actor identity from authenticated session        |
| assign-role.service → better-auth updateMemberRole | Subset + NON_DELEGATABLE + self-escalation gate BEFORE the BA call (BA has no subset check) |
| provisioning → organization_role                   | Preset roles minted server-side, never from client input                                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                               |
| --------- | ---------------------- | --------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-083-13  | Elevation of Privilege | Assigning a role stronger than the assigner holds (BA `updateMemberRole` has no subset check — C-1) | mitigate    | assignment-subset guard `isSubsetOf(target, actorEffective)` in assign-role.service before updateMemberRole (D-06, Task 1); e2e in plan 05                                                                                                    |
| T-083-14  | Elevation of Privilege | Self-escalation — actor assigns themselves a stronger role                                          | mitigate    | SelfRoleAssignmentError blocks any self-target; BA sole-owner lockout backstops (D-06, Pitfall 4, Task 1)                                                                                                                                     |
| T-083-15  | Elevation of Privilege | Assigning a role bearing a NON_DELEGATABLE perm                                                     | mitigate    | containsNonDelegatable on target role in assign path + beforeUpdateMemberRole backstop (D-04, Task 1/2)                                                                                                                                       |
| T-083-16  | Elevation of Privilege | Multi-role CSV / array smuggling to widen perms (Pitfall 3)                                         | mitigate    | assign accepts single string only; system-role slugs rejected; D-05 one-role-per-member enforced at the wrapper (Task 1)                                                                                                                      |
| T-083-17  | Elevation of Privilege | Direct call to BA `/organization/update-member-role` bypassing RestOS guard                         | mitigate    | beforeUpdateMemberRole hook DEFINITIVELY resolves the target role's perms and throws ForbiddenException on `containsNonDelegatable` (not a no-op); BA endpoint not exposed (D-14 from plan 02); hook-rejection asserted by unit spec (Task 2) |
| T-083-18  | Repudiation            | Assignment audit missing who acted                                                                  | mitigate    | afterUpdateMemberRole emit now populates actorUserId (D-16, Task 2)                                                                                                                                                                           |

</threat_model>

<verification>
- Assignment-subset, NON_DELEGATABLE-on-assign, and self-escalation all unit-proven to short-circuit before `updateMemberRole`.
- Single-role-only + system-role-rejection enforced.
- Preset seeding idempotent, non-blocking, carries no non-delegatable perms.
- beforeUpdateMemberRole present; assignment audit records actorUserId.
- `tsc --noEmit` + `pnpm --filter @resto/api build` green.
</verification>

<success_criteria>

- ROADMAP SC#2: owner assigns a custom role to a staff member; effective permissions reflect it on next request (server side complete; UI in plan 04; live e2e in plan 05).
- ROADMAP SC#3 substrate: the creator-subset/assigner-subset guard is server-enforced (proof e2e in plan 05).
- D-02, D-04 (assign side), D-05, D-06, D-07, D-10 (server side), D-16 (assignment actor) discharged.
  </success_criteria>

<output>
Create `.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-03-SUMMARY.md` when done.
</output>
