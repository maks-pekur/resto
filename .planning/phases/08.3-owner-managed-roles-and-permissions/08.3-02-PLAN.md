---
phase: 08.3-owner-managed-roles-and-permissions
plan: 02
type: execute
wave: 2
depends_on: ["08.3-01"]
files_modified:
  - packages/db/migrations/0062_identity_roles_archived_at.sql
  - apps/api/src/contexts/identity/domain/errors.ts
  - apps/api/src/contexts/identity/application/dto.ts
  - apps/api/src/contexts/identity/application/create-role.service.ts
  - apps/api/src/contexts/identity/application/update-role.service.ts
  - apps/api/src/contexts/identity/application/archive-role.service.ts
  - apps/api/src/contexts/identity/application/list-roles.service.ts
  - apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts
  - apps/api/src/contexts/identity/interfaces/http/roles.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts
  - apps/api/src/contexts/identity/identity-http.module.ts
autonomous: true
requirements: [RBAC-02, RBAC-03, RBAC-07, RBAC-08, RBAC-13]
user_setup: []

must_haves:
  truths:
    - "Owner can create a tenant-scoped custom role via POST /v1/roles; a non-owner gets 403 (D-01, SC#1)"
    - "A create/update proposing any NON_DELEGATABLE permission is rejected with 403 before reaching better-auth (D-04)"
    - "organizationId passed to better-auth is always resolved from ALS (getTenantContext), never from the request body (D-14, cross-tenant pinning)"
    - "Archiving a role that has >=1 assigned member is blocked with a RoleOccupiedError; an empty role sets archived_at instead of hard-deleting (D-12)"
    - "listRoles excludes archived roles (archived_at IS NOT NULL filtered out) (D-12)"
    - "AuthGuard.lookupBaseRole resolves 'staff' from CSV 'staff,gm' and undefined from a custom-only slug (D-15)"
    - "Role-definition create/update/archive emit identity.role_created.v1 / role_permissions_changed.v1 / role_deleted.v1 via the outbox (D-16)"
    - "Reserved system slugs (owner/admin/staff) and per-tenant duplicate names are rejected (D-09)"
  artifacts:
    - path: "apps/api/src/contexts/identity/interfaces/http/roles.controller.ts"
      provides: "D-14 wrapper: POST/PUT/DELETE/GET /v1/roles, owner-only via @Permissions({ac:[...]})"
      exports: ["RolesController"]
    - path: "apps/api/src/contexts/identity/application/create-role.service.ts"
      provides: "NON_DELEGATABLE guard + auth.api.createOrgRole + role_created audit"
    - path: "apps/api/src/contexts/identity/application/archive-role.service.ts"
      provides: "occupied-check + soft-archive via archived_at + role_deleted audit (D-12)"
    - path: "packages/db/migrations/0062_identity_roles_archived_at.sql"
      provides: "archived_at column on organization_role (D-12)"
      contains: "archived_at"
    - path: "apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts"
      provides: "typed RolePluginApi bridge (no as-casts in service bodies)"
      exports: ["roleApi"]
  key_links:
    - from: "roles.controller.ts"
      to: "getTenantContext (ALS)"
      via: "requireTenantContext() → TenantId.parse(ctx.tenantId) — organizationId never from body"
      pattern: "requireTenantContext"
    - from: "create-role.service.ts / update-role.service.ts"
      to: "containsNonDelegatable"
      via: "reject before auth.api.createOrgRole/updateOrgRole"
      pattern: "containsNonDelegatable"
    - from: "archive-role.service.ts"
      to: "organization_role.archived_at"
      via: "AuthDrizzle UPDATE after member-count guard"
      pattern: "archived_at"
---

<objective>
Build the RestOS server-side wrapper around better-auth's role-CRUD endpoints (D-14) — the single leverage point for the NON_DELEGATABLE filter (D-04), owner-only authoring (D-01), soft-delete (D-12), role-definition audit (D-16), and cross-tenant `organizationId` pinning. Also add the `archived_at` migration and fix `AuthGuard.lookupBaseRole` for custom-role members (D-15).

better-auth's native role endpoints (`/api/auth/organization/*-role`) MUST NOT be reachable by the browser; every role mutation flows through `RolesController → *-role.service → auth.api.*`.

Purpose: Owner can create/edit/list/archive custom roles safely; every path is server-guarded and audited.
Output: 4 application services, a typed BA bridge, the RolesController, migration 0062, error classes + mappings, the lookupBaseRole fix, and module wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-CONTEXT.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md
@.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified contracts the executor uses directly. -->

BA server API (auth.api.* — RESEARCH §D-14, call via typed bridge, never a raw `as` in method bodies):
  createOrgRole({ body: { organizationId, role: string, permission: Record<string,string[]> }, headers }) → { role?: string }
  updateOrgRole({ body: { organizationId, roleName?|roleId?, data: { permission?, roleName? } }, headers }) → unknown
  deleteOrgRole({ body: { organizationId, roleName?|roleId? }, headers }) → unknown   // NOT used — D-12 soft-archive replaces it
  listOrgRoles({ query: { organizationId }, headers }) → { roles: Array<{ id, role, permission }> }

DI tokens (apps/api/.../identity/identity.tokens.ts):
  AUTH_TOKEN = Symbol('Auth')            // inject Auth
  AUTH_DRIZZLE_TOKEN = Symbol('AuthDrizzle')  // inject AuthDrizzle for direct member/role queries
Event emitter (application/ports/identity-event-emitter.port.ts):
  IDENTITY_EVENT_EMITTER = Symbol('IDENTITY_EVENT_EMITTER'); interface { emit(envelope): Promise<void> }

Controller building blocks (verified present):
  @resto/db: requireTenantContext(), getTenantContext()
  shared/auth: @BrandNeutral(), @Permissions(spec), @RequiresTenantContext()
  shared/api/wrap: wrapWith(mapper)
  toWebHeaders(raw): Headers — helper in auth.guard.ts:168 (duplicate or export it)
  CurrentOperator decorator → OperatorPrincipal { userId, email, tenantId?, baseRole? }

AuthGuard.lookupBaseRole (auth.guard.ts:153-165) CURRENT — returns undefined for CSV/custom slugs. Fix per D-15.

organization_role schema: packages/db/src/schema/auth.ts:68-77 (TEXT permission JSON; NO archived_at yet).
Latest migration on disk: 0061 → new file is 0062.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0062 (archived_at) + errors + typed BA bridge + lookupBaseRole fix (D-12/D-15/D-09)</name>
  <read_first>
    - packages/db/migrations/0059_session_active_brand.sql (analog — simple ALTER TABLE ADD COLUMN)
    - packages/db/src/schema/auth.ts (lines 68-77 — organization_role schema; add archived_at column def to mirror the migration)
    - apps/api/src/contexts/identity/domain/errors.ts (file being modified — IdentityDomainError base + TenantMismatchError pattern)
    - apps/api/src/contexts/identity/interfaces/http/error-mapping.ts (file being modified — existing instanceof→HttpException mappings)
    - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts (lines 153-165 lookupBaseRole; lines 168-188 toWebHeaders)
    - apps/api/src/contexts/identity/infrastructure/better-auth/permission-checker.adapter.ts (lines 36-46 — the `auth.api as unknown as {...}` cast pattern to base the bridge on)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md → sections "0062_identity_roles_archived_at.sql", "errors.ts", "error-mapping.ts", "auth.guard.ts", "BA API Cast Pattern"
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-12 (Option A), §D-15, §D-09, Pitfall 5
  </read_first>
  <action>
    Create migration `packages/db/migrations/0062_identity_roles_archived_at.sql` with `ALTER TABLE "organization_role" ADD COLUMN IF NOT EXISTS "archived_at" timestamp with time zone;`. Add the matching `archivedAt` column to the Drizzle `organization_role` schema in `packages/db/src/schema/auth.ts` (nullable timestamp with timezone) so `AuthDrizzle` can read/write it type-safely. Do NOT add an RLS policy change — `resto_auth` already has `USING(true)` on organization_role via migration 0054; application-level filtering is the isolation per RESEARCH Pitfall 5.

    In `apps/api/src/contexts/identity/domain/errors.ts`: append four `IdentityDomainError` subclasses with explicit `this.name` (via `new.target.name` base): `RoleOccupiedError(roleSlug, memberCount)` code `'role.occupied'`; `RoleNotFoundError(roleSlug)` code `'role.not_found'`; `RoleNameReservedError(name)` code `'role.name_reserved'`; `InsufficientPermissionsToMintError()` code `'role.insufficient_permissions'`. Exact messages per PATTERNS errors.ts excerpt.

    In `apps/api/src/contexts/identity/interfaces/http/error-mapping.ts`: add `instanceof` mappings — `RoleOccupiedError` → `BadRequestException`, `RoleNotFoundError` → `NotFoundException`, `RoleNameReservedError` → `BadRequestException`, `InsufficientPermissionsToMintError` → `ForbiddenException` (each `{ code: err.code, message: err.message }`).

    Fix `AuthGuard.lookupBaseRole` (auth.guard.ts:153-165) per D-15: after fetching `member.role`, split on ',', trim parts, return the highest system role (`owner` > `admin` > `staff`) present, else `undefined` for a custom-only member. Do NOT change any other AuthGuard behavior — the `BrandScopeGuard` owner-bypass relies only on `baseRole === 'owner'`, which the fix preserves.

    Create NEW file `apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts` exporting `interface RolePluginApi` (createOrgRole/updateOrgRole/deleteOrgRole/listOrgRoles/updateMemberRole signatures from the interfaces block) and `const roleApi = (auth: Auth): RolePluginApi => auth.api as unknown as RolePluginApi;`. This localizes the single `as unknown` cast so service bodies stay cast-free (mirrors permission-checker.adapter.ts).
  </action>
  <verify>
    <automated>pnpm --filter @resto/db build && pnpm --filter @resto/api exec tsc --noEmit -p tsconfig.json 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/migrations/0062_identity_roles_archived_at.sql` exists and contains `archived_at`
    - `packages/db/src/schema/auth.ts` organization_role has an `archivedAt` column
    - `errors.ts` exports `RoleOccupiedError`, `RoleNotFoundError`, `RoleNameReservedError`, `InsufficientPermissionsToMintError` (grep confirms all four)
    - `error-mapping.ts` maps all four to the specified HttpException subclasses
    - `role-api.bridge.ts` exports `roleApi` and `RolePluginApi`
    - Unit test: `lookupBaseRole` on `'staff,gm'` → `'staff'`; on `'custom-manager'` → `undefined`; on `'admin'` → `'admin'` (add a focused unit spec for the private method behavior via a small guard test)
    - `tsc --noEmit` on apps/api exits 0
  </acceptance_criteria>
  <done>Migration + schema column landed; four error classes mapped; lookupBaseRole handles CSV/custom slugs; typed BA bridge exported; api typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create/update/archive/list-roles services + DTOs (NON_DELEGATABLE, name guard, soft-delete, audit)</name>
  <read_first>
    - apps/api/src/contexts/identity/application/bootstrap-owner.service.ts (primary analog — Injectable, execute(), AUTH_TOKEN inject, buildEnvelope emit, BA api cast)
    - apps/api/src/contexts/identity/application/list-my-brands.service.ts (analog for list-roles.service.ts)
    - apps/api/src/contexts/identity/infrastructure/better-auth/role-api.bridge.ts (created in Task 1 — use roleApi(auth))
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (lines 232-258 — audit emit pattern to mirror)
    - apps/api/src/contexts/identity/application/dto.ts (file being modified/created — add CreateRoleInputDto/UpdateRoleInputDto Zod DTOs)
    - packages/domain/src/rbac/non-delegatable.ts (containsNonDelegatable — from plan 01)
    - packages/domain/src/rbac/permissions.ts (PERMISSIONS_STATEMENT — validate proposed resource/action keys)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md → sections "create-role.service.ts", "update-role.service.ts", "archive-role.service.ts", "list-roles.service.ts", "Audit Event Emission", "BA API Cast Pattern"
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-04, §D-09 (RoleNameSchema), §D-12 (Option A), §D-16, Pitfall 1, Pitfall 2, Pitfall 3
  </read_first>
  <behavior>
    - create-role.service rejects with InsufficientPermissionsToMintError when permission contains any NON_DELEGATABLE pair (e.g. { billing: ['update'] } or { ac: ['create'] }) — BEFORE calling auth.api.createOrgRole
    - create-role.service rejects RoleNameReservedError for names normalizing to 'owner'/'admin'/'staff'
    - create-role.service validates every proposed (resource, action) against PERMISSIONS_STATEMENT; unknown keys rejected
    - create-role.service emits identity.role_created.v1 (with actorUserId, roleSlug, permission, tenantId) after a successful BA create; emit failure never blocks the primary response
    - update-role.service fetches previous permission (listOrgRoles/getOrgRole) then emits role_permissions_changed.v1 with previousPermission + newPermission; NON_DELEGATABLE check runs on the new set
    - archive-role.service throws RoleOccupiedError when member-count > 0; when 0, sets organization_role.archived_at = NOW() via AuthDrizzle (NOT auth.api.deleteOrgRole) and emits role_deleted.v1
    - list-roles.service returns only rows where archived_at IS NULL
  </behavior>
  <action>
    In `apps/api/src/contexts/identity/application/dto.ts`: add Zod DTOs `CreateRoleInputDto` (`roleName` via a `RoleNameSchema` = `z.string().min(1).max(64).regex(/^[a-zA-Z0-9\s\-_]+$/).transform(s => s.toLowerCase())` per D-09; `permission: z.record(z.string(), z.array(z.string()))`) and `UpdateRoleInputDto` (`permission` only; roleSlug comes from the path param). Derive types via `z.infer`.

    Create `create-role.service.ts` (`CreateRoleService`, Injectable, `.execute(input)` where input = `{ roleName, permission, organizationId, actorUserId, headers }`): (1) reject reserved names (`RoleNameReservedError` if normalized name ∈ {owner,admin,staff}); (2) validate proposed permission keys/actions against `PERMISSIONS_STATEMENT`; (3) `containsNonDelegatable(permission)` → throw `InsufficientPermissionsToMintError`; (4) call `roleApi(auth).createOrgRole({ body: { organizationId, role: roleName, permission }, headers })` — organizationId ALWAYS the ALS-resolved tenant; (5) emit `IdentityRoleCreatedV1` via `IDENTITY_EVENT_EMITTER` + `buildEnvelope`, wrapped so emit failure logs but never rethrows (mirror auth.config afterUpdateMemberRole). Note (Pitfall 1): the NON_DELEGATABLE check MUST run before the BA call, because after plan 01 adds `ac` to PERMISSIONS_STATEMENT, BA's own `checkForInvalidResources` would otherwise accept `{ ac: ['create'] }`.

    Create `update-role.service.ts` (`UpdateRoleService`): same guards on the new permission set; fetch the role's previous permission via `roleApi(auth).listOrgRoles`/`getOrgRole` before calling `updateOrgRole`; emit `IdentityRolePermissionsChangedV1` with `previousPermission` + `newPermission`. Throw `RoleNotFoundError` if the slug does not exist.

    Create `archive-role.service.ts` (`ArchiveRoleService`): inject `AUTH_DRIZZLE_TOKEN`. Count members holding the slug (query `member` table via AuthDrizzle, same pattern BA uses; `member.role` CSV contains the slug). If count > 0 → `RoleOccupiedError(roleSlug, count)`. If 0 → direct Drizzle `UPDATE organization_role SET archived_at = NOW() WHERE organization_id = <ALS tenant> AND role = <slug>`; emit `IdentityRoleDeletedV1`. Do NOT call `auth.api.deleteOrgRole` (it hard-deletes — forbidden per no-hard-delete rule; D-12 Option A: since no member holds an archived slug, BA's hasPermission never resolves it).

    Create `list-roles.service.ts` (`ListRolesService`): call `roleApi(auth).listOrgRoles({ query: { organizationId }, headers })`, filter out rows whose `archived_at` is set (join/read archived_at via AuthDrizzle if BA's list payload omits it), map to a `RoleView { id, role, permission }` shape, and include a `memberCount` per role where cheap (query member table) for the UI's "who's on this role" (Product M-2).
  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/unit/identity --run 2>&1 | grep -iE "role|create-role|archive|non-delegat" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Unit spec: `CreateRoleService.execute({ permission: { billing: ['update'] }, ... })` throws `InsufficientPermissionsToMintError` and `roleApi.createOrgRole` is NOT called (mock asserts zero calls)
    - Unit spec: `CreateRoleService.execute({ roleName: 'Owner', ... })` throws `RoleNameReservedError`
    - Unit spec: `CreateRoleService` emits `identity.role_created.v1` with the correct actorUserId on success (emitter mock asserts one call)
    - Unit spec: `ArchiveRoleService` with member-count=2 throws `RoleOccupiedError` and never issues the archived_at UPDATE; with count=0 issues the UPDATE and emits `identity.role_deleted.v1`
    - Unit spec: `ArchiveRoleService` never calls `auth.api.deleteOrgRole`
    - Unit spec: `ListRolesService` filters archived rows
    - `pnpm --filter @resto/api test apps/api/test/unit/identity --run` exits 0
  </acceptance_criteria>
  <done>Four role-CRUD services enforce NON_DELEGATABLE + reserved-name + soft-archive + audit, all with unit coverage; organizationId always from ALS.</done>
</task>

<task type="auto">
  <name>Task 3: RolesController (D-14 wrapper) + module wiring</name>
  <read_first>
    - apps/api/src/contexts/payments/interfaces/http/refunds.controller.ts (primary analog — owner-only @Permissions, requireTenantContext(), wrapWith)
    - apps/api/src/contexts/identity/interfaces/http/me-brands.controller.ts (secondary analog — identity context + BA, @BrandNeutral)
    - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts (lines 168-188 toWebHeaders helper)
    - apps/api/src/contexts/identity/identity-http.module.ts (file being modified — controllers/providers arrays; existing MeBrandsController/SetActiveBrandController wiring is the template)
    - apps/api/src/shared/auth/index.ts (BrandNeutral, Permissions, RequiresTenantContext exports)
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-PATTERNS.md → sections "roles.controller.ts", "Authentication + Tenant Cross-Check", "wrapWith Error Mapping"
    - .planning/phases/08.3-owner-managed-roles-and-permissions/08.3-RESEARCH.md → §D-14, Example 4, Pitfall 5
  </read_first>
  <action>
    Create `apps/api/src/contexts/identity/interfaces/http/roles.controller.ts` (`RolesController`, `@ApiTags('identity')`, `@BrandNeutral()`, `@Controller('v1/roles')`) with four handlers, each `@RequiresTenantContext()` and permission-gated per D-14:
    - `@Post()` `@Permissions({ ac: ['create'] })` `@HttpCode(201)` createRole — body `CreateRoleInputDto`.
    - `@Put(':roleSlug')` `@Permissions({ ac: ['update'] })` updateRole — path `roleSlug` + body `UpdateRoleInputDto`.
    - `@Delete(':roleSlug')` `@Permissions({ ac: ['delete'] })` `@HttpCode(204)` archiveRole.
    - `@Get()` `@Permissions({ ac: ['read'] })` listRoles.
    Each handler: resolve `const ctx = requireTenantContext(); const tenantId = TenantId.parse(ctx.tenantId);` (organizationId ALWAYS from ALS, never body — Pitfall 5), build `headers = toWebHeaders(req.headers)`, capture `operator.userId` via `@CurrentOperator()`, and return `wrap(() => service.execute({ organizationId: tenantId, actorUserId: operator.userId, headers, ...input }))` where `const wrap = wrapWith(mapIdentityError)`.

    Wire in `apps/api/src/contexts/identity/identity-http.module.ts`: import + register `RolesController` in `controllers`, and register `CreateRoleService`, `UpdateRoleService`, `ArchiveRoleService`, `ListRolesService` in `providers` (they inject `AUTH_TOKEN`, `AUTH_DRIZZLE_TOKEN`, `IDENTITY_EVENT_EMITTER` which are already provided in the identity module graph). Do NOT expose better-auth's `/api/auth/organization/*-role` routes to the browser — the RestOS controller is the only public role surface (D-14). Do NOT add the member-role assignment controller here — that is plan 03.
  </action>
  <verify>
    <automated>pnpm --filter @resto/api exec tsc --noEmit -p tsconfig.json 2>&1 | tail -10 && pnpm --filter @resto/api test apps/api/test/unit/identity --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `roles.controller.ts` declares `@Controller('v1/roles')` with `@BrandNeutral()` and the four `@Permissions({ ac: [...] })` handlers
    - No handler reads `organizationId` from the request body (grep: no `body.organizationId` / `input.organizationId` from client); each uses `requireTenantContext()`
    - `identity-http.module.ts` lists `RolesController` in controllers and the four services in providers
    - `tsc --noEmit` exits 0 and identity unit suite exits 0
    - App boots with the flag on: `pnpm --filter @resto/api build` exits 0 (module graph resolves)
  </acceptance_criteria>
  <done>RolesController exposes owner-only /v1/roles CRUD, ALS-pinned, wired into the identity HTTP module; BA role endpoints stay server-internal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RestOS RolesController | Untrusted role name + permission payload + (ignored) organizationId cross here |
| RolesController → better-auth auth.api.* | organizationId pinned from ALS; BA membership check is a second gate |
| archive path → organization_role table | Direct AuthDrizzle UPDATE (soft-delete) instead of BA hard-delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-083-06 | Elevation of Privilege | Minting a role bearing NON_DELEGATABLE perms (billing:update, ac:*, tenant:delete/transfer, staff:remove) even by owner | mitigate | `containsNonDelegatable` runs in create/update service BEFORE `auth.api.*` (Task 2); unit test asserts BA not called on rejection |
| T-083-07 | Elevation of Privilege | Direct browser access to `/api/auth/organization/*-role` bypassing RestOS guards | mitigate | D-14 wrapper is the only exposed surface; BA endpoints not proxied; controller is `@Permissions`-gated owner-only (Task 3) |
| T-083-08 | Tampering / Information Disclosure | Cross-tenant role read/write via client-supplied organizationId | mitigate | organizationId always `requireTenantContext()` from ALS, never body (Task 3, Pitfall 5); BA membership check backstops |
| T-083-09 | Spoofing | Custom role shadowing a system-role name (owner/admin/staff) | mitigate | RoleNameSchema + reserved-name guard in create service (D-09, Task 2); BA `checkIfRoleNameIsTakenByPreDefinedRole` backstops |
| T-083-10 | Tampering | Hard-delete of a role (violates no-hard-delete rule) or deleting an occupied role mid-shift | mitigate | archive-role sets archived_at via AuthDrizzle; occupied-role blocked with RoleOccupiedError; deleteOrgRole never called (D-12, Task 2) |
| T-083-11 | Elevation of Privilege | Custom-role member breaks BrandScopeGuard owner-bypass because lookupBaseRole returns undefined | mitigate | D-15 fix parses CSV → highest system role; owner keeps bypass, custom-only member falls through to brand-scope table (Task 1) |
| T-083-12 | Repudiation | Role create/update/archive leaves no audit record | mitigate | Services emit role_created/permissions_changed/deleted.v1 with actorUserId via outbox (D-16, Task 2) |
</threat_model>

<verification>
- `tsc --noEmit` on apps/api exits 0; identity unit suite green.
- NON_DELEGATABLE rejection proven to short-circuit before the BA call (mock asserts zero BA calls).
- Archive on occupied role blocked; empty role sets archived_at; deleteOrgRole never invoked.
- lookupBaseRole CSV behavior covered by a focused test.
- Migration 0062 present; organization_role schema has archivedAt.
</verification>

<success_criteria>
- ROADMAP SC#1: owner creates a tenant-scoped custom role via the admin-callable /v1/roles endpoint (server side complete; UI in plan 04).
- D-01, D-04, D-09, D-12, D-14, D-15, D-16 (create/update/archive side) discharged.
- better-auth role endpoints are never exposed to the browser; every role mutation is ALS-pinned, guarded, audited.
</success_criteria>

<output>
Create `.planning/phases/08.3-owner-managed-roles-and-permissions/08.3-02-SUMMARY.md` when done.
</output>
