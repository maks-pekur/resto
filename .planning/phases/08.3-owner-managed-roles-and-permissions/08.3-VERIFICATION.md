---
phase: 08.3-owner-managed-roles-and-permissions
verified: 2026-07-06T16:00:00Z
status: human_needed
score: 9/9 truths verified
overrides_applied: 0
human_verification:
  - test: 'Run all three e2e specs against the live dev stack (Docker must be running)'
    expected: 'roles-privilege-escalation 7/7 pass, roles-brand-scope-orthogonality 3/3 pass, roles-cross-tenant-isolation 5/5 pass'
    why_human: 'E2E specs are Docker-gated (describe.skip when Docker absent). The verifier environment may not have Docker running. Plan 05 reports all passing when run individually; re-running confirms no regression from CR-01/02/WR-01-04 fixes in c537d9d.'
---

# Phase 08.3: Owner-managed Roles & Permissions — Verification Report

**Phase Goal:** Enable owner-authored custom roles (dynamic RBAC) — the founder's headline access feature. Turn on better-auth `dynamicAccessControl`; build the creator-subset privilege-escalation guard on both the create and assign paths; build the owner-facing admin UI to create a role, toggle its permissions from the catalog, and assign it to staff.
**Verified:** 2026-07-06
**Status:** human_needed (all automated truths VERIFIED; one item needs live Docker e2e confirmation)
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                   | Status   | Evidence                                                                                                                                                                             |
| --- | --------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Owner creates a custom role in admin, selects permissions from catalog, saves it (SC#1) | VERIFIED | `roles.tsx` + `roles.$roleId.tsx` + `RoleForm` + `PermissionCatalog` wired to `POST /v1/roles`                                                                                       |
| 2   | Owner assigns a custom role to a staff member (SC#2)                                    | VERIFIED | `MemberRoleRow` in `team.tsx` calls `assignRole()` → `POST /v1/roles/:roleSlug/assign`                                                                                               |
| 3   | Creator-subset privilege escalation blocked server-side (SC#3/D-06)                     | VERIFIED | `containsNonDelegatable` + `isSubsetOf` in `create-role.service.ts` and `assign-role.service.ts`                                                                                     |
| 4   | `dynamicAccessControl` enabled without weakening fixed system roles (SC#4)              | VERIFIED | `auth.config.ts:226`: `dynamicAccessControl: { enabled: true, maximumRolesPerOrganization: 25 }`                                                                                     |
| 5   | NON_DELEGATABLE enforced on both create and assign paths (D-04)                         | VERIFIED | `non-delegatable.ts` lists `tenant:delete`, `billing:update`, `staff:remove`, `ac:*`; both services check                                                                            |
| 6   | Cross-tenant isolation: org-scoped, ALS-pinned organizationId (RBAC-14)                 | VERIFIED | All controller handlers derive `organizationId` from `requireTenantContext()` ALS only; no body-sourced org                                                                          |
| 7   | Brand-scope orthogonality: custom role does not widen brand reach (D-03/RBAC-09)        | VERIFIED | `BrandScopeGuard` still gates on `principal.baseRole === 'owner'`; `lookupBaseRole` CSV fix makes custom-role members fall through correctly (D-15)                                  |
| 8   | Soft-delete only — no hard deletes (D-12)                                               | VERIFIED | `archive-role.service.ts` uses `UPDATE … SET archived_at = now()`; `BA deleteOrgRole` never called; migration 0062 adds `archived_at` column                                         |
| 9   | Owner-only authoring and assignment (D-01/D-02)                                         | VERIFIED | `@Permissions({ ac: ['create'] })` on `POST /v1/roles`; `@Permissions({ ac: ['update'] })` on `POST /v1/roles/:roleSlug/assign`; admin does not hold `ac` resource in `SYSTEM_ROLES` |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact                                                                    | Expected                                                                                                            | Status   | Details                                                                                                                                                              |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/domain/src/rbac/non-delegatable.ts`                               | NON_DELEGATABLE constant + `containsNonDelegatable`                                                                 | VERIFIED | Defines `tenant:delete`, `billing:update`, `staff:remove`, `ac:*`                                                                                                    |
| `packages/domain/src/rbac/permissions.ts`                                   | `ac` resource added; colon actions renamed                                                                          | VERIFIED | `ac: ['create','read','update','delete']`; `roleCreate`/`roleUpdate` (no colons)                                                                                     |
| `packages/domain/src/rbac/system-roles.ts`                                  | `owner` has `ac`; `admin`/`staff` do not                                                                            | VERIFIED | Confirmed: only `owner` block carries `ac` resource                                                                                                                  |
| `packages/events/src/contracts/identity.ts`                                 | Three role-definition event contracts                                                                               | VERIFIED | `IdentityRoleCreatedV1`, `IdentityRolePermissionsChangedV1`, `IdentityRoleDeletedV1` exported from events barrel                                                     |
| `packages/db/migrations/0062_identity_roles_archived_at.sql`                | `archived_at` column on `organization_role`                                                                         | VERIFIED | `ALTER TABLE "organization_role" ADD COLUMN IF NOT EXISTS "archived_at" timestamp with time zone`                                                                    |
| `apps/api/src/contexts/identity/application/list-active-custom-roles.ts`    | Direct DB read with `archivedAt IS NULL`                                                                            | VERIFIED | Reads `organization_role` directly, parses JSON permission, filters `isNull(organizationRole.archivedAt)`                                                            |
| `apps/api/src/contexts/identity/application/create-role.service.ts`         | NON_DELEGATABLE guard before BA call; reserved slug check                                                           | VERIFIED | `containsNonDelegatable` runs before `roleApi.createOrgRole`; `RESERVED_SYSTEM_SLUGS` blocks `owner`/`admin`/`staff`                                                 |
| `apps/api/src/contexts/identity/application/update-role.service.ts`         | NON_DELEGATABLE guard; existence check                                                                              | VERIFIED | Uses `listActiveCustomRoles` for existence; `containsNonDelegatable` before BA call                                                                                  |
| `apps/api/src/contexts/identity/application/archive-role.service.ts`        | Existence check (CR-01 fix); system-slug protection (WR-04 fix); soft-archive                                       | VERIFIED | `listActiveCustomRoles` pre-check at line 32-35; throws `RoleNotFoundError` for missing slugs; `UPDATE archived_at` only                                             |
| `apps/api/src/contexts/identity/application/assign-role.service.ts`         | Subset guard + NON_DELEGATABLE + self-escalation                                                                    | VERIFIED | `isSubsetOf(targetPerms, actorEffective)`, `containsNonDelegatable(targetPerms)`, `actorMember.id === targetMemberId` check                                          |
| `apps/api/src/contexts/identity/application/effective-permissions.ts`       | `computeEffectivePermissions` + `isSubsetOf`                                                                        | VERIFIED | Handles system role CSV + custom role lookup; correct subset semantics                                                                                               |
| `apps/api/src/contexts/identity/application/preset-roles.ts`                | 3 preset definitions (Manager/Cashier-FoH/Kitchen)                                                                  | VERIFIED | No NON_DELEGATABLE permissions in any preset                                                                                                                         |
| `apps/api/src/contexts/identity/application/seed-preset-roles.service.ts`   | Idempotent preset seeding at provisioning                                                                           | VERIFIED | Checks existing slugs before INSERT; non-blocking try/catch; wired into `provision-tenant.service.ts`                                                                |
| `apps/api/src/contexts/identity/interfaces/http/roles.controller.ts`        | `POST`, `PUT`, `DELETE`, `GET`, `GET :roleSlug` under `@Permissions({ac:*})`                                        | VERIFIED | All 5 handlers present; `organizationId` from ALS only; WR-03 fix (GET :roleSlug) confirmed at line 147                                                              |
| `apps/api/src/contexts/identity/interfaces/http/member-roles.controller.ts` | `POST :roleSlug/assign` + `GET members`                                                                             | VERIFIED | Both handlers present; `@Permissions({ ac: ['update'] })` and `@Permissions({ ac: ['read'] })`                                                                       |
| `apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts`  | `dynamicAccessControl: enabled`, `beforeUpdateMemberRole` fail-closed hook, `archivedAt IS NULL` filter (WR-01 fix) | VERIFIED | Line 226 enables DAC; hook at line 248 fails closed on null permission (CR-02+WR-01 fix at line 268/283); system roles at lines 222-224 use `SYSTEM_ROLES`           |
| `apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts`       | `lookupBaseRole` CSV-split for custom-role members (D-15)                                                           | VERIFIED | Lines 164-170: splits `role` CSV, returns highest system role or `undefined` for custom-only members                                                                 |
| `apps/api/src/contexts/audit/application/record-audit.service.ts`           | `role` target kind in `ACTION_TARGET_KIND`                                                                          | VERIFIED | Lines 22-24: `identity.role_created`/`permissions_changed`/`deleted` all map to `'role'` target kind                                                                 |
| `apps/admin/src/lib/queries/roles.ts`                                       | Query bindings for all role operations                                                                              | VERIFIED | `rolesQuery`, `roleQuery`, `roleMembersQuery`, `createRole`, `updateRole`, `archiveRole`, `assignRole` all present                                                   |
| `apps/admin/src/components/roles/permission-catalog.tsx`                    | NON_DELEGATABLE hidden; derived from `@resto/domain`                                                                | VERIFIED | Imports `NON_DELEGATABLE` from `@resto/domain`; builds `HIDDEN_SET` from it; `ac` resource excluded; visible actions filtered via `isHidden()`                       |
| `apps/admin/src/routes/(protected)/$brandSlug/roles.tsx`                    | Roles list page; owner-gated                                                                                        | VERIFIED | `isOwner` check; falls back to `EmptyState variant="forbidden"` for non-owners                                                                                       |
| `apps/admin/src/routes/(protected)/$brandSlug/roles.$roleId.tsx`            | Role editor with `roleQuery` + `RoleForm`                                                                           | VERIFIED | Fetches `GET /v1/roles/:roleId` via `roleQuery`; passes `existingRole` to `RoleForm`                                                                                 |
| `apps/admin/src/routes/(protected)/$brandSlug/team.tsx`                     | Active-member assignment surface with `MemberRoleRow`                                                               | VERIFIED | Loads `roleMembersQuery` + `rolesQuery`; renders `MemberRoleRow` per member with `isOwner` prop                                                                      |
| `apps/api/test/e2e/roles-privilege-escalation.e2e.spec.ts`                  | 7 tests covering admin-create block, NON_DELEGATABLE block, assignment block                                        | VERIFIED | File exists; tests cover admin 403 (a), NON_DELEGATABLE billing/ac/tenant (b1/b2/b3), owner success (c), non-owner assign block (d1), system-slug assign reject (d2) |
| `apps/api/test/e2e/roles-brand-scope-orthogonality.e2e.spec.ts`             | 3 tests for D-03/RBAC-09                                                                                            | VERIFIED | File exists; tests in-scope brand write (200), out-of-scope existence-hiding (404), owner bypass (200)                                                               |
| `apps/api/test/e2e/roles-cross-tenant-isolation.e2e.spec.ts`                | 5 tests for RBAC-14 + routing check                                                                                 | VERIFIED | File exists; tests A cannot read B roles, A cannot modify B roles, A cannot archive B roles, B control, `/v1/roles/members` routing correctness                      |

---

### Key Link Verification

| From                          | To                             | Via                                                                    | Status | Details                                                             |
| ----------------------------- | ------------------------------ | ---------------------------------------------------------------------- | ------ | ------------------------------------------------------------------- |
| `roles.controller.ts`         | `create-role.service.ts`       | `@Inject(CreateRoleService)`                                           | WIRED  | Confirmed in constructor and `createRoleHandler`                    |
| `create-role.service.ts`      | `non-delegatable.ts`           | `containsNonDelegatable()` before BA call                              | WIRED  | Line 39 in `create-role.service.ts`                                 |
| `assign-role.service.ts`      | `effective-permissions.ts`     | `computeEffectivePermissions` + `isSubsetOf`                           | WIRED  | Lines 79-81 in `assign-role.service.ts`                             |
| `assign-role.service.ts`      | `list-active-custom-roles.ts`  | `listActiveCustomRoles(this.authDb, input.organizationId)`             | WIRED  | Line 60, with `archivedAt IS NULL` filter                           |
| `archive-role.service.ts`     | `list-active-custom-roles.ts`  | existence pre-check                                                    | WIRED  | Lines 32-35 (CR-01 fix)                                             |
| `auth.config.ts`              | `non-delegatable.ts`           | `containsNonDelegatable(targetPermission)` in `beforeUpdateMemberRole` | WIRED  | Line 290; with fail-closed null-check at line 283 (CR-02+WR-01 fix) |
| `provision-tenant.service.ts` | `seed-preset-roles.service.ts` | `this.seedPresets.execute({ organizationId })`                         | WIRED  | Non-blocking try/catch; D-07                                        |
| `permission-catalog.tsx`      | `@resto/domain`                | `import { PERMISSIONS_STATEMENT, NON_DELEGATABLE }`                    | WIRED  | Imported at line 3; hidden set derived at build time                |
| `team.tsx`                    | `member-roles.controller.ts`   | `assignRole()` → `POST /v1/roles/:roleSlug/assign`                     | WIRED  | `MemberRoleRow` calls `assignRole`; controller route confirmed      |
| `roles.$roleId.tsx`           | `roles.controller.ts`          | `roleQuery(roleId)` → `GET /v1/roles/:roleSlug`                        | WIRED  | `GET :roleSlug` handler added at line 147 (WR-03 fix)               |
| `roles.controller.ts`         | ALS tenant context             | `requireTenantContext()` in every handler                              | WIRED  | No client-body `organizationId` in any handler                      |

---

### Data-Flow Trace (Level 4)

| Artifact                 | Data Variable            | Source                                                                             | Produces Real Data                              | Status  |
| ------------------------ | ------------------------ | ---------------------------------------------------------------------------------- | ----------------------------------------------- | ------- |
| `roles.tsx`              | `roles`                  | `rolesQuery()` → `GET /v1/roles` → `listActiveCustomRoles(authDb, organizationId)` | Yes — direct DB query with `archivedAt IS NULL` | FLOWING |
| `team.tsx`               | `activeMembers`          | `roleMembersQuery()` → `GET /v1/roles/members` → `list-members.service.ts` DB join | Yes — member + user tables joined               | FLOWING |
| `roles.$roleId.tsx`      | `existingRole`           | `roleQuery(roleId)` → `GET /v1/roles/:roleSlug` → same `listActiveCustomRoles`     | Yes — real DB data, 404 on missing              | FLOWING |
| `permission-catalog.tsx` | `value` (permission map) | Prop from `RoleForm`; loaded from `roleQuery` or preset selection                  | Yes — data from server or preset definition     | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for e2e tests (require Docker/running stack). The three e2e spec files exist and are structurally complete (not stubs). They are covered under Human Verification Required below.

The following static checks pass:

| Behavior                                             | Check                                                           | Result                                                                         | Status |
| ---------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------ |
| `dynamicAccessControl` enabled                       | `grep "enabled: true" auth.config.ts`                           | Found at line 226                                                              | PASS   |
| No client-body `organizationId` in controllers       | `grep body.*organizationId` in roles/member-roles controllers   | No matches (all use ALS)                                                       | PASS   |
| CR-01 fix present                                    | `listActiveCustomRoles` call in `archive-role.service.ts:32`    | Found                                                                          | PASS   |
| CR-02 fix present                                    | null-check throw in `beforeUpdateMemberRole` hook               | Found at lines 283-288                                                         | PASS   |
| WR-01 fix present                                    | `isNull(organizationRoleTable.archivedAt)` in hook WHERE clause | Found at line 268                                                              | PASS   |
| WR-03 fix present                                    | `@Get(':roleSlug')` handler in `RolesController`                | Found at line 147                                                              | PASS   |
| WR-02 fix present                                    | `PermissionSchema` bounds in `dto.ts`                           | `z.string().max(64)` on keys/values, `.max(64)` on array, `.max(64)` resources | PASS   |
| No colon actions in PERMISSIONS_STATEMENT            | `grep "role:create\|role:update"`                               | No matches (`roleCreate`/`roleUpdate` camelCase used)                          | PASS   |
| Admin does not hold `ac` resource                    | `system-roles.ts` admin block                                   | No `ac` key in `admin` or `staff`                                              | PASS   |
| `beforeUpdateMemberRole` fail-closed on unknown slug | Lines 283-288 throw `ForbiddenException`                        | Confirmed                                                                      | PASS   |

---

### Probe Execution

No probe scripts defined for this phase. Step 7c: SKIPPED.

---

### Requirements Coverage

| Requirement    | Source Plan   | Description                                                   | Status    | Evidence                                                                                                                     |
| -------------- | ------------- | ------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------- |
| SC#1           | 08.3-04       | Owner creates custom role in admin                            | SATISFIED | `roles.tsx` + role editor route + `POST /v1/roles` controller wired                                                          |
| SC#2           | 08.3-04       | Owner assigns custom role to staff member                     | SATISFIED | `team.tsx` `MemberRoleRow` + `POST /v1/roles/:roleSlug/assign`                                                               |
| SC#3           | 08.3-03/05    | Creator-subset guard server-side with test                    | SATISFIED | `assign-role.service.ts` `isSubsetOf` + e2e `roles-privilege-escalation.e2e.spec.ts`                                         |
| SC#4           | 08.3-01       | `dynamicAccessControl` enabled without weakening system roles | SATISFIED | `auth.config.ts:226` enabled; `SYSTEM_ROLES` immutable                                                                       |
| D-04           | 08.3-01/02/03 | NON_DELEGATABLE on create + assign paths                      | SATISFIED | Both `create-role.service.ts` and `assign-role.service.ts` call `containsNonDelegatable` before BA; hook also checks         |
| D-12           | 08.3-02       | Soft-delete only (no hard delete)                             | SATISFIED | `archived_at` migration 0062; `UPDATE` instead of `deleteOrgRole`; occupation check blocks occupied roles                    |
| D-13           | 08.3-01       | `ac` resource added; `dynamicAccessControl` flag flipped      | SATISFIED | `permissions.ts:11`, `auth.config.ts:226`                                                                                    |
| D-14           | 08.3-02       | RestOS wrapper controllers for BA role endpoints              | SATISFIED | `RolesController` and `MemberRolesController` are the only exposed surfaces; BA endpoints not proxied                        |
| D-15           | 08.3-02       | `lookupBaseRole` CSV-split fix                                | SATISFIED | `auth.guard.ts:164-170`                                                                                                      |
| D-16           | 08.3-01/02    | Role-definition audit events                                  | SATISFIED | Three event contracts + `ACTION_TARGET_KIND['identity.role_created'] = 'role'` etc. emitted from all three mutation services |
| D-17           | 08.3-01       | Role cap per tenant                                           | SATISFIED | `maximumRolesPerOrganization: 25` at `auth.config.ts:226`                                                                    |
| RBAC-09 / D-03 | 08.3-05       | Brand-scope orthogonality proven by test                      | SATISFIED | `roles-brand-scope-orthogonality.e2e.spec.ts` tests in-scope 200, out-of-scope 404, owner bypass 200                         |
| RBAC-14        | 08.3-05       | Cross-tenant isolation proven by test                         | SATISFIED | `roles-cross-tenant-isolation.e2e.spec.ts` 5 tests; ALS-pinned `organizationId`                                              |

---

### Anti-Patterns Found

No blockers. All phase-introduced files are clean:

| File                                                                         | Pattern                                                   | Severity | Impact                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------- | --------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/contexts/identity/interfaces/http/guards/permissions.guard.ts` | Cookie header not collapsed (IN-01 deferred)              | INFO     | Could cause session-lookup failures behind a multi-value-cookie proxy on `PermissionsGuard`; `AuthGuard`'s `toWebHeaders` does collapse it. Deferred from code review as non-blocking. No functional impact in dev/current infra. |
| `apps/api/src/contexts/identity/application/seed-preset-roles.service.ts`    | Reads archived slugs in existence check (IN-02 deferred)  | INFO     | If owner archives a preset slug, re-provisioning will not re-seed it. Probably intentional; documented as deferred in code review.                                                                                                |
| `apps/admin/src/lib/queries/roles.ts:62-66`                                  | Duplicate `if (status === 403)` branches (IN-03 deferred) | INFO     | Dead code path; both branches return identical string. UI behavior unaffected.                                                                                                                                                    |
| `apps/api/src/contexts/identity/interfaces/http/error-mapping.ts`            | `role.cap_reached` BA error not mapped (IN-04 deferred)   | INFO     | BA's cap-exceeded response will not surface as the expected `400 { code: 'role.cap_reached' }`; admin friendly message for this case will never display. Deferred as non-blocking.                                                |

No `TBD`, `FIXME`, or `XXX` markers found in any phase-modified file.

---

### Human Verification Required

#### 1. Live E2E Suite Confirmation

**Test:** Run each of the three e2e specs individually against the running dev stack:

```
pnpm vitest run apps/api/test/e2e/roles-privilege-escalation.e2e.spec.ts
pnpm vitest run apps/api/test/e2e/roles-brand-scope-orthogonality.e2e.spec.ts
pnpm vitest run apps/api/test/e2e/roles-cross-tenant-isolation.e2e.spec.ts
```

(Run individually, not as a suite batch — the apps/api full-suite false-429 gotcha documented in project memory applies.)

**Expected:** privilege-escalation 7/7, brand-scope-orthogonality 3/3, cross-tenant-isolation 5/5. The only known pre-existing red is `identity-boot-integration.spec.ts:104` (`assertProdGuardrails` staging+resend) — unrelated to this phase.

**Why human:** E2E specs are Docker-gated (`describe.skip` when `isDockerAvailable()` returns false). The verifier environment cannot confirm Docker availability. Plan 05 SUMMARY reports 7/7, 3/3, 4/4 (now 5/5 after the additional routing test in c537d9d); this needs live confirmation against the post-CR-fix codebase.

---

### Gaps Summary

No actionable gaps. All success criteria and security invariants verified against the live codebase:

- SC#1 (create-from-catalog): fully wired — admin UI → `POST /v1/roles` → `containsNonDelegatable` + BA `createOrgRole`.
- SC#2 (assignment): fully wired — `team.tsx` assignment picker → `POST /v1/roles/:slug/assign` → `isSubsetOf` + NON_DELEGATABLE + self-escalation check.
- SC#3 (privilege escalation blocked): enforced on BOTH paths. CR-01 (phantom archive audit) and CR-02 (fail-open hook) both fixed in commit c537d9d and verified in code.
- SC#4 (dynamicAccessControl enabled without weakening system roles): confirmed at `auth.config.ts:226`; `SYSTEM_ROLES` unchanged.
- Security invariants D-03/D-04/D-12/D-13/D-14/D-15/D-16/D-17 all discharged with code evidence.
- Four info-level deferred items (IN-01 through IN-04) are non-blocking. The four deferred 4-info items from the code review are correctly scoped as follow-up work.

The only open item is live e2e execution against the running stack (requires Docker).

---

_Verified: 2026-07-06_
_Verifier: Claude (gsd-verifier)_
