# Phase 08.3: Owner-managed Roles & Permissions - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 08.3-owner-managed-roles-and-permissions
**Areas discussed:** Кто управляет, Область роли, Что делегируем, Жизнь роли
**Persona reviewers:** persona-cto, persona-skeptic, persona-product-strategist (findings in `08.3-PERSONA-REVIEWS.md`)

---

## Кто управляет (authoring authority)

| Option | Description | Selected |
|--------|-------------|----------|
| Только владелец | Owner-only authoring; matches "owner-managed"; simplifies creator-subset; admin gets it later (Phase 17) | ✓ |
| Владелец + админ | Admin also creates roles, bounded by their own perms; adds surface/complexity early | |

**User's choice:** Только владелец (реком.)
**Notes:** CTO/Skeptic/Product all favored owner-only; collapses the create-side subset check to near-trivial. → CONTEXT D-01.

---

## Область роли (role ↔ brand scope)

| Option | Description | Selected |
|--------|-------------|----------|
| Роль на сеть, бренды — отдельно | Role = tenant-wide permission set; brand access stays 08.2's orthogonal mechanism; must be explicitly reconciled | ✓ |
| Роль привязана к бренду | "Manager of brand X"; new data model, not native to better-auth; costlier/riskier | |

**User's choice:** Роль на сеть, бренды — отдельно (реком.)
**Notes:** Matches better-auth org-scoping + 08.2 brand-scope. Skeptic CRITICAL-2: custom roles currently bypass `BrandScopeGuard` — reconciliation is mandatory. Per-brand roles noted as closer to Phase 08.4. → CONTEXT D-03.

---

## Что делегируем (delegation surface + multiplicity + assignment authority)

| Option | Description | Selected |
|--------|-------------|----------|
| Нет, опасные — только владелец | NON_DELEGATABLE allowlist (tenant:delete/transfer, billing:update, role-mgmt, staff:remove) never enters a custom role | ✓ |
| Чистый creator-subset без исключений | No special exclusions; simpler code, more dangerous | |
| — Число ролей: Одна роль | System XOR custom, one per member; avoids CSV base-role ambiguity | ✓ |
| — Число ролей: Можно несколько | Composition; flexible but harder + BA CSV bug (H-1) | |
| — Кто назначает: Только владелец | Closes CTO C-1 (assignment escalation); + subset/self-escalation guard still built | ✓ |
| — Кто назначает: Владелец + админ | Convenient delegation, opens attack surface earlier | |

**User's choice:** опасные — только владелец + одна роль + назначает только владелец (все реком.)
**Notes:** Non-delegatable allowlist matches CTO C-3 + prior AUDIT.md. Assignment authority ties to CTO C-1 (BA's updateMemberRole lacks a subset check). → CONTEXT D-02, D-04, D-05, D-06.

---

## Жизнь роли (UX shape + lifecycle + assignment surface)

| Option | Description | Selected |
|--------|-------------|----------|
| Сначала готовые пресеты | 2–3 seeded presets (Менеджер/Кассир-зал/Кухня); custom = "duplicate & tweak" | ✓ |
| Сразу пустой конструктор | Blank role + 20+ checkbox grid; max flexibility, confusing enterprise UX | |
| — Назначение: минимальный экран в 08.3 | Member list + role picker so SC#2 works end-to-end; full Team page stays Phase 17 | ✓ |
| — Назначение: только создание ролей | Authoring-only; SC#2 deferred; feature can't be shown end-to-end | |
| — Удаление занятой роли: нельзя, пока назначена | Reassign first, then archive (soft-delete); no silent access change | ✓ |
| — Удаление: носители → staff | Auto-demote to base staff; convenient but silently changes access | |

**User's choice:** пресеты-first + минимальный экран назначения + удаление заблокировано пока назначена (все реком.)
**Notes:** Presets-first per Product H-2 (owners think in job titles). Minimal assignment surface per Product H-1 (today team.tsx is invite-only). Block-until-reassigned aligns with the project's no-hard-delete + no-silent-access-change posture. → CONTEXT D-07, D-08, D-10, D-11, D-12.

---

## Claude's Discretion

- Exact preset permission sets (Manager / Cashier-FoH / Kitchen) — derive from SYSTEM_ROLES + PERMISSIONS_STATEMENT.
- Exact `ac` resource shape + BA re-seed mechanism (`pnpm db:rbac:sync`).
- Keep BA per-request role resolution vs add caching; `cacheAllRoles` invalidation contract.
- better-auth 1.4.22 `dynamicAccessControl` cross-tenant isolation spike (tie to the deferred BA 1.4→1.6 CVE decision).

## Deferred Ideas

- Full team-management page (invitations, revoke, bulk role-change, 2FA subordinate reset) → Phase 17 (TEAM-01..05).
- Per-brand / per-location custom roles → Phase 08.4 (location-scoped access).
- Multi-role composition per member → post-MVP.
- Admin-authored / admin-assigned roles → Phase 17 (guards built now so it's a policy flip later).
- AI-authored roles (NL → permission set) → MVP-2 AI surface (the actual "headline"; 08.3 is the manual substrate).

## Meta

- **Sequencing flag** raised by all 3 personas (08.3 ahead of Phase 10 / PROJECT.md's old "custom RBAC post-MVP" note). Founder kept the roadmap order and constrained scope to keep it lean. Recorded, not re-litigated.
- **Platform prerequisites** (BA `ac` resource + re-seed, wrapper controllers, base-role fix, role audit, role cap) recorded as locked CONTEXT decisions D-13..D-17, not gray areas — reviewers verified them against BA 1.4.22 source.
