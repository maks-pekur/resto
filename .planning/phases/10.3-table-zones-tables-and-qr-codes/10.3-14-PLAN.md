---
phase: 10.3-table-zones-tables-and-qr-codes
plan: 14
type: execute
wave: 8
depends_on: [10.3-10, 10.3-11, 10.3-12, 10.3-13]
files_modified:
  - docs/api/openapi.yaml
  - packages/api-client/src/generated/api.ts
autonomous: false
requirements: [TBL-06, TBL-08, TBL-09, TBL-10]

must_haves:
  truths:
    - 'pnpm openapi:check passes with no drift after regeneration'
    - 'The published create-order schema no longer carries the free-text table field and does carry tableId'
    - 'The published feed and detail schemas carry tableZoneName and tableNumber'
    - 'The public table resolution route and the admin table-zone routes are in the committed artifact'
    - 'A code printed from the real sheet, scanned with a real phone, opens the menu showing the right table'
    - 'An unknown id scanned from a hand-made code opens the menu showing the not-recognised state'
  artifacts:
    - path: 'docs/api/openapi.yaml'
      provides: 'The regenerated published contract'
      contains: 'tableId'
    - path: 'packages/api-client/src/generated/api.ts'
      provides: 'The regenerated typed client'
      contains: 'tableId'
  key_links:
    - from: 'docs/api/openapi.yaml'
      to: 'packages/api-client/src/generated/api.ts'
      via: 'openapi-typescript codegen, both regenerated in the same pass by pnpm openapi:check'
      pattern: 'tableZoneName'
---

<objective>
Regenerate the two committed API artifacts so the phase's contract changes are published, then put one real printed code in front of a phone before the phase closes.

Purpose: removing the free-text `table` field from `CreateOrderInputSchema` is a change to a published artifact gated by CI, not an internal refactor — `docs/api/openapi.yaml:3543` carries the field and `.github/workflows/ci.yml:135` runs an `openapi-drift` job. Regeneration is part of the change; the drift job going red between plan 10.3-09 and this one is the expected signal, not a surprise. And this phase ships a print-and-glue surface for a URL shape no client has yet walked end to end, on a database that phase 10.2 D-12 still declares resettable — the founder accepted that risk knowingly, and the mitigation they named costs ten minutes.
Output: two regenerated artifacts and one human-verified scan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-SPEC.md
@.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-CONTEXT.md
@.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-RESEARCH.md
@.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-09-SUMMARY.md
@.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-11-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. -->

`package.json:21` — `"openapi:check": "tsx tools/openapi-check.ts"`.

`tools/openapi-check.ts` — runs the api package's `openapi:emit` (writing `docs/api/openapi.yaml`) and then the api-client codegen (writing `packages/api-client/src/generated/api.ts`), then asks git whether the working tree drifted from the committed artifacts and exits non-zero if so. It injects placeholder `DATABASE_URL` / `NATS_URL` / `S3_*` env vars because `AppModule` wiring needs them before `NestFactory` can render the document; no real service is contacted.

`packages/api-client/package.json:16` — `"gen": "openapi-typescript ../../docs/api/openapi.yaml -o ./src/generated/api.ts"`.

Surfaces added or changed in this phase that must appear in the regenerated artifact:

- `GET /v1/tables/:id` (plan 10.3-08)
- The seven `/v1/tenancy/table-zones` routes (plan 10.3-07)
- `POST /v1/orders`: `table` removed, `tableId` added (plan 10.3-09)
- `GET /v1/menu/availability`: the optional `t` query parameter (plan 10.3-12)
- The operator feed and detail responses: `tableZoneName` and `tableNumber` (plan 10.3-13)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the published contract and the typed client</name>
  <read_first>
    - `tools/openapi-check.ts` — the whole file; what it runs, in what order, and the env block it injects
    - `packages/api-client/package.json` — the `gen` script
    - `.github/workflows/ci.yml` around line 135 — the `openapi-drift` job, so the local command matches what CI will run
    - `docs/api/openapi.yaml` around line 3543 — the `table` field this change removes, so the diff can be read deliberately rather than accepted blind
  </read_first>
  <files>docs/api/openapi.yaml, packages/api-client/src/generated/api.ts</files>
  <action>
Run `pnpm openapi:check` from the repo root. It regenerates both artifacts and then fails on drift, so the first run is expected to report a diff — that diff IS the work. Commit the regenerated files and re-run until it exits 0.

Read the diff before accepting it, rather than treating it as machine output. Confirm each of these, one by one, and record the confirmation in the SUMMARY:

- The create-order request schema no longer has `table` and now has `tableId` as an optional UUID.
- The `dine_in` requirement description reflects the new rule.
- `GET /v1/tables/{id}` appears with a three-field 200 response and a 404, and its response schema contains no `locationId`.
- The seven `/v1/tenancy/table-zones` routes appear with their permission-relevant responses.
- `GET /v1/menu/availability` gained the optional `t` query parameter.
- The feed row and order detail schemas gained `tableZoneName` and `tableNumber` and kept `tableIdentifier`.

If the diff contains anything beyond those six groups, stop and investigate — an unexpected schema change in a regenerated artifact is how an accidental contract break ships unnoticed.

Then confirm nothing downstream broke on the regenerated client: `pnpm nx run-many -t typecheck` and `pnpm nx run-many -t build` across the workspace, since `apps/website` and `apps/qr-menu` both consume `@resto/api-client`.
</action>
<verify>
<automated>pnpm openapi:check</automated>
</verify>
<acceptance_criteria>
  - `pnpm openapi:check` exits 0 on a clean working tree after the regenerated artifacts are committed.
  - `grep -c "tableId" docs/api/openapi.yaml` returns at least `1` and the create-order request schema's property list contains no bare `table` — confirm by reading the schema block, not just by grepping the whole file.
  - `grep -c "tableZoneName" docs/api/openapi.yaml` returns at least `2`.
  - `grep -c "/v1/tables/{id}" docs/api/openapi.yaml` returns `1`.
  - `grep -c "table-zones" docs/api/openapi.yaml` returns at least `4`.
  - `pnpm nx run-many -t typecheck` and `pnpm nx run-many -t build` both exit 0.
  - The SUMMARY lists the six diff groups and states that nothing outside them appeared.
</acceptance_criteria>
<done>pnpm openapi:check exits 0 on a clean tree, the diff matches the six expected groups and nothing else, and the workspace typechecks and builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Scan a real printed code, and a dead one</name>
  <read_first>
    - `.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-11-SUMMARY.md` — how to reach the tables screen and trigger the downloads
    - `.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-10-SUMMARY.md` — what the guest app renders in each of the three table states
    - `apps/api/src/config/env.schema.ts` around `PUBLIC_APEX_DOMAIN` — what the dev host must be for a scanned URL to resolve from a phone
  </read_first>
  <action>
Do not automate this task. Walk the eight steps below on a real phone against the dev stack, then report the outcome of each one separately. This is the only check in the phase that crosses from a generated PDF to a camera, and it is the mitigation CONTEXT D-28 names for shipping a print-and-glue surface ahead of the checkout phase. If any step misbehaves, record what happened and reopen the owning plan rather than closing the phase.
  </action>
  <what-built>
    The whole phase: zones and tables with their CRUD, the A4 sheet and the single SVG, the public resolution route, the order's table reference and its location, and the operator's table label. Everything above this line is verified by automated tests. This step verifies the one thing no test can: that a code drawn by the PDF generator, printed on paper, and photographed by a phone camera actually opens the right menu showing the right table.

    CONTEXT D-28 records this as the accepted-risk mitigation. This phase ships a print-and-glue surface for a URL shape no client has yet walked end to end, on a database that 10.2 D-12 still declares resettable — so the founder should see one code work before anything is glued to furniture.

  </what-built>
  <how-to-verify>
    1. Start the dev stack (`pnpm dev:up`) and the api, and make sure the guest host for your dev tenant is reachable from your phone on the same network. Note in the resume signal what host you used.
    2. In admin, open `/{locationSlug}/tables`, create a zone with 8 tables, and download the sheet.
    3. Open the PDF. Confirm: 8 codes across 2 pages, six on the first; the table numbers appear only in the gutter outside the cut guides; and the codes are in ascending order left to right then top to bottom.
    4. Print page 1. Cut one code out along its guide.
    5. Scan the cut code with a phone camera. Expected: the guest menu opens and the strip shows that table's zone and number — the same numbers that were in the gutter of the square you cut.
    6. Download the single SVG for a different table and open it in any image viewer. Confirm it is vector (scales without pixellation) and scans to that table's own menu.
    7. Archive that table in admin, then scan its code again. Expected: the menu still opens, and the strip shows the "table not recognised" line instead of a number.
    8. Edit the scanned URL by hand to `?table=99` and open it. Expected: the menu opens with no table strip at all.
    Expected outcome: steps 5, 6, 7 and 8 all behave as described. Report anything that does not.
  </how-to-verify>
  <acceptance_criteria>
    - A human reports the outcome of steps 3, 5, 6, 7 and 8 individually — not a single "looks good".
    - The number read from the gutter in step 4 matches the label rendered in step 5.
    - Step 7 shows the not-recognised state and an HTTP 200 menu, not an error page.
    - Step 8 shows no table strip.
    - If any step fails, the phase does not close; the failure is recorded and the owning plan is reopened.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the host you used, or describe which step misbehaved and what you saw.</resume-signal>
  <done>A human reported the outcome of each of the five scan steps individually and every one behaved as described.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                            |
| --------------------------------------- | -------------------------------------------------------------------------------------- |
| generated artifact → published contract | `docs/api/openapi.yaml` is what external consumers and the typed client are built from |
| printed vinyl → guest phone             | The one boundary in this phase that no automated test can cross                        |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                  |
| --------- | ----------------- | ---------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-10.3-59 | Tampering         | An unintended schema change riding along in a regenerated artifact     | mitigate    | The diff is read against six named expected groups; anything outside them stops the task                                                                                                                                                         |
| T-10.3-60 | Spoofing          | A printed code encoding the wrong host for a custom-domain tenant      | mitigate    | The URL is server-built (`GuestMenuUrlService`); the human scan is the end-to-end confirmation that the shape the server produces is the shape a phone resolves                                                                                  |
| T-10.3-61 | Repudiation       | Stickers glued against table ids from a database that later gets reset | accept      | Phase 10.2 D-12 still allows a dev-database reset while no real tenant data exists. Recorded, not fixed here; the ROADMAP entry for the follow-up checkout phase carries the note that the allowance is void once any tenant has printed a sheet |
| T-10.3-62 | Denial of Service | A code that cannot be scanned once hand-cut                            | mitigate    | The cut guide sits outside the quiet zone and the geometry is computed from the real matrix size; step 4 of the human check cuts a code and step 5 scans it                                                                                      |
| T-10.3-SC | Tampering         | npm/pip/cargo installs                                                 | mitigate    | No package installs in this plan                                                                                                                                                                                                                 |

</threat_model>

<verification>
- `pnpm openapi:check` exits 0 on a clean tree
- `pnpm nx run-many -t typecheck` and `pnpm nx run-many -t build`
- `pnpm nx run-many -t test` and `pnpm nx run-many -t e2e` as the phase-wide regression sweep
- A human-reported result for each of the five scan steps
</verification>

<success_criteria>

- Both committed artifacts match the code, and the drift gate is green
- The published create-order schema carries `tableId` and no free-text `table`
- Every new route and field appears in the artifact, and nothing unexpected does
- A printed, cut code scans to the right table; an archived one shows the not-recognised state; a hand-edited `?table=` shows nothing
  </success_criteria>

<output>
Create `.planning/phases/10.3-table-zones-tables-and-qr-codes/10.3-14-SUMMARY.md` when done.
</output>
