---
phase: 01-tenancy-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts
  - apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts
  - packages/db/test/integration/concurrent-write-race.spec.ts
  - packages/db/test/integration/raw-tx-rls-fence.spec.ts
  - apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts
autonomous: true
requirements:
  - TEN-08

must_haves:
  truths:
    - 'All 4 fixture categories from D-07 (ALS leak, NATS subscriber tenant-context mix, concurrent-write race, cross-tenant read-leak / raw-tx RLS fence) are implemented; partial coverage = phase NOT done. This plan is the PHASE GATE (per D-04 PR 6 + D-07).'
    - "Fixture 1 (ALS leak): two `Promise.all`'d concurrent HTTP requests, one under tenant A and one under tenant B, each see ONLY their own tenant_id in every Postgres query. Test runs 100 iterations in CI to surface low-probability interleavings; zero leaks observed across all iterations."
    - "Fixture 2 (NATS mix): publish A/B/A/B/A/B interleaved envelopes; assert `runDeduped` + downstream handler always processes the envelope's `tenantId`, not a leaked one from a prior message."
    - "Fixture 3 (concurrent write): two `Promise.all`'d writes under different `withTenant(...)` callbacks each insert a tenant-scoped row; assert each row's `tenant_id` matches the calling context (no GUC drift); a `pg_sleep(0.5)` widens the leak window to maximize the test's bug-catching power (Pitfall 5)."
    - "Fixture 4 (raw-tx RLS fence): deliberately omit `WHERE tenant_id` from a raw `tx.select()`; assert RLS still returns ONLY the bound tenant's rows — proves the database-layer defense holds even when application-layer `ScopedTx` is bypassed."
    - 'All 4 fixtures USE the existing `with-real-stack.setup.ts` + `startPostgres()` patterns; NO mocking of ALS, NO mocking of NATS — per D-06 these failure modes only manifest with real async boundaries.'
    - "Each fixture deliberately introduces a controllable leak (e.g., temporarily comments out `#assertGucUnchanged` in `client.ts`) during validation to prove the test FAILS in the leaky state — i.e., the test design actually catches the bug class it's named for."
    - 'Existing `apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts` is INVENTORIED at the top of this plan (per OQ-6: the file is 227 lines and may already cover some fixture categories; scope down accordingly if so).'
  artifacts:
    - path: 'apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts'
      provides: 'Fixture 1 — concurrent HTTP requests under different tenants; assert per-request tenant_id isolation; 100-iteration tight loop.'
    - path: 'apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts'
      provides: "Fixture 2 — interleaved A/B publish; assert handler always processes envelope's tenantId via runDeduped."
    - path: 'packages/db/test/integration/concurrent-write-race.spec.ts'
      provides: 'Fixture 3 — Promise.all writes under different withTenant callbacks; pg_sleep(0.5) widens window.'
    - path: 'packages/db/test/integration/raw-tx-rls-fence.spec.ts'
      provides: 'Fixture 4 — raw tx.select without WHERE tenant_id; assert RLS blocks cross-tenant rows.'
  key_links:
    - from: 'Fixture 1 (ALS leak)'
      to: 'TenantContextMiddleware + AsyncLocalStorage'
      via: 'concurrent HTTP requests'
      pattern: "Promise\\.all.*request"
    - from: 'Fixture 2 (NATS mix)'
      to: 'NatsAuditSubscriber + runDeduped'
      via: 'interleaved publishes'
      pattern: 'runDeduped'
    - from: 'Fixture 3 (concurrent write)'
      to: 'ScopedTx + app_bind_tenant GUC'
      via: 'Promise.all withTenant blocks'
      pattern: "withTenant.*Promise\\.all"
    - from: 'Fixture 4 (raw-tx RLS fence)'
      to: 'Postgres RLS policy on tenant-scoped tables'
      via: 'raw tx.select() without WHERE'
      pattern: "tx\\.select.*FROM.*(?!.*WHERE)"
---

<objective>
PR 06 is the **PHASE GATE**. Per D-04, Phase 1 is NOT done until this PR's 4 fixture categories land green on CI. The CTO persona review (RESEARCH §Specifics) is explicit: under-budgeting this PR ships Phase 1 with a half-built regression net, which is the only place a future Phase 2..16 cross-tenant data leak could hide undetected.

**The 4 fixture categories from D-07 (all required, partial = not done):**

1. **ALS leak fixture** (`apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts`) — spawn two concurrent tenant-context HTTP requests; verify Postgres `app_bind_tenant` GUC values do not bleed across the async boundary. Per Pitfall 4: this MUST use `Promise.all` and run 100 iterations to surface low-probability interleavings.

2. **NATS subscriber tenant-context mix fixture** (`apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts`) — publish events from tenant A and tenant B interleaved; verify `runDeduped` + handler always sees the correct tenant context for the envelope being processed.

3. **Concurrent-write race fixture** (`packages/db/test/integration/concurrent-write-race.spec.ts`) — two parallel writers to the same tenant-scoped table from different tenants; per Pitfall 5: inject `pg_sleep(0.5)` to widen the leak window; verify composite FK + RLS hold under contention.

4. **Cross-tenant read-leak fixture** (`packages/db/test/integration/raw-tx-rls-fence.spec.ts`) — verify that `WHERE tenant_id` predicate omission (a future raw `tx.select()` mistake) is blocked by RLS even when application-layer `ScopedTx` would have prevented it.

**OQ-6 inventory:** Existing `apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts` is 227 lines. Task 0 reads it in full and inventories which categories (if any) are already covered. Anything that overlaps is consolidated; anything that doesn't is added. This is the planner-recommended approach per RESEARCH §Open Questions OQ-6 — do NOT skip this step; over-spec'd duplicate tests degrade signal.

**Deferred guard (do NOT slip into PR 06):** the `releaseOutboxClaim` claim-token race in `packages/events/src/outbox/repository.ts:110-128` is Phase 7 ORD-11 work, not Phase 1. CTO persona review explicitly carved this out.

Purpose: Phase 1 closes with a regression net so robust that any future PR introducing a cross-tenant leak fails CI before merge. PR 06 is the proof.

Output: 4 fixture files (3 new + 1 possibly extending the existing isolation spec from Task 0 inventory) + 1 inventory note. Zero new dependencies; zero migrations; zero schema changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenancy-hardening/01-CONTEXT.md
@.planning/phases/01-tenancy-hardening/01-RESEARCH.md
@.planning/phases/01-tenancy-hardening/01-PATTERNS.md
@./CLAUDE.md
@./apps/api/test/e2e/with-real-stack.setup.ts
@./apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts
@./apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts
@./packages/db/test/setup.ts
@./packages/db/test/integration/tenant-isolation.spec.ts
@./packages/db/test/integration/auth-role-grants.spec.ts
@./packages/db/src/client.ts
@./packages/db/src/context.ts
@./packages/events/src/correlation.ts
@./packages/events/src/inbox/run-duped.ts
@./apps/api/src/shared/tenant-context.middleware.ts

<interfaces>
<!-- Canonical test setups for the 4 fixtures -->

From apps/api/test/e2e/with-real-stack.setup.ts:

- `startRealStack()` → `{ app, container, natsContainer, teardown }`
- Spins Postgres + NATS testcontainers + full NestJS app
- Used by all 12 existing e2e specs

From packages/db/test/setup.ts:

- `startPostgres()` → `{ url, container, end }`
- `isDockerAvailable()` → boolean (gate every spec on this)

From packages/db/src/client.ts:

- `withTenant<T>(tenantId: string, fn: (tx, scoped) => Promise<T>): Promise<T>` — binds `app_bind_tenant` GUC, sets ALS frame
- `withoutTenant<T>(reason: string, fn: (tx) => Promise<T>): Promise<T>` — system-level operation
- `#assertGucUnchanged` — internal sentinel; commenting this out is the way to deliberately introduce a leak during validation (do this once during test development to confirm the test catches it; restore before final commit)

From packages/db/test/integration/tenant-isolation.spec.ts:

- The canonical RLS regression test — template for fixtures 3 and 4
- Uses real Postgres + the project's role setup (`resto_app` non-bypassing user)
- Each test seeds two tenants in their own `withTenant` blocks

From packages/events/src/inbox/run-duped.ts:

- `runDeduped(db, envelope, consumerName, fn)` — at-most-once handler invocation; inserts inbox row + handler body in same DB transaction
- For Fixture 2: subscribe to a test subject, publish A/B/A/B envelopes, assert each handler invocation processes the envelope's tenantId

From apps/api/src/shared/tenant-context.middleware.ts:

- Resolves tenant from request host or header
- Binds `app_bind_tenant` GUC via `db.withTenant(...)` for the duration of the request
- For Fixture 1: two concurrent HTTP requests under different `Host:` headers exercise this entry point
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 0: Inventory existing cross-tenant-isolation.e2e.spec.ts and decide scope (OQ-6)</name>
  <files>apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts (the FULL file — all 227 lines per OQ-6)
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §Open Questions OQ-6 (read the existing scaffold first, inventory coverage)
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-07 (the 4 fixture categories; all required)
  </read_first>
  <action>
    Read `apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts` in full. Identify which of the 4 D-07 categories (ALS leak, NATS mix, concurrent write, raw-tx RLS fence) are already covered. Write the inventory as an inline comment at the TOP of the file:

    ```
    // PR 06 / TEN-08 inventory (Task 0):
    // - Fixture 1 (ALS leak): {COVERED | PARTIAL | NOT COVERED} — <reason>
    // - Fixture 2 (NATS mix): {COVERED | PARTIAL | NOT COVERED} — <reason>
    // - Fixture 3 (concurrent write): {COVERED | PARTIAL | NOT COVERED} — <reason>
    // - Fixture 4 (raw-tx RLS fence): {COVERED | PARTIAL | NOT COVERED} — <reason>
    ```

    If any category is COVERED here, the new fixture file (Tasks 1-4 below) becomes a SCOPED extension (e.g., the 100-iteration loop, the pg_sleep widening) rather than a duplicate. If any is PARTIAL, the new fixture file fills the gap.

    **Do not modify** the existing test bodies in this task — only add the inventory comment. Modifications to consolidate happen in the relevant Task 1-4 if needed.

    Document the decision: if Fixture 3 is fully covered already, Task 3 becomes a no-op (write a single-line file saying "covered by cross-tenant-isolation.e2e.spec.ts line X-Y" and skip the actual test body — but verify with the executor that this is preferable to deleting the new file).

  </action>
  <verify>
    <automated>grep -c "PR 06 / TEN-08 inventory" apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The inventory comment block appears at the top of `apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts` (or in a co-located README — choice locked here: in-file comment because it's discoverable when reading the test).
    - All 4 fixture categories appear in the inventory with a coverage classification AND a reason.
    - The inventory's classification is reflected in the implementation choices made in Tasks 1-4 (e.g., if Task 0 marks Fixture 4 as COVERED, Task 4's spec body is correspondingly minimal).
  </acceptance_criteria>
  <done>
    Inventory comment present; subsequent tasks have a clear scope decision per category.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Fixture 1 — ALS leak across async boundary (concurrent HTTP requests, 100 iterations)</name>
  <files>apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/with-real-stack.setup.ts (canonical setup; use `startRealStack()`)
    - apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts (per Task 0 inventory — see what HTTP-request helpers / tenant-provisioning helpers already exist)
    - apps/api/src/shared/tenant-context.middleware.ts (the entry point being exercised — verify which header binds the tenant: `Host`, `X-Tenant-Slug`, or both)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 6 row for `cross-tenant-als-leak.e2e.spec.ts`
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §Pitfall 4 (the warning: writing the test as sequential proves nothing; MUST use Promise.all + 100 iterations)
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-07 fixture 1 description
  </read_first>
  <behavior>
    - Provision 2 tenants (A, B) with seed data: each has a distinct menu (say, 3 items for A, 2 items for B).
    - In a `for (let i = 0; i < 100; i++)` loop, fire `Promise.all([GET /v1/menu under tenant A, GET /v1/menu under tenant B])`.
    - For each iteration, assert: tenant A's response contains ONLY A's items (count + each item's tenant_id check); tenant B's response contains ONLY B's items.
    - Total iterations: 100. Total HTTP requests: 200. Test timeout: 90 seconds.
    - On any single iteration where a leak is observed, the assertion fires and the test fails IMMEDIATELY with a clear message identifying the iteration number and the leaked rows.
    - The spec exits 0 if all 100 iterations are clean.
  </behavior>
  <action>
    Create `apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts`:

    Gate on `isDockerAvailable()` from `packages/db/test/setup.ts`.

    `beforeAll`: `startRealStack()`. Provision two tenants (use existing test helper from `cross-tenant-isolation.e2e.spec.ts` if available — read Task 0 inventory to find it). Seed each tenant's catalog with 3 / 2 items respectively. Capture the `tenantId` and the expected item-set per tenant.

    One `it()` block: `'maintains tenant isolation across 100 concurrent request pairs'`:
    ```
    for (let i = 0; i < 100; i++) {
      const [respA, respB] = await Promise.all([
        request(app.getHttpServer()).get('/v1/menu').set('Host', `${slugA}.test.local`),
        request(app.getHttpServer()).get('/v1/menu').set('Host', `${slugB}.test.local`),
      ]);
      expect(respA.body.items.map(i => i.id).sort()).toEqual(expectedItemsA.sort());
      expect(respB.body.items.map(i => i.id).sort()).toEqual(expectedItemsB.sort());
    }
    ```
    Use whichever HTTP request helper the project standardizes on (`supertest`, `Fastify.inject`, etc.) — match `cross-tenant-isolation.e2e.spec.ts`. Use whichever tenant-resolution header the project's middleware reads (likely `Host` for subdomain-based routing — confirm by reading `tenant-context.middleware.ts`).

    `vi.setConfig({ testTimeout: 90_000 })` at the top of the describe block.

    `afterAll`: teardown.

    Validation step (run ONCE during development, then revert before final commit): temporarily comment out the `#assertGucUnchanged` sentinel (or whichever GUC drift check exists) in `packages/db/src/client.ts` — confirm the test FAILS in the leaky state. This is the persona-skeptic "actually catches the bug" check from RESEARCH §Pitfall 5 warning signs. Document this validation in the test's top-of-file comment block with a reference to the commit hash where the validation was performed.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Promise.all" apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts | grep -v '^#'` returns at least 1 (the test MUST be concurrent, NOT sequential — Pitfall 4).
    - `grep -nE "for \\(.*i < 100" apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts` returns at least 1 match (100-iteration loop).
    - `grep -n "testTimeout" apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts` confirms timeout > 60s.
    - `pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts` exits 0; spec runs within timeout; total wall time between 30-90s typical.
    - The top-of-file comment block documents the leak-validation step that proved the test design catches the bug class.
  </acceptance_criteria>
  <done>
    Fixture 1 lands; 100-iteration Promise.all loop is the proof of concurrent test design; validation step documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture 2 — NATS subscriber tenant-context mix</name>
  <files>apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/with-real-stack.setup.ts (the canonical setup)
    - apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts (existing subscriber — verify subject pattern + how it invokes `runDeduped`)
    - packages/events/src/inbox/run-duped.ts (signature: `runDeduped(db, envelope, consumerName, fn) — fn receives the tx`)
    - apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts (per Task 0 inventory — find tenant provisioning helpers and any existing publish/subscribe test infrastructure)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 6 row for `cross-tenant-nats-mix.e2e.spec.ts`
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-07 fixture 2 description
  </read_first>
  <behavior>
    - Provision 2 tenants A, B.
    - Set up a test subscriber on a throwaway subject (e.g., `test.cross-tenant-mix.v1`) that uses `runDeduped` and records each invocation's `(envelope.tenantId, observed tenantContext)` tuple into an array.
    - Publish 100 envelopes interleaved: A, B, A, B, ..., A, B (50 of each).
    - Wait for the subscriber to drain (poll the recorded-invocations array length until >= 100, with a 30s timeout).
    - Assert: for every recorded tuple, `envelope.tenantId === observed tenantContext.tenantId` — i.e. the handler always processes the correct tenant for the envelope it received, regardless of interleaving.
    - Total recorded invocations: exactly 100 (no duplicates from `runDeduped`, no skips).
  </behavior>
  <action>
    Create `apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts`:

    Gate on `isDockerAvailable()`.

    `beforeAll`: `startRealStack()`. Provision 2 tenants. Identify the NATS client + a subject to publish to (use a test-only subject like `test.cross-tenant-mix.v1` defined inline — register the subject in the test if the stream config requires explicit subjects; otherwise rely on the `>` wildcard).

    Set up a recording subscriber:
    ```
    const observations: Array<{ envelopeTenantId: string; handlerSawTenantId: string }> = [];
    // Subscribe to `test.cross-tenant-mix.v1` with runDeduped invocation:
    //   on each envelope received, call runDeduped(db, envelope, 'test-cross-tenant-mix-consumer', async (tx) => {
    //     const ctx = getTenantContext(); // however the project exposes the current tenant
    //     observations.push({ envelopeTenantId: envelope.tenantId, handlerSawTenantId: ctx?.tenantId ?? 'platform' });
    //   });
    ```

    Verify with the executor: depending on how the existing audit subscriber sets the tenant context BEFORE invoking the handler (likely `db.withTenant(envelope.tenantId, ...)` wrapper), this fixture should observe that wrapper's effect. If the subscriber does NOT wrap with `withTenant`, the handler will see whatever ALS frame is bound — which IS the bug class TEN-08 is trying to catch. Read `nats-audit-subscriber.ts` carefully.

    One `it()` block: `'NATS subscriber processes interleaved A/B envelopes with correct tenant context each time'`:
    - Publish 100 envelopes interleaved A/B/A/B (use `Promise.all` to publish in batches of 10 to maximize interleaving).
    - Poll for `observations.length >= 100` with 30s timeout, 100ms interval.
    - For each observation, assert `envelopeTenantId === handlerSawTenantId`.
    - Assert no duplicates: `observations.length === 100` exactly (not >= 100).

    `vi.setConfig({ testTimeout: 60_000 })`.

    `afterAll`: teardown subscriber, teardown stack.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "runDeduped" apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts` returns at least one match.
    - `grep -nE "for.*100|publish.*100|repeat.*100" apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts` confirms 100-iteration design.
    - `pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts` exits 0; the spec finishes within 60s.
    - The assertion compares `envelope.tenantId` and `handlerSawTenantId` per observation (verify by `grep -A1 expect.*tenantId apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts`).
  </acceptance_criteria>
  <done>
    Fixture 2 lands; 100 interleaved envelopes processed by handler with correct tenant context each; no duplicate invocations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixture 3 — Concurrent-write race (Promise.all withTenant + pg_sleep widening)</name>
  <files>packages/db/test/integration/concurrent-write-race.spec.ts</files>
  <read_first>
    - packages/db/test/integration/tenant-isolation.spec.ts (the canonical RLS regression test — direct template for this fixture)
    - packages/db/test/setup.ts (`startPostgres()` + `isDockerAvailable()`)
    - packages/db/src/client.ts (`withTenant` signature; `app_bind_tenant` GUC binding mechanism)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 6 row for `concurrent-write-race.spec.ts`
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §Pitfall 5 (warning signs — pg_sleep widening + deliberate-leak validation)
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-07 fixture 3 description
  </read_first>
  <behavior>
    - In a single test run, two `withTenant(A, ...)` and `withTenant(B, ...)` callbacks executed via `Promise.all`. Each callback inserts a tenant-scoped row into the same table (e.g., `tenants` or a test-only table seeded with composite FK).
    - Inside one of the callbacks, `tx.execute(sql\`SELECT pg_sleep(0.5)\`)` runs before the INSERT — this widens the window during which ALS context could leak.
    - Assert: each row's `tenant_id` matches the calling context's `tenantId` (not the other's).
    - Repeat the inner Promise.all 20 times to surface low-probability interleavings (smaller than Fixture 1's 100 because per-iteration DB cost is higher and pg_sleep extends each).
    - All 20 iterations clean → exit 0.
  </behavior>
  <action>
    Create `packages/db/test/integration/concurrent-write-race.spec.ts`:

    Gate on `isDockerAvailable()`.

    `beforeAll`: start postgres via `startPostgres()`. Run migrations. Provision 2 tenants (use whichever helper `tenant-isolation.spec.ts` uses).

    Pick a tenant-scoped target table. Best candidate: a test-only `test_concurrent_writes` table created in the spec's `beforeAll` via raw migration-style DDL, or — preferred — reuse an existing tenant-scoped table like `tenants_audit_log` if it accepts arbitrary inserts. **Prefer** creating a test-only table inside the spec to avoid polluting real tables.

    One `it()` block: `'concurrent withTenant writes do not leak tenant_id under pg_sleep widening'`:
    ```
    for (let i = 0; i < 20; i++) {
      const [resA, resB] = await Promise.all([
        db.withTenant(tenantA.id, async (tx) => {
          await tx.execute(sql`SELECT pg_sleep(0.5)`);
          await tx.execute(sql`INSERT INTO test_concurrent_writes (tenant_id, marker) VALUES (current_setting('app.current_tenant')::uuid, ${`A-${i}`}) RETURNING tenant_id`);
        }),
        db.withTenant(tenantB.id, async (tx) => {
          await tx.execute(sql`INSERT INTO test_concurrent_writes (tenant_id, marker) VALUES (current_setting('app.current_tenant')::uuid, ${`B-${i}`}) RETURNING tenant_id`);
        }),
      ]);
      // Assert via a follow-up read: rows tagged A-i have tenant_id = tenantA.id; rows tagged B-i have tenant_id = tenantB.id.
      const rows = await db.execute(sql`SELECT marker, tenant_id FROM test_concurrent_writes WHERE marker LIKE ${`%-${i}`}`);
      for (const r of rows) {
        const expectedTenant = r.marker.startsWith('A') ? tenantA.id : tenantB.id;
        expect(r.tenant_id).toBe(expectedTenant);
      }
    }
    ```

    Use `current_setting('app.current_tenant')::uuid` to PROVE the GUC reading is what the test relies on — that's exactly the value that would drift in a leak scenario.

    `vi.setConfig({ testTimeout: 60_000 })` (20 iterations × ~0.5s pg_sleep + overhead = ~15s typical, but allow headroom).

    `afterAll`: drop the test table; teardown postgres.

    Validation step: temporarily comment out `#assertGucUnchanged` in `client.ts` — confirm the test FAILS (Pitfall 5 warning sign). Document the commit hash in the top-of-file comment block.

  </action>
  <verify>
    <automated>pnpm --filter @resto/db test packages/db/test/integration/concurrent-write-race.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Promise.all" packages/db/test/integration/concurrent-write-race.spec.ts` returns at least one match (concurrent design).
    - `grep -n "pg_sleep" packages/db/test/integration/concurrent-write-race.spec.ts` returns at least one match (widened window per Pitfall 5).
    - `grep -n "current_setting('app.current_tenant')" packages/db/test/integration/concurrent-write-race.spec.ts` returns at least one match (proves test reads the actual GUC value).
    - `pnpm --filter @resto/db test packages/db/test/integration/concurrent-write-race.spec.ts` exits 0.
    - The top-of-file comment block documents the validation that the test catches a deliberately-introduced leak.
  </acceptance_criteria>
  <done>
    Fixture 3 lands; 20-iteration concurrent write with pg_sleep widening; validation step documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fixture 4 — Cross-tenant read-leak via raw tx.select without WHERE tenant_id</name>
  <files>packages/db/test/integration/raw-tx-rls-fence.spec.ts</files>
  <read_first>
    - packages/db/test/integration/tenant-isolation.spec.ts (template — find existing RLS regression tests that use raw `tx.select`)
    - packages/db/src/client.ts (verify `withTenant` provides access to a raw `tx` that has RLS active per the policy; the test must bypass `ScopedTx` (the application-layer fence) and ONLY rely on RLS (the database-layer fence))
    - packages/db/test/setup.ts (`startPostgres()` + `isDockerAvailable()`)
    - packages/db/migrations/ (find the RLS policy on a tenant-scoped table — likely `tenants` or `menu_items`; identify which table is suitable as a fixture target)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 6 row for `raw-tx-rls-fence.spec.ts`
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-07 fixture 4 description (RLS holds even when ScopedTx is bypassed)
  </read_first>
  <behavior>
    - Provision 2 tenants A, B. Each tenant has at least 2 rows in some tenant-scoped table (e.g., `menu_items`, or a test-only `test_rls_fence` table).
    - Under `db.withTenant(tenantA.id, async (tx) => { ... })`, execute a RAW SELECT against the tenant-scoped table without `WHERE tenant_id = ?` predicate (e.g., `tx.execute(sql\`SELECT id, tenant_id FROM menu_items\`)`).
    - Assert: the returned rows ALL have `tenant_id === tenantA.id`. RLS at the database layer must filter B's rows out even though the SELECT did not include the WHERE predicate.
    - Symmetrically: under `withTenant(tenantB.id, ...)`, the same SELECT returns ONLY B's rows.
    - This proves the database-layer fence (RLS policy) holds independently of the application-layer fence (`ScopedTx` auto-appended WHERE clause).
  </behavior>
  <action>
    Create `packages/db/test/integration/raw-tx-rls-fence.spec.ts`:

    Gate on `isDockerAvailable()`.

    `beforeAll`: `startPostgres()`. Run migrations. Provision 2 tenants. Seed each tenant with at least 2 rows in a tenant-scoped target table. Choose the target table carefully: it should be a table with a confirmed RLS policy (read migrations to confirm), and seeding it should be feasible. Reasonable candidates: `tenants_audit_log`, or a test-only table the spec creates with the SAME RLS policy pattern as production tables.

    If creating a test-only table, the `beforeAll` must:
    1. CREATE TABLE under the admin role (not `resto_app`).
    2. ALTER TABLE ENABLE ROW LEVEL SECURITY.
    3. CREATE POLICY identical to the project's standard pattern (`USING (tenant_id = current_setting('app.current_tenant')::uuid)`).
    4. GRANT SELECT, INSERT to resto_app.

    One `it()` block: `'RLS returns only the bound tenants rows even when WHERE tenant_id is omitted from raw tx.select'`:
    ```
    // Seed step
    await db.withTenant(tenantA.id, async (tx) => {
      await tx.execute(sql`INSERT INTO test_rls_fence (tenant_id, marker) VALUES (${tenantA.id}, 'A1'), (${tenantA.id}, 'A2')`);
    });
    await db.withTenant(tenantB.id, async (tx) => {
      await tx.execute(sql`INSERT INTO test_rls_fence (tenant_id, marker) VALUES (${tenantB.id}, 'B1'), (${tenantB.id}, 'B2')`);
    });

    // Assertion under tenant A
    const rowsA = await db.withTenant(tenantA.id, async (tx) => {
      // DELIBERATELY omit WHERE tenant_id — proves RLS catches it
      const r = await tx.execute(sql`SELECT marker, tenant_id FROM test_rls_fence`);
      return r;
    });
    expect(rowsA.length).toBe(2);
    expect(rowsA.every(r => r.tenant_id === tenantA.id)).toBe(true);
    expect(rowsA.map(r => r.marker).sort()).toEqual(['A1', 'A2']);

    // Symmetric: tenant B
    const rowsB = await db.withTenant(tenantB.id, async (tx) => {
      const r = await tx.execute(sql`SELECT marker, tenant_id FROM test_rls_fence`);
      return r;
    });
    expect(rowsB.length).toBe(2);
    expect(rowsB.every(r => r.tenant_id === tenantB.id)).toBe(true);
    ```

    `afterAll`: drop test table; teardown.

    Validation step (run during development): temporarily DROP the RLS policy from `test_rls_fence` — confirm the test FAILS (the raw SELECT returns all 4 rows). This proves the test is actually asserting RLS, not just the GUC value. Document the commit hash where validation was performed.

  </action>
  <verify>
    <automated>pnpm --filter @resto/db test packages/db/test/integration/raw-tx-rls-fence.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SELECT.*FROM test_rls_fence" packages/db/test/integration/raw-tx-rls-fence.spec.ts` returns at least 2 matches (the deliberate WHERE-less SELECTs under each tenant).
    - `grep -nE "WHERE tenant_id" packages/db/test/integration/raw-tx-rls-fence.spec.ts` returns 0 (the SELECTs are deliberately predicate-less — the test's whole point).
    - `grep -n "ENABLE ROW LEVEL SECURITY" packages/db/test/integration/raw-tx-rls-fence.spec.ts` returns at least one match (the seed step creates the policy).
    - `pnpm --filter @resto/db test packages/db/test/integration/raw-tx-rls-fence.spec.ts` exits 0.
    - The top-of-file comment block documents the RLS-disable validation step.
  </acceptance_criteria>
  <done>
    Fixture 4 lands; RLS proven to be the canonical defense; ScopedTx is NOT the only thing keeping tenants isolated.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                                                                                                                 |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Concurrent HTTP requests→`TenantContextMiddleware` ALS | Two requests must each see only their own tenant context, even when scheduled to run on the same Node.js event-loop tick.                                   |
| NATS subscriber→handler invocation                     | The subscriber wraps with `db.withTenant(envelope.tenantId, ...)` (assumed); if not, the handler sees a leaked ALS frame — the bug class TEN-08 must catch. |
| Application code→Postgres raw tx                       | `ScopedTx` is one fence; RLS is the second fence; both must hold independently.                                                                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                                                                  | Disposition         | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                   |
| --------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | Information Disclosure | ALS frame leaks between concurrent HTTP requests → tenant A sees tenant B's menu                                                                           | mitigate (CRITICAL) | Fixture 1: 100-iteration `Promise.all` HTTP requests under different tenants; assert per-request isolation. Validation step proves test catches a deliberately-introduced leak. Persona-skeptic explicit watch: "TEN-08 PR ships with a single `it('isolates tenants')` that takes <100ms. Hard reject in code review." This task design hard-locks 100 iterations + Promise.all. |
| T-06-02   | Information Disclosure | NATS subscriber processes envelope with leaked tenant context → audit row attributed to wrong tenant                                                       | mitigate (HIGH)     | Fixture 2: 100 interleaved A/B publishes; assert handler always sees envelope's tenantId. Validates the subscriber's `withTenant` wrap is correct.                                                                                                                                                                                                                                |
| T-06-03   | Tampering              | Concurrent writes from different tenants → GUC drift leaks one tenant's INSERT into the other's transaction                                                | mitigate (HIGH)     | Fixture 3: `Promise.all` `withTenant(A, ...)` + `withTenant(B, ...)` with `pg_sleep(0.5)` widening; 20 iterations; assert each row's `tenant_id` matches calling context's.                                                                                                                                                                                                       |
| T-06-04   | Information Disclosure | A future raw SQL mistake omits `WHERE tenant_id` predicate → cross-tenant read leak                                                                        | mitigate (HIGH)     | Fixture 4: deliberately predicate-less raw `tx.select`; assert RLS returns ONLY the bound tenant's rows. Proves the database-layer fence is independent of the application-layer `ScopedTx`.                                                                                                                                                                                      |
| T-06-05   | Tampering              | Test scaffold itself has a tenant-bypass shortcut (e.g. uses `withoutTenant` to seed both tenants in one tx) → tests pass while production isolation fails | mitigate (HIGH)     | Each fixture seeds each tenant in its OWN `withTenant(tenantId, ...)` block (NEVER `withoutTenant` for both). Acceptance criteria check this via grep.                                                                                                                                                                                                                            |
| T-06-06   | Repudiation            | A test passes for the wrong reason (assertion is too weak — e.g. only checks count, not actual tenant_id values)                                           | mitigate            | Each fixture's assertion checks actual `tenant_id` values, NOT just row counts. Validation step (deliberately-introduce-a-leak then confirm test fails) proves the test actually catches the bug. Documented in each spec's top-of-file comment block.                                                                                                                            |
| T-06-SC   | Tampering              | No package installs in this PR                                                                                                                             | accept              | This PR adds only test files. Zero new npm packages.                                                                                                                                                                                                                                                                                                                              |

</threat_model>

<verification>
After all 5 tasks (Task 0 + 4 fixtures):

1. `pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-als-leak.e2e.spec.ts` — exits 0.
2. `pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-nats-mix.e2e.spec.ts` — exits 0.
3. `pnpm --filter @resto/db test packages/db/test/integration/concurrent-write-race.spec.ts` — exits 0.
4. `pnpm --filter @resto/db test packages/db/test/integration/raw-tx-rls-fence.spec.ts` — exits 0.
5. `pnpm --filter @resto/api test apps/api/test/e2e/cross-tenant-isolation.e2e.spec.ts` — exits 0 (inventory comment present, existing tests still green).
6. Full Phase 1 test sweep: `pnpm test` exits 0; no regressions in PR 1-5 specs.
7. Each fixture's top-of-file comment block documents the validation step (deliberately-introduce-leak → test fails → restore → test passes) — verified by visual review.
   </verification>

<success_criteria>

- Phase Success Criterion 5 fully delivered: "cross-tenant isolation tests pass under concurrent load with no ALS leak."
- All 4 D-07 fixture categories shipped and green on CI.
- Each fixture validated to actually catch its bug class (not pass by accident).
- Phase 1 complete: 18/18 TEN-xx requirements covered across 6 PRs.
  </success_criteria>

<output>
Create `.planning/phases/01-tenancy-hardening/01-06-SUMMARY.md` when done. Per the phase gate semantics:

- List the inventory outcome from Task 0 (which categories were COVERED / PARTIAL / NOT COVERED in the pre-existing spec).
- For each of the 4 fixtures: file path, test count, CI runtime, validation step (commit hash where the deliberately-introduced leak was tested).
- A "Phase 1 close-out" subsection: confirm all 6 PRs' summary files exist; confirm all 18 TEN-xx requirements have green tests; confirm the boot preflight chain runs all 5 assertions successfully against the dev stack.
- A "Known scaling gates" subsection: cardinality ceiling for OTel labels (50+ tenants per D-05), reminder that `releaseOutboxClaim` claim-token race is Phase 7 ORD-11, retention sweep DELETE grant is narrow per OQ-2.
  </output>
