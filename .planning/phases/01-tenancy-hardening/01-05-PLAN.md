---
phase: 01-tenancy-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - apps/api/src/contexts/identity/identity-core.module.ts
  - apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts
  - apps/api/src/infrastructure/outbox-dispatcher.service.ts
  - apps/api/src/shared/http-metrics.interceptor.ts
  - apps/api/src/app.module.ts
  - .planning/phases/01-tenancy-hardening/audit-gap.md
  - apps/api/test/e2e/identity-audit.e2e.spec.ts
  - apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts
autonomous: true
requirements:
  - TEN-09
  - TEN-10
  - TEN-14

must_haves:
  truths:
    - 'All 8 existing `correlationId: randomUUID()` literal call sites — 3 in `apps/api/src/contexts/identity/identity-core.module.ts` (L106, L122, L147 per RESEARCH.md) and 5 in `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts` (L296, L312, L323, L338, L352) — migrated to `buildEnvelope(contract, payload, opts)`.'
    - 'TEN-09 audit gap closure (identity context) and TEN-14 8-site migration ship in the SAME commit (this PR 5) — D-15 forbids splitting them across PRs to avoid mid-PR merge conflicts.'
    - 'This plan is PR 5 of the locked 6-PR sequence (per D-04); observability + audit gap close landing in Wave 3.'
    - 'All 8 `eslint-disable-next-line no-restricted-syntax -- TEN-14 PR-5` comments inserted by PR 4 Task 5 are REMOVED (proves the migration actually deletes the literal — not just hides it behind a disable comment).'
    - '`.planning/phases/01-tenancy-hardening/audit-gap.md` exists as a markdown table per OQ-4 with columns `Action | Context | Current Status | Gap | Closure Task`; 8 rows covering {provision, archive, offboard, suspend, erase, sign-in, sign-out, role-change} × {tenancy, identity} (per D-14); each gap either CLOSED (closure task lists a PR-5 commit) or BLOCKED (with reason — e.g., role-change → BA hook surface unavailable, deferred to AUTH-09 in Phase 3 per OQ-5).'
    - "Outbox-dispatcher OTel metrics (`outbox.delivered`, `outbox.lag`, `outbox.claim_failures`) carry a `'tenant.id'` attribute (with `'platform'` for platform-level envelopes whose tenantId is null per D-05)."
    - "HTTP request rate + error rate metrics carry `'tenant.id'` label per request via a NestJS interceptor reading `requireTenantContext()`."
    - 'No new metric SERIES introduced — labels are added to existing metric series (TEN-10 scope is label emission, NOT new metrics; cardinality ceiling documented as known 50+ tenant scaling event per D-05).'
  artifacts:
    - path: 'apps/api/src/contexts/identity/identity-core.module.ts'
      provides: '3 envelope-construction sites migrated to `buildEnvelope(...)`.'
      contains: 'buildEnvelope'
    - path: 'apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts'
      provides: '5 envelope-construction sites migrated; `domainEventToEnvelope` switch is now 100% buildEnvelope-driven.'
      contains: 'buildEnvelope'
    - path: 'apps/api/src/infrastructure/outbox-dispatcher.service.ts'
      provides: "`'tenant.id'` attribute added to `deliveredCounter.add(...)`, `lagHistogram.record(...)`, `claimFailuresCounter.add(...)` calls."
    - path: 'apps/api/src/shared/http-metrics.interceptor.ts'
      provides: "NestJS interceptor emitting per-request `http.server.requests` counter + `http.server.errors` counter with `'tenant.id'` attribute."
    - path: '.planning/phases/01-tenancy-hardening/audit-gap.md'
      provides: 'Markdown table per OQ-4 documenting tenancy + identity audit coverage; rows for 8 critical actions; CLOSED/BLOCKED disposition per row.'
      contains: 'tenancy.tenant_provisioned'
  key_links:
    - from: 'identity-core.module.ts hooks (signed-out / password-reset-completed / active-organization-set)'
      to: 'buildEnvelope(IdentityContract, payload, opts)'
      via: 'direct call replacing the literal'
      pattern: "buildEnvelope\\("
    - from: 'tenant-drizzle.repository.ts domainEventToEnvelope existing cases'
      to: 'buildEnvelope(TenantContract, payload, opts)'
      via: 'direct call replacing the literal'
      pattern: "buildEnvelope\\("
    - from: 'OutboxDispatcher metric emission'
      to: "OTel attribute `'tenant.id'`"
      via: "extending existing `{ 'event.type': ... }` attribute bag"
      pattern: "'tenant\\.id'"
    - from: 'HTTP request handlers (every route)'
      to: 'http-metrics interceptor'
      via: 'globally-registered interceptor'
      pattern: 'APP_INTERCEPTOR'
---

<objective>
PR 05 closes the observability + audit gap once `buildEnvelope` (PR 3) and the enforcement rules (PR 4) are in place. Per D-15, TEN-09 and TEN-14 MUST ship in the same PR because they touch the same files in `identity-core.module.ts` — splitting them creates mid-PR merge conflicts.

Three deliverables:

1. **TEN-14 call-site migration (8 sites)** — Replace `{ correlationId: randomUUID(), ... }` literals with `buildEnvelope(contract, payload, opts)` across:
   - `apps/api/src/contexts/identity/identity-core.module.ts:106` (`onSignedOut` hook)
   - `apps/api/src/contexts/identity/identity-core.module.ts:122` (`onPasswordResetCompleted` hook)
   - `apps/api/src/contexts/identity/identity-core.module.ts:147` (`onActiveOrganizationSet` hook)
   - `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:296` (`TenantProvisioned` case)
   - `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:312` (`TenantArchived` case)
   - `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:323` (`TenantOffboardingScheduled` case)
   - `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:338` (`TenantOffboardingCancelled` case)
   - `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:352` (`TenantErasureCompleted` case)
     PLUS removal of the 8 `eslint-disable -- TEN-14 PR-5` comments inserted by PR 4 Task 5.

2. **TEN-09 audit gap analysis + closure** — Write `.planning/phases/01-tenancy-hardening/audit-gap.md` as a markdown table per OQ-4 with rows for {provision, archive, offboard, suspend, erase, sign-in, sign-out, role-change} × {tenancy, identity} (D-14 scope: these two contexts only). For each gap, close it by extending `ACTION_TARGET_KIND` map in `record-audit.service.ts` OR document as BLOCKED if no hookable surface exists (OQ-5: role-change may have no BA hook — deferred to AUTH-09 in Phase 3). Update `apps/api/test/e2e/identity-audit.e2e.spec.ts` to cover the now-closed gaps.

3. **TEN-10 per-tenant OTel labels** — Add `'tenant.id'` attribute (with `'platform'` sentinel for null) to existing outbox metric emissions in `apps/api/src/infrastructure/outbox-dispatcher.service.ts`. Create `apps/api/src/shared/http-metrics.interceptor.ts` emitting per-request HTTP counters with the same label. Per D-05: label emission only — no dashboards, no alerts, no cardinality ceiling work.

Purpose: PR 3 ships the helper; PR 4 enforces "don't write new literals"; PR 5 retroactively cleans up the existing literals AND adds the per-tenant observability layer that proves the cleanup worked. The audit gap analysis ensures every critical action has a permanent forensic record.

Output: 4 modified existing files + 1 new HTTP-metrics interceptor + 1 new audit-gap doc + 2 extended e2e specs. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tenancy-hardening/01-CONTEXT.md
@.planning/phases/01-tenancy-hardening/01-RESEARCH.md
@.planning/phases/01-tenancy-hardening/01-PATTERNS.md
@./CLAUDE.md
@./apps/api/src/contexts/identity/identity-core.module.ts
@./apps/api/src/contexts/identity/infrastructure/identity-event-emitter.adapter.ts
@./apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts
@./apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts
@./apps/api/src/contexts/audit/application/record-audit.service.ts
@./apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts
@./apps/api/src/infrastructure/outbox-dispatcher.service.ts
@./apps/api/src/shared/tenant-context.middleware.ts
@./apps/api/src/bootstrap-telemetry.ts
@./apps/api/src/app.module.ts
@./apps/api/test/e2e/identity-audit.e2e.spec.ts
@./apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts
@./packages/events/src/envelope.ts
@./packages/events/src/contracts/tenancy.ts
@./packages/events/src/contracts/identity.ts

<interfaces>
<!-- Existing patterns to migrate; existing surfaces to extend -->

From apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts:

- `domainEventToEnvelope(event)` switch has 5 OLD case branches (untouched by PR 3) with `correlationId: randomUUID()` literals; PR 5 migrates all 5 to `buildEnvelope(...)`.
- PR 3 added 2 NEW case branches for TenantSuspended / TenantResumed already using `buildEnvelope` — those do NOT need migration; verify they remain unchanged.

From apps/api/src/contexts/identity/identity-core.module.ts:

- 3 callback/hook bodies construct event envelopes inline at L106 (`onSignedOut`), L122 (`onPasswordResetCompleted`), L147 (`onActiveOrganizationSet`).
- Each currently calls something like `{ id: randomUUID(), type: 'identity.signed_out.v1', correlationId: randomUUID(), tenantId, payload: { ... } }`.
- Identity event contracts live in `packages/events/src/contracts/identity.ts` — import the contract per event type, then call `buildEnvelope(contract, payload, opts)`.

From apps/api/src/infrastructure/outbox-dispatcher.service.ts:

- Three existing metric emission lines:
  - `this.deliveredCounter.add(1, { 'event.type': envelope.type })`
  - `this.lagHistogram.record(lagSeconds, { 'event.type': envelope.type })`
  - `this.claimFailuresCounter.add(1, { 'event.type': envelope.type })`
- Each is on a specific line in the file; this task extends the attribute bag with `'tenant.id': envelope.tenantId ?? 'platform'`.

From apps/api/src/contexts/audit/application/record-audit.service.ts:

- `ACTION_TARGET_KIND` is a `Record<string, TargetKind>` — already extended in PR 3 for tenant_suspended/tenant_resumed.
- PR 5 verifies no further additions are needed; if any of the 8 actions in scope (provision/archive/offboard/suspend/erase/sign-in/sign-out/role-change) lacks a mapping, add it.

From apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts:

- BA hook surface — OQ-5 investigation point. Read the file in Task 4 to determine if `role-change` is hookable; if not, the audit gap row for `identity / role-change` is BLOCKED.

From apps/api/src/shared/tenant-context.middleware.ts (TEN-10 HTTP interceptor consumes this):

- Exports `requireTenantContext()` / `getTenantContext()` reading from ALS.
- Returns `{ tenantId: string }` or undefined.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: TEN-14 migration — 5 envelope sites in tenant-drizzle.repository.ts</name>
  <files>apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts (the full `domainEventToEnvelope` switch — find ALL 7 case branches: 5 OLD (literal correlationId), 2 NEW from PR 3 (already using buildEnvelope))
    - packages/events/src/contracts/tenancy.ts (the existing contracts: `TenantProvisionedV1`, `TenantArchivedV1`, `TenantOffboardingScheduledV1`, `TenantOffboardingCancelledV1`, `TenantErasureCompletedV1` — verify each is exported from `@resto/events`)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md §Shared Patterns / "domainEventToEnvelope branch (using buildEnvelope)"
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §Pattern 2 (migration target list) and §Pattern 1 (the buildEnvelope branch body shape)
  </read_first>
  <action>
    Edit `apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts`:

    1. For each of the 5 OLD case branches at L296, L312, L323, L338, L352 (line numbers per RESEARCH.md but may have drifted slightly after PR 3 added 2 new branches — locate by `case '<EventKind>'`):
       - Replace the `return { id: randomUUID(), type: ..., correlationId: randomUUID(), tenantId: ..., payload: { ... } }` literal with `return buildEnvelope(<ContractV1>, { ...payload fields... }, { tenantId: event.tenantId, occurredAt: event.occurredAt });`
       - Specifically:
         - `case 'TenantProvisioned'` → `buildEnvelope(TenantProvisionedV1, { tenantId, slug, ... }, { tenantId, occurredAt })`
         - `case 'TenantArchived'` → `buildEnvelope(TenantArchivedV1, { tenantId, archivedAt, ... }, { tenantId, occurredAt })`
         - `case 'TenantOffboardingScheduled'` → similar
         - `case 'TenantOffboardingCancelled'` → similar
         - `case 'TenantErasureCompleted'` → similar
       - The exact payload field set per branch comes from reading the existing literal; do NOT invent fields not present today.

    2. REMOVE the 5 `// eslint-disable-next-line no-restricted-syntax -- TEN-14 PR-5: ...` comments inserted by PR 4 Task 5 above each of these 5 sites. The migration deletes the offending literal, so the disable comment is no longer needed.

    3. Verify imports: `buildEnvelope` from `@resto/events` is already imported (PR 3 added it for the 2 new branches). Remove `randomUUID` from `node:crypto` import IF no other use of it remains in this file (likely the 5 OLD branches were the only consumers — confirm with grep before removing the import).

    4. Do NOT touch the 2 NEW branches added in PR 3 (`case 'TenantSuspended'` and `case 'TenantResumed'`) — they already use buildEnvelope correctly.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api typecheck && pnpm --filter @resto/api lint apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "correlationId: randomUUID\|correlationId: crypto.randomUUID" apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts` returns 0 (all 5 literals gone).
    - `grep -c "no-restricted-syntax -- TEN-14 PR-5" apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts` returns 0 (all 5 disable comments removed).
    - `grep -c "buildEnvelope(" apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts | grep -v '^#'` returns at least 7 (5 migrated branches + 2 from PR 3 + possibly the import line — minimum 7).
    - `pnpm --filter @resto/api typecheck` exits 0.
    - `pnpm --filter @resto/api lint apps/api/src/contexts/tenancy/infrastructure/tenant-drizzle.repository.ts` exits 0 (no `no-restricted-syntax` violations — the rule from PR 4 now passes because the literals are gone).
  </acceptance_criteria>
  <done>
    All 5 tenant-repository envelope branches use `buildEnvelope`; no literal correlationId in this file; ESLint clean without disable comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: TEN-14 migration — 3 envelope sites in identity-core.module.ts</name>
  <files>apps/api/src/contexts/identity/identity-core.module.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/identity-core.module.ts (the full file — find the 3 hook bodies at L106 (onSignedOut), L122 (onPasswordResetCompleted), L147 (onActiveOrganizationSet) per RESEARCH.md §Pattern 2 migration target list)
    - packages/events/src/contracts/identity.ts (the existing identity contracts — find `SignedOutV1`, `PasswordResetCompletedV1`, `ActiveOrganizationSetV1` or whatever the project names them — verify exports)
    - apps/api/src/contexts/identity/infrastructure/identity-event-emitter.adapter.ts (related — the system-context emitter pattern; verify whether the hooks emit via this adapter or directly)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 5 row for `identity-core.module.ts`
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-15 (TEN-09 + TEN-14 MUST be in same PR — this is that PR)
  </read_first>
  <action>
    Edit `apps/api/src/contexts/identity/identity-core.module.ts`:

    1. For each of the 3 hook bodies at L106, L122, L147 (locate by hook name — `onSignedOut`, `onPasswordResetCompleted`, `onActiveOrganizationSet`):
       - Replace the `{ id: randomUUID(), type: 'identity.<event>.v1', correlationId: randomUUID(), tenantId, payload: { ... } }` literal with `buildEnvelope(<IdentityContract>, { ...payload }, { tenantId, occurredAt: new Date() })`.
       - Use the contract import name as exported from `packages/events/src/contracts/identity.ts` (may be `SignedOutV1`, `IdentitySignedOutV1`, or similar — verify before editing).

    2. REMOVE the 3 `// eslint-disable-next-line no-restricted-syntax -- TEN-14 PR-5: ...` comments above these 3 sites.

    3. Verify imports: add `buildEnvelope` from `@resto/events` to the existing imports if not already present. Add the identity contract imports if not present. Remove `randomUUID` from `node:crypto` import if no other use remains.

    4. The hooks fire from background contexts (Better Auth callbacks) where ALS may or may not be bound — `buildEnvelope`'s D-10 fallback (WARN + fresh UUID) is the correct behavior. No explicit `correlationId` override needed UNLESS the surrounding code already threads one in; preserve any explicit threading.

    5. Do NOT touch unrelated lines, do NOT modify the Better Auth config block, do NOT change which hooks fire.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api typecheck && pnpm --filter @resto/api lint apps/api/src/contexts/identity/identity-core.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "correlationId: randomUUID\|correlationId: crypto.randomUUID" apps/api/src/contexts/identity/identity-core.module.ts` returns 0.
    - `grep -c "no-restricted-syntax -- TEN-14 PR-5" apps/api/src/contexts/identity/identity-core.module.ts` returns 0.
    - `grep -c "buildEnvelope(" apps/api/src/contexts/identity/identity-core.module.ts | grep -v '^#'` returns at least 3.
    - `pnpm --filter @resto/api typecheck` exits 0.
    - `pnpm --filter @resto/api lint apps/api/src/contexts/identity/identity-core.module.ts` exits 0.
  </acceptance_criteria>
  <done>
    All 3 identity-core envelope sites use `buildEnvelope`; ESLint clean without disable comments.
  </done>
</task>

<task type="auto">
  <name>Task 3: TEN-10 — add tenant.id attribute to outbox-dispatcher OTel metrics</name>
  <files>apps/api/src/infrastructure/outbox-dispatcher.service.ts</files>
  <read_first>
    - apps/api/src/infrastructure/outbox-dispatcher.service.ts (find the three metric emission lines: `deliveredCounter.add`, `lagHistogram.record`, `claimFailuresCounter.add` — the existing attribute bag is `{ 'event.type': envelope.type }` per RESEARCH §Pattern map)
    - apps/api/src/bootstrap-telemetry.ts (verify OTel meter provider is initialized; no change needed here)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 5 row for `outbox-dispatcher.service.ts`
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §TEN-10 row in Phase Requirements table
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-05 (emit label NOW; no dashboards; cardinality ceiling = 50+ tenants is a known scaling gate, documented not enforced)
  </read_first>
  <action>
    Edit `apps/api/src/infrastructure/outbox-dispatcher.service.ts`:

    1. Locate the three metric emission call sites:
       - `this.deliveredCounter.add(1, { 'event.type': envelope.type })`
       - `this.lagHistogram.record(lagSeconds, { 'event.type': envelope.type })`
       - `this.claimFailuresCounter.add(1, { 'event.type': envelope.type })` (or whichever exact metric names exist — read the file first)

    2. Extend each attribute bag with `'tenant.id': envelope.tenantId ?? 'platform'`:
       - `this.deliveredCounter.add(1, { 'event.type': envelope.type, 'tenant.id': envelope.tenantId ?? 'platform' })`
       - same for lag and claim_failures

    3. The `'platform'` sentinel covers platform-level events (envelopes with `tenantId: null`). This is necessary because OTel attribute values must be non-nullish primitives.

    4. Add WHY-comment ONCE above the first emission line: `// TEN-10 / D-05: 'tenant.id' label enables per-tenant outbox observability. Cardinality ceiling: 50+ tenants is a known scaling gate; revisit metric series shape at that point.`

    5. Do NOT add new metric series. Do NOT modify any other line. Do NOT touch advisory-lock leader election or shutdown logic (TEN-16 owns that).

  </action>
  <verify>
    <automated>pnpm --filter @resto/api typecheck && pnpm --filter @resto/api lint apps/api/src/infrastructure/outbox-dispatcher.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'tenant.id'" apps/api/src/infrastructure/outbox-dispatcher.service.ts | grep -v '^#'` returns at least 3 (one per metric emission).
    - `grep -c "envelope.tenantId ?? 'platform'" apps/api/src/infrastructure/outbox-dispatcher.service.ts | grep -v '^#'` returns at least 3.
    - `grep -c "TEN-10" apps/api/src/infrastructure/outbox-dispatcher.service.ts` returns at least 1 (the WHY-comment).
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Three existing outbox metric emissions now carry `'tenant.id'` attribute; null tenantIds resolve to `'platform'`; no new metric series introduced.
  </done>
</task>

<task type="auto">
  <name>Task 4: TEN-10 — HTTP-metrics interceptor for per-request rate + error rate</name>
  <files>apps/api/src/shared/http-metrics.interceptor.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/shared/ (list — find existing interceptors; the `correlation.middleware.ts` and `tenant-context.middleware.ts` show patterns for cross-cutting request-scoped reads)
    - apps/api/src/bootstrap-telemetry.ts (verify `metrics.getMeter(...)` is the OTel API entry point; identify the meter name convention)
    - apps/api/src/shared/tenant-context.middleware.ts (find `getTenantContext()` / `requireTenantContext()` export — the interceptor reads from this)
    - apps/api/src/app.module.ts (find the `providers` block; identify how interceptors are globally registered — probably `{ provide: APP_INTERCEPTOR, useClass: SomeInterceptor }`)
    - .planning/phases/01-tenancy-hardening/01-PATTERNS.md PR 5 row for `http-metrics.interceptor.ts` (NEW file)
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-05 (cardinality ceiling 50+ tenants — document inline)
  </read_first>
  <action>
    Create `apps/api/src/shared/http-metrics.interceptor.ts`:

    1. `@Injectable()` class `HttpMetricsInterceptor` implementing `NestInterceptor`.
    2. Constructor obtains OTel meter via `metrics.getMeter('resto.api.http')` (matches the existing meter naming pattern — read `bootstrap-telemetry.ts` to confirm).
    3. Create two counters at construction time:
       - `this.requestsCounter = meter.createCounter('http.server.requests', { description: 'HTTP requests handled, labeled by tenant.id and method', unit: '1' })`
       - `this.errorsCounter = meter.createCounter('http.server.errors', { description: '5xx responses, labeled by tenant.id and status_code', unit: '1' })`
    4. `intercept(ctx, next)`:
       - On entry: read tenant context via `getTenantContext()`; default to `'platform'` if absent.
       - Read method (`req.method`) and route (`req.url` or `ctx.switchToHttp().getRequest().routeOptions.url` — match Fastify pattern in this project).
       - Wrap `next.handle()` with `.pipe(tap({ next: () => this.requestsCounter.add(1, attrs), error: (err) => { this.requestsCounter.add(1, attrs); this.errorsCounter.add(1, { ...attrs, 'http.status_code': inferStatusCode(err) }); } }))`.
       - `attrs = { 'tenant.id': tenantId ?? 'platform', 'http.method': method, 'http.route': route }`.
    5. WHY-comment: `// TEN-10 / D-05: per-tenant HTTP rate + error rate. Cardinality watch: 50+ tenants × 20 routes ≈ 1000 series — monitor at scaling event, no enforcement now.`

    Edit `apps/api/src/app.module.ts`:
    - Add a provider entry `{ provide: APP_INTERCEPTOR, useClass: HttpMetricsInterceptor }` in the global `providers` array.
    - Import `APP_INTERCEPTOR` from `@nestjs/core` if not already imported.

    Do NOT make this interceptor depend on `RequireActiveTenantGuard` or any guard — it is a metrics interceptor, not an access-control interceptor.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api typecheck && pnpm --filter @resto/api lint apps/api/src/shared/http-metrics.interceptor.ts apps/api/src/app.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class HttpMetricsInterceptor" apps/api/src/shared/http-metrics.interceptor.ts` returns one match.
    - `grep -n "implements NestInterceptor" apps/api/src/shared/http-metrics.interceptor.ts` returns one match.
    - `grep -c "createCounter" apps/api/src/shared/http-metrics.interceptor.ts | grep -v '^#'` returns 2 (the two counters).
    - `grep -n "'tenant.id'" apps/api/src/shared/http-metrics.interceptor.ts` returns at least one match.
    - `grep -n "APP_INTERCEPTOR.*HttpMetricsInterceptor\|HttpMetricsInterceptor.*APP_INTERCEPTOR" apps/api/src/app.module.ts` returns one match.
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Interceptor emits per-request counters with `'tenant.id'` attribute; registered globally via APP_INTERCEPTOR; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: TEN-09 — Author audit-gap.md table for tenancy + identity contexts (D-14 scope)</name>
  <files>.planning/phases/01-tenancy-hardening/audit-gap.md</files>
  <read_first>
    - apps/api/src/contexts/audit/application/record-audit.service.ts (the `ACTION_TARGET_KIND` map — list every event type currently mapped; this is the "Current Status" column source)
    - apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts (subject patterns — what `tenancy.>` and `identity.>` subscribers process today)
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (OQ-5 investigation point — does BA emit a hook for role-change? If not, the role-change row is BLOCKED)
    - packages/events/src/contracts/tenancy.ts and packages/events/src/contracts/identity.ts (verify which event contracts exist for the 8 critical actions)
    - .planning/phases/01-tenancy-hardening/01-CONTEXT.md §D-14 (scope = tenancy + identity ONLY; 8 actions; catalog deferred to Phase 4, ordering to Phase 7)
    - .planning/codebase/CONCERNS.md (read the audit-related tech-debt entries if any — the gap analysis should reference them)
    - .planning/phases/01-tenancy-hardening/01-RESEARCH.md §Open Questions OQ-4 (markdown table format) and OQ-5 (role-change BA hook surface unknown)
  </read_first>
  <action>
    Create `.planning/phases/01-tenancy-hardening/audit-gap.md`:

    Use a markdown table per OQ-4 with the columns `Action | Context | Current Status | Gap | Closure Task`. Eight rows for the 8 actions in scope (D-14):

    For each row, fill columns by reading the codebase:
    - `Action` — one of `provision`, `archive`, `offboard`, `suspend`, `erase`, `sign-in`, `sign-out`, `role-change`.
    - `Context` — `tenancy` or `identity` (some actions live in both — e.g. `sign-in` is identity, `provision` is tenancy).
    - `Current Status` — `'WIRED'` if an event contract exists AND `ACTION_TARGET_KIND` maps it AND the subscriber processes it; `'PARTIAL'` if some piece is missing; `'NOT WIRED'` if no event flow exists.
    - `Gap` — concrete missing piece (e.g. "no audit row for sign-out because no `identity.signed_out.v1` event is emitted from `onSignedOut` hook").
    - `Closure Task` — either a reference to a commit/task in this PR (`'Task 2 — onSignedOut migrated to buildEnvelope; audit_log row now appears'`), OR `BLOCKED — <reason>` for cases like role-change with no hookable BA surface.

    For the role-change row (OQ-5):
    - Read `apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts` to determine if BA exposes a hook surface for member/role mutations.
    - If yes, the gap is closable — propose a follow-up task or note that the closure happens by extending `ACTION_TARGET_KIND` with the new event type.
    - If no, mark as `BLOCKED — Better Auth member-plugin role-mutation hook surface not available in 1.4.22; deferred to AUTH-09 in Phase 3 where role seeding/management ships`. Persona-skeptic flagged this specifically as a "premature done" risk — DO NOT pretend it's closed.

    Header of the file:
    ```
    # Audit Gap Analysis — Phase 1 (D-14 scope: tenancy + identity)

    Scope per D-14: tenancy + identity contexts only. catalog deferred to Phase 4. ordering to Phase 7.

    Eight critical actions assessed against ACTION_TARGET_KIND map in record-audit.service.ts and NatsAuditSubscriber subject patterns.
    ```

    Below the table, include a "Closure Audit" subsection listing the closure tasks within PR 5 (typically: "Migrated 8 envelope sites in PR 5 Tasks 1+2 → events now flow through outbox → NATS → audit_log; verified by extended `identity-audit.e2e.spec.ts` in Task 6"). Document each BLOCKED row with the reason and the future-phase pointer.

    Do NOT include catalog or ordering rows. Do NOT speculate about Phase 4+ work.

  </action>
  <verify>
    <automated>test -f .planning/phases/01-tenancy-hardening/audit-gap.md && grep -c "^|" .planning/phases/01-tenancy-hardening/audit-gap.md | awk '$1 >= 9 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-tenancy-hardening/audit-gap.md`.
    - The file contains a markdown table with the columns `Action | Context | Current Status | Gap | Closure Task` (header row + at least 8 data rows = 9+ lines starting with `|`).
    - `grep -c "provision\|archive\|offboard\|suspend\|erase\|sign-in\|sign-out\|role-change" .planning/phases/01-tenancy-hardening/audit-gap.md` returns at least 8 matches (one per action).
    - `grep -c "BLOCKED" .planning/phases/01-tenancy-hardening/audit-gap.md` returns at least 0 (zero or more, depending on OQ-5 investigation outcome; if BA has no role-change hook, this returns at least 1).
    - The file references the closure tasks within PR 5 by task number or commit-style anchor.
  </acceptance_criteria>
  <done>
    `audit-gap.md` lives at the documented path; 8-row table covers all critical actions in tenancy + identity; role-change row reflects the actual BA hook surface (or is explicitly BLOCKED with reason).
  </done>
</task>

<task type="auto">
  <name>Task 6: Extend identity-audit.e2e.spec.ts and outbox-dispatcher.e2e.spec.ts to cover the new audit rows + OTel labels</name>
  <files>apps/api/test/e2e/identity-audit.e2e.spec.ts, apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/identity-audit.e2e.spec.ts (current contents — what audit assertions exist; add cases for any newly-closed gaps from Task 5)
    - apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts (current contents — add an assertion for the OTel `'tenant.id'` attribute presence)
    - apps/api/test/e2e/with-real-stack.setup.ts (the canonical e2e setup)
    - .planning/phases/01-tenancy-hardening/audit-gap.md (Task 5 — list of closed gaps to cover with assertions)
  </read_first>
  <action>
    Edit `apps/api/test/e2e/identity-audit.e2e.spec.ts`:
    - For each CLOSED row in `audit-gap.md` (Task 5), ensure the spec has at least one `it()` block that triggers the action and asserts the audit row materializes within 5s.
    - Specifically:
      - `'records audit row for identity.signed_out.v1'` — sign in, sign out, poll audit_log for the row.
      - `'records audit row for identity.password_reset_completed.v1'` — trigger password reset, complete it, poll.
      - `'records audit row for identity.active_organization_set.v1'` — switch active org, poll.
    - For each BLOCKED row, do NOT add a test (it would fail; the gap is acknowledged in `audit-gap.md`).

    Edit `apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts`:
    - Add one `it()` block named `'emits tenant.id attribute on delivered counter for tenant-scoped events'`:
      - Spawn an OTel in-memory exporter (Vitest-mocked or via the existing test harness — check the file for how OTel is currently inspected in this spec).
      - Trigger a tenant-scoped event (e.g., provision a tenant which emits `tenancy.tenant_provisioned.v1`).
      - Wait for the dispatcher to flush.
      - Read the in-memory exporter's recorded metric points; assert at least one point on `outbox.delivered` (or whatever the metric name is) has attributes including `'tenant.id': <the tenantId>`.
    - Add one `it()` block named `'emits tenant.id=platform attribute for platform-level events'`:
      - Trigger a platform-level event (one where `envelope.tenantId === null` — e.g., a system-level audit event if one exists; otherwise mock by constructing an envelope with null tenantId and calling `appendToOutbox` directly).
      - Assert the metric point has `'tenant.id': 'platform'`.

    If reading the in-memory OTel exporter is not feasible with the current test harness, fall back to a less ideal but observable assertion: spy on the `Counter.add` method via Vitest's `vi.spyOn` on the dispatcher's `deliveredCounter` and assert it was called with the expected attributes — verify the file's existing approach first to choose the most consistent path.

  </action>
  <verify>
    <automated>pnpm --filter @resto/api test apps/api/test/e2e/identity-audit.e2e.spec.ts apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @resto/api test apps/api/test/e2e/identity-audit.e2e.spec.ts` exits 0; output shows new test cases for the closed audit gaps.
    - `pnpm --filter @resto/api test apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts` exits 0; new `tenant.id` attribute assertions pass.
    - The number of new `it()` blocks added matches the count of CLOSED rows in `audit-gap.md` (1:1 mapping enforced via review checklist).
  </acceptance_criteria>
  <done>
    Both e2e specs extended; new test cases prove the closed audit gaps and the OTel label emission.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                           |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Producer→outbox row       | `buildEnvelope` is now the SINGLE supported path to a well-formed envelope; eliminates the class of "literal correlationId lost the trace link" bugs. |
| OTel exporter→backend     | Metric attributes leave the process boundary; high-cardinality labels could DoS the backend at scale (D-05 cardinality ceiling).                      |
| BA hook surface→audit log | Some critical actions (role-change) may have no hookable surface; the gap is documented, not hidden.                                                  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                       | Disposition                      | Mitigation Plan                                                                                                                                                                                                                                                                                                                 |
| --------- | ---------------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01   | Repudiation            | Identity audit gap — account-takeover events not recorded → forensic gap during incident                        | mitigate                         | TEN-09 / D-14: `audit-gap.md` enumerates all 8 critical actions across tenancy + identity. For closeable gaps, PR 5 Tasks 1+2 wire the events through outbox → NATS → audit. For BLOCKED gaps (role-change without BA hook), explicit reason + deferral to AUTH-09 in Phase 3. E2E spec asserts each closed gap materializes.   |
| T-05-02   | Information Disclosure | OTel labels include PII (email, IP) → log retention may violate GDPR                                            | mitigate                         | TEN-10 / D-05: ONLY `'tenant.id'` (UUID) is added. No email, no IP, no PII. The HTTP interceptor uses `req.method` and `req.url` (route shape, not query strings or bodies). Verify in code review that interceptor does not log `req.body` or `req.headers.authorization`.                                                     |
| T-05-03   | Denial of Service      | Per-tenant labels at scale → metric cardinality explosion at backend                                            | accept (with documented ceiling) | D-05: cardinality ceiling = 50+ tenants is a known scaling gate; revisit metric series shape at that point. Documented inline in `outbox-dispatcher.service.ts` and `http-metrics.interceptor.ts` WHY-comments. No enforcement now; Phase 1 cannot afford to design dashboards/alerts.                                          |
| T-05-04   | Tampering              | Migration of 8 envelope sites accidentally drops a field from the payload (e.g., `tenantId` lost in copy-paste) | mitigate                         | `buildEnvelope` is generic over the contract's payload type; TypeScript enforces the payload shape matches the contract's Zod-derived `z.infer<>` type. Type errors surface at `pnpm typecheck`. Additionally, TEN-17 (`EventEnvelope.parse()` in `appendToOutbox` from PR 1) catches any envelope-shape regression at runtime. |
| T-05-05   | Repudiation            | A future engineer reads `audit-gap.md` and assumes role-change is wired (premature done)                        | mitigate                         | The BLOCKED row in the table explicitly says BLOCKED and points to AUTH-09 in Phase 3. Persona-skeptic's "premature done" risk addressed by NEVER adding a `it()` test case for BLOCKED gaps — failing test would be the only signal that someone mis-marked the row.                                                           |
| T-05-SC   | Tampering              | No package installs in this PR                                                                                  | accept                           | This PR modifies existing code and adds one new file (HTTP-metrics interceptor) + one doc. Zero new npm packages.                                                                                                                                                                                                               |

</threat_model>

<verification>
After all 6 tasks:

1. `grep -c "correlationId: randomUUID" apps/api/src/ -r` returns 0 (every literal is migrated or PR 4 disable-tagged sites are now eliminated).
2. `grep -c "no-restricted-syntax -- TEN-14 PR-5" apps/api/src/ -r` returns 0 (every PR 4 disable comment is removed).
3. `pnpm --filter @resto/api typecheck && pnpm --filter @resto/api lint` — both clean.
4. `pnpm --filter @resto/api test apps/api/test/e2e/identity-audit.e2e.spec.ts apps/api/test/e2e/outbox-dispatcher.e2e.spec.ts apps/api/test/e2e/tenancy-suspend.e2e.spec.ts apps/api/test/e2e/background-jobs.e2e.spec.ts` — all four phase-1 e2e specs green.
5. `cat .planning/phases/01-tenancy-hardening/audit-gap.md` — human-readable, 8 rows, BLOCKED rows have reasons.
   </verification>

<success_criteria>

- Phase Success Criterion 5 fully delivered: "All `EventEnvelope` construction goes through `buildEnvelope`" — proven by `grep -c "correlationId: randomUUID" apps/api/src/ -r` returning 0.
- TEN-09 fully delivered: audit gap analysis written + closed (or BLOCKED with reason) for all 8 critical actions in tenancy + identity.
- TEN-10 fully delivered: outbox metrics + HTTP request/error counters carry `'tenant.id'` label.
- TEN-14 fully delivered (call-site migration component): all 8 sites migrated; ESLint clean without disable comments.
  </success_criteria>

<output>
Create `.planning/phases/01-tenancy-hardening/01-05-SUMMARY.md` when done. List:
- All 8 migrated call sites with their event types.
- The closed and BLOCKED gaps from `audit-gap.md`.
- The new metric attribute confirmations.
- Test commands run and their results.
</output>
