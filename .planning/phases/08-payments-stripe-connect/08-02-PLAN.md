---
phase: 08-payments-stripe-connect
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/api/src/contexts/tenancy/domain/ports.ts
  - apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts
  - apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts
  - apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/tenancy/tenancy.module.ts
  - apps/api/src/config/env.schema.ts
  - apps/api/src/config/prod-guardrails.ts
  - apps/api/package.json
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx
  - apps/admin/src/lib/payments-api.ts
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-06]
user_setup:
  - service: stripe
    why: "Stripe Connect platform for restaurant↔guest direct charges (test mode for local build)"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys (test mode sk_test_…)"
      - name: STRIPE_CONNECT_CLIENT_ID
        source: "Stripe Dashboard → Connect → Settings (ca_… ; only if needed for account_link flows)"
      - name: STRIPE_APPLICATION_FEE_AMOUNT
        source: "Platform config — integer minor units, DEFAULT 0 (D-01/D-03; lever stays open)"
    dashboard_config:
      - task: "Enable Connect (Express) on the platform account in test mode + accept the Connect platform agreement"
        location: "Stripe Dashboard → Connect → Get started"

must_haves:
  truths:
    - "PAY-01: StripeConnectAdapter implements an expanded StripeConnectPort backed by the real stripe node SDK"
    - "PAY-02/PAY-03: operator can start Connect Express onboarding FROM ADMIN — a 'Connect Stripe / Accept payments' button in apps/admin calls POST /v1/tenancy/stripe-onboarding and redirects to the returned account_link URL"
    - "PAY-13/D-12: the admin payouts surface reflects KYC / can-accept-payments status (pending-KYC vs ready) from GET /v1/tenancy/stripe-status"
    - "D-02/PAY-06: createPaymentIntent uses a DIRECT charge on the connected account (stripeAccount request option), NOT a destination charge — restaurant is merchant-of-record, bears fees + disputes"
    - "D-03: application_fee_amount is read from STRIPE_APPLICATION_FEE_AMOUNT config (default 0), never a hardcoded 0"
    - "D-09: every paymentIntents.create and refunds.create call passes a deterministic Stripe idempotencyKey derived from a stable order/refund identifier"
    - "D-06: createPaymentIntentForOrder cancels/voids any prior in-flight PaymentIntent on the same order before creating a new one (double-charge guard substrate)"
  artifacts:
    - path: "apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts"
      provides: "real Stripe adapter: account create + account_link + createPaymentIntent (direct charge) + cancelPaymentIntent + createRefund, all idempotency-keyed, retry/timeout cloned from resend adapter"
      contains: "stripeAccount"
    - path: "apps/api/src/contexts/tenancy/domain/ports.ts"
      provides: "expanded StripeConnectPort interface"
      contains: "createPaymentIntent"
    - path: "apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx"
      provides: "admin Connect-Stripe onboarding button + KYC/can-accept-payments status surface (PAY-02/03/13)"
      contains: "onboardingUrl"
  key_links:
    - from: "stripe-connect.adapter.ts"
      to: "stripe.paymentIntents.create"
      via: "second arg { stripeAccount, idempotencyKey }"
      pattern: "stripeAccount"
    - from: "stripe-connect.adapter.ts"
      to: "STRIPE_APPLICATION_FEE_AMOUNT"
      via: "application_fee_amount from config (default 0)"
      pattern: "application_fee_amount"
    - from: "brands.$slug.payouts.tsx (admin)"
      to: "POST /v1/tenancy/stripe-onboarding"
      via: "button → apiFetch → window.location = onboardingUrl"
      pattern: "onboardingUrl"
---

<objective>
Replace `NoopStripeConnectAdapter` with a real Stripe Connect Express adapter implementing
an expanded `StripeConnectPort`, plus the onboarding service + controller AND the admin
surface that invokes it (B3). Use the Resend adapter as the reference implementation for the
failure model (bounded retry with jitter, hard timeout via Promise.race, idempotency keys
threaded into the SDK call). Charge model is DIRECT charges on the connected account (D-02):
the `stripeAccount` request option targets the tenant's account so the restaurant is
merchant-of-record and bears Stripe fees + dispute liability; `application_fee_amount` is read
from config (default 0, D-03), never hardcoded. The admin onboarding button lives on the
existing `brands.$slug.payouts.tsx` route: it calls the onboarding endpoint, redirects to the
Stripe-hosted account_link, and reflects KYC/can-accept-payments status (PAY-02/03/13).

Purpose: stand up the outbound Stripe call surface (onboarding, PaymentIntent create/cancel,
refund) with money-correct idempotency AND give the operator a real way to start onboarding
from the admin panel before the webhook + checkout slices wire it in.
Output: PAY-01/02/03 onboarding (API + admin UI) + the createPaymentIntent/createRefund
methods that 08-03 (webhooks), 08-04a (checkout), 08-05 (refunds) consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-payments-stripe-connect/08-CONTEXT.md
@.planning/phases/08-payments-stripe-connect/08-01-SUMMARY.md

<interfaces>
<!-- Reference implementation + the port to expand + the admin surface. -->

From apps/api/src/contexts/identity/infrastructure/email/resend.adapter.ts (CLONE the failure model):
- RETRY_DELAYS_MS = [0, 250, 1000, 4000]; SEND_TIMEOUT_MS = 5500; jitter(); isRetryable(statusCode)
  (null/5xx retryable, 4xx terminal); Promise.race timeout with finally clearTimeout (WR-11);
  idempotencyKey threaded into the SDK call; terminal failure → log + rethrow.
- Pattern: a `*ClientLike` interface captures the minimal SDK surface so tests inject a mock; a
  factory wraps the real SDK. Mirror this with a `StripeClientLike`.

From apps/api/src/contexts/tenancy/domain/ports.ts (the port to EXPAND):
- export interface StripeConnectPort { ensureExpressAccount(input): Promise<string|null> }  // Noop today
- export const STRIPE_CONNECT_PORT = Symbol('STRIPE_CONNECT_PORT')

From apps/api/src/contexts/tenancy/application/provision-tenant.service.ts:
- already injects STRIPE_CONNECT_PORT and calls ensureExpressAccount during provisioning — keep that
  call working (it may now create a real Express account or stay a deferred no-op until the operator
  explicitly onboards; pick: provisioning creates the Express account, onboarding generates the link).

Admin surface (apps/admin — Vite SPA, @tanstack/react-router + TanStack Query):
- apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx — TODAY a placeholder
  PageHeading "Brand Payouts" + a "ships with RES-92" note. This is the surface to wire the onboarding button into.
- apps/admin/src/lib/api-client.ts — `apiFetch<T>(path, { method, body, brandSlug, signal })` returns { status, ok, data };
  injects x-tenant-id from the active org session + optional x-brand-slug; 401 → signOut + redirect. Use it for the POST/GET.
- apps/admin/src/lib/query-client.ts — TanStack Query client; components use useQuery/useMutation.
- Pattern: routes use createRoute + a component; PageHeading from '@/components/page-heading'; shadcn Button from the ui components.

Stripe node SDK specifics (verified via Stripe docs, 2026-06):
- Direct charge: stripe.paymentIntents.create({ amount, currency, application_fee_amount, … }, { stripeAccount: connectedAccountId, idempotencyKey }).
  On a DIRECT charge the connected account bears Stripe fees + dispute liability; application_fee transfers to the platform.
- Onboarding: stripe.accounts.create({ type: 'express', … }) then stripe.accountLinks.create({ account, refresh_url, return_url, type: 'account_onboarding' }).
- Refund: stripe.refunds.create({ payment_intent | charge, amount, reason }, { stripeAccount, idempotencyKey }).
- Cancel: stripe.paymentIntents.cancel(id, undefined, { stripeAccount, idempotencyKey }).

From apps/api/src/config/prod-guardrails.ts: assertProdGuardrails rejects dev-fallback/placeholder secrets in non-dev/test — extend it for STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET.
</interfaces>

@apps/api/src/contexts/identity/infrastructure/email/resend.adapter.ts
@apps/api/src/contexts/tenancy/domain/ports.ts
@apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts
@apps/api/src/contexts/tenancy/tenancy.module.ts
@apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx
@apps/admin/src/lib/api-client.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: verify + install the stripe npm package (legitimacy gate)</name>
  <action>Human verifies stripe on npmjs.com then installs the pinned version. NEVER auto-approvable. Steps in how-to-verify.</action>
  <what-built>The `stripe` npm package is required to build the adapter. It is [ASSUMED] until verified (no RESEARCH.md Package Legitimacy Audit exists for this phase — fallback policy treats all package-manager installs as ASSUMED). This checkpoint is NEVER auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/stripe and confirm: publisher is `stripe` (the official org), weekly downloads in the millions, repository github.com/stripe/stripe-node, recent maintenance.
    2. Confirm the version to pin is the current stable major compatible with Node 22 (record it).
    3. Only after confirming, install: `cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api add stripe@<pinned-version>`.
  </how-to-verify>
  <resume-signal>Type "verified stripe@<version>" once the npm page checks out and the package is installed, or report a discrepancy.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: expand StripeConnectPort + real StripeConnectAdapter (onboarding + PI + refund, idempotency keys)</name>
  <read_first>
    - apps/api/src/contexts/tenancy/domain/ports.ts (StripeConnectPort + STRIPE_CONNECT_PORT symbol)
    - apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts (the Noop to replace; strip the stale ADR-0009/0010 comment)
    - apps/api/src/contexts/identity/infrastructure/email/resend.adapter.ts (retry/timeout/idempotency/ClientLike/factory pattern to clone)
    - apps/api/src/config/env.schema.ts (where to add STRIPE_SECRET_KEY / STRIPE_APPLICATION_FEE_AMOUNT / STRIPE_CONNECT_* )
  </read_first>
  <behavior>
    - Port methods (Promise-returning, all amounts in minor units, currency uppercase ISO):
      createExpressAccount({ tenantId, displayName, country, defaultCurrency }) → { accountId }.
      createAccountLink({ accountId, refreshUrl, returnUrl }) → { url, expiresAt }.
      createPaymentIntent({ orderId, connectedAccountId, amountMinor, currency, applicationFeeMinor, metadata }) → { paymentIntentId, clientSecret, status }.  // DIRECT charge: stripeAccount option = connectedAccountId
      cancelPaymentIntent({ paymentIntentId, connectedAccountId, reason }) → { status }.
      createRefund({ paymentIntentId, connectedAccountId, amountMinor, reason, refundRequestId }) → { stripeRefundId, status }.
      retrieveAccount({ accountId }) → { chargesEnabled, payoutsEnabled, requirementsDue }.  // for account.updated reconcile + boot ping
    - Idempotency (D-09): paymentIntents.create idempotencyKey = `pi:${orderId}:${attempt}` (attempt supplied by caller so retry-on-same-order makes a NEW key but the adapter's own retry budget reuses the SAME key); refunds.create idempotencyKey = `refund:${refundRequestId}`.
    - Direct charge (D-02): the second SDK arg always carries `{ stripeAccount: connectedAccountId, idempotencyKey }`. NEVER set transfer_data.destination (that is destination charge — forbidden by D-02).
    - application_fee_amount comes from the caller's applicationFeeMinor (sourced from STRIPE_APPLICATION_FEE_AMOUNT config, default 0) — adapter passes it through, never hardcodes 0.
    - Retry/timeout: clone resend's RETRY_DELAYS_MS + SEND_TIMEOUT_MS + Promise.race + isRetryable; Stripe SDK errors expose statusCode — 4xx terminal, 5xx/network retryable. The idempotency key makes retry-after-timeout safe.
  </behavior>
  <action>
    Expand the StripeConnectPort interface in ports.ts with the six methods in <behavior>; drop the stale "MVP-2 / ADR-0009/0010" docstring. Rewrite stripe-connect.adapter.ts as `StripeConnectAdapter implements StripeConnectPort`: define a `StripeClientLike` interface capturing only the SDK calls used (accounts.create/retrieve, accountLinks.create, paymentIntents.create/cancel, refunds.create), clone the resend retry/timeout helpers, and a `createStripeClientAdapter(stripe, env, logger)` factory wrapping the real SDK. Each method builds the request payload + the second-arg options `{ stripeAccount, idempotencyKey }` per <behavior>. Keep `ensureExpressAccount` as a thin shim over createExpressAccount so provision-tenant.service.ts keeps compiling (or update that service to call createExpressAccount — minimal edit, no behavior regression).
    Add to env.schema.ts: STRIPE_SECRET_KEY (z.string(), required in non-dev with a dev fallback), STRIPE_APPLICATION_FEE_AMOUNT (z.coerce.number().int().nonnegative().default(0) — D-03), STRIPE_CONNECT_RETURN_URL + STRIPE_CONNECT_REFRESH_URL (z.string().url()), and STRIPE_WEBHOOK_SECRET (consumed in 08-03 but declare here). Extend prod-guardrails.ts so STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET are rejected when equal to a known dev-fallback/placeholder in staging/production. Ensure the db logger redact list (packages/db/src/logger.ts) covers a 'stripe-signature' / secret key — verify it already redacts 'token'; add 'stripe_signature' if missing.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api typecheck && pnpm --filter @resto/api vitest run apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit test (mock StripeClientLike): createPaymentIntent calls paymentIntents.create with second arg `{ stripeAccount: <connectedAccountId>, idempotencyKey: 'pi:<orderId>:<attempt>' }` and application_fee_amount equal to the supplied applicationFeeMinor (assert 0 when fee config is 0).
    - Unit test: the create call NEVER includes transfer_data.destination (assert absence — proves direct charge, D-02).
    - Unit test: createRefund passes idempotencyKey `refund:<refundRequestId>` and stripeAccount.
    - Unit test: a 5xx then success retries with the SAME idempotency key (no double create); a 4xx is terminal (no retry, throws).
    - typecheck exits 0.
  </acceptance_criteria>
  <done>Real adapter exists with direct-charge PI, config-driven fee, deterministic idempotency keys, resend-style retry/timeout; specs green.</done>
</task>

<task type="auto">
  <name>Task 2: onboarding service + controller (+ stripe-status read) + module wiring + boot ping</name>
  <read_first>
    - apps/api/src/contexts/tenancy/tenancy.module.ts (swap NoopStripeConnectAdapter → StripeConnectAdapter provider; wire the SDK factory)
    - apps/api/src/contexts/tenancy/application/provision-tenant.service.ts (existing STRIPE_CONNECT_PORT consumer)
    - apps/api/src/contexts/tenancy/interfaces/http (controller + error-mapping conventions; @RequireActiveTenant + Permissions guards)
    - apps/api/src/main.ts (assertEmailAdapterWired-style boot ping precedent — add a Stripe key ping)
    - apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts (canAcceptPayments from 08-01; onboarding status updates land via 08-03 account.updated)
  </read_first>
  <behavior>
    - POST /v1/tenancy/stripe-onboarding (operator-authenticated, owner/admin permission, @RequireActiveTenant): ensures the tenant has an Express account (creates one via the adapter if stripeAccountId is null, persists it on the tenant), then returns a fresh account_link URL built from STRIPE_CONNECT_RETURN_URL/REFRESH_URL. Returns 200 { onboardingUrl }.
    - GET /v1/tenancy/stripe-status (operator-authenticated, owner/admin, @RequireActiveTenant): returns { onboardingStatus, chargesEnabled, payoutsEnabled, canAcceptPayments, requirementsDue? } read from the tenant aggregate (canAcceptPayments from 08-01) so the admin page (Task 3) can show pending-KYC vs ready. Read-only.
    - The provider in tenancy.module.ts becomes StripeConnectAdapter (via the SDK factory) in non-test envs; tests can still inject a mock through the port symbol.
    - Boot: when NODE_ENV ∈ {staging, production}, ping Stripe (adapter.retrieveAccount on the platform or stripe.accounts.list({limit:1}) equivalent) to surface a bad STRIPE_SECRET_KEY loudly before the first checkout — mirror the assertEmailAdapterWired/verifyTransport precedent. Skip in dev/test.
  </behavior>
  <action>
    Create start-stripe-onboarding.service.ts (single execute(input) method) that: reads the current tenant via the repository, if stripeAccountId is null calls adapter.createExpressAccount + persists via tenant aggregate + repository.save, then calls adapter.createAccountLink with the configured return/refresh URLs, returns { onboardingUrl }. Create stripe-onboarding.controller.ts at @Controller('v1/tenancy') with @Post('stripe-onboarding') AND @Get('stripe-status') (the read used by the admin page), the operator-auth + owner/admin Permissions decorators, wrapWith(mapTenancyError). Add any new domain error (e.g. StripeOnboardingFailedError) to error-mapping.ts → 502/409 as appropriate. Wire the StripeConnectAdapter provider + the SDK factory in tenancy.module.ts (replace NoopStripeConnectAdapter; keep the Noop class only if a test/dev fallback wants it under NATS_DISABLED-style gating — otherwise delete it). Add the boot ping in main.ts behind the NODE_ENV guard.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api typecheck && pnpm --filter @resto/api vitest run apps/api/src/contexts/tenancy</automated>
  </verify>
  <acceptance_criteria>
    - POST /v1/tenancy/stripe-onboarding returns an onboardingUrl (account_link) and persists a stripeAccountId when previously null (service/e2e test with a mocked port).
    - GET /v1/tenancy/stripe-status returns { onboardingStatus, canAcceptPayments, … } read from the tenant aggregate.
    - Both routes are operator-authenticated + owner/admin gated (not @Public).
    - tenancy.module.ts provides StripeConnectAdapter for STRIPE_CONNECT_PORT; the Noop is removed or test-only.
    - typecheck exits 0; tenancy context specs green.
  </acceptance_criteria>
  <done>Operator can initiate Connect onboarding and read can-accept-money status via the API; adapter wired; boot ping guards a bad key in non-dev.</done>
</task>

<task type="auto">
  <name>Task 3: admin Connect-Stripe onboarding button + KYC status surface (B3)</name>
  <read_first>
    - apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx (the placeholder route to wire the button into)
    - apps/admin/src/lib/api-client.ts (apiFetch signature + 401 handling + brandSlug header)
    - apps/admin/src/lib/query-client.ts (TanStack Query useQuery/useMutation conventions)
    - apps/admin/src/components/page-heading.tsx + a route that already uses a shadcn Button + useMutation (e.g. the team or settings route) for the loading/toast pattern
    - apps/admin/src/components/settings/two-factor-section.tsx (an existing section component that calls an endpoint + reflects status — clone its structure)
  </read_first>
  <behavior>
    - On the payouts page, a "Connect Stripe / Accept payments" button: on click it POSTs /v1/tenancy/stripe-onboarding via apiFetch, then on a 200 sets window.location.href = data.onboardingUrl (full-page redirect to the Stripe-hosted account_link). Disable the button + show a spinner while the mutation is in flight; surface an error toast/message on non-2xx.
    - KYC/can-accept-payments status: a useQuery on GET /v1/tenancy/stripe-status drives the UI — show "Payments active" (ready) when canAcceptPayments is true; "Onboarding pending — finish Stripe verification" with the Connect button when onboardingStatus is 'pending'/'not_started'; "Restricted — additional info required" (with requirementsDue if present) when 'restricted'. The button label adapts ("Connect Stripe" when not_started, "Continue onboarding" when pending/restricted).
    - All API calls go through apiFetch (which injects the active-org tenant header + handles 401). Keep the existing PageHeading "Brand Payouts".
  </behavior>
  <action>
    Add apps/admin/src/lib/payments-api.ts with two thin helpers over apiFetch: startStripeOnboarding() (POST /v1/tenancy/stripe-onboarding → { onboardingUrl }) and getStripeStatus() (GET /v1/tenancy/stripe-status → the status shape). Rewrite brands.$slug.payouts.tsx: replace the "ships with RES-92" placeholder with a status-aware section — a useQuery(['stripe-status'], getStripeStatus) that renders the ready/pending/restricted states per <behavior>, and a useMutation that calls startStripeOnboarding and on success does the full-page redirect to onboardingUrl. Use the shadcn Button + a small status badge; reuse the existing toast/error pattern. Do NOT hand-roll a fetch — go through apiFetch. Keep the brandSlug param usage (the page already reads useParams).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter admin typecheck && grep -q "stripe-onboarding" apps/admin/src/lib/payments-api.ts && grep -rq "onboardingUrl" "apps/admin/src/routes/(protected)/dashboard/\$brandSlug/brands.\$slug.payouts.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The payouts page renders a "Connect Stripe / Accept payments" button that calls POST /v1/tenancy/stripe-onboarding and redirects the browser to the returned onboardingUrl.
    - The page reflects KYC/can-accept-payments state from GET /v1/tenancy/stripe-status: a ready state (canAcceptPayments true), a pending-KYC state (button shown), and a restricted state.
    - All API calls go through apiFetch (grep shows no raw fetch in the new files); typecheck (`pnpm --filter admin typecheck`) exits 0.
  </acceptance_criteria>
  <done>The operator can start Stripe onboarding from the admin payouts page, gets redirected to Stripe, and sees pending-KYC vs ready status (PAY-02/03/13, B3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: admin onboarding click-through (test mode)</name>
  <action>Human clicks the admin Connect-Stripe button and confirms the redirect + status reflect correctly. Steps in how-to-verify.</action>
  <what-built>The admin payouts page (apps/admin) with a "Connect Stripe / Accept payments" button that calls POST /v1/tenancy/stripe-onboarding, redirects to the Stripe-hosted account_link, and reflects pending-KYC vs ready status from GET /v1/tenancy/stripe-status.</what-built>
  <how-to-verify>
    1. Start the dev stack + api + admin (`pnpm dev:up`, then run api + admin dev servers); set test-mode STRIPE_SECRET_KEY + STRIPE_CONNECT_RETURN_URL/REFRESH_URL.
    2. Log into admin as the tenant owner, open a brand → Payouts. Confirm the page shows a not-yet-connected state with a "Connect Stripe" button.
    3. Click the button → confirm the browser redirects to a Stripe-hosted Connect onboarding (account_link) URL.
    4. Complete Stripe's test onboarding, return to the app. Confirm account.updated (from 08-03, once that ships) flips the status; in the meantime confirm GET /v1/tenancy/stripe-status returns the persisted onboardingStatus and the page reflects pending vs ready.
  </how-to-verify>
  <resume-signal>Type "admin onboarding verified" once the button redirects to Stripe and the status surface reflects pending/ready, or describe what broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api → Stripe API | outbound money calls; secret key must never be logged; retries must not double-charge |
| operator → onboarding endpoint | only owner/admin of the active tenant may start onboarding |
| admin browser → onboarding/status endpoints | the admin button only triggers a server-authoritative onboarding; status is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Spoofing/EoP | onboarding endpoint | mitigate | operator-auth (better-auth session) + owner/admin Permissions guard + @RequireActiveTenant (not @Public); admin apiFetch injects the active-org tenant header |
| T-08-10 | Tampering | double PaymentIntent/Refund on retry | mitigate | deterministic Stripe idempotencyKey on every create call (D-09); retry budget reuses the same key |
| T-08-11 | Information disclosure | STRIPE_SECRET_KEY in logs | mitigate | env-only secret; prod-guardrails rejects placeholder in prod; db logger redact covers token/secret; never log the key; only the account_link URL reaches the admin browser |
| T-08-12 | Repudiation/financial | platform silently pays restaurant fees | mitigate | DIRECT charge (stripeAccount option, no transfer_data.destination) → connected account bears fees + disputes (D-02); unit test asserts no destination |
| T-08-SC | Tampering | `stripe` npm install | mitigate | blocking-human legitimacy checkpoint before install (npmjs.com/package/stripe); never auto-approvable |
</threat_model>

<verification>
- `pnpm --filter @resto/api typecheck` + `pnpm --filter admin typecheck` green; adapter + tenancy specs green.
- Unit tests prove direct-charge shape (stripeAccount, no destination), config-driven fee, idempotency keys, retry-with-same-key.
- Onboarding endpoint is operator-gated and returns an account_link; the admin button redirects to it and shows KYC status; human-verify confirms the click-through.
</verification>

<success_criteria>
- PAY-01/02/03 satisfied: real adapter implementing the port; operator onboarding from the admin panel → Stripe-hosted account_link; admin reflects KYC/can-accept-payments status (PAY-13 surface).
- PAY-06 reframed per D-02: PaymentIntent is a direct charge with a configurable application fee (default 0).
- D-09 idempotency keys + D-06 cancel-prior-PI capability present for the webhook/checkout slices to use.
</success_criteria>

<output>
Create `.planning/phases/08-payments-stripe-connect/08-02-SUMMARY.md` when done
</output>
