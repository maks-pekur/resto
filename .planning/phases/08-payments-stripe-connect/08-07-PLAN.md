---
phase: 08-payments-stripe-connect
plan: 07
type: execute
wave: 3
depends_on: ['08-01', '08-02']
files_modified:
  - apps/api/src/infrastructure/outbox-dispatcher.service.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/config/env.schema.ts
  - apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts
autonomous: true
requirements: [PAY-11, PAY-12]
user_setup: []

must_haves:
  truths:
    - 'D-14: OutboxDispatcherService exposes an outbox.is_leader OTel gauge reading 1 when this instance holds the advisory lock, 0 otherwise'
    - 'D-14: a leader that acquired the lock but never dispatched while the outbox has undelivered rows older than the threshold is reported NOT ready (false-negative fixed)'
    - "D-14: PAY-12's >30s SLA and the env default are reconciled (default lowered to 30_000ms or the SLA text updated — pick the 30_000 default)"
    - "PAY-11: the StripeAccountId Zod schema has .max(255) AND it is the schema actually used to parse the account.updated webhook payload (08-03 webhook DTO / shared type) — proven by a safeParse('a'.repeat(256)).success === false unit test"
  artifacts:
    - path: 'apps/api/src/infrastructure/outbox-dispatcher.service.ts'
      provides: 'outbox.is_leader observable gauge + lock-acquisition heartbeat'
      contains: 'is_leader'
    - path: 'apps/api/src/health/health.controller.ts'
      provides: 'stale-leader probe that consults backlog age, not just lastDispatchAt'
      contains: 'checkOutboxLeader'
    - path: 'apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts'
      provides: 'StripeAccountId Zod schema with .max(255) used by the onboarding input + the 08-03 webhook parse'
      contains: 'max(255)'
  key_links:
    - from: 'apps/api/src/infrastructure/outbox-dispatcher.service.ts'
      to: 'OTel meter'
      via: "createObservableGauge('outbox.is_leader')"
      pattern: 'createObservableGauge'
    - from: 'apps/api/src/health/health.controller.ts'
      to: 'outbox backlog age'
      via: 'query oldest undelivered outbox row when leader has null lastDispatchAt'
      pattern: 'outbox'
    - from: 'StripeAccountId schema'
      to: '08-03 account.updated payload parse'
      via: '.max(255) on the shared schema the webhook DTO references'
      pattern: 'StripeAccountId'
---

<objective>
Close the two genuine PAY-12 deltas (D-14) — the bulk of PAY-12 (advisory-lock leader
election, heartbeat, /readyz stale-leader drain) already shipped in 07.5-03 and must NOT be
re-planned — plus the PAY-11 Zod hardening. The deltas: (1) add the `outbox.is_leader` OTel
gauge (1/0), (2) fix the never-dispatched-leader false-negative in
`getOutboxLeaderHealth()`/`checkOutboxLeader()` so a wedged-at-startup leader with a
non-empty outbox is drained, and (3) reconcile the PAY-12 ">30s" SLA with the current
`OUTBOX_STALL_THRESHOLD_MS` 60_000ms default. PAY-11 adds a `.max(255)` bound on the
StripeAccountId schema AND ensures that bound is on the SAME schema the 08-03 webhook uses to
parse the `account.updated` payload (not an unused decorative schema), proven by a
safeParse-rejects-256-chars unit test (W5).

Purpose: a real Stripe-event flow must not silently stall behind a wedged outbox leader
(paid-order confirmation emails depend on the outbox publishing). PAY-11 closes a missing
length bound on the externally-supplied Stripe account id on the path that actually parses it.
Output: the gauge, the probe fix, the reconciled default, and an effective `.max(255)` on
StripeAccountId with a rejecting unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-payments-stripe-connect/08-CONTEXT.md

<interfaces>
<!-- Already-shipped seams (07.5-03). Extend, do not reinvent. -->

From apps/api/src/infrastructure/outbox-dispatcher.service.ts:

- private readonly meter = metrics.getMeter('resto.api.outbox') — already creates counters
  (outbox.delivered) + histogram (outbox.delivery_lag_ms) + claim_failures counter.
- isLeader(): boolean === (this.leaderConn !== null).
- getOutboxLeaderHealth(): { isLeader, lastDispatchAt: Date|null, staleMs: number|null }.
- lastDispatchAt is set only in wrapPublisherForMetrics on a SUCCESSFUL publish (the false-negative source).
- TenantAwareDb is injected (this.db) — usable to query outbox backlog age.

From apps/api/src/health/health.controller.ts:

- checkOutboxLeader(): non-leaders always ok; leader ok when staleMs===null OR staleMs<=threshold.
  The staleMs===null path is the false-negative (a leader that NEVER dispatched reports ok forever).
- OUTBOX_STALL_THRESHOLD_MS token injected; stallThresholdMs is the number.

From apps/api/src/config/env.schema.ts line ~49:

- OUTBOX_STALL_THRESHOLD_MS: z.coerce.number().int().positive().default(60_000) — PAY-12 says >30s.

From the schema package (outbox table): `schema.outboxEvents` exists in @resto/db with a
delivered/claim marker — query the oldest undelivered row's age. Confirm the exact column
names by reading packages/db/src/schema for the outbox table before querying.

PAY-11 / StripeAccountId (W5 — the bound must be EFFECTIVE):

- 08-03's webhook DTO (apps/api/src/contexts/payments/application/dto.ts) parses the account.updated payload, including
  event.account (the connected account id). That DTO is the actual untrusted-input parse path.
- 08-02's onboarding flow + tenant aggregate also carry stripeAccountId.
- The `.max(255)` must live on a SHARED StripeAccountId Zod schema that BOTH the 08-03 webhook DTO and the onboarding/
  tenant input reference — so the bound is enforced where the account id is actually parsed, not on a dead schema.
  </interfaces>

@apps/api/src/infrastructure/outbox-dispatcher.service.ts
@apps/api/src/health/health.controller.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outbox.is_leader gauge + lock-acquisition heartbeat + backlog-aware stale check</name>
  <read_first>
    - apps/api/src/infrastructure/outbox-dispatcher.service.ts (meter setup, leaderConn, lastDispatchAt, getOutboxLeaderHealth, tryAcquireLeadership)
    - apps/api/src/health/health.controller.ts (checkOutboxLeader — the staleMs===null false-negative)
    - apps/api/src/health/health.controller.spec.ts (existing leader-health tests — extend, keep green)
    - packages/db/src/schema (the outbox_events table column names — find the undelivered/claim/created_at columns to compute backlog age)
  </read_first>
  <behavior>
    - On lock acquisition (tryAcquireLeadership success, before startDispatcher), set lastDispatchAt = new Date() so the staleness clock starts immediately (closes the never-dispatched false-negative without a backlog query in the common case).
    - Register an OTel observable gauge `outbox.is_leader` via this.meter.createObservableGauge; its callback reports 1 when isLeader() else 0, labelled { 'tenant.id': 'platform' } to match the existing metric label convention.
    - getOutboxLeaderHealth stays the same shape; because lastDispatchAt is now seeded at acquisition, a wedged leader's staleMs grows from acquisition time.
    - checkOutboxLeader: a leader whose staleMs > threshold is NOT ready (existing path now reachable for the wedged-at-startup case). Additionally, when staleMs > threshold, the probe consults the outbox backlog: if there are zero undelivered rows it MAY stay ready (idle queue is healthy); if there are undelivered rows older than the threshold it is NOT ready. Implement the backlog query on TenantAwareDb (system context) reading the oldest undelivered outbox_events row age.
  </behavior>
  <action>
    Edit outbox-dispatcher.service.ts: add `private readonly isLeaderGauge = this.meter.createObservableGauge('outbox.is_leader', { description: 'Whether this instance holds the outbox dispatcher advisory lock (1/0)' })` and register an `addCallback` (or use the observable-result API) that observes `this.isLeader() ? 1 : 0` with the `{ 'tenant.id': 'platform' }` attribute. In tryAcquireLeadership, after `this.leaderConn = reserved` and before `this.startDispatcher(...)`, set `this.lastDispatchAt = new Date()`. Add a method `getOldestUndeliveredOutboxAgeMs(): Promise<number | null>` using `this.db.withoutTenant('outbox backlog age probe', tx => ...)` selecting the min created_at of undelivered outbox rows (use the real column names found while reading the schema) and returning the age in ms or null when empty.
    Edit health.controller.ts checkOutboxLeader: keep "non-leader → ok"; when isLeader and staleMs > threshold, call getOldestUndeliveredOutboxAgeMs — if it returns null (empty queue) return ok (idle leader is healthy), else return NOT ok with a detail noting backlog age. Keep the method async (Promise.all in readiness already awaits it).
    Extend health.controller.spec.ts with the wedged-at-startup case (leader, lastDispatchAt seeded at acquisition, staleMs>threshold, non-empty backlog → 503) and the idle-leader case (staleMs>threshold, empty backlog → ok).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api typecheck && pnpm --filter @resto/api vitest run apps/api/src/health/health.controller.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `outbox.is_leader` gauge is registered via createObservableGauge and observes 1 when leader, 0 otherwise.
    - lastDispatchAt is seeded at lock acquisition (grep shows the assignment inside tryAcquireLeadership).
    - Spec: wedged-at-startup leader with non-empty backlog → checkOutboxLeader not ready (503).
    - Spec: idle leader (stale but empty backlog) → ok.
    - typecheck exits 0.
  </acceptance_criteria>
  <done>The is_leader gauge exists, the never-dispatched false-negative is fixed via acquisition-time heartbeat + backlog-aware probe, specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcile 30s SLA default + PAY-11 effective StripeAccountId .max(255) (W5)</name>
  <read_first>
    - apps/api/src/config/env.schema.ts (OUTBOX_STALL_THRESHOLD_MS default 60_000 + its WHY-comment citing 30s)
    - apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts (where stripeAccountId is validated — find the Zod schema or branded type; if it is a plain string today, add the bound as a shared schema)
    - apps/api/src/contexts/tenancy/application (the onboarding/provision input DTO that carries stripeAccountId)
    - apps/api/src/contexts/payments/application/dto.ts (08-03 webhook DTO that parses event.account from account.updated — the ACTUAL untrusted-input parse path; it MUST reference the shared StripeAccountId schema)
  </read_first>
  <behavior>
    - OUTBOX_STALL_THRESHOLD_MS default becomes 30_000 (matches PAY-12 + ROADMAP SC-5 ">30s"); the WHY-comment is updated to state 30s is the SLA default.
    - A shared `StripeAccountId = z.string().max(255)` Zod schema (in the tenancy domain, next to the tenant value schemas) is the single source of truth for the connected-account id, and it is referenced by BOTH the onboarding/tenant input DTO AND the 08-03 webhook DTO's account field — so the .max(255) is enforced exactly where the externally-supplied account id is parsed (W5: not a dead/unused schema).
  </behavior>
  <action>
    Edit env.schema.ts: change `OUTBOX_STALL_THRESHOLD_MS` default from 60_000 to 30_000 and update its comment to reflect the 30s SLA. Create/extend a shared `StripeAccountId = z.string().max(255)` schema in the tenancy domain (next to the tenant value schemas) and reference it from the onboarding input DTO. Coordinate with 08-03: the webhook DTO in payments/application/dto.ts that parses event.account from account.updated MUST import and use this shared StripeAccountId schema for the account field (if 08-03 already inlined a plain string, replace it with this schema). Add a unit test (e.g. in the tenancy value-schema spec) asserting `StripeAccountId.safeParse('a'.repeat(256)).success === false` and `StripeAccountId.safeParse('acct_123').success === true`. Do NOT widen scope into the adapter.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && grep -q "30_000" apps/api/src/config/env.schema.ts && grep -rn "max(255)" apps/api/src/contexts/tenancy && pnpm --filter @resto/api typecheck && pnpm --filter @resto/api vitest run apps/api/src/contexts/tenancy</automated>
  </verify>
  <acceptance_criteria>
    - OUTBOX_STALL_THRESHOLD_MS default is 30_000.
    - A shared StripeAccountId Zod schema has `.max(255)` and is referenced by the onboarding input DTO AND the 08-03 account.updated webhook DTO (the actual parse path) — not a dead schema.
    - Unit test: `StripeAccountId.safeParse('a'.repeat(256)).success === false` and a valid `acct_…` parses true (W5).
    - typecheck exits 0.
  </acceptance_criteria>
  <done>SLA default reconciled to 30s; PAY-11 length bound is effective on the parse path the webhook uses and proven by a rejecting unit test; typecheck green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                         |
| --------------------------------------------- | ------------------------------------------------------------------- |
| webhook/onboarding → tenant.stripe_account_id | externally supplied account id is untrusted input                   |
| outbox leader → LB readiness                  | a wedged leader must drain itself so paid-order events keep flowing |

## STRIDE Threat Register

| Threat ID | Category          | Component                                              | Disposition     | Mitigation Plan                                                                                                                                                                |
| --------- | ----------------- | ------------------------------------------------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-08-06   | Denial of service | wedged outbox leader silently queues paid-order events | mitigate        | acquisition-time heartbeat + backlog-aware /readyz probe drains the wedged leader (D-14)                                                                                       |
| T-08-07   | Tampering         | oversized/garbage stripe_account_id                    | mitigate        | shared StripeAccountId z.string().max(255) referenced by the 08-03 webhook DTO + onboarding input (PAY-11) — effective on the parse path, proven by a rejecting unit test (W5) |
| T-08-08   | Repudiation       | no leader-state observability                          | accept→mitigate | outbox.is_leader gauge gives 1/0 visibility for dashboards/alerts                                                                                                              |

</threat_model>

<verification>
- `pnpm --filter @resto/api typecheck` green; health.controller.spec passes including the two new cases; the StripeAccountId rejecting unit test passes.
- grep confirms the gauge, the 30_000 default, and the .max(255) bound on the tenancy StripeAccountId schema.
</verification>

<success_criteria>

- PAY-12 is closed to the D-14 scope only (gauge + false-negative fix + SLA reconcile) — no re-implementation of leader election.
- PAY-11 length bound is in place AND effective on the webhook parse path, proven by safeParse rejecting 256 chars (W5).
  </success_criteria>

<output>
Create `.planning/phases/08-payments-stripe-connect/08-07-SUMMARY.md` when done
</output>
