---
phase: 08.2-brand-first-routing-and-brand-scoped-access-model
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/auth.ts
  - packages/db/migrations/0059_session_active_brand.sql
  - packages/db/migrations/meta/_journal.json
  - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts
  - apps/api/src/contexts/identity/application/set-active-brand.service.ts
  - apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts
  - apps/api/test/e2e/set-active-brand.e2e.spec.ts
autonomous: true
requirements: [D-01, D-02, D-09, D-12, D-13, SC-5]
must_haves:
  truths:
    - 'The active brand lives in the server session (session.activeBrandId), not a client cookie'
    - 'A client cannot write activeBrandId via better-auth /update-session (input:false)'
    - 'D-12: An owner can re-pin to any brand of the active tenant via POST /v1/identity/me/set-active-brand (no re-login)'
    - 'D-13: A non-owner can only re-pin to a brand in their member_brand_scope; out-of-scope returns 403; the re-pin is a silent session re-issue, not a password re-entry'
    - 'AuthGuard surfaces session.activeBrandId onto the request so downstream guards can read it'
  artifacts:
    - path: 'packages/db/src/schema/auth.ts'
      provides: 'session.activeBrandId column'
      contains: 'activeBrandId'
    - path: 'apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts'
      provides: 'session.additionalFields.activeBrandId input:false + set-active hook re-pin'
      contains: 'activeBrandId'
    - path: 'apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts'
      provides: 'POST /v1/identity/me/set-active-brand (owner + non-owner scope-validated re-pin)'
      exports: ['POST']
  key_links:
    - from: 'set-active-brand.service.ts'
      to: 'internalAdapter.updateSession'
      via: 'server-side write of activeBrandId'
      pattern: 'updateSession|activeBrandId'
    - from: 'set-active-brand.service.ts'
      to: 'member_brand_scope'
      via: 'non-owner scope re-validation before re-pin'
      pattern: 'findBrandScopeForMember|MEMBER_BRAND_SCOPE_READER'
---

<objective>
Give the "active brand" a tamper-resistant server-side home (D-09): add
`session.activeBrandId` as a better-auth `additionalField` with `input:false` (client cannot
forge it via `/update-session`, identical to the existing `requiresPasswordChange` field).
Add the server-side write paths: re-pin on `organization/set-active` (login/org-switch) and a
new `POST /v1/identity/me/set-active-brand` endpoint that owners use to free-switch (D-12) and
non-owners use to silently re-pin within their scope (D-13). Surface `activeBrandId` onto the
request in `AuthGuard` so Plan 05's reconciliation cross-check can read it.

Purpose: the founder explicitly rejected the forgeable-cookie approach; this is the server pin
the whole anti-tamper model (D-10) hangs off. Plan 05 (reconciliation) and Plan 04 (the
brand-switcher API wire) both depend on this endpoint + the request-surfaced field.

Output: schema column + migration 0059, BA config field + hook, set-active-brand
service+controller, AuthGuard surfacing, and an integration spec proving `input:false`
tamper-resistance + owner/non-owner re-pin semantics.

Scope guard (D-01/D-02): brand-level only. No BA teams plugin (RESEARCH: 2 new tables, wrong
tool). No `dynamicAccessControl`. `member_brand_scope.role` left untouched (skeptic LOW).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.2-brand-first-routing-and-brand-scoped-access-model/08.2-RESEARCH.md

<interfaces>
auth.config.ts (VERIFIED — copy these patterns):
  user.additionalFields.requiresPasswordChange = { type:'boolean', defaultValue:false, input:false, returned:true }  // L260-267
  session: { expiresIn, updateAge }  // L269-272 — ADD additionalFields.activeBrandId here
  databaseHooks.session.update.after  // L273-310 fires on POST /api/auth/organization/set-active (path === '/api/auth/organization/set-active') — extend to also set activeBrandId
  Write path: ctx.context.internalAdapter.updateSession(sessionToken, { activeBrandId })

packages/db/src/schema/auth.ts (VERIFIED): session table has activeOrganizationId text, NO activeBrandId.

auth.guard.ts (VERIFIED): getSession() at L79; principal built L91; principal.baseRole resolved
L138-140; session cast exposes activeOrganizationId (L99,193,215). req.principal set L143.
SURFACE activeBrandId similarly: cast session.session.activeBrandId and attach to req.

MemberBrandScopeReader port (reuse, do NOT modify here):
findBrandScopeForMember({ userId, tenantId: TenantId }): Promise<readonly string[] | null>
MEMBER_BRAND_SCOPE_READER symbol token.
NOTE: Plan 05 flips the null semantics; this plan only READS scope for the non-owner re-pin
validation and must treat null-for-owner correctly — gate the owner path on baseRole==='owner'
BEFORE the scope lookup (same ordering as the guard).

me-brands.controller pattern (VERIFIED): @Controller('v1/me'), @RequiresTenantContext(),
@Permissions, CurrentOperator() decorator, createZodDto + RestoZodValidationPipe per-param.
Latest migration after Plan 02's 0058 is 0059 (sequence; coordinate journal entry).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: session.activeBrandId column + BA additionalField (input:false) + set-active hook re-pin</name>
  <read_first>
    - packages/db/src/schema/auth.ts (session table — add the column next to activeOrganizationId)
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (L260-310: additionalFields pattern + databaseHooks.session.update.after)
    - packages/db/migrations/0022_tenant_guc_lock.sql (migration authoring/journal convention)
  </read_first>
  <action>
    Add `activeBrandId text` (nullable, soft-FK comment — BA does not enforce FK) to the session
    table in packages/db/src/schema/auth.ts. Author migration 0059_session_active_brand.sql (ADD
    COLUMN IF NOT EXISTS) + journal entry. In auth.config.ts add to the `session:` object an
    `additionalFields.activeBrandId = { type:'string', defaultValue:null, input:false,
    returned:true }` per D-09 — `input:false` makes BA reject client writes via `/update-session`
    (tamper-resistant by construction, same as requiresPasswordChange at L264). Extend the
    existing `databaseHooks.session.update.after` hook: when it fires on
    `/api/auth/organization/set-active`, resolve the member's first scoped brand (or first brand
    for an owner) for the new active org and set `activeBrandId` via
    `internalAdapter.updateSession` in the same update so a freshly-switched org lands on a valid
    pinned brand. Keep the hook's existing exact-path equality check (WR-07).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && grep -q "activeBrandId" packages/db/src/schema/auth.ts && grep -q "input: false" apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts && grep -q "0059" packages/db/migrations/meta/_journal.json && pnpm --filter @resto/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `session` schema has `activeBrandId` (grep auth.ts)
    - auth.config.ts `session.additionalFields.activeBrandId` has `input: false` and `returned: true`
    - migration 0059 + journal entry present
    - set-active hook sets activeBrandId on org-switch (grep auth.config.ts for activeBrandId in the hook block)
  </acceptance_criteria>
  <done>Column + tamper-resistant additionalField + login/org-switch re-pin landed; types green.</done>
</task>

<task type="auto">
  <name>Task 2: SetActiveBrandService + POST /v1/identity/me/set-active-brand (owner free-switch + non-owner scoped re-pin)</name>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/me-brands.controller.ts (controller/DTO/guard/decorator conventions to mirror)
    - apps/api/src/contexts/identity/application/list-my-brands.service.ts (scope reader usage + owner semantics)
    - apps/api/src/contexts/identity/application/ports/member-brand-scope-reader.port.ts (the reader contract)
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (internalAdapter.updateSession access path)
  </read_first>
  <action>
    Create SetActiveBrandService (application) with a single `execute({ userId, tenantId,
    baseRole, brandId })`. Ordering MUST mirror the guard (Pitfall 5): if `baseRole === 'owner'`,
    validate `brandId` belongs to the active tenant (any tenant brand passes) and re-pin. Else
    (non-owner): call the MEMBER_BRAND_SCOPE_READER and require `brandId` ∈ the member's scope;
    if not in scope (or empty scope) throw a domain error mapped to 403. On success, write
    `activeBrandId` via better-auth `internalAdapter.updateSession` (the only server-side write
    path) and return `{ slug }`. Create set-active-brand.controller.ts: `@Controller('v1/me')`,
    `POST set-active-brand`, `@RequiresTenantContext()`, body `{ brandId: z.string().uuid() }`
    via createZodDto + RestoZodValidationPipe, `@CurrentOperator()`. Per D-13 this is a silent
    session re-issue — NO password re-entry. Add the controller + service to the identity module
    wiring. Use the new error → 403 mapping in the context's error-mapping.ts (existence-friendly:
    out-of-scope brand returns the same 403 shape, not a distinguishing 404 here — the
    existence-hiding 404 is the request-path reconciliation in Plan 05).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && grep -q "set-active-brand" apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts && grep -q "updateSession" apps/api/src/contexts/identity/application/set-active-brand.service.ts && pnpm --filter @resto/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `POST /v1/identity/me/set-active-brand` route exists (grep controller for the path)
    - Owner-bypass check runs BEFORE the scope lookup (grep service for `baseRole` then scope reader ordering)
    - Non-owner out-of-scope brand → 403 (mapped from a domain error, asserted in Task 3 e2e)
    - Service writes `activeBrandId` via `updateSession` and returns `{ slug }`
    - `pnpm --filter @resto/api typecheck` passes
  </acceptance_criteria>
  <done>Endpoint live; owner free-switch + non-owner scoped silent re-pin implemented server-side.</done>
</task>

<task type="auto">
  <name>Task 3: Surface activeBrandId on request in AuthGuard + integration spec (input:false tamper + re-pin semantics)</name>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts (session cast L99/L193/L215; req.principal attach L143)
    - apps/api/test/e2e/brand-scope-guard.e2e.spec.ts (e2e harness: how a session/operator is set up)
    - apps/api/test/e2e/catalog-brand-scope.e2e.spec.ts (owner vs scoped-operator fixtures)
  </read_first>
  <action>
    In AuthGuard.canActivate, after getSession, cast `session.session.activeBrandId` (string |
    null) and attach it to the request object (e.g. `req.activeBrandId`) alongside `req.principal`
    — extend the FastifyRequest augmentation the same way `principal` is declared. This is the
    field Plan 05's reconciliation reads. Create apps/api/test/e2e/set-active-brand.e2e.spec.ts
    asserting: (a) a direct better-auth `/update-session` POST attempting to set `activeBrandId`
    does NOT change it (input:false tamper-resistance — verifies RESEARCH assumption A1);
    (b) owner POST /v1/identity/me/set-active-brand to any tenant brand returns 200 + slug and
    the session's activeBrandId updates; (c) non-owner POST to an in-scope brand re-pins (200);
    (d) non-owner POST to an out-of-scope brand returns 403 and does NOT change the pin.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api test:e2e -- set-active-brand</automated>
  </verify>
  <acceptance_criteria>
    - `req.activeBrandId` populated from session in AuthGuard (grep auth.guard.ts for activeBrandId)
    - e2e proves `/update-session` cannot set activeBrandId (input:false) — this resolves RESEARCH A1
    - e2e proves owner re-pin (200), non-owner in-scope re-pin (200), non-owner out-of-scope (403, pin unchanged)
    - suite green
  </acceptance_criteria>
  <done>activeBrandId on request; tamper + owner/non-owner re-pin semantics proven by integration test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                      |
| ---------------------------------- | ---------------------------------------------------------------- |
| client → /update-session           | client attempts to write the active-brand pin directly           |
| client → set-active-brand endpoint | non-owner attempts to pin a brand outside their scope            |
| session storage → guards           | downstream guards must read the server pin, never a client value |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-08.2-08 | Tampering              | client forges session.activeBrandId via /update-session         | mitigate    | BA additionalField `input:false`; e2e asserts the write is rejected (A1 resolved)                                      |
| T-08.2-09 | Elevation of privilege | non-owner re-pins to an out-of-scope brand via set-active-brand | mitigate    | service re-validates member_brand_scope before updateSession; out-of-scope → 403, pin unchanged (D-13)                 |
| T-08.2-10 | Spoofing               | downstream code trusts a client-supplied active brand           | mitigate    | AuthGuard surfaces ONLY the server-session activeBrandId onto req; x-brand-slug stays advisory (reconciled in Plan 05) |
| T-08.2-SC | Tampering              | npm/pip/cargo installs                                          | mitigate    | none — no new packages added this plan; slopcheck N/A                                                                  |

</threat_model>

<verification>
- `pnpm --filter @resto/api typecheck` + `pnpm --filter @resto/db typecheck` green
- `pnpm --filter @resto/api test:e2e -- set-active-brand` green
- session.activeBrandId exists in schema + migration 0059 applied
- AuthGuard surfaces activeBrandId on the request
</verification>

<success_criteria>

- D-09: active brand is server-session state, not forgeable (input:false proven by e2e)
- D-12: owner free-switches any tenant brand in-session, no re-login
- D-13: non-owner silent scoped re-pin (no password); out-of-scope rejected
- SC-5 mechanism in place (the SPA wiring lands in Plan 04)
  </success_criteria>

<output>
Create `.planning/phases/08.2-brand-first-routing-and-brand-scoped-access-model/08.2-03-SUMMARY.md` when done
</output>
