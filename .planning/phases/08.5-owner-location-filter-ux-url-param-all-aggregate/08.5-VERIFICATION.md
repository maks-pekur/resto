---
phase: 08.5-owner-location-filter-ux-url-param-all-aggregate
verified: 2026-08-10T00:13:55Z
status: passed
score: 19/19 decisions verified (D-01..D-19), both persona BLOCKs satisfied
overrides_applied: 0
deferred:
  - truth: "Wire LocationPermissionChecker as the live location-aware permission checker"
    addressed_in: "Not scheduled — explicit re-defer per D-11, no committed phase number yet"
    evidence: "08.5-CONTEXT.md D-11 + 08.5-03-SUMMARY.md: 'LocationPermissionChecker RE-DEFERRED — wiring requires PermissionsGuard to thread activeLocationId, and a blind swap would block all non-owner permission checks.' Confirmed unwired: PERMISSION_CHECKER token still binds BetterAuthPermissionChecker in identity-core.module.ts."
---

# Phase 08.5: Owner location filter UX (URL-param + all-aggregate) Verification Report

**Phase Goal:** Rework admin location selection from the 08.4 server-session pin + relogin model into an OWNER-ONLY URL search-param filter (`?location=all|<id>`). `all` = aggregate summary across all brand locations (dashboard + stop-list); a specific location filters dashboard/menu/all surfaces by it. Adds backend aggregate endpoints for the `all` case — resolves the documented 08.4 owner-brand-global stop-list gap (c). Reworks `LocationScopeGuard` usage to drop the session-pin-vs-request 404 comparison on the owner filter path (pure member-scope check remains). PRESERVES 08.4 D-10/D-11 for STAFF.

**Verified:** 2026-08-10T00:13:55Z
**Status:** passed
**Re-verification:** No — initial verification

**Methodology note:** Phase carries no formal REQ-IDs (confirmed: `.planning/REQUIREMENTS.md` has no `08.5` mapping — not orphaned, simply out of scope for this phase's requirement-tracking model). Per the task's explicit instruction, the requirement record is `08.5-CONTEXT.md`'s D-01..D-19 decisions plus the two persona BLOCKs and all HIGH items in `08.5-PERSONA-REVIEWS.md`. Every item below was checked against live source and by **running** the actual test suites (unit, e2e-docker, and a live Playwright browser smoke against real `apps/api` + `apps/admin` dev servers) — not by reading SUMMARY.md claims.

**Note on ROADMAP.md wording:** The ROADMAP.md Phase 08.5 goal text still literally says "drop the session-pin-vs-request 404 comparison" — this is the pre-persona-review seed wording that BLOCK-2/D-08 proved was a mis-read of the guard (the comparison is the STAFF isolation control and must stay). `08.5-CONTEXT.md` explicitly documents this correction ("CORRECTS seed D-4") and the shipped code implements the corrected, more secure behavior (guard untouched, fix is `@LocationNeutral` on the new aggregate endpoints). This is a stale-roadmap-copy issue, not an implementation gap — informational only.

## Goal Achievement

### Observable Truths (D-01..D-19 + persona BLOCKs)

| # | Decision | Status | Evidence |
|---|----------|--------|----------|
| 1 | D-01 — `?location=all\|<uuid>` zod-validated search param on `$brandSlug` | ✓ VERIFIED | `_layout.tsx`: `locationSearchSchema = z.object({ location: z.union([z.literal('all'), z.string().uuid()]).optional() })` + `validateSearch`. Inherited via `Route.useSearch()` in `use-effective-location.ts`. |
| 2 | D-02 — Filter is owner-only, backend-enforced | ✓ VERIFIED | See D-09/BLOCK-1 below — real backend gate, not UI-only. |
| 3 | D-03 — Default = first active location alphabetically; FLAG documented | ✓ VERIFIED | `use-effective-location.ts:52`: `defaultLocation = activeLocations[0]` after `localeCompare` sort; WHY-comment at lines 48-51 cites the D-03 FLAG verbatim. |
| 4 | D-04 — Owner: URL is sole source of truth; session pin no longer read | ✓ VERIFIED | `apiFetch` (api-client.ts) has zero `activeLocationId` references (grep confirmed empty); header derives solely from `opts.locationId`. |
| 5 | D-05 — `all` = merged totals, stop-list read-only; write endpoints stay location-scoped | ✓ VERIFIED | `StopListAggregateTable` renders no stop/unstop control (read-only notice only). `catalog.controller.ts:230-250`: `stopListAdd`/`stopListRemove` carry only `@RequireBrand()`, no `@LocationNeutral`. |
| 6 | D-06 — N/M badge: M = active locations at read time; archived excluded from N and M | ✓ VERIFIED | Ran `stop-list-aggregate.e2e.spec.ts` live: item stopped at locations A+B+D, D then archived → `totalActiveLocations=2`, `stoppedLocationCount=2` (D excluded from both). 6/6 tests pass. |
| 7 | D-07 — Aggregate scope = stop-list only; no silent-empty order counters | ✓ VERIFIED | `index.tsx` renders only `TodaysWidget` (stop-derived `stopCount`); no order-counter code path exists anywhere in the diff. |
| 8 | **D-08 / BLOCK-2** — Owner change is purely client-side; `location-scope.guard.ts` byte-unchanged; staff non-owner path intact | ✓ VERIFIED | `git log --oneline -- .../location-scope.guard.ts` shows last touches at `bbc33c8`/`e4c8ef7`, both **08.4** commits — zero commits in the 08.5 range (`670389a..HEAD`). Ran `location-isolation.e2e.spec.ts` live: **12/12 pass**, including staff `x-location-id != pin → 404` and staff out-of-scope → 403. |
| 9 | **D-09 / BLOCK-1** — Aggregate endpoints carry a real backend owner-only gate; non-owner gets 404 with no leak | ✓ VERIFIED | `catalog.controller.ts:418-431`: `stop-list/aggregate` carries `@Permissions @RequireBrand @LocationNeutral() @OwnerOnly()`. Ran `owner-only.guard.spec.ts` live (5/5 pass) and `stop-list-aggregate.e2e.spec.ts` live (6/6 pass) — the BLOCK-1 case explicitly asserts non-owner (scoped to location A, `menu:read`) → 404, `body.items === undefined`, response body does NOT contain `locationBId` or `'Aggregate Item'`. |
| 10 | D-10 — Owner single-location read validated server-side against active brand; existence-hiding 404 | ✓ VERIFIED | `get-stop-list.service.ts` D-10 check; e2e (live run): foreign-brand location → 404, archived location → 404, in-brand active location → 200. |
| 11 | D-11 — Does not depend on location-aware RBAC; `LocationPermissionChecker` re-deferred, not wired | ✓ VERIFIED | `identity-core.module.ts:344-345`: `PERMISSION_CHECKER` binds `BetterAuthPermissionChecker`, not `LocationPermissionChecker`. Re-defer explicitly recorded in CONTEXT.md/PERSONA-REVIEWS/08.5-03-SUMMARY. |
| 12 | D-12 — Exactly one location authority per role in `api-client.ts` | ✓ VERIFIED | `grep -n "activeLocationId" apps/admin/src/lib/api-client.ts` → **no matches**. `use-effective-location.ts` is the single hook; owner branches to URL, staff branches to `activeLocationIdQuery` pin, no fallback between them. |
| 13 | D-13 — Owner pin retired to no-op; staff pin + `resetActiveLocation` untouched | ✓ VERIFIED | `set-active-location.service.ts:34-38`: owner branch returns `{ locationId: null }`, no write, no scope lookup. `resetActiveLocation` in `set-active-brand.service.ts:56-66` unchanged (still calls `initialLocation.resolveForUserInBrand`). |
| 14 | D-14 — Non-owner `set-active-brand` returns 403 | ✓ VERIFIED | `set-active-brand.service.ts:42-44`: `if (input.baseRole !== 'owner') throw new NonOwnerBrandSwitchForbiddenError();` as first statement. e2e (live run) confirms 403 `identity.non_owner_brand_switch_forbidden` for both scoped and unscoped non-owner. |
| 15 | D-15 — Staff model unchanged (login-pick-location, one active location, re-login to switch) | ✓ VERIFIED | `location-isolation.e2e.spec.ts` 12/12 green — staff isolation cases untouched. |
| 16 | D-16 — Aggregate off edge cache; single set-based query; no new migration | ✓ VERIFIED | e2e (live run) confirms `Cache-Control: private, no-store`, no `Set-Cookie`. Repository uses one raw-tx `GROUP BY`. `git log 670389a..HEAD -- packages/db` → **empty** (zero commits touching migrations/schema). |
| 17 | **D-17 (HIGH)** — Zero `location.context_required` white-screens in `all`; real browser/e2e smoke required | ✓ VERIFIED | `apps/admin/e2e/adm-01-all-mode-smoke.spec.ts` is a genuinely non-mocked Playwright test: seeds via live `apps/api` HTTP calls, signs in through the real `/login` form, walks `?location=all` on dashboard + stop-list, switches to a concrete location. **Executed live** against real `apps/api` (:5001) + `apps/admin` (:4000) dev servers — passed 3 consecutive clean runs after eliminating dev-environment rate-limit noise from repeated verification requests (see Anti-Patterns/Notes section — this was an artifact of my own repeated testing in the same minute window, not an app defect; SUMMARY.md documents this exact caveat independently). |
| 18 | D-18 — Invalid `?location` fallback is client-only; server still 403/404s | ✓ VERIFIED | `use-effective-location.ts`: `needsFallback` → client `navigate({ replace: true })` to default; never sent to server as authority. D-10 confirms server independently 404s regardless of client behavior. |
| 19 | D-19 — Zero-locations owner → empty state + CTA, no default redirect | ✓ VERIFIED | `index.tsx`: `mode === 'none'` renders `EmptyState` + "Create a location" CTA linking to `/$brandSlug/locations`; switcher hides when `locations.length === 0`. |

**Score:** 19/19 decisions verified. Both persona BLOCKs (BLOCK-1/D-09, BLOCK-2/D-08) independently confirmed against source + live test execution, not SUMMARY claims.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Wire `LocationPermissionChecker` as the live location-aware permission checker (08.4 KNOWN GAP b) | Not yet scheduled — explicit re-defer, D-11 | `08.5-CONTEXT.md` D-11: "08.5 does NOT depend on location-aware RBAC... wire it OR record an explicit re-defer — do not dangle it a third phase." Re-defer recorded in `08.5-03-SUMMARY.md`. Confirmed unwired in source. This is an explicit, load-bearing scope decision of THIS phase, not a gap — flagged here for milestone-level tracking only. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/src/shared/auth/owner-only.decorator.ts` | `OWNER_ONLY_KEY` + `OwnerOnly()` | ✓ VERIFIED | Exists, matches `LocationNeutral` convention exactly. |
| `apps/api/src/contexts/identity/interfaces/http/guards/owner-only.guard.ts` | `OwnerOnlyGuard`, bare 404 for non-owner | ✓ VERIFIED | Confirmed source; unit spec 5/5 live-run green. |
| `apps/api/src/contexts/identity/identity-http.module.ts` | `OwnerOnlyGuard` as 5th `APP_GUARD` | ✓ VERIFIED | `AuthGuard → PermissionsGuard → BrandScopeGuard → LocationScopeGuard → OwnerOnlyGuard`, exact order. |
| `apps/api/src/contexts/catalog/application/get-stop-list-aggregate.service.ts` | Brand→active-locations resolution + aggregate read | ✓ VERIFIED | Confirmed; resolves `LOCATION_REPOSITORY.listForBrand`, filters `status==='active'`, never accepts caller-supplied location list. |
| `apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts` (`stop-list/aggregate`) | `@LocationNeutral @OwnerOnly`, private no-store | ✓ VERIFIED | Confirmed at lines 418-431; e2e live-run proves cache header + owner-only. |
| `apps/api/test/e2e/stop-list-aggregate.e2e.spec.ts` | BLOCK-1 no-leak, D-06, D-10, D-16 coverage | ✓ VERIFIED | 6/6 live-run pass. |
| `apps/admin/src/lib/hooks/use-effective-location.ts` | Single per-role location authority | ✓ VERIFIED | Confirmed source; owner→URL+default+fallback, staff→pin, no cross-fallback. |
| `apps/admin/src/components/location-switcher.tsx` | Pure URL filter, no round-trip | ✓ VERIFIED | `grep "location.reload\|set-active-location"` → empty; `switchTo` calls `navigate({ search })` only. |
| `apps/admin/src/components/menu/stop-list-aggregate-table.tsx` | Read-only N/M badge table | ✓ VERIFIED | Confirmed source: no stop/unstop control, per-item `Badge` with `stoppedLocationCount`/`totalActiveLocations`. |
| `apps/admin/e2e/adm-01-all-mode-smoke.spec.ts` | Real browser smoke, zero 403s in `all` | ✓ VERIFIED | Executed live 3x clean — see D-17 row. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `identity-http.module.ts` | `OwnerOnlyGuard` | `APP_GUARD` registration, 5th position | ✓ WIRED | Confirmed exact line order. |
| `catalog.controller.ts stopListAggregate` | `OwnerOnlyGuard` | `@OwnerOnly()` decorator | ✓ WIRED | Confirmed + proven by live e2e (404 for non-owner). |
| `get-stop-list-aggregate.service.ts` | `LOCATION_REPOSITORY.listForBrand` | brand→active-locations resolution | ✓ WIRED | Confirmed source; no caller-supplied location list accepted. |
| `use-effective-location.ts` | `Route.useSearch().location` | owner branch reads URL | ✓ WIRED | Confirmed source. |
| `api-client.ts` | `x-location-id` header | `opts.locationId` (pure, no session read) | ✓ WIRED | Confirmed via grep + source read. |
| `stopListAggregateQuery` | `/v1/catalog/stop-list/aggregate` | `apiFetch` with `locationId:'all'` | ✓ WIRED | Confirmed source + live e2e response shape match. |
| `location-switcher.tsx` | `?location` search param | `useNavigate` + search updater | ✓ WIRED | Confirmed source; proven live by Playwright smoke Step 3 (switch updates URL, page stays on current route after the navigate-target bug fix). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `StopListAggregateTable` (`stop-list.tsx`, `index.tsx`) | `aggregateResult.data.items` / `totalActiveLocations` | `stopListAggregateQuery` → `GET /v1/catalog/stop-list/aggregate` → `GetStopListAggregateService` → real `GROUP BY` over `menu_stop_list`, tenant+brand-bound | ✓ FLOWING | Live e2e proves real per-item counts (stoppedLocationCount=2) computed from actual seeded stop rows, not static/empty. |
| Dashboard `TodaysWidget` count | `stopCount` | `stopListQuery`/`stopListAggregateQuery` (mode-branched) | ✓ FLOWING | Live Playwright smoke asserts `todays-widget-count` text is `'1'` against a real seeded stopped item — not a hardcoded value. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| tsc clean (api) | `pnpm --filter @resto/api exec tsc -p tsconfig.json --noEmit` | exit 0 | ✓ PASS |
| tsc clean (admin) | `pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit` | exit 0 | ✓ PASS |
| OwnerOnlyGuard unit spec | `vitest run test/unit/identity/owner-only.guard.spec.ts` | 5/5 pass | ✓ PASS |
| Stop-list aggregate e2e (docker) | `vitest run test/e2e/stop-list-aggregate.e2e.spec.ts` | 6/6 pass | ✓ PASS |
| Location isolation e2e (docker) | `vitest run test/e2e/location-isolation.e2e.spec.ts` | 12/12 pass | ✓ PASS |
| set-active-brand e2e (docker) | `vitest run test/e2e/set-active-brand.e2e.spec.ts` | 8/10 pass (2 pre-existing `member_brand_scope` fixture failures, documented) | ✓ PASS (with documented pre-existing debt) |
| Admin unit suite | `vitest run` (apps/admin) | 44/44 pass | ✓ PASS |
| Identity+catalog unit suites (api) | `vitest run test/unit/identity/ test/unit/catalog/` | 323/324 pass (1 pre-existing unrelated Stripe env-guardrail flake) | ✓ PASS (with documented pre-existing debt) |
| D-17 browser smoke | `playwright test e2e/adm-01-all-mode-smoke.spec.ts` | 1/1 pass, 3 consecutive clean runs | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` convention used by this phase; verification used direct test-suite execution (unit/e2e/Playwright) instead, per the plans' own `<verify>` blocks.

### Pre-existing Test Debt (confirmed NOT caused by this phase — independently re-verified, not just trusted from deferred-items.md)

| Test | Failure | Independent confirmation |
|------|---------|---------------------------|
| `catalog-reads.e2e.spec.ts` (2 tests), `catalog-brand-read-isolation.e2e.spec.ts` (1 test) | 403 `location.context_required` | Re-ran the exact failing test (`GET /stop-list surfaces stoppedAt`) against a `git worktree` checked out at the **pre-08.5** commit (`038128d`, 08.4 completion) with the same docker stack — **identical 403 failure reproduced with zero 08.5 code present.** Confirms this predates the phase entirely (likely an 08.4-era fixture gap: `setupAuthedTenant` never creates a location or sends `x-location-id`). |
| `set-active-brand.e2e.spec.ts` — 2 tests (`non-owner in-scope: can re-pin`, `D-13: non-owner scoped to 2 brands...`) | `member_brand_scope` vs `member_location_scope` fixture mismatch | Reproduced live; matches `deferred-items.md`'s root-cause analysis exactly (fixtures seed the legacy table, already 403 before D-14 even fires). |
| `identity-boot-integration.spec.ts` (1 test) | `ProdGuardrailsError: STRIPE_CONNECT_RETURN_URL is unset` | Reproduced live; unrelated to identity/location/brand — a local-env Stripe guardrail gap, matches `deferred-items.md`. |

None of these are attributable to phase 08.5 — confirmed by direct reproduction against a pre-phase commit, not by trusting the SUMMARY's claim.

### Anti-Patterns Found

No blocking anti-patterns. Scanned all 39 files touched during the phase (`git diff --name-only 670389a..HEAD`) for `TBD|FIXME|XXX|HACK|PLACEHOLDER|coming soon|not yet implemented`: zero matches except two benign `console.warn('Docker not available — skipping.')` lines matching the existing repo convention for docker-gated e2e suites.

**Note (environmental, not a code defect):** The D-17 Playwright smoke initially failed twice during my live verification run with a 429 `rate-limit-exceeded` on `/api/auth/sign-in/email` and `/v1/me` — caused by my own repeated manual `curl`/test invocations against the same dev API process within the default rate-limit windows (`RATE_LIMIT_PUBLIC_PER_MIN=60`, `RATE_LIMIT_AUTH_SIGNIN_PER_MIN=10`). This is the exact caveat `08.5-05-SUMMARY.md` documents independently ("Local iteration briefly hit the default sign-in rate limit... not a defect in the test or the app"). After restarting the dev API with elevated rate-limit env vars (matching the pattern already used by the project's own vitest e2e harnesses), the smoke passed 3 consecutive clean runs. This is not a phase defect — it is inherent to any manual/local verification session that makes many requests in a short window, and does not indicate a genuine `location.context_required` regression.

### Human Verification Required

None. All 19 decisions plus both persona BLOCKs were verified by direct source inspection and live test/tool execution (tsc, vitest unit, vitest e2e against the real docker stack, and a live Playwright browser run against real `apps/api` + `apps/admin` dev servers) — no visual/UX judgment call was left unverified.

### Gaps Summary

No gaps. All 19 decisions in `08.5-CONTEXT.md`, both persona BLOCKs (BLOCK-1/D-09 backend owner-only gate, BLOCK-2/D-08 guard-untouched), and every HIGH item in `08.5-PERSONA-REVIEWS.md` are honored in the shipped code and proven by live-executed tests, not SUMMARY.md narrative. The three pre-existing test-debt clusters (catalog-reads owner-fixture gap, member_brand_scope legacy fixtures, Stripe env-guardrail flake) were independently re-confirmed as predating this phase (one via direct reproduction against a pre-phase git commit) and are correctly excluded from the phase's pass/fail determination per the task's explicit instruction.

---

_Verified: 2026-08-10T00:13:55Z_
_Verifier: Claude (gsd-verifier)_
