---
phase: 08.5-owner-location-filter-ux-url-param-all-aggregate
plan: 05
type: execute
wave: 4
depends_on: ['08.5-04']
files_modified:
  - apps/admin/src/components/location-switcher.tsx
  - apps/admin/src/components/app-sidebar.tsx
  - apps/admin/src/routes/(protected)/$brandSlug/index.tsx
  - apps/admin/src/routes/(protected)/$brandSlug/menu/stop-list.tsx
  - apps/admin/e2e/adm-01-all-mode-smoke.spec.ts
autonomous: true
requirements: [D-05, D-06, D-07, D-14, D-17, D-19]

must_haves:
  truths:
    - 'The location switcher is a pure URL filter: selecting a location/all calls navigate({ search: (prev) => ({...prev, location}) }) — NO apiFetch, NO server round-trip, NO window.location.reload (D-01/D-04).'
    - "The switcher offers an 'All locations' option (?location=all) plus each active location (?location=<id>)."
    - 'The dashboard and the dedicated stop-list page render correctly in `all` mode — aggregate branch, ZERO location.context_required 403s (closes 08.4 gap c; D-17).'
    - "In `all` the stop-list is READ-ONLY with a per-item 'stopped at N of M locations' badge; stop/unstop requires selecting a concrete location (D-05/D-06)."
    - 'The `all` dashboard renders only stop-list-derived counters — no empty/silent order counters (D-07).'
    - 'stop-list.tsx declares loaderDeps on ?location so the loader re-runs on param change (no stale data).'
    - 'A brand with zero locations (owner) shows an empty state + create-location CTA; the switcher is hidden (D-19).'
    - 'A real browser/e2e smoke in `all` mode asserts zero 403 / location.context_required responses across dashboard + stop-list (D-17, load-bearing).'
  artifacts:
    - path: 'apps/admin/src/components/location-switcher.tsx'
      provides: 'URL-filter switcher (navigate on select, no server round-trip)'
      contains: 'navigate'
    - path: 'apps/admin/src/routes/(protected)/$brandSlug/menu/stop-list.tsx'
      provides: 'loaderDeps + aggregate/single branch + N/M badge + read-only in all'
      contains: 'loaderDeps'
    - path: 'apps/admin/e2e/adm-01-all-mode-smoke.spec.ts'
      provides: 'Playwright all-mode smoke: no 403 on dashboard + stop-list'
      contains: 'location=all'
  key_links:
    - from: 'location-switcher.tsx'
      to: '?location search param'
      via: 'useNavigate + search updater'
      pattern: 'navigate'
    - from: 'index.tsx / stop-list.tsx'
      to: 'useEffectiveLocation + stopListAggregateQuery'
      via: 'all -> aggregate, single -> stopListQuery(locationId)'
      pattern: 'stopListAggregateQuery'
---

<objective>
Wire the surfaces to `?location` and turn the switcher into a pure client-side URL filter (D-01/D-04) — dropping the `apiFetch('/v1/me/set-active-location')` round-trip and the full-page `window.location.reload()`. Then complete the D-17 all-mode sweep: both reads a brand-global owner issues today — the dashboard `TodaysWidget` and the dedicated `menu/stop-list` page — get the aggregate branch, so neither white-screens (`location.context_required` 403) in `all`. This closes the documented 08.4 gap (c). Finally, ship the load-bearing verification: a REAL browser smoke in `all` mode asserting zero 403s (mocked-repo unit tests missed this exact bug class in 08.4).

Also: `all` stop-list is read-only with the N/M badge (D-05/D-06); the `all` dashboard renders only stop-list-derived counters (D-07 — no silent-empty order counters); a zero-locations owner gets an empty state + create-location CTA (D-19).

Purpose: deliver the owner-facing `all`/single filter UX end-to-end and prove it with a browser smoke.
Output: URL-filter switcher, sidebar threading, dashboard + stop-list aggregate branches, zero-locations empty state, and a Playwright all-mode smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-owner-location-filter-ux-url-param-all-aggregate/08.5-CONTEXT.md
@.planning/phases/08.5-owner-location-filter-ux-url-param-all-aggregate/08.5-RESEARCH.md
@.planning/phases/08.5-owner-location-filter-ux-url-param-all-aggregate/08.5-PATTERNS.md
@.planning/phases/08.5-owner-location-filter-ux-url-param-all-aggregate/08.5-PERSONA-REVIEWS.md

<interfaces>
<!-- Extracted from codebase + plan-04 outputs. -->

location-switcher.tsx today (26-84): useQueryClient + switchTo -> apiFetch('/v1/me/set-active-location') + qc.invalidate + window.location.reload(). Props: { isOwner, locations, activeLocationId }. Early-return when !isOwner || locations.length === 0.
Target: useNavigate (replace useQueryClient); switchTo(value) => navigate({ search: (prev) => ({ ...prev, location: value }) }); value = 'all' | <uuid>. Keep DropdownMenu / SidebarMenuButton / MapPin / lucide shell.

app-sidebar.tsx (43-53, 105-114): passes isOwner + locations + activeLocationId(session) into LocationSwitcher.
Target: switcher no longer needs the session activeLocationId; it reflects ?location. Provide the current effective selection to the trigger label (via ?location / useEffectiveLocation).

index.tsx (24-34): activeLocationIdQuery gating + stopListQuery(brandSlug); stopCount = items.length.
Target: use useEffectiveLocation; mode 'all' -> useQuery(stopListAggregateQuery(brandSlug)), stopCount = aggregate items.length; mode 'single' -> useQuery(stopListQuery(brandSlug, locationId)); mode 'none' -> empty-state CTA. Only stop-derived counters (D-07) — dashboard renders no order counters today (structurally D-07-safe).

stop-list.tsx (11-37): loader ensureQueryData(stopListQuery(brandSlug)) [unconditional]; useSuspenseQuery(stopListQuery(brandSlug)).
Target: loaderDeps: ({ search }) => ({ location: search.location }); loader branches deps.location==='all' ? stopListAggregateQuery(brandSlug) : stopListQuery(brandSlug, deps.location); page renders single vs aggregate (N/M badge, read-only in all).

Playwright harness: apps/admin/playwright.config.ts + apps/admin/e2e/adm-00-smoke-walk.spec.ts + e2e/fixtures/{operator-session,seed-tenants}.ts (owner session fixture already exists).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switcher becomes a URL filter (no round-trip) + sidebar threading + zero-locations (D-01/D-04/D-19)</name>
  <files>apps/admin/src/components/location-switcher.tsx, apps/admin/src/components/app-sidebar.tsx</files>
  <read_first>
    - apps/admin/src/components/location-switcher.tsx (full — the switchTo round-trip to remove)
    - apps/admin/src/components/app-sidebar.tsx (43-53 composition; 105-114 LocationSwitcher props)
    - apps/admin/src/lib/hooks/use-effective-location.ts (plan 04 — the effective selection source)
    - apps/admin/src/routes/(protected)/$brandSlug/_layout.tsx (plan 04 — the ?location search param + Route id)
  </read_first>
  <action>
    Rework `location-switcher.tsx`: replace `useQueryClient` with `useNavigate` (TanStack). `switchTo(value: 'all' | string)` calls `navigate({ search: (prev) => ({ ...prev, location: value }) })` — preserve other search params via the updater form. Remove `apiFetch`, `toast`, `qc.invalidateQueries`, and `window.location.reload()`. Keep the DropdownMenu/SidebarMenuButton/MapPin shell and the `if (!isOwner || locations.length === 0) return null;` early return. Add an "All locations" item mapping to `'all'` (replaces the old "Brand-global" item), then each active location mapping to its id. The trigger label reflects the current `?location` (name of the active location, or "All locations" when `all`).

    In `app-sidebar.tsx`: the switcher no longer needs the session `activeLocationIdQuery` — remove that prop path for the switcher (owner selection now comes from `?location`). Keep `isOwner` + `meLocationsQuery` (active locations). Ensure the switcher receives the active locations list and reads `?location` for its current selection.

    D-19 zero-locations: the switcher already hides when `locations.length === 0`; ensure no default-location redirect is attempted for a zero-location owner (the empty-state CTA lives on the dashboard in Task 2). Do not crash when locations is empty.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: `location-switcher.tsx` contains NO `apiFetch`, NO `window.location.reload`, NO `set-active-location` reference; selection calls `navigate({ search: (prev) => ({ ...prev, location }) })`.
    - source: the switcher renders an "All locations" option (-> `'all'`) and one item per active location (-> its id).
    - source: `app-sidebar.tsx` no longer feeds the session `activeLocationId` into the switcher for owner selection.
    - CLI: `grep -n "location.reload\|set-active-location" apps/admin/src/components/location-switcher.tsx` returns nothing.
    - CLI: `pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit` exits 0.
  </acceptance_criteria>
  <done>The switcher is a pure URL filter with an All-locations option and no server round-trip; the sidebar threads ?location; zero-locations doesn't crash.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard + stop-list aggregate branch, N/M badge, read-only all, D-07 counters, D-19 empty state</name>
  <files>apps/admin/src/routes/(protected)/$brandSlug/index.tsx, apps/admin/src/routes/(protected)/$brandSlug/menu/stop-list.tsx</files>
  <read_first>
    - apps/admin/src/routes/(protected)/$brandSlug/index.tsx (24-34 gating + TodaysWidget)
    - apps/admin/src/routes/(protected)/$brandSlug/menu/stop-list.tsx (11-37 loader + StopListTable + TodaysWidget)
    - apps/admin/src/components/menu/stop-list-table.tsx (how rows + stop/unstop actions render — for the read-only + N/M badge changes)
    - apps/admin/src/lib/hooks/use-effective-location.ts + apps/admin/src/lib/queries/catalog.ts (plan 04: useEffectiveLocation, stopListQuery(brandSlug, locationId), stopListAggregateQuery)
  </read_first>
  <action>
    `index.tsx`: replace the `activeLocationIdQuery` gating with `useEffectiveLocation`. mode `'all'` -> `useQuery(stopListAggregateQuery(brandSlug))`, `stopCount = aggregate.items.length`. mode `'single'` -> `useQuery(stopListQuery(brandSlug, locationId))`, `stopCount = items.length`. mode `'none'` (zero active locations, owner) -> render an empty state with a "create location" CTA linking to `/{brandSlug}/locations` (D-19), no stop widget. Render ONLY stop-derived counters — do NOT add any order counter (D-07; the dashboard renders none today, keep it that way).

    `stop-list.tsx`: add `loaderDeps: ({ search }) => ({ location: search.location })` and branch the loader: `deps.location === 'all' ? queryClient.ensureQueryData(stopListAggregateQuery(brandSlug)) : queryClient.ensureQueryData(stopListQuery(brandSlug, deps.location))`. In the component, read the effective mode; when `all`, render the aggregate list with a per-item "stopped at N of M locations" badge (N = `stoppedLocationCount`, M = response `totalActiveLocations`, D-05/D-06) and make it READ-ONLY — the stop/unstop actions are hidden/disabled in `all`, with a hint to select a concrete location to edit (D-05). When `single`, render the existing `StopListTable` with stop/unstop actions unchanged. Resolve the effective `location` for the loader from the route search (default resolution is handled by the hook/component; the loader uses `deps.location` and treats `undefined` as the not-yet-resolved case — the component's `useEffectiveLocation` drives the actual query used for render).

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: `index.tsx` uses `useEffectiveLocation` and branches aggregate vs single; no `activeLocationIdQuery` gating remains; mode `none` renders a create-location CTA (D-19).
    - source: `index.tsx` renders no order counter (only stop-derived count — D-07).
    - source: `stop-list.tsx` declares `loaderDeps` on `search.location` and branches the loader between `stopListAggregateQuery` and `stopListQuery(brandSlug, location)`.
    - source: in `all` mode the stop-list shows a per-item N-of-M badge and hides/disables stop/unstop actions (read-only, D-05/D-06).
    - CLI: `pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both owner reads have an aggregate branch (no white-screen in all); the all stop-list is read-only with N/M badges; zero-locations shows a CTA; no empty order counters.</done>
</task>

<task type="auto">
  <name>Task 3: Real browser all-mode smoke — zero 403 / location.context_required (D-17, load-bearing)</name>
  <files>apps/admin/e2e/adm-01-all-mode-smoke.spec.ts</files>
  <read_first>
    - apps/admin/e2e/adm-00-smoke-walk.spec.ts (existing Playwright smoke shape — owner session, navigation)
    - apps/admin/e2e/fixtures/operator-session.ts + e2e/fixtures/seed-tenants.ts (owner session + seed fixtures)
    - apps/admin/playwright.config.ts (baseURL / webServer / how the stack is started)
  </read_first>
  <action>
    Create `apps/admin/e2e/adm-01-all-mode-smoke.spec.ts` (Playwright) using the existing owner-session + seed fixtures, seeded with a brand that has >=2 active locations and at least one stopped item. Attach a response listener (`page.on('response', ...)`) that FAILS the test if any `/v1/*` response has status 403 OR a JSON body code `location.context_required` during the walk. Walk:
      1. Owner visits `/{brand}?location=all` (dashboard) — assert the page renders (stop widget visible, no error boundary) and no 403 fired.
      2. Owner visits `/{brand}/menu/stop-list?location=all` — assert the aggregate stop-list renders with an N-of-M badge, no 403 fired.
      3. Owner selects a concrete location via the switcher (or navigates `?location=<id>`) — assert single-location stop-list renders and stop/unstop actions are present, no 403 fired.
    This is the D-17 requirement: a REAL browser smoke in `all` mode, not a mocked-repo unit test (project memory "verify feature, not call shape"). If the Playwright webServer cannot bring up a full api+admin stack in this environment, fall back to an api-level e2e that drives the same three GETs (aggregate + single) with an owner cookie and asserts zero 403/location.context_required — but PREFER the browser smoke.
  </action>
  <verify>
    <automated>pnpm --filter @resto/admin exec playwright test e2e/adm-01-all-mode-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e/browser: owner in `?location=all` loads the dashboard AND `/menu/stop-list` with ZERO responses of status 403 or body code `location.context_required` (D-17, closes 08.4 gap c).
    - e2e/browser: the `all` stop-list shows the N-of-M badge and no stop/unstop action; switching to a concrete location shows the stop/unstop actions (D-05).
    - CLI: `pnpm --filter @resto/admin exec playwright test e2e/adm-01-all-mode-smoke.spec.ts` exits 0 (or, if the browser harness is unavailable here, the fallback api-level e2e exits 0 and the SUMMARY records why the browser path was not runnable).
  </acceptance_criteria>
  <done>A real browser (preferred) all-mode smoke proves zero 403/context-required across the owner's dashboard + stop-list reads, closing the exact bug class 08.4 shipped.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                  |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| owner ?location=all -> aggregate render | The UI renders cross-location data for an owner; correctness of the owner-only property is enforced at the backend (plans 01/03), the UI merely consumes it. |
| switcher -> URL                         | The switcher is now client-only navigation; it performs no privileged mutation (the old `set-active-location` round-trip is gone).                           |

## STRIDE Threat Register

| Threat ID | Category                                                | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                             |
| --------- | ------------------------------------------------------- | ------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-085-17  | Information Disclosure (API1 BOLA, verified end-to-end) | `all`-mode cross-location render for a non-owner | mitigate    | Backend `@OwnerOnly()` (plan 01/03) is the control; this plan's browser smoke additionally proves the owner path issues zero 403s and (implicitly, via plan 03 e2e) a non-owner is 404'd. UI affordance is NOT the control. |
| T-085-18  | Denial of Service / UX regression (white-screen)        | `all`-mode `location.context_required` 403       | mitigate    | D-17 — every owner `all`-mode read (dashboard + stop-list) has an aggregate/neutral branch; the browser smoke fails on any 403/context-required, catching the 08.4 bug class the mocked unit tests missed.                  |
| T-085-19  | Tampering (privileged client action)                    | switcher server round-trip removed               | mitigate    | Switcher performs no `apiFetch`/mutation; location selection is pure URL navigation, eliminating the `set-active-location` path from the owner UX entirely (D-01/D-04).                                                     |
| T-085-20  | Information Disclosure (empty/misleading data)          | `all` dashboard order counters                   | mitigate    | D-07 — dashboard renders only stop-derived counters; no silent-empty order counter is shipped in `all` (order feed is Phase 10).                                                                                            |

</threat_model>

<verification>
- `pnpm --filter @resto/admin exec tsc -p tsconfig.json --noEmit` exits 0.
- `grep -n "location.reload\|set-active-location" apps/admin/src/components/location-switcher.tsx` returns nothing.
- `pnpm --filter @resto/admin exec playwright test e2e/adm-01-all-mode-smoke.spec.ts` green (or documented fallback api e2e green).
</verification>

<success_criteria>

- Owner `all`/single filter works end-to-end with no server round-trip and no white-screens (closes 08.4 gap c, D-17).
- `all` stop-list is read-only with N/M badges (D-05/D-06); dashboard shows only stop-derived counters (D-07); zero-locations shows a CTA (D-19).
- A real browser smoke proves zero 403/location.context_required in `all` mode.
  </success_criteria>

<output>
Create `.planning/phases/08.5-owner-location-filter-ux-url-param-all-aggregate/08.5-05-SUMMARY.md` when done.
</output>
