---
phase: 08.4-location-scoped-access
plan: 11
type: execute
wave: 7
depends_on: [08.4-05, 08.4-06, 08.4-07, 08.4-08, 08.4-09, 08.4-10]
files_modified:
  - apps/api/test/e2e/location-isolation.e2e.spec.ts
autonomous: true
requirements: [D-04, D-05, D-10, D-11, D-17]

must_haves:
  truths:
    - 'A non-owner request for an out-of-scope location returns an existence-hiding 404 (session-vs-request mismatch).'
    - 'A non-owner request for an out-of-scope brand (no scoped location in it) returns 404/403.'
    - 'A non-owner cannot self-re-pin activeLocationId to a second location without re-login (403 location.already_pinned).'
    - 'The owner reaches every location/brand unrestricted (bypass).'
    - "Archiving a location that a staff member is scoped only to removes that member's access on the next request (default-deny), matching the blast-radius warning (D-17)."
  artifacts:
    - path: 'apps/api/test/e2e/location-isolation.e2e.spec.ts'
      provides: 'end-to-end proof of the location isolation boundary (success criterion #2)'
      contains: '404'
  key_links:
    - from: 'apps/api/test/e2e/location-isolation.e2e.spec.ts'
      to: 'LocationScopeGuard + SetActiveLocationService + archive'
      via: 'HTTP requests asserting 404/403/bypass/access-loss'
      pattern: 'location'
---

<objective>
Prove the phase's headline security guarantee end-to-end (ROADMAP success criterion #2): out-of-scope location/brand returns existence-hiding 404/403, staff cannot self-switch locations without re-login, the owner is unrestricted, and archiving a location removes scoped-only staff access. This is the last plan — it runs against the fully migrated DB with all guards, the pin, and the role model in place.

Purpose: A live regression net for the location isolation boundary that a future refactor cannot silently break.
Output: `location-isolation.e2e.spec.ts` covering the D-04/D-05/D-10/D-11/D-17 behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md

<interfaces>
- apps/api/test/e2e/brand-scope-guard.e2e.spec.ts + brand-isolation.e2e.spec.ts — the closest analogs: how e2e seeds tenants/brands/members, signs in an operator, sets active scope, and asserts 404/403 for out-of-scope requests. Mirror their bootstrapping + request helpers.
- The 08.2 SC-4 isolation e2e (referenced in ROADMAP 08.2-06) — the forge-cookie/URL -> existence-hiding 404/403 pattern to extend to location grain.
- Endpoints under test: a location-scoped operator route (e.g. stop-list or a locations-scoped read), `POST /v1/me/set-active-location`, `GET /v1/me/locations`, tenancy locations create/archive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Out-of-scope 404/403 + owner-bypass + brand-derivation e2e</name>
  <files>apps/api/test/e2e/location-isolation.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/brand-scope-guard.e2e.spec.ts (seeding + sign-in + set-active + assert-404 helpers)
    - apps/api/test/e2e/brand-isolation.e2e.spec.ts (cross-scope read matrix)
    - .planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md (D-04/D-05 existence-hiding; success criterion #2)
  </read_first>
  <action>
    Create `location-isolation.e2e.spec.ts`. Seed one tenant with one brand and two locations (A, B); create a staff member scoped only to location A with a role; create an owner. Assert: (1) the staff, pinned to A, requesting a location-scoped route with a forged `x-location-id`/context for B returns an existence-hiding 404 (session-vs-request mismatch, not 403); (2) the staff has no scoped location in a second brand (or a zero-location brand) -> that brand is unreachable (404/403); (3) the owner reaches both locations and both brands unrestricted; (4) `GET /v1/me/locations` for the staff returns only location A. Use the analog e2e's sign-in + request helpers.
  </action>
  <acceptance_criteria>
    - The spec asserts a non-owner out-of-scope location request returns status 404 (existence-hiding), not 403.
    - The spec asserts the owner reaches both locations/brands (200-class).
    - The spec asserts `GET /v1/me/locations` for the staff returns only their scoped location.
    - `pnpm --filter @resto/api test:e2e -- location-isolation` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api test:e2e -- location-isolation</automated>
  </verify>
  <done>Out-of-scope location/brand returns existence-hiding 404/403; owner bypass proven; me-locations scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Staff no-self-switch + archive access-loss e2e</name>
  <files>apps/api/test/e2e/location-isolation.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/brand-scope-guard.e2e.spec.ts (set-active + re-pin request shape)
    - .planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md (D-10/D-11 re-login-to-switch; D-17 archive removes scoped-only access)
  </read_first>
  <action>
    Extend the spec with: (5) a staff member pinned to location A calling `POST /v1/me/set-active-location` for location B returns 403 `location.already_pinned` (server-enforced re-login-to-switch, D-11) — while an owner re-pinning succeeds; (6) after the owner archives location A, the staff member scoped only to A loses access on their next location-scoped request (default-deny, D-17) — confirming the "N staff will lose access" blast-radius warning was accurate. Keep assertions on observable HTTP status/behavior (not internal state).
  </action>
  <acceptance_criteria>
    - The spec asserts a non-owner re-pin to a different location returns 403 `location.already_pinned`.
    - The spec asserts an owner re-pin succeeds.
    - The spec asserts a staff member scoped only to an archived location loses access on the next request.
    - `pnpm --filter @resto/api test:e2e -- location-isolation` exits 0 (all cases).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api test:e2e -- location-isolation</automated>
  </verify>
  <done>Staff cannot self-switch (403); archive removes scoped-only access; full isolation e2e green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                             |
| ------------------------------------------- | --------------------------------------------------------------------------------------- |
| attacker (non-owner) -> location-scoped API | The e2e is the live proof that the boundary holds under forged scope + re-pin attempts. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                              | Disposition | Mitigation Plan                                                                                 |
| --------- | ---------------------------------- | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-084-32  | Information Disclosure / Elevation | out-of-scope location/brand read       | mitigate    | e2e asserts existence-hiding 404/403 (Task 1) — the live backstop for LocationScopeGuard + RLS. |
| T-084-33  | Elevation of Privilege             | staff self-switch without re-login     | mitigate    | e2e asserts 403 location.already_pinned on non-owner re-pin (Task 2).                           |
| T-084-34  | Elevation of Privilege             | archived-location staff retains access | mitigate    | e2e asserts access loss after archive (Task 2, D-17 default-deny).                              |

</threat_model>

<verification>
- `pnpm --filter @resto/api test:e2e -- location-isolation` green (all six behaviors).
- Run in an isolated/small batch (whole apps/api e2e in one process gives false 429/timeouts — MEMORY note).
</verification>

<success_criteria>

- ROADMAP 08.4 success criterion #2 proven by an isolation e2e: out-of-scope location/brand -> existence-hiding 404/403; staff login-pin + no self-switch; owner unrestricted; archive removes scoped-only access.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-11-SUMMARY.md` when done.
</output>
