---
phase: 08.4-location-scoped-access
plan: 09
type: execute
wave: 5
depends_on: [08.4-03, 08.4-04]
files_modified:
  - apps/admin/src/lib/api-client.ts
  - apps/admin/src/lib/queries/locations.ts
  - apps/admin/src/routes/(protected)/$brandSlug/locations.tsx
  - apps/admin/src/components/location-switcher.tsx
  - apps/admin/src/components/app-sidebar.tsx
  - apps/admin/src/routes/(auth)/pick-location.tsx
  - apps/admin/src/routes/(auth)/login.tsx
autonomous: false
requirements: [D-09, D-10, D-14, D-16, D-17]

must_haves:
  truths:
    - "Every admin API request echoes x-location-id from the current session's activeLocationId (mirrors x-brand-slug/x-tenant-id)."
    - "An owner can create, list, and archive a brand's locations at /{brand}/locations; archive shows the 'N staff will lose access' blast-radius before confirming."
    - 'The locationSwitcher (owner-only) switches within the active brand and offers a brand-global (all locations) option; staff see no switcher.'
    - 'A staff member with 2+ scoped locations is routed to a pick-location screen after login; picking one POSTs set-active-location then enters the dashboard.'
  artifacts:
    - path: 'apps/admin/src/routes/(protected)/$brandSlug/locations.tsx'
      provides: 'Locations CRUD page (D-14)'
      contains: 'locations'
    - path: 'apps/admin/src/components/location-switcher.tsx'
      provides: 'owner-only in-brand location switcher + brand-global (D-16)'
      contains: 'set-active-location'
    - path: 'apps/admin/src/routes/(auth)/pick-location.tsx'
      provides: 'staff login pick-location interstitial (D-10)'
      contains: 'set-active-location'
  key_links:
    - from: 'apps/admin/src/lib/api-client.ts'
      to: 'x-location-id header'
      via: 'echo session.activeLocationId on every request'
      pattern: 'x-location-id'
    - from: 'apps/admin/src/routes/(auth)/login.tsx'
      to: 'pick-location'
      via: 'activeLocationId null after setActive -> navigate to pick-location'
      pattern: 'pick-location'
---

<objective>
Ship the owner/staff location UX in the admin SPA: the `x-location-id` request echo, the Locations CRUD page (D-14) with a non-blocking archive + blast-radius warning (D-17), the owner-only `locationSwitcher` with a brand-global option (D-09/D-16), and the staff post-login pick-location interstitial (D-10). All authorization is re-verified server-side; this UI only reflects it. A follow-up UI-polish phase may refine layout (UI-SPEC skipped this phase).

Purpose: Make the location model usable by owners and staff without weakening the server boundary.
Output: Functional location management + switching + login pick, with `x-location-id` wired so location-scoped API calls reconcile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md

<interfaces>
- apps/admin/src/lib/api-client.ts — how x-brand-slug / x-tenant-id are attached (mirror for x-location-id from session.activeLocationId).
- apps/admin/src/routes/(protected)/$brandSlug/team.tsx:1-23 — createRoute shape (path/loader/component) for the sibling locations route.
- apps/admin/src/components/brand-switcher.tsx — DropdownMenu + POST-then-navigate; isOwner gate; add a brand-global item + gate the whole render on owner.
- apps/admin/src/routes/(auth)/login.tsx:52-55 — organization.setActive then navigate to dashboard (insert the activeLocationId-null check here).
- apps/admin/src/components/app-sidebar.tsx + nav-main.tsx — nav entry + switcher mount points.
- Backend endpoints consumed: tenancy `GET/POST /v1/brands/:slug/locations` + `POST :id/archive` (plan 04, returns scopedMemberCount); identity `POST /v1/me/set-active-location` + `GET /v1/me/locations` (plan 03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: x-location-id echo + locations query module + Locations CRUD page</name>
  <files>apps/admin/src/lib/api-client.ts, apps/admin/src/lib/queries/locations.ts, apps/admin/src/routes/(protected)/$brandSlug/locations.tsx, apps/admin/src/components/app-sidebar.tsx</files>
  <read_first>
    - apps/admin/src/lib/api-client.ts (x-brand-slug / x-tenant-id attachment)
    - apps/admin/src/lib/queries/tenancy.ts + queries/team.ts (query-hook conventions, AbortSignal.timeout usage)
    - apps/admin/src/routes/(protected)/$brandSlug/team.tsx (route + table layout)
    - apps/admin/src/components/app-sidebar.tsx + nav-main.tsx (nav entry pattern)
    - apps/admin/src/components/danger-zone-card.tsx (archive/confirm affordance pattern)
  </read_first>
  <action>
    In api-client.ts attach `x-location-id` from the current session's `activeLocationId` on every request when present (mirror the x-brand-slug/x-tenant-id attachment; include `AbortSignal.timeout(...)` on any new fetch — existing helper already does). Create `lib/queries/locations.ts` with TanStack Query hooks: `brandLocationsQuery(slug)` (list), `createLocationMutation`, `archiveLocationMutation` (returns scopedMemberCount), and `meLocationsQuery` (GET /v1/me/locations) — all via `apiFetch`. Create the `locations.tsx` route (sibling of team.tsx under `$brandSlug`, `path: '/locations'`) rendering a create form (name/address/timezone/contacts, client `.max` mirrors) + a table of the brand's locations with an archive action that first shows the returned `scopedMemberCount` as an "N staff will lose access" confirm (D-17) and does NOT block. Add a "Locations" nav entry to app-sidebar.tsx under the brand shell.
  </action>
  <acceptance_criteria>
    - api-client.ts sends `x-location-id` when the session has an activeLocationId.
    - lib/queries/locations.ts exports `brandLocationsQuery`, `createLocationMutation`, `archiveLocationMutation`, `meLocationsQuery`.
    - locations.tsx renders a create form + list; the archive flow surfaces the blast-radius count before confirming and is non-blocking.
    - app-sidebar.tsx has a Locations nav entry.
    - `pnpm --filter @resto/admin typecheck` exits 0 and `pnpm --filter @resto/admin build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/admin typecheck && pnpm --filter @resto/admin build</automated>
  </verify>
  <done>Owner can manage locations; archive warns on blast-radius; every request echoes x-location-id.</done>
</task>

<task type="auto">
  <name>Task 2: locationSwitcher (owner-only) + brand-global + pick-location interstitial + login flow</name>
  <files>apps/admin/src/components/location-switcher.tsx, apps/admin/src/components/app-sidebar.tsx, apps/admin/src/routes/(auth)/pick-location.tsx, apps/admin/src/routes/(auth)/login.tsx</files>
  <read_first>
    - apps/admin/src/components/brand-switcher.tsx (DropdownMenu + POST-then-navigate + isOwner)
    - apps/admin/src/routes/(auth)/login.tsx (organization.setActive -> navigate)
    - apps/admin/src/routes/(auth)/_layout.tsx + accept-invitation.$id.tsx (auth-route shape)
    - .planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md (Focus Area 8: switcher differences + pick-location has no precedent)
  </read_first>
  <action>
    Create `location-switcher.tsx` mirroring `brand-switcher.tsx` but: (a) lists locations of the CURRENT brand only via `meLocationsQuery`, (b) includes a "Brand-global (all locations)" item that POSTs `set-active-location` with a null/clear semantic, (c) gates the WHOLE component render on `principal.baseRole === 'owner'` (staff get no switcher, D-10) — this is a UX gate, not the boundary. Mount it in the sidebar next to the brand switcher. Create `pick-location.tsx` (new auth route) that lists the staff member's pinnable locations (`meLocationsQuery`) and, on selection, POSTs `set-active-location` then navigates to the brand dashboard. In login.tsx, after `organization.setActive`, read the session's `activeLocationId`; if the operator is a non-owner and `activeLocationId` is still null (staff with 2+ scoped locations), navigate to `/pick-location` instead of `/dashboard` (owners and single-location staff proceed directly).
  </action>
  <acceptance_criteria>
    - location-switcher.tsx returns null / does not render for non-owner principals.
    - location-switcher.tsx POSTs `/v1/me/set-active-location` and includes a brand-global (null) option.
    - pick-location.tsx lists pinnable locations and POSTs set-active-location on selection.
    - login.tsx routes non-owner + null activeLocationId to pick-location; owner/single-location proceeds to dashboard.
    - `pnpm --filter @resto/admin typecheck` exits 0 and `pnpm --filter @resto/admin build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/admin typecheck && pnpm --filter @resto/admin build</automated>
  </verify>
  <done>Owner switches locations silently (incl. brand-global); staff pick a location at login and cannot switch in-session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify owner/staff location UX</name>
  <what-built>Locations CRUD page (create/list/archive with blast-radius warning), owner-only locationSwitcher with a brand-global option, and the staff post-login pick-location interstitial; x-location-id echo wired.</what-built>
  <how-to-verify>
    1. As an OWNER: visit `/{brand}/locations`, create 2 locations, confirm they list; archive one and confirm the "N staff will lose access" count shows and archive completes (non-blocking).
    2. As the OWNER: use the locationSwitcher to switch between the two locations and to "Brand-global (all locations)" — confirm each switch is silent (no re-login) and the dashboard reflects the selection.
    3. As a STAFF member scoped to 2+ locations: log in and confirm you land on the pick-location screen; pick one, confirm you enter the dashboard and there is NO in-session location switcher.
    4. As that STAFF member: confirm switching locations requires logging out and back in (no self-service switch).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues observed.</resume-signal>
  <files>(no source files — human verification only)</files>
  <action>Pause execution and present the verification steps below to the user. Wait for explicit approval before completing the plan.</action>
  <verify><human-check>User confirms owner switching (incl. brand-global), staff login-pin with no in-session switch, and archive blast-radius warning all behave as described</human-check></verify>
  <done>User has typed "approved" (or rejected with specific issues) per resume-signal</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                                                                             |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| admin UI -> API | The UI only reflects authorization; every location decision is re-verified server-side (LocationScopeGuard / SetActiveLocationService). |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-084-28  | Elevation of Privilege | staff bypass the missing-switcher UI to self-switch       | mitigate    | Client gate is cosmetic; SetActiveLocationService's asymmetric one-time-pin rule (plan 03) is the real boundary; forged x-location-id -> LocationScopeGuard 404 (plan 05). |
| T-084-29  | Information Disclosure | switcher/pick screens list locations a user cannot access | mitigate    | Both consume GET /v1/me/locations which returns only pinnable locations (plan 03).                                                                                         |

</threat_model>

<verification>
- `pnpm --filter @resto/admin typecheck` + `pnpm --filter @resto/admin build` green.
- Human-verify checkpoint approved for owner switching + staff login-pin + archive blast-radius.
</verification>

<success_criteria>

- Owner-manageable locations UI, owner-only silent switcher (incl. brand-global), staff login pick + no in-session switch, x-location-id echoed on every request.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-09-SUMMARY.md` when done.
</output>
