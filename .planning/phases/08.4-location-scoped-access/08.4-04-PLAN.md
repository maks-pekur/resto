---
phase: 08.4-location-scoped-access
plan: 04
type: execute
wave: 2
depends_on: [08.4-01, 08.4-02]
files_modified:
  - apps/api/src/contexts/tenancy/domain/location.aggregate.ts
  - apps/api/src/contexts/tenancy/domain/ports.ts
  - apps/api/src/contexts/tenancy/domain/errors.ts
  - apps/api/src/contexts/tenancy/application/provision-location.service.ts
  - apps/api/src/contexts/tenancy/application/archive-location.service.ts
  - apps/api/src/contexts/tenancy/application/list-locations.service.ts
  - apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts
  - apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/tenancy/tenancy.module.ts
  - apps/api/test/unit/tenancy/provision-location.service.spec.ts
  - apps/api/test/unit/tenancy/archive-location.service.spec.ts
autonomous: true
requirements: [D-01, D-12, D-14, D-17]

must_haves:
  truths:
    - 'An owner can create a location under a brand (name, address, timezone, contacts) — no location is auto-created (D-12).'
    - "Locations can be listed for a brand and archived via soft-archive (status='archived'/archived_at); no hard delete."
    - 'Archiving a location returns the count of members scoped only to it (blast-radius) but does not block (D-17).'
    - 'Locations CRUD is exposed under the brand-first path and gated by brand scope + location permissions; validation caps free-text fields.'
  artifacts:
    - path: 'apps/api/src/contexts/tenancy/domain/location.aggregate.ts'
      provides: 'Location aggregate (snapshot + pullEvents) mirroring Brand'
      contains: 'class Location'
    - path: 'apps/api/src/contexts/tenancy/application/archive-location.service.ts'
      provides: 'soft-archive + blast-radius count (D-17)'
      contains: 'countScopedMembers'
    - path: 'apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts'
      provides: 'create/list/archive endpoints under /{brand}/locations'
      exports: ['POST', 'GET']
  key_links:
    - from: 'apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts'
      to: 'schema.locations'
      via: 'ScopedTx withTenant selectFrom/insertInto/updateTable'
      pattern: 'schema.locations'
    - from: 'apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts'
      to: 'ProvisionLocationService / ArchiveLocationService / ListLocationsService'
      via: '@Permissions({ location: [...] }) brand-scoped verbs'
      pattern: 'location'
---

<objective>
Build the tenancy-context Locations CRUD: the `Location` aggregate, repository, provision/list/archive services, and the brand-scoped controller under the brand-first shell (D-14). No location is auto-created (D-12); archive is soft and non-blocking with a blast-radius count (D-17).

Purpose: D-01/D-14 give owners a way to manage a brand's locations; the API is the surface the admin page (plan 09) renders and the default-location resolver (plan 06) reads.
Output: `/v1/brands/:slug/locations` (or the brand-first equivalent) create/list/archive endpoints with domain-error mapping and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md

<interfaces>
Copy the tenancy CRUD shapes (PATTERNS.md "Tenancy: Locations CRUD" + RESEARCH Focus Area 4):
- brand.aggregate.ts — snapshot + private ctor + fromSnapshot/toSnapshot/pullEvents.
- tenancy/domain/ports.ts:20-35 — BrandRepository / BRAND_REPOSITORY (drop domain-host methods; add listForBrand + countScopedMembers).
- provision-brand.service.ts — idempotent create shape.
- archive-tenant.service.ts — find -> mutate -> save archive shape.
- brand-drizzle.repository.ts — TenantScopedRepository + withTenant(scoped => selectFrom/insertInto/updateTable).
- tenants.controller.ts + brand-onboarding.controller.ts (@Permissions + @RequireActiveTenant + brand-scoped decorator stacking).
- tenancy/domain/errors.ts + interfaces/http/error-mapping.ts (mapDomainError).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Location aggregate, port, Drizzle repository</name>
  <files>apps/api/src/contexts/tenancy/domain/location.aggregate.ts, apps/api/src/contexts/tenancy/domain/ports.ts, apps/api/src/contexts/tenancy/domain/errors.ts, apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/domain/brand.aggregate.ts
    - apps/api/src/contexts/tenancy/domain/ports.ts
    - apps/api/src/contexts/tenancy/domain/errors.ts
    - apps/api/src/contexts/tenancy/infrastructure/brand-drizzle.repository.ts
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (Tenancy CRUD section + Shared Pattern 5)
  </read_first>
  <action>
    Create `LocationSnapshot` (id: LocationId, tenantId, brandId, name, address?, timezone?, contacts?, status: 'active'|'archived', createdAt/updatedAt/archivedAt) and the `Location` aggregate mirroring `Brand` (private ctor, fromSnapshot/toSnapshot, an `archive()` state transition pushing a domain event, pullEvents). Free-text fields (name/address/contacts) MUST carry Zod `.max()` bounds; `timezone` uses a loose IANA-zone regex (`/^[A-Za-z_]+\/[A-Za-z_]+$/` allowing `Etc/UTC`). Add `LocationRepository` + `LOCATION_REPOSITORY` Symbol to ports.ts with: `findById`, `listForBrand(brandId, tenantId)`, `save(snapshot)`, and `countScopedMembers(locationId): Promise<number>` (for D-17). Add `LocationNotFoundError` to errors.ts. Implement `LocationDrizzleRepository extends TenantScopedRepository` using `withTenant(scoped => ...)` with the mandatory `eq(schema.locations.tenantId, ctx.tenantId)` filter on top of RLS; `countScopedMembers` counts member_location_scope rows for the location.
  </action>
  <acceptance_criteria>
    - location.aggregate.ts exports `class Location` with `fromSnapshot`, `toSnapshot`, `archive`, `pullEvents`.
    - ports.ts exports `LOCATION_REPOSITORY` and a `LocationRepository` with `countScopedMembers`.
    - location-drizzle.repository.ts every select includes `eq(schema.locations.tenantId, ...)`.
    - Location free-text schema fields have `.max(...)`.
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api typecheck</automated>
  </verify>
  <done>Aggregate, port, and repository compile with the ScopedTx double-enforcement pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provision / list / archive services (no auto-create, blast-radius)</name>
  <files>apps/api/src/contexts/tenancy/application/provision-location.service.ts, apps/api/src/contexts/tenancy/application/list-locations.service.ts, apps/api/src/contexts/tenancy/application/archive-location.service.ts, apps/api/test/unit/tenancy/provision-location.service.spec.ts, apps/api/test/unit/tenancy/archive-location.service.spec.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/application/provision-brand.service.ts
    - apps/api/src/contexts/tenancy/application/archive-tenant.service.ts
    - .planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md (Focus Area 4: archive-location must count scoped members server-side; D-12 no auto-provision)
  </read_first>
  <behavior>
    - ProvisionLocationService.execute creates a new active location for (brandId, tenantId) with the given fields; it is an explicit owner action only (no idempotency-on-brand-create path, no auto-synthesis — D-12).
    - ArchiveLocationService.execute(locationId) loads the location (LocationNotFoundError if absent), soft-archives it (status='archived'), and returns `{ scopedMemberCount }` computed via repo.countScopedMembers — it never blocks and never deletes member_location_scope rows (D-17).
    - ListLocationsService.execute(brandId) returns the brand's active + archived locations.
  </behavior>
  <action>
    Implement the three services. `ProvisionLocationService` mirrors `ProvisionBrandService` minus the domain-hostname coupling; assigns `LocationId.parse(randomUUID())`, status 'active'. `ArchiveLocationService` mirrors the find->mutate->save archive shape and additionally calls `repo.countScopedMembers(locationId)` and surfaces the count in its result DTO so the admin confirmation dialog renders "N staff will lose access" (computed server-side, not client-side). `ListLocationsService` returns `repo.listForBrand(brandId, tenantId)`. Write unit specs: provision creates an active row; archive returns the correct scoped-member count and sets status archived without touching scope rows.
  </action>
  <acceptance_criteria>
    - archive-location.service.ts returns a scoped-member count and calls `countScopedMembers`.
    - No service and no code path auto-creates a location on brand create (grep the tenancy provision-brand path for `locations` -> 0 matches).
    - archive sets status to 'archived' and does not delete any member_location_scope row.
    - `pnpm --filter @resto/api test -- provision-location archive-location` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api test -- location.service</automated>
  </verify>
  <done>Create/list/archive services unit-proven; archive is non-blocking with a blast-radius count; no auto-provisioning.</done>
</task>

<task type="auto">
  <name>Task 3: Locations controller (brand-scoped) + error mapping + module wiring</name>
  <files>apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts, apps/api/src/contexts/tenancy/tenancy.module.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts
    - apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts (brand-scoped decorator stacking: @Permissions + @RequireActiveTenant)
    - apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts
    - apps/api/src/contexts/tenancy/tenancy.module.ts
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (controller section; Shared Patterns 4 + 6)
  </read_first>
  <action>
    Create `LocationsController` exposing brand-scoped CRUD: `POST` create, `GET` list, `POST :id/archive` (or DELETE mapped to soft-archive) — under the brand-first path so BrandScopeGuard resolves the brand from the URL/header. Stack `@Permissions({ location: ['create'|'read'|'update'] })` + `@RequireActiveTenant()`; read tenant/brand from context (never request body). Use `RestoZodValidationPipe` per-parameter for the create DTO (Zod-derived, `.max()` bounds). Add `LocationNotFoundError` mapping to `error-mapping.ts` (NotFoundException). Register the controller + the three services + `LOCATION_REPOSITORY` binding in `tenancy.module.ts`. Do NOT create a `@RequireLocation()` opt-in decorator (Pitfall 4 — locations CRUD is brand-grain, gated by BrandScopeGuard, not LocationScopeGuard).
  </action>
  <acceptance_criteria>
    - locations.controller.ts uses `@Permissions({ location: [...] })` and derives tenant/brand from context, not the body.
    - error-mapping.ts maps `LocationNotFoundError` to a 404.
    - tenancy.module.ts registers `LocationsController` and binds `LOCATION_REPOSITORY`.
    - No new `RequireLocation` decorator file is created.
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api typecheck</automated>
  </verify>
  <done>Locations CRUD endpoints wired under the brand-first shell with domain-error mapping.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                             |
| ------------------------ | ----------------------------------------------------------------------- |
| client -> locations CRUD | Out-of-scope brand must not reveal or mutate another brand's locations. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                     | Disposition     | Mitigation Plan                                                                                                                                               |
| --------- | ---------------------------------- | ------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-084-10  | Information Disclosure / Elevation | non-owner reads/writes locations of a brand they cannot reach | mitigate        | Controller is brand-scoped (BrandScopeGuard default-on + existence-hiding 404); repo enforces ScopedTx + brand-grain RLS from plan 01.                        |
| T-084-11  | Tampering                          | tenantId/brandId taken from request body                      | mitigate        | tenant/brand read from ALS context only (mirrors 08.3 D-14 RolesController rule).                                                                             |
| T-084-12  | Denial of Service                  | unbounded free-text name/address/contacts                     | mitigate        | Zod `.max()` bounds on every free-text field (packages/domain/CLAUDE.md).                                                                                     |
| T-084-13  | Elevation                          | archive blocked or cascade-deletes scope rows                 | accept/mitigate | D-17: archive is allowed and non-blocking; scope rows are NOT deleted (default-deny handles access loss); blast-radius count surfaced for the confirm dialog. |

</threat_model>

<verification>
- `pnpm --filter @resto/api typecheck` + `pnpm --filter @resto/api test -- location.service` green.
- Grep confirms no auto-provisioning of a default location anywhere in the tenancy provision path.
</verification>

<success_criteria>

- Owner-manageable per-brand locations (create/list/archive), soft-archive only, non-blocking archive with a server-computed blast-radius count, no auto-created first location.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-04-SUMMARY.md` when done.
</output>
