---
phase: 08.4-location-scoped-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/ids.ts
  - packages/domain/src/rbac/permissions.ts
  - packages/domain/src/rbac/system-roles.ts
  - packages/domain/src/index.ts
  - packages/domain/test/rbac/system-roles.spec.ts
autonomous: true
requirements: [D-06, D-07]

must_haves:
  truths:
    - 'A branded LocationId type is exported from @resto/domain.'
    - 'The RBAC permission catalogue includes a `location` resource with read/create/update/delete actions (no colon in action names).'
    - 'owner/admin/staff system roles each carry a `location` permission set; owner retains sole `ac` authority.'
    - 'A regression test pins that `admin` does NOT receive NON_DELEGATABLE permissions and does not gain `ac`.'
  artifacts:
    - path: 'packages/domain/src/ids.ts'
      provides: 'LocationId branded type'
      contains: 'LocationId'
    - path: 'packages/domain/src/rbac/permissions.ts'
      provides: 'location resource in PERMISSIONS_STATEMENT'
      contains: 'location'
    - path: 'packages/domain/src/rbac/system-roles.ts'
      provides: 'location perms on owner/admin/staff'
      contains: 'location'
  key_links:
    - from: 'packages/domain/src/rbac/system-roles.ts'
      to: 'packages/domain/src/rbac/permissions.ts'
      via: 'location resource actions match the catalogue'
      pattern: 'location'
---

<objective>
Add the domain-layer contracts that location-scoped RBAC and the branded ID rely on: the `LocationId` branded type and a `location` resource in the RBAC permission catalogue + system roles. This is pure `@resto/domain` work with zero infrastructure dependency, runnable in parallel with the DB foundation.

Purpose: D-06 (roles reference `location` permissions) and the owner-only assignment gate (D-15/D-07) both need a `location` resource in the catalogue and system-role sets before the tenancy CRUD controller and the location-role assignment service can compile.
Output: Extended branded IDs + RBAC catalogue with a passing admin-escalation regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@packages/domain/CLAUDE.md

<interfaces>
- packages/domain/src/ids.ts:6-7 — `BrandId` branded-type pattern to mirror for `LocationId`.
- packages/domain/src/rbac/permissions.ts — `PERMISSIONS_STATEMENT`; `brand` resource is the analog (PATTERNS.md notes `brand` at ~line 9).
- packages/domain/src/rbac/system-roles.ts — `SYSTEM_ROLES` owner/admin/staff; `brand` perms per role are the analog; `ac` lives under owner only.
- packages/domain/src/rbac/non-delegatable.ts — `NON_DELEGATABLE` (unchanged; the regression test references it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LocationId branded type + location RBAC resource + system-role perms</name>
  <files>packages/domain/src/ids.ts, packages/domain/src/rbac/permissions.ts, packages/domain/src/rbac/system-roles.ts, packages/domain/src/index.ts</files>
  <read_first>
    - packages/domain/src/ids.ts (BrandId definition to mirror)
    - packages/domain/src/rbac/permissions.ts (PERMISSIONS_STATEMENT + brand resource)
    - packages/domain/src/rbac/system-roles.ts (SYSTEM_ROLES owner/admin/staff + brand + ac)
    - packages/domain/CLAUDE.md (RBAC rules: system roles immutable, no `:` in action names, admin-escalation regression)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (system-roles.ts section)
  </read_first>
  <behavior>
    - `LocationId.parse(uuid)` succeeds and produces a branded value; parsing a non-uuid throws.
    - `PERMISSIONS_STATEMENT.location` equals `['read','create','update','delete']` (no colon in any action).
    - `SYSTEM_ROLES.owner.location` includes all four actions; `SYSTEM_ROLES.admin.location` and `SYSTEM_ROLES.staff.location` include `read` (admin's exact write scope per planner discretion, default `['read']`).
    - `SYSTEM_ROLES.owner` retains `ac` and no other role gains `ac`.
  </behavior>
  <action>
    Add `LocationId` to ids.ts mirroring `BrandId` (same branded z.string().uuid() pattern) and re-export from index.ts. Add a `location` key to `PERMISSIONS_STATEMENT` in permissions.ts with actions `['read','create','update','delete']` (mirror `brand`; enforce no `:` in action names). In system-roles.ts add `location` to owner (`['read','create','update','delete']`), admin (`['read']`), and staff (`['read']`); leave `ac` on owner only (it is the owner-only gate used by D-15's assignment endpoint). Keep the `satisfies Record<...>` constraint intact.
  </action>
  <acceptance_criteria>
    - `grep -c 'LocationId' packages/domain/src/ids.ts` >= 1 and index.ts re-exports it.
    - permissions.ts `PERMISSIONS_STATEMENT` has a `location` entry; `grep ':' ` over the location action strings finds none.
    - system-roles.ts has `location` under owner, admin, and staff; only `owner` has `ac`.
    - `pnpm --filter @resto/domain typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/domain typecheck</automated>
  </verify>
  <done>LocationId + location resource compile and are exported on the barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin-escalation regression test for the location resource</name>
  <files>packages/domain/test/rbac/system-roles.spec.ts</files>
  <read_first>
    - packages/domain/test/rbac/system-roles.spec.ts (existing admin-must-NOT-receive assertions, if present) OR the nearest existing rbac test file
    - packages/domain/src/rbac/non-delegatable.ts (NON_DELEGATABLE set)
    - packages/domain/CLAUDE.md (RBAC: "adding a permission to admin requires a passing regression test that pins what admin must NOT receive")
  </read_first>
  <behavior>
    - Test asserts `SYSTEM_ROLES.admin` has no `ac` permission and none of the NON_DELEGATABLE actions.
    - Test asserts `SYSTEM_ROLES.staff.location` does not include `create`/`update`/`delete` (staff is read-only on locations).
  </behavior>
  <action>
    Add (or extend) the system-roles regression spec so the newly added `location` resource cannot silently escalate `admin` or `staff`. Pin: admin has no `ac`, admin has no NON_DELEGATABLE action, staff.location is `read`-only. If no such spec file exists, create `packages/domain/test/rbac/system-roles.spec.ts` following the repo's existing domain test conventions.
  </action>
  <acceptance_criteria>
    - The spec asserts `SYSTEM_ROLES.admin` lacks `ac` and lacks every NON_DELEGATABLE action.
    - `pnpm --filter @resto/domain test -- system-roles` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/domain test -- system-roles</automated>
  </verify>
  <done>Regression test green; admin/staff cannot escalate via the new location resource.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                      |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| RBAC catalogue -> permission checks | The permission catalogue is the source of truth for every guard; adding a resource widens the authority surface. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                   | Disposition | Mitigation Plan                                                                       |
| --------- | ---------------------- | ----------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------- |
| T-084-04  | Elevation of Privilege | admin/staff silently gain location write or `ac` authority  | mitigate    | Task 2 regression test pins what admin/staff must NOT receive; `ac` stays owner-only. |
| T-084-05  | Tampering              | colon in action name breaks `resource:action` serialization | mitigate    | Location actions verified colon-free (packages/domain/CLAUDE.md RBAC rule).           |

</threat_model>

<verification>
- `pnpm --filter @resto/domain typecheck` and `pnpm --filter @resto/domain test` green.
- The RBAC import-boundary test still passes (no infra imports introduced).
</verification>

<success_criteria>

- `LocationId` exported; `location` resource present in the catalogue and on all three system roles with the correct grains; admin-escalation regression test green.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-02-SUMMARY.md` when done.
</output>
