---
phase: 08.4-location-scoped-access
plan: 05
type: execute
wave: 3
depends_on: [08.4-03]
files_modified:
  - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts
  - apps/api/src/shared/auth/location-neutral.decorator.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts
  - apps/api/src/contexts/identity/identity-http.module.ts
  - apps/api/test/unit/identity/location-scope.guard.spec.ts
autonomous: true
requirements: [D-04, D-05]

must_haves:
  truths:
    - 'LocationScopeGuard is a default-on global APP_GUARD; routes opt out with @LocationNeutral().'
    - "An owner (baseRole==='owner') bypasses location scope entirely."
    - 'For a non-owner, a session activeLocationId that is null or != the request locationId returns an existence-hiding 404.'
    - 'A non-owner whose member_location_scope does not include the request location returns 403 location.access_denied.'
    - "BrandScopeGuard's non-owner branch derives reachable brands from scoped locations (supersedes member_brand_scope reads)."
  artifacts:
    - path: 'apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts'
      provides: 'owner-bypass + existence-hiding 404 + scope-denied 403 at location grain'
      contains: 'NotFoundException'
    - path: 'apps/api/src/shared/auth/location-neutral.decorator.ts'
      provides: 'opt-out decorator (LOCATION_NEUTRAL_KEY)'
      contains: 'LocationNeutral'
  key_links:
    - from: 'apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts'
      to: 'req.activeLocationId + getLocationId()'
      via: 'session-vs-request reconciliation'
      pattern: 'activeLocationId'
    - from: 'apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts'
      to: 'findReachableBrandsForMember'
      via: 'non-owner reachable-brand derivation'
      pattern: 'findReachableBrandsForMember'
---

<objective>
Enforce location scope on every operator route: a default-on `LocationScopeGuard` that mirrors `BrandScopeGuard` (owner-bypass, existence-hiding 404 on session-vs-request mismatch, 403 on out-of-scope) and re-point `BrandScopeGuard` to derive reachable brands from scoped locations (D-04). This is the request-time isolation boundary at location grain (D-05).

Purpose: Success criterion #2 — out-of-scope location/brand returns existence-hiding 404/403; the guard is the real fence (RLS is the backstop).
Output: `LocationScopeGuard` registered globally, `@LocationNeutral()` opt-out, `BrandScopeGuard` re-pointed, unit matrix proving owner/mismatch/out-of-scope outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md

<interfaces>
Copy verbatim (PATTERNS.md "LocationScopeGuard" section — the security-critical excerpt):
- brand-scope.guard.ts (full, 78 lines) — owner-bypass line, existence-hiding 404 (NotFoundException on session-vs-request mismatch), 403 on out-of-scope; APP_GUARD registration at identity-http.module.ts:79-81.
- apps/api/src/shared/auth/brand-neutral.decorator.ts — BRAND_NEUTRAL_KEY / BrandNeutral (mirror; do NOT copy the vestigial require-brand.decorator.ts — Pitfall 4).
- member-location-scope reader (plan 03) — findLocationScopeForMember + findReachableBrandsForMember.
- packages/db getLocationId() (plan 01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LocationScopeGuard + @LocationNeutral opt-out + registration</name>
  <files>apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts, apps/api/src/shared/auth/location-neutral.decorator.ts, apps/api/src/contexts/identity/identity-http.module.ts, apps/api/test/unit/identity/location-scope.guard.spec.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts (full — copy the guard shape 1:1)
    - apps/api/src/shared/auth/brand-neutral.decorator.ts
    - apps/api/src/contexts/identity/identity-http.module.ts (APP_GUARD registration order at 79-81)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (LocationScopeGuard verbatim + Shared Pattern 2; Pitfall 4)
  </read_first>
  <behavior>
    - @LocationNeutral() route -> guard returns true.
    - baseRole==='owner' -> guard returns true (bypass) regardless of activeLocationId.
    - non-owner with req.activeLocationId null OR != getLocationId() -> NotFoundException (404, existence-hiding).
    - non-owner with activeLocationId === locationId but not in findLocationScopeForMember -> ForbiddenException code 'location.access_denied'.
    - non-owner with activeLocationId === locationId AND in scope -> true.
  </behavior>
  <action>
    Create `LocationNeutral` decorator (`LOCATION_NEUTRAL_KEY = 'identity:location-neutral'`, `SetMetadata`) in apps/api/src/shared/auth/. Create `LocationScopeGuard` copying `BrandScopeGuard` exactly with the substitutions: `getBrandId()` -> `getLocationId()`, `req.activeBrandId` -> `req.activeLocationId`, `findBrandScopeForMember` -> `findLocationScopeForMember`, `BRAND_NEUTRAL_KEY` -> `LOCATION_NEUTRAL_KEY`, error codes `location.context_required`/`location.operator_required`/`location.tenant_required`/`location.access_denied`. Keep the owner-bypass single line and the NotFoundException (404 not 403) for the null/mismatch case. Register it as an `APP_GUARD` in identity-http.module.ts at the SAME chain position after `BrandScopeGuard`. Do NOT create a `@RequireLocation()` opt-in decorator. Write the unit matrix covering all five behaviors with a mocked reader + reflector.
  </action>
  <acceptance_criteria>
    - location-scope.guard.ts throws `NotFoundException` (not Forbidden) when activeLocationId is null or mismatched for a non-owner.
    - The single owner-bypass line `if (principal.baseRole === 'owner') return true;` is present.
    - location-neutral.decorator.ts exports `LocationNeutral` + `LOCATION_NEUTRAL_KEY`; no `RequireLocation` file exists.
    - identity-http.module.ts registers `LocationScopeGuard` as `APP_GUARD` after `BrandScopeGuard`.
    - `pnpm --filter @resto/api test -- location-scope.guard` exits 0 (5 cases).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api test -- location-scope.guard</automated>
  </verify>
  <done>Default-on location guard enforces owner-bypass + existence-hiding 404 + scope-denied 403, unit-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point BrandScopeGuard non-owner branch to reachable-brand derivation</name>
  <files>apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts (non-owner branch lines 64-69 reading findBrandScopeForMember)
    - apps/api/src/contexts/identity/infrastructure/member-location-scope-drizzle.reader.ts (findReachableBrandsForMember from plan 03)
    - .planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md (Focus Area 5: reachable brands are DERIVED; State of the Art table)
  </read_first>
  <action>
    Change `BrandScopeGuard`'s non-owner branch to check the request brand against `findReachableBrandsForMember` (member_location_scope -> locations -> brands) instead of the superseded `member_brand_scope` read. Inject the location-scope reader in place of (or alongside) the brand-scope reader. Keep the owner-bypass and the existence-hiding 404 unchanged. Do NOT drop the member_brand_scope table; only stop reading it. If the previous `MEMBER_BRAND_SCOPE_READER` injection becomes fully unused after this change, remove the injection from this guard (leave the port/table for now).
  </action>
  <acceptance_criteria>
    - brand-scope.guard.ts references `findReachableBrandsForMember` and no longer calls `findBrandScopeForMember` in its non-owner branch.
    - Owner-bypass and NotFoundException-on-mismatch behavior are unchanged.
    - `pnpm --filter @resto/api typecheck` exits 0 and existing brand-scope-guard tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api test -- brand-scope</automated>
  </verify>
  <done>Reachable brands derive from scoped locations; member_brand_scope is no longer read on the guard path.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                 |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| client request -> operator route | activeLocationId (session) vs request locationId (header/ALS) must reconcile; scope membership is re-checked every request. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                        | Disposition | Mitigation Plan                                                                                                                   |
| --------- | ---------------------------------- | ---------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-084-14  | Elevation / Information Disclosure | non-owner accesses an out-of-scope location                      | mitigate    | LocationScopeGuard existence-hiding 404 (mismatch) + 403 (out-of-scope), default-on globally; RLS from plan 01 as backstop.       |
| T-084-15  | Information Disclosure             | reachable-brand check leaks a brand with no scoped location      | mitigate    | BrandScopeGuard re-pointed to findReachableBrandsForMember (join through locations); zero-location brand -> not reachable (D-04). |
| T-084-16  | Tampering                          | dead @RequireLocation decorator gives false sense of enforcement | mitigate    | Only @LocationNeutral (opt-out) implemented; guard is default-on (Pitfall 4).                                                     |

</threat_model>

<verification>
- `pnpm --filter @resto/api test -- location-scope.guard brand-scope` green.
- Grep confirms the guard registration order and that no `RequireLocation` decorator exists.
</verification>

<success_criteria>

- Location scope enforced by default on every operator route with owner-bypass + existence-hiding 404 + 403; brand reachability derived from scoped locations.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-05-SUMMARY.md` when done.
</output>
