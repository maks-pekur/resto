---
phase: 08.4-location-scoped-access
plan: 06
type: execute
wave: 3
depends_on: [08.4-01, 08.4-03, 08.4-04]
files_modified:
  - packages/db/src/schema/menu.ts
  - packages/db/migrations/0067_stop_list_location_regrain.sql
  - packages/db/migrations/0068_stop_list_location_rls.sql
  - packages/db/migrations/meta/_journal.json
  - apps/api/src/contexts/catalog/domain/ports.ts
  - apps/api/src/contexts/catalog/application/get-menu-availability.service.ts
  - apps/api/src/contexts/catalog/application/stop-list.service.ts
  - apps/api/src/contexts/catalog/application/default-location-resolver.service.ts
  - apps/api/src/contexts/catalog/infrastructure/postgres-menu-version.adapter.ts
  - apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts
  - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts
  - apps/api/src/contexts/catalog/catalog.module.ts
  - docs/runbooks/menu-edge-caching.md
  - packages/db/test/integration/tenant-isolation.spec.ts
autonomous: true
requirements: [D-02]

must_haves:
  truths:
    - 'menu_stop_list is keyed by location_id (unique on tenantId, locationId, itemId); catalog stop-version is per-location (catalog_location_stop_version PK (locationId, tenantId)).'
    - "The operator stop/unstop action reads the operator's active location (pin), not a brand."
    - 'The guest availability read resolves a default location (earliest active by createdAt, then id) since guests have no session pin.'
    - 'menu_stop_list + catalog_location_stop_version carry location-grain RESTRICTIVE RLS layered on the existing brand policy.'
    - 'Menu items/categories/modifiers/prices remain brand-level (unchanged).'
  artifacts:
    - path: 'apps/api/src/contexts/catalog/application/default-location-resolver.service.ts'
      provides: 'guest default-location resolution shared with ordering'
      contains: 'createdAt'
    - path: 'packages/db/migrations/0068_stop_list_location_rls.sql'
      provides: 'location-grain RESTRICTIVE policies on stop-list + stop-version'
      contains: 'current_location_id()'
    - path: 'docs/runbooks/menu-edge-caching.md'
      provides: 'documented CDN cache-key location-dimension gap'
      contains: 'location'
  key_links:
    - from: 'apps/api/src/contexts/catalog/application/stop-list.service.ts'
      to: 'requireLocationContext'
      via: 'operator active-location instead of brand'
      pattern: 'requireLocationContext'
    - from: 'apps/api/src/contexts/catalog/application/get-menu-availability.service.ts'
      to: 'DefaultLocationResolverService'
      via: 'guest read resolves default location'
      pattern: 'DefaultLocationResolver'
---

<objective>
Re-grain availability/stop-list from brand to location (D-02): the operator stop/unstop action now targets the operator's active location, guest availability resolves a default location, and per-location stop-list + stop-version get location-grain RLS. Menu items/categories/prices stay brand-level. A shared `DefaultLocationResolverService` (reused by ordering in plan 08) handles the guest "which location?" question.

Purpose: Each kitchen 86's its own items (D-02) while the menu remains shared per brand.
Output: Location-keyed stop-list schema + migration + RLS, re-grained catalog services, default-location resolver, an updated CDN runbook note, and isolation-spec coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md
@docs/runbooks/menu-edge-caching.md

<interfaces>
- packages/db/src/schema/menu.ts — `menu_stop_list` (brandId + unique (tenantId, itemId)) and `catalog_brand_stop_version` (PK (brandId, tenantId)) to re-key to location.
- packages/db/migrations/0058_brand_rls.sql + 0060/0061 — location-grain RESTRICTIVE policy shape; `current_location_id()` + `app_bind_location()` were created in plan 01.
- catalog: get-menu-availability.service.ts, stop-list.service.ts, postgres-menu-version.adapter.ts, catalog-drizzle.repository.ts (addToStopList/removeFromStopList/listStoppedItemIds/listStopListWithStoppedAt keyed on brandId), domain/ports.ts, public-menu.controller.ts (availability handler ETag/Cache-Control).
- initial-brand-drizzle.repository.ts — "earliest by createdAt, then id" ordering idiom for the resolver.
- packages/db withLocation/requireLocationContext (plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Re-key stop-list + stop-version to location (schema + migrations + db:migrate)</name>
  <files>packages/db/src/schema/menu.ts, packages/db/migrations/0067_stop_list_location_regrain.sql, packages/db/migrations/0068_stop_list_location_rls.sql, packages/db/migrations/meta/_journal.json, packages/db/test/integration/tenant-isolation.spec.ts</files>
  <read_first>
    - packages/db/src/schema/menu.ts (menu_stop_list ~324-356; catalog_brand_stop_version ~410-433)
    - packages/db/migrations/0058_brand_rls.sql + 0060_brand_rls_restrictive.sql (RESTRICTIVE location-grain policy shape)
    - packages/db/test/integration/tenant-isolation.spec.ts (Plan 04a-07 menu_stop_list block)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (Migrations Tier-2 + Tests section); RESEARCH Focus Area 3
  </read_first>
  <action>
    In menu.ts: change `menu_stop_list` to carry `location_id uuid NOT NULL` with a composite FK `(location_id, tenant_id) -> locations(id, tenant_id)` `.onDelete('restrict')`, and change the unique index to `(tenantId, locationId, itemId)`. Rename `catalog_brand_stop_version` -> `catalog_location_stop_version` with PK `(locationId, tenantId)` and composite FK to locations. Keep menu items/categories/modifiers untouched. Run `pnpm --filter @resto/db db:generate` for the table DDL. Hand-write `0068_stop_list_location_rls.sql` adding the Tier-2 location-grain RESTRICTIVE policies `menu_stop_list_location_iso` and `catalog_location_stop_version_location_iso` using `current_location_id()` (mirror 0058/0060; these AND with the existing brand policies). Because data is near-empty (D-13) and D-12 forbids synthesized defaults, do NOT backfill/synthesize a location — update any test/seed fixtures that stop items to create a location first. [BLOCKING] run `pnpm --filter @resto/db db:migrate` (exit 0) before proceeding. Add tenant-isolation.spec.ts describe entries asserting menu_stop_list + catalog_location_stop_version location-grain isolation (mirror the existing menu_stop_list zero-rows + composite-FK-rejection pattern).
  </action>
  <acceptance_criteria>
    - menu.ts `menu_stop_list` declares `FOREIGN KEY (location_id, tenant_id) REFERENCES locations(id, tenant_id)` and a unique index on `(tenant_id, location_id, item_id)`.
    - `catalog_location_stop_version` exists with PK `(location_id, tenant_id)`; `catalog_brand_stop_version` no longer referenced by schema.
    - `0068_stop_list_location_rls.sql` contains `AS RESTRICTIVE` policies referencing `current_location_id()`.
    - No migration contains `INSERT INTO locations`.
    - `pnpm --filter @resto/db db:migrate` exits 0; `pnpm --filter @resto/db test -- tenant-isolation` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/db db:migrate && pnpm --filter @resto/db test -- tenant-isolation</automated>
  </verify>
  <done>Stop-list + stop-version are location-keyed with location-grain RLS applied and isolation-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Default-location resolver + re-grain catalog services/adapter/ports</name>
  <files>apps/api/src/contexts/catalog/application/default-location-resolver.service.ts, apps/api/src/contexts/catalog/domain/ports.ts, apps/api/src/contexts/catalog/application/get-menu-availability.service.ts, apps/api/src/contexts/catalog/application/stop-list.service.ts, apps/api/src/contexts/catalog/infrastructure/postgres-menu-version.adapter.ts, apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts, apps/api/src/contexts/catalog/catalog.module.ts</files>
  <read_first>
    - apps/api/src/contexts/catalog/application/get-menu-availability.service.ts (full, 33 lines)
    - apps/api/src/contexts/catalog/application/stop-list.service.ts (full, 80 lines)
    - apps/api/src/contexts/catalog/infrastructure/postgres-menu-version.adapter.ts (currentStop(brandId))
    - apps/api/src/contexts/catalog/domain/ports.ts (addToStopList/removeFromStopList/listStoppedItemIds/listStopListWithStoppedAt)
    - apps/api/src/contexts/identity/infrastructure/initial-brand-drizzle.repository.ts (earliest-by-createdAt ordering idiom)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (Catalog re-grain section; RESEARCH Open Question 2)
  </read_first>
  <action>
    Create `DefaultLocationResolverService.resolveForBrand(brandId, tenantId): Promise<string>` returning the earliest active location by (createdAt, id) for the brand (mirror the InitialBrand ordering idiom); throw a `NoLocationForBrandError`-style domain error if the brand has zero locations (guests can't order/read availability there — consistent with D-04/D-12). Re-grain the `CatalogRepository` port methods `addToStopList`/`removeFromStopList`/`listStoppedItemIds`/`listStopListWithStoppedAt` from `brandId` to `locationId` and update `catalog-drizzle.repository.ts` accordingly (key on `menu_stop_list.locationId`). Change `postgres-menu-version.adapter.ts` `currentStop(brandId)` -> `currentStop(locationId)` reading `catalog_location_stop_version`. In `stop-list.service.ts` (operator action) swap `requireBrandContext()` -> `requireLocationContext()` (the operator's active-location pin, D-08) with the same `db.withTenant`/outbox idiom. In `get-menu-availability.service.ts` (guest `@Public`) resolve the default location via `DefaultLocationResolverService` and bind it with `withLocation` before reading stopped items + stop-version (guests have no pin). Register the resolver in catalog.module.ts.
  </action>
  <acceptance_criteria>
    - default-location-resolver.service.ts orders by `createdAt` then `id` and returns a single location id.
    - stop-list.service.ts calls `requireLocationContext()` (grep -c >= 1) and no longer calls `requireBrandContext()`.
    - get-menu-availability.service.ts resolves a default location for guests (references `DefaultLocationResolver`).
    - catalog ports + repository + version adapter key on `locationId`, not `brandId`.
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api typecheck</automated>
  </verify>
  <done>Operator stop-list acts on active location; guest availability resolves a default location; all stop-list reads/writes location-keyed.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve availability endpoint shape + document CDN cache-key gap</name>
  <files>apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts, docs/runbooks/menu-edge-caching.md</files>
  <read_first>
    - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts (availability handler ~144-163: ETag + Cache-Control s-maxage=5 + 304)
    - docs/runbooks/menu-edge-caching.md (line ~24: "Cache key: host + path")
    - .planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md (Pitfall 3 — CDN edge-cache location dimension)
  </read_first>
  <action>
    Keep the availability handler's ETag/Cache-Control/304 shape identical — only the underlying stopVersion source is now location-keyed. Do NOT add a location dimension to the URL or attempt to fix the CDN cache key in this phase (guest location-selection is deferred per D-03). Update `docs/runbooks/menu-edge-caching.md` to document the known limitation: the edge-cache key is `host + path` with no location dimension, so a brand with 2+ active locations can serve one location's stop-list to another location's guests; note the future fix options (location URL segment/query param in the cache key, or Vary on a location header) and that it is out of scope until guest-facing location selection ships.
  </action>
  <acceptance_criteria>
    - public-menu.controller.ts availability handler still emits an ETag + `Cache-Control: public, s-maxage=5` + 304 path (unchanged shape).
    - docs/runbooks/menu-edge-caching.md contains a section documenting the location cache-key gap and the deferral rationale.
    - `pnpm --filter @resto/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/api typecheck</automated>
  </verify>
  <done>Availability wire contract preserved; the CDN location cache-key gap is documented, not silently shipped.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                       |
| ------------------------------ | ------------------------------------------------------------------------------------------------- |
| operator -> stop-list mutation | Operator may only 86 items for their active location.                                             |
| guest -> availability read     | Guest read resolves a default location deterministically; no client-supplied location this phase. |
| CDN edge -> guest              | Shared per-host cache cannot represent two locations' stop-lists.                                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                            | Disposition | Mitigation Plan                                                                                                                               |
| --------- | ---------------------- | -------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-084-17  | Information Disclosure | cross-location stop-list read/write via forgotten location filter    | mitigate    | ScopedTx location key + Tier-2 location-grain RESTRICTIVE RLS on menu_stop_list + stop-version; tenant-isolation.spec.ts coverage (Task 1).   |
| T-084-18  | Information Disclosure | CDN serves one location's availability to another (shared cache key) | accept      | Low severity (menu availability, not PII); latent until a brand's 2nd location; documented in runbook (Task 3) per D-03 deferral (Pitfall 3). |
| T-084-19  | Tampering              | migration synthesizes a default location to satisfy NOT NULL         | mitigate    | No backfill/synthesis; fixtures updated explicitly (Pitfall 5); acceptance asserts no `INSERT INTO locations`.                                |

</threat_model>

<verification>
- `pnpm --filter @resto/db db:migrate` + `pnpm --filter @resto/db test -- tenant-isolation` + `pnpm --filter @resto/api typecheck` green.
- Existing catalog availability/stop-list tests updated and passing (location-keyed).
</verification>

<success_criteria>

- Stop-list/availability are per-location; operator acts on active location; guest resolves a default location; menu content stays brand-level; CDN gap documented.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-06-SUMMARY.md` when done.
</output>
