---
phase: 08.4-location-scoped-access
plan: 10
type: execute
wave: 6
depends_on: [08.4-07, 08.4-09]
files_modified:
  - apps/admin/src/lib/queries/team.ts
  - apps/admin/src/components/team/member-location-role-matrix.tsx
  - apps/admin/src/routes/(protected)/$brandSlug/team.tsx
autonomous: false
requirements: [D-15]

must_haves:
  truths:
    - 'The Team page shows, per member, a matrix of (location -> role) pairs the owner can add and remove.'
    - 'Assigning/removing a location role calls the owner-only location-role endpoint and invalidates the team/roles queries.'
    - "The stale 'Role permissions apply across all brands. Brand access is controlled separately via brand scope.' copy is rewritten to reflect the per-location model."
    - "The matrix is visible/editable only for the owner (isOwner gate), matching the server-side ac:['update'] gate."
  artifacts:
    - path: 'apps/admin/src/components/team/member-location-role-matrix.tsx'
      provides: 'per-member (location -> role) add/remove matrix (D-15)'
      contains: 'location'
    - path: 'apps/admin/src/routes/(protected)/$brandSlug/team.tsx'
      provides: 'Team page using the location-role matrix + corrected copy'
      contains: 'member-location-role-matrix'
  key_links:
    - from: 'apps/admin/src/components/team/member-location-role-matrix.tsx'
      to: 'POST /v1/members/:memberId/location-roles'
      via: 'assign/remove mutation + invalidateQueries'
      pattern: 'location-roles'
---

<objective>
Extend the Team page (08.3) with a per-member (location -> role) assignment matrix (D-15): the owner adds/removes location-role pairs per member, consolidating assignment on the existing Team surface rather than a new screen. Also fix the now-factually-wrong Team copy that asserts the pre-08.4 tenant-wide-role/brand-scope model.

Purpose: D-15 — owner-only per-location role management on the Team page.
Output: A `member-location-role-matrix` component replacing the single-role cell, wired to the location-role API, with corrected UI copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-location-scoped-access/08.4-CONTEXT.md
@.planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md
@.planning/phases/08.4-location-scoped-access/08.4-RESEARCH.md

<interfaces>
- apps/admin/src/components/team/member-role-row.tsx — single-role `<Select>` + useMutation/toast/invalidateQueries + isOwner/isSelf gating to generalize into a matrix.
- apps/admin/src/routes/(protected)/$brandSlug/team.tsx:78-81 — the stale copy string to rewrite; table layout at ~48-112.
- apps/admin/src/lib/queries/team.ts + queries/roles.ts — member/role query conventions.
- apps/admin/src/lib/queries/locations.ts (plan 09) — meLocationsQuery / brandLocationsQuery for the location column.
- Backend: identity `POST /v1/members/:memberId/location-roles` (assign) + remove verb (plan 07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: (location -> role) matrix component + location-role queries</name>
  <files>apps/admin/src/lib/queries/team.ts, apps/admin/src/components/team/member-location-role-matrix.tsx</files>
  <read_first>
    - apps/admin/src/components/team/member-role-row.tsx (full — the Select + mutation + gating pattern to generalize)
    - apps/admin/src/lib/queries/team.ts + roles.ts (query/mutation conventions, invalidation keys)
    - apps/admin/src/lib/queries/locations.ts (from plan 09 — the location list hooks)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (member-location-role-matrix section)
  </read_first>
  <action>
    Add location-role query hooks to team.ts: `memberLocationRolesQuery(memberId)` (current pairs), `assignLocationRoleMutation` (POST /v1/members/:id/location-roles), `removeLocationRoleMutation`. Create `member-location-role-matrix.tsx` mirroring `member-role-row.tsx` but rendering, per member, an expandable list of `(location, role)` rows — each a location label + role `<Select>` (SYSTEM_ROLES + available custom roles) + remove button — plus an "add location" affordance (location picker sourced from the brand's locations). Reuse the same `useMutation`/`toast`/`invalidateQueries(['team','members'] + ['roles','members'])` idiom and the same `isOwner`/`isSelf` gating. Editing is owner-only.
  </action>
  <acceptance_criteria>
    - team.ts exports `assignLocationRoleMutation` + `removeLocationRoleMutation` + `memberLocationRolesQuery`.
    - member-location-role-matrix.tsx renders per-member (location, role) rows with add/remove and gates editing on isOwner.
    - The mutation targets the `location-roles` endpoint and invalidates the team/roles queries.
    - `pnpm --filter @resto/admin typecheck` exits 0 and `pnpm --filter @resto/admin build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/admin typecheck && pnpm --filter @resto/admin build</automated>
  </verify>
  <done>Per-member (location -> role) matrix component wired to the owner-only location-role API.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the matrix into the Team page + rewrite stale copy</name>
  <files>apps/admin/src/routes/(protected)/$brandSlug/team.tsx</files>
  <read_first>
    - apps/admin/src/routes/(protected)/$brandSlug/team.tsx (full — MemberRoleRow usage + the stale copy at ~78-81 + loader)
    - apps/admin/src/components/team/member-location-role-matrix.tsx (from Task 1)
    - .planning/phases/08.4-location-scoped-access/08.4-PATTERNS.md (KNOWN LANDMINE — stale copy)
  </read_first>
  <action>
    Replace the single-role cell (`<MemberRoleRow>`'s role `<Select>`) with `<MemberLocationRoleMatrix>` for each member; keep the member email/status columns. Rewrite the stale copy at team.tsx:78-81 ("Role permissions apply across all brands. Brand access is controlled separately via brand scope.") to correctly state the 08.4 model: roles are assigned per location, and a member's brand access is DERIVED from the brands of their scoped locations. Extend the route loader to ensure the location list is available. Keep owner-only editing consistent with the server `ac:['update']` gate.
  </action>
  <acceptance_criteria>
    - team.tsx renders `MemberLocationRoleMatrix` per member and no longer renders the single-role `<Select>` cell.
    - `grep -F 'apply across all brands' apps/admin/src/routes/(protected)/$brandSlug/team.tsx` returns 0 matches (stale copy removed).
    - The new copy references per-location roles + derived brand access.
    - `pnpm --filter @resto/admin typecheck` exits 0 and `pnpm --filter @resto/admin build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @resto/admin typecheck && pnpm --filter @resto/admin build</automated>
  </verify>
  <done>Team page uses the location-role matrix; stale pre-08.4 copy corrected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Team location-role matrix</name>
  <what-built>Per-member (location -> role) assignment matrix on the Team page (owner-only) with add/remove, and corrected model copy.</what-built>
  <how-to-verify>
    1. As OWNER on `/{brand}/team`: for a staff member, add a location-role pair (e.g. Manager at Location A) and confirm it persists after refresh.
    2. Add a second pair (e.g. Cashier at Location B) for the same member; confirm both show.
    3. Remove one pair; confirm the member loses that location's role.
    4. Confirm a non-owner viewing the Team page sees no edit affordances (read-only), matching the server gate.
    5. Confirm the Team page copy no longer claims roles apply across all brands.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues observed.</resume-signal>
  <files>(no source files — human verification only)</files>
  <action>Pause execution and present the verification steps below to the user. Wait for explicit approval before completing the plan.</action>
  <verify><human-check>User confirms add/remove/persist of (location,role) pairs, owner-only editing, and the corrected copy</human-check></verify>
  <done>User has typed "approved" (or rejected with specific issues) per resume-signal</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                    |
| ----------------------------- | ---------------------------------------------------------------------------------------------- |
| owner UI -> location-role API | Only the owner may mint per-location roles; the UI gate mirrors the server ac:['update'] gate. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                               | Disposition | Mitigation Plan                                                                                           |
| --------- | ---------------------- | ------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-084-30  | Elevation of Privilege | non-owner uses the matrix to assign roles               | mitigate    | Client isOwner gate is cosmetic; server AssignLocationRoleService is owner-only (ac:['update'], plan 07). |
| T-084-31  | Information Disclosure | stale copy misleads operators about the isolation model | mitigate    | Copy rewritten to the per-location/derived-brand model (Task 2).                                          |

</threat_model>

<verification>
- `pnpm --filter @resto/admin typecheck` + `pnpm --filter @resto/admin build` green.
- Human-verify checkpoint approved for add/remove/persist + owner-only + corrected copy.
</verification>

<success_criteria>

- Owner manages per-location roles via a Team-page matrix; stale copy corrected; editing owner-only.
  </success_criteria>

<output>
Create `.planning/phases/08.4-location-scoped-access/08.4-10-SUMMARY.md` when done.
</output>
