# Phase 08.1: Payments — Provider Layer & Onboarding UX - Pattern Map

**Mapped:** 2026-06-28
**Files analyzed:** 13 files to create/modify
**Analogs found:** 11 / 13 (85%)

## File Classification

| New/Modified File                                                                 | Role                       | Data Flow               | Closest Analog                                                                               | Match Quality |
| --------------------------------------------------------------------------------- | -------------------------- | ----------------------- | -------------------------------------------------------------------------------------------- | ------------- |
| `apps/api/src/contexts/payments/domain/ports.ts`                                  | port interface             | request-response        | `apps/api/src/contexts/tenancy/domain/ports.ts`                                              | exact         |
| `apps/api/src/contexts/payments/domain/payment-provider.ts`                       | domain schema              | request-response        | `apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts` (StripeOnboardingStatus)          | role-match    |
| `apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts` | adapter                    | CRUD + request-response | `apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts`                     | exact         |
| `apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.ts`     | adapter (stub)             | request-response        | `apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts`            | role-match    |
| `apps/api/src/contexts/tenancy/domain/brand.aggregate.ts`                         | aggregate                  | CRUD + event-driven     | `apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts`                                   | exact         |
| `apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts`     | service                    | request-response        | `apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts`               | exact         |
| `packages/db/src/schema/brands.ts`                                                | schema (Drizzle)           | DDL                     | `packages/db/src/schema/brands.ts` + `packages/db/src/schema/tenants.ts`                     | exact         |
| `packages/db/migrations/0057_brand_stripe_connect.sql`                            | migration                  | DDL                     | `packages/db/migrations/0055_payments_money_path.sql` + `0013_brands_rls.sql`                | exact         |
| `apps/admin/src/lib/brand-payments-api.ts`                                        | API client                 | request-response        | `apps/admin/src/lib/payments-api.ts`                                                         | role-match    |
| `apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx` | component                  | request-response        | `apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx` (existing) | exact         |
| `apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts`    | controller                 | request-response        | `apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts`              | exact         |
| `apps/api/src/config/env.schema.ts`                                               | config                     | request-response        | `apps/api/src/config/env.schema.ts` (existing)                                               | exact         |
| `apps/api/eslint.config.mjs`                                                      | config (architecture test) | request-response        | `apps/api/eslint.config.mjs` (existing)                                                      | exact         |

## Pattern Assignments

### `apps/api/src/contexts/payments/domain/ports.ts` (port interface, request-response)

**Analog:** `apps/api/src/contexts/tenancy/domain/ports.ts` (StripeConnectPort)

**Imports pattern** (lines 1–5):

```typescript
import type { TenantId } from '@resto/domain';
import type { RestoTx } from '@resto/db';
```

**Symbol token pattern** (lines 124):

```typescript
export const STRIPE_CONNECT_PORT = Symbol('STRIPE_CONNECT_PORT');
```

**Port interface pattern** (lines 114–122):

```typescript
export interface StripeConnectPort {
  ensureExpressAccount(input: {
    tenantId: TenantId;
    displayName: string;
  }): Promise<string | null>;
  createExpressAccount(
    input: CreateExpressAccountInput,
  ): Promise<CreateExpressAccountResult>;
  createAccountLink(
    input: CreateAccountLinkInput,
  ): Promise<CreateAccountLinkResult>;
  createPaymentIntent(
    input: CreatePaymentIntentInput,
  ): Promise<CreatePaymentIntentResult>;
  cancelPaymentIntent(
    input: CancelPaymentIntentInput,
  ): Promise<CancelPaymentIntentResult>;
  createRefund(input: CreateRefundInput): Promise<CreateRefundResult>;
  retrieveAccount(input: RetrieveAccountInput): Promise<RetrieveAccountResult>;
}
```

**Action:** Rename to `PaymentProviderPort`, add methods `createOnboardingSession`, `exchangeOAuthCode`, `verifyWebhookSignature`; move from tenancy context to payments context domain; swap `tenantId` for `brandId` in signatures.

---

### `apps/api/src/contexts/payments/domain/payment-provider.ts` (domain schema, request-response)

**Analog:** `apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts` (StripeOnboardingStatus enum)

**Type pattern** (lines 33):

```typescript
export type StripeOnboardingStatus =
  | 'not_started'
  | 'pending'
  | 'complete'
  | 'restricted';
```

**Action:** Create Zod enums for `PaymentProvider` and `AccountType`:

```typescript
import { z } from 'zod';

export const PaymentProvider = z.enum(['stripe']).brand<'PaymentProvider'>();
export const AccountType = z
  .enum(['express', 'standard'])
  .brand<'AccountType'>();
export const PaymentOnboardingStatus = z.enum([
  'not_started',
  'pending',
  'complete',
  'restricted',
]);
```

---

### `apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts` → `apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts`

**Analog:** `apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts` (entire file)

**Imports + resilience wrapper** (lines 1–132):

```typescript
import { Injectable, Logger } from '@nestjs/common';
import type Stripe from 'stripe';
import type { Env } from '../../../config/env.schema';

const RETRY_DELAYS_MS = [0, 250, 1000, 4000] as const;
const CALL_TIMEOUT_MS = 5500;

async function withTimeout<T>(fn: () => Promise<T>): Promise<T> {
  let timeoutId: NodeJS.Timeout | undefined;
  const timeoutPromise = new Promise<never>((_, reject) => {
    timeoutId = setTimeout(() => {
      reject(
        new Error(`Stripe call timed out after ${String(CALL_TIMEOUT_MS)}ms`),
      );
    }, CALL_TIMEOUT_MS);
  });
  try {
    return await Promise.race([fn(), timeoutPromise]);
  } finally {
    if (timeoutId !== undefined) clearTimeout(timeoutId);
  }
}

async function withRetry<T>(
  logger: Logger,
  label: string,
  fn: () => Promise<T>,
): Promise<T> {
  let lastErr: unknown;
  for (let attempt = 0; attempt < RETRY_DELAYS_MS.length; attempt++) {
    const delay = RETRY_DELAYS_MS[attempt] ?? 0;
    if (delay > 0) await sleep(delay + jitter());
    try {
      return await withTimeout(fn);
    } catch (err) {
      // ... retry logic
    }
  }
  throw lastErr;
}
```

**Class structure + payment intent method** (lines 135–207):

```typescript
@Injectable()
export class StripeConnectAdapter implements StripeConnectPort {
  readonly #client: StripeClientLike;
  readonly #logger: Logger;

  constructor(client: StripeClientLike, _env: StripeEnv, logger?: Logger) {
    this.#client = client;
    this.#logger = logger ?? new Logger(StripeConnectAdapter.name);
  }

  async createPaymentIntent(input: CreatePaymentIntentInput): Promise<CreatePaymentIntentResult> {
    const idempotencyKey = `pi:${input.orderId}:${String(input.attempt)}`;
    const pi = await withRetry(this.#logger, 'paymentIntents.create', () =>
      this.#client.paymentIntents.create(
        {
          amount: input.amountMinor,
          currency: input.currency.toLowerCase(),
          application_fee_amount: input.applicationFeeMinor,
          metadata: input.metadata,
        },
        {
          stripeAccount: input.connectedAccountId,
          idempotencyKey,
        },
      ),
    );
    return {
      paymentIntentId: pi.id,
      clientSecret: pi.client_secret ?? '',
      status: pi.status,
    };
  }
```

**New methods to add:** `createOnboardingSession` (embedded Account Sessions), `exchangeOAuthCode` (Standard OAuth token exchange), `verifyWebhookSignature` (extract to port).

---

### `apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.ts` (stub adapter, request-response)

**Analog:** `apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts`

**Action:** Create a stub that `implements PaymentProviderPort` returning canned values:

```typescript
import { Injectable } from '@nestjs/common';
import type { PaymentProviderPort } from '../../domain/ports';

@Injectable()
export class StubProviderAdapter implements PaymentProviderPort {
  async ensureOnboardingAccount(): Promise<{ accountId: string }> {
    return { accountId: 'stub_acct_12345' };
  }

  async createOnboardingSession(): Promise<{ clientSecret: string }> {
    return { clientSecret: 'stub_secret_test' };
  }

  // ... all other methods, never throws, returns stub values
}
```

---

### `apps/api/src/contexts/tenancy/domain/brand.aggregate.ts` (aggregate, CRUD + event-driven)

**Analog:** `apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts`

**Snapshot interface pattern** (lines 35–55):

```typescript
export interface TenantSnapshot {
  readonly id: TenantId;
  readonly slug: TenantSlug;
  readonly displayName: string;
  readonly status: TenantStatus;
  readonly stripeAccountId: string | null;
  readonly stripeChargesEnabled: boolean;
  readonly stripePayoutsEnabled: boolean;
  readonly stripeOnboardingStatus: StripeOnboardingStatus;
  readonly stripeRequirementsDue: unknown;
  // ... other fields
}
```

**Aggregate methods pattern** (snapshot-based):

```typescript
export class Tenant {
  readonly #events: TenantDomainEvent[] = [];

  private constructor(private snapshot: TenantSnapshot) {}

  static fromSnapshot(snapshot: TenantSnapshot): Tenant {
    return new Tenant(snapshot);
  }

  applyStripeCapabilities(input: ApplyStripeCapabilitiesInput): void {
    this.snapshot = {
      ...this.snapshot,
      stripeChargesEnabled: input.chargesEnabled,
      stripePayoutsEnabled: input.payoutsEnabled,
      stripeOnboardingStatus: input.onboardingStatus,
      stripeRequirementsDue: input.requirementsDue,
    };
    this.#events.push({ kind: 'StripeCapabilitiesApplied', ... });
  }

  pullEvents(): readonly TenantDomainEvent[] {
    const events = [...this.#events];
    this.#events.length = 0;
    return events;
  }
}
```

**Action:** Extend `BrandSnapshot` with payment-connection fields:

- `paymentProvider: PaymentProvider`
- `accountType: AccountType`
- `stripeAccountId: string | null`
- `stripeChargesEnabled: boolean`
- `stripePayoutsEnabled: boolean`
- `stripeOnboardingStatus: StripeOnboardingStatus`
- `stripeRequirementsDue: unknown`
- `defaultCurrency: Currency` (move from tenant, already present)

Add methods: `linkPaymentAccount(...)`, `applyPaymentCapabilities(...)`, `canAcceptPayments()`.

---

### `apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts` (service, request-response)

**Analog:** `apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts` (per-tenant onboarding)

**Service pattern** (dependency injection):

```typescript
@Injectable()
export class StartStripeOnboardingService {
  constructor(
    @Inject(STRIPE_CONNECT_PORT) private readonly port: StripeConnectPort,
    private readonly repo: TenantRepository,
  ) {}

  async execute(
    input: StartStripeOnboardingInput,
  ): Promise<StartStripeOnboardingOutput> {
    // orchestrate tenant + port + repo
  }
}
```

**Action:** Create per-brand equivalent with methods:

- `startEmbeddedOnboarding(brandId, slug)` → account session endpoint
- `startOAuthOnboarding(brandId, slug)` → OAuth authorize URL
- `handleOAuthCallback(code, state)` → link the account

---

### `packages/db/src/schema/brands.ts` (schema, DDL)

**Analog:** `packages/db/src/schema/brands.ts` (existing) + `packages/db/src/schema/tenants.ts`

**Schema column pattern** (lines 26–76):

```typescript
export const brands = pgTable(
  'brands',
  {
    id: pkUuid(),
    tenantId: tenantIdColumn(),
    slug: citext('slug').notNull(),
    displayName: text('display_name').notNull(),
    status: text('status').notNull().default('active'),
    locale: text('locale'),
    defaultCurrency: text('default_currency'),  // already present
    theme: jsonb('theme').$type<Record<string, unknown> | null>(),
    stripeAccountId: text('stripe_account_id'),  // already present, currently unused
    // ... existing columns ...
  },
  (table) => [
    foreignKey({...}).onDelete('cascade'),
    uniqueIndex('brands_tenant_slug_uq').on(table.tenantId, table.slug),
    // ... existing indexes/constraints ...
  ],
);
```

**Action:** Add these columns to `brands` table (alongside existing `stripe_account_id`):

- `paymentProvider: text('payment_provider').default('stripe')` — check constraint `IN ('stripe')`
- `accountType: text('account_type')` — check constraint `IN ('express', 'standard')`
- `stripeChargesEnabled: boolean('stripe_charges_enabled').default(false)`
- `stripePayoutsEnabled: boolean('stripe_payouts_enabled').default(false)`
- `stripeOnboardingStatus: text('stripe_onboarding_status').default('not_started')` — check constraint
- `stripeRequirementsDue: jsonb('stripe_requirements_due')`

Keep the existing composite-FK + RLS (inherited from `brands` table setup).

---

### `packages/db/migrations/0057_brand_stripe_connect.sql` (migration, DDL)

**Analog:** `packages/db/migrations/0055_payments_money_path.sql` + `0013_brands_rls.sql`

**Migration structure pattern** (0055):

```sql
-- Phase 8 plan 01: payments/order/tenant money-path schema redesign
-- Adds: PaymentIntent model, partial-refund accounting, SCA state,
-- Stripe-account linkage, tenant capability flags, guest email (B2/GNOTIF).

--> statement-breakpoint
-- ─── payments: new columns ──────────────────────────────────────────────────
ALTER TABLE "payments"
  ADD COLUMN IF NOT EXISTS "payment_intent_id"    text,
  ...
--> statement-breakpoint
ALTER TABLE "payments" DROP CONSTRAINT IF EXISTS "payments_status_chk";
--> statement-breakpoint
ALTER TABLE "payments"
  ADD CONSTRAINT "payments_status_chk"
  CHECK (status IN ('pending','requires_action','succeeded','failed','refunded','partially_refunded'));
--> statement-breakpoint
CREATE UNIQUE INDEX IF NOT EXISTS "payments_payment_intent_id_uq"
  ON "payments" ("tenant_id", "payment_intent_id")
  WHERE "payment_intent_id" IS NOT NULL;
--> statement-breakpoint
```

**RLS migration pattern** (0013):

```sql
ALTER TABLE brands ENABLE ROW LEVEL SECURITY;
--> statement-breakpoint
ALTER TABLE brands FORCE ROW LEVEL SECURITY;
--> statement-breakpoint
CREATE POLICY brands_iso ON brands
  USING (is_system_session() OR tenant_id = current_tenant_id())
  WITH CHECK (is_system_session() OR tenant_id = current_tenant_id());
--> statement-breakpoint
```

**Action:** Create migration file `packages/db/migrations/0057_brand_stripe_connect.sql`:

```sql
-- Phase 08.1 plan 01: per-brand Stripe Connect columns
-- Moves tenant.stripe_* columns to brand.stripe_* (clean reshape, no backfill)
-- Adds: paymentProvider, accountType, per-brand currency

--> statement-breakpoint
-- ─── brands: add payment-connection columns ──────────────────────────
ALTER TABLE "brands"
  ADD COLUMN IF NOT EXISTS "payment_provider"        text NOT NULL DEFAULT 'stripe',
  ADD COLUMN IF NOT EXISTS "account_type"            text,
  ADD COLUMN IF NOT EXISTS "stripe_charges_enabled"  boolean NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "stripe_payouts_enabled"  boolean NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "stripe_onboarding_status" text NOT NULL DEFAULT 'not_started',
  ADD COLUMN IF NOT EXISTS "stripe_requirements_due" jsonb;
--> statement-breakpoint
-- Check constraints for new enum columns
ALTER TABLE "brands"
  ADD CONSTRAINT "brands_payment_provider_chk"
  CHECK (payment_provider IN ('stripe'));
--> statement-breakpoint
ALTER TABLE "brands"
  ADD CONSTRAINT "brands_account_type_chk"
  CHECK (account_type IS NULL OR account_type IN ('express', 'standard'));
--> statement-breakpoint
ALTER TABLE "brands"
  ADD CONSTRAINT "brands_stripe_onboarding_status_chk"
  CHECK (stripe_onboarding_status IN ('not_started','pending','complete','restricted'));
--> statement-breakpoint
-- ─── tenants: DROP the old per-tenant Stripe columns (D-06, no backfill) ──────
ALTER TABLE "tenants" DROP COLUMN IF EXISTS "stripe_account_id";
--> statement-breakpoint
ALTER TABLE "tenants" DROP COLUMN IF EXISTS "stripe_charges_enabled";
--> statement-breakpoint
ALTER TABLE "tenants" DROP COLUMN IF EXISTS "stripe_payouts_enabled";
--> statement-breakpoint
ALTER TABLE "tenants" DROP COLUMN IF EXISTS "stripe_onboarding_status";
--> statement-breakpoint
ALTER TABLE "tenants" DROP COLUMN IF EXISTS "stripe_requirements_due";
--> statement-breakpoint
-- Drop the check constraint on tenants (now on brands)
ALTER TABLE "tenants" DROP CONSTRAINT IF EXISTS "tenants_stripe_onboarding_status_chk";
--> statement-breakpoint
-- ─── finalize RLS (brands already RLS-enabled; just restate for clarity) ──────
DO $grant$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'resto_app') THEN
    GRANT SELECT, INSERT, UPDATE ON TABLE brands TO resto_app;
  END IF;
END
$grant$;
--> statement-breakpoint
```

---

### `apps/admin/src/lib/brand-payments-api.ts` (API client, request-response)

**Analog:** `apps/admin/src/lib/payments-api.ts`

**Pattern** (lines 1–20):

```typescript
import { apiFetch } from '@/lib/api-client';

export type StripeOnboardingStatus =
  | 'not_started'
  | 'pending'
  | 'complete'
  | 'restricted';

export interface StripeStatusResponse {
  readonly onboardingStatus: StripeOnboardingStatus;
  readonly chargesEnabled: boolean;
  readonly payoutsEnabled: boolean;
  readonly canAcceptPayments: boolean;
  readonly requirementsDue: unknown;
}

export interface StripeOnboardingResponse {
  readonly onboardingUrl: string;
}

export const getStripeStatus = () =>
  apiFetch<StripeStatusResponse>('/v1/tenancy/stripe-status');

export const startStripeOnboarding = () =>
  apiFetch<StripeOnboardingResponse>('/v1/tenancy/stripe-onboarding', {
    method: 'POST',
  });
```

**Action:** Create similar per-brand endpoints:

```typescript
export const getBrandPaymentStatus = (brandSlug: string) =>
  apiFetch<PaymentProviderStatus>(
    `/v1/tenancy/brands/${brandSlug}/payment-status`,
    {
      brandSlug,
    },
  );

export const startBrandEmbeddedOnboarding = (brandSlug: string) =>
  apiFetch<{ clientSecret: string }>(
    `/v1/tenancy/brands/${brandSlug}/onboarding/account-session`,
    {
      method: 'POST',
      brandSlug,
    },
  );

export const startBrandOAuthOnboarding = (brandSlug: string) =>
  apiFetch<{ authorizeUrl: string }>(
    `/v1/tenancy/brands/${brandSlug}/onboarding/oauth/start`,
    {
      brandSlug,
    },
  );
```

---

### `apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx` (component, request-response)

**Analog:** Existing payouts component (same file)

**Pattern:** Use `apiFetch` + `useEffect` for data fetching, render status + method choice + embedded component.

**Action:** Extend existing payouts component to:

1. Call `getBrandPaymentStatus()` on mount
2. Render method-choice UI: "Create new (Express)" vs "Connect existing (Standard)"
3. On method selection, either:
   - **Express:** call `startBrandEmbeddedOnboarding()` → render `<ConnectAccountOnboarding onExit={() => refetch} />` inside `<ConnectComponentsProvider>`
   - **Standard:** call `startBrandOAuthOnboarding()` → redirect to the OAuth authorize URL
4. On component exit (Express) or successful callback (Standard), refetch status

---

### `apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts` (controller, request-response)

**Analog:** `apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts`

**Controller pattern:**

```typescript
import { Controller, Post, Get, Param, Query, Body } from '@nestjs/common';
import { RequireActiveTenant, Permissions } from '@/shared/decorators';
import { BrandOnboardingService } from '../../application/...';

@Controller('v1/tenancy/brands/:slug/onboarding')
export class BrandOnboardingController {
  constructor(private readonly service: BrandOnboardingService) {}

  @Post('account-session')
  @RequireActiveTenant()
  @Permissions({ tenant: ['transfer'] })
  async createAccountSession(
    @Param('slug') slug: string,
  ): Promise<{ clientSecret: string }> {
    return this.service.createEmbeddedSession({ slug });
  }

  @Get('oauth/start')
  @RequireActiveTenant()
  @Permissions({ tenant: ['transfer'] })
  async startOAuth(
    @Param('slug') slug: string,
  ): Promise<{ authorizeUrl: string }> {
    return this.service.startOAuth({ slug });
  }

  @Get('oauth/callback')
  async handleOAuthCallback(
    @Query('code') code: string,
    @Query('state') state: string,
  ): Promise<void> {
    await this.service.handleOAuthCallback({ code, state });
    // redirect to admin UI
  }
}
```

---

### `apps/api/src/config/env.schema.ts` (config, request-response)

**Analog:** Existing env schema

**Pattern:** Zod env validation with required/optional fields.

**Action:** Add/verify these env vars:

```typescript
STRIPE_SECRET_KEY: z.string().min(1),
STRIPE_WEBHOOK_SECRET: z.string().min(1),
STRIPE_CONNECT_CLIENT_ID: z.string().min(1),
STRIPE_APPLICATION_FEE_AMOUNT: z.coerce.number().nonnegative(),
STRIPE_CONNECT_RETURN_URL: z.string().url(),
STRIPE_CONNECT_REFRESH_URL: z.string().url(),
```

For admin Vite SPA env, add:

```typescript
VITE_STRIPE_PUBLISHABLE_KEY: z.string().min(1), // in admin's env schema with fail-loud guard
```

---

### `apps/api/eslint.config.mjs` (architecture test, request-response)

**Analog:** `apps/api/eslint.config.mjs` (existing no-restricted-imports rules)

**Existing pattern** (lines 29–44):

```javascript
{
  files: ['src/**/*.ts'],
  rules: {
    'no-restricted-imports': [
      'error',
      {
        paths: [
          {
            name: '@resto/db',
            importNames: ['runInTenantContext'],
            message:
              'ADR-0020 I-6: runInTenantContext is HTTP-middleware-only...',
          },
        ],
      },
    ],
  },
},
```

**Action:** Add new override block to forbid `stripe` imports in payments application/domain:

```javascript
{
  files: ['src/contexts/payments/application/**/*.ts', 'src/contexts/payments/domain/**/*.ts'],
  rules: {
    'no-restricted-imports': [
      'error',
      {
        paths: [
          {
            name: 'stripe',
            message:
              'PAY-16: payments app/domain must depend on PaymentProviderPort, not the Stripe SDK. Stripe stays in infrastructure/stripe/.',
          },
        ],
        patterns: ['**/infrastructure/stripe/**'],
      },
    ],
  },
},
```

---

## Shared Patterns

### Symbol Token DI Pattern

**Source:** `apps/api/src/contexts/tenancy/domain/ports.ts` (lines 41, 124, 166)

**Apply to:** All port interfaces and module wiring

```typescript
// domain/ports.ts
export const PAYMENT_PROVIDER_PORT = Symbol('PAYMENT_PROVIDER_PORT');
export interface PaymentProviderPort { ... }

// tenancy.module.ts (wiring example)
{
  provide: PAYMENT_PROVIDER_PORT,
  inject: [ENV_TOKEN],
  useFactory: (env: Env) => new StripeProviderAdapter(stripe, env),
},
```

### Aggregate Event Pattern

**Source:** `apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts` (lines 83–150)

**Apply to:** Brand aggregate payment methods

```typescript
// Private event array
readonly #events: BrandDomainEvent[] = [];

// Method that mutates snapshot + emits event
linkPaymentAccount(accountId: string, accountType: string): void {
  this.snapshot = { ...this.snapshot, stripeAccountId: accountId, accountType };
  this.#events.push({
    kind: 'BrandPaymentAccountLinked',
    brandId: this.snapshot.id,
    accountId,
    occurredAt: new Date(),
  });
}

// Event drain
pullEvents(): readonly BrandDomainEvent[] {
  const events = [...this.#events];
  this.#events.length = 0;
  return events;
}
```

### Resilience Wrapper Pattern

**Source:** `apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts` (lines 87–132)

**Apply to:** Any Stripe adapter method

```typescript
async createPaymentIntent(input: CreatePaymentIntentInput): Promise<CreatePaymentIntentResult> {
  const pi = await withRetry(this.#logger, 'paymentIntents.create', () =>
    this.#client.paymentIntents.create({ ... }, { stripeAccount: input.connectedAccountId, ... }),
  );
  return { paymentIntentId: pi.id, clientSecret: pi.client_secret ?? '', status: pi.status };
}
```

### API Client Pattern (Admin SPA)

**Source:** `apps/admin/src/lib/api-client.ts` (lines 1–70)

**Apply to:** All brand-scoped API calls

```typescript
export const apiFetch = async <T>(
  path: string,
  opts: {
    method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
    body?: unknown;
    brandSlug?: string;
    signal?: AbortSignal;
  } = {},
): Promise<ApiFetchResult<T>> => {
  const timeoutMs = isGet ? TIMEOUT_GET_MS : TIMEOUT_MUTATION_MS;
  const timeoutSignal = AbortSignal.timeout(timeoutMs);
  const signal =
    opts.signal !== undefined
      ? AbortSignal.any([timeoutSignal, opts.signal])
      : timeoutSignal;
  const headers: Record<string, string> = {
    accept: 'application/json',
    ...(opts.brandSlug !== undefined ? { 'x-brand-slug': opts.brandSlug } : {}),
  };
  const res = await fetch(`${VITE_API_ORIGIN}${path}`, {
    method: opts.method ?? 'GET',
    headers,
    body: opts.body !== undefined ? JSON.stringify(opts.body) : undefined,
    credentials: 'include',
    signal,
  });
  // ... retry logic for GET 5xx only
};
```

### Migration Pattern (DDL)

**Source:** `packages/db/migrations/0055_payments_money_path.sql` (lines 10–102) + `0013_brands_rls.sql`

**Apply to:** Any schema reshape with RLS

```sql
--> statement-breakpoint
-- ─── table: new columns ──────────────────────────────────────────────────
ALTER TABLE "table_name"
  ADD COLUMN IF NOT EXISTS "new_col1" text,
  ADD COLUMN IF NOT EXISTS "new_col2" boolean NOT NULL DEFAULT false;
--> statement-breakpoint
-- Check constraint
ALTER TABLE "table_name"
  ADD CONSTRAINT "table_name_col_chk" CHECK (new_col IN ('a', 'b', 'c'));
--> statement-breakpoint
-- RLS (if new table)
ALTER TABLE "table_name" ENABLE ROW LEVEL SECURITY;
--> statement-breakpoint
ALTER TABLE "table_name" FORCE ROW LEVEL SECURITY;
--> statement-breakpoint
CREATE POLICY "table_name_iso" ON "table_name"
  USING (is_system_session() OR tenant_id = current_tenant_id())
  WITH CHECK (is_system_session() OR tenant_id = current_tenant_id());
--> statement-breakpoint
```

## No Analog Found

Files with sufficient precedent but requiring original implementation based on patterns:

| File                                                                          | Role       | Data Flow        | Reason                                                                                                   |
| ----------------------------------------------------------------------------- | ---------- | ---------------- | -------------------------------------------------------------------------------------------------------- |
| `apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts` | service    | request-response | Extends tenant onboarding with embedded account sessions + OAuth; per-brand scoping is new in this phase |
| `apps/admin/src/lib/brand-payments-api.ts`                                    | API client | request-response | Per-brand variant; tenant-level payments-api already exists; per-brand shape is phase-specific           |

## Metadata

**Analog search scope:** `apps/api/src/contexts/tenancy/`, `apps/api/src/contexts/payments/`, `packages/db/src/schema/`, `packages/db/migrations/`, `apps/admin/src/lib/`, `apps/api/eslint.config.mjs`

**Files scanned:** 13 core + 8 supporting (migration structure, RLS patterns, API client shape, ESLint config)

**Pattern extraction date:** 2026-06-28

**Coverage:**

- Exact-match analogs (same role + data flow): 8 files
- Role-match analogs (same role, adapted flow): 3 files
- No analog (original per phase spec): 2 files

---

## Cross-File Dependencies (Planning Notes)

1. **Port → Adapter → Module:** `PaymentProviderPort` in payments domain → `StripeProviderAdapter` in infrastructure → wire in both `TenancyModule` (old dependency) AND `PaymentsModule` (new consumer)
2. **Brand snapshot → Repository → Service → Controller:** Brand aggregate methods → `BrandDrizzleRepository.save()` appends events → `StartBrandOnboardingService` orchestrates port + repo → `BrandOnboardingController` exposes HTTP
3. **Schema → Migration → Repository:** `brands` table columns added → migration `0057` adds + validates → `BrandSnapshot` interface updated → repo materializes the shape
4. **Admin SPA:** `brand-payments-api.ts` calls endpoints from `BrandOnboardingController` → embeds `<ConnectAccountOnboarding>` component → refetches status via `apiFetch`

---

_Phase: 08.1-payments-provider-layer-and-onboarding-ux_
_Pattern mapping date: 2026-06-28_
