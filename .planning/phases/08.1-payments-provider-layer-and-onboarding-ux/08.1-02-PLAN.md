---
phase: 08.1-payments-provider-layer-and-onboarding-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/contexts/payments/domain/payment-provider.ts
  - apps/api/src/contexts/payments/domain/ports.ts
  - apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts
  - apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.ts
  - apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.spec.ts
  - apps/api/eslint.config.mjs
autonomous: true
requirements: [PAY-16]

must_haves:
  truths:
    - 'A provider-agnostic PaymentProviderPort (account onboarding, payment intent, refund, webhook verify) exists in the payments domain, keyed by PAYMENT_PROVIDER_PORT'
    - 'The real Stripe adapter lives in payments infrastructure and implements PaymentProviderPort, reusing the Phase 8 retry/timeout/idempotency wrapper'
    - 'A stub second-provider adapter implements PaymentProviderPort and proves substitutability (compile-time satisfies + canned returns)'
    - 'ESLint forbids importing the stripe SDK from payments application/domain (architecture test)'
    - 'A paymentProvider discriminator and accountType type exist as Zod enums'
  artifacts:
    - path: 'apps/api/src/contexts/payments/domain/ports.ts'
      provides: 'PaymentProviderPort interface + PAYMENT_PROVIDER_PORT Symbol token'
      contains: 'PaymentProviderPort'
    - path: 'apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts'
      provides: 'Real Stripe adapter implementing PaymentProviderPort (renamed from StripeConnectAdapter, extended with account session + OAuth + verifyWebhookSignature)'
      contains: 'implements PaymentProviderPort'
    - path: 'apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.ts'
      provides: 'Stub adapter proving the port is provider-neutral'
      contains: 'satisfies PaymentProviderPort'
    - path: 'apps/api/eslint.config.mjs'
      provides: 'no-restricted-imports rule forbidding stripe in payments app/domain'
      contains: 'PAY-16'
  key_links:
    - from: 'apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts'
      to: 'apps/api/src/contexts/payments/domain/ports.ts'
      via: 'implements PaymentProviderPort'
      pattern: 'implements PaymentProviderPort'
    - from: 'apps/api/eslint.config.mjs'
      to: 'payments/application + payments/domain'
      via: 'no-restricted-imports stripe'
      pattern: 'src/contexts/payments/(application|domain)'
---

<objective>
Generalize `StripeConnectPort` → provider-agnostic `PaymentProviderPort` in the **payments** context domain (D-03, PAY-16): move + rename the port, move + rename the existing `StripeConnectAdapter` → `StripeProviderAdapter` under `payments/infrastructure/stripe/` (reusing the battle-tested `withRetry`/`withTimeout`/idempotency wrapper), add the new methods (`createOnboardingSession`, `exchangeOAuthCode`, `verifyWebhookSignature`) as port contract + adapter stubs-to-be-filled-by-real-Stripe-calls, build a `StubProviderAdapter` that proves the seam, and add the ESLint `no-restricted-imports` architecture test forbidding `stripe` imports in payments application/domain. Add the `paymentProvider`/`accountType` Zod discriminators.

Purpose: This is the provider seam (ROADMAP SC#3). After this plan, checkout/refund/webhook (Plan 03) depend only on `PaymentProviderPort`; a second provider slots in via a new adapter + config only. NO runtime registry (YAGNI per D-03).
Output: `PaymentProviderPort`, `StripeProviderAdapter`, `StubProviderAdapter`, the ESLint boundary rule, and the discriminator enums.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-CONTEXT.md
@.planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-RESEARCH.md
@.planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-PATTERNS.md
@apps/CLAUDE.md

<interfaces>
<!-- Current StripeConnectPort being renamed/moved (extract from tenancy/domain/ports.ts) -->
From apps/api/src/contexts/tenancy/domain/ports.ts:
  export interface StripeConnectPort {
    ensureExpressAccount(input: { tenantId; displayName }): Promise<string | null>;
    createExpressAccount(input: CreateExpressAccountInput): Promise<CreateExpressAccountResult>;
    createAccountLink(input: CreateAccountLinkInput): Promise<CreateAccountLinkResult>;
    createPaymentIntent(input: CreatePaymentIntentInput): Promise<CreatePaymentIntentResult>;
    cancelPaymentIntent(input: CancelPaymentIntentInput): Promise<CancelPaymentIntentResult>;
    createRefund(input: CreateRefundInput): Promise<CreateRefundResult>;
    retrieveAccount(input: RetrieveAccountInput): Promise<RetrieveAccountResult>;
  }
  export const STRIPE_CONNECT_PORT = Symbol('STRIPE_CONNECT_PORT');
  // + the Create*Input/Result interfaces (lines 43-112) move WITH the port into payments domain

From apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts:
// withTimeout / withRetry helpers (lines 87-132), StripeClientLike (lines 40-80),
// @Injectable() class StripeConnectAdapter implements StripeConnectPort,
// createStripeClientAdapter(stripe, env, logger) factory (lines 255-277) — MOVE all of this.

From apps/api/eslint.config.mjs (existing no-restricted-imports block — PATTERNS.md lines 546-564):
{ files: ['src/**/*.ts'], rules: { 'no-restricted-imports': ['error', { paths: [
{ name: '@resto/db', importNames: ['runInTenantContext'], message: 'ADR-0020 I-6: ...' } ] }] } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaymentProviderPort + discriminator enums in payments domain; move Create*Input/Result types</name>
  <files>apps/api/src/contexts/payments/domain/ports.ts, apps/api/src/contexts/payments/domain/payment-provider.ts</files>
  <read_first>
    - apps/api/src/contexts/payments/domain/ports.ts (target — currently only PAYMENT_REPOSITORY; add the provider port + Symbol token here, payments-context style)
    - apps/api/src/contexts/tenancy/domain/ports.ts (source — StripeConnectPort lines 114-124 + Create*Input/Result interfaces lines 43-112; PATTERNS.md lines 27-55)
    - apps/api/src/contexts/tenancy/domain/tenant.aggregate.ts (StripeOnboardingStatus enum line 33; PATTERNS.md lines 59-75)
    - apps/CLAUDE.md (Zod-as-source-of-truth, branded vs unbranded)
  </read_first>
  <action>
    Create `payment-provider.ts`: `export const PaymentProvider = z.enum(['stripe']);` `export type PaymentProvider = z.infer<typeof PaymentProvider>;` `export const AccountType = z.enum(['express', 'standard']);` `export type AccountType = z.infer<typeof AccountType>;` `export const PaymentOnboardingStatus = z.enum(['not_started','pending','complete','restricted']);` with derived type (PATTERNS.md lines 68-75). Do NOT brand these — they cross the HTTP/DB boundary; keep unbranded so DTOs emit `type: string`.
    In `payments/domain/ports.ts` add `export interface PaymentProviderPort` with the existing payment/refund/account methods renamed provider-neutral plus the three new contract methods: `createOnboardingSession(input: { accountId: string }): Promise<{ clientSecret: string }>`; `exchangeOAuthCode(input: { code: string }): Promise<{ accountId: string }>`; `verifyWebhookSignature(input: { rawBody: Buffer; signature: string }): WebhookEvent` where `WebhookEvent` is typed as the Stripe event for now (`import type Stripe from 'stripe'` is allowed in the domain port type only because the type is erased at runtime — but prefer aliasing `export type WebhookEvent = Stripe.Event` to keep one Stripe reference; if ESLint forbids it in domain, define `WebhookEvent = unknown` and narrow in the handler). Keep `ensureOnboardingAccount`/`createOnboardingLink` (renamed from ensureExpressAccount/createAccountLink) carrying an `accountType` param. Move the `Create*Input`/`Create*Result`/`Cancel*`/`Refund*`/`RetrieveAccount*` interfaces from tenancy ports into this file (or a sibling `provider-io.ts`), swapping `tenantId` for `brandId` where present. Export `export const PAYMENT_PROVIDER_PORT = Symbol('PAYMENT_PROVIDER_PORT');`.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && grep -c 'PaymentProviderPort\|PAYMENT_PROVIDER_PORT\|createOnboardingSession\|exchangeOAuthCode\|verifyWebhookSignature' apps/api/src/contexts/payments/domain/ports.ts</automated>
  </verify>
  <acceptance_criteria>
    - `payments/domain/ports.ts` contains `export interface PaymentProviderPort` and `export const PAYMENT_PROVIDER_PORT = Symbol('PAYMENT_PROVIDER_PORT')`.
    - `PaymentProviderPort` declares `createPaymentIntent`, `cancelPaymentIntent`, `createRefund`, `retrieveAccount`, `createOnboardingSession`, `exchangeOAuthCode`, `verifyWebhookSignature`.
    - `payment-provider.ts` exports `PaymentProvider` (Zod enum with `'stripe'`) and `AccountType` (Zod enum `'express'|'standard'`).
    - `grep` count from the verify command is ≥ 5.
  </acceptance_criteria>
  <done>Provider-agnostic port + Symbol token + discriminator enums exist in payments domain with the new onboarding/OAuth/webhook-verify methods on the contract.</done>
</task>

<task type="auto">
  <name>Task 2: Move + rename StripeConnectAdapter → StripeProviderAdapter; add account-session, OAuth, verifyWebhookSignature methods</name>
  <files>apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/infrastructure/stripe-connect.adapter.ts (source — copy the entire file: withTimeout/withRetry lines 87-132, StripeClientLike lines 40-80, class lines 134-253, factory lines 255-277; PATTERNS.md lines 79-157)
    - apps/api/src/contexts/payments/domain/ports.ts (the PaymentProviderPort it must implement — from Task 1)
    - .planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-RESEARCH.md (Code Examples lines 372-410: accountSessions.create with `components: { account_onboarding: { enabled: true } }`; `stripe.oauth.token({ grant_type: 'authorization_code', code })` → `stripe_user_id`; `Stripe.webhooks.constructEvent`; Pitfall 1 lines 334-338 — KEEP `type: 'express'`, NO controller properties)
  </read_first>
  <action>
    Create `stripe-provider.adapter.ts` as the moved+renamed adapter: class `StripeProviderAdapter implements PaymentProviderPort`. Carry over `withTimeout`/`withRetry`/`isRetryable`/`getStatusCode` and the existing `createPaymentIntent`/`cancelPaymentIntent`/`createRefund`/`retrieveAccount`/`ensureOnboardingAccount`(was ensureExpressAccount)/`createOnboardingLink`(was createAccountLink) bodies UNCHANGED except the rename. In `ensureOnboardingAccount`, KEEP `accounts.create({ type: 'express', capabilities: { card_payments: { requested: true }, transfers: { requested: true } }, ... })` exactly — do NOT introduce `controller` properties (RESEARCH Pitfall 1, the #1 money-model trap). Extend `StripeClientLike` with `accountSessions.create`, `oauth.token`, and keep webhook verification via the static `Stripe.webhooks.constructEvent`. Add `createOnboardingSession({ accountId })` → `accountSessions.create({ account: accountId, components: { account_onboarding: { enabled: true } } })` wrapped in `withRetry`, returns `{ clientSecret: session.client_secret }`. Add `exchangeOAuthCode({ code })` → `oauth.token({ grant_type: 'authorization_code', code })`, returns `{ accountId: resp.stripe_user_id }`; do NOT persist `access_token` (deprecated, RESEARCH State of the Art). Add `verifyWebhookSignature({ rawBody, signature })` returning `Stripe.webhooks.constructEvent(rawBody, signature, this.#webhookSecret)` — pass `STRIPE_WEBHOOK_SECRET` into the adapter constructor/env. Update `createStripeClientAdapter` factory to wire `accountSessions`, `oauth`, and the webhook secret. Delete the old tenancy adapter file in Plan 03 (when wiring moves); for now create the new file.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && grep -E "type: 'express'" apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts && grep -c 'createOnboardingSession\|exchangeOAuthCode\|verifyWebhookSignature\|account_onboarding' apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `stripe-provider.adapter.ts` contains `class StripeProviderAdapter implements PaymentProviderPort`.
    - The file contains the literal `type: 'express'` and does NOT contain the substring `controller` in any `accounts.create` call (grep `controller` returns 0 hits inside `accounts.create` params).
    - The file contains `createOnboardingSession`, `exchangeOAuthCode`, `verifyWebhookSignature`, and `components: { account_onboarding: { enabled: true } }`.
    - `exchangeOAuthCode` returns `stripe_user_id` and the file does NOT persist `access_token`.
    - `withRetry` / `withTimeout` helpers are present (resilience wrapper reused).
  </acceptance_criteria>
  <done>The real adapter is moved to payments infrastructure, implements PaymentProviderPort, keeps `type: 'express'`, and has account-session + OAuth + webhook-verify methods reusing the resilience wrapper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub provider adapter + ESLint architecture test (no stripe import in payments app/domain)</name>
  <files>apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.ts, apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.spec.ts, apps/api/eslint.config.mjs</files>
  <read_first>
    - apps/api/src/contexts/payments/domain/ports.ts (the PaymentProviderPort to satisfy — Task 1)
    - apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts (role analog — Task 2)
    - apps/api/eslint.config.mjs (existing no-restricted-imports block to extend — PATTERNS.md lines 546-587; RESEARCH lines 282-294)
    - .planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-RESEARCH.md (Pattern 4 lines 278-294 — stub + satisfies + ESLint boundary as the architecture test)
  </read_first>
  <behavior>
    - `StubProviderAdapter` satisfies `PaymentProviderPort` at compile time (`const _check: PaymentProviderPort = new StubProviderAdapter()` or a `satisfies` assertion), proving the port is implementable without Stripe.
    - Every method returns a deterministic canned value (e.g. `createOnboardingSession` → `{ clientSecret: 'stub_secret_test' }`, `exchangeOAuthCode` → `{ accountId: 'stub_acct_12345' }`) and never throws.
    - ESLint errors when `import Stripe from 'stripe'` (or any `import ... from 'stripe'`) is added to a file under `payments/application/**` or `payments/domain/**`.
  </behavior>
  <action>
    Create `stub-provider.adapter.ts`: `@Injectable() export class StubProviderAdapter implements PaymentProviderPort` returning canned values for all methods (PATTERNS.md lines 166-181). It MUST NOT import `stripe`.
    Create `stub-provider.adapter.spec.ts`: a Vitest test that (a) instantiates the stub and asserts each method resolves to its canned shape, and (b) includes a type-level `satisfies PaymentProviderPort` assertion so `tsc` proves substitutability.
    In `apps/api/eslint.config.mjs` append a new override block (after the existing `no-restricted-imports` block) targeting `files: ['src/contexts/payments/application/**/*.ts', 'src/contexts/payments/domain/**/*.ts']` with `'no-restricted-imports': ['error', { paths: [{ name: 'stripe', message: 'PAY-16: payments app/domain must depend on PaymentProviderPort, not the Stripe SDK. Stripe stays in infrastructure/stripe/.' }], patterns: ['**/infrastructure/stripe/**'] }]` (RESEARCH lines 284-293). NOTE: if `WebhookEvent = Stripe.Event` was used in the domain port (Task 1), exclude `ports.ts` from this rule via a tighter glob OR switch the port to `WebhookEvent = unknown` so the boundary stays clean — prefer the latter.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api test stub-provider && pnpm --filter @resto/api exec eslint src/contexts/payments/application --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `stub-provider.adapter.ts` contains `class StubProviderAdapter implements PaymentProviderPort` and does NOT import `stripe` (`grep -c "from 'stripe'" stub-provider.adapter.ts` returns 0).
    - `stub-provider.adapter.spec.ts` passes and contains a `satisfies PaymentProviderPort` (or equivalent typed) assertion.
    - `apps/api/eslint.config.mjs` contains `PAY-16` and the glob `src/contexts/payments/application`.
    - Adding `import Stripe from 'stripe'` to any `payments/application/**` file produces an ESLint error (the orchestrator may verify by a throwaway lint of a temp file, then revert).
    - `eslint src/contexts/payments/application --max-warnings 0` passes on the real (Stripe-free) application code.
  </acceptance_criteria>
  <done>Stub adapter proves the port is provider-neutral (compile-time + runtime), and ESLint enforces no `stripe` import in payments application/domain — the architecture test for PAY-16.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                              |
| -------------------------------------- | ------------------------------------------------------------------------ |
| payments app/domain → Stripe SDK       | The provider seam: application/domain must never import the concrete SDK |
| Stripe webhook raw body → parsed event | Signature verification is the integrity gate; it moves into the port     |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| ------------ | ---------------------- | -------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08.1-02-01 | Tampering              | Webhook signature verification through the port refactor | mitigate    | `verifyWebhookSignature` delegates to `Stripe.webhooks.constructEvent(rawBody, signature, secret)` (constant-time, replay-window-aware); never hand-rolled HMAC (RESEARCH Don't-Hand-Roll). The route + raw-body handling stay Stripe-specific (D-08) and unchanged until Plan 03 rewires the call. |
| T-08.1-02-02 | Information Disclosure | Stripe SDK leaking into the provider-agnostic layer      | mitigate    | ESLint `no-restricted-imports` (the architecture test) blocks `stripe` imports in payments application/domain; the stub adapter proves substitutability.                                                                                                                                            |
| T-08.1-02-03 | Information Disclosure | OAuth `access_token` over-persistence                    | mitigate    | `exchangeOAuthCode` returns only `stripe_user_id`; `access_token`/`stripe_publishable_key` are deprecated and explicitly not stored (RESEARCH State of the Art).                                                                                                                                    |
| T-08.1-02-SC | Tampering              | npm/pip/cargo installs                                   | mitigate    | No new package installs in this plan (server `stripe` already present); package legitimacy checkpoints apply in Plan 04 for the `@stripe/*` browser packages.                                                                                                                                       |

</threat_model>

<verification>
- `PaymentProviderPort` + `PAYMENT_PROVIDER_PORT` exist in payments domain.
- `StripeProviderAdapter implements PaymentProviderPort`, keeps `type: 'express'`, adds the three new methods.
- Stub adapter compiles against the port and its spec passes.
- ESLint architecture rule present and the real application code lints clean.
</verification>

<success_criteria>

- The payments application code can depend only on a provider-agnostic `PaymentProviderPort` (ROADMAP SC#3).
- A second provider slots in via a new adapter + config only — proven by the stub adapter + ESLint architecture test.
- `paymentProvider` discriminator exists as a Zod enum.
  </success_criteria>

<output>
Create `.planning/phases/08.1-payments-provider-layer-and-onboarding-ux/08.1-02-SUMMARY.md` when done.
</output>
