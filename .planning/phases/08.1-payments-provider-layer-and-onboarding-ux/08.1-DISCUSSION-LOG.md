# Phase 08.1: Payments — Provider Layer & Onboarding UX - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-28
**Phase:** 08.1-payments-provider-layer-and-onboarding-ux
**Areas discussed:** Onboarding methods, Embedded vs hosted, Provider abstraction depth, Account/provider scoping, Currency reconciliation, Migration

---

## Onboarding methods (PAY-15 / PAY-17)

| Option                | Description                                              | Selected |
| --------------------- | -------------------------------------------------------- | -------- |
| Both, Express default | Express primary CTA, Standard OAuth secondary            | ✓        |
| Only Express now      | Standard OAuth as a fast-follow; PAY-15 drops from phase |          |
| Both, equal weight    | Two equal options, no default                            |          |

**User's choice:** Both, Express default.
**Notes:** Most restaurants have no Stripe → Express is the low-friction default; Standard serves tenants who already run Stripe.

---

## Embedded vs hosted (PAY-14)

| Option                             | Description                                                  | Selected |
| ---------------------------------- | ------------------------------------------------------------ | -------- |
| Embedded-primary + hosted fallback | Connect.js in admin primary; hosted account_link as fallback | ✓        |
| Fully embedded                     | Drop hosted entirely                                         |          |
| Keep hosted                        | Don't build embedded; PAY-14 drops                           |          |

**User's choice:** Embedded-primary + hosted fallback.
**Notes:** Embedded applies to Express only; Standard uses OAuth. Lower risk — always a fallback path for resume/errors/uncovered regions.

---

## Provider abstraction depth (PAY-16)

| Option               | Description                                                                                     | Selected |
| -------------------- | ----------------------------------------------------------------------------------------------- | -------- |
| Minimum + stub proof | Generalize port + paymentProvider discriminator + stub adapter + arch test; no runtime registry | ✓        |
| Full registry        | Config-driven provider selection, factory, runtime multi-provider now                           |          |
| No abstraction now   | Stripe-only; PAY-16 drops                                                                       |          |

**User's choice:** Minimum + stub proof.
**Notes:** Only Stripe is real today; Mollie/Adyen are PAYMP-01/02. Avoid YAGNI — prove the seam without a runtime registry.

---

## Account / provider scoping

| Option                                 | Description                                                                   | Selected |
| -------------------------------------- | ----------------------------------------------------------------------------- | -------- |
| Per-tenant, schema ready for per-brand | One account per tenant, modeled to add per-brand without a breaking migration |          |
| Per-brand immediately                  | Each brand connects its own Stripe                                            | ✓        |
| Strictly per-tenant                    | Simplest; rework if multibrand with separate legal entities arrives           |          |

**User's choice:** Per-brand immediately.
**Notes:** Conflicts with Phase 8 D-05 (one currency per tenant) and the as-implemented per-tenant linkage — triggered the currency + migration follow-ups below.

---

## Currency reconciliation (revises Phase 8 D-05)

| Option                                 | Description                                                                   | Selected |
| -------------------------------------- | ----------------------------------------------------------------------------- | -------- |
| Currency also per-brand                | Each brand: own account + own currency, set at brand onboarding; revises D-05 | ✓        |
| Account per-brand, currency per-tenant | Different accounts but one tenant currency                                    |          |
| Revert to per-tenant                   | Drop per-brand                                                                |          |

**User's choice:** Currency also per-brand.
**Notes:** Logical consequence of a per-brand connected account. Phase 8 D-05 is explicitly revised.

---

## Migration (tenant-level linkage → brand)

| Option                  | Description                                                          | Selected |
| ----------------------- | -------------------------------------------------------------------- | -------- |
| Clean reshape to brand  | Move columns to brand, drop tenant-level; no backfill (no prod data) | ✓        |
| tenant as default-brand | Keep tenant-level as fallback/default                                |          |
| Planner decides         | Leave migration mechanics to research/plan                           |          |

**User's choice:** Clean reshape to brand.
**Notes:** No live connected accounts (prod stand-up deferred to first customer) → nothing to back-fill.

---

## Claude's Discretion

- Exact `PaymentProviderPort` signatures; embedded `account_session` endpoint shape; Connect.js React integration in the Vite SPA; migration mechanics (column moves, composite-FK `(brand_id, tenant_id)`, RLS); `accountType` onboarding-branch structure in admin UI; stub-adapter + architecture-test mechanism; `paymentProvider`/`accountType` enum vs lookup; Standard OAuth Connect app registration/config.
- Webhook: `verifyWebhookSignature` in the port, HTTP route stays Stripe-only until provider #2 (D-08) — accepted as a stated default, not separately voted.

## Deferred Ideas

- RestOS monetization model (commission vs flat subscription vs hybrid) → FIN-07, Phase 14.
- Full multi-provider runtime registry + provider-aware webhook routing → PAYMP-01/02/03.
- Custom (white-labeled) Connect accounts → not now.
- Multi-currency within a brand → one currency per brand.
- Reviewed-not-folded todo: `restructure-roadmap-ai-driven` (keyword false-match, out of scope).
