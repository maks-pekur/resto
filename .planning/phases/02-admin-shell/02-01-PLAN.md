---
phase: 02-admin-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/lib/env.ts
  - apps/admin/instrumentation.ts
  - apps/admin/lib/api-server.ts
  - apps/admin/lib/api-server-internal.ts
  - apps/admin/lib/actions/set-active-brand.ts
  - apps/admin/lib/actions/create-brand.ts
  - apps/admin/app/login/page.tsx
  - apps/admin/proxy.ts
  - apps/admin/.env.example
  - apps/admin/test/env.spec.ts
  - apps/admin/test/api-server.spec.ts
  - apps/admin/test/set-active-brand-action.spec.ts
  - apps/admin/test/create-brand-action.spec.ts
  - apps/admin/test/proxy.spec.ts
autonomous: true
requirements:
  - ADM-02
  - ADM-08

must_haves:
  truths:
    - 'Booting `apps/admin` in `NODE_ENV=production` without `NEXT_PUBLIC_API_ORIGIN`, `ADMIN_WEB_URL`, or `INTERNAL_API_TOKEN` crashes the process at startup (`instrumentation.ts`), not on the first request (ADM-08, CONTEXT D-05, D-06).'
    - "No `?? 'http://localhost:3001'` (or analogous) production fallback string remains in any `apps/admin/lib/*.ts` file (apps/CLAUDE.md env-var rule)."
    - 'Every server-side `fetch` in `apps/admin/lib/api-server.ts` uses `AbortSignal.timeout(10_000)` for GETs and `AbortSignal.timeout(30_000)` for mutations (apps/CLAUDE.md network rule, CONTEXT D-09).'
    - 'A single retry with ~500ms backoff is performed on idempotent GET 5xx responses; mutations are NEVER retried (apps/CLAUDE.md, CONTEXT D-11).'
    - 'The `getActiveTenantId` helper (which hits the idempotent GET `/api/auth/get-session`) reuses the same one-retry-on-5xx + 500ms-backoff semantics as `apiFetch` GETs, plus the 10s `AbortSignal.timeout` — `/api/auth/get-session` is idempotent and benefits from the same reliability profile.'
    - 'Server-side `apiFetch` receiving HTTP 401 calls `redirect(''/login?expired=1'')` directly; the login page reads `?expired=1` and renders a "session expired" notice (CONTEXT D-10).'
    - "Cookies written by `set-active-brand.ts` and `create-brand.ts` include `secure: process.env.NODE_ENV === 'production'` (CONTEXT D-04, apps/CLAUDE.md cookie rule)."
    - '`INTERNAL_API_TOKEN` accessor is exported only from server-only `apps/admin/lib/env.ts`; importing it from a client boundary fails at build time (apps/CLAUDE.md `INTERNAL_API_TOKEN` rule).'
    - 'The `proxy.ts` middleware refines the `next=` redirect target against open-redirect: only same-origin paths beginning with a single `/` and NOT `//` (protocol-relative) are accepted; everything else falls through to `next=/dashboard` (apps/CLAUDE.md open-redirect rule).'
  artifacts:
    - path: 'apps/admin/lib/env.ts'
      provides: 'Zod-validated env accessors for `NEXT_PUBLIC_API_ORIGIN`, `ADMIN_WEB_URL`, `INTERNAL_API_TOKEN`, `ACTIVE_BRAND_COOKIE_SECRET`; throws at module load in non-dev.'
      contains: 'z.object'
    - path: 'apps/admin/instrumentation.ts'
      provides: 'Next.js 16 boot hook that imports `./lib/env` so missing env crashes startup before any request renders.'
      contains: 'register'
    - path: 'apps/admin/lib/api-server.ts'
      provides: '`apiFetch` with AbortSignal timeouts, one-retry on idempotent GET 5xx, 401 redirect-to-login.'
      contains: 'AbortSignal.timeout'
    - path: 'apps/admin/lib/actions/set-active-brand.ts'
      provides: "Cookie write with `secure: process.env.NODE_ENV === 'production'`."
      contains: 'secure:'
    - path: 'apps/admin/lib/actions/create-brand.ts'
      provides: "Cookie write with `secure: process.env.NODE_ENV === 'production'`."
      contains: 'secure:'
    - path: 'apps/admin/app/login/page.tsx'
      provides: 'Renders "Your session expired. Please sign in again." notice when `?expired=1` query param is present.'
      contains: 'expired'
    - path: 'apps/admin/proxy.ts'
      provides: 'Open-redirect-refined `next=` query param: rejects protocol-relative (`//evil.com`) and non-`/`-prefixed values by falling back to `/dashboard`.'
      contains: 'next'
  key_links:
    - from: 'apps/admin/instrumentation.ts'
      to: 'apps/admin/lib/env.ts'
      via: 'import side-effect at boot'
      pattern: 'import.*lib/env'
    - from: 'apps/admin/lib/api-server.ts'
      to: 'apps/admin/lib/env.ts'
      via: "env accessor calls instead of `process.env.* ?? 'localhost'`"
      pattern: 'apiOrigin|adminOrigin'
    - from: 'apps/admin/lib/api-server.ts'
      to: '/login?expired=1'
      via: 'next/navigation `redirect()` on HTTP 401'
      pattern: 'redirect.*expired=1'
---

<objective>
Plan 01 — Foundation hardening — closes the four `apps/CLAUDE.md` rule violations and the one ADM-08 requirement that every other Phase 02 plan depends on:

1. **ADM-08 — env-var boot validation.** Ships `apps/admin/lib/env.ts` (Zod-parsed, throws in non-dev) and wires it into `apps/admin/instrumentation.ts` (Next.js 16 boot hook). Removes the two `?? 'http://localhost:3001'` fallback patterns in `lib/api-server.ts:21,23` that violate `apps/CLAUDE.md`'s env-var rule. Per CONTEXT D-05 and D-06 (D-06 explicitly keeps ADM-08 in Phase 02 against skeptic's defer-to-03 suggestion because every other Phase 02 plan reads env vars).
2. **apiFetch reliability — partial ADM-02.** Adds `AbortSignal.timeout(10_000)` to GET fetches and `AbortSignal.timeout(30_000)` to mutations, a single ~500ms-backoff retry on idempotent GET 5xx (also applied to `getActiveTenantId`, which is an idempotent GET to `/api/auth/get-session`), and 401 → `redirect('/login?expired=1')` handling (CONTEXT D-09, D-10, D-11). The login page reads `?expired=1` and surfaces a calm "session expired" notice.
3. **Cookie `secure:` flag fix.** Adds `secure: process.env.NODE_ENV === 'production'` to the two cookie writes at `lib/actions/set-active-brand.ts:32-36` and `lib/actions/create-brand.ts:68-72` (CONTEXT D-04, persona-cto HIGH).
4. **Open-redirect refinement on `proxy.ts`.** The proxy middleware redirects unauthenticated requests to `/login?next=<original-path>` (line 24-27 in `apps/admin/proxy.ts`). Today the `next` value is the raw `req.nextUrl.pathname + search` — generally safe because Next.js paths start with `/`, but a path of `//evil.com/x` would survive `startsWith('/')` and could later be consumed as a protocol-relative redirect target by the login `actions.ts` (Phase 03 work). Plan 01 closes the producer side here: refine the `next` value to reject `//`-prefixed paths, fallback to `/dashboard`. Closes apps/CLAUDE.md auth-tenancy open-redirect rule for the Phase 02 surface.

Purpose: Plans 02, 03, 04, and 05 all consume `lib/env.ts`, the hardened `apiFetch`, and the secure cookie behavior. Foundation-first prevents downstream plans from inheriting the broken patterns. Per CTO review entry posture: "Phase is 60% bug-fixes against `apps/CLAUDE.md` rules + 40% genuine new code" — Plan 01 ships the 60%.

Output: One new file (`lib/env.ts`), one new file (`instrumentation.ts`), five edits (`api-server.ts`, `api-server-internal.ts` adjustment if it shares the same fallback, two cookie action files, `proxy.ts`), one edit to `login/page.tsx` for the `?expired=1` notice, one `.env.example` update, and four new/extended Vitest specs (including a new `proxy.spec.ts`). No production runtime dependency additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-shell/02-CONTEXT.md
@.planning/phases/02-admin-shell/02-PERSONA-REVIEWS.md
@./CLAUDE.md
@./apps/CLAUDE.md
@./apps/admin/lib/api-server.ts
@./apps/admin/lib/actions/set-active-brand.ts
@./apps/admin/lib/actions/create-brand.ts
@./apps/admin/app/login/page.tsx
@./apps/admin/proxy.ts
@./apps/admin/test/api-server.spec.ts
@./apps/admin/test/set-active-brand-action.spec.ts
@./apps/admin/test/create-brand-action.spec.ts
@./apps/api/src/config/env.schema.ts

<interfaces>
<!-- Surfaces this plan touches. Read once during execution; do not re-explore. -->

From apps/admin/lib/api-server.ts (current, broken — lines 21-23):

- `const apiOrigin = (): string => process.env.NEXT_PUBLIC_API_ORIGIN ?? 'http://localhost:3000';`
- `const adminOrigin = (): string => process.env.ADMIN_WEB_URL ?? 'http://localhost:3001';`
- These two `??` fallbacks ARE the apps/CLAUDE.md env-var rule violation. Replace with imports from `lib/env.ts`.

From apps/admin/lib/api-server.ts (current `apiFetch` signature):

- `export const apiFetch = async <T>(path: string, options: RequestOptions = {}): Promise<ApiResponse<T>>`
- Returns `{ status, ok, data, raw }`. Callers (dashboard layout, server actions) currently treat `!ok` as a generic failure with no 401 differentiation.

From next/navigation:

- `redirect(url: string): never` — throws a Next.js internal error that the framework converts to an HTTP redirect. Calling from inside `apiFetch` interrupts the calling RSC render or server action. Safe inside try/catch ONLY if you re-throw the `RedirectError`.

From apps/api/src/config/env.schema.ts (reference pattern for Zod env validation):

- `export const EnvSchema = z.object({ ... }).superRefine((env, ctx) => { /* prod guardrails */ });`
- `export const loadEnv = (raw: NodeJS.ProcessEnv = process.env): Env => { const parsed = EnvSchema.safeParse(raw); if (!parsed.success) throw new EnvValidationError(...); return parsed.data; };`
- Mirror the shape; do NOT mirror the size — admin needs ~5 vars vs api's ~30 (per CTO review).

From apps/admin/lib/actions/set-active-brand.ts:32-36 (current, missing `secure:`):

```
cookieStore.set('resto.active_brand', slug, {
  httpOnly: true,
  sameSite: 'lax',
  path: '/',
});
```

From apps/admin/lib/actions/create-brand.ts:68-72 (current, missing `secure:`):

```
cookieStore.set('resto.active_brand', slug, {
  httpOnly: true,
  sameSite: 'lax',
  path: '/',
});
```

From apps/admin/proxy.ts (current, lines 24-27):

```
const loginUrl = new URL('/login', req.url);
const dest = `${req.nextUrl.pathname}${req.nextUrl.search}`;
loginUrl.searchParams.set('next', dest);
return NextResponse.redirect(loginUrl);
```

`dest` is the raw path+search. A request to `//evil.com/x` would result in `next=//evil.com/x`, which downstream code consuming `next` as a redirect target could honor as a protocol-relative URL. Plan 01 Task 4 refines `dest` to reject `//`-prefix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADM-08 — Ship apps/admin/lib/env.ts (Zod-validated) + instrumentation.ts boot hook + remove ?? fallbacks</name>
  <files>apps/admin/lib/env.ts, apps/admin/instrumentation.ts, apps/admin/lib/api-server.ts, apps/admin/lib/api-server-internal.ts, apps/admin/.env.example, apps/admin/test/env.spec.ts</files>
  <read_first>
    - apps/admin/lib/api-server.ts (full file, ~177 lines — locate every `process.env.*` access and every fallback)
    - apps/admin/lib/api-server-internal.ts (check whether it also has `?? 'http://localhost'` fallbacks — fix in same task if so; if it does not, leave untouched and remove it from `files_modified` at execution time)
    - apps/api/src/config/env.schema.ts (reference Zod schema + `loadEnv` + `EnvValidationError` pattern — DO NOT copy verbatim, admin has only ~5 vars)
    - apps/admin/next.config.mjs (verify whether `instrumentation.ts` is already declared or needs to be created from scratch)
    - .planning/phases/02-admin-shell/02-CONTEXT.md §D-05, §D-06 (the env-var decisions verbatim)
    - apps/CLAUDE.md (the env-var rule it is enforcing)
  </read_first>
  <behavior>
    - In `NODE_ENV=production` with `NEXT_PUBLIC_API_ORIGIN` missing: importing `apps/admin/lib/env.ts` throws an error whose message names the missing variable.
    - In `NODE_ENV=production` with `ADMIN_WEB_URL` missing: same — throws on import.
    - In `NODE_ENV=production` with `INTERNAL_API_TOKEN` missing: same — throws on import.
    - In `NODE_ENV=production` with `ACTIVE_BRAND_COOKIE_SECRET` missing OR shorter than 32 chars: throws on import (Plan 03 consumes this var; declaring + validating it here means Plan 03 never has to add a second env file).
    - In `NODE_ENV=development` or `NODE_ENV=test`: missing vars do NOT throw; permissive defaults are returned (`'http://localhost:3000'` for `NEXT_PUBLIC_API_ORIGIN`, `'http://localhost:3001'` for `ADMIN_WEB_URL`, `'dev-internal-token'` for `INTERNAL_API_TOKEN`, a 32-char dev-only constant for `ACTIVE_BRAND_COOKIE_SECRET`).
    - Calling `apiOrigin()` or `adminOrigin()` from `lib/api-server.ts` returns the env-validated value, NEVER reads `process.env.*` directly.
    - `instrumentation.ts` calls `register()` which side-effect-imports `./lib/env`, so missing-var crashes happen at server boot before any request handler mounts.
    - `INTERNAL_API_TOKEN` is exported via a separate named export that is NOT re-exported from anywhere that a client component could import (server-only enforcement).
  </behavior>
  <action>
    Create `apps/admin/lib/env.ts` with:

    1. `import 'server-only';` at the top (the `server-only` package is already in dependencies; importing it makes any client-component import fail at build time — this is the key defense against `INTERNAL_API_TOKEN` leak per apps/CLAUDE.md).
    2. A Zod schema `AdminEnvSchema = z.object({ NEXT_PUBLIC_API_ORIGIN: z.string().url(), ADMIN_WEB_URL: z.string().url(), INTERNAL_API_TOKEN: z.string().min(16), ACTIVE_BRAND_COOKIE_SECRET: z.string().min(32) })`. Use `.url()` for the two origins (catches truncated values like `"http"`); use `.min(32)` for the cookie secret (HMAC-SHA256 minimum entropy).
    3. A `loadAdminEnv()` function that, when `process.env.NODE_ENV === 'development' || process.env.NODE_ENV === 'test'`, fills missing values with permissive dev defaults (`'http://localhost:3000'`, `'http://localhost:3001'`, `'dev-internal-token-min-16-chars'`, a deterministic 32-char dev constant like `'dev-only-active-brand-secret-XXXX'`) BEFORE running `safeParse`. In production: no fallbacks — `safeParse` failure throws an `EnvValidationError` whose `.message` lists every failing key + reason (mirror apps/api/src/config/env.schema.ts:`EnvValidationError` shape).
    4. Module-load-time call: `const env = loadAdminEnv(); export const apiOrigin = () => env.NEXT_PUBLIC_API_ORIGIN; export const adminOrigin = () => env.ADMIN_WEB_URL; export const internalApiToken = () => env.INTERNAL_API_TOKEN; export const activeBrandCookieSecret = () => env.ACTIVE_BRAND_COOKIE_SECRET;` (function accessors so the import is a no-op until called, but the underlying validation already ran at module load).

    Create `apps/admin/instrumentation.ts` (Next.js 16 boot hook — see Next 16 docs via Context7 if uncertain about file location):

    ```typescript
    export async function register() {
      if (process.env.NEXT_RUNTIME === 'nodejs') {
        await import('./lib/env');
      }
    }
    ```

    The `NEXT_RUNTIME === 'nodejs'` guard prevents the Edge runtime from trying to import `server-only` modules. The dynamic `await import('./lib/env')` triggers `loadAdminEnv()` at boot, before any request handler runs.

    Edit `apps/admin/lib/api-server.ts:21-23`: REMOVE the two arrow functions and the `?? 'http://localhost:...'` fallbacks. Replace with `import { apiOrigin, adminOrigin } from './env';` at the top of the file. Every call site (currently `apiOrigin()` and `adminOrigin()` invocations on lines ~111, 114, 160, 161) needs no change because the named exports keep the same call shape.

    If `apps/admin/lib/api-server-internal.ts` contains any `process.env.*` accesses or `??` fallbacks (verify during `read_first`), refactor them through `lib/env.ts` in the same task. If it does not, leave it untouched and remove from `files_modified`.

    Update `apps/admin/.env.example` to add `ACTIVE_BRAND_COOKIE_SECRET=` with a comment explaining the 32-char minimum and that it MUST be different from `BETTER_AUTH_SECRET` per CONTEXT D-03 (Plan 03 reads this value).

    Create `apps/admin/test/env.spec.ts` modeled on existing spec patterns (`apps/admin/test/api-server.spec.ts` uses `vi.mock` + `await import` for module-load side effects). Tests:

    - `it('throws in production when NEXT_PUBLIC_API_ORIGIN is missing')` — set `process.env.NODE_ENV = 'production'`, delete the var, dynamic-import `./lib/env`, expect `.rejects.toThrow(/NEXT_PUBLIC_API_ORIGIN/)`.
    - Same for `ADMIN_WEB_URL`, `INTERNAL_API_TOKEN`, `ACTIVE_BRAND_COOKIE_SECRET`.
    - `it('throws in production when ACTIVE_BRAND_COOKIE_SECRET is shorter than 32 chars')` — set to a 10-char string, expect rejection.
    - `it('uses dev defaults in development when vars are missing')` — set `NODE_ENV=development`, delete vars, import succeeds, accessors return the documented dev constants.
    - `it('uses test defaults in test env')` — same with `NODE_ENV=test`.

    Restore `process.env.NODE_ENV` in `afterEach` to avoid bleed.

    Do NOT add `instrumentation.ts` registration to `next.config.mjs` — Next.js 16 auto-detects the file. Verify by reading the next.config.mjs file once.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/env.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "process.env.NEXT_PUBLIC_API_ORIGIN\|process.env.ADMIN_WEB_URL" apps/admin/lib/api-server.ts | grep -v '^#'` returns zero matches (every direct read removed).
    - `grep -cE "'\?\? 'http://localhost" apps/admin/lib/*.ts | grep -v '^#'` returns 0 (no fallback strings remain in `lib/`).
    - `grep -n "import 'server-only'" apps/admin/lib/env.ts` returns exactly one match on or near line 1.
    - `grep -nE "z\.string\(\)\.url\(\)" apps/admin/lib/env.ts` returns at least 2 matches (the two origin URLs).
    - `grep -nE "z\.string\(\)\.min\(32\)" apps/admin/lib/env.ts` returns at least one match (cookie secret).
    - `grep -n "NEXT_RUNTIME" apps/admin/instrumentation.ts` returns exactly one match.
    - `pnpm --filter @resto/admin test apps/admin/test/env.spec.ts` exits 0 with at least 5 passing test cases.
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0 (no new ESLint warnings; the new files comply with `no-console`, `consistent-type-imports`, etc.).
    - Manual smoke: `NODE_ENV=production node -e "require('./apps/admin/lib/env').loadAdminEnv()"` (or equivalent via tsx) throws an error mentioning a missing required variable when `process.env` is unset. NOT run by automated CI but recorded in SUMMARY.
  </acceptance_criteria>
  <done>
    `lib/env.ts` exists, validates via Zod, throws in production on missing/malformed vars, returns dev defaults in development/test. `instrumentation.ts` triggers validation at boot. `api-server.ts` no longer has `?? 'http://localhost:...'` fallbacks. `.env.example` documents the new `ACTIVE_BRAND_COOKIE_SECRET` var. Test suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ADM-02 — apiFetch reliability (timeout + retry + 401 redirect + getActiveTenantId parity)</name>
  <files>apps/admin/lib/api-server.ts, apps/admin/app/login/page.tsx, apps/admin/test/api-server.spec.ts</files>
  <read_first>
    - apps/admin/lib/api-server.ts (lines 94-147 — the `apiFetch` body; locate the single `fetch` call and the response handling block)
    - apps/admin/lib/api-server.ts (lines 157-176 — `getActiveTenantId` helper, which also makes a `fetch` and needs the same timeout + one-retry treatment since `/api/auth/get-session` is an idempotent GET)
    - apps/admin/test/api-server.spec.ts (full file — read its mock pattern; the existing spec uses `vi.mock('next/headers', ...)` and `globalThis.fetch = fetchMock` — extend that pattern)
    - apps/admin/app/login/page.tsx (the `?reset=success` notice block — line 32-37 — mirror the pattern for `?expired=1`)
    - apps/admin/proxy.ts (Note: the proxy already redirects unauthenticated requests when the session cookie is absent — Task 2's 401-redirect is the complementary case where the cookie IS present but the api returns 401 for stale BA session)
    - apps/CLAUDE.md (network rule lines about AbortSignal timeout + one-retry-on-idempotent-GET-5xx — the exact policy this task implements)
  </read_first>
  <behavior>
    - `apiFetch('/v1/x')` (GET) calls `fetch(url, { signal: AbortSignal.timeout(10_000), ... })`. A hung upstream (>10s) rejects with an `AbortError`-shaped error caught and returned as `{ ok: false, status: 0, data: null }` (NOT thrown — keeps current call-site contract).
    - `apiFetch('/v1/x', { method: 'POST' })` uses `AbortSignal.timeout(30_000)`.
    - On HTTP 500/502/503/504 for GET requests: ONE retry, sleep ~500ms (`await new Promise(r => setTimeout(r, 500))`), then re-issue the same request with a fresh timeout. If retry also fails (5xx or timeout), return `{ ok: false, ... }` to caller. NEVER retry on `method !== 'GET'`. NEVER retry more than once.
    - On HTTP 401 (any method): call `redirect('/login?expired=1')` from `next/navigation`. The `redirect()` throws a special Next.js error that the framework intercepts — DO NOT wrap it in try/catch (that would swallow the redirect signal). The function call after `redirect()` is unreachable; TypeScript narrows it via `redirect()`'s `never` return type.
    - 401 redirect does NOT fire for the BA session-lookup probe (`/api/auth/get-session`) because that lookup is the redirect's own dependency — short-circuit on `path === '/api/auth/get-session'` and return `{ ok: false, ... }` instead. (The same exclusion already exists for `x-tenant-id` injection on line 109.)
    - `getActiveTenantId` helper (line ~157-176) gets the same reliability treatment as `apiFetch` GETs: `AbortSignal.timeout(10_000)` on its inner `fetch`, ONE retry on 5xx with ~500ms backoff, and returns `null` on AbortError or final-retry failure (matches the existing helper contract). Per the apps/CLAUDE.md rule: `/api/auth/get-session` is idempotent GET → reuse the retry helper from apiFetch (extract a shared `executeWithRetry` utility if cleaner, otherwise inline).
    - The login page at `/login?expired=1` renders a status notice "Your session expired. Please sign in again." styled identically to the existing `?reset=success` notice. The notice does NOT autodisappear and does NOT block the form.
  </behavior>
  <action>
    Edit `apps/admin/lib/api-server.ts`:

    1. Add `import { redirect } from 'next/navigation';` at the top.
    2. Define a constant `const TIMEOUT_GET_MS = 10_000; const TIMEOUT_MUTATION_MS = 30_000;` near the top of the file.
    3. Extract a shared helper `executeWithRetry(input: RequestInfo, init: RequestInit, opts: { isGet: boolean; timeoutMs: number }): Promise<Response>` that wraps `fetch` with `AbortSignal.timeout(opts.timeoutMs)` and, when `opts.isGet` is true, performs one retry on `status >= 500 && status <= 504` with 500ms backoff. The retry path tracks attempts via a local counter so a flaky upstream can never cause more than 2 total attempts.
    4. Replace the single `await fetch(url, { ... })` call at line 121 in `apiFetch` with `await executeWithRetry(url, { ...init }, { isGet: options.method === undefined || options.method === 'GET', timeoutMs: isMutation ? TIMEOUT_MUTATION_MS : TIMEOUT_GET_MS })`.
    5. After the fetch returns (success or final-retry failure), insert the 401 redirect BEFORE the JSON parsing block: `if (res.status === 401 && path !== '/api/auth/get-session') { redirect('/login?expired=1'); }` — and a WHY comment line `// CONTEXT D-10: stale session → bounce to login with a notice instead of rendering an empty dashboard`.
    6. Wrap the network call in `try { ... } catch (err) { if (err instanceof Error && err.name === 'AbortError') return { ok: false, status: 0, data: null, raw: new Response(null, { status: 0 }) }; throw err; }` — preserve the existing error contract.
    7. In `getActiveTenantId` (line ~157-176): use the same `executeWithRetry` helper with `{ isGet: true, timeoutMs: TIMEOUT_GET_MS }`. Wrap in try/catch and return `null` on AbortError or final-retry failure (matches the existing catch behavior on that helper). Result: `/api/auth/get-session` survives a single rolling-deploy 5xx + bounds at 10s + ~500ms wall worst-case.

    Edit `apps/admin/app/login/page.tsx`:

    1. Extend `PageProps.searchParams` type to include `readonly expired?: string`.
    2. Add `const sessionExpired = params.expired === '1';` near line 19.
    3. Add a notice block above the `LoginForm` mirroring the existing `resetSuccess` block:
       ```
       {sessionExpired ? (
         <p role="status" className="bg-muted text-foreground mb-4 rounded-md px-3 py-2 text-sm">
           Your session expired. Please sign in again.
         </p>
       ) : null}
       ```
       Per CONTEXT D-08 voice rules: calm, no exclamation marks, 1-2 sentences max.

    Extend `apps/admin/test/api-server.spec.ts` with three new describe blocks:

    - `describe('apiFetch — AbortSignal timeout')` — mock `globalThis.fetch` to never resolve; assert the call rejects/returns within ~10.5s wall-time AND that `fetchMock` was called with an `init.signal` whose `aborted` becomes true (verify by spying on `signal` argument). To avoid 10s test wall: use `vi.useFakeTimers()` + `vi.advanceTimersByTime(10_001)` to simulate timeout instantly.
    - `describe('apiFetch — one-retry on idempotent GET 5xx')` — mock fetch to return 503 then 200 on second call; assert `fetchMock` was called exactly 2 times, the final `apiFetch` returns `{ ok: true, status: 200 }`. Also assert a POST request returning 503 is NOT retried: `fetchMock` called exactly 1 time, returns `{ ok: false, status: 503 }`. Add a third sub-test: `getActiveTenantId` also retries once on 5xx from `/api/auth/get-session` (assert `fetchMock` called 2 times, helper returns the tenant id from the 2nd response).
    - `describe('apiFetch — 401 redirects to /login?expired=1')` — mock `next/navigation` `redirect` as a vitest spy, mock fetch to return 401; assert `redirect` was called once with `'/login?expired=1'`. Skip when `path === '/api/auth/get-session'`: assert `redirect` NOT called.

    Use `vi.useFakeTimers()` / `vi.useRealTimers()` setup blocks to control time for the timeout + 500ms-backoff tests.

    Plan 01 does NOT touch `apps/admin/e2e/adm-00-smoke-walk.spec.ts` — Plan 02 (Wave 2, depends on Plan 01) writes that spec with scenarios 1, 3, 5, 6 already active (since Plan 02 runs AFTER Plan 01 lands, the 401-redirect behavior is in place by then).

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/api-server.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "AbortSignal\.timeout\(" apps/admin/lib/api-server.ts | grep -v '^#'` returns at least 2 matches (apiFetch + getActiveTenantId; could be 1 if both flow through `executeWithRetry` — in that case, also assert `grep -nE "executeWithRetry" apps/admin/lib/api-server.ts | grep -v '^#'` returns at least 3 references — definition + both call sites).
    - `grep -nE "redirect\(['\"]/login\?expired=1['\"]\)" apps/admin/lib/api-server.ts | grep -v '^#'` returns exactly one match.
    - `grep -n "// CONTEXT D-10" apps/admin/lib/api-server.ts` returns one WHY comment near the 401 check.
    - `grep -nE "status >= 500.*504|>=\s*500\s*&&" apps/admin/lib/api-server.ts | grep -v '^#'` returns at least one match (the retry guard).
    - `grep -n "params.expired" apps/admin/app/login/page.tsx` returns at least one match.
    - `pnpm --filter @resto/admin test apps/admin/test/api-server.spec.ts` exits 0; new describe blocks all pass (including the `getActiveTenantId` retry sub-test); existing X-Brand-Slug describe blocks still pass (no regression).
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0.
  </acceptance_criteria>
  <done>
    `apiFetch` enforces 10s read / 30s mutation timeout, retries idempotent GET 5xx exactly once with 500ms backoff, and bounces to `/login?expired=1` on 401. `getActiveTenantId` inherits the same reliability profile (timeout + one retry) because `/api/auth/get-session` is idempotent. Login page renders the session-expired notice. All new and existing tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ADM-04 hardening — Cookie secure-flag fix for set-active-brand + create-brand</name>
  <files>apps/admin/lib/actions/set-active-brand.ts, apps/admin/lib/actions/create-brand.ts, apps/admin/test/set-active-brand-action.spec.ts, apps/admin/test/create-brand-action.spec.ts</files>
  <read_first>
    - apps/admin/lib/actions/set-active-brand.ts (full file — lines 32-36 are the violation site)
    - apps/admin/lib/actions/create-brand.ts (full file — lines 68-72 are the violation site)
    - apps/admin/test/set-active-brand-action.spec.ts (existing spec — read mock pattern; this task adds an assertion to existing tests)
    - apps/admin/test/create-brand-action.spec.ts (existing spec — same pattern)
    - apps/CLAUDE.md (cookie rule: `secure: process.env.NODE_ENV === 'production'`)
    - .planning/phases/02-admin-shell/02-CONTEXT.md §D-04 (the verbatim user decision)
  </read_first>
  <behavior>
    - Calling `setActiveBrandAction('my-brand')` in production sets a cookie whose `secure` attribute is `true`; calling in development sets `secure: false`.
    - Same for `createBrandAction` on its success path (line 68-72 today writes without `secure:`).
    - Cookie attributes preserved: `httpOnly: true`, `sameSite: 'lax'`, `path: '/'`. (Phase 03 will sweep all server-action cookies; Phase 02 fixes only these two per CONTEXT D-04.)
  </behavior>
  <action>
    Edit `apps/admin/lib/actions/set-active-brand.ts:32-36`. Replace:

    ```
    cookieStore.set('resto.active_brand', slug, {
      httpOnly: true,
      sameSite: 'lax',
      path: '/',
    });
    ```

    with:

    ```
    cookieStore.set('resto.active_brand', slug, {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'lax',
      path: '/',
    });
    ```

    No WHY-comment per CLAUDE.md "no comments" rule — the literal `process.env.NODE_ENV === 'production'` is self-documenting given the apps/CLAUDE.md rule.

    Edit `apps/admin/lib/actions/create-brand.ts:68-72` with the same change.

    Extend `apps/admin/test/set-active-brand-action.spec.ts` with one new test case:

    - `it('sets secure: true when NODE_ENV is production')` — set `process.env.NODE_ENV = 'production'`, call the action, assert the captured `cookieStore.set` mock was called with `{ ...args, secure: true }`. Restore NODE_ENV in `afterEach`.
    - `it('sets secure: false when NODE_ENV is development')` — same shape with `NODE_ENV = 'development'`, assert `secure: false`.

    Apply the same two test cases to `apps/admin/test/create-brand-action.spec.ts` for the brand-creation success path (the existing spec already mocks api success at line 37; extend that test or add adjacent).

    No other changes. Do NOT extend the secure-cookie fix to `sign-in.ts`, `sign-out.ts`, or other action files — that is explicitly Phase 03 work per CONTEXT D-04 + Deferred Items.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/set-active-brand-action.spec.ts apps/admin/test/create-brand-action.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "secure: process.env.NODE_ENV === 'production'" apps/admin/lib/actions/set-active-brand.ts` returns exactly one match.
    - `grep -n "secure: process.env.NODE_ENV === 'production'" apps/admin/lib/actions/create-brand.ts` returns exactly one match.
    - `pnpm --filter @resto/admin test apps/admin/test/set-active-brand-action.spec.ts apps/admin/test/create-brand-action.spec.ts` exits 0; the new "secure:" assertions all pass; existing tests in both specs still pass.
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0.
  </acceptance_criteria>
  <done>
    Both server-action cookie writes include `secure: process.env.NODE_ENV === 'production'`. Tests assert the value in both dev and prod modes. Existing test suites still green. The two apps/CLAUDE.md cookie-rule violations called out by persona-cto are closed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Open-redirect refinement on proxy.ts `next=` query param</name>
  <files>apps/admin/proxy.ts, apps/admin/test/proxy.spec.ts</files>
  <read_first>
    - apps/admin/proxy.ts (full file — 33 lines; lines 24-27 are the `next=` producer to refine)
    - apps/CLAUDE.md (lines 56-61: "Server actions that consume `next=` / `redirect=` query params must refine against protocol-relative URLs. `z.string().startsWith('/')` accepts `//evil.com`; add `.refine(s => !/^\/[\\/]/.test(s))`.")
    - .planning/phases/02-admin-shell/02-CONTEXT.md §D-02 (the three Phase 03 placeholder routes don't have to consume `next=`, but proxy.ts produces it and the producer side is what we close here)
  </read_first>
  <behavior>
    - A request to `/dashboard` without a session cookie redirects to `/login?next=/dashboard` (unchanged).
    - A request to `//evil.com/dashboard` without a session cookie redirects to `/login?next=/dashboard` — the `//`-prefixed path is rejected by the refinement, fallback is `/dashboard`.
    - A request to `/onboarding/brand?welcome=1` redirects to `/login?next=/onboarding/brand?welcome=1` (search params preserved).
    - A request to `/dashboard` from a path that resolves to something NOT starting with `/` (defensive — `req.nextUrl.pathname` always starts with `/` in Next.js, so this is theoretical) falls back to `/dashboard`.
  </behavior>
  <action>
    Edit `apps/admin/proxy.ts`. Replace the body of the `proxy` function (lines 20-28). After:

    ```typescript
    const loginUrl = new URL('/login', req.url);
    const dest = `${req.nextUrl.pathname}${req.nextUrl.search}`;
    loginUrl.searchParams.set('next', dest);
    return NextResponse.redirect(loginUrl);
    ```

    Replace with:

    ```typescript
    const loginUrl = new URL('/login', req.url);
    const rawDest = `${req.nextUrl.pathname}${req.nextUrl.search}`;
    // apps/CLAUDE.md open-redirect rule: reject protocol-relative paths (`//evil.com/...`)
    // and any value that does not start with a single `/`. Fall back to /dashboard.
    const safeDest = rawDest.startsWith('/') && !rawDest.startsWith('//') ? rawDest : '/dashboard';
    loginUrl.searchParams.set('next', safeDest);
    return NextResponse.redirect(loginUrl);
    ```

    Create `apps/admin/test/proxy.spec.ts` modeled on existing Vitest patterns in `apps/admin/test/`. Use mocked `NextRequest` / `NextResponse` constructors or a thin shim — Playwright is overkill for this unit. If `NextRequest` mocking is awkward, build a tiny `makeReq(url)` helper that returns an object shaped enough for the function:

    ```typescript
    const makeReq = (path: string, hasSession = false) => ({
      url: `http://localhost:3001${path}`,
      cookies: { has: () => hasSession },
      nextUrl: new URL(`http://localhost:3001${path}`),
    });
    ```

    Tests:
    - `it('passes through when session cookie is present')` — assert `proxy(req)` returns `NextResponse.next()`.
    - `it('redirects to /login?next=/dashboard for unauthenticated /dashboard request')` — assert the Location header includes `next=%2Fdashboard` (URL-encoded).
    - `it('rejects protocol-relative paths via fallback to /dashboard')` — pass path `//evil.com/x`; assert `next` query param is `/dashboard`, NOT `//evil.com/x`.
    - `it('preserves query string in safe paths')` — pass `/onboarding/brand?welcome=1`; assert `next` is `/onboarding/brand?welcome=1` (URL-encoded).
    - `it('rejects empty path via fallback')` — pass empty string (theoretical); assert fallback `/dashboard`.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/proxy.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "startsWith\('/'\)" apps/admin/proxy.ts | grep -v '^#'` returns at least one match.
    - `grep -nE "startsWith\('//')" apps/admin/proxy.ts | grep -v '^#'` returns at least one match (the protocol-relative rejection).
    - `grep -n "safeDest" apps/admin/proxy.ts` returns at least one match (the refined value bound to a local).
    - `pnpm --filter @resto/admin test apps/admin/test/proxy.spec.ts` exits 0; all 5 test cases pass.
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0.
  </acceptance_criteria>
  <done>
    `proxy.ts` no longer forwards protocol-relative paths as `next=` values. Open-redirect surface on the proxy producer side is closed. Phase 03 picks up the consumer-side hardening (login `actions.ts` reading `next` and routing to it) — Plan 01 closes only the producer.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                                             |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| browser → admin RSC                 | Operator's signed BA session cookie crosses this boundary on every request. Stale or forged values are routed to `/login?expired=1` via the new 401 handler.                            |
| RSC → apps/api                      | Server-side `fetch` calls from `apiFetch` cross to the backend. A hung upstream without `AbortSignal.timeout` was the prior DoS-via-hung-request vector.                                |
| process boot → env vars             | `instrumentation.ts` enforces that production deploys with missing env vars crash at boot rather than silently routing to localhost.                                                    |
| admin server bundle → client bundle | `lib/env.ts` is gated by `import 'server-only'`; importing it from a client component fails at build time so `INTERNAL_API_TOKEN` cannot leak into the browser.                         |
| unauth user → proxy.ts              | Proxy produces `next=` param on redirect to `/login`. Plan 01 refines the produced value against protocol-relative paths so the open-redirect primitive is closed on the producer side. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                               |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Information Disclosure | `INTERNAL_API_TOKEN` leak to browser              | mitigate    | `lib/env.ts` carries `import 'server-only'`; any client-component import path triggers a build error. Existing `api-server.ts` already imports `server-only` (line 1); this plan extends the pattern to env.                                                  |
| T-02-02   | Spoofing               | Cookie tampering over plain HTTP                  | mitigate    | `secure: process.env.NODE_ENV === 'production'` on the two `resto.active_brand` write sites. Cookie is also `httpOnly` so JS cannot read it; only the server sets it.                                                                                         |
| T-02-03   | Denial of Service      | Hung upstream → exhausted RSC handles             | mitigate    | `AbortSignal.timeout(10_000)` on every server-side `fetch`. The dashboard layout fan-out (`Promise.all` of two `apiFetch` calls) now has a bounded worst-case render time of ~10.5s, down from unbounded.                                                     |
| T-02-04   | Spoofing               | Stale BA session silently renders empty dashboard | mitigate    | `apiFetch` on HTTP 401 calls `redirect('/login?expired=1')`. The login page surfaces a calm notice; no operator sees a blank empty-state when they're actually logged out. Closes persona-skeptic BLOCK on 401 handling.                                      |
| T-02-05   | Tampering              | Production deploy misconfigured                   | mitigate    | `instrumentation.ts` runs `loadAdminEnv()` at process boot. Missing `NEXT_PUBLIC_API_ORIGIN` / `ADMIN_WEB_URL` / `INTERNAL_API_TOKEN` crashes the container immediately rather than routing real users to `localhost:3000` (apps/CLAUDE.md no-fallback rule). |
| T-02-06   | Repudiation            | Open redirect via `?next=` query param            | mitigate    | `proxy.ts` refines the produced `next` value against `//`-prefixed protocol-relative paths, falling back to `/dashboard`. Phase 03 picks up the consumer-side hardening when login `actions.ts` reads and routes to `next`.                                   |

</threat_model>

<verification>
After all four tasks pass, run the full plan-scope checks:

1. `pnpm --filter @resto/admin test` — all admin specs green; existing pass-count not reduced.
2. `pnpm --filter @resto/admin typecheck` + `pnpm --filter @resto/admin lint` — both clean.
3. `pnpm --filter @resto/admin build` — production build succeeds with `NEXT_PUBLIC_API_ORIGIN=https://api.example.com ADMIN_WEB_URL=https://admin.example.com INTERNAL_API_TOKEN=test-token-min-16 ACTIVE_BRAND_COOKIE_SECRET=dev-only-active-brand-secret-XXXX`.
4. `pnpm --filter @resto/admin build` with one env var removed — build OR boot fails with a Zod error naming the missing var. (Document the failing variable in the SUMMARY.)
5. Manual scan: `grep -rn "process.env.NEXT_PUBLIC_API_ORIGIN\|process.env.ADMIN_WEB_URL\|process.env.INTERNAL_API_TOKEN" apps/admin --include='*.ts' --include='*.tsx' | grep -v 'lib/env.ts' | grep -v '^#'` — zero matches outside `lib/env.ts`.
   </verification>

<success_criteria>

- `apps/admin/lib/env.ts` exists, validates four env vars via Zod, throws in production on missing/malformed, returns dev defaults in development/test.
- `apps/admin/instrumentation.ts` exists and triggers env validation at server boot.
- Zero `?? 'http://localhost:...'` fallback strings remain in any `apps/admin/lib/*.ts` file.
- `apiFetch` enforces 10s read / 30s mutation timeouts and retries idempotent GET 5xx exactly once with 500ms backoff. `getActiveTenantId` inherits the same timeout + retry profile because `/api/auth/get-session` is idempotent.
- `apiFetch` on HTTP 401 calls `redirect('/login?expired=1')`; login page surfaces the calm session-expired notice.
- Both `set-active-brand.ts` and `create-brand.ts` write the `resto.active_brand` cookie with `secure: process.env.NODE_ENV === 'production'`.
- `proxy.ts` refines the produced `next=` value against protocol-relative paths.
- ADM-08 closed; ADM-02 401-redirect behavior closed; apps/CLAUDE.md env-var + cookie + timeout + open-redirect violations identified by persona-cto all resolved.

</success_criteria>

<output>
Create `.planning/phases/02-admin-shell/02-01-SUMMARY.md` when done. List:

- New files created: `apps/admin/lib/env.ts`, `apps/admin/instrumentation.ts`, `apps/admin/test/env.spec.ts`, `apps/admin/test/proxy.spec.ts`.
- Files modified: `apps/admin/lib/api-server.ts`, `apps/admin/lib/actions/set-active-brand.ts`, `apps/admin/lib/actions/create-brand.ts`, `apps/admin/app/login/page.tsx`, `apps/admin/proxy.ts`, `apps/admin/.env.example`, three existing test files extended.
- Test pass-count before/after.
- The `grep` outputs proving the acceptance criteria (env fallback removal, timeout presence, secure-flag presence, open-redirect refinement).
- Any decisions deferred to Phase 03 (e.g., consumer-side hardening of `next=` in login `actions.ts` — Plan 01 closes producer only).
  </output>
  </content>
  </invoke>
