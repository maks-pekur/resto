---
phase: 02-admin-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/admin/lib/active-brand-cookie.ts
  - apps/admin/lib/actions/set-active-brand.ts
  - apps/admin/lib/actions/create-brand.ts
  - apps/admin/lib/api-server.ts
  - apps/admin/app/dashboard/layout.tsx
  - apps/admin/test/active-brand-cookie.spec.ts
  - apps/admin/test/set-active-brand-action.spec.ts
  - apps/admin/test/create-brand-action.spec.ts
  - apps/admin/test/api-server.spec.ts
autonomous: true
requirements:
  - ADM-05

must_haves:
  truths:
    - 'The `resto.active_brand` cookie value carries an HMAC-SHA256 signature derived from `ACTIVE_BRAND_COOKIE_SECRET` so a tampered slug fails verification on read (CONTEXT D-03).'
    - '`ACTIVE_BRAND_COOKIE_SECRET` is a dedicated env var separate from `BETTER_AUTH_SECRET`; rotating one does NOT invalidate the other (CONTEXT D-03).'
    - 'Cookie HMAC is defense-in-depth ONLY: the api remains the authoritative source for `(operator, tenant, brand)` membership; the `x-brand-slug` header is always re-validated server-side (CONTEXT D-03 — "Cookie is a UX cache, the api is the source of truth"; persona-skeptic + persona-cto agree).'
    - 'Read sites (`apps/admin/app/dashboard/layout.tsx`, `apps/admin/lib/api-server.ts` line ~103) call a `readActiveBrand()` helper that verifies the HMAC; an unverifiable cookie value is treated as if the cookie were absent (no error surfaced, no panic — operator silently falls back to "all brands" or no brand context).'
    - 'Constant-time comparison is used in HMAC verification (`crypto.timingSafeEqual`) so cookie verification is not a timing oracle.'
  artifacts:
    - path: 'apps/admin/lib/active-brand-cookie.ts'
      provides: '`signActiveBrand(slug)` and `readActiveBrand()` helpers. Server-only (`import "server-only"`). Uses `node:crypto` `createHmac` with SHA-256.'
      contains: 'createHmac'
    - path: 'apps/admin/lib/actions/set-active-brand.ts'
      provides: 'Cookie write goes through `signActiveBrand(slug)` instead of writing the raw slug.'
      contains: 'signActiveBrand'
    - path: 'apps/admin/lib/actions/create-brand.ts'
      provides: 'Cookie write goes through `signActiveBrand(slug)` instead of writing the raw slug.'
      contains: 'signActiveBrand'
    - path: 'apps/admin/app/dashboard/layout.tsx'
      provides: 'Reads `activeBrandSlug` via `readActiveBrand()` not via direct `cookieStore.get(...)?.value`.'
      contains: 'readActiveBrand'
    - path: 'apps/admin/lib/api-server.ts'
      provides: 'The `x-brand-slug` header forwarding (line ~103) sources from `readActiveBrand()` so a tampered cookie never reaches the api.'
      contains: 'readActiveBrand'
  key_links:
    - from: 'apps/admin/lib/actions/set-active-brand.ts'
      to: 'apps/admin/lib/active-brand-cookie.ts (signActiveBrand)'
      via: 'function call producing the cookie value'
      pattern: 'signActiveBrand'
    - from: 'apps/admin/app/dashboard/layout.tsx'
      to: 'apps/admin/lib/active-brand-cookie.ts (readActiveBrand)'
      via: 'function call returning verified slug or null'
      pattern: 'readActiveBrand'
    - from: 'apps/admin/lib/active-brand-cookie.ts'
      to: 'apps/admin/lib/env.ts (activeBrandCookieSecret)'
      via: 'env accessor'
      pattern: 'activeBrandCookieSecret'
---

<objective>
Plan 03 — Signed `resto.active_brand` cookie — closes ADM-05's "signed cookie" requirement per CONTEXT D-03. Ships a thin HMAC-SHA256 sign/verify helper and threads it through all four cookie write and read sites in `apps/admin`.

Per CONTEXT D-03 and persona-cto + persona-skeptic alignment: the cookie HMAC is **defense-in-depth, not the security boundary**. The api remains authoritative — `apps/admin/lib/api-server.ts` already forwards `x-brand-slug` to the api, and the api's `PermissionsGuard` re-validates the operator's membership of that brand on every request. The HMAC mitigates two specific risks: (1) cookie value tampering by a hostile browser extension or proxy, (2) cross-site cookie injection without the secret. It does NOT replace the api-side check, and Plan 03 does NOT modify the api.

Per CONTEXT D-03 cookie attributes: `secure: process.env.NODE_ENV === 'production'` (already shipped in Plan 01 Task 3), `httpOnly: true`, `sameSite: 'lax'`, no explicit `Domain`. Plan 03 preserves all four.

Depends on Plan 01 because the HMAC secret comes from `apps/admin/lib/env.ts:activeBrandCookieSecret()` which Plan 01 ships. Wave 2.

Output: One new file (`lib/active-brand-cookie.ts`), modifications to the two cookie write sites (already touched by Plan 01 for the `secure:` flag — Plan 03 changes the VALUE, not the attributes), modifications to the two cookie read sites (`dashboard/layout.tsx:25` and `lib/api-server.ts:103`), one new test spec, and extensions to three existing test specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-shell/02-CONTEXT.md
@.planning/phases/02-admin-shell/02-PERSONA-REVIEWS.md
@.planning/phases/02-admin-shell/02-01-PLAN.md
@./CLAUDE.md
@./apps/CLAUDE.md
@./apps/admin/lib/api-server.ts
@./apps/admin/app/dashboard/layout.tsx
@./apps/admin/lib/actions/set-active-brand.ts
@./apps/admin/lib/actions/create-brand.ts
@./packages/domain/src/brand-slug.ts

<interfaces>
<!-- After Plan 01 lands, the following surfaces exist. Plan 03 builds on them. -->

From apps/admin/lib/env.ts (shipped in Plan 01):

- `export const activeBrandCookieSecret = (): string => env.ACTIVE_BRAND_COOKIE_SECRET;`
- The secret is Zod-validated `z.string().min(32)`.

From apps/admin/lib/actions/set-active-brand.ts (Plan 01 added `secure:`):

- Cookie write at lines 32-37; value is currently the raw `slug` string.

From apps/admin/lib/actions/create-brand.ts (Plan 01 added `secure:`):

- Cookie write at lines 68-73; value is currently the raw `slug` string.

From apps/admin/app/dashboard/layout.tsx (line 25):

- `const activeBrandSlug = cookieStore.get('resto.active_brand')?.value ?? null;`
- This is the layout-level read. Plan 03 replaces with `readActiveBrand()`.

From apps/admin/lib/api-server.ts (line 103):

- `const activeBrand = cookieStore.get('resto.active_brand')?.value;`
- This is the per-request read used to set the `x-brand-slug` header. Plan 03 replaces with `readActiveBrand()`.

From packages/domain/src/brand-slug.ts (verified during planning — line 3):

- `const BRAND_SLUG_RE = /^[a-z0-9][a-z0-9-]{1,62}[a-z0-9]$/u;` — lowercase alphanumeric + hyphens only; 3–64 chars; must NOT start or end with `-`; underscores `_` are NOT allowed.
- `BrandSlugValue` Zod schema: `z.string().min(3).max(64).toLowerCase().refine(v => BRAND_SLUG_RE.test(v), ...)`.
- `BrandSlug = BrandSlugValue.brand<'BrandSlug'>()` — branded variant for domain-internal use.
- Test fixtures used in Plan 03 specs MUST respect this shape: `'demo-brand'`, `'three-brand'`, `'a-b'` (3-char min) are valid; `'invalid_slug_with_underscore'` is rejected (underscore not in regex); `'-leading-hyphen'`, `'trailing-hyphen-'`, and `'ab'` (under 3 chars) are also rejected.

From node:crypto:

- `createHmac(algorithm: string, key: BinaryLike): Hmac` — call `.update(data)` then `.digest('base64url')`.
- `timingSafeEqual(a: Uint8Array, b: Uint8Array): boolean` — constant-time comparison. REQUIRES the two buffers have the same byte-length; the implementation throws otherwise — use a length-check guard first.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship apps/admin/lib/active-brand-cookie.ts (HMAC sign + verify)</name>
  <files>apps/admin/lib/active-brand-cookie.ts, apps/admin/test/active-brand-cookie.spec.ts</files>
  <read_first>
    - apps/admin/lib/env.ts (Plan 01 output — confirm `activeBrandCookieSecret()` signature)
    - apps/admin/lib/api-server.ts (note the `import 'server-only';` at line 1 — match that pattern)
    - .planning/phases/02-admin-shell/02-CONTEXT.md §D-03 (the verbatim user decision on cookie strategy)
    - packages/domain/src/brand-slug.ts (the `BrandSlugValue` / `BrandSlug` Zod regex — underscore is REJECTED; lowercase alphanumeric + hyphens; 3-64 chars; no leading/trailing hyphen — see `<interfaces>` block above for the exact regex)
    - packages/domain/src/index.ts (confirm `BrandSlug` is exported from the barrel)
    - Context7: `node:crypto` `createHmac` + `timingSafeEqual` API surface (verify call signatures and Buffer vs Uint8Array nuances).
  </read_first>
  <behavior>
    - `signActiveBrand('my-brand')` returns a string of the shape `${slug}.${base64urlSignature}` where the signature is HMAC-SHA256(slug, secret) base64url-encoded.
    - `readActiveBrand()` reads the cookie via `next/headers` `cookies()` and returns:
      - The verified `slug` string on a valid HMAC.
      - `null` if the cookie is absent.
      - `null` if the cookie value is malformed (no `.` separator, wrong segment count).
      - `null` if the HMAC does not verify.
    - The function does NOT throw on invalid input — calling code treats `null` as "no active brand selected."
    - Verification uses `timingSafeEqual` with a length-check guard so a forged signature of a different length does not throw.
    - The `BrandSlug` Zod schema (from `@resto/domain`) is applied to the parsed slug before returning so a valid-HMAC-but-bad-slug returns `null`. Bad-slug examples per the verified regex: any underscore-containing slug (`'invalid_slug_with_underscore'`), any leading-hyphen slug (`'-leading-hyphen'`), any sub-3-char slug (`'ab'`).
  </behavior>
  <action>
    Create `apps/admin/lib/active-brand-cookie.ts`:

    ```typescript
    import 'server-only';
    import { cookies } from 'next/headers';
    import { createHmac, timingSafeEqual } from 'node:crypto';
    import { BrandSlug } from '@resto/domain';
    import { activeBrandCookieSecret } from './env';

    const COOKIE_NAME = 'resto.active_brand';
    const SEPARATOR = '.';

    const sign = (slug: string): string => {
      return createHmac('sha256', activeBrandCookieSecret()).update(slug).digest('base64url');
    };

    export const signActiveBrand = (slug: string): string => {
      return `${slug}${SEPARATOR}${sign(slug)}`;
    };

    export const readActiveBrand = async (): Promise<string | null> => {
      const cookieStore = await cookies();
      const raw = cookieStore.get(COOKIE_NAME)?.value;
      if (!raw) return null;
      const idx = raw.lastIndexOf(SEPARATOR);
      if (idx <= 0 || idx === raw.length - 1) return null;
      const slugCandidate = raw.slice(0, idx);
      const signature = raw.slice(idx + 1);
      const expected = sign(slugCandidate);
      const a = Buffer.from(signature, 'base64url');
      const b = Buffer.from(expected, 'base64url');
      if (a.length !== b.length) return null;
      if (!timingSafeEqual(a, b)) return null;
      const parsed = BrandSlug.safeParse(slugCandidate);
      if (!parsed.success) return null;
      return parsed.data;
    };
    ```

    Create `apps/admin/test/active-brand-cookie.spec.ts` with test cases. Use `vi.mock('next/headers', ...)` matching the pattern in existing specs (`apps/admin/test/api-server.spec.ts:4-13`):

    - `it('signActiveBrand returns slug.signature shape')` — call `signActiveBrand('demo-brand')`, assert result contains the slug, a single dot at the expected position, and a non-empty base64url segment.
    - `it('readActiveBrand returns the slug for a freshly-signed cookie')` — call `signActiveBrand('demo-brand')`, install the result as the cookie value via the mocked `cookies()`, call `readActiveBrand()`, assert `'demo-brand'`.
    - `it('readActiveBrand returns null for a tampered slug')` — sign `'demo-brand'`, then mutate to `'other-brand.{sig-of-demo}'`, call `readActiveBrand()`, assert `null`.
    - `it('readActiveBrand returns null for a tampered signature')` — sign `'demo-brand'`, then mutate the signature segment by appending a character, call `readActiveBrand()`, assert `null`.
    - `it('readActiveBrand returns null for malformed cookie (no separator)')` — install raw value `'just-a-slug'`, assert `null`.
    - `it('readActiveBrand returns null for empty cookie')` — install raw value `''`, assert `null`.
    - `it('readActiveBrand returns null when cookie is absent')` — `cookies().get` returns `undefined`, assert `null`.
    - `it('readActiveBrand returns null for a valid HMAC over an invalid slug')` — sign `'invalid_slug_with_underscore'` (underscore is rejected by `BrandSlugValue` regex per `packages/domain/src/brand-slug.ts:3`), call `readActiveBrand()` with the resulting signed value installed, assert `null`. Additionally cover a leading-hyphen slug like `'-bad-start'` and a too-short slug like `'ab'` — both should also return `null`.
    - `it('signActiveBrand is deterministic for the same slug + secret')` — call twice, assert results equal.
    - `it('signature differs between two different slugs')` — sign `'a-b'` and `'c-d'` (both ≥3 chars to satisfy `BrandSlugValue`), assert signatures differ.

    Mock `lib/env.ts` so the test uses a known 32-char secret like `'test-secret-AAAAAAAAAAAAAAAAAAAA'` for deterministic assertions.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/active-brand-cookie.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/lib/active-brand-cookie.ts` exists; starts with `import 'server-only';`.
    - `grep -n "createHmac" apps/admin/lib/active-brand-cookie.ts` returns at least one match.
    - `grep -n "timingSafeEqual" apps/admin/lib/active-brand-cookie.ts` returns at least one match (with a length-check guard above it).
    - `grep -n "BrandSlug.safeParse" apps/admin/lib/active-brand-cookie.ts` returns at least one match (slug-shape re-validation post-HMAC).
    - `pnpm --filter @resto/admin test apps/admin/test/active-brand-cookie.spec.ts` exits 0; all ~10 test cases pass, including the underscore-slug rejection assertion.
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0.
  </acceptance_criteria>
  <done>
    `signActiveBrand` and `readActiveBrand` helpers exist, are server-only, use constant-time HMAC comparison, re-validate slug shape after verification (rejecting underscores, leading/trailing hyphens, sub-3-char and over-64-char slugs per the verified `BrandSlug` regex), and return `null` on any tampering. Test coverage spans valid-roundtrip, tampered-slug, tampered-signature, malformed, empty, absent, and invalid-slug cases.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread signActiveBrand/readActiveBrand through write + read sites</name>
  <files>apps/admin/lib/actions/set-active-brand.ts, apps/admin/lib/actions/create-brand.ts, apps/admin/app/dashboard/layout.tsx, apps/admin/lib/api-server.ts, apps/admin/test/set-active-brand-action.spec.ts, apps/admin/test/create-brand-action.spec.ts, apps/admin/test/api-server.spec.ts</files>
  <read_first>
    - apps/admin/lib/actions/set-active-brand.ts (Plan 01 already added `secure:`; current cookie write at lines 32-37 still uses raw slug)
    - apps/admin/lib/actions/create-brand.ts (Plan 01 already added `secure:`; current cookie write at lines 68-73 still uses raw slug)
    - apps/admin/app/dashboard/layout.tsx (line 25 — the layout read site)
    - apps/admin/lib/api-server.ts (Plan 01 modified; line ~103 read site for `activeBrand` is still `cookieStore.get(...)?.value`)
    - apps/admin/test/set-active-brand-action.spec.ts (Plan 01 extended; check existing mock pattern for cookieStore.set assertions)
    - apps/admin/test/create-brand-action.spec.ts (same)
    - apps/admin/test/api-server.spec.ts (Plan 01 extended; the X-Brand-Slug test block needs an update because the header value now comes from the signed cookie)
  </read_first>
  <behavior>
    - After this task: `setActiveBrandAction('demo')` writes a cookie whose value is `'demo.<base64url-sig>'`, not `'demo'`. (Browser observers see the signed form; the rest of the codebase treats it as opaque.)
    - `createBrandAction(...)` on its success path writes the same signed form.
    - `apps/admin/app/dashboard/layout.tsx` line 25 calls `await readActiveBrand()` and assigns the result to `activeBrandSlug` (null-aware as before).
    - `apps/admin/lib/api-server.ts` line ~103 reads via `readActiveBrand()` so the `x-brand-slug` header reflects the verified slug. A tampered cookie produces a `null` slug → no `x-brand-slug` header sent → api falls back to no brand context (operator sees "all brands" view), which is the correct degraded behavior.
    - Existing `<BrandSwitcher>` props don't change — `activeBrandSlug: string | null` is the same.
    - Tests covering the X-Brand-Slug header behavior in `api-server.spec.ts` are updated: install a signed cookie value (not a raw slug) and assert the header reflects the unsigned slug.
  </behavior>
  <action>
    Edit `apps/admin/lib/actions/set-active-brand.ts:32-37`. Replace the cookie write:

    ```typescript
    cookieStore.set('resto.active_brand', slug, {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'lax',
      path: '/',
    });
    ```

    With:

    ```typescript
    cookieStore.set('resto.active_brand', signActiveBrand(slug), {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'lax',
      path: '/',
    });
    ```

    Add `import { signActiveBrand } from '@/lib/active-brand-cookie';` at the top of the file.

    Edit `apps/admin/lib/actions/create-brand.ts:68-73` with the analogous change.

    Edit `apps/admin/app/dashboard/layout.tsx`:

    1. Add `import { readActiveBrand } from '@/lib/active-brand-cookie';`.
    2. Remove the `cookies()` from the `Promise.all` (line 15-19) — `readActiveBrand` already calls `cookies()` internally; calling it again is harmless but redundant.
    3. Replace line 25:
       ```typescript
       const activeBrandSlug = cookieStore.get('resto.active_brand')?.value ?? null;
       ```
       With:
       ```typescript
       const activeBrandSlug = await readActiveBrand();
       ```
    4. Remove the now-unused `cookieStore` variable.

    Edit `apps/admin/lib/api-server.ts` around line 103:

    1. Add `import { readActiveBrand } from './active-brand-cookie';` at the top (use relative path since `api-server.ts` is in the same `lib/` directory).
    2. Replace:
       ```typescript
       const activeBrand = cookieStore.get('resto.active_brand')?.value;
       ```
       With:
       ```typescript
       const activeBrand = await readActiveBrand();
       ```
    3. The subsequent `...(activeBrand ? { 'x-brand-slug': activeBrand } : {})` line works unchanged because `readActiveBrand` returns `string | null` with the same truthy semantics.

    Extend `apps/admin/test/set-active-brand-action.spec.ts`:
    - New test `it('writes the signed cookie value via signActiveBrand')` — mock `signActiveBrand` to return a deterministic value; call the action; assert `cookieStore.set` was called with the mocked value, not the raw slug.

    Extend `apps/admin/test/create-brand-action.spec.ts`:
    - Same shape — new test `it('writes signed cookie on brand-create success path')`.

    Update `apps/admin/test/api-server.spec.ts` X-Brand-Slug describe block:
    - In `beforeEach`, when seeding the cookie entry, use the signed form: install `cookieEntries.push({ name: 'resto.active_brand', value: signActiveBrand('demo-brand') })` (or mock `readActiveBrand` to return `'demo-brand'` directly — both are valid; the mock-`readActiveBrand` approach is simpler and tests the integration point cleanly).
    - Existing assertions on `x-brand-slug: 'demo-brand'` continue to pass.
    - Add one new test `it('omits x-brand-slug header when cookie HMAC fails')` — mock `readActiveBrand` to return `null`; assert the `headers` argument to `fetch` does NOT contain `x-brand-slug`.

    Do NOT modify `<BrandSwitcher>` or `<AppSidebar>` — they receive `activeBrandSlug: string | null` already; the shape is preserved.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin test apps/admin/test/set-active-brand-action.spec.ts apps/admin/test/create-brand-action.spec.ts apps/admin/test/api-server.spec.ts apps/admin/test/active-brand-cookie.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "signActiveBrand" apps/admin/lib/actions/set-active-brand.ts` returns at least one match.
    - `grep -n "signActiveBrand" apps/admin/lib/actions/create-brand.ts` returns at least one match.
    - `grep -n "readActiveBrand" apps/admin/app/dashboard/layout.tsx` returns at least one match.
    - `grep -n "readActiveBrand" apps/admin/lib/api-server.ts` returns at least one match.
    - `grep -nE "cookieStore\.get\('resto\.active_brand'\)" apps/admin/app/dashboard/layout.tsx apps/admin/lib/api-server.ts` returns ZERO matches (no remaining direct reads).
    - Test suites all green: `pnpm --filter @resto/admin test` exits 0; the four target specs each show all-pass.
    - `pnpm --filter @resto/admin typecheck` exits 0.
    - `pnpm --filter @resto/admin lint` exits 0.
    - Manual end-to-end check (recorded in SUMMARY): start dev stack, sign in as the three-brand operator, switch brands, observe in browser dev tools that the `resto.active_brand` cookie value contains a `.` separator and a base64url segment — NOT just a slug.
  </acceptance_criteria>
  <done>
    All four cookie I/O sites (2 writes, 2 reads) go through `signActiveBrand` / `readActiveBrand`. A tampered cookie silently falls back to no-active-brand. Test suite covers signed-cookie write, signed-cookie read, and tampered-cookie-falls-back behaviors. The `x-brand-slug` header sent to the api is now sourced from a verified slug.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                                                                                              |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| browser ↔ admin RSC       | The `resto.active_brand` cookie crosses this boundary on every request. Plan 03 adds HMAC verification on read so a tampered slug is silently downgraded to "no active brand" instead of being forwarded to the api as `x-brand-slug`.   |
| admin RSC ↔ apps/api      | The `x-brand-slug` header now carries only HMAC-verified slugs. The api STILL re-validates `(operator, tenantId, brandSlug)` membership; HMAC is defense-in-depth, not the authoritative check. CONTEXT D-03 + persona-skeptic agreed.   |
| secret store ↔ admin boot | `ACTIVE_BRAND_COOKIE_SECRET` lives in env; Plan 01 validates it at module load. Rotating the secret invalidates all live brand cookies in flight; operators silently fall back to "no active brand" on next render — acceptable UX cost. |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                   |
| --------- | ---------------------- | --------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-10   | Tampering              | Cookie value forged client-side         | mitigate    | HMAC-SHA256 over slug with `ACTIVE_BRAND_COOKIE_SECRET`; `timingSafeEqual` with length-guard prevents both signature forgery and timing-oracle leakage. Persona-skeptic still notes the api is the authoritative check, which this plan preserves.                |
| T-02-11   | Information Disclosure | HMAC secret leak via shared env         | mitigate    | Dedicated `ACTIVE_BRAND_COOKIE_SECRET` separate from `BETTER_AUTH_SECRET` (CONTEXT D-03). Rotating one does not impact the other. Server-only `import 'server-only'` in `active-brand-cookie.ts` prevents accidental client-bundle leak.                          |
| T-02-12   | Repudiation            | Operator denies brand-switch action     | accept      | Phase 02 does not add audit logging for brand-switch. Persona-skeptic noted this is a UX cookie, not a security boundary; audit trail for brand context (if needed) lives at the api layer when the api processes `x-brand-slug`. Out of Plan 03 scope.           |
| T-02-13   | Denial of Service      | HMAC computation cost per request       | accept      | HMAC-SHA256 of a ~20-byte slug is sub-microsecond on modern CPUs. Per-request cost is negligible relative to the existing `/api/auth/get-session` round-trip (line ~157-176 in api-server.ts).                                                                    |
| T-02-14   | Spoofing               | Cookie injection via subdomain takeover | mitigate    | Cookie has no explicit `Domain` (defaults to host); HMAC binds it to the slug, not the user. An attacker who controls a subdomain cannot inject a cookie that passes HMAC verification without `ACTIVE_BRAND_COOKIE_SECRET`. The api re-check is also a backstop. |

</threat_model>

<verification>
After both tasks pass:

1. `pnpm --filter @resto/admin test` — all admin specs green; pre-Plan-03 pass count not reduced (Plan 01 + Plan 02 specs still pass).
2. `pnpm --filter @resto/admin typecheck` + `pnpm --filter @resto/admin lint` — both clean.
3. Manual end-to-end: launch dev stack, sign in as `three@e2e.test` (Plan 02 seed fixture), inspect `resto.active_brand` cookie in browser DevTools → value contains `.<sig>` segment, NOT plain slug. Switch brand → cookie updates with new signed value. Tamper with cookie via DevTools (change one character of the signature) → reload dashboard → operator silently falls back to "all brands" view, no error.
4. ADM-00 Plan 02 scenario 3 (3-brand switch) still passes — the dropdown UX is unchanged.
   </verification>

<success_criteria>

- `apps/admin/lib/active-brand-cookie.ts` exists with `signActiveBrand` + `readActiveBrand` exports.
- All four cookie I/O sites go through the helpers — zero direct `cookieStore.get('resto.active_brand')?.value` reads remain outside `active-brand-cookie.ts`.
- HMAC verification uses `timingSafeEqual` with a length-check guard; tampered or malformed cookies return `null` without throwing.
- The `x-brand-slug` header forwarded to the api is sourced from `readActiveBrand()`, never from raw cookie value.
- ADM-05 is functionally closed pending end-to-end verification through Plan 02's smoke walk + Plan 04 UI work.

</success_criteria>

<output>
Create `.planning/phases/02-admin-shell/02-03-SUMMARY.md` when done. List:

- New file: `apps/admin/lib/active-brand-cookie.ts` (sign + verify).
- Files modified: `set-active-brand.ts`, `create-brand.ts`, `dashboard/layout.tsx`, `api-server.ts`, plus four test specs.
- Manual end-to-end verification result (cookie value inspected in browser; tamper test result).
- Note on the tradeoff CONTEXT D-03 made: HMAC is defense-in-depth, api remains authoritative. Future Phase 03 work may revisit if API-side `x-brand-slug` validation reveals gaps.
- Confirmation that ADM-05 ("active-brand state persists in signed cookie") is closed.

</output>
</content>
</invoke>
