---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 16
type: execute
wave: 11
depends_on: ["10.6-09", "10.6-10", "10.6-15"]
files_modified:
  - .planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md
autonomous: false
requirements: [ING-01, ING-02, ING-03, ING-04, ING-05, ING-08, ING-09, ING-10, ING-11, ING-12, ING-13, ING-14, ING-15]

must_haves:
  truths:
    - "Every project typechecks, every touched suite passes, and the committed OpenAPI artifact matches the controllers"
    - "No dropped column, no deleted concept and no forbidden pattern survives anywhere in the tree (D-02, D-05, D-06, D-33)"
    - "A founder walks the operator flow and the guest flow in a browser and confirms the phase does what it promised"
    - "The known accepted consequence of no cascade is observed and not treated as a bug (D-25)"
  artifacts:
    - path: ".planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md"
      provides: "the phase's verification evidence and the founder's sign-off"
  key_links:
    - from: "the running dev stack"
      to: "a placed order carrying an added ingredient and an excluded composition line"
      via: "browser walk-through, admin order detail confirming both"
      pattern: "excludedLabel"
---

<objective>
Prove the phase, end to end, with an automated sweep, a forbidden-pattern sweep, and a founder
walking both surfaces in a browser.

Purpose: every plan before this one verified its own slice. Nothing so far has watched a guest add
bacon, cross out onion, place the order, and an operator read both on the ticket.
Output: verification evidence in the SUMMARY and a founder sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The automated sweep</name>
  <files>.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md</files>
  <read_first>
    - `apps/api/vitest.config.ts` — `pool: 'forks'` with `singleFork: true`; the whole `apps/api` suite in one process produces false 429 and timeout failures, so every api command below names files or a directory
    - `apps/api/project.json` — the `e2e` target's `--poolOptions.forks.singleFork=false --fileParallelism=false` flags
    - `.github/workflows/ci.yml` — the `openapi-drift` job, the one gate that is CI-only
    - every SUMMARY written by plans 01-15 — the record of what was actually built and of anything deferred
  </read_first>
  <action>
Run each of these and record the exact output in the SUMMARY. Do not run the bare `apps/api` suite.

Typecheck: `pnpm exec nx run-many -t typecheck` (db, domain, api, admin, ui, api-client, cart,
qr-menu, website).
Lint: `pnpm exec nx affected -t lint` against the phase's base commit.
Contract: `pnpm openapi:check`.
Database: `pnpm -C packages/db exec vitest run test/integration/tenant-isolation.spec.ts`,
`pnpm -C packages/db exec vitest run test/integration/composite-tenant-fk.spec.ts` and
`pnpm -C packages/db exec vitest run test/integration/erase-includes-ingredients.spec.ts` — the
third is plan 02's seeded GDPR proof and is the only gate in the phase that would catch a
`tenancy_erase_tenant` missing a DELETE for one of the three new tables.
API unit: `pnpm -C apps/api exec vitest run test/unit/catalog` and
`pnpm -C apps/api exec vitest run test/unit/create-order.service.spec.ts`.
API e2e, one file per invocation with the e2e flags:
`test/e2e/catalog.e2e.spec.ts`, `test/e2e/catalog-rbac.e2e.spec.ts`,
`test/e2e/catalog-tenant-isolation.e2e.spec.ts`, `test/e2e/catalog-reads.e2e.spec.ts`,
`test/e2e/ingredient-stop.e2e.spec.ts`, `test/e2e/order-lifecycle.e2e.spec.ts`.
Front ends: `pnpm -C apps/admin exec vitest run`, `pnpm -C apps/qr-menu exec vitest run`,
`pnpm -C apps/website exec vitest run`.

For any failure, state whether it was already red on the phase's base commit — check out the base in
a throwaway worktree and run the same command there before calling it a regression. `main` in this
repository has failing checks of its own, so "CI is red" says nothing on its own.
  </action>
  <verify>
    <automated>pnpm exec nx run-many -t typecheck &amp;&amp; pnpm openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec nx run-many -t typecheck` exits 0 for every project
    - `pnpm openapi:check` exits 0
    - All three `packages/db` integration specs pass, `erase-includes-ingredients.spec.ts` included
    - All six named e2e files pass when run individually
    - All three front-end suites pass
    - Every command's result is recorded in the SUMMARY, and any failure is attributed to the base commit or to this phase with evidence
    - No command in this task ran the whole `apps/api` test tree in one process
  </acceptance_criteria>
  <done>Every automated gate the phase can run has run, and its result is written down.</done>
</task>

<task type="auto">
  <name>Task 2: The forbidden-pattern and coverage sweep</name>
  <files>.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md</files>
  <read_first>
    - `CLAUDE.md` — the comment ban, the ScopedTx/RLS invariant, the hard-delete ban, the `withoutTenant` allowlist rule
    - `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md` — the decision list this sweep checks against
    - `packages/db/src/withoutTenant.allowlist.ts` and `apps/api/eslint.config.mjs` — the mirrored allowlist that must not have grown
  </read_first>
  <action>
Run each grep and record the result. Every one of these must come back empty unless noted; a
non-empty result is a defect to fix in this plan, not a note for later.

Dropped concepts, whole tree excluding `.planning` and `node_modules`:
`grep -rn "minSelectable\|maxSelectable" apps packages docs` — expect zero. `grep -rn "min_selectable\|max_selectable" packages/db/src apps` — expect zero (the migration file legitimately names them; `packages/db/migrations` is excluded).
`grep -rn "defaultAmount" packages/ui apps/qr-menu apps/website` — expect zero; D-05 removed preselection from the guest.
`grep -rn "isSingleChoiceGroup\|preselected(" packages apps` — expect zero.

Forbidden patterns:
`git diff <base> -- packages/ui/src/guest apps/qr-menu/src apps/website/components | grep "^+.*from 'lucide-react'"` — expect zero. Six guest files already import lucide directly at the base commit (`cart-line-row`, `cart-bar`, `cart-button`, `item-detail`, `nutrition-info`, `theme-toggle`); the gate is that this phase ADDS none, not that the directory is clean. Do not open those six to fix them — that is a separate change.
`git diff --stat <base> -- packages/db/src/withoutTenant.allowlist.ts apps/api/eslint.config.mjs` — expect no change; this phase adds no bypass.
`grep -rn "tx.delete\|tx.insert\|tx.select" apps/api/src --include=*.ts | grep -v "drizzle.repository\|drizzle.reader"` — expect zero new sites outside the sanctioned adapters (ADR-0020 I-1).

Comment hygiene (the ban is retroactive and this phase touched ~65 files): review
`git diff <base> -- apps packages` for every added comment line and delete each one that does not
carry a WHY a reader could not recover from the code. The two legitimate survivors are the migration
file's hand-written header (D-31) and any two-line note citing an ADR or a decision id. No file
headers, no restating comments, no section banners, no "added for X".

Coverage: confirm each of ING-01..05 and ING-08..15 is demonstrably implemented by naming, per
requirement, the file and the test that proves it. Confirm ING-06 and ING-07 appear NOWHERE in the
shipped code — no `default_amount > 0` behaviour, no stepper, no `amount` other than 1 — since both
are deferred to the backlog and D-02 forbids leaving hooks.
  </action>
  <verify>
    <automated>grep -rn "minSelectable\|maxSelectable" apps packages docs --include=*.ts --include=*.tsx --include=*.json --include=*.yaml | grep -v node_modules | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0
    - `git diff <base> -- packages/ui/src/guest apps/qr-menu/src apps/website/components | grep "^+.*from 'lucide-react'"` returns nothing — no NEW direct lucide import was introduced by this phase
    - `git diff --stat` shows no change to `packages/db/src/withoutTenant.allowlist.ts` or to the ESLint allowlist in `apps/api/eslint.config.mjs`
    - Every comment added by this phase either carries a WHY citing an ADR or decision id, or has been deleted; the SUMMARY states the net comment delta
    - The SUMMARY carries a per-requirement table naming the implementing file and the proving test for all 13 in-scope ING ids
    - The SUMMARY states explicitly that ING-06 and ING-07 have no hooks in the shipped code
  </acceptance_criteria>
  <done>Nothing dropped survives, nothing forbidden was introduced, and every requirement can be pointed at.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk both surfaces in a browser</name>
  <files>.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md</files>
  <read_first>
    - `tools/scripts/seed/commands/seed-demo.ts` — how a demo restaurant with a menu is seeded into the reset dev database
    - `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md` D-25 — the accepted consequence to observe and NOT fix
    - the SUMMARYs of plans 12-15 for anything they recorded as deferred or chosen differently
  </read_first>
  <what-built>
The whole phase: an ingredient library with its own screen, groups described by display and
behaviour, single ingredients on a dish, a two-mode composition with removable lines, a per-location
ingredient stop list with the dish offer, the guest's tiles and pill strips and composition line,
and exclusions reaching the order as their own rows.

Type-checking and unit tests cannot see any of this working. Only a browser can — and this project's
own history says so: `tsc` plus `vitest` green is not the same as the feature working.
  </what-built>
  <action>
Bring up the dev stack and the four dev servers (api on 5001, admin on 4000 via
`pnpm exec nx run admin:serve`, website on 3002, qr-menu on 3003), seed a demo restaurant, then walk
the flow below and record what you saw in the SUMMARY.
  </action>
  <how-to-verify>
    1. Admin, Menu → Ингредиенты: create three ingredients — one with a photo and a description, one with a photo and no description, one with no photo at all. Confirm the grid shows a placeholder for the third and that no search box exists.
    2. Groups tab: create a group "Топпинги" as tiles + several, not required, and add all three ingredients; drag to reorder them. Create a second group "Тесто" as tabs + one + required with two ingredients. Confirm no field anywhere asks for a number.
    3. Open a pizza: attach "Топпинги" and "Тесто"; then try to attach one of the topping ingredients as a single ingredient — confirm the picker refuses it and names the group.
    4. Attach a DIFFERENT library ingredient to the pizza as a single ingredient.
    5. In the pizza's Состав section switch to "Из ингредиентов", add three lines, mark one removable, reorder them. Confirm no price and no gram appears on any line.
    6. Publish the menu.
    7. Guest (qr-menu on 3003): open the pizza. Confirm the toppings render as photo tiles with the placeholder on the photoless one and the description as a small line; confirm Тесто renders as a tab strip that cannot hold two; confirm the single ingredient is offered; confirm the composition reads under the description and the removable line strikes through when tapped and restores when tapped again.
    8. Add two toppings, cross out the removable composition line, add to cart, and place the order. Confirm the cart price counts the toppings and not the exclusion.
    9. Admin, Orders → open that order: confirm the two toppings render as they always did and the exclusion appears as its own "Без …" line.
    10. Stop list: stop one topping. Confirm the dialog lists ONLY dishes whose composition contains it (not dishes that merely offer it as an extra), that nothing is ticked, and that choosing "Только ингредиент" stops nothing else.
    11. Reload the guest menu: the stopped topping is greyed with "Сегодня недоступно" and cannot be selected, and the dish is still in the menu.
    12. **Observe, do not fix (D-25):** stop every option of the required Тесто group. The pizza stays in the menu and looks orderable until opened, and only then is the Add button inactive. This is the accepted price of "only the operator decides" and is not a bug in this phase.
    13. Confirm the website (3002) renders the same item sheet as the qr-menu — both consume the same component.
  </how-to-verify>
  <acceptance_criteria>
    - Steps 1-13 were performed against a running stack, not reasoned about
    - Any step that behaved differently from its description is written into the SUMMARY with what was seen, and either fixed in this plan or recorded as a gap with an owner
    - Step 12's behaviour was observed and explicitly NOT changed
    - The placed order's `order_modifiers` rows are checked in the database: the added toppings carry `kind = 'added'` with their price deltas, the exclusion carries `kind = 'excluded'`, `price_delta = 0` and a `name_snapshot`, and no exclusion text appears in any order comment field
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec nx run-many -t typecheck</automated>
  </verify>
  <done>A guest added two toppings and crossed out a composition line, the order carries both as structured rows with the right kinds and prices, and the operator sees them on the order detail.</done>
  <resume-signal>Type "approved" when the walk-through matches, or describe exactly which step differed and what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the whole phase to production | this is the last gate before the work is considered done; every boundary the earlier plans declared is re-checked here in one place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-16-01 | Elevation of Privilege | a write route shipped without a permission decorator | mitigate | plan 08's per-route RBAC e2e is re-run here as part of the sweep |
| T-10.6-16-02 | Information Disclosure | a cross-tenant leak through one of the three new tables | mitigate | both `packages/db` integration specs and the catalog tenant-isolation e2e are re-run here |
| T-10.6-16-03 | Tampering | an exclusion smuggled into a comment field rather than a structured row | mitigate | the browser walk-through checks the actual `order_modifiers` rows and the comment field in the database |
| T-10.6-16-04 | Repudiation | a green typecheck standing in for a working feature | mitigate | the blocking human walk-through is the control; this project's history records the exact failure mode |
| T-10.6-16-SC | Tampering | npm/pip/cargo installs | mitigate | the phase installed zero external packages; the sweep confirms `pnpm-lock.yaml` is unchanged except for nothing |
</threat_model>

<verification>
- `pnpm exec nx run-many -t typecheck` exits 0
- `pnpm openapi:check` exits 0
- Every named test command passes, each run narrowly, `erase-includes-ingredients.spec.ts` among them
- The forbidden-pattern sweep comes back empty
- The founder's walk-through is recorded and signed off
</verification>

<success_criteria>
- Every project typechecks and every touched suite passes
- No dropped column, deleted concept or forbidden pattern survives
- Every in-scope ING requirement is pointed at a file and a test
- A guest's added topping and crossed-out onion both reach the operator's order detail, proven in a browser and in the database
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-16-SUMMARY.md` when done
</output>
