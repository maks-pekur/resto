---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 08
type: execute
wave: 7
depends_on: ["10.6-07"]
files_modified:
  - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts
  - docs/api/openapi.yaml
  - packages/api-client/src/generated/api.ts
  - apps/api/test/e2e/catalog.e2e.spec.ts
  - apps/api/test/e2e/catalog-rbac.e2e.spec.ts
  - apps/api/test/e2e/ingredient-stop.e2e.spec.ts
  - apps/api/test/e2e/catalog-reads.e2e.spec.ts
  - apps/api/test/e2e/catalog-tenant-isolation.e2e.spec.ts
  - apps/api/test/e2e/catalog-tenant-read-isolation.e2e.spec.ts
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, ING-08, ING-09, ING-13]

must_haves:
  truths:
    - "Every ingredient, membership, composition and ingredient-stop use case is reachable over HTTP under the menu permission — no new RBAC resource (D-29)"
    - "A second attachment of the same ingredient to one dish answers 409 and names where it already is (D-04)"
    - "Stopping an ingredient makes it appear in /v1/menu/availability's second list without changing /v1/menu (D-20, D-21, ING-08)"
    - "An ingredient in two groups appears once in the published payload (D-03, ING-09)"
    - "The committed OpenAPI artifact and the generated client match the controllers (CI gate openapi-drift)"
  artifacts:
    - path: "apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts"
      provides: "nine routes for the library, membership, composition and ingredient stops"
    - path: "docs/api/openapi.yaml"
      provides: "regenerated contract"
    - path: "apps/api/test/e2e/ingredient-stop.e2e.spec.ts"
      provides: "stop reaches availability, menu unchanged"
  key_links:
    - from: "catalog.controller.ts"
      to: "@Permissions({ menu: ['update'] })"
      via: "the same decorator every existing catalog write carries"
      pattern: "Permissions"
    - from: "docs/api/openapi.yaml"
      to: "packages/api-client/src/generated/api.ts"
      via: "nx run api:openapi:emit then nx run api-client:gen"
      pattern: "modifier-options"
---

<objective>
Expose the new catalog use cases over HTTP, regenerate the committed contract, and prove the whole
API path with narrow e2e specs.

Purpose: the admin derives its types from `components['schemas']`; nothing on the operator surface
can be built until this artifact is regenerated.
Output: nine routes, a regenerated OpenAPI + client, and e2e coverage for the refusals and the
dedupe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@CLAUDE.md

Read before starting (targeted):
- `10.6-PATTERNS.md` section "catalog.controller.ts" — the decorator stack, reproduced in full
- `10.6-RESEARCH.md` section 6 "Contract surface" — the CI `openapi-drift` job and the exact two-command regeneration; and section 7 for which e2e files to extend
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nine routes on the catalog controller</name>
  <files>apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts</files>
  <read_first>
    - `apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts` lines 1-70 (imports), 93-125 (the constructor injection list) and 164-240 (`modifierGroup`, `modifierOption`, `setItemModifierGroupsRoute`, `stopListAdd`, `stopListRemove` — the exact decorator stacks to copy)
    - `apps/api/src/contexts/catalog/application/dto.ts` — the DTO classes plans 05 and 07 added
    - `10.6-CONTEXT.md` D-29 — permissions follow the menu resource; no new RBAC resource, no role re-sync
  </read_first>
  <action>
Add nine handlers, each a single `return wrap(() => this.someService.execute(...))` line, each
injecting its service into the constructor by class reference the way every existing service is
injected:

- `GET modifier-options` — `ListModifierOptionsService`, `@LocationNeutral()`, returns
  `ModifierOptionListResponseDto`.
- `PATCH modifier-options/:id/archive` — `ArchiveModifierOptionService`, `@Permissions({ menu: ['update'] })`,
  `@LocationNeutral()`, `@Param('id', ParseUUIDPipe)`.
- `GET modifier-options/:id/usage` — `GetModifierOptionUsageService`, `@LocationNeutral()`, returns
  `ModifierOptionUsageResponseDto`.
- `PUT modifier-groups/:id/options` — `SetGroupModifierOptionsService`, write decorators.
- `PUT items/:id/modifier-options` — `SetItemModifierOptionsService`, write decorators.
- `PUT items/:id/composition` — `SetItemCompositionService`, write decorators.
- `POST stop-list/options` — `OptionStopListService.stop`, write decorators, NO `@LocationNeutral()`
  (it is location-scoped by nature, exactly like the existing `stop-list` pair).
- `DELETE stop-list/options/:optionId` — `OptionStopListService.unstop`,
  `@HttpCode(HttpStatus.NO_CONTENT)`.
- `GET stop-list/options` — `GetOptionStopListService`.

Every write carries `@Permissions({ menu: ['update'] })` — the same decorator the 24 existing
catalog writes carry. Do not introduce a new RBAC resource and do not add a role re-sync migration
(D-29 is confirmed by the code: Menu has no per-sub-resource split).

Every body is validated with `new RestoZodValidationPipe(SomeInputDto)` per parameter, never a
global pipe. Every handler body is one `wrap(...)` line — no try/catch, no business logic in the
controller.

Order the new route declarations so `stop-list/options` is declared BEFORE any
`stop-list/:itemId`-shaped route, or Fastify will match `options` as an item id.
  </action>
  <verify>
    <automated>pnpm exec nx run api:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Permissions({ menu: \['update'\] })" apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts` increased by exactly 6 (the six write routes)
    - `grep -n "stop-list/options" apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts` shows those declarations at a lower line number than `stop-list/:itemId`
    - No handler body contains a `try` block — every one is a single `wrap(...)` return
    - `grep -n "RestoZodValidationPipe" apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts` shows one per new body-carrying route
    - `pnpm exec nx run api:typecheck` exits 0
  </acceptance_criteria>
  <done>Nine routes exist, all gated on the menu resource, all one-liners over the plan-07 services.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the committed contract</name>
  <files>docs/api/openapi.yaml, packages/api-client/src/generated/api.ts</files>
  <read_first>
    - `packages/api-client/CLAUDE.md` — the two-command regeneration and the rule that both artifacts are committed
    - `.github/workflows/ci.yml` — the `openapi-drift` job that fails the PR on a diff; it is a CI gate, NOT a pre-commit hook, so a stale artifact passes every local check
  </read_first>
  <action>
Run `pnpm exec nx run api:openapi:emit` then `pnpm exec nx run api-client:gen`, then
`git diff -- docs/api packages/api-client` and commit both artifacts. Confirm the emitted schema
names the new operations and that `components['schemas']` now carries the reshaped
`ModifierGroupListItem` (display/behaviour, no numbers), `ModifierOptionListResponse` and
`ModifierOptionUsageResponse` — those three are exactly what plan 11's admin queries derive from.

Then run `pnpm openapi:check` and confirm it exits 0 with no diff. A red `openapi-drift` job is the
single most likely way this phase fails in CI while every local check is green.
  </action>
  <verify>
    <automated>pnpm openapi:check</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm openapi:check` exits 0
    - `git status --porcelain docs/api packages/api-client` is empty after the regeneration commit
    - `grep -c "modifier-options" docs/api/openapi.yaml` returns at least 4
    - `grep -n "minSelectable" docs/api/openapi.yaml` returns zero matches
  </acceptance_criteria>
  <done>The committed contract and the generated client match the controllers, and CI's drift gate will pass.</done>
</task>

<task type="auto">
  <name>Task 3: e2e coverage for the refusals, the stop and the dedupe</name>
  <files>apps/api/test/e2e/catalog.e2e.spec.ts, apps/api/test/e2e/catalog-rbac.e2e.spec.ts, apps/api/test/e2e/ingredient-stop.e2e.spec.ts, apps/api/test/e2e/catalog-reads.e2e.spec.ts, apps/api/test/e2e/catalog-tenant-isolation.e2e.spec.ts, apps/api/test/e2e/catalog-tenant-read-isolation.e2e.spec.ts</files>
  <read_first>
    - `apps/api/test/e2e/catalog.e2e.spec.ts` — the harness (app bootstrap, tenant seeding, authenticated request helper) and an existing modifier-group test to copy. Its own `POST /v1/catalog/modifier-groups` bodies at lines 181-182, 196, 206, 423-424, 951, 1021, 1118, 1127 and 1185-1186 still send `minSelectable`/`maxSelectable`, its `POST /v1/catalog/modifier-options` bodies at 436, 961 and 1031 still send `modifierGroupId`, and the test at line 201 asserts the deleted `maxSelectable < minSelectable` refusal. Its `/v1/menu` response block around lines 452-473 breaks too, not just the request bodies: the `json<{ ... options[] ... defaultAmount }>()` annotation and the `expect(grp?.options[0]?.defaultAmount).toBe(1)` assertion both die once groups lose their inline `options` array and the option loses `defaultAmount` — rewrite them against the top-level `modifierOptions[]` and the group's `optionIds[]`
    - `apps/api/test/e2e/catalog-reads.e2e.spec.ts` lines 266-267, 332 and 365 — three `POST /v1/catalog/modifier-groups` bodies carrying the two numbers and no `display`/`behaviour`; lines 340 and 373 attach an option with `modifierGroupId`, which after plan 05 is not a field of the input schema at all
    - `apps/api/test/e2e/catalog-tenant-isolation.e2e.spec.ts` lines 136 and 148-149 — the same two bodies
    - `apps/api/test/e2e/catalog-tenant-read-isolation.e2e.spec.ts` line 75 — its `createGroup` helper's body
    - `apps/api/test/e2e/catalog-rbac.e2e.spec.ts` — how a route is asserted to refuse a principal lacking `menu:update`
    - `apps/api/vitest.config.ts` and `apps/api/project.json` — the e2e target runs with `--poolOptions.forks.singleFork=false --fileParallelism=false`; run single files, never the whole tree
    - `10.6-RESEARCH.md` section 7 — no ingredient-stop e2e exists today; this file is new
  </read_first>
  <action>
Extend `catalog.e2e.spec.ts` with: creating an ingredient without a group id succeeds and returns an
id; setting a group's ingredients then reading `GET modifier-groups/:id` returns them in the written
order; attaching to a dish an ingredient already reachable through one of its groups answers 409
with `code: 'catalog.ingredient_already_attached'` (D-04); setting an assembled composition then
reading the item detail returns the same lines in the same order with their `removable` flags
(ING-13/ING-14); and `GET /v1/menu` for a tenant whose bacon sits in two groups contains exactly one
`modifierOptions` entry for it, referenced by id from both groups (D-03, ING-09).

Then move every EXISTING modifier-group and modifier-option request body in the e2e tree onto the
new schemas. These are runtime failures, not type errors — Zod strips unknown keys rather than
rejecting them, so a body that still sends `minSelectable` gets a 400 only because
`display`/`behaviour` are missing, and a body that still sends `modifierGroupId` gets a 200 and
silently creates an unlinked ingredient, which is what breaks the surviving assertions. Four files:

- `catalog.e2e.spec.ts` — add `display: 'tiles'` and `behaviour: 'several'` (or `'one'` where the
  test's intent was a single choice) to every group body and delete both numbers. Delete the
  `rejects an invalid modifier group (maxSelectable < minSelectable) at the DTO boundary` test at
  line 201: the `.refine()` it exercises is gone and there is no cross-field rule left to guard.
  Replace each `POST modifier-options` body's `modifierGroupId` with a follow-up
  `PUT /v1/catalog/modifier-groups/:id/options` carrying the created option's id — that is where
  membership lives now (D-01/D-03), and it is the route task 1 added.
- `catalog-reads.e2e.spec.ts` — same treatment. Its `GET /modifier-groups returns option-count` and
  `GET /modifier-groups/:id returns embedded options` tests both depend on the option actually
  being IN the group, so the `PUT .../options` follow-up is load-bearing there, not cosmetic; their
  assertions (`optionCount >= 1`, `options.length >= 1`) stay exactly as they are.
- `catalog-tenant-isolation.e2e.spec.ts` — two bodies, group fields only; the hijack test's 404
  assertion is untouched.
- `catalog-tenant-read-isolation.e2e.spec.ts` — one body inside the `createGroup` helper.

Extend `catalog-rbac.e2e.spec.ts` so each of the six new write routes is refused for a principal
without `menu:update` — copy the existing per-route assertion rather than asserting once.

Create `apps/api/test/e2e/ingredient-stop.e2e.spec.ts`: stopping an ingredient at a location makes
its id appear in `GET /v1/menu/availability`'s `stoppedIngredientIds` while `stoppedItemIds` stays
unchanged and `GET /v1/menu` still contains the ingredient (the published menu omits nothing, D-21);
resuming removes it; and a `POST /v1/orders` containing the stopped ingredient answers 422 with
`code: 'ordering.modifier_unavailable'` (D-24). Assert explicitly that no dish was stopped as a side
effect — D-20 forbids a cascade and D-25 accepts the consequence.
  </action>
  <verify>
    <automated>pnpm -C apps/api exec vitest run test/e2e/ingredient-stop.e2e.spec.ts test/e2e/catalog.e2e.spec.ts test/e2e/catalog-reads.e2e.spec.ts --poolOptions.forks.singleFork=false --fileParallelism=false</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C apps/api exec vitest run test/e2e/ingredient-stop.e2e.spec.ts --poolOptions.forks.singleFork=false --fileParallelism=false` passes
    - `pnpm -C apps/api exec vitest run test/e2e/catalog.e2e.spec.ts --poolOptions.forks.singleFork=false --fileParallelism=false` passes
    - `pnpm -C apps/api exec vitest run test/e2e/catalog-rbac.e2e.spec.ts --poolOptions.forks.singleFork=false --fileParallelism=false` passes
    - `pnpm -C apps/api exec vitest run test/e2e/catalog-reads.e2e.spec.ts test/e2e/catalog-tenant-isolation.e2e.spec.ts test/e2e/catalog-tenant-read-isolation.e2e.spec.ts --poolOptions.forks.singleFork=false --fileParallelism=false` passes
    - `grep -rn "minSelectable\|maxSelectable" apps/api/test/e2e/` returns zero matches, test names included
    - `grep -rn "modifierGroupId:" apps/api/test/e2e/` returns zero matches — every option-to-group link goes through `PUT modifier-groups/:id/options`
    - The dedupe test asserts `modifierOptions.filter(o => o.id === baconId).length === 1`, not merely that the array is non-empty
    - The stop test asserts the stopped ingredient is STILL present in `GET /v1/menu`
    - No verification command in this plan runs the whole `apps/api` test tree in one process
  </acceptance_criteria>
  <done>The HTTP surface is proven end to end for the union, the refusals, the stop and the dedupe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator session to `/v1/catalog/*` | authenticated; authorisation is the `menu` resource and the bound tenant/location |
| unauthenticated client to a catalog write | must be refused by the guard chain before any service runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-08-01 | Elevation of Privilege | a new route shipped without a permission decorator | mitigate | every write carries `@Permissions({ menu: ['update'] })`, and `catalog-rbac.e2e.spec.ts` asserts the refusal per route — a forgotten decorator fails a test, not a review |
| T-10.6-08-02 | Spoofing | a guest reaching an operator route | mitigate | the controller's existing guard chain is unchanged; no new route is marked `@Public()` |
| T-10.6-08-03 | Information Disclosure | the usage endpoint leaking another tenant's dish names | mitigate | the query runs under the bound tenant with RLS behind it; `catalog-tenant-isolation.e2e.spec.ts` already covers the pattern for this controller |
| T-10.6-08-04 | Tampering | a stale committed OpenAPI artifact letting the admin build against a contract the server does not serve | mitigate | `pnpm openapi:check` is run in this plan and gated in CI by `openapi-drift` |
| T-10.6-08-SC | Tampering | npm/pip/cargo installs | mitigate | no package is installed by this plan |
</threat_model>

<verification>
- `pnpm exec nx run api:typecheck` exits 0
- `pnpm openapi:check` exits 0
- All six named e2e specs pass when run as individual files
- `grep -rn "minSelectable\|maxSelectable\|modifierGroupId:" apps/api/test/e2e/` returns zero matches
</verification>

<success_criteria>
- Nine routes exist, gated on `menu`, with no new RBAC resource
- The committed contract matches the controllers
- 409 on a duplicate attachment, 422 on a stopped ingredient, one payload entry per ingredient
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-08-SUMMARY.md` when done
</output>
