---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 09
type: execute
wave: 4
depends_on: ["10.6-03"]
files_modified:
  - packages/api-client/src/menu-types.ts
  - packages/api-client/src/public.ts
  - packages/cart/src/cart.ts
  - apps/qr-menu/src/api/client.ts
  - apps/qr-menu/src/App.tsx
  - apps/qr-menu/test/quick-add.spec.tsx
  - apps/qr-menu/test/menu-screen.spec.tsx
  - apps/qr-menu/test/menu-finder.spec.tsx
  - apps/qr-menu/test/nutrition-info.spec.tsx
  - apps/qr-menu/test/theme.spec.tsx
  - apps/qr-menu/test/table.spec.tsx
  - apps/qr-menu/test/menu-refresh.spec.tsx
  - apps/qr-menu/test/drawer.spec.tsx
  - apps/website/lib/api-client.ts
  - apps/website/app/page.tsx
  - apps/website/components/menu/menu-page-client.tsx
  - apps/website/test/menu-render.spec.tsx
  - packages/ui/src/guest/menu-screen.tsx
  - packages/ui/src/guest/item-dialog.tsx
  - packages/ui/src/guest/item-detail.tsx
  - packages/ui/src/guest/use-item-selection.ts
  - packages/ui/src/guest/cart-line-row.tsx
autonomous: true
requirements: [ING-05, ING-08, ING-09, ING-11, ING-13, ING-14, ING-15]

must_haves:
  truths:
    - "All three hand-written guest wire-type files describe the same payload, changed in one commit (RESEARCH 8.8)"
    - "Both guest apps carry the stopped-ingredient list beside the stopped-item list (D-21, ING-08)"
    - "The selection hook reads the group's own behaviour field and has no numeric ceiling, no preselection and no default amount (D-05, D-06, D-07, ING-05, ING-11)"
    - "The guest can mark a removable composition line excluded, and the exclusion reaches the cart line as its own modifier row with kind excluded and price zero (D-16, D-18, D-34, ING-15)"
  artifacts:
    - path: "packages/api-client/src/menu-types.ts"
      provides: "MenuModifierOptionDto with description/imageUrl, group display/behaviour/optionIds, item composition fields, MenuDto.modifierOptions"
      contains: "modifierOptions"
    - path: "packages/ui/src/guest/use-item-selection.ts"
      provides: "behaviour-keyed selection, exclusion state, no preselection"
      contains: "excluded"
    - path: "packages/cart/src/cart.ts"
      provides: "CartModifier.kind"
      contains: "kind"
  key_links:
    - from: "apps/qr-menu/src/App.tsx"
      to: "MenuScreen stoppedIngredientIds"
      via: "state set from fetchAvailability, threaded to ItemDialog"
      pattern: "stoppedIngredientIds"
    - from: "packages/ui/src/guest/item-detail.tsx"
      to: "onAddToCart"
      via: "chosen modifiers and exclusions merged into one modifiers array"
      pattern: "kind: 'excluded'"
---

<objective>
Change the guest wire types in all three places that hand-maintain them, thread the second
availability list through both apps, and rewrite the selection hook around the group's own fields.

Purpose: nothing ties these three type files to the backend Zod schema or to each other. Updating
two of three typechecks clean everywhere and fails only as an `undefined` in a browser
(RESEARCH 8.8) — so they move together, in one plan, in one wave.
Output: both guest apps compile against the new payload and carry exclusion state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-UI-SPEC.md
@CLAUDE.md

Read before starting (targeted):
- `10.6-RESEARCH.md` section 4 "Guest surface" (whole section) and 8.7, 8.8
- `10.6-PATTERNS.md` sections "use-item-selection.ts", "menu-types.ts", "qr-menu/src/api/client.ts + website/lib/api-client.ts", "App.tsx"
</context>

<scope_note>
This plan is data and state only. The tiles renderer, the tabs+several pill strip and the visible
composition line are plan 10's work in the same files. Where a render branch must change here only
to keep `packages/ui` compiling against the new types, change the call site and leave the branch
body alone — and do not describe the intermediate state as a first version anywhere in code, a
commit message or the SUMMARY. The phase ships both halves.
</scope_note>

<tasks>

<task type="auto">
  <name>Task 1: The three hand-written wire-type files and the cart modifier</name>
  <files>packages/api-client/src/menu-types.ts, packages/api-client/src/public.ts, apps/qr-menu/src/api/client.ts, apps/website/lib/api-client.ts, packages/cart/src/cart.ts</files>
  <read_first>
    - `packages/api-client/src/menu-types.ts` — the whole file (136 lines); `MenuModifierOptionDto` (21-28), `MenuModifierGroupDto` (30-37), `MenuItemDto` (39-60), `MenuDto` (128-136)
    - `apps/qr-menu/src/api/client.ts` lines 42-59 — the locally declared `MenuAvailability` and `fetchAvailability` with its `{ stoppedItemIds: [] }` 404 fallback
    - `apps/website/lib/api-client.ts` lines 34-48 — the third, independent copy of the same interface
    - `apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts` — plan 03's Zod wire schemas; these hand-written types must match them field for field
    - `packages/cart/src/cart.ts` lines 4-24 — `CartModifier` and `CartLineItem`
  </read_first>
  <action>
Keep the existing type NAMES — do not introduce a `MenuIngredientDto`; the code says `modifier`
and `MenuModifierOptionDto` already exists and is exported.

`packages/api-client/src/menu-types.ts`:
- `MenuModifierOptionDto`: add `description: LocalizedText | null` and `imageUrl: string | null`;
  remove `sortOrder` and `defaultAmount`. `defaultAmount` goes because plan 03 removed it from the
  wire schema and this plan deletes its only reader (`preselected` in `use-item-selection.ts`) in
  task 3 — D-05 takes default-in out of the phase, so nothing in the guest may keep a handle on it.
  `freeAmount` stays exactly as it is: it is still on the wire, the server prices with it, and this
  phase's decisions say nothing about it.
- `MenuModifierGroupDto`: remove `minSelectable`, `maxSelectable` and `options`; add
  `display: 'tiles' | 'tabs'`, `behaviour: 'one' | 'several'`, `optionIds: readonly string[]`.
- `MenuItemDto`: add `extraOptionIds: readonly string[]`,
  `compositionMode: 'text' | 'assembled'`, `composition: readonly string[]`, and
  `compositionLines: readonly { readonly optionId: string; readonly removable: boolean }[]`.
- `MenuDto`: add `modifierOptions: readonly MenuModifierOptionDto[]`.
Re-export anything newly named from `packages/api-client/src/public.ts` beside the existing block.

`apps/qr-menu/src/api/client.ts` AND `apps/website/lib/api-client.ts`: add
`stoppedIngredientIds: string[]` to each file's own `MenuAvailability` interface AND to each file's
404 fallback object. Both files, in this task, in this commit — a miss in one of them typechecks
clean in every project except its own consumers and surfaces only at runtime.

`packages/cart/src/cart.ts`: add `readonly kind?: 'added' | 'excluded'` to `CartModifier`. Do not
add a second array to `CartLineItem` — D-34 is one collection with one discriminating field, and the
cart's existing line-total loop already contributes zero for a `priceDelta` of `'0'`.
  </action>
  <verify>
    <automated>pnpm exec nx run api-client:typecheck &amp;&amp; pnpm exec nx run cart:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stoppedIngredientIds" apps/qr-menu/src/api/client.ts` returns at least 2 (interface and 404 fallback)
    - `grep -c "stoppedIngredientIds" apps/website/lib/api-client.ts` returns at least 2
    - `grep -n "minSelectable\|maxSelectable" packages/api-client/src/menu-types.ts` returns zero matches — both fields live only on the group interface, so the whole file is the right scope for them
    - `awk '/^export interface MenuModifierOptionDto/,/^}/' packages/api-client/src/menu-types.ts | grep -c "defaultAmount\|sortOrder"` prints 0. This half is scoped to the option interface deliberately: `sortOrder` survives on `MenuPhotoDto` (line 6), `MenuItemSizeDto` (18), `MenuItemDto` (57) and `MenuCategoryDto` (67), four entities this phase does not touch. A whole-file grep would read as an order to strip ordering from photos, sizes, items and categories and break the guest wire contract for all four. The scoped command prints 2 before the change and must print 0 after
    - `grep -n "modifierOptions" packages/api-client/src/menu-types.ts` returns a match inside `MenuDto`
    - `grep -n "kind" packages/cart/src/cart.ts` shows the field on `CartModifier` and nowhere else new
    - Every field added here matches a field in `public-menu.controller.ts`'s Zod schemas by name and type (verified by reading both side by side)
  </acceptance_criteria>
  <done>All three hand-written copies of the wire shape agree with the server and with each other.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the second availability list through both apps</name>
  <files>apps/qr-menu/src/App.tsx, apps/website/app/page.tsx, apps/website/components/menu/menu-page-client.tsx, packages/ui/src/guest/menu-screen.tsx, packages/ui/src/guest/item-dialog.tsx</files>
  <read_first>
    - `apps/qr-menu/src/App.tsx` — the `stoppedItemIds` state (~line 72) and both `fetchAvailability(...).then(...)` call sites (~111-113 and ~164-166)
    - `apps/website/app/page.tsx` line ~47 and `apps/website/components/menu/menu-page-client.tsx` — the server-side fetch and the prop hand-off
    - `packages/ui/src/guest/menu-screen.tsx` — `MenuScreenProps` (~34), the `stopped` `useMemo` (~92), the `dialogGroups` filter (~141) and the `ItemDialog` mount (~313)
    - `packages/ui/src/guest/item-dialog.tsx` — `ItemDialogProps` and its pass-through to `ItemDetail`
    - `packages/ui/src/guest/menu-screen.tsx` lines 34-35 — `stoppedItemIds` is a REQUIRED prop today. The second list is added the same way, which breaks every existing `<MenuScreen>` mount in both apps' test trees; task 4 owns those and this task must not soften the prop to avoid them
  </read_first>
  <action>
Add `stoppedIngredientIds: readonly string[]` to `MenuScreenProps` and `ItemDialogProps`, and add
`modifierOptions: readonly MenuModifierOptionDto[]` to `ItemDialogProps` and `ItemDetailProps` —
the guest now resolves a group's options by id out of the menu's top-level array instead of reading
them inline off the group (ING-09).

In `menu-screen.tsx`, keep `dialogGroups` filtering the menu's groups by
`dialogItem.modifierGroupIds`, and pass `menu.modifierOptions` and `stoppedIngredientIds` straight
through to `ItemDialog`. Do not build a second `Set` for ingredients at this level — the item sheet
is where a stopped ingredient is rendered, not the item grid.

In `apps/qr-menu/src/App.tsx` add a `stoppedIngredientIds` state beside `stoppedItemIds` and set it
in BOTH `fetchAvailability(...).then(...)` callbacks; pass it into `MenuScreen`. In
`apps/website/app/page.tsx` extend the `.catch(() => ({ stoppedItemIds: [], stoppedIngredientIds: [] }))`
fallback and pass the value through `MenuPageClient` into `MenuScreen`.

Add `stoppedIngredientIds` as a REQUIRED prop, exactly as `stoppedItemIds` is required today. Do not
make it optional to spare the existing mounts: an optional second availability list is how one guest
surface silently ships with no stopped-ingredient state at all. Nine existing spec files stop
compiling the moment this and task 1 land — that is expected, and task 4 closes it. Until then the
gate for this task is "no surviving qr-menu or website type error names a `src/`, `app/` or
`components/` file", not "the project typechecks".
  </action>
  <verify>
    <automated>pnpm exec nx run qr-menu:typecheck 2&gt;&amp;1 | grep "error TS" | grep -v "test/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0 — every surviving qr-menu type error names a file under `test/`, and task 4 owns all of them
    - `pnpm exec nx run website:typecheck 2>&1 | grep "error TS" | grep -v "test/" | wc -l` also prints 0
    - `grep -c "stoppedIngredientIds" apps/qr-menu/src/App.tsx` returns at least 3 (state, and both fetch callbacks)
    - `grep -n "stoppedIngredientIds" apps/website/app/page.tsx apps/website/components/menu/menu-page-client.tsx packages/ui/src/guest/menu-screen.tsx packages/ui/src/guest/item-dialog.tsx` returns a match in every one of the four files
    - `stoppedIngredientIds` is declared without `?` on both `MenuScreenProps` and `ItemDialogProps`
  </acceptance_criteria>
  <done>Both guest apps carry the stopped-ingredient list and the top-level option array down to the item sheet.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the selection hook around behaviour, and add exclusion state</name>
  <files>packages/ui/src/guest/use-item-selection.ts, packages/ui/src/guest/item-detail.tsx, packages/ui/src/guest/cart-line-row.tsx</files>
  <read_first>
    - `packages/ui/src/guest/use-item-selection.ts` — the whole file (118 lines); `isSingleChoiceGroup` (8-9), `preselected` (24-33) and its three call sites (44, 50, 115), `requiredCount` (35-36), `toggleOption`'s cap (103), `chosenModifiers` (57-74)
    - `packages/ui/src/guest/item-detail.tsx` — `ItemDetailProps` (15-25), `handleAdd` (45-58), and the `singleChoice` call site (~127)
    - `packages/ui/src/guest/cart-line-row.tsx` lines 1-30 — the line-total loop and the `details` join of modifier names
    - `10.6-UI-SPEC.md` "useItemSelection" and "Composition line" sections
    - `10.6-CONTEXT.md` D-05, D-06, D-07, D-16, D-18, D-34
  </read_first>
  <action>
Rewrite `use-item-selection.ts`:
- Delete `isSingleChoiceGroup` entirely and replace every use with `group.behaviour === 'one'` read
  off the DTO (D-07). Export nothing in its place.
- Delete `preselected` and all three of its call sites; the initial state is an empty selection
  (D-05 — there is no default-in in this phase, and `defaultAmount` is not read anywhere in the
  guest any more).
- Delete the `current.size >= group.maxSelectable` cap in `toggleOption` — `several` is uncapped
  (D-33 removed the number from the model).
- Simplify `requiredCount` to `group.isRequired ? 1 : 0`.
- Take a fourth argument: an options lookup (`ReadonlyMap<string, MenuModifierOptionDto>` built by
  the caller from the menu's `modifierOptions`), and read each chosen option's `name` and
  `priceDelta` from it instead of from the group's deleted inline `options` array. `amount` stays
  hardcoded `1` (D-06 — it already is).
- Add exclusion state: `excludedOptionIds: ReadonlySet<string>`, `toggleExclusion(optionId)`, and a
  derived `excludedModifiers: readonly CartModifier[]` built from the item's `compositionLines`
  where `removable` is true and the id is excluded, each entry carrying
  `kind: 'excluded'`, `priceDelta: '0'`, `amount: 1` and the ingredient's localized name from the
  lookup. Reset the exclusion set in the same effect that resets the selection when the item
  changes.

In `item-detail.tsx`, build the options lookup once with `useMemo` from the new `modifierOptions`
prop, pass it to the hook, and change `handleAdd` to send
`modifiers: [...selection.chosenModifiers, ...selection.excludedModifiers]`. UI-SPEC describes the
excluded set as its own array and D-34 requires one collection on the wire — both hold: the hook
exposes two arrays, the cart line carries one, discriminated by `kind`. Nothing is ever written into
a comment field (D-18). Update the `singleChoice` call site to `group.behaviour === 'one'` so the
package compiles; the render branches themselves are plan 10's.

In `cart-line-row.tsx`, render an excluded modifier's name with a leading MINUS SIGN character in
the joined details string so the guest does not read "onion" as if it had been added. This is a
typographic marker, not new copy — do not add an i18n key that UI-SPEC's copy contract does not
list.
  </action>
  <verify>
    <automated>pnpm exec nx run ui:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isSingleChoiceGroup\|preselected\|defaultAmount\|maxSelectable\|minSelectable" packages/ui/src/guest/use-item-selection.ts` returns zero matches
    - `grep -n "amount: 1" packages/ui/src/guest/use-item-selection.ts` shows every emitted modifier carrying amount 1
    - `grep -n "kind: 'excluded'" packages/ui/src/guest/use-item-selection.ts` returns one match, with `priceDelta: '0'` on the same object literal
    - `grep -n "excludedModifiers" packages/ui/src/guest/item-detail.tsx` shows them merged into the single `modifiers` array passed to `onAddToCart`
    - `grep -rn "comment" packages/ui/src/guest/item-detail.tsx` shows no exclusion text being written into any comment field
    - `pnpm exec nx run ui:typecheck` exits 0 — `packages/ui`'s tsconfig does not include the apps' test trees, so this one CAN be green here
    - `pnpm exec nx run qr-menu:typecheck 2>&1 | grep "error TS" | grep -v "test/" | wc -l` prints 0, and the same command for `website:typecheck` prints 0. Do NOT assert either project exits 0 at this point: their spec fixtures are still red by design and task 4 is the closure point
  </acceptance_criteria>
  <done>Selection is keyed on the group's own behaviour, nothing is preselected, and an exclusion travels to the cart as its own row.</done>
</task>

<task type="auto">
  <name>Task 4: The nine existing spec fixtures that build these DTOs</name>
  <files>apps/website/test/menu-render.spec.tsx, apps/qr-menu/test/quick-add.spec.tsx, apps/qr-menu/test/menu-screen.spec.tsx, apps/qr-menu/test/menu-finder.spec.tsx, apps/qr-menu/test/nutrition-info.spec.tsx, apps/qr-menu/test/theme.spec.tsx, apps/qr-menu/test/table.spec.tsx, apps/qr-menu/test/menu-refresh.spec.tsx, apps/qr-menu/test/drawer.spec.tsx</files>
  <read_first>
    - `apps/qr-menu/tsconfig.json` — `include` carries `test/**/*.ts` and `test/**/*.tsx`, and `qr-menu:typecheck` runs `tsc -p tsconfig.json`; every fixture below is therefore a hard type error, not a test-only concern. `apps/website/tsconfig.json` includes `**/*.tsx` for the same reason
    - `apps/qr-menu/test/menu-screen.spec.tsx` — `item()` returning `MenuItemDto` (line 7), `buildMenu()` returning `MenuDto` (31), the `renderMenu` helper's `<MenuScreen>` mount (56)
    - `apps/qr-menu/test/menu-finder.spec.tsx` — the `menu` const is `as unknown as MenuDto` (line 42) so the fixture itself is fine; the break is the `<MenuScreen menu stoppedItemIds>` mount at line 47
    - `apps/qr-menu/test/nutrition-info.spec.tsx` — `dish()` annotated `MenuItemDto` (line 6); it mounts `NutritionInfo`, never `MenuScreen`
    - `apps/qr-menu/test/theme.spec.tsx` (line 7), `apps/qr-menu/test/table.spec.tsx` (line 10), `apps/qr-menu/test/menu-refresh.spec.tsx` (line 6) — each a `const menu: MenuDto` literal with one item; each also mocks `fetchAvailability` to resolve `{ stoppedItemIds: [] }` (theme 58, table 58-60/84, menu-refresh 62)
    - `apps/qr-menu/test/drawer.spec.tsx` line 26 — the same `fetchAvailability` mock; its menu fixture is cast, so this file typechecks either way
    - `apps/qr-menu/test/quick-add.spec.tsx` — the whole file (199 lines); `item()` at 6, and the `ItemDetail choices` describe block at 97-178 with its two mounts at 128 and 163
    - `apps/website/test/menu-render.spec.tsx` — its fixture builder
  </read_first>
  <action>
Every one of these files constructs a DTO task 1 reshaped or mounts a component task 2 gave a new
required prop. Fix the fixtures; do not weaken a type or a prop to avoid the work.

The mechanical part, in seven files: add `modifierOptions: []` to every literal annotated `MenuDto`,
and `extraOptionIds: []`, `compositionMode: 'text'`, `composition: []`, `compositionLines: []` to
every literal annotated `MenuItemDto` or built by a helper whose return type is one. That covers
`menu-screen.spec.tsx` (`item()` and `buildMenu()`), `nutrition-info.spec.tsx` (`dish()`),
`theme.spec.tsx`, `table.spec.tsx` and `menu-refresh.spec.tsx` (one `const menu: MenuDto` each,
one item each), and `menu-render.spec.tsx`'s builder. `menu-finder.spec.tsx` needs no fixture change
at all — its menu is cast through `unknown`.

Three component mounts in the test tree now miss a required prop. The two `<MenuScreen>` mounts —
`menu-screen.spec.tsx:56` and `menu-finder.spec.tsx:47` — must both pass `stoppedIngredientIds`;
give `menu-screen.spec.tsx`'s `renderMenu` helper a third parameter defaulting to `[]` so its
existing call sites keep working unchanged, and pass `[]` inline in `menu-finder.spec.tsx`. The
third is `menu-render.spec.tsx:71`, which mounts `<MenuPageClient menu stoppedItemIds footerLinks>`;
task 2 gave `MenuPageClientProps` the same required second list, so add it there too, again through
that file's own `renderMenu` helper default.

Four files mock `fetchAvailability` to resolve `{ stoppedItemIds: [] }`: add
`stoppedIngredientIds: []` to each resolved value, in `theme.spec.tsx`, `menu-refresh.spec.tsx`,
`drawer.spec.tsx`, and in `table.spec.tsx` both in the `fetchAvailabilityMock` type parameter and in
its `mockResolvedValue`. Without it `App.tsx` pushes `undefined` into the state task 2 added and the
prop's type is a lie at runtime — `drawer.spec.tsx` typechecks clean without this edit, which is
exactly why it is named here explicitly.

`quick-add.spec.tsx` needs judgement, not just fields. Add the four new item fields to `item()`.
Both `ItemDetail` mounts need the `modifierOptions` prop task 2 added: lift the options out of each
group's inline `options` array into a standalone array passed as `modifierOptions`, and describe the
group with `display`, `behaviour: 'one'`, `isRequired: true` and `optionIds` instead of
`minSelectable`/`maxSelectable`/`options`. Then DELETE the
`opens with the option the kitchen would use anyway` case (lines 98-139) outright. It asserts that
`getByRole('radio', { name: 'Традиционное' })` is checked and that Add is enabled on open — which is
exactly the preselection D-05 removes. With nothing preselected the radio is unchecked and a
required group leaves Add disabled, so both assertions invert; the case tested a behaviour that no
longer exists. Do not preserve preselection to keep it green, and do not rewrite it into a
"nothing is preselected" test here — plan 10 owns the new guest behaviour specs. KEEP the
`will not let a required question go unanswered` case: disabled, click, enabled still holds exactly,
and it is the only guard that a required group cannot be skipped.

That single deletion is the only assertion change in this task. Every other assertion in all nine
files stays exactly as it is — none of the other qr-menu specs carries a modifier group with
options, so none of them encodes preselection or a numeric cap, and their edits are fixture fields
only.
  </action>
  <verify>
    <automated>pnpm exec nx run qr-menu:typecheck &amp;&amp; pnpm exec nx run website:typecheck &amp;&amp; pnpm -C apps/qr-menu exec vitest run &amp;&amp; pnpm -C apps/website exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec nx run qr-menu:typecheck` and `pnpm exec nx run website:typecheck` BOTH exit 0 — this task closes the red window tasks 1-3 opened
    - `pnpm -C apps/qr-menu exec vitest run` passes, all nine spec files included
    - `pnpm -C apps/website exec vitest run` passes
    - `grep -rn "minSelectable\|maxSelectable\|defaultAmount" apps/qr-menu/test apps/website/test` returns zero matches
    - `grep -c "stoppedIngredientIds" apps/qr-menu/test/theme.spec.tsx apps/qr-menu/test/table.spec.tsx apps/qr-menu/test/menu-refresh.spec.tsx apps/qr-menu/test/drawer.spec.tsx` returns at least 1 for each of the four
    - `grep -n "the kitchen would use anyway" apps/qr-menu/test/quick-add.spec.tsx` returns zero matches, and `grep -n "required question go unanswered" apps/qr-menu/test/quick-add.spec.tsx` returns one
    - `grep -n "stoppedIngredientIds?" packages/ui/src/guest/menu-screen.tsx packages/ui/src/guest/item-dialog.tsx` returns zero matches — no prop was made optional to shortcut this task
  </acceptance_criteria>
  <done>Every existing guest spec compiles and passes against the new payload, and the only assertion removed is the one that tested preselection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload to guest render | the menu is untrusted-ish input to the browser: ids may not resolve, arrays may be empty |
| guest cart to `POST /v1/orders` | everything built here is re-validated server-side; the client is a convenience, never an authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-09-01 | Tampering | a guest editing local state to send a free or forged modifier | mitigate | plan 04 re-prices from the published snapshot and re-checks the union set and `removableOptionIds`; nothing in this plan is trusted server-side |
| T-10.6-09-02 | Denial of Service | an `optionId` in a group or composition that is absent from `modifierOptions` crashing the sheet | mitigate | the lookup is a Map and every read is guarded — an unresolved id renders nothing rather than throwing |
| T-10.6-09-03 | Information Disclosure | wire-type drift silently widening a field to `any` and leaking a raw S3 key into the DOM | mitigate | the three files are changed together and each field is checked against `public-menu.controller.ts`; the payload has no key field to leak |
| T-10.6-09-SC | Tampering | npm/pip/cargo installs | mitigate | no package is installed by this plan |
</threat_model>

<verification>
- `pnpm exec nx run api-client:typecheck`, `cart:typecheck`, `ui:typecheck`, `qr-menu:typecheck`, `website:typecheck` all exit 0 AFTER task 4 — tasks 1-3 deliberately leave the two app projects red on their test trees only
- `pnpm -C apps/website exec vitest run` passes
- `pnpm -C apps/qr-menu exec vitest run` passes, all nine touched spec files included
</verification>

<success_criteria>
- The three hand-written wire-type files and the cart type describe the new payload identically
- Both guest apps carry `stoppedIngredientIds`
- The hook has no numeric ceiling, no preselection and no default amount, and exposes exclusion state
- An exclusion reaches the cart as a `kind: 'excluded'` modifier with price zero
- Every existing guest spec compiles and passes against the new payload, with `stoppedIngredientIds` required rather than softened
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-09-SUMMARY.md` when done
</output>
