---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 13
type: execute
wave: 9
depends_on: ["10.6-11"]
files_modified:
  - apps/admin/src/components/menu/modifier-group-form.tsx
  - apps/admin/src/components/menu/group-ingredients-picker.tsx
  - apps/admin/src/components/menu/group-editor-shell.tsx
  - apps/admin/src/components/menu/modifier-options-list.tsx
  - apps/admin/test/components/menu/modifier-group-form.spec.tsx
autonomous: true
requirements: [ING-03, ING-05]

must_haves:
  truths:
    - "The operator sets a group's display and behaviour with two radio pairs and a required switch, and never types a number (D-07, D-33, ING-05)"
    - "A group holds an ordered set of library ingredients, added from the shared picker and reordered by drag (D-02, ING-03)"
    - "One ingredient can sit in several groups; the group screen never edits an ingredient's identity (D-01, D-03)"
  artifacts:
    - path: "apps/admin/src/components/menu/modifier-group-form.tsx"
      provides: "LocalizedField name + two RadioGroups + a Switch, no numeric inputs"
      contains: "RadioGroup"
    - path: "apps/admin/src/components/menu/group-ingredients-picker.tsx"
      provides: "the ordered membership list with dnd-kit reorder and remove"
  key_links:
    - from: "apps/admin/src/components/menu/group-editor-shell.tsx"
      to: "group-ingredients-picker.tsx"
      via: "replaces the deleted modifier-options-list mount"
      pattern: "GroupIngredientsPicker"
    - from: "apps/admin/src/components/menu/group-ingredients-picker.tsx"
      to: "setGroupIngredients"
      via: "one ordered write of the whole membership"
      pattern: "setGroupIngredients"
---

<objective>
Turn the group editor from "a place where options are created" into "a place where a group's shape
is set and library ingredients are arranged".

Purpose: D-01 and D-02 move identity to the ingredient and behaviour to the group. The screen that
used to blur the two is rewritten here.
Output: a group form with two radio pairs and a switch, and an ordered membership picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-UI-SPEC.md
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@.claude/skills/admin-ui/SKILL.md
@CLAUDE.md

Read before starting (targeted):
- `10.6-PATTERNS.md` sections "modifier-group-form.tsx", "item-detail-form.tsx" (for the dnd-kit skeleton), "zod-schemas.ts"
- `10.6-RESEARCH.md` section 5 — `ModifierGroupFormSchema` never had an `isRequired` control; this is genuinely new UI, not migrated UI
</context>

<known_broken_after_this_plan>
`apps/admin` does not typecheck as a whole while this plan runs. Plan 08 regenerated
`components['schemas']` and plan 11 rewrote both form schemas; plans 12, 13 and 14 run in PARALLEL in
this wave and each close a different slice of the resulting red.

| Still red after this plan | Owned and closed by |
|---|---|
| `routes/(protected)/menu/modifier-groups.*` and the `main.tsx` route tree | plan 12, this wave |
| `item-modifier-groups-card.tsx`, `item-detail-form.tsx`, `item-editor-shell.tsx` — the `ingredients` field and the composition move | plan 14, this wave |
| `stop-list` route and table, `order-detail-sheet.tsx` | plan 15, next wave |

Your gate is the narrow `grep -c` in each task, matching only the files this plan owns
(`modifier-group-form.tsx`, `group-ingredients-picker.tsx`, `group-editor-shell.tsx`). **Plan 15
carries the whole-app `admin:typecheck` gate** — no wave-9 plan can, because the three of us are red
on each other until all three land. Never silence a red line in a file this plan does not own by
re-adding `minSelectable`, `maxSelectable` or an inline option array.

One cross-plan note: this plan DELETES `modifier-options-list.tsx` in task 3. Plan 12 runs in
parallel and has been retargeted at `localized-field.tsx` for its `Tabs` idiom, so it no longer
needs this file — delete it as planned.
</known_broken_after_this_plan>

<design_authority>
`10.6-UI-SPEC.md` item 5 is this plan's form layout, and its Focal Points table says the behaviour
radio group is the anchor (it decides how the guest interacts) while Required is one toggle among
several with no size or colour escalation. Implement that; do not redesign it.
</design_authority>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the group form around display, behaviour and required</name>
  <files>apps/admin/src/components/menu/modifier-group-form.tsx, apps/admin/test/components/menu/modifier-group-form.spec.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/modifier-group-form.tsx` — the whole file (172 lines); keep the `useForm` + `zodResolver` + `useMutation` + `onSubmit` scaffolding (lines 30-96) untouched EXCEPT for the post-create `navigate` at lines 75-80, which the action below retargets, and replace only the `FieldGroup` body (97-169), in particular the `grid-cols-[1fr_8rem_8rem]` name/min/max row at line 98
    - `apps/admin/src/main.tsx` lines 44-52 — where plan 12, in this same wave, replaces the two `modifier-groups` route registrations with `menuIngredientsRoute` / `menuIngredientDetailRoute` at `/menu/ingredients` and `/menu/ingredients/$id`. `main.tsx` line 130 declares TanStack Router's `interface Register`, so every `navigate({ to })` in the app is type-checked against that route tree
    - `apps/admin/src/components/ui/radio-group.tsx` — plan 11's generated primitive; there is no existing `RadioGroup` usage in this repo to copy, so its usage inside a `FormField` render follows the same `field`/`fieldState` destructuring the deleted numeric fields used
    - `apps/admin/src/components/menu/stop-list-table.tsx` lines 108-116 — the `Switch` `checked`/`onCheckedChange` binding to copy for Required
    - `apps/admin/src/lib/menu/zod-schemas.ts` — plan 11's rewritten `ModifierGroupFormSchema`
    - `apps/admin/test/components/orders/order-detail-sheet.spec.tsx` — the testing-library harness for the new spec
  </read_first>
  <action>
Replace the form body with: the existing `LocalizedField` name field full width and unchanged; then
a `grid grid-cols-2` row holding two horizontal `RadioGroup`s — `menu.groups.displayLabel` with
`displayTiles`/`displayTabs`, and `menu.groups.behaviourLabel` with
`behaviourOne`/`behaviourSeveral`; then a `Switch` row bound to `isRequired` labelled
`menu.groups.requiredLabel`.

Delete the numeric min/max inputs outright. Do not keep them hidden, disabled, or behind a flag —
the columns no longer exist and D-02's principle read symmetrically means nothing is kept "for
later".

One explicit exception to "keep lines 30-96 untouched": the post-create `navigate` at lines 75-80
targets `'/menu/modifier-groups/$id'`, a route file plan 12 DELETES in this same wave. Retarget it
to `'/menu/ingredients/$id'`, keeping `params: { id: savedId }` and `replace: true` exactly as they
are. Because `main.tsx` registers the router type, leaving it is not a dead link that shows up in a
browser later — it is a type error that keeps `admin:typecheck` red for the whole app, and plan 15
carries that gate. Nothing else in the `onSubmit` block changes.

Add `apps/admin/test/components/menu/modifier-group-form.spec.tsx` asserting: the form renders no
`input[type=number]`; choosing Tabs + One and submitting calls the mutation with
`display: 'tabs', behaviour: 'one'`; and toggling Required submits `isRequired: true`.
  </action>
  <verify>
    <automated>pnpm -C apps/admin exec vitest run test/components/menu/modifier-group-form.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C apps/admin exec vitest run test/components/menu/modifier-group-form.spec.tsx` passes with three cases
    - `grep -n "type=\"number\"\|minSelectable\|maxSelectable" apps/admin/src/components/menu/modifier-group-form.tsx` returns zero matches
    - `grep -c "RadioGroup" apps/admin/src/components/menu/modifier-group-form.tsx` returns at least 2
    - `grep -n "modifier-groups/\$id" apps/admin/src/components/menu/modifier-group-form.tsx` returns zero matches and `grep -n "ingredients/\$id" apps/admin/src/components/menu/modifier-group-form.tsx` returns one
    - `pnpm exec nx run admin:typecheck 2>&1 | grep "error TS" | grep "modifier-group-form.tsx"` returns nothing, OR its only line is the router complaining that `'/menu/ingredients/$id'` is not a known route id — plan 12 registers that route in this same wave and closes it, and plan 15 in wave 10 carries the whole-app gate. Any OTHER error on this file is this plan's to fix. Do not revert the navigate to the deleted route to make the line disappear
    - The Required control is a `Switch`, not a third radio pair, and carries no size or colour escalation over the two radio groups
    - Every label comes from the `menu.groups.*` keys plan 11 added; no literal string in the JSX
  </acceptance_criteria>
  <done>A group's shape is two explicit choices plus a switch, and the operator can never type a number again.</done>
</task>

<task type="auto">
  <name>Task 2: The ordered membership picker</name>
  <files>apps/admin/src/components/menu/group-ingredients-picker.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/categories-table.tsx` — the `@dnd-kit` machinery to copy verbatim: imports (5-22), `useSortable` (91-93), `arrayMove` (191/340), `DndContext` + `SortableContext` wiring (392-402)
    - `apps/admin/src/components/menu/ingredient-picker-sheet.tsx` — plan 11's shared picker, mounted here with `showPrice: true`
    - `apps/admin/src/lib/queries/catalog.ts` — plan 11's `setGroupIngredients(groupId, optionIds)`
    - `10.6-CONTEXT.md` D-02, D-03, ING-03
  </read_first>
  <action>
Create `apps/admin/src/components/menu/group-ingredients-picker.tsx`: an ordered list of the
group's ingredients, each row showing the photo thumbnail, the name and the price (read-only — the
price belongs to the ingredient and is edited on the Ingredients tab; D-03 means one bacon is one
price everywhere), a drag handle, and a remove button.

Reorder with the same `@dnd-kit` skeleton `categories-table.tsx` uses — `DndContext` +
`SortableContext` + `useSortable` + `CSS.Transform.toString(transform)` — but simpler: this is a
flat list, so `arrayMove` on the id array is the whole reorder, with no nesting maths.

Adding opens the shared `IngredientPickerSheet` with `showPrice: true` and no `disabledIds` — an
ingredient may sit in several groups, and that is the point of the phase (D-03).

Every mutation writes the WHOLE ordered id array through `setGroupIngredients` — membership is one
write, so the order the operator arranged is what the guest sees. Do not attempt a per-row
add/remove endpoint; there is none.

The row carries no behaviour control of any kind: no default-in, no how-many-times, no per-group
price. Those live on the ingredient or nowhere (D-01, D-02, D-05, D-06).
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck 2&gt;&amp;1 | grep "error TS" | grep -c "group-ingredients-picker.tsx" || true</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0
    - `grep -n "setGroupIngredients" apps/admin/src/components/menu/group-ingredients-picker.tsx` shows the whole ordered array being sent, not a single id
    - `grep -n "DndContext\|SortableContext\|useSortable" apps/admin/src/components/menu/group-ingredients-picker.tsx` returns all three
    - `grep -n "defaultAmount\|freeAmount\|maxAmount\|priceDelta.*input\|Input" apps/admin/src/components/menu/group-ingredients-picker.tsx` shows no editable field on a row
    - The picker is mounted with `disabledIds` empty or absent
  </acceptance_criteria>
  <done>A group holds an ordered set of library ingredients and nothing about them is edited from here.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the group editor shell and delete the options list</name>
  <files>apps/admin/src/components/menu/group-editor-shell.tsx, apps/admin/src/components/menu/modifier-options-list.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/group-editor-shell.tsx` — the whole file (112 lines); `emptyValues`/`valuesFromGroup` still build `minSelectable`/`maxSelectable`, and it mounts `ModifierOptionsList`
    - `apps/admin/src/components/menu/modifier-options-list.tsx` — the whole file (287 lines), the component being deleted; check nothing outside the group editor imports it before removing
    - `apps/admin/src/lib/queries/catalog.ts` — `ModifierGroupDetailApi`, whose shape changed with plan 08's regeneration
  </read_first>
  <action>
Update `emptyValues()` and `valuesFromGroup()` to the new form shape —
`{ name, display: 'tiles', behaviour: 'several', isRequired: false }` for a new group, and the
group's own three fields when editing. Replace the `ModifierOptionsList` mount with
`GroupIngredientsPicker`, passing the group id and its current ordered options.

Then delete `apps/admin/src/components/menu/modifier-options-list.tsx`. Options are no longer
created or edited inside a group (D-01, D-02) — this component is superseded, not extended. Before
deleting, grep for other importers; if one exists outside the group editor, report it in the SUMMARY
rather than leaving a dangling import.

The card that held the options list keeps its heading and its position; only its body changes.
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck 2&gt;&amp;1 | grep "error TS" | grep -c "group-editor-shell.tsx\|modifier-group-form.tsx\|group-ingredients-picker.tsx" || true</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0
    - `apps/admin/src/components/menu/modifier-options-list.tsx` no longer exists
    - `grep -rn "modifier-options-list" apps/admin/src/` returns zero matches
    - `grep -n "minSelectable\|maxSelectable" apps/admin/src/components/menu/group-editor-shell.tsx` returns zero matches
    - A new group's default form values are tiles + several + not required
  </acceptance_criteria>
  <done>The group editor sets a shape and arranges ingredients; the option-editing screen is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser to the membership write | the whole ordered id array is client-supplied; the server re-resolves every id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-13-01 | Tampering | a membership write naming another tenant's ingredient | mitigate | plan 06's `replaceGroupModifierOptions` re-resolves every id under the bound tenant and the composite FK refuses it at the database |
| T-10.6-13-02 | Elevation of Privilege | a read-only operator submitting the form | mitigate | the API refuses without `menu:update`; the UI gate is convenience only |
| T-10.6-13-03 | Tampering | a stale `display`/`behaviour` pair reaching the server as a number | mitigate | both are `z.enum` client-side and server-side, and the columns carry CHECK constraints |
| T-10.6-13-SC | Tampering | npm/pip/cargo installs | mitigate | no package is installed; `@dnd-kit` is already an admin dependency used for category reorder |
</threat_model>

<verification>
- `pnpm -C apps/admin exec vitest run test/components/menu/modifier-group-form.spec.tsx` passes
- `pnpm exec nx run admin:typecheck 2>&1 | grep "error TS" | grep -c "modifier-group-form\|group-editor-shell\|group-ingredients-picker"` returns 0 — with the one exception task 1 names: if plan 12 has not yet landed in this wave, the router does not know `/menu/ingredients/$id` and that single error is expected. Reverting the navigate to silence it is not a fix. Plan 15 carries the whole-app gate.
- `grep -rn "modifier-options-list" apps/admin/src/` returns nothing
- `apps/admin` as a whole is still red until plans 12 and 14 land in this same wave and plan 15 lands in the next; do NOT assert a whole-app typecheck here
</verification>

<success_criteria>
- No numeric selection input survives anywhere in the admin
- A group's members are an ordered list drawn from the library
- The superseded options list is deleted, not left behind
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-13-SUMMARY.md` when done
</output>
