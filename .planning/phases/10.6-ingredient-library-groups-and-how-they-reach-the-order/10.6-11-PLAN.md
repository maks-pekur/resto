---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 11
type: execute
wave: 8
depends_on: ["10.6-08"]
files_modified:
  - apps/admin/src/components/ui/radio-group.tsx
  - apps/admin/src/lib/i18n/messages/ru.json
  - apps/admin/src/lib/i18n/messages/en.json
  - apps/admin/src/lib/i18n/messages/es.json
  - apps/admin/src/components/layout/app-sidebar.tsx
  - apps/admin/src/lib/queries/catalog.ts
  - apps/admin/src/lib/menu/zod-schemas.ts
  - apps/admin/src/components/menu/ingredient-picker-sheet.tsx
autonomous: true
requirements: [ING-02, ING-03, ING-04, ING-05, ING-08, ING-13]

must_haves:
  truths:
    - "Every admin string this phase needs exists in all three catalogs, and the Menu nav entry reads Ингредиенты and points at the new route (D-26, ING-02)"
    - "Every admin data call for ingredients, membership, composition and ingredient stops exists as a typed query or mutation derived from the generated OpenAPI schemas (admin-ui skill rule 6)"
    - "The group form schema is display + behaviour + required, and the item form schema carries a composition mode and both payloads (D-07, D-14, ING-05, ING-13)"
    - "One shared ingredient picker serves both the dish's singles row and the composition editor (admin-ui skill rule 1)"
  artifacts:
    - path: "apps/admin/src/lib/queries/catalog.ts"
      provides: "ingredientsQuery, ingredientUsageQuery, upsertIngredient, archiveIngredient, setGroupIngredients, setItemIngredients, setItemComposition, ingredientStopListQuery, toggleIngredientStopList"
      contains: "ingredientsQuery"
    - path: "apps/admin/src/lib/menu/zod-schemas.ts"
      provides: "IngredientFormSchema, rewritten ModifierGroupFormSchema, composition fields on ItemEditorFormSchema"
    - path: "apps/admin/src/components/menu/ingredient-picker-sheet.tsx"
      provides: "the searchable library picker both the item card and the composition editor mount"
  key_links:
    - from: "apps/admin/src/lib/queries/catalog.ts"
      to: "components['schemas']"
      via: "type derivation, never a hand-written response interface"
      pattern: "Schemas\\["
    - from: "apps/admin/src/components/layout/app-sidebar.tsx"
      to: "/menu/ingredients"
      via: "url change on the existing menuModifiers entry, key unchanged"
      pattern: "/menu/ingredients"
---

<objective>
Lay the admin foundation the three screen plans build on: the missing shadcn primitive, every copy
string, every typed data call, the two form schemas, and the shared library picker.

Purpose: plans 12, 13 and 14 run in parallel in the next wave. Everything they share lands here so
none of them has to invent it twice.
Output: no screen changes yet — primitives, queries, schemas and copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-UI-SPEC.md
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@.claude/skills/admin-ui/SKILL.md
@CLAUDE.md

Read before starting (targeted):
- `10.6-PATTERNS.md` sections "apps/admin/src/lib/queries/catalog.ts", "zod-schemas.ts", "app-sidebar.tsx"
- `10.6-RESEARCH.md` section 5 "TanStack Query convention" and "i18n"
</context>

<known_broken_after_this_plan>
`apps/admin` does not typecheck between plan 08 and plan 14. Plan 08 regenerated
`components['schemas']`, so `ModifierGroupApi` lost its numeric fields and every component reading
them is red; this plan rewrites the two form schemas, which reddens the same components further.
Plans 12, 13 and 14 close it, screen by screen. Your gate is the narrow one in each task, not
`admin:typecheck`. Never re-add a field to a schema to silence a component another plan owns.
</known_broken_after_this_plan>

<tasks>

<task type="auto">
  <name>Task 1: The radio-group primitive, every copy string, and the nav entry</name>
  <files>apps/admin/src/components/ui/radio-group.tsx, apps/admin/src/lib/i18n/messages/ru.json, apps/admin/src/lib/i18n/messages/en.json, apps/admin/src/lib/i18n/messages/es.json, apps/admin/src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - `10.6-UI-SPEC.md` "Copywriting Contract" — the admin key table is the source of truth; every key and its ru/en value is written out there
    - `apps/admin/src/lib/i18n/messages/ru.json` — the existing `menu.modifiers`, `menu.modifierGroups`, `menu.stopList` and `menu.editor` blocks the new keys sit beside
    - `apps/admin/src/components/layout/app-sidebar.tsx` line ~102 — the `menuModifiers` entry
    - `.claude/skills/admin-ui/SKILL.md` rules 3 and 4 — generated primitives are never hand-edited, every string goes through i18next in all three catalogs
  </read_first>
  <action>
Run `npx shadcn add radio-group` from `apps/admin`. The file lands in
`apps/admin/src/components/ui/radio-group.tsx` and is generated output — do not hand-edit it, do
not move it. It is an official-registry block, so no registry vetting gate applies.

Add every key from UI-SPEC's admin Copywriting Contract to all three catalogs — `menu.ingredients.*`
(18 keys), `menu.groups.*` (7 keys), `menu.itemIngredients.*` (4 keys), `menu.editor.composition*`
(5 keys), `menu.stopList.*` (5 keys) and `orders.detail.excludedLabel`. ru and en values are given
verbatim in the contract; write es the way the neighbouring es entries in the same file already
read. A key present in two catalogs and missing from the third falls back to the raw key string in
production and fails no build — so add all three together.

Apply the contract's three renames exactly: delete `menu.editor.ingredientsLabel` (the composition
field carries no label of its own now — the mode radio group and the card title establish context),
rename `menu.editor.ingredientsPlaceholder` to `menu.editor.compositionTextPlaceholder` keeping its
value, and rename `menu.editor.ingredientsHint` to `menu.editor.compositionTextHint`. Do not leave
the old keys behind beside the new ones.

In `app-sidebar.tsx`, change ONLY the `url` on the `menuModifiers` entry to `/menu/ingredients`.
The translation KEY stays `menuModifiers` — its VALUE becomes "Ингредиенты"/"Ingredients" in the
catalogs. Renaming the key would break the call site for no reason.
  </action>
  <verify>
    <automated>node -e "const a=require('./apps/admin/src/lib/i18n/messages/ru.json'),b=require('./apps/admin/src/lib/i18n/messages/en.json'),c=require('./apps/admin/src/lib/i18n/messages/es.json');const s=[a,b,c].map(x=&gt;JSON.stringify(x));for(const k of ['tabIngredients','displayLabel','behaviourLabel','requiredLabel','compositionModeAssembled','stopIngredientDialogTitle','excludedLabel']){for(const [i,j] of s.entries()){if(!j.includes(k))throw new Error('catalog '+i+' missing '+k)}}console.log('copy ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/components/ui/radio-group.tsx` exists and is unmodified CLI output
    - The verify command prints `copy ok`
    - `grep -c "ingredientsLabel\|ingredientsPlaceholder\|ingredientsHint" apps/admin/src/lib/i18n/messages/ru.json` returns 0 — the three old keys are gone, not duplicated
    - `grep -n "menu/ingredients" apps/admin/src/components/layout/app-sidebar.tsx` returns one match and `grep -c "menuModifiers" apps/admin/src/components/layout/app-sidebar.tsx` is unchanged
    - The ru value for `menuModifiers` reads "Ингредиенты"
  </acceptance_criteria>
  <done>The primitive exists, every string this phase needs is in all three catalogs, and the nav points at the new route.</done>
</task>

<task type="auto">
  <name>Task 2: Typed queries, mutations and the two form schemas</name>
  <files>apps/admin/src/lib/queries/catalog.ts, apps/admin/src/lib/menu/zod-schemas.ts</files>
  <read_first>
    - `apps/admin/src/lib/queries/catalog.ts` lines 1-60 (the `components['schemas']` derivation and the comment explaining the past drift incident), 130-140 (`modifierGroupsQuery`/`modifierGroupQuery`) and 229-296 (`upsertModifierGroup`, `upsertItemModifierGroups`, `toggleStopList`) — the exact shapes to copy
    - `apps/admin/src/lib/menu/zod-schemas.ts` lines 45-110 — `ItemEditorFormSchema`'s `ingredients` field, `ModifierGroupFormSchema` and its localized-refine factory, `ModifierOptionFormSchema`
    - `packages/api-client/src/generated/api.ts` — plan 08's regenerated output; the new operation and schema names to derive from
    - `.claude/skills/admin-ui/SKILL.md` rules 6 and 7 — derive response types from the OpenAPI file, money is a string end to end
  </read_first>
  <action>
In `apps/admin/src/lib/queries/catalog.ts` add, copying the existing query/mutation shapes verbatim
and deriving every response type from `components['schemas']` (never a hand-written interface —
the file's own comment records the incident that rule came from):

- `ingredientsQuery()` on key `['catalog','ingredients']` → `GET /v1/catalog/modifier-options`
- `ingredientUsageQuery(id)` on key `['catalog','ingredient-usage', id]` → `GET /v1/catalog/modifier-options/:id/usage`
- `upsertIngredient(input)` → `POST /v1/catalog/modifier-options`
- `archiveIngredient(id)` → `PATCH /v1/catalog/modifier-options/:id/archive`
- `setGroupIngredients(groupId, optionIds)` → `PUT /v1/catalog/modifier-groups/:id/options`
- `setItemIngredients(itemId, optionIds)` → `PUT /v1/catalog/items/:id/modifier-options`
- `setItemComposition(itemId, payload)` → `PUT /v1/catalog/items/:id/composition`
- `ingredientStopListQuery(locationId)` on key `['catalog','ingredient-stop-list', locationId]` → `GET /v1/catalog/stop-list/options`
- `toggleIngredientStopList(optionId, stopped, locationId)` → `POST` / `DELETE /v1/catalog/stop-list/options`

Every call goes through the existing `apiFetch` wrapper — never a hand-rolled `fetch`. Every
mutation invalidates by prefix the way the existing ones do, and the membership and composition
mutations additionally invalidate `['catalog','item', itemId]` so the item editor re-reads.

In `apps/admin/src/lib/menu/zod-schemas.ts`:
- Rewrite `ModifierGroupFormSchema`: drop `minSelectable`, `maxSelectable` AND the `.refine()` that
  compared them; add `display: z.enum(['tiles','tabs'])`, `behaviour: z.enum(['one','several'])`,
  `isRequired: z.boolean()`. Keep the `modifierGroupFormSchema(defaultLocale)` factory and its
  name-required refine exactly as it is.
- Add `IngredientFormSchema` following the same three-part shape: `name` via
  `LocalizedTextFormSchema`, `description: z.string().max(140).nullable()` (140 is UI-SPEC's cap so
  the line stays one `line-clamp-1` on a tile), `priceDelta: z.number().min(0)`,
  `imageS3Key: z.string().nullable()`, plus an `ingredientFormSchema(defaultLocale)` factory adding
  the name-required refine.
- On `ItemEditorFormSchema`: rename `ingredients` to `compositionText` keeping its
  `z.array(z.string().min(1).max(100)).max(50)` bound, and add
  `compositionMode: z.enum(['text','assembled'])` and
  `compositionAssembled: z.array(z.object({ optionId: z.string().uuid(), removable: z.boolean() })).max(50)`.
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck 2&gt;&amp;1 | grep "error TS" | grep -c "lib/queries/catalog.ts\|lib/menu/zod-schemas.ts" || true</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0 — neither of the two files this task owns reports an error (components under `components/menu` are expected to be red until plans 12-14)
    - `grep -n "interface .*Response\|type .*Response = {" apps/admin/src/lib/queries/catalog.ts` returns zero hand-written response shapes among the additions
    - `grep -c "apiFetch" apps/admin/src/lib/queries/catalog.ts` increased by at least 9
    - `grep -n "minSelectable\|maxSelectable" apps/admin/src/lib/menu/zod-schemas.ts` returns zero matches
    - `grep -n "compositionMode\|compositionAssembled\|compositionText" apps/admin/src/lib/menu/zod-schemas.ts` returns all three
    - `IngredientFormSchema`'s description field carries `.max(140)`
  </acceptance_criteria>
  <done>Every data call the Ingredients screen needs exists and is typed from the contract, and both form schemas describe the new model.</done>
</task>

<task type="auto">
  <name>Task 3: The shared ingredient picker sheet</name>
  <files>apps/admin/src/components/menu/ingredient-picker-sheet.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/item-modifier-groups-card.tsx` lines 200-270 — the existing add-affordance and chip-row idiom this picker is opened from
    - `apps/admin/src/components/menu/category-form.tsx` — the `Sheet` with an explicit Cancel/confirm footer, the pattern to copy
    - `10.6-UI-SPEC.md` "Component Inventory" items 6 and 7 — the picker searches (unlike the main grid) because it is a lookup, not a management screen; the composition editor mounts the same picker name-only
    - `.claude/skills/admin-ui/SKILL.md` rule 1 and 2 — look for an existing component first, split the reusable half out
  </read_first>
  <action>
Create `apps/admin/src/components/menu/ingredient-picker-sheet.tsx`: a `Sheet` listing the tenant's
ingredients from `ingredientsQuery()`, with a search `Input` filtering client-side on the localized
name (the library is tens of rows — D-27's reasoning means no server-side search is needed), each
row showing the photo thumbnail, the name and, when the caller asks for it, the price.

Props: `open`, `onOpenChange`, `onPick(optionId)`, `showPrice: boolean`, and
`disabledIds: ReadonlySet<string>` with an optional `disabledReason(optionId): string` used as the
row's tooltip. The composition editor mounts it with `showPrice: false` — a composition carries no
cost, ever (D-15). The dish's singles row mounts it with `showPrice: true` and passes the ids
already reachable through the dish's assigned groups as `disabledIds`, with the
`menu.itemIngredients.duplicateError` string as the reason, so the second attachment is refused in
the UI before the server has to answer 409 (D-04).

Every string goes through `useTranslation` with a `keyPrefix`, matching how every sibling component
in `components/menu` does it. No literal user-facing text in JSX.
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck 2&gt;&amp;1 | grep "error TS" | grep -c "ingredient-picker-sheet.tsx" || true</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0
    - The component takes `showPrice` and never renders a price when it is false
    - `disabledIds` renders the row disabled AND surfaces `disabledReason` as a tooltip
    - `grep -n "useTranslation" apps/admin/src/components/menu/ingredient-picker-sheet.tsx` returns one match with a `keyPrefix`
    - No literal user-facing string appears in the JSX
    - The file sits in `components/menu/`, matching the admin-ui skill's folder rule
  </acceptance_criteria>
  <done>One picker exists, and both screens that need it in the next wave can mount it without duplicating a list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin SPA to `/v1/catalog/*` | the browser is convenience only; the route guard and the API's `@Permissions` are what actually refuse |
| generated contract to admin types | a hand-written response shape drifts silently; the contract is the only source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-11-01 | Elevation of Privilege | an operator without `menu:update` seeing and using ingredient controls | mitigate | visibility follows the existing `hasPermission(me,'menu','read')` sidebar gate; the refusal itself is server-side (D-29, plan 08's RBAC e2e) |
| T-10.6-11-02 | Tampering | a hand-written response type widening a field and hiding a contract break | mitigate | every new type derives from `components['schemas']`; the acceptance criteria assert no hand-written response interface was added |
| T-10.6-11-03 | Denial of Service | an unbounded description reaching the API | mitigate | `IngredientFormSchema` caps it at 140 client-side and plan 05's DTO caps it server-side |
| T-10.6-11-SC | Tampering | third-party registry component | mitigate | `radio-group` is an official shadcn registry block copied into the repo as source, not an npm dependency; UI-SPEC's Registry Safety table records the gate as not required |
</threat_model>

<verification>
- `pnpm exec nx run admin:typecheck 2>&1 | grep "error TS" | grep -c "lib/queries/catalog.ts\|lib/menu/zod-schemas.ts\|ingredient-picker-sheet.tsx"` returns 0
- All three admin catalogs carry every new key
- `apps/admin` as a whole is still red — plans 12, 13 and 14 close it
</verification>

<success_criteria>
- `radio-group` exists as unmodified CLI output
- Every phase string is in ru, en and es, with the three renames applied and no old key left
- Nine typed queries/mutations exist, all derived from the generated contract
- Both form schemas describe display/behaviour and the two-mode composition
- One shared picker serves both consumers
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-11-SUMMARY.md` when done
</output>
