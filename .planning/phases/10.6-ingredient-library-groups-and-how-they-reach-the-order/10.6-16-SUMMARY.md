---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 16
subsystem: testing
tags: [vitest, nx, drizzle, openapi, e2e, eslint, comment-hygiene]

requires:
  - phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
    provides: plans 01-15, the whole ingredient library feature
provides:
  - The phase's own verification evidence (typecheck, lint, contract, db, api, front-end suites)
  - A forbidden-pattern and dropped-concept sweep against the phase's real starting point
  - A per-requirement coverage table for all 13 in-scope ING requirements
  - Six comment-hygiene fixes (five over-length WHY blocks trimmed, one redundant test-body
    comment deleted)
affects: [10.7, any-future-phase-reading-this-phase-for-context]

tech-stack:
  added: []
  patterns:
    - "Long-lived stacked phase branch: git merge-base main HEAD is stale (predates phases
      10.3-10.5), so diff-based gates need the true phase start commit, not literal main."

key-files:
  created: []
  modified:
    - apps/admin/test/item-editor-null-fields.spec.tsx
    - apps/api/src/contexts/catalog/application/dto.ts
    - apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts
    - apps/api/test/e2e/catalog.e2e.spec.ts
    - packages/db/test/integration/composite-tenant-fk.spec.ts
    - packages/ui/src/guest/item-detail.tsx
    - .planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/deferred-items.md

key-decisions:
  - "Plan's literal BASE formula (git merge-base main HEAD = 2a3958db) predates phases 10.3-10.5
    in this stacked-branch repo (main hasn't moved past 2a3958db). Used literally where the plan's
    acceptance criteria demand it, but the true phase-10.6 start (3ec7a969) was used as a
    cross-check for every diff-based gate and for the comment-hygiene review, since the literal
    BASE produces false positives for work done in earlier, unrelated phases."
  - "order-lifecycle.e2e.spec.ts (8/8 failing) is a pre-existing defect inherited from a stacked
    prior phase's status/payment_status split, not caused by 10.6. Documented in deferred-items.md,
    not fixed — outside every 10.6 plan's files_modified and outside this task's scope boundary."

requirements-completed: []

duration: TBD — plan paused at Task 3 (blocking human-verify checkpoint)
completed: TBD
---

# Phase 10.6 Plan 16: Prove the phase — automated sweep, forbidden-pattern sweep, browser walkthrough (PARTIAL)

**Tasks 1-2 complete with one pre-existing (non-10.6) test failure documented; Task 3 (the founder's
browser walkthrough) has not run — this SUMMARY will be appended to after sign-off.**

## Status

- [x] Task 1: The automated sweep
- [x] Task 2: The forbidden-pattern and coverage sweep
- [ ] Task 3: Walk both surfaces in a browser — **blocking human-verify checkpoint, not executed**

## BASE commit(s) used

The plan's task 1 defines `BASE=$(git merge-base main HEAD)`. Computed value:
**`2a3958dbfb893dee47798f484c781a0c61bebc6d`** ("chore: merge fix-doc-drift", 2026-08-29).

This repository carries long-lived stacked phase branches and `main` has not advanced since that
commit — so this BASE actually predates phases 10.3, 10.4 and 10.5 as well as 10.6 (331 commits,
495 files touched since then). Two of the plan's own "six guest files already import lucide-react
at the base commit" (`nutrition-info.tsx`, `theme-toggle.tsx`) do not exist yet at this BASE —
they were created by an earlier stacked phase. Diffing against it for the lucide/comment-hygiene
gates would attribute other phases' work to 10.6.

For that reason, every diff-based gate below is reported against **both** commits:
- **Literal BASE** (plan's formula): `2a3958dbfb893dee47798f484c781a0c61bebc6d`
- **True phase-10.6 start** (supplied by the orchestrator for exactly this ambiguity, confirmed by
  commit-log inspection to be the actual point at which 10.6's own commits begin):
  `3ec7a96950643c99efe1193db2a475f7893e90e4`

## Task 1: The automated sweep

All commands run with `NO_COLOR=1 FORCE_COLOR=0`, output inspected for the tool's own success/fail
markers (not a bare `grep error`), per the plan's verification-integrity requirement.

### Typecheck

`pnpm exec nx run-many -t typecheck --skip-nx-cache` (forced fresh, no cache) — **12/12 projects
succeeded**: config-tailwind, api-client, domain, events, cart, seed-cli, qr-menu, website, db, ui,
admin, api. Every project's output line was `tsc -p tsconfig.json --noEmit` followed by
`NX Successfully ran target typecheck for 12 projects` — confirmed as a real (non-cached) run.

### Lint

`pnpm exec nx affected -t lint --base=2a3958dbfb893dee47798f484c781a0c61bebc6d` — **11/11 affected
projects succeeded**: admin, api, qr-menu, website, api-client, ui, cart, db, events, domain,
seed-cli. `NX Successfully ran target lint for 11 projects`.

### Contract

`pnpm openapi:check` — **exit 0**. Regenerated `docs/api/openapi.yaml` and
`packages/api-client/src/generated/api.ts`, diffed against the committed artefacts:
`openapi:check: artefacts are in sync.` `git status --short` after the run showed no changes —
confirms no drift.

### Database (packages/db)

| Spec | Result |
|---|---|
| `test/integration/tenant-isolation.spec.ts` | **51/51 passed** |
| `test/integration/composite-tenant-fk.spec.ts` | **8/8 passed** |
| `test/integration/erase-includes-ingredients.spec.ts` | **2/2 passed** — reported separately per the plan: `erases menu_option_stop_list, menu_item_modifier_options and menu_modifier_group_options with zero rows left, without a foreign-key violation` (row-count proof) AND `the erase function itself names all three new tables above the menu_modifier_options delete — closes the recurring omission class (migrations 0072/0074/0077)` (the `pg_get_functiondef` proof) — both passed independently. |

### API unit

| Spec | Result |
|---|---|
| `test/unit/catalog` (12 files) | **52/52 passed** |
| `test/unit/create-order.service.spec.ts` | **21/21 passed** |

### API e2e / integration (each run individually with `--poolOptions.forks.singleFork=false --fileParallelism=false`, no bare `apps/api` suite run)

| File | Result |
|---|---|
| `test/e2e/catalog.e2e.spec.ts` | **31/31 passed** |
| `test/e2e/catalog-rbac.e2e.spec.ts` | **10/10 passed** (T-10.6-16-01 RBAC re-check) |
| `test/e2e/catalog-tenant-isolation.e2e.spec.ts` | **6/6 passed** (T-10.6-16-02 re-check) |
| `test/e2e/catalog-tenant-read-isolation.e2e.spec.ts` | **3/3 passed** |
| `test/e2e/catalog-reads.e2e.spec.ts` | **14/14 passed** |
| `test/e2e/ingredient-stop.e2e.spec.ts` | **4/4 passed** |
| `test/e2e/order-lifecycle.e2e.spec.ts` | **0/8 passed — pre-existing failure, not caused by this phase (see below)** |
| `test/e2e/outbox-nats-decoupling.e2e.spec.ts` | **3/3 passed** |
| `test/integration/create-order-idempotency.spec.ts` | **4/4 passed** |

**`order-lifecycle.e2e.spec.ts` failure — confirmed pre-existing, not a 10.6 regression.** All 8
tests fail identically: `seedOrder`'s fixture always inserts `status: 'paid'`, which stopped being
a legal `orders.status` value once migration `0010_order_payment_status.sql` split the column into
a fulfilment-stage `status` and an independent `payment_status` — every insert now hits
`orders_status_chk` (SQLSTATE 23514).

Verified this predates 10.6 by inspecting the tree at the true phase-10.6 branch point
(`3ec7a96950643c99efe1193db2a475f7893e90e4`, before any 10.6 commit):
- `git show 3ec7a969...:apps/api/test/e2e/order-lifecycle.e2e.spec.ts` already contains the
  identical `status: 'paid'` literal at all 11 `seedOrder(...)` call sites.
- `git show 3ec7a969...:packages/db/migrations/0010_order_payment_status.sql` already exists with
  the same restrictive `orders_status_chk`.
- `git log -- apps/api/test/e2e/order-lifecycle.e2e.spec.ts` shows the only post-BASE commit
  touching the file is `1a88a409` (an unrelated `fulfillmentMode` → `orderType` rename); no
  10.6-prefixed commit ever touched it.

A full worktree re-run at the phase-start commit was attempted for extra confirmation but hit an
unrelated testcontainers/role-provisioning error (`role "resto_app" does not exist`) before
reaching this assertion — abandoned in favor of the byte-for-byte source comparison above, which is
conclusive on its own (identical fixture code + identical constraint = identical outcome).
Logged to `deferred-items.md` ("Plan 16 — order-lifecycle.e2e.spec.ts's seedOrder fixture inserts
an invalid status, unrelated to ingredients"); not fixed, per Task 2's own scope-boundary rule
(pre-existing failures in files outside every 10.6 plan's `files_modified`).

### Front ends

| Suite | Result |
|---|---|
| `apps/admin` | **260/260 passed** (49 files) |
| `apps/qr-menu` | **89/89 passed** (20 files) |
| `apps/website` | **65/65 passed** (9 files) |

### Task 1 acceptance criteria — assessed

- [x] `pnpm exec nx run-many -t typecheck` exits 0 for every project
- [x] `pnpm openapi:check` exits 0
- [x] `BASE` recorded as a concrete commit sha from `git merge-base main HEAD` (see "BASE commit(s)
      used" above — recorded together with the true-phase-start cross-check, given the stacked-branch
      caveat)
- [x] All three `packages/db` integration specs pass, `pg_get_functiondef` reported separately
- [ ] All nine named e2e/integration files pass individually — **8/9 pass; `order-lifecycle.e2e.spec.ts`
      fails, confirmed pre-existing and unrelated to 10.6 (evidence above)**
- [x] All three front-end suites pass
- [x] Every command's result recorded with evidence; the one failure attributed to a pre-10.6 commit
- [x] No command ran the whole `apps/api` test tree in one process

## Task 2: The forbidden-pattern and coverage sweep

### Dropped concepts (whole tree, excluding `.planning` and `node_modules`)

| Grep | Raw hits | After triage |
|---|---|---|
| `minSelectable\|maxSelectable` (ts/tsx/json/yaml) | 2 | **0 real** — both are `expect(... 'minSelectable' in call).toBe(false)` in `apps/api/test/unit/catalog/upsert-modifier-group.service.spec.ts`: a regression test *proving the field's absence*, not a resurrection of it. |
| `min_selectable\|max_selectable` (`packages/db/src`, `apps`) | 3 | **0 real** — all 3 are in `apps/api/dist/main.cjs`/`.map`, a stale local build artifact. `git check-ignore` confirms both paths are gitignored and `git ls-files` confirms neither is tracked. Not part of the shipped tree. |
| `defaultAmount` (`packages/ui`, `apps/qr-menu`, `apps/website`) | 0 | Clean. (Note: `defaultAmount` still exists as a column on `menu_modifier_options` — but that column predates 10.6 entirely, present verbatim in `0000_baseline.sql`, and this phase's job per D-05 was only to keep the guest surfaces from reading it for pre-selection, which they don't.) |
| `isSingleChoiceGroup\|preselected(` (`packages`, `apps`) | 0 in tracked source | The one raw hit was `apps/website/.next/server/chunks/.../*.js.map` — a Next.js build cache artifact, not tracked. |

### Forbidden patterns

**New `lucide-react` imports in guest surfaces.** Run against both BASE candidates because the
plan's own "six files already import lucide at the base commit" claim is only true at the true
phase-10.6 start, not at the literal `merge-base main HEAD`:

- Against literal BASE (`2a3958db`): 2 hits — `packages/ui/src/guest/nutrition-info.tsx` and
  `theme-toggle.tsx` both show as entirely new files (`+++ b/...`, `@@ -0,0 +1,N @@`). **False
  positives**: both files already existed with these exact imports at the true phase-10.6 start
  (`git show 3ec7a969...:packages/ui/src/guest/nutrition-info.tsx` / `theme-toggle.tsx` both
  contain the same import lines) — they were created by an earlier stacked phase (10.3-10.5), not
  10.6.
- Against the true phase-10.6 start (`3ec7a969`): **0 hits.** The substantive gate — this phase
  adds no new direct `lucide-react` import — is satisfied.

**`withoutTenant.allowlist.ts` / `apps/api/eslint.config.mjs` diff.** `git diff --stat` against
both BASE candidates: **empty against both.** No bypass added.

**`tx.delete\|tx.insert\|tx.select` outside sanctioned adapters.** One hit:
`apps/api/src/contexts/audit/application/record-audit.service.ts:50` (`tx.insert(schema.auditLog)`).
This is the explicitly sanctioned audit consumer named in CLAUDE.md's own invariant text ("banned
outside `*-drizzle.repository.ts` / `*-drizzle.reader.ts` adapters ... and the audit consumer").
Confirmed unchanged since BASE (`git diff 2a3958db -- .../record-audit.service.ts` is empty) — not
new. No other site found.

### Comment hygiene

Reviewed every `+` comment line in `git diff 3ec7a969...HEAD -- apps packages` (142 files touched
by 10.6 itself, vs. 495 under the literal-BASE diff which pulls in unrelated stacked-phase work —
used the true phase-10.6 start here so the review is actually scoped to this phase's own ~65
substantive files). `packages/api-client/src/generated/api.ts`'s JSDoc is 100% `openapi-typescript`
codegen output (confirmed in sync via Task 1's `openapi:check`), not hand-authored — excluded from
review, matching how the committed `openapi.yaml` itself isn't hand-edited.

Of the added `//`/`/**` comment lines found, six needed action:

| File | Before | Issue | Fix | Commit |
|---|---|---|---|---|
| `apps/admin/test/item-editor-null-fields.spec.tsx` | 5-line block above a module-level fixture | Over the ~2-line WHY cap | Trimmed to 2 lines, kept the `[...null]` crash citation | this task's commit |
| `apps/api/src/contexts/catalog/application/dto.ts` | 3-line block (TS2509 constraint) | Over cap | Trimmed to 2 lines | this task's commit |
| `apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts` | 3-line block (D-04 dedupe) | Over cap | Trimmed to 2 lines | this task's commit |
| `apps/api/test/e2e/catalog.e2e.spec.ts` | 2-line comment **inside a test body** | Forbidden — the enclosing `it('GET modifier-options exposes imageS3Key, and a price-only upsert preserves it (GAP 1 fix)', ...)` name already carries the intent | Deleted | this task's commit |
| `packages/db/test/integration/composite-tenant-fk.spec.ts` | 4-line block (`NOTE (10.6-01, D-01)`) | Over cap | Trimmed to 2 lines, kept the decision-id citation | this task's commit |
| `packages/ui/src/guest/item-detail.tsx` | 3-line block (tabs+one rationale) | Over cap | Trimmed to 2 lines | this task's commit |

Every other added comment inspected (≈35 blocks across `apps/admin/src/lib/queries/catalog.ts`,
`dto.ts`, `ports.ts`, `published-menu.ts`, `catalog-drizzle.repository.ts`,
`get-photo-upload-url.service.ts`, `set-item-composition.service.ts`,
`archive-modifier-option.service.ts`, `packages/db/src/schema/menu.ts`, `use-item-selection.ts`)
was ≤2 lines and cites a decision id, a ticket id, or a concrete non-obvious constraint (e.g. the
Postgres index-before-FK ordering requirement) — kept as-is. `pill-multi-choice.tsx`'s JSDoc block
(design rationale for a `packages/ui` public component) and `packages/domain/src/schema/menu-modifier.ts`'s
updated JSDoc (a public `packages/domain` export, correctly rewritten to describe the new
display/behaviour/isRequired shape instead of the dropped min/max) both fit the project's narrow
JSDoc exception for `packages/*` public API — kept as-is.

**Migration `0019_ingredient_library.sql` carries substantially more than 2-line comment blocks**
throughout (index-ordering rationale, RLS-pattern provenance, erasure-function reproduction
rationale — all citing D-numbers). Not edited: D-31 already grandfathers this file's header as a
"legitimate survivor," hand-written SQL migrations are not safely rewritten after being applied to
any environment, and the same tooling constraint that forced this file to be hand-written (no
`drizzle-kit generate` baseline — `.planning/todos/pending/drizzle-generate-is-broken.md`) argues
for treating its comments as historical record rather than application-source prose. Flagged here
for the founder's awareness, not corrected.

**Net comment delta for this cleanup:** 6 files, 6 blocks addressed (5 trimmed, 1 deleted) — the
raw diff for the cleanup commit is `+10/-20` lines (`git diff --stat`); re-ran every affected test
suite after the edits (admin `item-editor-null-fields.spec.tsx` 3/3, api e2e `catalog.e2e.spec.ts`
31/31, db `composite-tenant-fk.spec.ts` 8/8, qr-menu 89/89, plus a fresh `nx run-many -t typecheck`
across api/admin/db/ui/events — all green) and `prettier --check` on all six files.

### Requirement coverage (13 in-scope ING requirements)

| Req | Implementing file(s) | Proving test |
|---|---|---|
| ING-01 | `packages/db/src/schema/menu.ts` (`menuModifierOptions`, `sourceExternalId`) | `packages/db/test/integration/composite-tenant-fk.spec.ts` (I-2 probes), `apps/api/test/unit/catalog/upsert-modifier-group.service.spec.ts` |
| ING-02 | `apps/admin/src/routes/(protected)/menu/ingredients.tsx`, `ingredients.$id.tsx`, `components/menu/ingredient-card-grid.tsx`, `ingredient-form-sheet.tsx` | `apps/admin/test/components/menu/ingredient-card-grid.spec.tsx`, `ingredient-form-sheet.spec.tsx` |
| ING-03 | `packages/db/src/schema/menu.ts` (`menuModifierGroupOptions`), `apps/admin/src/components/menu/group-ingredients-picker.tsx` | `apps/api/test/unit/catalog/upsert-modifier-group.service.spec.ts` |
| ING-04 | `packages/db/src/schema/menu.ts` (`menuItemModifierOptions`), `apps/api/src/contexts/catalog/application/modifiers/set-item-modifier-options.service.ts` | `apps/api/test/unit/create-order.service.spec.ts` — `'accepts an ingredient attached directly to the dish and persists it with a null group id (ING-04)'` |
| ING-05 | `apps/api/src/contexts/catalog/application/modifiers/upsert-modifier-group.service.ts`, `packages/domain/src/schema/menu-modifier.ts` | `apps/api/test/unit/catalog/upsert-modifier-group.service.spec.ts` — asserts `minSelectable`/`maxSelectable` absent from the payload |
| ING-08 | `apps/api/src/contexts/catalog/application/availability/option-stop-list.service.ts`, `get-option-stop-list.service.ts` | `apps/api/test/e2e/ingredient-stop.e2e.spec.ts` (4/4, Task 1) |
| ING-09 | `apps/api/src/contexts/catalog/domain/published-menu.ts` (top-level `modifierOptions[]`), `apps/api/src/contexts/catalog/application/publishing/get-published-menu.service.ts` | `apps/api/test/e2e/catalog.e2e.spec.ts` — `'GET modifier-options exposes imageS3Key, and a price-only upsert preserves it (GAP 1 fix)'` |
| ING-10 | `apps/api/src/contexts/ordering/application/create-order.service.ts` | `apps/api/test/unit/create-order.service.spec.ts` — `'rejects an option that belongs to neither the dish's groups nor its extraOptionIds'`, `'ignores any client-supplied unitPrice/discountSpec and uses catalog prices'` |
| ING-11 | `packages/ui/src/guest/item-detail.tsx`, `pill-multi-choice.tsx`, `segmented-choice.tsx` | **No automated component test found** — `apps/qr-menu/test/quick-add.spec.tsx` exercises `display`/`behaviour` fixtures only for the required-group-unanswered check, not tile/tab/placeholder rendering. This requirement's guest-rendering claims are proven only by the Task 3 browser walkthrough (pending). |
| ING-12 | `apps/api/src/contexts/catalog/application/items/set-item-composition.service.ts`, `apps/admin/src/components/menu/composition-editor.tsx` | `apps/admin/test/components/menu/composition-editor.spec.tsx` |
| ING-13 | `apps/api/src/contexts/catalog/application/dto.ts` (`SetItemCompositionInputSchema`), `set-item-composition.service.ts` | `apps/api/test/e2e/catalog.e2e.spec.ts` (`compositionMode: 'assembled'` round-trip, line ~1490) |
| ING-14 | `packages/ui/src/guest/item-detail.tsx` (removable line rendering) | **No automated component test found** — same gap as ING-11; proven only by the Task 3 browser walkthrough (pending). |
| ING-15 | `apps/api/src/contexts/ordering/application/create-order.service.ts` (`kind: 'excluded'`), `apps/admin/src/components/orders/order-detail-sheet.tsx:315` | `apps/api/test/unit/create-order.service.spec.ts` — `'accepts an excluded row for a removable ingredient and refuses it for a non-removable one (D-16/D-18/ING-15)'`; `apps/admin/test/components/orders/order-detail-sheet.spec.tsx` |

**Honest gap:** ING-11 and ING-14's guest-facing rendering behaviour (tile/tab display, photo
placeholder, description line, strikethrough interaction) has no automated component-level test —
only the API-contract shape (`display`/`behaviour`/`removable` fields) is unit/e2e-tested. The
actual rendering is real code (`item-detail.tsx`, `pill-multi-choice.tsx`, verified by direct
reading in this task) but its correctness as *experienced by a guest* rests entirely on Task 3's
browser walkthrough, which has not run yet.

### ING-06 / ING-07 — no hooks in shipped code

- `grep -rn "defaultAmount" packages/ui apps/qr-menu apps/website` — **0 hits.** No guest surface
  reads the pre-existing `defaultAmount` column for pre-selection.
- `grep -rln "stepper\|Stepper"` across `packages/ui/src`, `apps/qr-menu/src`, `apps/website` —
  **0 hits.** No stepper UI exists anywhere in the guest tree.
- `apps/api/src/contexts/ordering/application/create-order.service.ts:116` hardcodes `amount: 1`
  on every newly-created `order_modifiers` row (both `added` and `excluded` kinds) — the only
  place in this phase's own write path that sets `amount`, and it never varies.
- The `defaultAmount` *column* itself still exists on `menu_modifier_options` (confirmed present
  verbatim in `packages/db/migrations/0000_baseline.sql:492`, predating 10.6 entirely) — this is
  pre-existing legacy/iiko-readiness infrastructure this phase did not add and does not wire into
  any new behaviour, not a new hook.

### Task 2 acceptance criteria — assessed

- [ ] "The verify command prints 0" — **prints 2**, both confirmed false positives (test
      assertions of absence, see table above); the substantive gate (no live use of
      `minSelectable`/`maxSelectable`) is satisfied
- [x] No NEW `lucide-react` import added by this phase (confirmed against the true phase-10.6
      start; the literal-BASE diff shows 2 false positives from an earlier stacked phase, explained
      above)
- [x] `withoutTenant.allowlist.ts` / `eslint.config.mjs` diff empty against both BASE candidates
- [x] Every `git diff` in this task named a specific commit sha, never a bare `git diff` or `HEAD~1`
- [x] Every comment added by this phase either carries a WHY citing a decision/ticket id or has
      been deleted; net comment delta stated above
- [x] Per-requirement table naming file + test for all 13 in-scope ING ids (two honestly flagged as
      contract-tested-but-not-component-tested, pending Task 3)
- [x] ING-06 and ING-07 confirmed to have no hooks in the shipped code

## Task 3

**Not executed.** See the checkpoint returned to the orchestrator for the founder's exact browser
walkthrough steps, what to start, and what counts as pass/fail. This SUMMARY will be appended to
with the founder's sign-off (or the specific deviation reported) once Task 3 completes.

