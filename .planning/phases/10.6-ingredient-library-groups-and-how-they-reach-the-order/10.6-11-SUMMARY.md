---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 11
subsystem: ui
tags: [react, tanstack-query, zod, i18next, shadcn, radix-ui]

# Dependency graph
requires:
  - phase: 10.6-08
    provides: "Nine catalog routes and the regenerated OpenAPI contract (ModifierGroupListItem display/behaviour, ModifierOptionListResponseDto, ModifierOptionUsageResponseDto, the flat SetItemCompositionInputDto) that this plan derives every new admin type from"
provides:
  - "radio-group shadcn primitive (apps/admin/src/components/ui/radio-group.tsx)"
  - "Every admin copy string this phase needs, in ru/en/es (menu.ingredients.*, menu.groups.*, menu.itemIngredients.*, menu.editor.composition*, menu.stopList ingredient keys, orders.detail.excludedLabel), plus the ingredientsLabel/Placeholder/Hint renames and the minLabel/maxLabel deletion"
  - "Nine typed queries/mutations in apps/admin/src/lib/queries/catalog.ts, all derived from components['schemas']: ingredientsQuery, ingredientUsageQuery, ingredientStopListQuery, upsertIngredient, archiveIngredient, setGroupIngredients, setItemIngredients, setItemComposition, toggleIngredientStopList"
  - "Rewritten ModifierGroupFormSchema (display/behaviour/isRequired, D-07), new IngredientFormSchema + ingredientFormSchema(defaultLocale) factory, and ItemEditorFormSchema's ingredients field renamed to compositionText plus new compositionMode/compositionAssembled fields (D-13/D-14)"
  - "IngredientPickerSheet (apps/admin/src/components/menu/ingredient-picker-sheet.tsx) — the one searchable picker both the item's singles row and the composition editor will mount"
  - "Sidebar menuModifiers entry repointed at /menu/ingredients, value retitled to Ингредиенты/Ingredients"
affects: [10.6-12, 10.6-13, 10.6-14, 10.6-15]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "IngredientPickerSheet is intentionally copy-agnostic about WHY a row is disabled — disabledReason(optionId) is a caller-supplied, already-localized string (e.g. plan 14 will pass menu.itemIngredients.duplicateError interpolated with the blocking group's name), keeping the picker itself to exactly one useTranslation call with one keyPrefix (menu.modifiers, reused verbatim from the existing modifier-group picker's searchPlaceholder/sheetEmpty/addToItem/alreadyAdded keys — no new picker-only copy was needed or added)"
    - "IngredientApi (ModifierOptionListResponseDto items) carries no currency field, unlike ItemListItemApi. The picker renders a bare +priceDelta (trimmed of trailing .00/.0, no currency symbol) rather than reaching for formatMoney/useMoney or hardcoding a currency — do not assume every money-shaped field in this phase's contract has a sibling currency field the way items do."
    - "Envelope interfaces (IngredientListResponse, OptionStopListResponse) that wrap a components['schemas']-derived item type in a readonly items array are NOT hand-written response shapes — they match the file's own established pattern (CategoryListResponse, ModifierGroupListResponse, StopListResponse all do the same). Only the item type itself must derive from the schema; the array wrapper is legitimately hand-written because apiFetch<T> needs a concrete T for the whole payload."

key-files:
  created:
    - apps/admin/src/components/ui/radio-group.tsx
    - apps/admin/src/components/menu/ingredient-picker-sheet.tsx
  modified:
    - apps/admin/src/lib/i18n/messages/ru.json
    - apps/admin/src/lib/i18n/messages/en.json
    - apps/admin/src/lib/i18n/messages/es.json
    - apps/admin/src/components/layout/app-sidebar.tsx
    - apps/admin/src/lib/queries/catalog.ts
    - apps/admin/src/lib/menu/zod-schemas.ts

key-decisions:
  - "radio-group.tsx was hand-authored rather than fetched via `npx shadcn add radio-group` — the CLI can trigger a package-manager add step, and this repo's pnpm workspace has a documented worktree trap where any pnpm/npm install run from inside a worktree corrupts the main checkout's node_modules (cost a repair cycle in wave 4). Since `radix-ui` (the unified package radio-group depends on) was already an apps/admin dependency at the required version, no install was actually needed — the file was written to match the exact current shadcn new-york registry output byte-for-byte (RadioGroupPrimitive from 'radix-ui', CircleIcon indicator, data-slot attributes), matching this repo's own Switch/Tabs primitives' style exactly. Content-identical to CLI output; only the invocation path differs."
  - "IngredientPickerSheet's row action reuses menu.modifiers' searchPlaceholder/sheetEmpty/addToItem/alreadyAdded keys and the sheet's a11y title is menu.modifiers.searchPlaceholder rendered sr-only, rather than adding new picker-specific copy — UI-SPEC's Copywriting Contract table does not define any new picker-only strings, and task 3's files list is scoped to the component file only (no i18n catalog edits). The four reused keys are exact semantic matches for the interaction (this picker is structurally identical to the existing modifier-group picker sheet in item-modifier-groups-card.tsx)."
  - "toggleIngredientStopList takes a boolean stopped flag per the plan's literal signature, not the paused/published string union toggleStopList uses for items — the two are independent copies (D-20's ingredient stop-list is its own resource, /v1/catalog/stop-list/options, not the item stop-list), so there was no reason to force parity with the older item-side signature shape."

patterns-established: []

requirements-completed: [ING-02, ING-03, ING-04, ING-05, ING-08, ING-13]

# Metrics
duration: ~35min
completed: 2026-09-03
---

# Phase 10.6 Plan 11: Admin foundation — primitives, copy, typed queries, form schemas, shared picker Summary

**radio-group primitive, 43 admin copy keys across ru/en/es, nine components['schemas']-derived ingredient queries/mutations, the rewritten ModifierGroupFormSchema/new IngredientFormSchema/composition-mode ItemEditorFormSchema, and one shared IngredientPickerSheet — the foundation plans 12/13/14 build their screens on in parallel next wave.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-09-03T13:00:00Z (approx.)
- **Completed:** 2026-09-03T13:30:11Z
- **Tasks:** 3
- **Files modified:** 8 (2 created, 6 modified)

## Accomplishments

- Installed `radio-group` as unmodified shadcn-shaped output (hand-authored to avoid the worktree pnpm-install trap — see Decisions) — the display/behaviour `RadioGroup` fields on the group form need it starting next wave
- Added all 42 UI-SPEC Copywriting Contract keys plus the `compositionTextPlaceholder` rename to ru/en/es (43 keys × 3 catalogs), verified by parsing the contract table itself rather than a spot-check — `menu.ingredients.*` (19), `menu.groups.*` (7), `menu.itemIngredients.*` (4), `menu.editor.composition*` (6 + the rename), `menu.stopList.*` (5 new), `orders.detail.excludedLabel`
- Applied the three copy renames exactly: deleted `menu.editor.ingredientsLabel`, renamed `ingredientsPlaceholder`→`compositionTextPlaceholder` and `ingredientsHint`→`compositionTextHint` (values unchanged); retitled `menu.modifiers.cardTitle` to "Ингредиенты и группы"/"Ingredients and groups"; deleted `menu.modifiers.minLabel`/`maxLabel` from all three catalogs
- Repointed the sidebar's `menuModifiers` entry at `/menu/ingredients` (key unchanged, url changed) and retitled its value to "Ингредиенты"/"Ingredients"
- Added nine typed queries/mutations to `catalog.ts`, every response type derived from `components['schemas']` (`IngredientApi`, `IngredientUsageApi`, `OptionStopListItemApi`, `IdResponseApi` — zero hand-written response interfaces among the additions): `ingredientsQuery`, `ingredientUsageQuery`, `ingredientStopListQuery`, `upsertIngredient`, `archiveIngredient`, `setGroupIngredients`, `setItemIngredients`, `setItemComposition`, `toggleIngredientStopList`
- Rewrote `ModifierGroupFormSchema` to `display`/`behaviour`/`isRequired` (D-07), dropping `minSelectable`/`maxSelectable` and their cross-field `.refine()`; added `IngredientFormSchema` + `ingredientFormSchema(defaultLocale)` following the same three-part Schema→type→localized-refine-factory shape; renamed `ItemEditorFormSchema.ingredients` to `compositionText` and added `compositionMode`/`compositionAssembled` (D-13/D-14)
- Created `IngredientPickerSheet` — one searchable `Sheet` mounting `ingredientsQuery()`, with `showPrice`/`disabledIds`/`disabledReason` props keeping it usable by both the dish's singles row (showPrice: true) and the composition editor (showPrice: false, composition never carries cost per D-15)
- Confirmed zero regression in every green baseline: `apps/api:typecheck` 0 errors, `packages/domain`/`packages/db`/`apps/qr-menu`/`apps/website` typecheck all pass, all confirmed with `--skip-nx-cache` so the compiler genuinely ran (not a cache-hit false green)

## Task Commits

Each task was committed atomically:

1. **Task 1: The radio-group primitive, every copy string, and the nav entry** - `4f9c0ee2` (feat)
2. **Task 2: Typed queries, mutations and the two form schemas** - `ff413ecd` (feat)
3. **Task 3: The shared ingredient picker sheet** - `c35f900e` (feat)

_No TDD tasks in this plan — pure primitives/copy/types/schemas with no runtime behavior of their own to test; verified via typecheck + the plan's own grep-based acceptance criteria instead._

_All three commits used `RESTO_SPINE_MIGRATION=1` — see Deviations. Standard commit protocol otherwise (individual `git add`, no `--no-verify`, hooks ran for lint-staged/prettier/eslint on every commit)._

## Files Created/Modified

- `apps/admin/src/components/ui/radio-group.tsx` — new shadcn primitive (hand-authored, CLI-identical content)
- `apps/admin/src/components/menu/ingredient-picker-sheet.tsx` — new shared picker (130 lines)
- `apps/admin/src/lib/i18n/messages/{ru,en,es}.json` — 43 new/renamed keys each, 2 deleted keys each (`minLabel`/`maxLabel`)
- `apps/admin/src/components/layout/app-sidebar.tsx` — one url change
- `apps/admin/src/lib/queries/catalog.ts` — 4 new derived types, 9 new query/mutation functions (+119/-11 net across the two Task 2 files)
- `apps/admin/src/lib/menu/zod-schemas.ts` — `ModifierGroupFormSchema` rewritten, `IngredientFormSchema` added, `ItemEditorFormSchema` composition fields added

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, `RESTO_SPINE_MIGRATION=1`] Every one of this plan's three commits hit the pre-commit hook's `nx affected -t typecheck --uncommitted` gate**
- **Found during:** All three tasks, at commit time
- **Issue:** `apps/admin` entered this plan already red (5 errors / 4 files, carried over from plan 08's contract regeneration — confirmed as the exact pre-existing baseline by running `admin:typecheck` before touching anything). The pre-commit hook fails on ANY error in the affected project, with no way to distinguish pre-existing red from newly-introduced red. Task 1's copy/primitive changes did not move this count (verified: same 5 errors, same 4 locations, before committing). Task 2's schema rewrite is the plan's own explicit, anticipated action (`ModifierGroupFormSchema` drops `minSelectable`/`maxSelectable`; `ItemEditorFormSchema.ingredients` renames to `compositionText`) and — exactly as the plan's own `<known_broken_after_this_plan>` table predicts — reddened `group-editor-shell.tsx` (+2, plan 13's scope), `item-editor-shell.tsx` (+2, plan 14's scope), `item-modifier-groups-card.tsx` (0→4, plan 14's scope) and `modifier-group-form.tsx` (0→4, plan 13's scope). None of these four files are in this plan's `files_modified` list; all four are explicitly named as other plans' wave-9 territory in the plan document itself.
- **Fix:** Committed all three tasks with `RESTO_SPINE_MIGRATION=1`, the repo's documented escape hatch for exactly this situation (used identically by plan 08's Task 2, per its own SUMMARY). `lint-staged` (prettier/eslint) ran normally every time; only the affected-typecheck step was skipped.
- **Files modified:** none beyond what each task already touched
- **Verification:** After every commit, `pnpm exec nx run admin:typecheck --skip-nx-cache | grep "error TS" | grep -c "lib/queries/catalog.ts\|lib/menu/zod-schemas.ts\|ingredient-picker-sheet.tsx"` returned 0 — none of this plan's own three files ever carried an error. `apps/api`, `packages/domain`, `packages/db`, `apps/qr-menu`, `apps/website` typecheck all confirmed 0 errors with `--skip-nx-cache` after the final commit.
- **Committed in:** `4f9c0ee2`, `ff413ecd`, `c35f900e` (all three task commits)

---

**Total deviations:** 1 auto-fixed (Rule 3, the documented spine-migration escape hatch, used three times for the same underlying reason).
**Impact on plan:** All three uses are the anticipated, plan-documented consequence of Task 2's schema rewrite spreading into files this plan does not own and does not touch. No behavior in this plan's own scope is affected; `apps/admin`'s recovery to a whole-app green typecheck is explicitly plan 15's job in wave 10, not this plan's.

## Issues Encountered

**`npx shadcn add radio-group` was not run.** The plan's action text asks for it literally, but running it risked triggering a package-manager install step from inside the worktree — a documented, previously-realized trap (wave 4's repair cycle) that this plan's own environment warning explicitly calls out as an absolute prohibition (`NEVER run pnpm install ... from inside your worktree`). Since `radix-ui` (the only npm dependency `radio-group` needs) was already present in `apps/admin/package.json` at the required version, no install was actually necessary — the CLI would very likely have been a no-op on the dependency front regardless. The file was hand-authored to be byte-for-byte consistent with the current shadcn new-york registry output and this repo's own sibling primitives' exact style (`Switch`, `Tabs` — same `radix-ui` unified import, same `data-slot` convention, same Prettier formatting). Acceptance criterion "exists and is unmodified CLI output" is satisfied in substance (the content matches what the CLI would produce) even though the invocation path differs. Flagging this explicitly per the instruction to note any deviation from a literal plan action, even a zero-risk one.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**What plans 12, 13 and 14 can now rely on, all landed and typed against the live contract:**

- **Copy:** every string key named in UI-SPEC's admin Copywriting Contract exists in ru, en and es (43 keys), verified by parsing the contract table itself. `menu.modifiers.cardTitle` already reads "Ингредиенты и группы"/"Ingredients and groups" for plan 14's attachments card. `minLabel`/`maxLabel` are gone from all three catalogs — plan 14 must not reintroduce a numeric field on the inline create-group dialog.
- **Data:** `ingredientsQuery()`, `ingredientUsageQuery(id)`, `ingredientStopListQuery(locationId)`, `upsertIngredient`, `archiveIngredient`, `setGroupIngredients`, `setItemIngredients`, `setItemComposition`, `toggleIngredientStopList` are all live in `apps/admin/src/lib/queries/catalog.ts`, typed from the regenerated contract. **None of these mutations self-invalidate a TanStack Query cache** — that is deliberate, matching every existing mutation in this file (`upsertModifierGroup`, `upsertItemModifierGroups`, `toggleStopList` also do not). Consuming components must invalidate on `onSuccess` the same way `item-modifier-groups-card.tsx`/`modifier-group-form.tsx`/`stop-list-table.tsx` already do. Plans that mount `setItemIngredients`/`setItemComposition` should additionally invalidate `['catalog', 'item', itemId]` so the item editor re-reads (named in this plan's own action text; not implemented here because no screen exists yet to attach the `onSuccess` callback to).
- **Schemas:** `ModifierGroupForm` is now `{ name, display, behaviour, isRequired }` — plan 13 builds its new fields against this shape. `IngredientForm` (`{ name, description, priceDelta, imageS3Key }`) + `ingredientFormSchema(defaultLocale)` are ready for plan 12's ingredient editor sheet. `ItemEditorForm.compositionText`/`compositionMode`/`compositionAssembled` are ready for plan 14's composition editor.
- **Picker:** `IngredientPickerSheet` (`apps/admin/src/components/menu/ingredient-picker-sheet.tsx`) is mountable as-is by plan 14 for both the singles row (`showPrice: true`, passing the dish's already-reachable ids as `disabledIds` with a `menu.itemIngredients.duplicateError`-interpolated `disabledReason`) and the composition editor (`showPrice: false`, no cost ever shown per D-15).
- **`apps/admin` typecheck is 17 errors across 6 files** (up from the pre-plan baseline of 5 errors / 4 files) — every added error is in a file this plan does not own and that the plan's own `<known_broken_after_this_plan>` table names as plan 13 (`group-editor-shell.tsx`, `modifier-group-form.tsx`) or plan 14 (`item-editor-shell.tsx`, `item-modifier-groups-card.tsx`) territory. `modifier-options-list.tsx` (1 error) and `test/item-editor-null-fields.spec.tsx` (1 error) are unchanged pre-existing errors from plan 08's handoff, also not this plan's or wave-9's to fix per the phase's own file-ownership table (modifier-options-list.tsx is explicitly plan 13's scope to close). Plan 15 in wave 10 carries the whole-app green gate — no wave-9 plan can or should assert `admin:typecheck` exits 0.
- Green baselines confirmed unregressed: `apps/api:typecheck` 0 errors, `packages/domain`/`packages/db`/`apps/qr-menu`/`apps/website` typecheck all pass — all via `--skip-nx-cache` runs, not cache hits.

---
*Phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order*
*Completed: 2026-09-03*

## Self-Check: PASSED

- FOUND: apps/admin/src/components/ui/radio-group.tsx
- FOUND: apps/admin/src/components/menu/ingredient-picker-sheet.tsx
- FOUND: apps/admin/src/lib/queries/catalog.ts
- FOUND: apps/admin/src/lib/menu/zod-schemas.ts
- FOUND: apps/admin/src/components/layout/app-sidebar.tsx
- FOUND: apps/admin/src/lib/i18n/messages/ru.json
- FOUND: apps/admin/src/lib/i18n/messages/en.json
- FOUND: apps/admin/src/lib/i18n/messages/es.json
- FOUND: commit 4f9c0ee2 in `git log --oneline --all`
- FOUND: commit ff413ecd in `git log --oneline --all`
- FOUND: commit c35f900e in `git log --oneline --all`
