---
phase: 10.6-ingredient-library-groups-and-how-they-reach-the-order
plan: 14
type: execute
wave: 9
depends_on: ["10.6-11"]
files_modified:
  - apps/admin/src/components/menu/item-modifier-groups-card.tsx
  - apps/admin/src/components/menu/item-detail-form.tsx
  - apps/admin/src/components/menu/item-editor-shell.tsx
  - apps/admin/src/components/menu/composition-editor.tsx
  - apps/admin/test/components/menu/composition-editor.spec.tsx
autonomous: true
requirements: [ING-04, ING-12, ING-13, ING-14]

must_haves:
  truths:
    - "Composition has left the modifier card and lives in the item's description form, labelled Состав (D-13, ING-12)"
    - "The operator picks the composition mode per dish — free text, or an ordered list assembled from library ingredients (D-14, ING-13)"
    - "An assembled line carries exactly one property, removable or not, and never a gram or a price (D-15, D-16, ING-14)"
    - "A dish carries single ingredients beside its groups, and the picker refuses the second attachment naming where it already is (D-04, ING-04)"
  artifacts:
    - path: "apps/admin/src/components/menu/composition-editor.tsx"
      provides: "the mode toggle, the free-text input and the assembled ordered list"
      contains: "compositionMode"
    - path: "apps/admin/src/components/menu/item-modifier-groups-card.tsx"
      provides: "two chip rows — groups and single ingredients — and no composition field"
  key_links:
    - from: "apps/admin/src/components/menu/item-detail-form.tsx"
      to: "composition-editor.tsx"
      via: "the Состав section under the description"
      pattern: "CompositionEditor"
    - from: "apps/admin/src/components/menu/item-modifier-groups-card.tsx"
      to: "setItemIngredients"
      via: "the singles chip row writing the whole ordered id array"
      pattern: "setItemIngredients"
---

<objective>
Split the two meanings that share one card today: attachments stay on the modifier card and grow a
second chip row for single ingredients; composition moves to the item's description form and grows
a second authoring mode.

Purpose: D-13 says the tangle of "ingredients" meaning two things is half of why this phase exists.
Output: a dish that can carry single ingredients and an assembled, removable-flagged composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-UI-SPEC.md
@.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-CONTEXT.md
@.claude/skills/admin-ui/SKILL.md
@CLAUDE.md

Read before starting (targeted):
- `10.6-PATTERNS.md` sections "item-modifier-groups-card.tsx", "item-detail-form.tsx"
- `10.6-RESEARCH.md` section 5 — the exact current owner of the free-text field and what else that card holds
</context>

<design_authority>
`10.6-UI-SPEC.md` items 6 and 7 are this plan. The Copywriting Contract's rename table is binding:
`menu.editor.ingredientsLabel` is deleted (the composition field carries no label of its own),
`ingredientsPlaceholder` became `compositionTextPlaceholder` and `ingredientsHint` became
`compositionTextHint` — plan 11 already moved the catalogs, so use the new keys.
</design_authority>

<tasks>

<task type="auto">
  <name>Task 1: Move composition out of the modifier card and add the singles chip row</name>
  <files>apps/admin/src/components/menu/item-modifier-groups-card.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/item-modifier-groups-card.tsx` — the whole file (424 lines): the imports (1-30), the `commaListFromInput` helper (44-48), the `ingredientsText` state (65-69), the `ingredientsField` block (184-201), both of its render sites (210 and 285), and the assigned-group chip row with its `ChevronLeft`/`ChevronRight`/`X` button triple (211-260)
    - `apps/admin/src/components/menu/ingredient-picker-sheet.tsx` — plan 11's shared picker, mounted here with `showPrice: true` and a populated `disabledIds`
    - `apps/admin/src/lib/queries/catalog.ts` — plan 11's `setItemIngredients(itemId, optionIds)`
    - `10.6-CONTEXT.md` D-04, D-13
  </read_first>
  <action>
Delete the `ingredientsField` block, the `ingredientsText` state and the `commaListFromInput`
helper, and both render sites — the free text moves to the composition editor in task 2. Nothing in
this card writes `form.setValue('composition*', ...)` afterwards.

Keep the assigned-group chip row exactly as it is. Add a SECOND, visually identical chip row below
it for the dish's single ingredients, labelled `menu.itemIngredients.singlesRowLabel` (the group row
gets `groupsRowLabel`), reusing the same per-chip `ChevronLeft`/`ChevronRight`/`X` triple and the
same move/remove callback shape, writing the whole ordered id array through `setItemIngredients`.
Its add button is `menu.itemIngredients.addSingleBtn`, opening the shared `IngredientPickerSheet`
with `showPrice: true` and `disabledIds` set to every ingredient already reachable through the
dish's assigned groups, with `menu.itemIngredients.duplicateError` interpolated with the group's
name as the disabled reason (D-04 — the admin refuses the second attachment and names where it
already appears; the server answers 409 as the backstop).

Update the card title: it now describes attachments only, since composition has left. Use the
existing `menu.modifiers` card-title key if one exists; if not, add one to all three catalogs
following plan 11's key conventions and record it in the SUMMARY.

The inline create-group dialog this card holds must lose its numeric min/max inputs if it still has
them — group creation with the display/behaviour fields happens on the Groups tab (plan 13); keep
this dialog name-only, or remove it in favour of a link to the Groups tab, whichever leaves fewer
half-configured groups. State which you chose in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck 2&gt;&amp;1 | grep "error TS" | grep -c "item-modifier-groups-card.tsx" || true</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0
    - `grep -n "ingredientsField\|ingredientsText\|commaListFromInput\|ingredientsLabel\|ingredientsHint" apps/admin/src/components/menu/item-modifier-groups-card.tsx` returns zero matches
    - `grep -c "singlesRowLabel\|groupsRowLabel" apps/admin/src/components/menu/item-modifier-groups-card.tsx` returns at least 2
    - `grep -n "minSelectable\|maxSelectable\|type=\"number\"" apps/admin/src/components/menu/item-modifier-groups-card.tsx` returns zero matches
    - The picker is mounted with a non-empty `disabledIds` computed from the dish's assigned groups
    - `setItemIngredients` is called with the whole ordered array, never with a single id
  </acceptance_criteria>
  <done>The card carries two chip rows and no composition field, and the same ingredient cannot be attached to one dish twice.</done>
</task>

<task type="auto">
  <name>Task 2: The composition editor</name>
  <files>apps/admin/src/components/menu/composition-editor.tsx, apps/admin/test/components/menu/composition-editor.spec.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/categories-table.tsx` — the `@dnd-kit` skeleton (imports 5-22, `useSortable` 91-93, `arrayMove` 191/340, `DndContext`/`SortableContext` 392-402)
    - `apps/admin/src/components/menu/item-modifier-groups-card.tsx` lines 19-25 — the shadcn `Item`/`ItemGroup`/`ItemContent`/`ItemTitle` primitives already imported there, reused here for the assembled rows
    - `apps/admin/src/lib/menu/zod-schemas.ts` — plan 11's `compositionMode`, `compositionText`, `compositionAssembled` fields on `ItemEditorFormSchema`
    - `apps/admin/src/components/menu/ingredient-picker-sheet.tsx` — mounted here with `showPrice: false`
    - `10.6-UI-SPEC.md` Component Inventory item 7
  </read_first>
  <action>
Create `apps/admin/src/components/menu/composition-editor.tsx`, a form-bound section titled
"Состав" via the existing `compositionSectionTitle` key:

- A `RadioGroup` mode toggle bound to `compositionMode` with
  `menu.editor.compositionModeText` and `menu.editor.compositionModeAssembled`.
- Text mode renders the single-line `Input` moved out of the modifier card, bound to
  `compositionText` through the same comma-splitting behaviour it had, using
  `menu.editor.compositionTextPlaceholder` and `menu.editor.compositionTextHint`. No `FieldLabel` —
  the mode toggle and the section title already establish context (the old label key is deleted).
- Assembled mode renders an `ItemGroup` of `Item` rows bound to `compositionAssembled`, each row
  showing the ingredient's name ONLY — no price column, not ever, because a composition carries no
  cost (D-15) — plus a `Switch` labelled `menu.editor.compositionRemovableLabel` and a remove `X`
  whose `aria-label` is `menu.editor.compositionRemoveLineAriaLabel` interpolated with the name.
  Rows reorder by drag with the same `@dnd-kit` skeleton, `arrayMove` on the array being the whole
  reorder. `menu.editor.compositionAddLineBtn` opens the shared picker with `showPrice: false`.
- Both payloads stay in the form state; the mode field decides which one is live. Do not clear the
  other on a mode switch — the operator may switch back, and the server writes both columns anyway
  (plan 06's single-call composition write).

Add `apps/admin/test/components/menu/composition-editor.spec.tsx` asserting: switching to assembled
mode reveals the ordered list and hides the text input; a row renders a removable `Switch` and NO
price; and removing a row drops it from the submitted `compositionAssembled` array.
  </action>
  <verify>
    <automated>pnpm -C apps/admin exec vitest run test/components/menu/composition-editor.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C apps/admin exec vitest run test/components/menu/composition-editor.spec.tsx` passes with three cases
    - `grep -n "formatMoney\|price" apps/admin/src/components/menu/composition-editor.tsx` returns zero matches in the assembled row rendering
    - `grep -n "gram\|weight\|unit" apps/admin/src/components/menu/composition-editor.tsx` returns zero matches
    - The picker is mounted with `showPrice: false`
    - Switching modes does not clear the other payload (asserted by reading the change handler)
    - The remove button carries an interpolated `aria-label`, not a bare icon
  </acceptance_criteria>
  <done>A dish's composition is authored either way, and an assembled line carries exactly one property.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the editor and move the form defaults</name>
  <files>apps/admin/src/components/menu/item-detail-form.tsx, apps/admin/src/components/menu/item-editor-shell.tsx</files>
  <read_first>
    - `apps/admin/src/components/menu/item-detail-form.tsx` — where the description field sits; the composition section goes directly under it (D-13: composition belongs with the description, not with the attachments)
    - `apps/admin/src/components/menu/item-editor-shell.tsx` lines 25-55 — `ingredients: []` in the empty defaults (line 31) and `ingredients: [...(item.ingredients ?? [])]` in the from-item mapping (line 48)
    - `apps/admin/src/lib/queries/catalog.ts` — plan 11's `setItemComposition(itemId, payload)` and the item detail response shape after plan 08's regeneration
  </read_first>
  <action>
Mount `CompositionEditor` in `item-detail-form.tsx` immediately after the description field.

In `item-editor-shell.tsx`, replace the two `ingredients` mappings: empty defaults become
`compositionMode: 'text'`, `compositionText: []`, `compositionAssembled: []`; the from-item mapping
reads the item detail's `compositionMode`, `composition` and `compositionAssembled`. Wire the save
path so the composition reaches `setItemComposition` — whether that rides the existing item upsert
or a separate call depends on how this shell already batches its saves; follow whatever the sizes
and modifier-groups cards already do rather than inventing a third save shape, and say which in the
SUMMARY.
  </action>
  <verify>
    <automated>pnpm exec nx run admin:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec nx run admin:typecheck` exits 0 for the WHOLE admin app — this plan closes the admin red window opened by plan 08
    - `grep -n "ingredients" apps/admin/src/components/menu/item-editor-shell.tsx` returns zero matches
    - `CompositionEditor` is mounted in `item-detail-form.tsx` after the description field, not inside `item-modifier-groups-card.tsx`
    - `pnpm -C apps/admin exec vitest run` passes
  </acceptance_criteria>
  <done>Composition sits with the description, the item form carries all three fields, and the admin typechecks again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser to the composition write | line ids are client-supplied and re-resolved server-side |
| the duplicate check in the picker | a UI convenience; the authority is the server's 409 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.6-14-01 | Tampering | a composition line naming another tenant's ingredient | mitigate | plan 06's `setItemComposition` resolves every `optionId` under the bound tenant before writing the jsonb |
| T-10.6-14-02 | Tampering | bypassing the disabled picker row to attach a duplicate | mitigate | the server throws `MenuIngredientAlreadyAttachedError` and answers 409; the disabled row is convenience only (D-04) |
| T-10.6-14-03 | Denial of Service | a very long composition | mitigate | `.max(50)` on both payloads client-side and server-side |
| T-10.6-14-SC | Tampering | npm/pip/cargo installs | mitigate | no package is installed by this plan |
</threat_model>

<verification>
- `pnpm exec nx run admin:typecheck` exits 0
- `pnpm -C apps/admin exec vitest run` passes
- `grep -rn "ingredientsField\|ingredientsLabel" apps/admin/src/` returns nothing
</verification>

<success_criteria>
- The modifier card carries two chip rows and no composition field
- The composition editor offers both modes and never shows a price or a gram
- A single ingredient already reachable through a group cannot be attached again
- The admin typechecks end to end
</success_criteria>

<output>
Create `.planning/phases/10.6-ingredient-library-groups-and-how-they-reach-the-order/10.6-14-SUMMARY.md` when done
</output>
