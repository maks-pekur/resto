---
phase: 07.5-production-deploy
plan: 09
type: execute
wave: 3
depends_on:
  - 07.5-02
  - 07.5-08
files_modified:
  - .github/workflows/cd.yml
  - infra/runbooks/cd-pipeline.md
files_modified_note: "New cd.yml workflow (separate from ci.yml, which 07.5-02 owns — no file overlap) + a runbook. Depends on 07.5-02 (docker build job + bootable image) and 07.5-08 (ECS services to deploy into)."
autonomous: false
requirements: []
user_setup:
  - service: github-actions-aws
    why: "D-10 — CD deploys on merge to main; needs AWS OIDC/credentials + ECR push rights"
    dashboard_config:
      - task: "Add AWS deploy credentials (OIDC role preferred) as GitHub repo secrets; grant ECR push + ECS deploy + run-task on the migration task"
        location: "GitHub repo Settings -> Secrets, and AWS IAM"

must_haves:
  truths:
    - "On merge to main (after CI is green), the image builds to ECR, boots a smoke test, runs pnpm db:migrate as a GATED pre-rollout step, then deploys the ECS service only if the migration exited 0"
    - "A failed migration aborts the deploy — the old task definition keeps serving"
    - "Migrations run under the admin/migration role, never the runtime role"
  artifacts:
    - path: ".github/workflows/cd.yml"
      provides: "The deploy-on-merge pipeline: build -> ECR -> smoke -> gated migrate -> deploy -> verify"
      contains: "db:migrate"
    - path: "infra/runbooks/cd-pipeline.md"
      provides: "The CD flow, the migration-gating rationale, and the manual-rollback (app-only) procedure pointer"
      contains: "migrate"
  key_links:
    - from: ".github/workflows/cd.yml migrate step"
      to: "DATABASE_ADMIN_URL (Secrets Manager)"
      via: "ECS run-task executes pnpm db:migrate under the admin role, gating the deploy"
      pattern: "db:migrate"
    - from: ".github/workflows/cd.yml deploy step"
      to: "amazon-ecs-deploy-express-service"
      via: "deploy only after migrate exit 0"
      pattern: "ecs"
---

<objective>
Build the CD pipeline (D-10): on merge to main, after the existing nx-affected CI gate is green,
build the api image to ECR, boot-smoke it, run database migrations as a GATED pre-rollout step
(pnpm db:migrate under the admin role), and only then deploy the ECS service via
amazon-ecs-deploy-express-service — aborting the whole deploy if the migration fails so the old
task definition keeps serving. This is success criterion 3 (CD on merge to main; migrations as a
pre-rollout step).

Per RESEARCH: the migration MUST run before the new image rolls (Pitfall 6 — a new image that
rolls ahead of its migration trips assertTenantLockInstalled and fails boot). The migration is a
one-off ECS run-task under DATABASE_ADMIN_URL; the deploy waits on its exit code. AWS Copilot is
EOL — use the ECS Express Mode deploy action.

Purpose: This turns "merge to main" into a safe, gated production rollout that the solo founder can
trust, with the migration ordering that prevents the classic mid-deploy isolation-corruption race.
Output: a cd.yml workflow + a CD runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/07.5-08-PLAN.md
@apps/api/Dockerfile

<interfaces>
The CD surface (extracted from codebase + research):
.github/workflows/ci.yml: existing nx-affected jobs (install, affected, format, commitlint, secret-scan, audit, openapi-drift) + the docker-api job restored in 07.5-02. CD gates on these being green.
packages/db/package.json: db:migrate -> tsx src/cli/migrate.ts; requires DATABASE_ADMIN_URL in non-dev (no DATABASE_URL fallback).
RESEARCH "CD pipeline structure": 1 CI gate -> 2 build to ECR (tag GITHUB_SHA) -> 3 boot smoke (8 preflights + /healthz) -> 4 ECS run-task pnpm db:migrate (admin role), wait exit 0, abort on failure -> 5 amazon-ecs-deploy-express-service, wait for stability -> 6 curl https://api.resto.app/healthz.
RESEARCH "AWS Copilot End-of-Life Alert": use aws-actions/amazon-ecs-deploy-express-service (verify the action name/inputs against current AWS docs before writing — A5-adjacent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cd.yml — build -> ECR -> smoke -> gated migrate -> deploy -> verify</name>
  <files>.github/workflows/cd.yml</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md sections "CD pipeline structure", "AWS Copilot End-of-Life Alert", Pitfall 6 (migration ordering)
    - .github/workflows/ci.yml — the existing job names + the restored docker-api job (07.5-02), to gate CD on CI success and reuse the configure-aws-credentials pattern
    - packages/db/src/cli/migrate.ts — the DATABASE_ADMIN_URL requirement the migrate step must honor
    - VERIFY the amazon-ecs-deploy-express-service action name + inputs against current AWS docs (Context7 or the cited AWS blog) before writing — do not invent input keys
  </read_first>
  <action>
    Create .github/workflows/cd.yml triggered on push to main, gated on CI success
    (workflow_run on ci.yml conclusion success, or needs the same jobs — pick the form the repo's
    branch protection supports). Steps: configure AWS credentials (OIDC role preferred); docker
    build apps/api -> push to ECR tagged with the commit SHA (immutable); a boot smoke step that
    runs the image far enough to pass the 8 preflights + /healthz (reuse the 07.5-02 smoke shape
    against a throwaway/CI Postgres, OR rely on the docker-api CI gate having already proven the
    SHA boots and skip re-running here — document the choice); then an ECS run-task executing
    pnpm db:migrate with DATABASE_ADMIN_URL from Secrets Manager, waiting for exit code 0 and
    ABORTING the workflow on non-zero (old task definition keeps serving); then
    amazon-ecs-deploy-express-service to roll the new task definition, waiting for service
    stability; finally curl https://api.resto.app/healthz expecting 200. The migrate step MUST run
    before the deploy step (Pitfall 6). Keep it lean — no blue-green, a brief maintenance window is
    acceptable per CTO review.
  </action>
  <verify>
    <automated>grep -q 'db:migrate' .github/workflows/cd.yml && grep -qi 'ecs' .github/workflows/cd.yml && grep -qi 'healthz' .github/workflows/cd.yml</automated>
  </verify>
  <acceptance_criteria>
    - cd.yml triggers on merge to main, gated on CI green.
    - It builds + pushes the SHA-tagged image to ECR, runs the gated pnpm db:migrate (admin role) before deploy, deploys via the ECS Express Mode action, and verifies /healthz 200.
    - The migrate step aborts the deploy on non-zero exit (the old task definition keeps serving) — asserted by the step ordering + failure handling in the YAML.
    - The action name/inputs match current AWS docs (no invented keys).
  </acceptance_criteria>
  <done>Merge to main triggers a safe, migration-gated production rollout.</done>
</task>

<task type="auto">
  <name>Task 2: Write cd-pipeline.md (flow + gating rationale + rollback pointer)</name>
  <files>infra/runbooks/cd-pipeline.md</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section "G-06: Expand/contract rollback" (forward-only migrations, app-only image rollback)
    - the cd.yml just authored
  </read_first>
  <action>
    Document the CD flow in infra/runbooks/cd-pipeline.md: the 6 stages, why migrate is gated
    before deploy (Pitfall 6 + assertTenantLockInstalled as the safety net), how a failed migration
    leaves the old task definition serving, and a pointer to the expand/contract + app-only-rollback
    procedure (proven in 07.5-10). State the required GitHub repo secrets / OIDC role and the ECR
    repo. No secret values.
  </action>
  <verify>
    <automated>test -f infra/runbooks/cd-pipeline.md && grep -q migrate infra/runbooks/cd-pipeline.md</automated>
  </verify>
  <acceptance_criteria>
    - cd-pipeline.md documents the flow, the migrate-before-deploy gating rationale, and the rollback pointer.
    - Required CD credentials/role + ECR repo named; no secret values.
  </acceptance_criteria>
  <done>The CD pipeline is documented and reproducible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> AWS (ECR/ECS) | CD credentials with deploy power; must be least-privilege OIDC |
| migration run-task -> prod schema | DDL under the admin role; a mid-deploy race corrupts isolation if ungated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075-24 | Tampering/Integrity | new image rolls before its migration | mitigate | Migrate is a gated pre-rollout step; deploy waits on exit 0; assertTenantLockInstalled is the boot-time safety net |
| T-075-25 | Elevation of Privilege | over-broad CD IAM | mitigate | OIDC role scoped to ECR push + ECS deploy + the migration run-task only |
| T-075-26 | Tampering | migrating under the runtime role | mitigate | db:migrate requires DATABASE_ADMIN_URL (no DATABASE_URL fallback in non-dev) — runtime role cannot run DDL |
| T-075-SC | Tampering | GitHub Action supply chain (amazon-ecs-deploy-express-service) | mitigate | Pin the action to a released version SHA; it is an AWS-published action verified against AWS docs |
</threat_model>

<verification>
- cd.yml: build -> ECR -> smoke -> gated migrate -> deploy -> /healthz 200, migrate-before-deploy ordering.
- A failed migration aborts the deploy (old task definition keeps serving).
- cd-pipeline.md documents the flow + rollback pointer.
</verification>

<success_criteria>
CD deploys on merge to main on top of the existing CI, with database migrations as a gated pre-rollout step under the admin role (SC#3).
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-09-SUMMARY.md` when done.
</output>
