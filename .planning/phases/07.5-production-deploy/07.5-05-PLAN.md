---
phase: 07.5-production-deploy
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/admin/package.json
  - apps/website/package.json
  - apps/qr-menu/package.json
  - pnpm-lock.yaml
files_modified_note: "Dependency manifests + lockfile only. No source-file overlap with 07.5-02/03/04. Runs Wave 0 so 07.5-03 (imports @sentry/node) has the dep available. Lockfile churn is isolated to install."
autonomous: false
requirements: []

must_haves:
  truths:
    - "The Sentry SDK packages needed by the API and the web/qr-menu apps are installed and present in the lockfile"
    - "Every newly installed package was legitimacy-verified before install (no slopsquatted dependency entered the supply chain)"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Pinned Sentry SDK entries"
      contains: "sentry"
  key_links:
    - from: "apps/api/package.json"
      to: "@sentry/node"
      via: "dependency entry consumed by bootstrap-telemetry.ts (07.5-03)"
      pattern: "@sentry/node"
---

<objective>
Centralize the package-manager installs this phase introduces (Sentry SDKs for G-04) behind a
single legitimacy gate, so every other plan imports an already-installed, already-verified
dependency. Installs are isolated here to keep lockfile churn out of the source plans and to run
the legitimacy checkpoint once.

Packages introduced this phase:
- @sentry/node (apps/api — process-level unhandled-exception capture; consumed by 07.5-03)
- @sentry/nextjs (apps/admin, apps/website — Next.js error + sourcemap upload)
- @sentry/react + @sentry/vite-plugin (apps/qr-menu — React error capture + hidden-sourcemap upload)

Purpose: Supply-chain safety (T-075-SC) plus a clean dependency baseline for the runtime-readiness
plan. RESEARCH has no Package Legitimacy Audit table for these (none was produced), so per the
planner policy they are treated as [ASSUMED] and gated by a blocking human-verify checkpoint
against the registry before install.
Output: installed + pinned Sentry SDKs; a recorded legitimacy verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy-verify the Sentry SDK packages before install</name>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section G-04 (which Sentry SDKs map to which app)
  </read_first>
  <what-built>Nothing installed yet — this checkpoint verifies the four Sentry packages are the genuine Sentry-published packages before anything enters the lockfile.</what-built>
  <how-to-verify>
    1. Open npmjs.com/package/@sentry/node, @sentry/nextjs, @sentry/react, @sentry/vite-plugin.
    2. Confirm each is published by the Sentry org (getsentry), has millions of weekly downloads, a
       repository link to github.com/getsentry, and a current release date.
    3. Confirm there is no typo/scope-squat (the scope is exactly @sentry, not @sentrylabs or similar).
  </how-to-verify>
  <acceptance_criteria>
    - All four packages confirmed as genuine Sentry-org publications (not slopsquats).
    - The verdict (APPROVED, with the verified versions) is recorded so Task 2 can pin them.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the confirmed versions, or name any package that fails verification.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install + pin the Sentry SDKs into the right apps</name>
  <files>apps/api/package.json, apps/admin/package.json, apps/website/package.json, apps/qr-menu/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section G-04 (per-app SDK mapping)
    - the four package.json files — match the existing dependency-versioning style (caret vs exact) used in this repo
  </read_first>
  <action>
    Using pnpm with the workspace --filter flags, add @sentry/node to apps/api; @sentry/nextjs to
    apps/admin and apps/website; @sentry/react + @sentry/vite-plugin to apps/qr-menu — at the
    versions approved in Task 1. Run pnpm install --frozen-lockfile=false to update pnpm-lock.yaml.
    Do NOT add any Sentry init/config code here — that wiring lives in 07.5-03 (api) and the
    web-app Sentry config files land where the apps need them; this plan only establishes the
    dependency baseline. Confirm the install does not pull a transitive optional NestJS peer that
    build.mjs externalizes (it should not — Sentry is a normal dep).
  </action>
  <verify>
    <automated>pnpm exec nx run-many -t build --projects=admin,website,qr-menu,api --parallel=3</automated>
  </verify>
  <acceptance_criteria>
    - @sentry/node in apps/api/package.json; @sentry/nextjs in apps/admin + apps/website; @sentry/react + @sentry/vite-plugin in apps/qr-menu.
    - pnpm-lock.yaml updated and committed; `pnpm install --frozen-lockfile` succeeds afterward.
    - The four apps still build.
  </acceptance_criteria>
  <done>Sentry SDKs installed + pinned; downstream plans import a verified, present dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> lockfile | A new dependency entering the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075-SC | Tampering | Sentry SDK installs (@sentry/node, @sentry/nextjs, @sentry/react, @sentry/vite-plugin) | mitigate | Blocking human-verify legitimacy checkpoint against npmjs.com before install; [ASSUMED] packages are never auto-approved |
</threat_model>

<verification>
- Legitimacy checkpoint approved with confirmed versions.
- Sentry SDKs installed in the correct apps and present in the lockfile; the four apps build.
</verification>

<success_criteria>
The phase's only new dependencies are installed behind a single legitimacy gate; the runtime-readiness plan has @sentry/node available.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-05-SUMMARY.md` when done. Record the approved versions.
</output>
