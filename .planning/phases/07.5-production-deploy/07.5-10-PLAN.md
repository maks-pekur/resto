---
phase: 07.5-production-deploy
plan: '10'
type: execute
wave: 4
depends_on: ['07.5-06', '07.5-08', '07.5-09']
files_modified:
  - apps/admin/e2e/prod-smoke.spec.ts
  - apps/api/test/four-surface-smoke.spec.ts
  - infra/CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md
  - infra/terraform/buckets-cors.tf
  - docs/runbooks/go-live-checklist.md
autonomous: false
requirements: ['SC#5', 'G-02', 'G-06', 'G-07']
must_haves:
  truths:
    - 'An external four-surface HTTPS smoke passes: api /healthz, website tenant menu, qr-menu SPA, admin login — and a tenant menu renders end-to-end (SC#5)'
    - 'The ported ADM-00 Playwright smoke runs against the live static admin and passes'
    - 'A database restore has been proven once on the revenue path before go-live (G-02)'
    - 'An app-only image rollback has been proven (redeploy a prior SHA, app serves) without a DB rollback (G-06)'
    - 'The abandoned EKS/Terraform direction is superseded: ADR-0011/infra-CLAUDE/k8s/terraform stubs marked dead and the ElastiCache stub removed; no Redis (G-07)'
    - 'The stale RESEARCH.md Architectural Responsibility Map "Admin web app → Admin ECS service (Next.js SSR)" row is corrected or marked superseded (admin is static now per CONTEXT.md 2026-06-26) (G-07)'
  artifacts:
    - path: 'apps/api/test/four-surface-smoke.spec.ts'
      provides: 'External HTTPS smoke hitting all four live surfaces'
      contains: 'healthz'
    - path: 'docs/runbooks/go-live-checklist.md'
      provides: 'The go-live gate: restore proof + rollback proof + four-surface smoke + supersession'
      contains: 'rollback'
  key_links:
    - from: 'apps/api/test/four-surface-smoke.spec.ts'
      to: 'the four live HTTPS hosts'
      via: 'fetch each surface + assert a tenant menu renders'
      pattern: 'https://'
    - from: 'infra/CLAUDE.md + STACK.md + 07.5-RESEARCH.md'
      to: 'this phase decision (ECS, not EKS; admin static, not ECS; no Redis)'
      via: 'supersession notes replacing the ADR-0011/EKS/ElastiCache + admin-as-ECS language'
      pattern: 'superseded|ECS'
---

<objective>
Run the go-live gate that closes the phase: the SC#5 external four-surface HTTPS E2E smoke (api `/healthz`, website tenant menu, qr-menu SPA, admin login page) including the ported ADM-00 admin Playwright smoke and an end-to-end tenant-menu render; prove the G-02 restore once on the live data path; prove the G-06 app-only image rollback (redeploy a prior SHA, app serves, no DB rollback); and supersede the abandoned AWS-EKS/Terraform direction (G-07) — mark ADR-0011/`infra/CLAUDE.md`/`infra/k8s`/`infra/terraform` stubs dead, correct the stale RESEARCH.md "admin → ECS service" Architectural Responsibility Map row (admin is static now), and remove the ElastiCache stub (no Redis for MVP).

Purpose: This is the gate to revenue. SC#5 requires an external smoke confirming all four surfaces are reachable over HTTPS and a tenant menu renders end-to-end. G-02 (backups + tested restore) and G-06 (app-only rollback) are BLOCK/MEDIUM gates that must be proven before a paying restaurant's data lives here. G-07 closes the wrong-direction infra debt so the next engineer (or the founder in six months) does not build EKS/Redis or rebuild admin as an ECS SSR service — we are AWS ECS for api+website, admin+qr-menu are static, with no Redis (menu caching is HTTP/CDN ETag).

Output: a four-surface smoke spec + the ported ADM-00 Playwright smoke, the restore + rollback proofs, the superseded infra docs/stubs (including the corrected RESEARCH.md admin-as-ECS row), and `docs/runbooks/go-live-checklist.md` recording the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-production-deploy/07.5-CONTEXT.md
@.planning/phases/07.5-production-deploy/07.5-08-SUMMARY.md
@.planning/phases/07.5-production-deploy/07.5-09-SUMMARY.md
@docs/runbooks/database-backup-restore.md
@docs/runbooks/cd-deploy.md

<interfaces>
From apps/admin (Phase 2 ADM-00 + Phase 7.6 port):
  - the existing Playwright smoke (scaffold-walk: /login renders, auth-gated redirect) to point at the live admin host
From apps/api/src/health/health.controller.ts:
  - /healthz (200 liveness) — the api smoke target
From the live hosts (plan 08):
  - api.<domain>, website host, <brand>.menu.resto.app (qr-menu + same-origin /v1/menu), admin host
From docs/runbooks/database-backup-restore.md (plan 06):
  - the rehearsed restore procedure to re-prove (or reference) at go-live
From docs/runbooks/cd-deploy.md (plan 09):
  - the app-only rollback command (redeploy a prior SHA) to execute as the G-06 proof
From infra/CLAUDE.md + .planning/codebase/STACK.md:132 + infra/terraform/buckets-cors.tf:
  - the EKS/ElastiCache/ADR-0011 references to supersede (G-07); ElastiCache stub to remove
From .planning/phases/07.5-production-deploy/07.5-RESEARCH.md §"Architectural Responsibility Map" (the "Admin web app → Admin ECS service (Next.js SSR)" row, ~line 76):
  - the STALE admin-as-ECS entry to correct/mark superseded — admin is static (Cloudflare Pages) per CONTEXT.md 2026-06-26 re-plan; there is no admin Dockerfile / admin Fargate service
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-surface external HTTPS smoke + ported ADM-00 Playwright smoke (SC#5)</name>
  <read_first>
    - apps/admin existing Playwright smoke (ADM-00 scaffold-walk from Phase 2 / ported in 7.6) — the spec to retarget at the live admin host
    - apps/api/src/health/health.controller.ts (/healthz contract)
    - 07.5-08-SUMMARY.md (the four live host names + the qr-menu same-origin /v1/menu)
    - apps/CLAUDE.md (network-call timeout rule for the smoke fetches)
  </read_first>
  <action>
    Write a four-surface smoke (apps/api/test/four-surface-smoke.spec.ts or an equivalent runnable external check) that, against the LIVE prod hosts, asserts: (1) GET https://api.<domain>/healthz → 200; (2) GET the website tenant host → 200 and the response contains the rendered tenant menu (a known seeded item name or the menu container) — the end-to-end render SC#5 requires; (3) GET https://<brand>.menu.resto.app/ → 200 (qr-menu SPA shell) AND GET .../v1/menu → 200 same-origin (proves the Cloudflare rewrite); (4) GET https://admin.<domain>/login → 200 (static admin login). Each fetch uses AbortSignal.timeout per the apps/CLAUDE.md rule. Then port/retarget the existing ADM-00 admin Playwright smoke to run against the live admin host (login page renders, unauthenticated route redirects to /login) and run it green. Capture the smoke output (status codes + the rendered-menu assertion) for the go-live checklist.
  </action>
  <verify>
    <automated>pnpm --filter @resto/api test -- four-surface-smoke 2>&1 | tail -25 ; pnpm --filter @resto/admin exec playwright test prod-smoke 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The smoke asserts 200 from api /healthz, the website host, the qr-menu host, and admin /login — all over https.
    - The smoke asserts the website (or qr-menu) response renders a real tenant menu item (end-to-end render, not just a 200 shell).
    - The smoke asserts https://<brand>.menu.resto.app/v1/menu returns 200 same-origin (rewrite proof).
    - The ported ADM-00 Playwright smoke runs against the live admin host and passes (login renders + auth redirect).
  </acceptance_criteria>
  <done>All four surfaces proven reachable over HTTPS + a tenant menu renders end-to-end + admin Playwright smoke green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The two go-live proofs the gate requires before taking a paying customer: (a) a database restore on the live data path (G-02), and (b) an app-only image rollback (G-06). These execute against live prod infra and must be witnessed/recorded by the founder.</what-built>
  <how-to-verify>
    1. G-02 restore proof: following docs/runbooks/database-backup-restore.md, restore the latest production backup/snapshot to a throwaway target and verify the migrations journal + extensions + 3 roles + a sample tenant-scoped table read; record the transcript + RTO observed. (If plan 06 already rehearsed this, re-confirm it still holds against the now-live production data and re-record.)
    2. G-06 rollback proof: capture the current api image SHA; redeploy the immediately-prior SHA-tagged image via the plan-08 IaC redeploy command (docs/runbooks/cd-deploy.md); confirm the api still serves /healthz 200 on the prior version with NO database rollback; then redeploy the current SHA. Record both transitions.
    3. Confirm neither proof required (or performed) a DB rollback — forward-only holds.
  </how-to-verify>
  <resume-signal>Type "go-live-proofs-passed" with the restore RTO + the two rollback SHAs recorded, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Supersede the EKS/Terraform/ElastiCache + stale admin-as-ECS direction — G-07 (no Redis)</name>
  <read_first>
    - infra/CLAUDE.md (the EKS/k8s/terraform "EKS, RDS, ElastiCache" layout language to supersede)
    - .planning/codebase/STACK.md (line ~132: "Target: AWS EKS (ADR-0011); Terraform ... Helm charts ... (stub)")
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md §"Architectural Responsibility Map" (~line 76: the "Admin web app → Admin ECS service (Next.js SSR) … needs a Dockerfile" row — STALE after the 2026-06-26 admin→static re-plan)
    - .planning/phases/07.5-production-deploy/07.5-CONTEXT.md §"SCOPE UPDATE (2026-06-26)" (the authoritative admin-is-static decision that supersedes the RESEARCH.md row)
    - infra/terraform/buckets-cors.tf (keep — it is the R2/S3 CORS contract — but reconcile its "production infra phase" language)
    - infra/k8s/.gitkeep + infra/terraform/.gitkeep (the dead stubs)
  </read_first>
  <action>
    Supersede the abandoned AWS-EKS/Terraform direction and the stale admin-as-ECS entry (G-07).
    (1) In infra/CLAUDE.md, replace the EKS/Helm/Terraform-EKS-RDS-ElastiCache layout language with the actual 7.5 reality — AWS ECS/Fargate (api+website) + Cloudflare Pages static (admin+qr-menu) + the chosen managed Postgres + self-hosted single-node NATS + R2 + Cloudflare DNS/TLS/CDN + AWS Secrets Manager; explicitly state "ECS not EKS; no k8s; no multi-region; no Redis/ElastiCache (menu caching is HTTP/CDN ETag); admin+qr-menu are static (Cloudflare Pages), NOT ECS services — no admin Dockerfile, no admin Fargate task".
    (2) In STACK.md, replace the "Target: AWS EKS (ADR-0011); Helm charts (stub)" line with "Target: AWS ECS/Fargate (api+website) + Cloudflare Pages static (admin+qr-menu) (see Phase 7.5 runbooks); EKS direction superseded 2026-06-26".
    (3) In 07.5-RESEARCH.md §"Architectural Responsibility Map", correct the STALE "Admin web app → Admin ECS service (Next.js SSR) … Separate Fargate service … needs a Dockerfile" row: change the Primary Tier to "Cloudflare static hosting (Pages or R2+CDN)" matching the qr-menu row, and annotate the rationale that this row is **superseded by CONTEXT.md 2026-06-26 (admin re-platformed to a static Vite SPA; no admin ECS service / Dockerfile)**. Also reconcile the "Four-surface scope (revised 2026-06-21)" note below the table which still says "apps/admin (ECS)" — mark it superseded by the 2026-06-26 admin-static re-plan (admin static, not ECS). Do not delete the table; correct/annotate it so the record is accurate, not erased.
    (4) REMOVE the dead ElastiCache stub: delete any ElastiCache reference from infra/CLAUDE.md and confirm no infra/terraform file declares ElastiCache (the tree currently has only buckets-cors.tf + .gitkeep — remove the k8s stub dir or leave a single superseded README in it; do NOT leave EKS Helm/Terraform-estate scaffolding implying it should be built).
    (5) Reconcile buckets-cors.tf's "production infra phase" comment to point at the 7.5 R2 runbook instead. Do not break the buckets-cors.tf CORS contract.
    (6) Write docs/runbooks/go-live-checklist.md consolidating: the four-surface smoke result, the restore proof (RTO), the rollback proof (SHAs), and the supersession (EKS/ElastiCache + stale admin-as-ECS) — the single go/no-go record.
  </action>
  <verify>
    <automated>grep -niE 'EKS|ElastiCache|Redis' infra/CLAUDE.md .planning/codebase/STACK.md | grep -viE 'supersed|not eks|no redis|no elasticache|removed' | grep -v '^#' ; grep -niE 'Admin ECS service|apps/admin \(ECS\)' .planning/phases/07.5-production-deploy/07.5-RESEARCH.md | grep -viE 'supersed' ; echo '--- expect no live EKS/Redis claims and no un-superseded admin-as-ECS claim above ---'</automated>
  </verify>
  <acceptance_criteria>
    - infra/CLAUDE.md describes the ECS+Cloudflare+managed-PG+NATS+R2+Secrets-Manager reality and states "ECS not EKS; no k8s/multi-region; no Redis/ElastiCache; admin+qr-menu static, not ECS".
    - STACK.md no longer claims "Target: AWS EKS" as the live direction — it points to the 7.5 ECS+static reality and marks EKS superseded.
    - 07.5-RESEARCH.md's Architectural Responsibility Map "Admin web app" row no longer lists "Admin ECS service (Next.js SSR)" as a live primary tier — it is corrected to static hosting and/or annotated as superseded by CONTEXT.md 2026-06-26; the "apps/admin (ECS)" line in the four-surface-scope note is likewise marked superseded (grep above returns no un-superseded admin-as-ECS claim).
    - No infra file declares an ElastiCache/Redis resource (the ElastiCache stub is removed); the grep above shows no remaining live EKS/Redis/ElastiCache claim outside supersession notes.
    - buckets-cors.tf still defines the single-admin-origin CORS contract (not broken) and its comment points at the 7.5 R2 runbook.
    - go-live-checklist.md consolidates the smoke + restore + rollback + supersession (EKS + admin-as-ECS) into one go/no-go record.
  </acceptance_criteria>
  <done>EKS/Terraform/ElastiCache + stale admin-as-ECS direction superseded; no Redis; go-live checklist recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external client → four live surfaces | the smoke exercises the real public ingress |
| backup → restore target | restored copy holds real (eventually PII) production data |
| prior image rollback → prod | a prior app version runs against the current (forward-only) schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075-10-01 | Denial of Service | unverified restore path before taking money | mitigate | G-02 restore proven on the live data path with a recorded RTO before go-live |
| T-075-10-02 | Tampering | a bad deploy with no safe rollback | mitigate | G-06 app-only rollback proven (prior SHA serves, no DB rollback); forward-only/expand-contract holds |
| T-075-10-03 | Information Disclosure | restore target left running with prod data | mitigate | Throwaway restore target torn down after the drill (per database-backup-restore.md) |
| T-075-10-04 | Repudiation | wrong-direction infra rebuilt later (EKS/Redis or admin-as-ECS) | mitigate | G-07 supersedes ADR-0011/EKS/ElastiCache + the stale RESEARCH.md admin-as-ECS row in writing; ElastiCache stub removed; no Redis |
| T-075-10-05 | Spoofing | smoke passing against a non-prod/stale surface | mitigate | The smoke asserts an end-to-end tenant-menu render (real data), not just a 200 shell |
| T-075-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs; smoke uses existing test tooling (Playwright already present) |
</threat_model>

<verification>
- Four-surface external HTTPS smoke green; tenant menu renders end-to-end (SC#5).
- Ported ADM-00 admin Playwright smoke green against the live host.
- G-02 restore proven on the live data path (RTO recorded).
- G-06 app-only rollback proven (prior SHA serves, no DB rollback).
- G-07: EKS/Terraform/ElastiCache superseded; the stale RESEARCH.md admin-as-ECS row corrected/marked superseded; ElastiCache stub removed; no Redis.
- go-live-checklist.md consolidates the go/no-go record.
</verification>

<success_criteria>
- SC#5 met; G-02 + G-06 proven; G-07 superseded (incl. stale admin-as-ECS RESEARCH.md row) — the phase is go-live ready and Phase 8 has a public HTTPS webhook URL.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-10-SUMMARY.md` when done.
</output>
</content>
