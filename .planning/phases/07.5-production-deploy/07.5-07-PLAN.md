---
phase: 07.5-production-deploy
plan: '07'
type: execute
wave: 1
depends_on: ['07.5-03', '07.5-04']
files_modified:
  - apps/qr-menu/worker/index.ts
  - apps/qr-menu/worker/tsconfig.json
  - apps/qr-menu/test/worker.spec.ts
  - apps/qr-menu/wrangler.jsonc
  - apps/qr-menu/package.json
  - apps/admin/worker/index.ts
  - apps/admin/worker/tsconfig.json
  - apps/admin/test/worker.spec.ts
  - apps/admin/wrangler.jsonc
  - apps/admin/package.json
  - apps/admin/Dockerfile
  - pnpm-lock.yaml
  - docs/runbooks/spa-workers.md
autonomous: true
requirements: ['SC#1', 'SC#5', 'D-08', 'G-05']

must_haves:
  truths:
    - 'A guest on <slug>.menu.<apex> fetches /v1/menu same-origin — no cross-origin request, no CORS preflight'
    - 'Two different tenants never receive each other content, from the Worker cache or from any cache in front of it'
    - 'The API still resolves the correct tenant even though the proxied request is addressed to api.<apex>'
    - 'An operator keeps their Better Auth session cookie across every /api/auth/* call'
    - 'No authenticated admin response is ever written to any cache'
    - 'Each response says which cache path it took, so a live smoke can assert cache behaviour instead of guessing'
    - 'Neither Worker nor its config names a domain — routes and API origin are supplied at deploy time'
  artifacts:
    - path: 'apps/qr-menu/worker/index.ts'
      provides: 'qr-menu SPA serving + same-origin /v1/* proxy, tenant-keyed Worker cache, zone-cache opt-out on the subrequest, X-Resto-Cache signal'
      contains: 'X-Forwarded-Host'
    - path: 'apps/qr-menu/test/worker.spec.ts'
      provides: 'The cross-tenant isolation suite — the first-class proof for Pitfall #2, including the zone-cache opt-out'
      contains: 'cacheEverything'
    - path: 'apps/admin/worker/index.ts'
      provides: 'admin SPA serving + same-origin /v1/* and /api/* proxy, never cached at any layer'
      contains: 'X-Forwarded-Host'
    - path: 'apps/qr-menu/wrangler.jsonc'
      provides: 'Worker + Static Assets config with SPA fallback and run_worker_first, and deliberately no routes/vars'
      contains: 'run_worker_first'
  key_links:
    - from: 'apps/qr-menu/worker/index.ts'
      to: 'caches.default'
      via: 'cache key built from the ORIGINAL request URL, which carries the tenant subdomain'
      pattern: 'caches\\.default'
    - from: 'apps/qr-menu/worker/index.ts'
      to: 'the api.<apex> zone cache'
      via: 'fetch(..., { cf: { cacheTtl: 0, cacheEverything: false } }) so the tenant-blind subrequest is never cached upstream'
      pattern: 'cacheTtl'
    - from: 'apps/admin/worker/index.ts'
      to: 'https://api.<apex>/api/auth/*'
      via: 'same-origin proxy preserving method, body and Set-Cookie'
      pattern: '/api/'
    - from: 'wrangler deploy invocation'
      to: 'PUBLIC_APEX_DOMAIN'
      via: '--routes and --var supplied by the caller, never written into wrangler.jsonc'
      pattern: 'PUBLIC_APEX_DOMAIN'
---

<objective>
Build the two Cloudflare Workers that host the admin and qr-menu SPAs and give each a
same-origin path to the API — with per-tenant cache isolation proven by tests at both
cache layers, not just the one the Worker owns.

Purpose: the committed code already requires `*.admin.<apex>` and `*.menu.<apex>`.
Classic Pages custom domains cannot serve a wildcard host (Pitfall #1), so "Cloudflare
Pages" in the CONTEXT.md prose resolves to **Workers with Static Assets**. The proxy is
not optional plumbing: phase 10.2 F-41 found the operator session cookie silently
dropped whenever admin talks cross-origin, and deleted `VITE_API_ORIGIN` outright (D-39).

Output: two Worker modules, two `wrangler.jsonc` files carrying no hostname, two test
suites, and the deploy-invocation contract that supplies the apex at deploy time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/07.5-VERIFICATION.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-FINDINGS.md
@docs/runbooks/menu-edge-caching.md

Read the SCOPE UPDATE banners at the top of `07.5-CONTEXT.md` before the decision bodies.
D-08 survives in principle; its "Cloudflare Pages" wording is corrected to Workers with
Static Assets by RESEARCH.md Pattern 4.

**Do not reintroduce `VITE_API_ORIGIN`.** It was deleted in phase 10.2 (D-39) because
admin talking cross-origin loses the session cookie (`AUTH_COOKIE_DOMAIN` is Zod-validated
to start with `.` and scopes the cookie to the `admin` subtree). `grep -rn VITE_API_ORIGIN
apps/admin` must keep returning nothing.

**Where this plan and RESEARCH.md disagree, this plan wins.** Verification found that the
three research claims this subsystem inherited without re-deriving — a per-tenant ETag,
Pitfall #2's mitigation being complete, and `cf-cache-status` being observable from a
Worker — were the phase's highest-severity errors, all in the edge cache. Two are
corrected below.

**Standing rule for this phase, earned twice.** Round 1's three errors were research
claims about the edge cache inherited without re-derivation; Round 2's were three claims
about what a third-party component does — Cloudflare's `X-Forwarded-For` semantics, the
Resend/Stripe constructors, a grep's blast radius — each asserted confidently and never
executed. So: **any sentence asserting how a third-party component behaves needs either a
citation or a command.** If you add such a sentence while executing, add one too, and put
the observation in the summary.

@apps/qr-menu/vite.config.ts
@apps/admin/vite.config.ts

<interfaces>
<!-- Contracts the executor needs. Do not go exploring, and do not restore anything this
     block contradicts. -->

Tenant resolution the proxy must not break — `apps/api/src/shared/effective-host.ts`:

```typescript
export function effectiveHost(
  headers: Record<string, string | string[] | undefined>,
  trustProxy: boolean,
): string | undefined;
// with trustProxy on it reads headers['x-forwarded-host'] (first comma-separated value),
// else headers.host. A Worker fetch() to https://api.<apex>/... sets Host: api.<apex>,
// so without X-Forwarded-Host no tenant binds (Pitfall #3).
```

Plan 06's Caddyfile sets `trusted_proxies` so the header survives the second hop. The
Worker's job is only to set it.

The origin's cache contract (`apps/api/src/contexts/catalog/domain/menu-cache.ts`,
`docs/runbooks/menu-edge-caching.md`):

```
GET /v1/menu               public, s-maxage=300, stale-while-revalidate=60
GET /v1/menu/items/:id     public, s-maxage=300, stale-while-revalidate=60
GET /v1/menu/availability  public, s-maxage=5
```

**The ETag is NOT tenant-scoped.** `public-menu.controller.ts:272,298` builds it as
`'"' + version + '"'` — a bare menu-version integer. Two freshly published tenants both
sit at version 1 and emit identical ETags. Never use an ETag difference as an isolation
signal; RESEARCH.md's "per-tenant ETag" phrasing is wrong.

Build-time env the bundles read — derived from the apex by whoever builds them:

```typescript
// apps/admin/src/env.ts — getEnv throws in PROD if unset, and rejects localhost (G-05)
export const VITE_STRIPE_PUBLISHABLE_KEY: string;
export const VITE_ADMIN_HOST_SUFFIX: string;   // admin.<apex>; dev default admin.localhost
// apps/qr-menu/src/env.ts
export const VITE_STRIPE_PUBLISHABLE_KEY: string;
```

Worker runtime shape to code against (structural, NOT the ambient `Fetcher` type):

```typescript
export interface Env {
  ASSETS: { fetch(request: Request): Promise<Response> };
  API_ORIGIN: string;
}
export default {
  fetch(request: Request, env: Env, ctx: { waitUntil(p: Promise<unknown>): void }): Promise<Response>
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: qr-menu Worker — same-origin proxy, tenant-keyed cache, and no upstream cache to leak into</name>
  <files>apps/qr-menu/worker/index.ts, apps/qr-menu/worker/tsconfig.json, apps/qr-menu/test/worker.spec.ts</files>
  <behavior>
    Write these first; each one names a way the naive version is wrong.

    - `/` and `/assets/app.js` on `pizza.menu.example.invalid` go to `env.ASSETS.fetch`
      and no origin fetch happens.
    - `GET /v1/menu` on `pizza.menu.example.invalid` reaches
      `https://api.example.invalid/v1/menu` with `X-Forwarded-Host:
      pizza.menu.example.invalid` and `X-Forwarded-Proto: https`.
    - The `Request` handed to `cache.match`/`cache.put` has
      `url === 'https://pizza.menu.example.invalid/v1/menu'` — the incoming URL, never
      the rewritten origin URL. This is the Pitfall #2 collapse and the reason this task
      exists.
    - **Cross-tenant isolation:** warm `pizza`, then request
      `sushi.menu.example.invalid/v1/menu`; the origin is called a second time and the
      body is sushi's. Confirm this test goes red against an implementation keyed on the
      origin URL before making it green.
    - **The subrequest opts out of the zone cache:** the recorded `fetch` call carries
      `{ cf: { cacheTtl: 0, cacheEverything: false } }`. The Worker's cache and
      Cloudflare's `api.<apex>` zone cache are different caches; the inner fetch is
      byte-identical for every tenant and the origin marks the response
      `public, s-maxage=300`, so if that URL is ever cached at the zone — by a Cache
      Rule, by "Cache Everything", or by the CDN rules D-08 commits to — tenant B's
      Worker receives tenant A's menu *before* its own correctly-keyed cache is
      consulted. A stubbed `fetch` cannot observe that; asserting the opt-out is how a
      unit test reaches it.
    - **Cache signal:** the response carries `X-Resto-Cache: MISS` on the first request,
      `HIT` on the second identical one, and `BYPASS` for any non-cacheable path. Plan 10
      asserts this header live; `cf-cache-status` is emitted by Cloudflare's own pipeline
      and a Worker serving from `caches.default` does not produce it.
    - A second identical `pizza` request is served from cache with exactly one origin call
      in total.
    - `/v1/menu?location=a` and `?location=b` do not share an entry.
    - `POST /v1/orders` is proxied, never cached, and returns `X-Resto-Cache: BYPASS`.
    - An origin response carrying `Set-Cookie` is returned but not cached.
    - A 500 from the origin is returned and not cached.
    - `/v1/orders/abc` is proxied without touching the cache at all.

  </behavior>
  <action>
    Implement `apps/qr-menu/worker/index.ts` from RESEARCH.md's example with four
    deliberate deviations, all of them load-bearing.

    Type `ASSETS` structurally rather than importing `Fetcher` from
    `@cloudflare/workers-types`. The reason to expect trouble is that the ambient package
    redeclares `Request`/`Response` globally, which can collide with the jsdom/node lib set
    the existing vitest projects use — but do not take that on faith either way: it is a
    claim about a package, and the structural binding is the right shape regardless,
    because it keeps the module importable by a plain vitest run with no new test runner
    and no `@cloudflare/vitest-pool-workers`, which is not in RESEARCH.md's package
    legitimacy audit and must not be added. If you want the ambient types anyway, scope
    them to `worker/tsconfig.json` and confirm `pnpm --filter @resto/qr-menu exec vitest run`
    still passes before keeping them.

    Treat cacheability as an explicit allowlist — `/v1/menu`, `/v1/menu/availability`,
    and paths under `/v1/menu/items/` — matching the three routes the origin marks
    public. Deriving it from the response's `Cache-Control` would be cleverer and worse:
    it would silently start caching whatever a future route happens to mark public.

    Build the cache key with `new Request(url.toString(), request)` on the *incoming*
    URL. Read with `cache.match(cacheKey)`; write with
    `ctx.waitUntil(cache.put(cacheKey, response.clone()))` only when the method is GET,
    the path is allowlisted, the response is `ok`, and it carries no `Set-Cookie` — do
    not rely on the Cache API's own `Set-Cookie` refusal as the only guard.

    Issue the subrequest as `fetch(originRequest, { cf: { cacheTtl: 0, cacheEverything: false } })`
    for the reason in the behaviour list. Add the one WHY comment this file is allowed:
    two lines saying that the inner URL is tenant-blind and must never be cached upstream
    of the tenant-keyed entry.

    Set `X-Resto-Cache` on every response the Worker returns from the `/v1/` path. Clone
    the cached response before mutating headers: a `Response` handed back by the Cache API
    can have immutable headers, and the cheapest way to find out is that the HIT-path test
    fails loudly if you do not. Do not skip the clone to check.

    Add `apps/qr-menu/worker/tsconfig.json` extending the app config with `types` scoped
    to the workers types, so the Worker source typechecks against its real runtime while
    the SPA sources keep their DOM lib.

    In the test file, stub `globalThis.caches` with a Map-backed fake recording the
    `Request` it was handed, stub `globalThis.fetch` with a recorder capturing both the
    `Request` and the init object (the `cf` assertion needs the second argument), and pass
    a `ctx` whose `waitUntil` awaits synchronously so assertions do not race the write.
    No comments inside test bodies.

  </action>
  <verify>
    <automated>pnpm --filter @resto/qr-menu exec vitest run test/worker.spec.ts</automated>
  </verify>
  <done>Every behaviour above is a passing test, including the recorded `cf` init option and the `X-Resto-Cache` transitions. The cross-tenant case was observed failing against an origin-URL-keyed implementation and passing against the incoming-URL-keyed one, and that observation is in the summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin Worker — same-origin proxy that never caches, and retirement of the admin Dockerfile</name>
  <files>apps/admin/worker/index.ts, apps/admin/worker/tsconfig.json, apps/admin/test/worker.spec.ts, apps/admin/Dockerfile</files>
  <behavior>
    - `/`, `/login`, `/assets/*` on both `admin.example.invalid` and
      `pizza.admin.example.invalid` are served by `env.ASSETS.fetch`.
    - `GET /v1/orders` and `POST /api/auth/sign-in/email` are proxied to
      `https://api.example.invalid` with `X-Forwarded-Host` set to the incoming hostname
      (apex and per-tenant host each preserved) and `X-Forwarded-Proto: https`.
    - Method, body and headers (`Cookie`, `Content-Type`, `Authorization`) survive the hop.
    - A `Set-Cookie` on the origin response reaches the client unmodified — the F-41
      failure mode in test form.
    - `caches.default.match` and `caches.default.put` are never called for any path.
      Assert the stub's call count is zero, not the absence of a header.
    - Every proxied response carries `X-Resto-Cache: BYPASS`, so the live smoke can tell
      "admin proxy reached" from "SPA shell returned".
    - The subrequest carries `{ cf: { cacheTtl: 0, cacheEverything: false } }` — the same
      zone-cache opt-out as qr-menu, and more important here because these responses are
      authenticated.
    - An unknown path that is neither `/v1/` nor `/api/` falls through to ASSETS, so
      `/dashboard/orders` returns the SPA shell.
  </behavior>
  <action>
    Implement `apps/admin/worker/index.ts`: proxy `/v1/*` and `/api/*`, everything else to
    ASSETS, no cache interaction at all. Same structural `ASSETS` typing and
    `worker/tsconfig.json` treatment as Task 1. The absence of caching here is a security
    property, so the test asserts it directly rather than inferring it.

    Then delete `apps/admin/Dockerfile`. Two reasons, and the second is why the deletion
    is load-bearing rather than tidy-up: it was written for an admin-as-ECS-service
    topology that the 2026-06-26 pivot ended twice over (admin has been a static Vite SPA
    since phase 7.6, and there is no ECS), **and** line 27 bakes
    `ARG ADMIN_WEB_URL=https://admin.resto.app` — a domain default that would ship an
    unowned host into any image built from it. Removing the file removes the literal.
    Check for and remove any reference in `.dockerignore` or workflow files; the website
    Dockerfile from the same plan stays and is untouched here.

  </action>
  <verify>
    <automated>pnpm --filter @resto/admin exec vitest run test/worker.spec.ts && test ! -e apps/admin/Dockerfile && test -z "$(grep -rn 'apps/admin/Dockerfile' --include='*.yml' --include='*.yaml' --include='*.json' --include='.dockerignore' . --exclude-dir=node_modules --exclude-dir=.planning)" && test -z "$(grep -rn 'VITE_API_ORIGIN' apps/admin)"</automated>
  </verify>
  <done>The admin suite passes including zero cache calls, the `cf` opt-out, `X-Resto-Cache: BYPASS` and `Set-Cookie` pass-through; `apps/admin/Dockerfile` is gone with no dangling references, taking the baked `admin.resto.app` default with it; `VITE_API_ORIGIN` is still absent.</done>
</task>

<task type="auto">
  <name>Task 3: wrangler configuration with no hostname in it, and the deploy-invocation contract</name>
  <files>apps/qr-menu/wrangler.jsonc, apps/admin/wrangler.jsonc, apps/qr-menu/package.json, apps/admin/package.json, pnpm-lock.yaml, docs/runbooks/spa-workers.md</files>
  <action>
    Add `wrangler@4.129.0` and `@cloudflare/workers-types@5.20260904.1` as devDependencies
    of both apps at those exact versions. Both are Cloudflare-official, were discovered
    through official Cloudflare documentation, and are `[VERIFIED: npm registry]` /
    Approved in RESEARCH.md's Package Legitimacy Audit with live provenance dated
    2026-09-04 — no verification checkpoint is required. Add no other package; in
    particular not `@cloudflare/vitest-pool-workers`, which Tasks 1-2 were written
    specifically not to need. `pnpm-lock.yaml` changes as a result and is declared in
    `files_modified` — it is the only file this plan shares with nothing else in the wave.

    Write both `wrangler.jsonc` files with `name`, `main` → `worker/index.ts`, a pinned
    `compatibility_date`, and an `assets` block: `directory` `./dist`, `binding` `ASSETS`,
    `not_found_handling` `single-page-application`, and `run_worker_first` listing
    `/v1/*` for qr-menu and `/v1/*` plus `/api/*` for admin. Without `run_worker_first`
    the static-asset router answers those paths before the Worker runs — the SPA shell,
    via `not_found_handling` (`[CITED: developers.cloudflare.com/workers/static-assets/binding/]`,
    fetched 2026-09-04 per RESEARCH.md). Plan 10's smoke assertion 6 is what proves the
    binding took effect in production rather than only in the config.

    **Omit `routes` and `vars` from both files.** They are the only two fields that would
    carry the apex, so they are supplied at deploy time:
    `wrangler deploy --var API_ORIGIN:"https://api.$PUBLIC_APEX_DOMAIN" --routes "*.menu.$PUBLIC_APEX_DOMAIN/*"`
    for qr-menu; for admin the same `--var` plus `admin.$PUBLIC_APEX_DOMAIN/*` and
    `*.admin.$PUBLIC_APEX_DOMAIN/*`.

    Confirm those flags exist before relying on them: `wrangler deploy --help` must list
    `--routes` and `--var`. If either is absent in 4.129.0, fall back to a committed
    `wrangler.template.jsonc` per app rendered to `wrangler.generated.jsonc` with
    `envsubst` (reuse `infra/scripts/render-env.sh`'s shape) and passed via `--config`,
    with the generated file gitignored. Record which path was taken — plans 08 and 09
    both invoke this and must not guess.

    Add a `worker:dry-run` script to each app that builds and then runs
    `wrangler deploy --dry-run` with those flags. **State the limit plainly, in the
    runbook and in this task's summary:** `--dry-run` needs no credentials and therefore
    validates only that the invocation parses and the bundle builds. It does not contact
    Cloudflare and cannot prove a route binds or that a wildcard pattern is accepted for
    the zone. Route binding is proven for the first time in plan 08 Task 3, against the
    real zone.

    Write `docs/runbooks/spa-workers.md`: why Workers rather than classic Pages (one
    paragraph — wildcard custom domains, Pitfall #1); the exact deploy invocations; the
    build-time env each bundle needs (`VITE_ADMIN_HOST_SUFFIX=admin.<apex>` and
    `VITE_STRIPE_PUBLISHABLE_KEY` for admin, `VITE_STRIPE_PUBLISHABLE_KEY` for qr-menu);
    the DNS records the routes need alongside them (plan 08 creates them); the cache-key
    rule in one line with a pointer to the test that enforces it; the `X-Resto-Cache`
    contract; and the statement that these Workers' own cache **replaces** the Cloudflare
    CDN cache rules D-08 contemplated for `/v1/menu*` — the zone must not cache
    `api.<apex>/v1/*` at all. Plan 08's Cloudflare runbook sets that Bypass rule; naming
    the split here keeps one source for it.

  </action>
  <verify>
    <automated>PUBLIC_APEX_DOMAIN=example.invalid VITE_STRIPE_PUBLISHABLE_KEY=pk_test_dryrun VITE_ADMIN_HOST_SUFFIX=admin.example.invalid pnpm --filter @resto/qr-menu run worker:dry-run && PUBLIC_APEX_DOMAIN=example.invalid VITE_STRIPE_PUBLISHABLE_KEY=pk_test_dryrun VITE_ADMIN_HOST_SUFFIX=admin.example.invalid pnpm --filter @resto/admin run worker:dry-run && PUBLIC_APEX_DOMAIN="$(gh variable get PUBLIC_APEX_DOMAIN)" bash infra/scripts/assert-no-domain-literals.sh apps/admin/wrangler.jsonc apps/qr-menu/wrangler.jsonc apps/admin/worker apps/qr-menu/worker && grep -q 'X-Resto-Cache' docs/runbooks/spa-workers.md && grep -qi 'dry-run' docs/runbooks/spa-workers.md</automated>
  </verify>
  <done>Both dry runs succeed against a synthetic apex supplied entirely from the environment; plan 06's domain guard passes over the Worker sources and configs using the real apex; the runbook states the exact invocation plans 08 and 09 copy, the `X-Resto-Cache` contract, the zone-must-not-cache split, and — explicitly — that `--dry-run` does not prove route binding and plan 08 Task 3 does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                                | Description                                                              |
| --------------------------------------- | ------------------------------------------------------------------------ |
| guest browser → Worker                  | untrusted; every tenant's guests hit the same Worker script              |
| Worker → API origin                     | the Worker asserts tenant identity via `X-Forwarded-Host`                |
| Worker cache and zone cache → guests    | a wrong key at *either* layer serves one restaurant's menu to another's  |

## STRIDE Threat Register

| Threat ID    | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                    |
| ------------ | ---------------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-07.5-07-01 | Information Disclosure | Worker cache key                 | mitigate    | key on the incoming request URL; cross-tenant test required to have been seen failing against the naive key                        |
| T-07.5-07-02 | Information Disclosure | `api.<apex>` zone cache          | mitigate    | subrequest carries `cf: { cacheTtl: 0, cacheEverything: false }`, asserted in the unit test; plan 08 adds a zone Bypass rule as belt-and-braces |
| T-07.5-07-03 | Spoofing               | `X-Forwarded-Host`               | mitigate    | trustworthy only because the origin is unreachable except through Cloudflare (plan 08's ufw) and Caddy is configured to preserve it (plan 06) — both named so the dependency is not invisible |
| T-07.5-07-04 | Information Disclosure | admin response cached anywhere   | mitigate    | zero Cache API calls asserted by call count, plus the same `cf` opt-out on the subrequest                                          |
| T-07.5-07-05 | Session loss           | Better Auth cookie across proxy  | mitigate    | same-origin proxy keeps the cookie in the `AUTH_COOKIE_DOMAIN` subtree; `Set-Cookie` pass-through asserted (F-41 regression net)   |
| T-07.5-07-06 | Tampering              | caching a mutating/private route | mitigate    | explicit three-route allowlist rather than deriving cacheability from the response                                                 |
| T-07.5-07-SC | Tampering              | npm installs                     | mitigate    | only `wrangler` and `@cloudflare/workers-types` at pinned, audited versions; no other package permitted by this plan               |

</threat_model>

<verification>
- Both Worker suites green, including cross-tenant isolation, the recorded `cf` opt-out,
  `X-Resto-Cache`, zero admin cache calls and `Set-Cookie` pass-through.
- Both `worker:dry-run` scripts succeed with the apex from the environment.
- Plan 06's domain guard passes over every file this plan writes.
- `grep -rn VITE_API_ORIGIN apps/admin` returns nothing; `apps/admin/Dockerfile` is gone.
- The runbook records the `--dry-run` limitation rather than implying route validation.
</verification>

<success_criteria>
Both SPA surfaces can be served from Cloudflare's edge on wildcard per-tenant hosts with
a same-origin path to the API, and the three ways this arrangement fails silently — a
collapsed Worker cache key, a tenant-blind response cached at the zone, and a dropped
session cookie — each have a test that fails when reintroduced.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-07-SUMMARY.md` when done. Record
which wrangler parameterization path was taken (`--routes`/`--var`, or the rendered-config
fallback) — plans 08 and 09 invoke it verbatim.
</output>
