---
phase: 07.5-production-deploy
plan: '01'
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md
autonomous: true
requirements: ['D-04']

must_haves:
  truths:
    - 'A written DB-PROVIDER-DECISION exists naming AWS RDS for PostgreSQL as the locked production database with the founder rationale (D-04 LOCKED 2026-06-26)'
    - 'The decision record specifies the 3-role model on RDS (resto_app NOBYPASSRLS, admin-migration/schema-owner with CREATEROLE+DDL, resto_auth BYPASSRLS) under FORCE ROW LEVEL SECURITY with pgcrypto/citext/pg_trgm from the RDS supported list'
    - 'The decision record specifies the connection model (DATABASE_URL + DATABASE_DIRECT_URL for the dispatcher advisory lock; may be equal at first-customer volume; RDS Proxy optional later) and carries an explicit OPEN item: confirm at provisioning (plan 06) that rds_superuser can confer BYPASSRLS on resto_auth, else plan 06 hard-stops'
  artifacts:
    - path: '.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md'
      provides: 'The locked DB-PROVIDER-DECISION = AWS RDS for PostgreSQL — rationale, the 3-role model, the connection model, and the BYPASSRLS-on-RDS open item deferred to plan 06'
      contains: 'AWS RDS'
  key_links:
    - from: 'DB-PROVIDER-DECISION.md'
      to: 'packages/db/sql/roles.sql + auth-role.sql'
      via: 'the recorded role model names the same roles the committed provisioning SQL creates; the empirical BYPASSRLS confirmation runs against them in plan 06'
      pattern: 'resto_auth'
---

<objective>
Record the locked production-database decision: **AWS RDS for PostgreSQL** (D-04, LOCKED by the founder 2026-06-26 after declining Neon). This is a decision record only — no live database is created in this plan. The single genuine provider-capability question (whether RDS's `rds_superuser` master can confer `BYPASSRLS` on `resto_auth`, given the master is not a true `SUPERUSER`) is REAL but is **confirmed empirically once, against the real RDS instance, in plan 06** — not via a throwaway-DB spike. This plan captures the chosen provider, the rationale, the 3-role model, the connection model, and that open item as an explicit hard-stop condition for plan 06.

Purpose: The 3-role + FORCE-RLS model is the product's core tenant-isolation defensibility, and every downstream plan (06 provisioning, 08 secret injection) consumes a single unambiguous provider + connection-model. The founder has locked the provider to AWS RDS — AWS-native (same account as the ECS hosting surface), fully managed (automated backups, point-in-time recovery, patching, durable EBS-backed storage), and free of serverless-Postgres lock-in. The Investor review's BLOCK #1 (3-role + BYPASSRLS on managed Postgres) is honored not by a separate spike but by carrying the BYPASSRLS-on-RDS question forward as a recorded HARD-STOP gate that plan 06 must clear against the real instance before proceeding.

Output: `.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md` — a decision record naming AWS RDS, the founder rationale, the 3-role model on RDS, the `DATABASE_URL` / `DATABASE_DIRECT_URL` connection model (D-05, RDS-simplified), and the explicit "OPEN — confirm at provisioning (plan 06)" BYPASSRLS item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.5-production-deploy/07.5-CONTEXT.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/07.5-PERSONA-INVESTOR.md

<interfaces>
<!-- The committed role-provisioning SQL whose roles the decision record names; the empirical BYPASSRLS proof against them runs in plan 06, not here. -->
From packages/db/sql/roles.sql:
  - GRANT USAGE / SELECT,INSERT,UPDATE on schema public to resto_app (NO DELETE — hard deletes forbidden)
  - app_bind_tenant(text, boolean) SECURITY DEFINER wrapper EXECUTE grant
  - REVOKE all on account/two_factor/verification/session FROM resto_app
From packages/db/sql/auth-role.sql:
  - resto_auth granted SELECT/INSERT/UPDATE/DELETE on BA tables only; BYPASSRLS expected on the role
From packages/db/src/roles.ts + auth-role.ts:
  - provisionAppRole / provisionAuthRole — CREATE ROLE ... PASSWORD via validated client.unsafe() (the path plan 06 runs against the real RDS instance)
From packages/db/src/preflight.ts:
  - assertNoRlsBypass (checks pg_roles.rolsuper AND rolbypassrls on resto_app — must be false/false)
  - extension presence assertion (citext, pgcrypto)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DB-PROVIDER-DECISION.md recording AWS RDS as the locked production database</name>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-CONTEXT.md (the `### Database (Postgres)` block — D-04 LOCKED to AWS RDS, D-05 RDS-simplified)
    - packages/db/sql/roles.sql + packages/db/sql/auth-role.sql (the role names + grants the record must name)
    - packages/db/src/roles.ts + packages/db/src/auth-role.ts (provisionAppRole/provisionAuthRole — the path plan 06 will run)
    - packages/db/src/preflight.ts (assertNoRlsBypass + extension assertions the model must satisfy at prod boot)
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md (RDS role-model / supported-extensions notes, if present)
  </read_first>
  <action>
    Write `.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md` as a decision record — no live database, no SQL run against any instance. State the locked decision: production database = AWS RDS for PostgreSQL (D-04, LOCKED by the founder 2026-06-26; Neon declined). Record the rationale: AWS-native (same account as the ECS hosting surface), fully managed (automated backups, point-in-time recovery, patching, durable EBS-backed storage), no serverless-Postgres lock-in. Document the 3-role model on RDS exactly as the committed SQL names them: resto_app (NOBYPASSRLS, no DELETE), the admin-migration/schema-owner role (CREATEROLE + DDL + CREATE EXTENSION), and resto_auth (BYPASSRLS), under FORCE ROW LEVEL SECURITY, with the pgcrypto/citext/pg_trgm extensions taken from the RDS supported-extensions list. Document the connection model per D-05 (RDS-simplified): DATABASE_URL for the app request path and DATABASE_DIRECT_URL for the outbox leader's session-pinned advisory lock (already wired in code — plan 07.5-03); RDS has no built-in pooler, so at first-customer volume the two may be equal, with RDS Proxy added later only if connection counts demand it. Record one explicit OPEN item, verbatim in intent: "OPEN — confirm at provisioning (plan 06): on RDS the master is rds_superuser (not a true SUPERUSER) and granting BYPASSRLS normally needs superuser; confirm rds_superuser can confer BYPASSRLS on resto_auth and that pgcrypto/citext/pg_trgm are RDS-supported. If rds_superuser cannot confer BYPASSRLS on resto_auth, plan 06 HARD-STOPS and we revisit (e.g. an RDS-specific role workaround)." Do NOT run any SQL, do NOT provision anything, and do NOT require any cloud credentials — this is a written decision only.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md && grep -q 'AWS RDS' .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md && grep -q 'resto_auth' .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md && grep -q 'DATABASE_DIRECT_URL' .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md && grep -qi 'BYPASSRLS' .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md && grep -qi 'plan 06' .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - DB-PROVIDER-DECISION.md exists and names "AWS RDS" as the locked production database (D-04).
    - The record names all three roles: resto_app (NOBYPASSRLS), the admin-migration/schema-owner role, and resto_auth (BYPASSRLS), under FORCE ROW LEVEL SECURITY, with pgcrypto/citext/pg_trgm.
    - The record states the connection model: DATABASE_URL + DATABASE_DIRECT_URL (advisory lock), may be equal without a pooler, RDS Proxy optional later.
    - The record carries the explicit "OPEN — confirm at provisioning (plan 06)" BYPASSRLS-on-RDS item with the plan-06 hard-stop condition.
    - No SQL was run and no database was provisioned (this is a decision record).
  </acceptance_criteria>
  <done>DB-PROVIDER-DECISION.md written: AWS RDS locked + rationale + 3-role model + connection model + the plan-06 BYPASSRLS hard-stop open item.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                                        |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| decision record → downstream plans (06, 08)       | the recorded provider + role model + connection model becomes the contract every provisioning/secret plan consumes |
| recorded role model → production tenant isolation | the 3-role + FORCE-RLS model named here is the prod tenant-isolation fence, proven empirically in plan 06          |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                                                                               |
| ----------- | ---------------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-075-01-01 | Elevation of Privilege | resto_app role on RDS                               | mitigate    | Decision record locks resto_app as NOBYPASSRLS + not superuser; the empirical proof runs in plan 06; assertNoRlsBypass (preflight.ts) re-checks at every prod boot            |
| T-075-01-02 | Tampering              | BYPASSRLS unattainable on RDS for resto_auth        | mitigate    | Recorded as an explicit plan-06 HARD-STOP open item — provisioning proves it once against the real instance and halts if rds_superuser cannot confer it; no silent acceptance |
| T-075-01-03 | Repudiation            | provider decision made without a recorded rationale | mitigate    | The record documents the founder decision (AWS RDS, Neon declined), rationale, and the carried-forward gate so the choice is auditable                                        |
| T-075-01-SC | Tampering              | npm/pip/cargo installs                              | accept      | No package installs and no live database in this plan (decision record only); no install or DB surface                                                                        |

</threat_model>

<verification>
- DB-PROVIDER-DECISION.md exists and names AWS RDS as the locked production database.
- The 3-role model, FORCE RLS, and the three extensions are documented against the RDS supported list.
- The DATABASE_URL / DATABASE_DIRECT_URL connection model (D-05 RDS-simplified) is recorded.
- The plan-06 BYPASSRLS hard-stop open item is present.
- No live database was provisioned and no SQL was run (decision record only).
</verification>

<success_criteria>

- D-04 recorded: AWS RDS locked as the production database with rationale, the 3-role model, the connection model, and the plan-06 BYPASSRLS gate.
- Downstream plan 06 has an unambiguous provider + connection model + the single open item it must clear empirically.
  </success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
