---
phase: 07.5-production-deploy
plan: 10
type: execute
wave: 4
depends_on:
  - 07.5-06
  - 07.5-08
  - 07.5-09
files_modified:
  - infra/runbooks/backup-restore.md
  - infra/runbooks/rollback.md
  - infra/runbooks/go-live-smoke.md
  - infra/CLAUDE.md
  - infra/terraform/.gitkeep
  - infra/terraform/buckets-cors.tf
  - infra/k8s/.gitkeep
files_modified_note: 'Go-live proof runbooks + the G-07 infra-stub supersession. Depends on 07.5-06 (DB to restore), 07.5-08 (live HTTPS surface to smoke + image to roll back), 07.5-09 (CD to exercise rollback). This is the final go/no-go gate before Phase 8.'
autonomous: false
requirements: []

must_haves:
  truths:
    - 'A point-in-time restore of the production DB has been performed once, verified (row counts + last order timestamp consistent), and then torn down — RPO/RTO stated (G-02 BLOCK)'
    - 'An app-only image rollback has been proven against an expanded schema (deploy v2, roll back to v1, v1 boots on the expanded schema) — the expand/contract rollback strategy is real, not aspirational (G-06)'
    - 'All FOUR surfaces are reachable over HTTPS from outside AWS (SC#5): api /healthz 200, admin login page loads, the website renders a tenant menu over the public storefront, and the qr-menu SPA loads + renders a tenant menu on *.menu.resto.app — the public webhook surface exists before Phase 8'
    - 'The abandoned EKS/Terraform/k8s direction is explicitly superseded: infra/CLAUDE.md rewritten for ECS, infra/k8s + infra/terraform stubs removed (G-07)'
  artifacts:
    - path: 'infra/runbooks/backup-restore.md'
      provides: 'The tested restore procedure + the verified-restore evidence + RPO/RTO'
      contains: 'RPO'
    - path: 'infra/runbooks/rollback.md'
      provides: 'The expand/contract + app-only-rollback procedure + the proven-rollback evidence'
      contains: 'expand'
    - path: 'infra/runbooks/go-live-smoke.md'
      provides: 'The external end-to-end four-surface smoke (api /healthz + admin login + website tenant menu + qr-menu SPA, all from outside AWS) + the result'
      contains: 'menu'
    - path: 'infra/CLAUDE.md'
      provides: 'ECS/Fargate-oriented infra guidance superseding the EKS/Terraform/Vault narrative'
      contains: 'ECS'
  key_links:
    - from: 'external HTTPS client'
      to: 'tenant menu render'
      via: 'curl https://<slug>.menu.resto.app -> /v1/menu -> rendered menu'
      pattern: 'menu.resto.app'
    - from: 'infra/CLAUDE.md'
      to: 'ECS decisions'
      via: 'supersedes ADR-0011/EKS'
      pattern: 'ECS'
---

<objective>
Close the two go-live proof gates and prove the phase's headline outcome, then retire the dead
infra direction:

- G-02 (BLOCK): perform ONE real point-in-time restore of the production DB, verify the restored
  data is consistent (row counts + last order timestamp), tear it down, and document RPO/RTO. "No
  backups before taking money" is a fundability-killer (investor BLOCK #2).
- G-06 (MEDIUM): prove the expand/contract + app-only-rollback strategy works — deploy v2 against
  an expanded schema, roll the ECS service back to the v1 task definition, confirm v1 boots on the
  expanded schema. Migrations are forward-only (hard deletes forbidden), so rollback is never "roll
  the DB back" — this must be demonstrated, not discovered at 2am.
- SC#5: run an EXTERNAL HTTPS smoke (from outside AWS, e.g. the founder's laptop) across ALL FOUR
  surfaces — api /healthz 200, the admin login page over HTTPS, the website (public storefront)
  rendering a tenant menu, and the qr-menu SPA loading + rendering a tenant menu on
  \*.menu.resto.app — proving the full customer surface is live and the public webhook surface exists
  for Phase 8.
- G-07 (MEDIUM): supersede the abandoned EKS/Terraform direction — rewrite infra/CLAUDE.md for
  ECS, remove the infra/k8s + infra/terraform stubs. (No docs/adr/0011-\*.md file exists in the repo
  after the GSD reset — note that ADR-0011's supersession is captured here + in PROJECT.md, not via
  an ADR-file edit.)

Purpose: This is the final go/no-go gate. After it, a paying restaurant's data is recoverable, a
bad deploy is reversible, the public URL provably works, and the repo no longer points future work
at the wrong platform.
Output: three proof runbooks + the infra-stub cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md
@infra/CLAUDE.md

<interfaces>
The proof surface (extracted from codebase + research):
RESEARCH G-02: Neon PITR (branch create from a past timestamp) or RDS restore-db-instance-to-point-in-time; verify tenant count + last order timestamp; then delete the restore. RPO <= 24h, RTO <= 4h targets.
RESEARCH G-06: no down-migrations; rollback = ECS deploy previous task definition revision (app-only); pre-go-live proof = deploy v1 -> migrate -> deploy v2 -> roll back to v1 -> verify v1 boots on expanded schema.
RESEARCH SC-5 / Validation map: curl -f https://api.resto.app/healthz (manual post-deploy) + the tenant-menu render.
G-07 files: infra/CLAUDE.md (rewrite for ECS; drop k8s/terraform/Vault), infra/k8s/.gitkeep (remove), infra/terraform/.gitkeep + buckets-cors.tf (remove). docs/adr/0011-*.md does NOT exist (post-reset) — supersession noted, not file-edited.
docs/runbooks/menu-edge-caching.md: the public menu read contract the external smoke exercises.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Perform + verify a point-in-time restore (G-02) and an app-only rollback (G-06)</name>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md sections "G-02: Backup / Restore" (the provider-specific restore commands) + "G-06: Expand/contract rollback"
    - .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md — which restore mechanism applies (Neon PITR vs RDS PITR)
    - infra/runbooks/cd-pipeline.md (07.5-09) — how to roll the ECS service to a previous task-definition revision
  </read_first>
  <what-built>The live prod DB (07.5-06), the live ECS services (07.5-08), and the CD pipeline (07.5-09) — this checkpoint exercises their recovery paths once, for real.</what-built>
  <how-to-verify>
    1. RESTORE: using the chosen provider's PITR (Neon branch-from-timestamp or RDS restore-to-point-in-time), restore to ~1 hour ago into a throwaway target. Connect; confirm tenant count and the last order timestamp are consistent with expectations. Then delete the restore target. Record the wall-clock time the restore took (RTO evidence).
    2. ROLLBACK: ensure a schema-expanding migration is live (an additive, nullable column is fine — the existing migrations already expanded the schema relative to any earlier image). Deploy the current image (v2). Then roll the ECS api service back to the previous task-definition revision (v1) and confirm v1 boots and serves /healthz 200 against the expanded schema (no down-migration run). Roll forward again to v2.
    3. Capture both transcripts.
  </how-to-verify>
  <acceptance_criteria>
    - A PITR restore was performed, verified consistent (tenant count + last order timestamp), and torn down; the restore wall-clock time is recorded against the RTO target.
    - An app-only rollback to the previous task-definition revision booted successfully on the expanded schema (no DB rollback); transcript captured.
    - Both are reproducible from the runbooks Task 2 writes.
  </acceptance_criteria>
  <resume-signal>Paste the restore-verification transcript (tenant count + last order ts) and the rollback /healthz-200 transcript, then type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write backup-restore.md + rollback.md from the proven procedures</name>
  <files>infra/runbooks/backup-restore.md, infra/runbooks/rollback.md</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md sections "G-02" + "G-06"
    - the transcripts captured in Task 1
  </read_first>
  <action>
    Write infra/runbooks/backup-restore.md: the provider-specific PITR command, the verification
    queries (tenant count + last order timestamp), the teardown step, the stated RPO (<= 24h) and
    RTO (<= 4h, with the measured restore time as evidence), and the cadence. Write
    infra/runbooks/rollback.md: the expand/contract rule (forward-only, additive migrations; a new
    NOT NULL or column drop is a separate later migration; never roll the DB back), the app-only
    rollback procedure (ECS deploy previous task-definition revision), the assertTenantLockInstalled
    safety net, and the proven-rollback evidence. No secrets.
  </action>
  <verify>
    <automated>test -f infra/runbooks/backup-restore.md && test -f infra/runbooks/rollback.md && grep -q RPO infra/runbooks/backup-restore.md && grep -q expand infra/runbooks/rollback.md</automated>
  </verify>
  <acceptance_criteria>
    - backup-restore.md states RPO/RTO with the measured restore time and the verification queries.
    - rollback.md documents expand/contract + app-only rollback + the safety net, with the proven evidence.
    - No secrets in either file.
  </acceptance_criteria>
  <done>Recovery and rollback are documented, reproducible, and proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: External end-to-end smoke — all four surfaces reachable over HTTPS (SC#5)</name>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md Validation map (SC-5 row) + docs/runbooks/menu-edge-caching.md (the public menu read contract)
  </read_first>
  <what-built>The full live stack (DB + NATS + R2 + ECS api/admin/website + qr-menu static + Cloudflare). This checkpoint proves an outside-world request reaches every customer surface and renders a real tenant menu.</what-built>
  <how-to-verify>
    1. From OUTSIDE AWS (founder's laptop / phone on cellular), curl -f https://api.resto.app/healthz -> 200.
    2. admin: load https://admin.resto.app over HTTPS and confirm the login page renders (the operator surface is live; no auth bypass needed — just that it serves).
    3. website (public storefront): load https://resto.app (or the seeded tenant's storefront route) and confirm a published tenant menu renders — items + prices come back (checkout stays disabled until Phase 8; the surface must still be live).
    4. qr-menu SPA: hit https://<slug>.menu.resto.app and confirm the static SPA loads AND its same-origin /v1/menu fetch returns a tenant menu that renders end-to-end — items + prices, and the menu response carries the expected Cache-Control/ETag (no Set-Cookie) per the menu-edge-caching contract.
    5. Confirm the webhook path is reachable: an external POST to the eventual Stripe webhook host (api.resto.app) returns a controlled response (not a connection failure) — proving Phase 8 will have a public URL to call.
  </how-to-verify>
  <acceptance_criteria>
    - External curl to https://api.resto.app/healthz returns 200 from outside AWS.
    - admin (https://admin.resto.app) and website (https://resto.app) both load over HTTPS from outside AWS; the website renders a tenant menu.
    - The qr-menu SPA loads on https://<slug>.menu.resto.app and its same-origin /v1/menu fetch renders a tenant menu with the correct Cache-Control/ETag and no Set-Cookie.
    - The public API host is reachable from the outside for the future Stripe webhook (no connection failure).
  </acceptance_criteria>
  <resume-signal>Paste the external /healthz 200, the admin + website HTTPS loads, the website + qr-menu tenant-menu render evidence (items + headers), then type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Supersede the abandoned EKS/Terraform direction (G-07) + write go-live-smoke.md</name>
  <files>infra/CLAUDE.md, infra/terraform/.gitkeep, infra/terraform/buckets-cors.tf, infra/k8s/.gitkeep, infra/runbooks/go-live-smoke.md</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section "G-07: ADR-0011 / infra stubs Supersession" (the file table)
    - infra/CLAUDE.md — the current k8s/terraform/Vault references to remove
  </read_first>
  <action>
    Rewrite infra/CLAUDE.md for the ECS/Fargate reality: point at the new runbooks
    (db-provisioning, nats-ec2, r2-storage, ecs-alb-cloudflare, secrets-inventory, cd-pipeline,
    backup-restore, rollback), drop the k8s/Terraform/Vault narrative, and state that Phase 7.5
    supersedes the EKS/ADR-0011 direction (note: no docs/adr/0011 file exists post-reset, so the
    supersession is recorded here + in PROJECT.md Key Decisions, not via an ADR-file edit). Remove
    the infra/k8s stub (infra/k8s/.gitkeep) and the infra/terraform stubs (.gitkeep +
    buckets-cors.tf) — the bucket CORS now lives in r2-storage.md. Write
    infra/runbooks/go-live-smoke.md documenting the external end-to-end smoke from Task 3 and the
    go/no-go checklist (the consolidated set of proofs: green container, DB preflights, backups
    tested, rollback proven, all four surfaces reachable over HTTPS — api /healthz, admin login,
    website tenant menu, qr-menu SPA — and error tracker live). Also append a one-line
    Key Decision to PROJECT.md if that is the convention (Phase 7.5: ECS/Fargate prod, supersedes
    ADR-0011 EKS).
  </action>
  <verify>
    <automated>test -f infra/runbooks/go-live-smoke.md && grep -q ECS infra/CLAUDE.md && test ! -e infra/k8s/.gitkeep && test ! -e infra/terraform/buckets-cors.tf</automated>
  </verify>
  <acceptance_criteria>
    - infra/CLAUDE.md is ECS-oriented, references the new runbooks, and states the ADR-0011/EKS supersession.
    - infra/k8s and infra/terraform stubs are removed.
    - go-live-smoke.md documents the external smoke + the consolidated go/no-go checklist.
  </acceptance_criteria>
  <done>The repo points future work at ECS; the go-live proof set is consolidated; the EKS direction is retired.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                    |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| external client -> public HTTPS API | The outside-world ingress proven here is the surface Phase 8 webhooks will use |
| restore target -> prod data         | A restore is a copy of real customer data; must be torn down, not left exposed |

## STRIDE Threat Register

| Threat ID | Category                                      | Component                                            | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | --------------------------------------------- | ---------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-075-27  | Tampering/Integrity (unrecoverable data loss) | no tested restore on the revenue DB                  | mitigate    | One real PITR restore performed + verified + torn down; RPO/RTO documented (G-02 BLOCK closed)                                                                             |
| T-075-28  | Denial of Service                             | a bad deploy with no reversible path                 | mitigate    | App-only rollback to the previous task-definition revision proven against the expanded schema (G-06)                                                                       |
| T-075-29  | Information Disclosure                        | leftover restore copy of customer data               | mitigate    | Restore target deleted immediately after verification; runbook makes teardown a required step                                                                              |
| T-075-30  | Spoofing/Repudiation                          | public menu reads leaking Set-Cookie / going private | mitigate    | External four-surface smoke asserts the menu-edge-caching contract (Cache-Control public, ETag, no Set-Cookie) holds in prod on both the website and the qr-menu /v1 reads |

</threat_model>

<verification>
- A PITR restore was performed, verified, and torn down; RPO/RTO stated.
- App-only rollback proven on the expanded schema.
- External HTTPS reaches all four surfaces (api /healthz, admin login, website tenant menu, qr-menu SPA menu render).
- infra/CLAUDE.md ECS-oriented; k8s/terraform stubs removed; go-live-smoke.md written.
</verification>

<success_criteria>
The revenue path is recoverable (G-02), reversible (G-06), and provably reachable from the public internet end-to-end across all four surfaces (api/admin/website/qr-menu) (SC#5); the dead EKS direction is retired (G-07). The phase is go-live ready for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-10-SUMMARY.md` when done. Paste the restore-verification, rollback, and external-menu-render transcripts; list the consolidated go/no-go checklist result.
</output>
