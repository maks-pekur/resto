# Superseded Phase 7.5 plans (2026-06-21)

These 9 unexecuted plans were written **before** the admin → Vite/static migration (Phase 7.6)
and treat `apps/admin` as an ECS/Fargate service with its own Dockerfile. They are **stale**.

Re-planned on **2026-06-26** (founder decision) against the four-surface, admin-static scope —
see `../07.5-CONTEXT.md` → `<domain>` → "SCOPE UPDATE (2026-06-26)". The fresh plans live in the
parent directory and reuse these freed numbers (`01`, `03`–`10`).

**Not archived (still valid, executed):** `07.5-02` (esbuild Docker-boot fix) and the website
Dockerfile from `07.5-11`. The admin-Dockerfile half of `07.5-11` is superseded.

Kept for history only — do not execute.
