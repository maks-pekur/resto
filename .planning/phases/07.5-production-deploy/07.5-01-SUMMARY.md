---
phase: 07.5-production-deploy
plan: '01'
subsystem: database
tags: [postgres, rds, aws, rls, roles, decision-record]

# Dependency graph
requires: []
provides:
  - DB-PROVIDER-DECISION.md naming AWS RDS as the locked production database (D-04)
  - 3-role model documented (resto_app NOBYPASSRLS, resto_admin DDL, resto_auth BYPASSRLS)
  - Connection model documented (DATABASE_URL + DATABASE_DIRECT_URL, RDS Proxy optional)
  - Plan 06 BYPASSRLS hard-stop gate recorded
affects: [07.5-06, 07.5-08, 07.5-09, 07.5-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 3-role Postgres + FORCE RLS decision recorded as a durable artifact with explicit plan-06 gate

key-files:
  created:
    - .planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md
  modified: []

key-decisions:
  - 'D-04 LOCKED: AWS RDS for PostgreSQL is the production database (Neon declined for advisory-lock footgun)'
  - 'D-05 confirmed: DATABASE_DIRECT_URL separation kept; at first-customer volume may equal DATABASE_URL (no RDS pooler); RDS Proxy optional later'
  - 'BYPASSRLS-on-RDS is an empirical open item deferred to plan 06 — plan 06 HARD-STOPS if rds_superuser cannot confer BYPASSRLS on resto_auth'

patterns-established:
  - 'Decision record pattern: locked provider choice + rationale + 3-role model + connection model + explicit plan-N hard-stop gate'

requirements-completed: [D-04]

# Metrics
duration: 8min
completed: 2026-06-26
---

# Phase 07.5 Plan 01: DB-PROVIDER-DECISION Summary

**AWS RDS for PostgreSQL locked as the production database (D-04), with the 3-role model (resto_app/resto_admin/resto_auth), connection model (DATABASE_URL + DATABASE_DIRECT_URL), and explicit plan-06 BYPASSRLS hard-stop gate documented.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-06-26T17:10:00Z
- **Completed:** 2026-06-26T17:18:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Written `DB-PROVIDER-DECISION.md` recording the locked AWS RDS decision with full rationale, the 3-role model named against committed SQL files, the connection model, and the plan-06 BYPASSRLS gate
- Automated verify grep chain (AWS RDS / resto_auth / DATABASE_DIRECT_URL / BYPASSRLS / plan 06) passes cleanly
- Decision record committed atomically with conventional commit; no cloud credentials touched, no SQL run

## Task Commits

1. **Task 1: Write DB-PROVIDER-DECISION.md** — `f1b8aa1` (docs)

## Files Created/Modified

- `.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md` — locked DB provider decision record (AWS RDS, D-04): rationale, 3-role model with exact role names from committed SQL, connection model (D-05), boot preflight summary, plan-06 BYPASSRLS hard-stop open item

## Decisions Made

- **AWS RDS over Neon:** Neon's serverless model creates a transaction-mode pooler footgun on the session-pinned advisory lock; RDS is standard Postgres on direct connections — no workaround needed and the `DATABASE_DIRECT_URL` / `DATABASE_URL` separation stays conceptually clean for a future RDS Proxy
- **No throwaway-DB spike:** The BYPASSRLS-on-RDS question is confirmed empirically once against the real instance in plan 06 — a separate spike against a throwaway DB adds cost for a question AWS documentation already answers; the empirical confirmation doubles as the provisioning proof
- **Plan 06 is the empirical gate:** If `rds_superuser` cannot confer `BYPASSRLS` on `resto_auth`, plan 06 hard-stops and we revisit — the open item is recorded verbatim so it cannot be silently skipped

## Deviations from Plan

None — plan executed exactly as written. Written decision only; no SQL run, no infrastructure provisioned.

## Issues Encountered

None.

## User Setup Required

None — this plan is a decision record only. No external services touched.

## Next Phase Readiness

- Plan 07.5-06 (Postgres provisioning) now has an unambiguous provider, 3-role model, and connection model to execute against
- The single open item (BYPASSRLS-on-RDS empirical confirmation) is plan 06's first gate
- Plans 07.5-08 (secrets/ECS) and 07.5-09 (CD) consume `DATABASE_URL` / `DATABASE_DIRECT_URL` / `DATABASE_ADMIN_URL` as defined in this record

---

_Phase: 07.5-production-deploy_
_Completed: 2026-06-26_
