---
phase: 07.5-production-deploy
plan: '06'
type: execute
wave: 1
depends_on: ['07.5-02', '07.5-05']
files_modified:
  - infra/docker/docker-compose.prod.yml
  - infra/docker/Caddyfile
  - infra/docker/env/compose.env.example
  - infra/docker/env/api.env.example
  - infra/docker/env/website.env.example
  - infra/scripts/render-env.sh
  - infra/scripts/assert-no-destructive-compose.sh
  - infra/scripts/local-prod-rehearsal.sh
  - infra/scripts/backup-nightly.sh
  - infra/scripts/restore-drill.sh
  - apps/api/Dockerfile
  - docs/runbooks/production-stack.md
  - docs/runbooks/database-backup-restore.md
autonomous: false
requirements: ['SC#1', 'SC#4', 'G-02', 'D-01', 'D-03', 'D-04', 'D-05', 'D-06', 'D-09', 'PREREQ-APEX']
user_setup:
  - service: apex-domain
    why: 'PREREQ-APEX — every hostname this phase produces is derived from one apex domain the founder owns and has delegated to Cloudflare. No plan in 06-10 writes a domain literal; all of them read this value.'
    env_vars:
      - name: PUBLIC_APEX_DOMAIN
        source: 'The bare apex the founder owns (no scheme, no port). Recorded as a GitHub Actions repository VARIABLE (not a secret — it is public) and as the first line of /opt/resto/.env on the VPS. Never committed.'
    dashboard_config:
      - task: 'Register/confirm ownership of the apex domain and delegate its nameservers to a Cloudflare zone'
        location: 'Domain registrar -> nameservers; Cloudflare dashboard -> Add a site'
  - service: hetzner-cloud
    why: 'D-01 — the single VPS that runs the compose stack (provisioned for real in plan 08; the account must exist before then)'
    env_vars: []
    dashboard_config:
      - task: 'Create a Hetzner Cloud account and a project; no server yet (plan 08 provisions it)'
        location: 'console.hetzner.cloud'
  - service: stripe
    why: 'Pitfall #10 — assertProdGuardrails refuses to boot the api at all in NODE_ENV=production without a non-placeholder STRIPE_SECRET_KEY. Test-mode keys are accepted. Gathered NOW, not at the go-live smoke.'
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: 'Stripe Dashboard -> Developers -> API keys (test-mode sk_test_... is acceptable for initial go-live; the documented placeholder literal is rejected)'
      - name: STRIPE_WEBHOOK_SECRET
        source: 'Stripe Dashboard -> Developers -> Webhooks -> signing secret'
      - name: STRIPE_CONNECT_CLIENT_ID
        source: 'Stripe Dashboard -> Settings -> Connect -> Platform settings'
    dashboard_config: []
  - service: resend
    why: 'Pitfall #10 — assertProdGuardrails also refuses to boot without a real RESEND_API_KEY and a Resend adapter'
    env_vars:
      - name: RESEND_API_KEY
        source: 'Resend Dashboard -> API Keys'
    dashboard_config:
      - task: 'Verify a sending domain on the apex (SPF/DKIM/DMARC records — infra/runbooks/spf-dkim-dmarc-checklist.md already documents the record set)'
        location: 'Resend Dashboard -> Domains'

must_haves:
  truths:
    - 'The production runtime is described by committed files (docker-compose.prod.yml + Caddyfile + env templates) — no hand-typed server state defines what runs'
    - 'None of those files contain any domain literal: every hostname is derived from PUBLIC_APEX_DOMAIN at render time'
    - 'The only container publishing a port to the VPS public interface is caddy (80/443); postgres and nats publish nothing'
    - 'No committed script, cron entry or workflow can destroy the postgres-data volume — `docker compose down -v` appears nowhere and a guard script proves it'
    - 'Migrations and role provisioning run from a dedicated one-off image, not from the long-running api container and not from the CI runner'
    - 'The whole stack has been brought up locally end to end: three roles provisioned, all migrations applied, all six boot preflights green under NODE_ENV=production, and /healthz served through Caddy by Host header'
    - 'A dump taken from that stack restores into a scratch database and the restored database passes structural verification'
    - 'The founder has the apex domain, the Cloudflare zone, the Hetzner account, and real Stripe + Resend credentials in hand before any later plan needs them'
  artifacts:
    - path: 'infra/docker/docker-compose.prod.yml'
      provides: 'The production runtime: postgres + nats + api + website + caddy + a profile-gated migrate service'
      contains: 'postgres-data'
    - path: 'infra/docker/Caddyfile'
      provides: 'TLS termination with the Cloudflare Origin CA cert + Host-based routing to api/website'
      contains: '{$API_HOST}'
    - path: 'infra/scripts/assert-no-destructive-compose.sh'
      provides: 'The Pitfall #7 guard — fails if any committed automation can remove a named volume'
      contains: 'down -v'
    - path: 'infra/scripts/restore-drill.sh'
      provides: 'The reusable restore rehearsal: fetch dump -> scratch postgres -> pg_restore -> re-provision roles -> verify -> report observed RTO'
      contains: 'pg_restore'
    - path: 'apps/api/Dockerfile'
      provides: 'A `migrate` build stage carrying tsx + packages/db sources + packages/db/node_modules so db:migrate and provision-roles can run as a one-off container'
      contains: 'AS migrate'
  key_links:
    - from: 'infra/docker/docker-compose.prod.yml'
      to: 'infra/docker/Caddyfile'
      via: 'caddy service bind-mounts the Caddyfile and receives API_HOST/WEBSITE_HOST from the compose env'
      pattern: 'Caddyfile'
    - from: 'infra/scripts/render-env.sh'
      to: 'infra/docker/env/*.env.example'
      via: 'envsubst expands ${PUBLIC_APEX_DOMAIN} into the real .env files on the box'
      pattern: 'envsubst'
    - from: 'infra/scripts/local-prod-rehearsal.sh'
      to: 'packages/db/src/cli/provision-roles-ci.ts + packages/db/src/cli/migrate.ts'
      via: 'the migrate service runs the already-committed provisioning CLIs, not a new script'
      pattern: 'provision-roles-ci|db:migrate'
---

<objective>
Turn the production runtime into committed, domain-parameterized files and prove the
entire shape works before a single euro is spent on a server.

Purpose: everything plans 08-10 do to a real box is a *deployment* of what this plan
defines. If the compose stack, the role provisioning, the preflights and the restore
path are only first exercised against production, every one of them is a live
experiment. They are all rehearsable locally; this plan rehearses them.

Output: `infra/docker/docker-compose.prod.yml`, `infra/docker/Caddyfile`, three env
templates plus the renderer that expands them, a destructive-command guard, a
`migrate` image stage, the backup + restore-drill scripts, and two runbooks — plus a
recorded local transcript proving the whole sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md

Read the SCOPE UPDATE banners at the top of `07.5-CONTEXT.md` before the decision
bodies. The bodies of D-01, D-03, D-04, D-05, D-06, D-09 and D-10 describe an AWS
design that was abandoned on 2026-06-26. `07.5-RESEARCH.md`'s reconciled-decision
table is authoritative. **If anything you write mentions ECS, Fargate, RDS, ALB,
Terraform, AWS Secrets Manager or Kubernetes, you have read a dead decision body.**

@infra/docker/docker-compose.dev.yml
@apps/api/Dockerfile
@apps/api/src/config/env.schema.ts
@apps/api/src/config/prod-guardrails.ts

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. Do not go looking. -->

Boot preflight order — `apps/api/src/main.ts`, all must pass before Nest mounts:
```
assertNoRlsBypass(env.DATABASE_URL)
assertTenantLockInstalled(env.DATABASE_URL)
assertSetConfigRevoked(env.DATABASE_URL)
assertNoBaCredentialAccess(env.DATABASE_URL)
assertInboxProcessedDeletable(env.DATABASE_URL)
assertWithoutTenantCallsiteRegistered()
assertSystemRolesPresent()
assertProdGuardrails(env)                 // NODE_ENV !== development/test only
assertProdGuardrails(env, { emailAdapterName })
```

Provisioning + migration CLIs already committed — reuse, do not reimplement:
```
packages/db/src/cli/provision-roles-ci.ts   // needs DATABASE_ADMIN_URL, APP_ROLE_PASSWORD, AUTH_ROLE_PASSWORD
packages/db/src/cli/migrate.ts              // pnpm --filter @resto/db db:migrate; cwd must be packages/db
                                            // (migrationsFolder is './migrations', relative to cwd)
```
`packages/db/src/roles.ts` reads `../sql/roles.sql` with `readFileSync` at call time
(10.2-FINDINGS F-15) — so both CLIs run correctly under `tsx`, and the migrate image
must ship `packages/db/sql/` and `packages/db/migrations/` as real files.

Env vars `env.schema.ts` requires outside development/test (superRefine):
```
BETTER_AUTH_SECRET, BETTER_AUTH_BASE_URL, BETTER_AUTH_DATABASE_URL,
ADMIN_WEB_URL, WEBSITE_PUBLIC_URL, AUTH_COOKIE_DOMAIN, AUDIT_ERASURE_SALT,
TRUST_PROXY, INTERNAL_API_TOKEN, DATABASE_DIRECT_URL
```
Shape constraints that bite:
- `AUTH_COOKIE_DOMAIN` MUST start with `.` (Zod refine) — e.g. `.admin.<apex>`.
- `ADMIN_WEB_ORIGIN_WILDCARD` MUST be `https://*.<label>.<label>` shape.
- `PUBLIC_APEX_DOMAIN` MUST be a bare hostname, no scheme/port/path.
- `TRUST_PROXY=true` is REJECTED outside dev/test — supply a CIDR list.
- `assertProdGuardrails` rejects any of `MEDIA_PUBLIC_BASE_URL`, `S3_ENDPOINT`,
  `ADMIN_WEB_URL`, `WEBSITE_PUBLIC_URL`, `BETTER_AUTH_BASE_URL`,
  `STRIPE_CONNECT_RETURN_URL`, `STRIPE_CONNECT_REFRESH_URL`, `PUBLIC_APEX_DOMAIN`
  whose host is loopback / `*.devtunnels.ms` / `*.nip.io` / `*.lvh.me` /
  `*.trycloudflare.com` / ngrok. `.invalid` hosts are NOT rejected — that is what the
  local rehearsal uses.

api runtime image facts (`apps/api/Dockerfile`) that constrain the migrate mechanism:
- runtime stage copies `/workspace/node_modules`, `/workspace/apps/api/node_modules`,
  `/workspace/packages`, `apps/api/dist` — but NOT `packages/db/node_modules`, and it
  has no `pnpm`. `tsx packages/db/src/cli/migrate.ts` therefore CANNOT resolve
  `drizzle-orm`/`postgres` from the runtime image. A separate stage is required.
- root `node_modules/.bin/tsx` exists in the `deps` stage.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Founder prerequisites — apex domain, accounts, boot-blocking credentials</name>
  <what-built>Nothing yet. This is the named prerequisite (PREREQ-APEX) for the whole 06-10 set: five inputs only the founder can produce, two of which will otherwise stop the api container from booting at the very first go-live attempt (Pitfall #10).</what-built>
  <how-to-verify>
    Produce all five, then run the checks:

    1. **Apex domain, delegated to Cloudflare.** Decide the bare apex (no scheme, no
       port). Every hostname in this phase is derived from it:
       `api.<apex>`, `<apex>` + `*.<apex>`, `admin.<apex>` + `*.admin.<apex>`,
       `*.menu.<apex>`. Note that `resto.app` appears throughout the repo only as a
       test fixture; ownership has never been confirmed. If the founder does not own
       it, pick what is actually owned — nothing in plans 06-10 cares which.
       Check: `dig +short NS <apex>` lists Cloudflare nameservers.
    2. **Record the apex where automation reads it**, never in a committed file:
       `gh variable set PUBLIC_APEX_DOMAIN --body '<apex>'`
       Check: `gh variable get PUBLIC_APEX_DOMAIN` echoes it back.
    3. **Hetzner Cloud account + project** (no server yet — plan 08 creates it).
       Check: the founder can reach console.hetzner.cloud and see an empty project.
    4. **Stripe credentials** (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`,
       `STRIPE_CONNECT_CLIENT_ID`). Test-mode keys are acceptable for the initial
       go-live; `assertProdGuardrails` rejects only the documented placeholder literal
       and empty values.
       Check: `curl -s -o /dev/null -w '%{http_code}\n' https://api.stripe.com/v1/account -u "$STRIPE_SECRET_KEY:"` prints `200`.
    5. **Resend API key + a verified sending domain on the apex.**
       Check: `curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains` lists the apex with status `verified`.
    6. **A deploy-only SSH keypair** (not a personal key), e.g.
       `ssh-keygen -t ed25519 -f ~/.ssh/resto_deploy -C resto-deploy -N ''`.
       Check: `ls ~/.ssh/resto_deploy.pub` exists.

    Do NOT paste any secret value into the repo, into a plan, or into the summary.
    Only confirmation that each check passed.
  </how-to-verify>
  <resume-signal>Type "approved" plus the apex domain (the domain itself is not a secret and later tasks need to know its shape), or describe which check failed.</resume-signal>
  <action>Pause and present the six items above to the founder. Do not proceed to Task 2 until every one is confirmed — Task 3 boots the api under NODE_ENV=production, and the two credential items are what `assertProdGuardrails` refuses to start without. Record only that each check passed; never the values.</action>
  <verify>
    <automated>test -n "$(gh variable get PUBLIC_APEX_DOMAIN)" && dig +short NS "$(gh variable get PUBLIC_APEX_DOMAIN)" | grep -qi cloudflare && test -f "$HOME/.ssh/resto_deploy.pub"</automated>
  </verify>
  <done>`PUBLIC_APEX_DOMAIN` is set as a repository variable, its nameservers are Cloudflare's, a deploy-only SSH public key exists, and the founder has confirmed the Hetzner account plus working Stripe and Resend credentials (checks 3-5, which need secrets in the shell and so stay in the founder's hands).</done>
</task>

<task type="auto">
  <name>Task 2: Production compose stack, Caddy config, env templates, and the migrate image stage</name>
  <files>infra/docker/docker-compose.prod.yml, infra/docker/Caddyfile, infra/docker/env/compose.env.example, infra/docker/env/api.env.example, infra/docker/env/website.env.example, infra/scripts/render-env.sh, infra/scripts/assert-no-destructive-compose.sh, apps/api/Dockerfile</files>
  <action>
    Write the production runtime as committed files, parameterized on `PUBLIC_APEX_DOMAIN`.

    **`infra/docker/docker-compose.prod.yml`** — sibling of the dev compose, project
    name `resto-prod`, following RESEARCH.md Pattern 1 and its skeleton. Services:
    `postgres` (postgres:16-alpine, named volume `postgres-data`, `pg_isready`
    healthcheck, command flags for `shared_buffers=1GB`, `effective_cache_size=3GB`,
    `max_connections=40` per the CX32 starting values in Pattern 2 — flag them in the
    runbook as starting values to revisit against `pg_stat_activity`, not measured
    numbers); `nats` (nats:2.10-alpine, `--jetstream --store_dir=/data --http_port=8222
    --user=${NATS_USERNAME} --pass=${NATS_PASSWORD}`, named volume `nats-data`);
    `api` (`ghcr.io/${GHCR_OWNER}/resto-api:${API_IMAGE_TAG:-latest}`, `env_file:
    .env.api`, depends_on postgres+nats `service_healthy`); `website`
    (`ghcr.io/${GHCR_OWNER}/resto-website:${WEBSITE_IMAGE_TAG:-latest}`, `env_file:
    .env.website`); `caddy` (caddy:2-alpine, the ONLY service with `ports:`, exactly
    `80:80` and `443:443`, bind-mounting `./Caddyfile` read-only and `./certs`
    read-only, `environment:` carrying `API_HOST`, `WEBSITE_HOST`); and `migrate`
    (`ghcr.io/${GHCR_OWNER}/resto-migrate:${API_IMAGE_TAG:-latest}`, `profiles:
    ["tools"]` so `up -d` never starts it, no `restart:` policy, no `env_file` —
    `DATABASE_ADMIN_URL` is injected per-invocation and never stored on the box).
    `restart: unless-stopped` on every long-running service. Zero `ports:` outside
    `caddy`. Add the two-line WHY comment the research asks for above the `volumes:`
    block: plain `down` preserves the volumes, `-v` destroys `postgres-data`, and no
    script may ever pass it.

    **`infra/docker/Caddyfile`** — uses Caddy's own env placeholders so no hostname is
    written down: a site block for `{$API_HOST}` reverse-proxying `api:3000`, and a
    block for `{$WEBSITE_HOST}, *.{$WEBSITE_HOST}` reverse-proxying `website:3002`.
    Both present the Cloudflare Origin CA pair via the `tls` directive pointed at
    `/etc/caddy/certs/origin.pem` and `/etc/caddy/certs/origin-key.pem` (D-08: a
    15-year Origin CA cert, no ACME, no renewal cron — do not enable Caddy's automatic
    HTTPS). Disable the admin API (`admin off`).

    **Env templates under `infra/docker/env/`.** Name them `compose.env.example`,
    `api.env.example`, `website.env.example` — NOT `.env.*.example`: the root
    `.gitignore` line `.env.*` swallows that shape and the negation only rescues a file
    named exactly `.env.example`. Each template writes every hostname as
    `${PUBLIC_APEX_DOMAIN}` and every secret as an obvious `CHANGE_ME_*` token (the
    gitleaks CI job scans this repo — no realistic-looking key material).
    `compose.env.example` carries what Compose itself interpolates:
    `PUBLIC_APEX_DOMAIN`, `GHCR_OWNER`, `API_IMAGE_TAG`, `WEBSITE_IMAGE_TAG`,
    `API_HOST=api.${PUBLIC_APEX_DOMAIN}`, `WEBSITE_HOST=${PUBLIC_APEX_DOMAIN}`,
    `POSTGRES_ADMIN_USER`, `POSTGRES_ADMIN_PASSWORD`, `POSTGRES_DB`, `NATS_USERNAME`,
    `NATS_PASSWORD`. `api.env.example` carries the full runtime env from Pattern 5's
    secret inventory: `NODE_ENV=production`, `DATABASE_URL`, `DATABASE_DIRECT_URL`
    (per D-05 they may be the identical string — no pooler exists), the three role
    connection strings, `BETTER_AUTH_*`, `AUDIT_ERASURE_SALT`, `INTERNAL_API_TOKEN`,
    `NATS_*`, `S3_*` + `MEDIA_PUBLIC_BASE_URL` (R2), `SENTRY_DSN`, `RESEND_API_KEY`,
    `STRIPE_*`, `TRUST_PROXY` as a CIDR list (never `true` — the schema rejects it
    outside dev/test), `CORS_ALLOWED_ORIGINS`,
    `AUTH_COOKIE_DOMAIN=.admin.${PUBLIC_APEX_DOMAIN}`,
    `ADMIN_WEB_URL=https://admin.${PUBLIC_APEX_DOMAIN}`,
    `ADMIN_WEB_ORIGIN_WILDCARD=https://*.admin.${PUBLIC_APEX_DOMAIN}`,
    `WEBSITE_PUBLIC_URL`, `PUBLIC_APEX_DOMAIN`, `OTEL_DISABLED=true`. Deliberately
    ABSENT from `api.env.example`: `DATABASE_ADMIN_URL` — admin credentials must never
    sit as a standing file on the box (Pattern 5). Add that as a one-line comment at
    the bottom of the template, because its absence is otherwise indistinguishable
    from an oversight. `website.env.example` carries `WEBSITE_URL` and
    `NEXT_PUBLIC_API_ORIGIN` with a note that the latter is baked at image build time
    and the runtime copy exists only so a mismatch is visible.

    **`infra/scripts/render-env.sh`** — the parameterization contract made executable.
    Takes `PUBLIC_APEX_DOMAIN` (and the other substitutions) from the caller's
    environment and `envsubst`s a named template into a target path with mode 600.
    Refuses to run if `PUBLIC_APEX_DOMAIN` is unset or still contains a placeholder,
    and refuses to overwrite an existing target unless `--force` is passed (so a
    redeploy cannot silently clobber rotated secrets). Compose's `env_file:` does no
    variable expansion — this script is why the templates can hold `${...}` at all.

    **`infra/scripts/assert-no-destructive-compose.sh`** — the Pitfall #7 gate. Scans
    `infra/scripts/*.sh`, `infra/docker/*.yml`, `.github/workflows/*.yml` and
    `docs/runbooks/*.md` for `docker compose down` / `docker-compose down` in any form
    and for a `-v`/`--volumes` flag near a compose invocation. Strip comment lines
    (`grep -v '^[[:space:]]*#'`) BEFORE counting — the compose file deliberately
    carries the phrase in a warning comment, and a naive counter would make that
    comment self-invalidating. Exit non-zero with the offending file:line on any hit.
    Deliberately does not scan `package.json`: `pnpm dev:reset` uses `-v` by design
    against the dev stack.

    **`apps/api/Dockerfile`** — add a `migrate` stage after `build`. It exists because
    the runtime stage cannot run the CLIs: it ships neither `pnpm` nor
    `packages/db/node_modules`, so `drizzle-orm`/`postgres` are unresolvable from
    `packages/db/src/cli/*`. The new stage copies root `node_modules` (for
    `.bin/tsx`), `packages/db/node_modules`, `tsconfig.base.json` and the built
    `packages` tree, sets `WORKDIR /workspace/packages/db` (migrate.ts resolves
    `./migrations` from cwd) and defaults to running `migrate.ts` under tsx. Leave the
    existing `runtime` stage the final one so a plain `docker build` still produces the
    api image unchanged.

    Do not add narrative comments to any of these files beyond the two WHY comments
    named above (compose `-v` warning, `api.env.example`'s deliberate
    `DATABASE_ADMIN_URL` omission) — repo rule.
  </action>
  <verify>
    <automated>bash infra/scripts/assert-no-destructive-compose.sh && docker compose -f infra/docker/docker-compose.prod.yml --env-file infra/docker/env/compose.env.example config --format json | python3 -c "import json,sys; s=json.load(sys.stdin)['services']; p=sorted(k for k,v in s.items() if v.get('ports')); print('published:',p); sys.exit(0 if p==['caddy'] else 1)" && test -z "$(git check-ignore infra/docker/env/compose.env.example infra/docker/env/api.env.example infra/docker/env/website.env.example)" && test -z "$(grep -rnoE '[a-z0-9-]+\.(app|com|dev|io|net|org)' infra/docker/docker-compose.prod.yml infra/docker/Caddyfile infra/docker/env/ | grep -vE 'ghcr\.io|docker\.io')" && docker build -f apps/api/Dockerfile --target migrate -t resto-migrate:local . && docker run --rm resto-migrate:local /workspace/node_modules/.bin/tsx --version</automated>
  </verify>
  <done>`docker compose config` renders; `caddy` is the only service publishing ports; all three env templates are actually tracked by git (not swallowed by `.gitignore`); no domain literal survives in any of the new infra files; the guard script passes; the `migrate` target builds and its `tsx` runs.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the whole stack locally — boot, provision, migrate, preflight, back up, restore</name>
  <files>infra/scripts/local-prod-rehearsal.sh, infra/scripts/backup-nightly.sh, infra/scripts/restore-drill.sh, docs/runbooks/production-stack.md, docs/runbooks/database-backup-restore.md</files>
  <action>
    Write the backup + restore mechanism and then run the entire production sequence on
    this machine, against a throwaway apex, so that plan 08 is a deployment rather than
    a first attempt.

    **`infra/scripts/backup-nightly.sh`** — RESEARCH.md Pattern 3. `set -euo pipefail`;
    `pg_dump -Fc` out of the running postgres container via `docker exec`; gzip; upload
    with `rclone copy` to `${R2_REMOTE}:${BACKUP_BUCKET}/backups/` using an explicit
    `--config` path; delete the local temp file; log one structured line with the object
    key and byte size so the cron log is greppable. Support `BACKUP_LOCAL_ONLY=1` to
    skip the rclone step and leave the dump in `${BACKUP_DIR}` — that mode is what the
    local rehearsal uses and what a founder uses to take an ad-hoc dump before a risky
    migration. Never call `docker compose` at all, so the Pitfall #7 gate has nothing
    to find.

    **`infra/scripts/restore-drill.sh`** — the deliverable that makes G-02 real. It
    performs a full recovery rehearsal and prints an observed RTO, not a documented
    procedure: (1) obtain the dump — either `rclone copy` the newest object under the
    backups prefix, or a `--dump-file` path for the local rehearsal; the R2 path is the
    default because the round-trip through R2 is part of what must be proven;
    (2) `docker run -d --name resto-restore-drill` a throwaway `postgres:16-alpine` on
    an ephemeral port; (3) `pg_restore` the dump into it; (4) re-provision roles by
    running the committed `provision-roles-ci.ts` through the `migrate` image against
    the scratch database, then run `migrate.ts` — roles are cluster-level and are NOT
    in a database-scoped `pg_dump`, and re-running the committed provisioning is
    exactly what a real recovery onto a fresh box would do; (5) verify and fail loudly
    on any mismatch: the `__drizzle_migrations` row count equals the number of entries
    in `packages/db/migrations/meta/_journal.json`, `pg_roles` shows `resto_app` and
    `resto_auth` with `rolsuper=false` and `rolbypassrls=false`, and a `SELECT count(*)`
    on a tenant-scoped table returns without error; (6) tear the scratch container down
    with `docker rm -f` and print elapsed wall-clock seconds as the observed RTO.
    Teardown uses `docker rm -f` and, where a volume must go, an explicit
    `docker volume rm` by name — never a compose `-v`.

    **`infra/scripts/local-prod-rehearsal.sh`** — brings the production compose file up
    on this machine under a distinct project name (`resto-rehearsal`) so it cannot
    collide with `pnpm dev:up`, and drives the full sequence:
    render env files from the templates with `PUBLIC_APEX_DOMAIN=example.invalid`
    (RFC-reserved, unroutable, and — critically — not matched by
    `prod-guardrails.EPHEMERAL_HOST_RE`, so `NODE_ENV=production` actually boots);
    build the api and migrate images locally and pin `API_IMAGE_TAG` to the local tags;
    `up -d postgres nats`; run `provision-roles-ci.ts` then `db:migrate` through the
    profile-gated `migrate` service with `DATABASE_ADMIN_URL` passed per-invocation;
    `up -d api caddy`; then assert (a) the api container reached healthy, (b)
    `curl --resolve api.example.invalid:443:127.0.0.1 -k https://api.example.invalid/healthz`
    returns 200 — which proves Caddy's Host routing and the TLS wiring, not just that
    the container is alive — and (c) `/readyz` reports the leader-aware outbox state
    added by 07.5-03. Use a self-signed pair for the local run and say so; the real
    Origin CA pair is a plan-08 artifact. Then invoke `backup-nightly.sh` in
    `BACKUP_LOCAL_ONLY=1` mode and `restore-drill.sh --dump-file` against the result.
    Tear down with `docker compose -p resto-rehearsal down` followed by explicit
    `docker volume rm resto-rehearsal_postgres-data resto-rehearsal_nats-data` — the
    long way round on purpose, so this script never contains the flag that would make
    the guard fail and never teaches the pattern by example.

    **`docs/runbooks/production-stack.md`** — what the stack is, what each service does,
    the exact bring-up and rotate-a-secret sequences, the Postgres tuning values marked
    as unmeasured starting points, and the standing rule that `-v` never appears in
    automation. **`docs/runbooks/database-backup-restore.md`** — cadence (nightly 03:00
    UTC), retention (R2 lifecycle, 14 days), the two scripts' invocations, the stated
    targets (RPO ≤ 24h, RTO target < 1h) and a "Drill log" section whose first entry is
    this local rehearsal with its observed RTO. Plan 10 appends the production drill to
    the same section; leave the heading in place for it.

    Paste the rehearsal transcript (trimmed to the assertions and their results) into
    the plan summary as evidence.
  </action>
  <verify>
    <automated>bash infra/scripts/local-prod-rehearsal.sh && bash infra/scripts/assert-no-destructive-compose.sh</automated>
  </verify>
  <done>`local-prod-rehearsal.sh` exits 0, having: provisioned `resto_app`/`resto_auth`, applied every committed migration, booted the api under `NODE_ENV=production` with all preflights and both `assertProdGuardrails` calls green, served `/healthz` 200 through Caddy addressed by Host, produced a dump, restored it into a scratch database, and reported an observed RTO. The guard script still passes over the two new scripts. `docs/runbooks/database-backup-restore.md` has one drill-log entry with a real number in it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → VPS network interface | anything published here is reachable by the world; only 80/443 may be |
| CI/operator → production database | `DATABASE_ADMIN_URL` crosses here and must not persist on the box |
| repository → deployed artifact | anything committed is public to everyone with repo access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-06-01 | Information Disclosure | `docker-compose.prod.yml` `ports:` | mitigate | zero `ports:` outside `caddy`; asserted programmatically in Task 2's verify, not by review |
| T-07.5-06-02 | Denial of Service (self-inflicted) | any committed automation | mitigate | `assert-no-destructive-compose.sh` fails the build on `docker compose down`/`-v`; comment lines stripped before matching so the compose file's own warning cannot mask a real hit |
| T-07.5-06-03 | Information Disclosure | env templates in git | mitigate | placeholders only (`CHANGE_ME_*`), no realistic key material; gitleaks CI job is the backstop; `git check-ignore` asserted empty so the templates are visibly tracked rather than silently ignored |
| T-07.5-06-04 | Elevation of Privilege | `DATABASE_ADMIN_URL` | mitigate | deliberately absent from `api.env.example`; injected per-invocation into the profile-gated `migrate` service only, never a standing file |
| T-07.5-06-05 | Spoofing | `TRUST_PROXY` | mitigate | templates carry a CIDR list; `env.schema.ts` already rejects `true` outside dev/test — the template must not tempt anyone to try |
| T-07.5-06-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs no new packages; RESEARCH.md's Package Legitimacy Audit covers the two the phase adds (plan 07), both `[VERIFIED]`/Approved |
</threat_model>

<verification>
- `bash infra/scripts/assert-no-destructive-compose.sh` exits 0 over the whole repo.
- `docker compose -f infra/docker/docker-compose.prod.yml config` renders and `caddy`
  is the only service with published ports.
- No file created by this plan contains a registrable domain literal.
- `docker build --target migrate` succeeds and the resulting image can run `tsx`.
- `bash infra/scripts/local-prod-rehearsal.sh` exits 0 end to end.
- The founder has confirmed all six PREREQ-APEX checks.
</verification>

<success_criteria>
The production runtime exists as committed, domain-free files; the migration mechanism
is a real image that has been built and run; the backup and restore scripts have both
been executed and the restore verified; the entire boot sequence including
`assertProdGuardrails` has passed under `NODE_ENV=production` on a machine that is not
production. Plan 08 can now provision a box and deploy, rather than discover.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-06-SUMMARY.md` when done.
Record the observed local RTO and the rehearsal transcript excerpts — plan 10 compares
its production drill against them.
</output>
</content>
</invoke>
