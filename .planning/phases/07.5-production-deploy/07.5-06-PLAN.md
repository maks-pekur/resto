---
phase: 07.5-production-deploy
plan: '06'
type: execute
wave: 1
depends_on: ['07.5-02', '07.5-05']
files_modified:
  - infra/docker/docker-compose.prod.yml
  - infra/docker/Caddyfile
  - infra/docker/env/compose.env.example
  - infra/docker/env/api.env.example
  - infra/docker/env/website.env.example
  - infra/scripts/render-env.sh
  - infra/scripts/assert-no-destructive-compose.sh
  - infra/scripts/assert-no-domain-literals.sh
  - infra/scripts/local-prod-rehearsal.sh
  - infra/scripts/backup-nightly.sh
  - infra/scripts/restore-drill.sh
  - apps/api/Dockerfile
  - apps/api/src/config/prod-guardrails.ts
  - apps/api/test/unit/prod-guardrails.spec.ts
  - docs/runbooks/production-stack.md
  - docs/runbooks/database-backup-restore.md
autonomous: false
requirements:
  ['SC#1', 'SC#4', 'G-02', 'D-01', 'D-03', 'D-04', 'D-05', 'D-06', 'D-09', 'PREREQ-APEX']
user_setup:
  - service: apex-domain
    why: 'PREREQ-APEX — every hostname this phase produces is derived from one apex domain the founder owns and has delegated to Cloudflare. No plan in 06-10 writes a domain literal; all of them read this value.'
    env_vars:
      - name: PUBLIC_APEX_DOMAIN
        source: 'The bare apex the founder owns (no scheme, no port). Recorded as a GitHub Actions repository VARIABLE (not a secret — it is public) and as the first line of /opt/resto/.env on the VPS. Never committed.'
    dashboard_config:
      - task: 'Register/confirm ownership of the apex domain and delegate its nameservers to a Cloudflare zone'
        location: 'Domain registrar -> nameservers; Cloudflare dashboard -> Add a site'
  - service: hetzner-cloud
    why: 'D-01 — the single VPS that runs the compose stack (provisioned for real in plan 08; the account must exist before then)'
    env_vars: []
    dashboard_config:
      - task: 'Create a Hetzner Cloud account and a project; no server yet (plan 08 provisions it)'
        location: 'console.hetzner.cloud'
  - service: stripe
    why: 'Pitfall #10 — assertProdGuardrails refuses to boot the api at all in NODE_ENV=production without a non-placeholder STRIPE_SECRET_KEY. Test-mode keys are accepted. First used for real in plan 08 Task 3 against the box; gathered here so a boot-blocking credential surfaces before the paid box exists.'
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: 'Stripe Dashboard -> Developers -> API keys (test-mode sk_test_... is acceptable; the documented placeholder literal is rejected)'
      - name: STRIPE_WEBHOOK_SECRET
        source: 'Stripe Dashboard -> Developers -> Webhooks -> signing secret'
      - name: STRIPE_CONNECT_CLIENT_ID
        source: 'Stripe Dashboard -> Settings -> Connect -> Platform settings'
    dashboard_config: []
  - service: resend
    why: 'Pitfall #10 + the NODE_ENV-keyed adapter factory — in production the factory selects the Resend adapter and requires a real RESEND_API_KEY, and assertProdGuardrails re-checks the wired adapter name'
    env_vars:
      - name: RESEND_API_KEY
        source: 'Resend Dashboard -> API Keys'
    dashboard_config:
      - task: 'Verify a sending domain on the apex (SPF/DKIM/DMARC records — infra/runbooks/spf-dkim-dmarc-checklist.md already documents the record set). RESEND_FROM/RESEND_REPLY_TO must live on this domain or every operator invitation, password reset and verification email is rejected.'
        location: 'Resend Dashboard -> Domains'

must_haves:
  truths:
    - 'The production runtime is described by committed files (docker-compose.prod.yml + Caddyfile + env templates) — no hand-typed server state defines what runs'
    - 'No file this plan writes contains the apex domain, and a guard script proves it by searching for the real value rather than for a guessed TLD list'
    - 'The only container publishing a port to the VPS public interface is caddy (80/443); postgres and nats publish nothing'
    - 'No executable artifact in the repository can destroy the postgres-data volume, and the guard that proves it accepts every artifact this phase ships'
    - 'Caddy preserves the tenant hostname the Worker forwards, so a guest request still resolves to a tenant after the origin hop'
    - 'The /internal* IP gate cannot be defeated by a forged X-Forwarded-For, and no plan describes it as the control that protects that surface — InternalTokenGuard is'
    - 'Migrations and role provisioning run from a dedicated one-off image that has been proven to resolve drizzle-orm, not merely to contain tsx'
    - 'Provisioning runs in the order the codebase actually supports — migrations first, roles second'
    - 'A forgotten RESEND_FROM override fails the boot instead of silently sending from a domain the platform does not own'
    - 'The whole stack has been brought up locally end to end under NODE_ENV=production on synthetic credentials — no live secret enters a rehearsal transcript — and a dump taken from it restores into a scratch database whose row counts match the source'
  artifacts:
    - path: 'infra/docker/docker-compose.prod.yml'
      provides: 'The production runtime: postgres + nats + api + website + caddy + a profile-gated migrate service'
      contains: 'postgres-data'
    - path: 'infra/docker/Caddyfile'
      provides: 'TLS termination with the Cloudflare Origin CA cert, Host-based routing, trusted_proxies so X-Forwarded-Host survives, client_ip_headers CF-Connecting-IP so client_ip is not forgeable, and a defence-in-depth gate on /internal*'
      contains: 'client_ip_headers'
    - path: 'infra/scripts/assert-no-destructive-compose.sh'
      provides: 'The Pitfall #7 guard — one rule, scanning executable content only'
      contains: 'volumes'
    - path: 'infra/scripts/assert-no-domain-literals.sh'
      provides: 'The apex-parameterization guard — exact search for the configured apex, TLD regex as a secondary net'
      contains: 'PUBLIC_APEX_DOMAIN'
    - path: 'infra/scripts/restore-drill.sh'
      provides: 'Restore rehearsal: fetch dump -> scratch postgres -> pg_restore --exit-on-error -> migrate -> provision roles -> verify counts as resto_app -> observed RTO'
      contains: 'exit-on-error'
    - path: 'apps/api/Dockerfile'
      provides: 'A migrate build stage carrying tsx + packages/db sources + packages/db/node_modules'
      contains: 'AS migrate'
  key_links:
    - from: 'infra/docker/Caddyfile'
      to: 'apps/api/src/shared/effective-host.ts'
      via: 'trusted_proxies makes Caddy forward the inbound X-Forwarded-Host instead of overwriting it with api.<apex>'
      pattern: 'trusted_proxies'
    - from: 'infra/scripts/render-env.sh'
      to: 'infra/docker/env/*.env.example'
      via: 'envsubst expands ${PUBLIC_APEX_DOMAIN} into the real .env files on the box'
      pattern: 'envsubst'
    - from: 'infra/scripts/local-prod-rehearsal.sh'
      to: 'packages/db/src/cli/migrate.ts + packages/db/src/cli/provision-roles-ci.ts'
      via: 'the migrate service runs the committed CLIs in ci.yml order — migrations, then roles'
      pattern: 'provision-roles-ci|db:migrate'
---

<objective>
Turn the production runtime into committed, domain-parameterized files and prove the
entire shape works before a single euro is spent on a server.

Purpose: everything plans 08-10 do to a real box is a _deployment_ of what this plan
defines. If the compose stack, the role provisioning, the preflights and the restore
path are only first exercised against production, every one of them is a live
experiment.

Output: `docker-compose.prod.yml`, a `Caddyfile` that does not break tenant resolution,
three env templates plus the renderer that expands them, the two guard scripts this
phase's other plans reuse, a `migrate` image stage, the backup + restore-drill scripts,
and a recorded local transcript proving the whole sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@.planning/phases/07.5-production-deploy/07.5-VERIFICATION.md
@.planning/phases/07.5-production-deploy/DB-PROVIDER-DECISION.md

Read the SCOPE UPDATE banners at the top of `07.5-CONTEXT.md` before the decision
bodies. The bodies of D-01, D-03, D-04, D-05, D-06, D-09 and D-10 describe an AWS
design that was abandoned on 2026-06-26. `07.5-RESEARCH.md`'s reconciled-decision table
is authoritative. **If anything you write mentions ECS, Fargate, RDS, ALB, Terraform,
AWS Secrets Manager or Kubernetes, you have read a dead decision body.**

`07.5-VERIFICATION.md` blocked the first version of this plan. Where this plan and
RESEARCH.md disagree, this plan wins — three research statements were re-derived during
verification and two of them were wrong.

**Standing rule for this phase, earned twice.** Round 1's three errors were research
claims about the edge cache inherited without re-derivation; Round 2's were three claims
about what a third-party component does — Cloudflare's `X-Forwarded-For` semantics, the
Resend/Stripe constructors, a grep's blast radius — each asserted confidently and never
executed. So: **any sentence asserting how a third-party component behaves needs either a
citation or a command.** If you add such a sentence while executing, add one too, and put
the observation in the summary.

@infra/docker/docker-compose.dev.yml
@apps/api/Dockerfile
@apps/api/src/config/env.schema.ts
@apps/api/src/config/prod-guardrails.ts
@.github/workflows/ci.yml

<interfaces>
<!-- Contracts the executor needs; extracted and independently verified. Do not go
     looking, and do not "correct" these from RESEARCH.md. -->

**Provisioning order is migrations FIRST, roles SECOND.** `.github/workflows/ci.yml:253`
runs `db:migrate`, `:257` runs `provision-roles-ci.ts`; `packages/db/test/setup.ts:56`
does the same. The reason is in `packages/db/sql/auth-role.sql:14-24`, which — unlike
every other statement in that file — issues an **unguarded** grant:

```sql
GRANT SELECT, INSERT, UPDATE, DELETE ON
  "user", session, account, verification, two_factor, member, invitation, tenant_role
TO resto_auth;
```

On a database with no tables this raises `relation "user" does not exist`, the CLI exits
1, and `set -euo pipefail` kills the run. Roles-then-migrate cannot work.

Boot preflight order — `apps/api/src/main.ts`, all must pass before Nest mounts:

```
assertNoRlsBypass · assertTenantLockInstalled · assertSetConfigRevoked
assertNoBaCredentialAccess · assertInboxProcessedDeletable
assertWithoutTenantCallsiteRegistered · assertSystemRolesPresent
assertProdGuardrails(env)                       // NODE_ENV !== development/test only
assertProdGuardrails(env, { emailAdapterName }) // after the adapter is wired
```

CLIs to reuse, never reimplement:

```
packages/db/src/cli/migrate.ts             // cwd must be packages/db (migrationsFolder './migrations')
packages/db/src/cli/provision-roles-ci.ts  // DATABASE_ADMIN_URL, APP_ROLE_PASSWORD, AUTH_ROLE_PASSWORD
```

`packages/db/src/roles.ts` reads `../sql/roles.sql` with `readFileSync` at call time
(10.2-FINDINGS F-15), so both run under `tsx` and the migrate image must ship
`packages/db/sql/` and `packages/db/migrations/` as real files.

Env `env.schema.ts` requires outside development/test (superRefine):

```
BETTER_AUTH_SECRET, BETTER_AUTH_BASE_URL, BETTER_AUTH_DATABASE_URL, ADMIN_WEB_URL,
WEBSITE_PUBLIC_URL, AUTH_COOKIE_DOMAIN, AUDIT_ERASURE_SALT, TRUST_PROXY,
INTERNAL_API_TOKEN, DATABASE_DIRECT_URL
```

Shape constraints that bite: `AUTH_COOKIE_DOMAIN` must start with `.`;
`ADMIN_WEB_ORIGIN_WILDCARD` must be `https://*.<label>.<label>`; `PUBLIC_APEX_DOMAIN` is
a bare hostname; `TRUST_PROXY=true` is rejected outside dev/test (supply a CIDR list);
`assertProdGuardrails` rejects loopback / `devtunnels.ms` / `nip.io` / `lvh.me` /
`trycloudflare.com` / ngrok hosts in the URL keys — but `.invalid` passes, which is what
the rehearsal uses.

**Two `.default()`s that reach production populated and unchecked** —
`env.schema.ts:313,315`:

```typescript
RESEND_FROM: z.string().min(1).default('RestOS <noreply@resto.app>'),
RESEND_REPLY_TO: z.string().email().default('support@resto.app'),
```

`ResendEmailAdapter` uses `env.RESEND_FROM` verbatim. On any other apex Resend rejects
every send while `/healthz` stays 200 — operator invitations, password resets and email
verification all fail silently.

api runtime image facts (`apps/api/Dockerfile:40-50`, independently verified) that force
a separate migrate stage: the runtime stage copies root `node_modules`,
`apps/api/node_modules`, `packages` and `apps/api/dist` — but **not**
`packages/db/node_modules` — and has no `pnpm`. `tsx packages/db/src/cli/migrate.ts`
there cannot resolve `drizzle-orm`. Root `node_modules/.bin/tsx` does exist in `deps`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Founder prerequisites — apex domain, accounts, boot-blocking credentials</name>
  <what-built>Nothing yet. This is the named prerequisite (PREREQ-APEX) for the whole 06-10 set. Two of its items are needed by Task 3 of this very plan, not only at go-live: the local rehearsal boots under `NODE_ENV=production`, where the NODE_ENV-keyed email factory selects the Resend adapter and `assertProdGuardrails` re-checks both it and the Stripe key.</what-built>
  <how-to-verify>
    Produce all six, then run the checks:

    1. **Apex domain, delegated to Cloudflare.** Decide the bare apex (no scheme, no
       port). Every hostname in this phase derives from it: `api.<apex>`, `<apex>` +
       `*.<apex>`, `admin.<apex>` + `*.admin.<apex>`, `*.menu.<apex>`. `resto.app`
       appears throughout the repo only as a test fixture and ownership has never been
       confirmed; pick what is actually owned — nothing in plans 06-10 cares which, and
       a `.de` or `.eu` apex is as well supported as any other.
       Check: `dig +short NS <apex>` lists Cloudflare nameservers.
    2. **Record the apex where automation reads it**, never in a committed file:
       `gh variable set PUBLIC_APEX_DOMAIN --body '<apex>'`
       Check: `gh variable get PUBLIC_APEX_DOMAIN` echoes it back.
    3. **Hetzner Cloud account + project** (no server yet — plan 08 creates it).
    4. **Stripe credentials** (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`,
       `STRIPE_CONNECT_CLIENT_ID`); test-mode keys are fine.
       Check: `curl -s -o /dev/null -w '%{http_code}\n' https://api.stripe.com/v1/account -u "$STRIPE_SECRET_KEY:"`
    5. **Resend API key + a verified sending domain on the apex**, from which
       `RESEND_FROM` and `RESEND_REPLY_TO` will be issued.
       Check: `curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains`
    6. **A deploy-only SSH keypair**:
       `ssh-keygen -t ed25519 -f ~/.ssh/resto_deploy -C resto-deploy -N ''`

    Do not paste any secret value into the repo, a plan, or the summary. The two status
    codes from checks 4 and 5 disclose nothing and must be pasted — an "approved" with
    no evidence is how a boot-blocking credential gets discovered on the paid box.

    These credentials are not needed by Task 3 — the local rehearsal boots on synthetic
    values, because both SDK constructors are inert and both guardrails are string checks.
    They are first used for real in plan 08 Task 3, against the box. They are gathered now
    so that a boot-blocking credential is discovered here rather than there.

  </how-to-verify>
  <resume-signal>Type "approved", the apex domain, and the two HTTP status codes from checks 4 and 5. Or describe which check failed.</resume-signal>
  <action>Pause and present the six items. Do not proceed to Task 2 until every one is confirmed and the two status codes are pasted. Record only that each check passed; never the values.</action>
  <verify>
    <automated>test -n "$(gh variable get PUBLIC_APEX_DOMAIN)" && dig +short NS "$(gh variable get PUBLIC_APEX_DOMAIN)" | grep -qi cloudflare && test -f "$HOME/.ssh/resto_deploy.pub"</automated>
  </verify>
  <done>`PUBLIC_APEX_DOMAIN` is a repository variable resolving to Cloudflare nameservers, a deploy-only SSH public key exists, and the founder has pasted `200` for both the Stripe and Resend checks.</done>
</task>

<task type="auto">
  <name>Task 2: Compose stack, Caddy config, env templates, the two guard scripts, and the migrate image stage</name>
  <files>infra/docker/docker-compose.prod.yml, infra/docker/Caddyfile, infra/docker/env/compose.env.example, infra/docker/env/api.env.example, infra/docker/env/website.env.example, infra/scripts/render-env.sh, infra/scripts/assert-no-destructive-compose.sh, infra/scripts/assert-no-domain-literals.sh, apps/api/Dockerfile, apps/api/src/config/prod-guardrails.ts, apps/api/test/unit/prod-guardrails.spec.ts</files>
  <action>
    **`infra/docker/docker-compose.prod.yml`** — project name `resto-prod`, following
    RESEARCH.md Pattern 1. Services: `postgres` (postgres:16-alpine, volume
    `postgres-data`, `pg_isready` healthcheck, `shared_buffers=1GB`,
    `effective_cache_size=3GB`, `max_connections=40` as unmeasured CX32 starting values
    — say so in the runbook, not in a code comment); `nats` (nats:2.10-alpine,
    `--jetstream --store_dir=/data --http_port=8222 --user --pass`, volume `nats-data`);
    `api` and `website` pulling `ghcr.io/${GHCR_OWNER}/resto-{api,website}:${API_IMAGE_TAG}`
    with `env_file: .env.api` / `.env.website`; `caddy` — the only service with `ports:`,
    exactly `80:80` and `443:443`, bind-mounting `./Caddyfile` and `./certs` read-only,
    `environment:` carrying `API_HOST`, `WEBSITE_HOST`, `INTERNAL_ALLOWED_IPS`; and
    `migrate` (`ghcr.io/${GHCR_OWNER}/resto-migrate:${API_IMAGE_TAG}`,
    `profiles: ["tools"]` so `up -d` never starts it, no `restart:`, no `env_file` —
    `DATABASE_ADMIN_URL` is injected per invocation and never stored on the box).
    `restart: unless-stopped` on every long-running service.

    **Pin the Caddy tag to an explicit minor** (e.g. `caddy:2.10-alpine`), not `caddy:2`.
    Tenant resolution and the `/internal*` gate both depend on this version's
    `trusted_proxies` / `client_ip_headers` behaviour, and a floating tag can change it
    under a `docker compose pull`. Do not take the behaviour on trust either: after
    pinning, run the container once with the committed Caddyfile and confirm both
    properties by hand — a request carrying `X-Forwarded-Host: probe.menu.example.invalid`
    reaches the upstream with that header intact, and a request carrying only a forged
    `X-Forwarded-For` for an allowlisted address gets 404 on `/internal*`. Those are the
    two behaviours `07.5-VERIFICATION.md` Round 2 NEW-1 established against Caddy 2.11.4
    (tables C1-C4); re-establishing them against the tag you actually pin is three minutes
    and is the difference between a citation and an assumption. Confirm the tag exists
    before committing it — the verify does.

    **`infra/docker/Caddyfile`** — uses Caddy's env placeholders so no hostname is
    written down: `{$API_HOST}` reverse-proxying `api:3000`, and
    `{$WEBSITE_HOST}, *.{$WEBSITE_HOST}` reverse-proxying `website:3002`, both presenting
    the Origin CA pair from `/etc/caddy/certs/` via `tls` (no ACME, no automatic HTTPS —
    a 15-year cert needs no renewal). `admin off`.

    Two things beyond the research's shape, both load-bearing:

    1. **A global options block setting `servers { trusted_proxies static 0.0.0.0/0 ::/0 }`.**
       Without it Caddy's `reverse_proxy` overwrites `X-Forwarded-Host` with the request's
       own Host, so the tenant hostname the Worker forwards is replaced by `api.<apex>`;
       `effectiveHost()` returns that, and `NON_GUEST_SECOND_LABELS = new Set(['admin','api','www'])`
       in `tenant-resolver.service.ts:13` rejects it — **no tenant ever binds and every
       guest menu request dies.** Research Pitfall #3 analyses the Worker→origin hop and
       stops one hop short. The blanket range is acceptable only because `ufw` (plan 08)
       admits 80/443 exclusively from Cloudflare's published ranges, so the only client
       Caddy can ever see is already a trusted proxy. Put exactly that reasoning in a
       two-line WHY comment above the block — it is a deliberate decision that reads as a
       mistake without it, and it is void the moment the firewall rule changes.
    2. **`client_ip_headers CF-Connecting-IP` in the same `servers` block.** This is not
       optional once (1) is in place, and the reason is the opposite of intuitive: with
       every address trusted, Caddy's right-to-left walk over `X-Forwarded-For` finds
       nothing untrusted, gives up, and returns the **leftmost** XFF entry — which is
       whatever the client sent. Cloudflare **appends** the visitor address to a
       client-supplied `X-Forwarded-For` rather than replacing the header, so a forged
       value stays leftmost and wins. Cloudflare does overwrite `CF-Connecting-IP`, so
       reading that header instead is what makes `client_ip` mean anything here.
       Verified empirically against Caddy 2.11.4 in Docker (`07.5-VERIFICATION.md` Round 2
       NEW-1, tables T3/T4 and C1-C4): a forged `X-Forwarded-For` alone is admitted
       without this directive and rejected with it, and `X-Forwarded-Host` preservation —
       the whole point of (1) — is unaffected. Record in the WHY comment that (1) is what
       makes (2) necessary; they do not hold each other up.
    3. **A `client_ip` gate on `/internal*` as defence in depth.** The seed CLI
       (`pnpm resto:seed`) is the documented onboarding path for design-partner
       restaurants and talks to `/internal/v1/*`. **What actually protects that surface is
       `InternalTokenGuard`** (`apps/api/src/shared/api/internal-token.guard.ts`): it
       constant-time compares `X-Internal-Token` against `INTERNAL_API_TOKEN` and throws
       `UnauthorizedException` whenever the token is unset or mismatched outside
       `NODE_ENV=development`. The Caddy matcher — `path /internal*` with
       `not client_ip {$INTERNAL_ALLOWED_IPS}` and `respond 404` — narrows who can reach
       the guard at all; it is a second layer, not the control. Write it that way in the
       runbook too. Give the placeholder a Caddyfile default that matches nothing —
       `not client_ip {$INTERNAL_ALLOWED_IPS:192.0.2.1/32}` (TEST-NET-1, RFC 5737).
       **Not a bare `{$INTERNAL_ALLOWED_IPS}`:** if the compose `environment:` entry is
       missing it expands to empty, `not client_ip` with no argument is a Caddyfile *parse
       error*, and Caddy refuses to start — taking the whole origin down instead of failing
       closed. Task 3's rehearsal catches it locally, which is where you want to find it.
       Plan 08's `--stage full` proves the gate fires rather than asserting that it does.

    **Env templates under `infra/docker/env/`** — named `compose.env.example`,
    `api.env.example`, `website.env.example`, NOT `.env.*.example`: the root
    `.gitignore` line `.env.*` swallows that shape and `!.env.example` does not rescue
    it. Hostnames are written `${PUBLIC_APEX_DOMAIN}`; secrets are obvious `CHANGE_ME_*`
    tokens (gitleaks scans this repo — no realistic key material).
    `compose.env.example`: `PUBLIC_APEX_DOMAIN`, `GHCR_OWNER`, `API_IMAGE_TAG`,
    `WEBSITE_IMAGE_TAG`, `API_HOST=api.${PUBLIC_APEX_DOMAIN}`,
    `WEBSITE_HOST=${PUBLIC_APEX_DOMAIN}`, `INTERNAL_ALLOWED_IPS`, `POSTGRES_ADMIN_USER`,
    `POSTGRES_ADMIN_PASSWORD`, `POSTGRES_DB`, `NATS_USERNAME`, `NATS_PASSWORD`.
    `api.env.example`: the full Pattern 5 inventory — `NODE_ENV=production`,
    `DATABASE_URL`, `DATABASE_DIRECT_URL` (D-05: may be the same string), the three role
    URLs, `BETTER_AUTH_*`, `AUDIT_ERASURE_SALT`, `INTERNAL_API_TOKEN`, `NATS_*`, `S3_*`,
    `MEDIA_PUBLIC_BASE_URL`, `SENTRY_DSN`, `RESEND_API_KEY`, `STRIPE_*`,
    **`TRUST_PROXY` as the Compose bridge range PLUS Cloudflare's published ranges**
    (see below — a bridge-only value silently breaks rate limiting),
    `CORS_ALLOWED_ORIGINS`,
    `AUTH_COOKIE_DOMAIN=.admin.${PUBLIC_APEX_DOMAIN}`,
    `ADMIN_WEB_URL=https://admin.${PUBLIC_APEX_DOMAIN}`,
    `ADMIN_WEB_ORIGIN_WILDCARD=https://*.admin.${PUBLIC_APEX_DOMAIN}`,
    `WEBSITE_PUBLIC_URL`, `PUBLIC_APEX_DOMAIN`, `OTEL_DISABLED=true`, **and
    `RESEND_FROM="RestOS <noreply@${PUBLIC_APEX_DOMAIN}>"` plus
    `RESEND_REPLY_TO=support@${PUBLIC_APEX_DOMAIN}`** — the two schema defaults that are
    otherwise always populated with an apex the founder may not own. Deliberately absent:
    `DATABASE_ADMIN_URL`, with a one-line comment saying so, because its absence is
    otherwise indistinguishable from an oversight.

    **Why `TRUST_PROXY` is not just the Docker bridge.** Caddy appends its own direct
    peer to the inbound `X-Forwarded-For`, so the api receives
    `XFF: <client>, <cloudflare-pop>, <caddy-peer>`. Fastify's `trustProxy`
    (`main.ts:42`, `parseTrustProxy(bootEnv.TRUST_PROXY)`) walks right-to-left and stops
    at the first address outside the trusted set; with only `172.16.0.0/12` trusted it
    stops at the Cloudflare PoP and **`req.ip` becomes the PoP address**.
    `rateLimitKeyGenerator` (`apps/api/src/shared/security.ts:66-74`) keys on
    `ip:${req.ip}` for anonymous traffic and unconditionally for credential routes, so
    `RATE_LIMIT_AUTH_SIGNIN_PER_MIN` would become a per-PoP limit — one abusive client
    locking out a whole restaurant's staff, and credential stuffing diluting across PoPs.
    Widening the list is safe for tenant resolution because the middleware never
    evaluates the CIDRs: `tenant-context.middleware.ts:66` passes
    `this.env.TRUST_PROXY !== undefined && this.env.TRUST_PROXY.length > 0` — a boolean —
    into `effectiveHost`. Verify that yourself before writing the template:
    `grep -n 'TRUST_PROXY' apps/api/src/shared/tenant-context.middleware.ts` and
    `sed -n '60,75p' apps/api/src/shared/security.ts`.

    **`apps/api/src/config/prod-guardrails.ts`** — add `RESEND_FROM` and
    `RESEND_REPLY_TO` to the `DEV_DEFAULTS` map with their current schema defaults as the
    forbidden values, so a deploy that forgets to override them fails closed at boot
    rather than sending nothing and reporting healthy. Extend
    `apps/api/test/unit/prod-guardrails.spec.ts` with a case per key. Do not change
    `env.schema.ts` — the defaults must stay for dev, and the guard is the prod layer.

    **`infra/scripts/render-env.sh`** — `envsubst`s a template into a target at mode 600,
    refuses to run when `PUBLIC_APEX_DOMAIN` is unset or still a placeholder, and refuses
    to overwrite without `--force` so a redeploy cannot clobber rotated secrets.
    Compose's `env_file:` does not expand `${...}` inside the referenced file (only the
    compose file itself is interpolated), which is why the templates can hold `${...}` at
    all and why this script must exist. Confirm rather than assume, once, while writing it:
    put `FOO=${PUBLIC_APEX_DOMAIN}` in a scratch env file, reference it from a throwaway
    service, and check `docker compose config` reports the literal `${PUBLIC_APEX_DOMAIN}`
    rather than an expansion. Record the result in the summary.

    **`infra/scripts/assert-no-destructive-compose.sh` — one rule, stated once.** Scan
    the *executable content* of `.github/workflows/**`, `infra/scripts/*` (any
    extension — plan 10 adds a `.mjs`) and `infra/docker/*.yml`. "Executable content"
    means the file with `#`-comment lines removed, which is correct for shell and YAML
    alike. Fail on any `docker compose` / `docker-compose` invocation containing `down`,
    and on any compose invocation carrying `-v` or `--volumes`. **`docs/**` is out of
    scope entirely** — `#` is a heading in Markdown so the same stripping would be
    wrong there, and a runbook must be free to name the command it forbids. A guard that
    rejects its own project's documentation gets weakened by the next person who trips
    over it, which is the failure Pitfall #7 exists to prevent. State that rationale in
    the script header. `package.json` is also out of scope: `pnpm dev:reset` uses `-v`
    against the dev stack by design.

    **Join line continuations before matching** (`sed -e ':a' -e '/\\$/N; s/\\\n//; ta'`).
    A line-oriented matcher walks straight past a `docker compose` whose `down -v` sits on
    the next line after a trailing backslash — a plausible formatting accident, not an
    evasion. Comment-stripping already covers the trailing-comment case; this covers the
    other one.

    **Both guards get a `--self-test` mode**, running themselves against inline fixtures —
    a `down -v` line, a bare `down` line, a line-continuation form, and (for the domain
    guard) a file containing the configured apex — and failing if any fixture is missed. A
    guard nobody has watched fail is in the same category as a rollback nobody has watched
    work, and this phase does not accept the second. Plan 09 wires `--self-test` into the
    same CI step as the guards themselves.

    Put the `-v` warning that would otherwise live in a compose comment into
    `docs/runbooks/production-stack.md` instead, so the compose file has nothing for the
    guard to strip and the warning lives where someone will actually read it.

    **`infra/scripts/assert-no-domain-literals.sh`** — the apex guarantee, enforced once
    instead of five times. Takes paths, resolves the apex from `PUBLIC_APEX_DOMAIN` or
    `gh variable get`, and fails on any `grep -F` hit for that exact string — exact and
    TLD-agnostic, unlike a regex over `app|com|dev|io|net|org`, which misses `.de`,
    `.eu`, `.co.uk` and `.cloud` and would wave through the apexes an EU founder on
    Hetzner is most likely to pick. Keep the TLD regex as a secondary net with an
    allowlist for `ghcr.io`, `docker.io`, `github.com`, `cloudflare.com`, `rclone.org`,
    `stripe.com`, `resend.com`.

    Three properties this script needs because of what it will be pointed at:

    - **Hard-exclude `node_modules`, `dist`, `.next`, `coverage` and `.git`.** The admin
      bundle bakes `VITE_ADMIN_HOST_SUFFIX` into `dist/assets/*.js.map`, so any developer
      who has run a production build would otherwise fail this check locally on _any_
      apex. Those paths are gitignored, so excluding them loses nothing.
    - **An allowlist file** (`infra/scripts/domain-literal-allowlist.txt`) of accepted
      pre-existing `path:line` occurrences, so the guard is about _new_ literals. Measure
      the population before writing it rather than guessing at it —
      `grep -rIF "resto.app" apps | wc -l` returns 264 across 30+ files today, nearly all
      test fixtures and doc comments. This is also where `exception.filter.ts:91`'s
      RFC-7807 `type` namespace lives until someone decides what it should be.
    - **A degraded mode, stated rather than implied.** If the apex cannot be resolved — an
      unset variable, or a fork pull request where repository configuration may not reach
      the workflow — run the TLD-regex net only, print one line saying the exact check was
      skipped and why, and exit 0. Failing closed there turns a missing variable into a
      blocked repository; passing silently is worse. Print which of the three modes ran,
      every time.

    **`apps/api/Dockerfile`** — add a `migrate` stage after `build`, copying root
    `node_modules` (for `.bin/tsx`), `packages/db/node_modules`, `tsconfig.base.json` and
    the built `packages` tree, `WORKDIR /workspace/packages/db`, defaulting to
    `migrate.ts` under tsx. Leave `runtime` as the final stage so a plain `docker build`
    still produces the api image unchanged.

    No narrative comments anywhere beyond the two WHY comments named above (the
    `trusted_proxies` rationale and `api.env.example`'s deliberate `DATABASE_ADMIN_URL`
    omission) — repo rule.

  </action>
  <verify>
    <automated>bash infra/scripts/assert-no-destructive-compose.sh && PUBLIC_APEX_DOMAIN="$(gh variable get PUBLIC_APEX_DOMAIN)" bash infra/scripts/assert-no-domain-literals.sh infra/docker infra/scripts apps/api/Dockerfile && docker manifest inspect "$(grep -oE 'caddy:2\.[0-9]+-alpine' infra/docker/docker-compose.prod.yml | head -1)" >/dev/null && docker compose -f infra/docker/docker-compose.prod.yml --env-file infra/docker/env/compose.env.example config --format json | python3 -c "import json,sys; s=json.load(sys.stdin)['services']; p=sorted(k for k,v in s.items() if v.get('ports')); print('published:',p); sys.exit(0 if p==['caddy'] else 1)" && grep -qE 'trusted_proxies' infra/docker/Caddyfile && grep -qE 'client_ip_headers[[:space:]]+CF-Connecting-IP' infra/docker/Caddyfile && grep -qE 'client_ip' infra/docker/Caddyfile && bash infra/scripts/assert-no-destructive-compose.sh --self-test && PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-no-domain-literals.sh --self-test && grep -q 'RESEND_FROM' infra/docker/env/api.env.example && python3 -c "import re,sys; v=[l.split('=',1)[1].strip() for l in open('infra/docker/env/api.env.example') if l.startswith('TRUST_PROXY=')][0]; e=[x.strip() for x in v.split(',') if x.strip()]; pub=[x for x in e if not re.match(r'^(10\.|192\.168\.|172\.(1[6-9]|2[0-9]|3[01])\.|127\.)', x)]; print('TRUST_PROXY entries:',e); sys.exit(0 if len(e)>1 and pub else 1)" && test -z "$(git check-ignore infra/docker/env/compose.env.example infra/docker/env/api.env.example infra/docker/env/website.env.example)" && pnpm --filter @resto/api exec vitest run test/unit/prod-guardrails.spec.ts && docker build -f apps/api/Dockerfile --target migrate -t resto-migrate:local . && docker run --rm resto-migrate:local /workspace/node_modules/.bin/tsx -e "import('drizzle-orm/postgres-js/migrator').then(()=>import('postgres')).then(()=>process.exit(0))"</automated>
  </verify>
  <done>The pinned Caddy tag exists; `caddy` is the only service publishing ports; the Caddyfile sets `trusted_proxies`, reads the client address from `CF-Connecting-IP` (without which the `/internal*` gate admits anyone who sends one header) and gates `/internal*` on top of `InternalTokenGuard`; both guards pass their own `--self-test`; `TRUST_PROXY` in the template carries more than the bridge range; both guard scripts pass over the new files and the domain guard searches for the real apex; the three env templates are tracked by git and carry `RESEND_FROM`; the extended prod-guardrails spec is green; the `migrate` target builds and can actually import `drizzle-orm/postgres-js/migrator` and `postgres` — not merely run `tsx --version`, which a stage missing `packages/db/node_modules` would also pass.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the whole stack locally — boot, migrate, provision, back up, restore with row counts</name>
  <files>infra/scripts/local-prod-rehearsal.sh, infra/scripts/backup-nightly.sh, infra/scripts/restore-drill.sh, docs/runbooks/production-stack.md, docs/runbooks/database-backup-restore.md</files>
  <action>
    **`infra/scripts/backup-nightly.sh`** — `set -euo pipefail`; `pg_dump -Fc` out of the
    running postgres container; gzip; `rclone copy` to
    `${R2_REMOTE}:${BACKUP_BUCKET}/backups/` with an explicit `--config`; remove the
    local temp; log one greppable line with object key and byte size. `BACKUP_LOCAL_ONLY=1`
    skips the upload and leaves the dump in `${BACKUP_DIR}`.

    **Alongside every dump, write a manifest** (`<same-key>.manifest.json`, uploaded
    next to it): the dump's timestamp and `SELECT count(*)` for `tenants`, `orders`,
    `menu_items` and `tenant_domains` taken in the same transaction as the dump's
    snapshot. Without it the drill can only prove that a file parses. Never call
    `docker compose` from this script, so the guard has nothing to find.

    **`infra/scripts/restore-drill.sh`** — the deliverable that makes G-02 real, and the
    thing that has to be right rather than merely green:

    1. Obtain the dump: by default `rclone` the newest object under the backups prefix
       (the R2 round-trip is half of what is being proven); `--dump-file` for the local
       rehearsal. **Assert the object's `LastModified` is under 26 hours old** and fail
       otherwise — a cron that broke three weeks ago restores perfectly and would
       otherwise report a healthy drill while the stated RPO is fiction.
    2. `docker run -d --name resto-restore-drill` a throwaway `postgres:16-alpine` on an
       ephemeral port.
    3. `pg_restore --exit-on-error --no-owner --no-acl`. Without `--exit-on-error`
       `pg_restore` continues past errors and exits 0 anyway, so its exit code proves
       nothing and a dump that restored 6 of 78 tables would pass — check the flag's own
       description with `docker run --rm postgres:16-alpine pg_restore --help | grep -A1 exit-on-error`
       rather than taking this sentence's word for it. `--no-owner --no-acl` is not a workaround but
       the correct model: the dump's owner and grantees do not exist in a fresh cluster,
       and ownership and grants are re-established by the committed provisioning below,
       exactly as a real recovery onto a new box would do.
    4. Run `migrate.ts` (catch-up, normally a no-op), **then** `provision-roles-ci.ts` —
       the ci.yml order, for the reason in the interfaces block.
    5. Verify, failing loudly on any mismatch: `__drizzle_migrations` row count equals the
       entry count in `packages/db/migrations/meta/_journal.json`; `pg_roles` shows
       `resto_app` and `resto_auth` with `rolsuper=false` and `rolbypassrls=false`; and
       the four counts from the manifest match when re-run **connected as `resto_app`,
       not as the scratch superuser** — a superuser cannot see a missing grant, and
       proving the app role can read its own data is most of what recoverability means.
    6. `docker rm -f` the scratch container (never a compose `-v`), print elapsed seconds
       as the observed RTO.

    State in the script header and in the runbook **where the drill is expected to run**:
    on the operator workstation by default. Running it on the production CX32 puts a
    second Postgres and a full restore alongside the live one on 8 GB; if it must run
    there, require a free-disk precondition of at least three times the compressed dump
    and say so.

    **`infra/scripts/local-prod-rehearsal.sh`** — brings the production compose up under
    project name `resto-rehearsal` and drives the full sequence. It must **enumerate its
    own environment** rather than assuming a `.env` exists, because
    `NODE_ENV=production` makes `assertProdGuardrails` demanding:

    - Synthetic, non-dev-default values on `*.example.invalid` for `S3_ENDPOINT`,
      `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `MEDIA_PUBLIC_BASE_URL`, `INTERNAL_API_TOKEN`,
      `STRIPE_CONNECT_RETURN_URL`, `STRIPE_CONNECT_REFRESH_URL`, `ADMIN_WEB_URL`,
      `WEBSITE_PUBLIC_URL`, `BETTER_AUTH_BASE_URL`, `PUBLIC_APEX_DOMAIN`. `example.invalid`
      is RFC-reserved, unroutable and — unlike `localhost` — not matched by
      `EPHEMERAL_HOST_RE`, so the boot actually proceeds. This is also why local MinIO
      cannot be used for the rehearsal: `S3_ENDPOINT` may not be a loopback host. Nothing
      touches S3 during boot, so a synthetic endpoint is correct, not a fudge.
    - `TRUST_PROXY` covering the Docker bridge (`172.16.0.0/12`). Production additionally
      needs Cloudflare's ranges (see Task 2); locally there is no Cloudflare hop, so the
      bridge alone is right — note the difference in the script so nobody copies the
      rehearsal value onto the box.
    - **Synthetic `STRIPE_SECRET_KEY` and `RESEND_API_KEY`**, same treatment as the S3
      quartet: non-empty, not the documented dummy literal, no `replace_me`/`change_me`
      marker — e.g. `sk_test_rehearsal_local_only` and `re_rehearsal_local_only`. Both
      guardrails are string comparisons (`prod-guardrails.ts`), and the production branch
      of `createEmailAdapter` does two string checks and then `new Resend(key)`, which is
      a constructor with no network call; `tenancy.module.ts:80` constructs Stripe the
      same way. Nothing in this rehearsal sends an email or calls Stripe, so
      `adapterName === 'resend'` is satisfied and the second `assertProdGuardrails` call
      passes. Confirm rather than trust — the command is two lines and belongs in the
      task's evidence:
      `node --input-type=module -e "const {Resend}=await import('resend');const S=(await import('stripe')).default;new Resend('re_rehearsal_local_only');new S('sk_test_rehearsal_local_only');console.log('constructors inert')"`
      (run from `apps/api`, where both packages resolve; observed 87 ms, no network).
      **Do not route a live Stripe secret through this script.** Its transcript is pasted
      into the summary, and Task 1's own rule is that no secret value goes there. The real
      keys are needed against the real box in plan 08 Task 3, which is where they are used.
    - `RESEND_FROM`/`RESEND_REPLY_TO` on `example.invalid` — they must be _set_, since
      Task 2 just made the schema defaults fail closed; nothing is sent during boot.

    Then: build the api and migrate images locally and pin the tags; `up -d postgres nats`;
    run `db:migrate` **then** `provision-roles-ci.ts` through the profile-gated `migrate`
    service with `DATABASE_ADMIN_URL` per invocation; `up -d api caddy`; assert the api
    container reached healthy; assert
    `curl --resolve api.example.invalid:443:127.0.0.1 -k https://api.example.invalid/healthz`
    returns 200 — which proves Caddy's Host routing and TLS wiring, not just a live
    container — and that `/readyz` reports the leader-aware outbox state from 07.5-03.
    Use a self-signed pair locally and say so; the Origin CA pair is a plan-08 artifact.
    Then run `backup-nightly.sh` in `BACKUP_LOCAL_ONLY=1` mode and `restore-drill.sh
    --dump-file` against the result, with the manifest comparison live.

    Tear down with `docker compose -p resto-rehearsal stop`, `docker compose -p resto-rehearsal rm -f`,
    then explicit `docker volume rm resto-rehearsal_postgres-data resto-rehearsal_nats-data`.
    The long way round is deliberate: it contains no `down` and no `-v`, so this script
    conforms to the same guard it ships beside, and it never teaches the dangerous form by
    example.

    **`docs/runbooks/production-stack.md`** — the stack, the bring-up and secret-rotation
    sequences, the Postgres tuning values marked unmeasured, the `trusted_proxies`
    decision and what invalidates it, and the standing `-v` warning that was deliberately
    kept out of the compose file. **`docs/runbooks/database-backup-restore.md`** — cadence
    (nightly 03:00 UTC), retention (R2 lifecycle, 14 days), both scripts' invocations,
    where the drill runs, targets (RPO ≤ 24h, RTO target < 1h) and a **Drill log** whose
    first entry is this local rehearsal with its observed RTO and the four row counts.
    Plan 10 appends the production drill to that section.

  </action>
  <verify>
    <automated>bash infra/scripts/local-prod-rehearsal.sh && bash infra/scripts/assert-no-destructive-compose.sh && grep -qE 'Drill log' docs/runbooks/database-backup-restore.md && grep -qE 'observed RTO: [0-9]+s' docs/runbooks/database-backup-restore.md && grep -qE 'tenants=[0-9]+' docs/runbooks/database-backup-restore.md</automated>
  </verify>
  <done>`local-prod-rehearsal.sh` exits 0 having applied every migration, provisioned both roles in ci.yml order, booted the api under `NODE_ENV=production` with all preflights and both `assertProdGuardrails` calls green on synthetic, non-placeholder Stripe and Resend values (with the constructor-inertness command run as evidence), served `/healthz` 200 through Caddy addressed by Host, produced a dump plus manifest, and restored it into a scratch database whose four row counts matched when read as `resto_app`. The guard still passes over both new scripts. The drill log's first entry carries a real elapsed time and real counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                              | Description                                                       |
| ------------------------------------- | ----------------------------------------------------------------- |
| internet → VPS network interface      | anything published here is reachable by the world; only 80/443 may be |
| Cloudflare/Caddy → api                | the hop that carries the asserted tenant identity                 |
| CI/operator → production database     | `DATABASE_ADMIN_URL` crosses here and must not persist on the box  |
| repository → deployed artifact        | anything committed is public to everyone with repo access          |

## STRIDE Threat Register

| Threat ID    | Category                      | Component                          | Disposition | Mitigation Plan                                                                                                                       |
| ------------ | ----------------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-07.5-06-01 | Information Disclosure        | `docker-compose.prod.yml` `ports:` | mitigate    | zero `ports:` outside `caddy`, asserted programmatically rather than by review                                                        |
| T-07.5-06-02 | Denial of Service (self)      | any executable artifact            | mitigate    | one guard rule over executable content only, so every artifact this phase ships conforms and nobody has a reason to weaken it          |
| T-07.5-06-03 | Spoofing                      | `trusted_proxies static 0.0.0.0/0` | mitigate    | acceptable only because ufw admits 80/443 from Cloudflare ranges alone; recorded as a WHY comment naming the dependency                |
| T-07.5-06-04 | Elevation of Privilege        | `/internal*` on a public host      | mitigate    | `InternalTokenGuard` (constant-time compare, throws outside development) is the control; the Caddy `client_ip` allowlist narrows reachability as a second layer and reads `CF-Connecting-IP`, because with blanket `trusted_proxies` an XFF-based `client_ip` is client-supplied |
| T-07.5-06-08 | Spoofing                      | forged `X-Forwarded-For`           | mitigate    | `client_ip_headers CF-Connecting-IP`; Cloudflare appends to XFF and overwrites CF-Connecting-IP, verified against Caddy 2.11.4 (VERIFICATION Round 2 NEW-1, T3/T4 vs C1-C3) |
| T-07.5-06-09 | Denial of Service             | rate limiting keyed on a Cloudflare PoP | mitigate | `TRUST_PROXY` includes Cloudflare's ranges so `req.ip` is the visitor, not the PoP (`security.ts:66-74` keys on `ip:${req.ip}`)        |
| T-07.5-06-05 | Elevation of Privilege        | `DATABASE_ADMIN_URL`               | mitigate    | absent from `api.env.example`; injected per invocation into the profile-gated `migrate` service only                                    |
| T-07.5-06-06 | Repudiation / silent failure  | `RESEND_FROM` schema default       | mitigate    | added to the prod-guardrail dev-default set, so an unowned sender domain fails the boot instead of failing every email                 |
| T-07.5-06-07 | Information Disclosure        | env templates in git               | mitigate    | `CHANGE_ME_*` placeholders only; gitleaks is the backstop; `git check-ignore` asserted empty so the templates are visibly tracked      |
| T-07.5-06-SC | Tampering                     | npm/pip/cargo installs             | mitigate    | this plan installs no new packages; RESEARCH.md's Package Legitimacy Audit covers the two the phase adds (plan 07), both `[VERIFIED]`  |

</threat_model>

<verification>
- Both guard scripts exit 0 over the whole repository, and the domain guard resolves and
  searches for the real apex rather than a TLD guess.
- The pinned Caddy tag exists; `caddy` is the only service with published ports.
- The `migrate` target imports `drizzle-orm/postgres-js/migrator` and `postgres`.
- `prod-guardrails.spec.ts` is green including the two new keys.
- `local-prod-rehearsal.sh` exits 0 end to end, in ci.yml provisioning order.
- The founder confirmed all six PREREQ-APEX items including the two status codes.
</verification>

<success_criteria>
The production runtime exists as committed, apex-free files whose guarantees are enforced
by scripts the phase's own artifacts satisfy; the migration mechanism is a real image that
has been proven to resolve its dependencies; and a dump taken from a locally-run
production stack has been restored and checked against row counts read as the application
role. Plan 08 provisions and deploys rather than discovering.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-06-SUMMARY.md` when done. Record the
observed local RTO, the four row counts, and the Caddy tag pinned — plan 10 compares its
production drill against them.
</output>
