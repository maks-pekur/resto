# Phase 7.5: Production Deploy - Context

**Gathered:** 2026-06-21
**Status:** Ready for planning

<domain>
## Phase Boundary

Stand up the **first real production environment** so the MVP-1 spine (api + admin,
later website/qr-menu) is shippable over HTTPS on a real domain, and Phase 8's Stripe
webhooks have a public URL. **Pragmatic over ideal — managed services, not a k8s/Terraform
build-out.** Generates no revenue; it is the gate to revenue (Phase 8).

The five success criteria are fixed by ROADMAP.md §"Phase 7.5": (1) api+admin run on
managed prod over HTTPS with the 3-role Postgres provisioned; (2) object storage wired +
runtime secret injection; (3) CD on merge to `main` with migrations as a pre-rollout step;
(4) boot preflights (`assertProdGuardrails`, RLS-bypass) pass in real prod; (5) a public
HTTPS webhook endpoint + an end-to-end smoke (external request → API → tenant menu renders).

**Not in scope:** Stripe/payments (Phase 8), multi-region, k8s, full IaC platform.

> **SCOPE CHANGE (2026-06-21):** `apps/admin` is being re-platformed to a Vite SPA (Phase 7.6) and will deploy as **static** (Cloudflare Pages/R2 + CDN), NOT an ECS service. For the remaining 7.5 deploy, treat the hosting surface as **api + website (ECS) + qr-menu (static)** — drop the admin ECS service from D-01/D-08 and the admin Dockerfile from the plan set (07.5-11's admin half is superseded; its website Dockerfile stays). D-01..D-10 otherwise unchanged.

> **SCOPE UPDATE (2026-06-26 — re-plan, founder decision):** The admin static deploy now **folds back into Phase 7.5**, not 7.6. Rationale: 7.5's SC#1/SC#5 already enumerate admin as one of the four live surfaces, and Phase 7.6's deploy task (`07.6-07`) was blocked precisely because 7.5 prod did not exist. 7.5 now stands up **all four surfaces**: **api + website (ECS/Fargate) + qr-menu + admin (both static on Cloudflare Pages/CDN)**. This **supersedes Phase 7.6 plan `07.6-07`** (admin deploy). Admin is treated exactly like qr-menu — static SPA build → Cloudflare Pages/CDN, host-routed, wildcard TLS — **not** an ECS service: there is no admin Dockerfile and no admin Fargate task.
>
> **Already done (do NOT re-plan):** `07.5-02` (esbuild `.sql` text-loader Docker-boot fix + restored docker-api CI gate — 8 preflights green on real Postgres) and the **website** Dockerfile from `07.5-11` are executed and valid (see their SUMMARYs). The admin-Dockerfile half of `07.5-11` is superseded (admin is static now).
>
> **Stale plans archived:** the 9 unexecuted 2026-06-21 plans (`01`, `03`–`10`) assumed admin-as-ECS and are archived under `_superseded-2026-06-21/`. This re-plan regenerates the remaining production stand-up fresh against the four-surface (admin-static) scope, reusing the freed plan numbers; `02` and `11` stay in place as the done anchors.
>
> **Redis/ElastiCache:** none — menu caching moved to HTTP/CDN ETag (Redis fully removed pre-7.5). The `infra/terraform` ElastiCache stub is to be **removed** (folds into G-07 stub-supersession), never provisioned.
>
> **SCOPE UPDATE (2026-06-26 — VPS PIVOT, founder decision; SUPERSEDES the AWS direction):** Dropped AWS entirely. The prod spine (**api + postgres + nats**) runs in **Docker Compose on a single VPS** (Hetzner recommended, ~€8/mo). **Cloudflare** in front (DNS / TLS / CDN) + **R2** (media, already Cloudflare) + **Cloudflare Pages** (admin + qr-menu static; website also on the VPS as Next.js). **No ECS/Fargate, no RDS, no ALB, no AWS Secrets Manager, no AWS at all.** This **supersedes D-01 (ECS→VPS Docker), D-04 (RDS→self-managed Postgres in compose), D-09 (AWS Secrets Manager→`.env`/Docker secrets on the box)** and simplifies D-03 (IaC→docker-compose), D-05 (direct conn trivial; no pooler), D-06 (NATS→compose service, not EC2+EBS), D-08 (Cloudflare unchanged). Self-managed Postgres = superuser, so **BYPASSRLS works natively** — the 07.5-05 policy change is retained as portable hygiene, not a requirement. **G-02 backups are now the founder's responsibility, automated by us: nightly `pg_dump`/WAL-G → R2 + a tested restore drill** (the one real tradeoff of VPS over managed). The half-built (empty) AWS RDS instance was torn down. **Plans 06–10 are re-planned for the VPS+Cloudflare stack** (was: RDS/NATS-EC2/ECS/ECR-CD).

</domain>

<decisions>
## Implementation Decisions

### Hosting

- **D-01:** Host on **AWS ECS/Fargate**. Founder's explicit choice, **made after** seeing
  both persona reviews recommend Fly/Railway instead (experience/credits/control; accepts
  the ops overhead). Plan MUST account for the ECS surface a PaaS would hide: VPC,
  ALB/HTTPS listener, Fargate task/service defs, IAM, log routing, and CD wiring.
- **D-02:** The API must run **≥1 always-on instance** — the outbox dispatcher elects a
  leader via `pg_try_advisory_lock(4815115)` on a reserved Postgres connection
  (`apps/api/src/infrastructure/outbox-dispatcher.service.ts`). No idle-to-zero on the API
  task, or paid-order/email/audit events queue silently. (Leader election makes multi-task
  safe; the constraint is "never zero", not "exactly one".)
- **D-03:** Keep IaC **minimal** despite ECS — AWS Copilot CLI or a small/single Terraform
  module is acceptable; an explicit "**no full k8s, no multi-region, no sprawling Terraform
  estate in 7.5**" constraint is LOCKED (investor BLOCK against gold-plating).

### Database (Postgres)

- **D-04:** **AWS RDS for PostgreSQL** (LOCKED 2026-06-26 → RDS) is the production
  database. Founder decision (2026-06-26), after declining Neon: RDS is AWS-native (same
  account as ECS), fully managed (automated backups + point-in-time recovery + patching +
  durable EBS-backed storage), and avoids serverless-Postgres lock-in. RestOS needs
  `resto_auth` with `BYPASSRLS`, an admin-migration/schema-owner role with `CREATEROLE`/DDL,
  and `resto_app` NOBYPASSRLS under `FORCE ROW LEVEL SECURITY`, plus `pgcrypto`/`citext`/
  `pg_trgm`. **The one provider-capability risk to confirm empirically at provisioning (plan
  06):** on RDS the master is `rds_superuser`, **not** a true `SUPERUSER`, and setting
  `BYPASSRLS` on another role normally requires superuser — confirm that `rds_superuser` can
  confer `BYPASSRLS` on `resto_auth` **and** that the three extensions are in the RDS
  supported-extensions list. If it cannot, **plan 06 HARD-STOPS** and we revisit (e.g. an
  RDS-specific role workaround). No throwaway-DB spike is needed — RDS support is documented,
  and the empirical proof runs **once, against the real instance, in plan 06**. Plan 01 now
  just records `DB-PROVIDER-DECISION.md` (RDS + rationale + role/connection model).
- **D-05:** (RDS-simplified) The outbox leader's advisory lock (`pg_try_advisory_lock(4815115)`)
  still needs a **direct, session-pinned** connection (`DATABASE_DIRECT_URL`) — already wired in
  code (plan 07.5-03). RDS has **no built-in pooler**, so at first-customer volume the app and the
  dispatcher both use direct connections (`DATABASE_URL` may equal `DATABASE_DIRECT_URL`); add
  **RDS Proxy** later only if connection counts demand it. The Neon/PgBouncer pooler footgun no
  longer applies.

### Events (NATS)

- **D-06:** **Self-hosted single-node NATS JetStream** on the platform with persistence.
  Synadia Cloud / managed is over-engineered for first-customer volume. Single node is a
  SPOF on the order path — **mitigated by the transactional outbox** (writes commit to
  Postgres; dispatch is async/retried). Verify the outbox actually decouples ordering from
  NATS availability; confirm the DLQ / `max_deliver` poison-message handling live.

### Object storage

- **D-07:** **Cloudflare R2** for menu media. Zero egress, S3-compatible (existing AWS S3
  SDK + presigned URLs work unchanged), CLAUDE.md already prefers R2. No conflict — both
  personas endorsed.

### Domains / TLS / CDN

- **D-08:** **Cloudflare in front of the platform**: DNS, free wildcard TLS for
  `*.menu.resto.app`, and CDN cache-rules for the public menu reads (the menu-caching
  feature already emits versioned ETag/Cache-Control — see runbook). Tenant **custom
  domains** via Cloudflare for SaaS (custom hostnames) — wiring can be light in 7.5 (apex +
  wildcard first; full per-tenant custom-domain onboarding may land later).

### Secrets / CD

- **D-09:** Runtime secrets via **AWS Secrets Manager** (native to ECS); never baked into
  images or committed. `assertProdGuardrails` already fails-closed on placeholder secrets
  (BLOCK-4 fix) — it must pass in real prod.
- **D-10:** **CD = existing GitHub Actions** (nx-affected CI) extended to build the image
  and deploy the ECS service on merge to `main`; **`pnpm db:migrate` runs as a gated
  pre-rollout step** under the admin-migration role.

### Locked correctness gates (from persona reviews — planner MUST address)

- **G-01 (BLOCK):** Phase is GATED on a **green production container** passing all six boot
  preflights against **real managed Postgres** — the known `import.meta.dirname`-in-CJS
  Docker bug (deliberately uncaught, see `.github/workflows/ci.yml` note) must be fixed and
  proven, or a public URL ships a binary that crashes on boot.
- **G-02 (BLOCK):** **Backups + a tested restore** on the revenue path. Define RPO/RTO and
  prove a restore once before go-live (paying restaurants' menus/orders/PII live here).
- **G-03 (HIGH):** Pull forward a **minimal outbox-stall detector** — `/readyz` checks DB +
  broker reachability but not leader liveness / dispatch lag
  (`apps/api/src/health/health.controller.ts`). A silent stall = lost paid-order events.
- **G-04 (HIGH):** Add an **error tracker** (Sentry or equivalent) — none exists today.
- **G-05 (HIGH):** Fix the pre-existing **`NEXT_PUBLIC_API_ORIGIN` / `ADMIN_WEB_URL`
  localhost-fallback** bug (CONCERNS.md) — it goes live in exactly this phase and would
  silently route real users to localhost. (Note: CI build now injects placeholders — prod
  must inject real values.)
- **G-06 (MEDIUM):** Migrations are **forward-only** (no `down`, hard deletes forbidden) →
  rollback = **expand/contract migrations + app-only image rollback**, proven before go-live.
- **G-07 (MEDIUM):** This phase **explicitly supersedes** the abandoned AWS-EKS/Terraform
  direction in ADR-0011 + `infra/CLAUDE.md` + the `infra/k8s` / `infra/terraform` stubs
  (we are AWS **ECS**, not EKS).

### Claude's Discretion

- ECS provisioning tool (Copilot vs minimal Terraform vs CDK) — pick the lowest-ops option
  that satisfies D-03; surface the choice in the plan.
- Exact Sentry vs alternative; exact backup cadence within a stated RPO/RTO.
  </decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase scope + reviews

- `.planning/ROADMAP.md` §"Phase 7.5: Production Deploy" — goal + 5 success criteria (fixed).
- `.planning/phases/07.5-production-deploy/07.5-PERSONA-CTO.md` — operational-risk review; the leader/always-on argument, Docker-boot BLOCK, rollback + Neon-pooler caveats.
- `.planning/phases/07.5-production-deploy/07.5-PERSONA-INVESTOR.md` — cost/lock-in review; the two BLOCK gates (3-role + BYPASSRLS on managed Postgres, backups) + "no Terraform/k8s" leanness rule + ~$40–90/mo.

### Project constraints

- `./CLAUDE.md` (root) — bootstrap budget, R2-over-S3, NATS-over-Kafka, no-Redis-for-MVP, secret-injection rule, 3-role/RLS double-enforcement.
- `.planning/PROJECT.md` — constraints + Key Decisions; `.planning/codebase/CONCERNS.md` (NEXT_PUBLIC fallback bug, infra stubs), `STACK.md`, `INTEGRATIONS.md`.

### Deploy-critical code (read before planning the runtime)

- `apps/api/src/main.ts` + `apps/api/src/config/env.schema.ts` (+ `config/prod-guardrails.ts`) — boot order, required prod env, fail-closed preflights.
- `apps/api/src/infrastructure/outbox-dispatcher.service.ts` + `packages/events/src/outbox/dispatcher.ts` — leader election (advisory lock) → always-on + direct-connection constraints.
- `apps/api/src/health/health.controller.ts` — `/readyz` scope (G-03).
- `apps/api/Dockerfile` + `apps/api/build.mjs` + `.github/workflows/ci.yml` — image build + the `import.meta.dirname` boot bug (G-01) + the CD surface to extend (D-10).
- `packages/db/src/cli/migrate.ts` + `packages/db/sql/roles.sql` + `infra/docker/postgres/init/01-extensions.sql` — 3-role provisioning + extensions (D-04 gate).
- `apps/api/src/shared/tenant-context.middleware.ts` — host-based multi-tenant routing (D-08 DNS/TLS).
- `docs/runbooks/menu-edge-caching.md` — the CDN cache-rule design Cloudflare must satisfy (D-08).

### To supersede

- `docs/adr/0011-*` (EKS direction) + `infra/CLAUDE.md` + `infra/k8s/**` + `infra/terraform/**` stubs (G-07).
  </canonical_refs>

<code_context>

## Existing Code Insights

### Reusable Assets

- `assertProdGuardrails` + env-schema fail-closed boot (already hardened in BLOCK-4) — the prod-readiness gate is largely built; the phase wires real values + proves it boots.
- Transactional outbox + inbox-dedup (`@resto/events`) — decouples ordering from NATS uptime; the single-node-NATS SPOF risk rests on this holding.
- S3-compatible adapter (`s3-signed-image-url.adapter.ts`) — R2 is a config swap, not code.
- nx-affected GitHub Actions CI — CD extends this, doesn't replace it.

### Established Patterns

- 3-role Postgres + FORCE RLS double-enforcement is the product's core defensibility — "treat managed Postgres as commodity Postgres" (no proprietary primitives) keeps it portable and is the lens for the D-04 gate.
- Migrations forward-only (hard deletes forbidden by role grant) → expand/contract (G-06).

### Integration Points

- ECS task ↔ **AWS RDS** (direct session-pinned conn for the dispatcher; RDS Proxy optional later) ↔ self-hosted NATS ↔ R2 ↔ AWS Secrets Manager; Cloudflare terminates TLS/CDN in front of the ALB.
  </code_context>

<specifics>
## Specific Ideas

- Founder deliberately chose **AWS ECS over the unanimous persona recommendation** (Fly/Railway) and accepts the ops cost — the plan should make the ECS path as boring/managed as possible (Copilot/minimal Terraform), not a bespoke platform.
- Whole-stack target burn ~$40–90/mo; the scarce resource is **founder-weeks** → bias every plan choice toward fewer moving parts and days-to-HTTPS.
  </specifics>

<deferred>
## Deferred Ideas

- Full per-tenant **custom-domain onboarding UX** (Cloudflare for SaaS custom hostnames at scale) — wildcard + apex is enough for MVP-1; rich tenant-domain self-service can follow.
- Multi-region / read-replica menu edge — explicitly out of 7.5 (Cloudflare CDN covers menu reads for now).
- Managed/HA NATS (Synadia or multi-node) — revisit only if the single node becomes a real bottleneck post-PMF.

None of the above are blockers — discussion stayed within the prod-deploy boundary.
</deferred>

---

_Phase: 7.5-Production Deploy_
_Context gathered: 2026-06-21_
