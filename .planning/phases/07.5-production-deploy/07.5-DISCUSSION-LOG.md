# Phase 7.5: Production Deploy — Discussion Log

**Date:** 2026-06-21 (human reference only — not consumed by downstream agents)

## Persona reviews (spawned per project convention; ROADMAP reviewers = CTO + investor)
- **CTO** (`07.5-PERSONA-CTO.md`): recommended Fly + Neon + R2 + single NATS; decisive fact = outbox leader advisory lock ⇒ API must be always-on; BLOCK = Docker `import.meta.dirname` boot bug; HIGH = no outbox-stall detection, no Sentry, forward-only rollback; MEDIUM = Neon pooler/advisory-lock caveat, supersede ADR-0011 EKS.
- **Investor** (`07.5-PERSONA-INVESTOR.md`): "Pursue, lean"; stack ~$40–90/mo, scarce resource = founder-weeks; BLOCK = Neon 3-role BYPASSRLS feasibility + backups/tested restore; HIGH = single-node NATS SPOF (verify outbox mitigation), NEXT_PUBLIC localhost-fallback bug ships here.

## Areas discussed (all 4 selected)
| Area | Options presented | Decision |
|---|---|---|
| Hosting | Fly.io (rec) / Railway / Render / AWS ECS | **AWS ECS/Fargate** — chosen again on re-ask after both personas recommended Fly/Railway; founder accepts ops overhead (D-01). |
| Postgres + NATS | Neon+selfNATS (rec) / platform-PG / Supabase / RDS+Synadia | **Neon + self-hosted single-node NATS** (D-04/D-06) — Neon BYPASSRLS gated; RDS fallback. |
| Object storage | R2 (rec) / S3 / Backblaze | **Cloudflare R2** (D-07). |
| Domains/TLS | Cloudflare-in-front (rec) / platform-TLS / CF+defer | **Cloudflare in front** — wildcard TLS + DNS + CDN cache-rules (D-08). |

## Re-ask
Hosting was re-presented after the persona reviews completed (both advised against ECS). Founder confirmed **AWS ECS** with eyes open.

## Deferred
Per-tenant custom-domain onboarding UX, multi-region/HA NATS — out of 7.5.
