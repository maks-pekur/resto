---
phase: 07.5-production-deploy
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/admin/lib/api-server-internal.ts
  - apps/website/lib/env.ts
  - apps/qr-menu/vite.config.ts
  - apps/qr-menu/src/env.ts
files_modified_note: "Web-layer + qr-menu env hygiene + NATS consumer verify only. No overlap with 07.5-02 (build/ci/db) or 07.5-03 (apps/api/src). Treat files_modified as the CANDIDATE set; the verify steps decide what actually changes — list untouched files as VERIFIED-only in the SUMMARY."
autonomous: true
requirements: []

must_haves:
  truths:
    - "No web-layer app silently routes real users to localhost in prod — every NEXT_PUBLIC or VITE origin var fails loud at module load in non-dev"
    - "NATS consumers are confirmed to pass max_deliver plus a DLQ publisher LIVE (not just defined) so a poison message cannot stall a consumer forever in prod"
    - "qr-menu production build emits hidden source maps (not publicly served), ready for Sentry upload"
  artifacts:
    - path: ".planning/phases/07.5-production-deploy/PRE-DEPLOY-VERIFY.md"
      provides: "A checklist transcript proving each verify item green, plus any fixes applied"
      contains: "VERIFY"
  key_links:
    - from: "apps/admin/lib/api-server-internal.ts"
      to: "apps/admin/lib/env.ts"
      via: "no direct process.env origin read bypassing the fail-loud loader"
      pattern: "NEXT_PUBLIC_API_ORIGIN"
    - from: "audit/identity NATS consumer registration"
      to: "nats-subscriber maxDeliver and dlqPublisher"
      via: "each subscribe call passes maxDeliver and a real dlqPublisher"
      pattern: "maxDeliver"
---

<objective>
Close the three "verify before you trust it" gates the research flagged, fixing ONLY what the
verification actually finds broken. RESEARCH says G-05 (admin + website) and D-06 (NATS DLQ) are
LIKELY already done, so this plan CONFIRMS rather than blindly re-implements — avoiding both
scope reduction and duplicate work.

- G-05: prove every web-layer app (admin, website, qr-menu) fails loud on a missing prod origin
  env var instead of falling back to localhost. RESEARCH confirms apps/admin/lib/env.ts and
  apps/website/lib/env.ts already do this — verify no call site BYPASSES the loader with a raw
  localhost fallback, and confirm qr-menu (VITE vars) has the same fail-loud guard.
- D-06: prove the NATS max_deliver + DLQ poison-message handling is wired LIVE. A grep shows
  computeDlqAction, max_deliver, dlqSubjectFor, DEFAULT_MAX_DELIVER already exist in
  nats-subscriber.ts (shipped Phase 3 AUTH-10) — verify the actual consumer registrations PASS
  maxDeliver plus a real dlqPublisher (defined-but-not-wired is the failure mode).
- qr-menu source maps: confirm 'hidden' (not true) for prod (QRM-11/12 + apps/CLAUDE.md
  production-hygiene rule) so Sentry can upload maps guests cannot download.

Purpose: cheap, high-leverage prod-correctness items that silently break a live deploy. Done as
a verify pass to honor the no-scope-reduction rule without duplicating shipped work.
Output: PRE-DEPLOY-VERIFY.md transcript + only the minimal fixes the checks expose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.5-production-deploy/07.5-RESEARCH.md
@apps/CLAUDE.md

<interfaces>
The known-good baselines the verify compares against (extracted from codebase):
apps/admin/lib/env.ts: AdminEnvSchema + AdminEnvValidationError; throws in non-dev on missing NEXT_PUBLIC_API_ORIGIN/ADMIN_WEB_URL (CONFIRMED correct by research).
apps/website/lib/env.ts: WebsiteEnvSchema (NEXT_PUBLIC_API_ORIGIN + WEBSITE_URL); isPermissive(dev/test) only; throws WebsiteEnvValidationError otherwise (CONFIRMED present).
packages/events/src/infrastructure/nats-subscriber.ts: computeDlqAction, dlqSubjectFor, buildJetStreamConsumerConfig (sets max_deliver), DEFAULT_MAX_DELIVER; subscriber takes maxDeliver + dlqPublisher. GAP TO CHECK: do the real consumer subscribe registrations pass maxDeliver + a real dlqPublisher, or rely on defaults / pass dlqPublisher undefined?
apps/qr-menu/vite.config.ts: build.sourcemap setting (must be 'hidden', not true) per QRM-11/12 + apps/CLAUDE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify web-layer env fail-loud (admin/website/qr-menu); fix only bypasses found</name>
  <files>apps/admin/lib/api-server-internal.ts, apps/website/lib/env.ts, apps/qr-menu/vite.config.ts, apps/qr-menu/src/env.ts</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section G-05 — the 3-step verification and Open Question 2
    - apps/CLAUDE.md sections "Env vars at the web layer" + "Source maps + production hygiene"
    - apps/admin/lib/env.ts and apps/website/lib/env.ts — the known-good fail-loud baseline to diff every other call site against
  </read_first>
  <action>
    Grep all of apps/admin, apps/website, apps/qr-menu for raw reads of NEXT_PUBLIC_API_ORIGIN,
    ADMIN_WEB_URL, WEBSITE_URL, or any VITE origin var that do NOT go through the centralized
    lib/env.ts loader (any localhost fallback via nullish-coalescing or logical-or). For each
    bypass found, route it through the fail-loud loader, or add a fail-loud guard for qr-menu VITE
    vars mirroring the admin/website pattern (throw at module load in non-dev when the var is
    missing; dev/test defaults only behind an import.meta.env.DEV guard). Confirm qr-menu does NOT
    bake a production-dangerous VITE_TENANT_SLUG override without a DEV runtime guard (apps/CLAUDE.md
    qr-menu CR-02). Set apps/qr-menu/vite.config.ts build.sourcemap to 'hidden' if it is currently
    true or absent. If a check is already green, record it VERIFIED in PRE-DEPLOY-VERIFY.md and
    change nothing — do not rewrite working code.
  </action>
  <verify>
    <automated>pnpm exec nx affected -t test --parallel=3</automated>
  </verify>
  <acceptance_criteria>
    - A grep of apps/admin, apps/website, apps/qr-menu source finds no localhost fallback for a NEXT_PUBLIC or VITE origin var outside a dev/test guard (transcript in PRE-DEPLOY-VERIFY.md).
    - apps/admin and apps/website env loaders confirmed fail-loud in non-dev (recorded VERIFIED, or fixed if a bypass existed).
    - apps/qr-menu/vite.config.ts build.sourcemap is 'hidden'.
    - nx affected tests green for any app touched.
  </acceptance_criteria>
  <done>No silent localhost routing can ship; qr-menu maps are hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Verify NATS max_deliver + DLQ wired LIVE on every consumer; write PRE-DEPLOY-VERIFY.md</name>
  <files>.planning/phases/07.5-production-deploy/PRE-DEPLOY-VERIFY.md</files>
  <read_first>
    - .planning/phases/07.5-production-deploy/07.5-RESEARCH.md section "NATS max_deliver / DLQ gap" + Assumption A6
    - packages/events/src/infrastructure/nats-subscriber.ts — computeDlqAction / dlqSubjectFor / DEFAULT_MAX_DELIVER / the maxDeliver + dlqPublisher subscribe contract
    - the consumer registration sites (audit context + identity DLQ branch) — grep for subscribe calls and confirm each passes maxDeliver + a real dlqPublisher (not undefined)
    - Open Question 4 — whether the dev NATS container enforces auth (prod MUST: NATS_USERNAME/NATS_PASSWORD)
  </read_first>
  <action>
    Grep every NATS consumer registration (subscribe call / consumer config construction) across
    apps/api and packages/events. For each, confirm it passes maxDeliver (at least 1; default 5 is
    fine) AND a non-undefined dlqPublisher so terminal redeliveries route to the dlq subject instead
    of NAK-looping forever. If any consumer is registered WITHOUT a dlqPublisher
    (defined-but-not-wired), wire the existing DLQ publisher into that registration — this is the
    only place a real code change may be needed, and it is wiring of an already-built mechanism, not
    a new feature. Confirm the AUTH-10 poison-message e2e test from Phase 3 still exercises the path.
    Then write PRE-DEPLOY-VERIFY.md as a checklist, one row per item — web-env fail-loud
    (admin/website/qr-menu), qr-menu hidden maps, NATS max_deliver live, NATS DLQ publisher wired on
    every consumer, prod NATS auth required (NATS_USERNAME/NATS_PASSWORD) — each marked VERIFIED or
    FIXED with evidence (grep output or test name).
  </action>
  <verify>
    <automated>pnpm exec nx run @resto/events:test</automated>
  </verify>
  <acceptance_criteria>
    - PRE-DEPLOY-VERIFY.md exists with a checklist row per item, each VERIFIED or FIXED with evidence.
    - Every NATS consumer registration passes maxDeliver and a non-undefined dlqPublisher (grep evidence in the checklist).
    - The AUTH-10 poison-message e2e path is confirmed still exercised (@resto/events tests green).
  </acceptance_criteria>
  <done>The poison-message DLQ path is proven live, not just defined; the pre-deploy checklist is the go/no-go artifact for the live cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy env -> web app module load | A missing origin var must fail loud, never default to localhost |
| poison NATS message -> consumer | One malformed envelope must not stall the whole consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-075-08 | Spoofing/Repudiation | web-layer localhost fallback in prod | mitigate | Fail-loud env loaders verified across all three web apps; grep gate proves no localhost fallback ships |
| T-075-09 | Denial of Service | NATS consumer poison-message stall | mitigate | Verify maxDeliver + dlqPublisher wired live on every consumer so terminal redeliveries route to DLQ |
| T-075-10 | Spoofing (message injection) | prod NATS without auth | mitigate | Checklist requires NATS_USERNAME/NATS_PASSWORD enforced in prod; VPC security group limits port 4222 (provisioned in 07.5-06) |
</threat_model>

<verification>
- Grep proves no web-layer localhost origin fallback ships.
- qr-menu source maps are 'hidden'.
- Every NATS consumer passes maxDeliver + a real dlqPublisher; @resto/events tests green.
- PRE-DEPLOY-VERIFY.md is the consolidated go/no-go checklist.
</verification>

<success_criteria>
G-05 (web env fail-loud) and D-06 (NATS DLQ live) are proven, not assumed; qr-menu prod hygiene confirmed; a single pre-deploy checklist artifact gates the cutover.
</success_criteria>

<output>
Create `.planning/phases/07.5-production-deploy/07.5-04-SUMMARY.md` when done. Note which items were already VERIFIED vs FIXED so downstream plans know what changed.
</output>
