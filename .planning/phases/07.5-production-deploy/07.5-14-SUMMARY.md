---
phase: 07.5-production-deploy
plan: '14'
subsystem: infra
tags: [caddy, single-apex, x-forwarded-host, restore-drill, aws-retirement, cloudflare]

requires:
  - phase: 07.4-admin-tenant-in-path
    provides: "07.4-02: guest checkout same-origin /v1/* with the dev-only Next rewrite gated NODE_ENV!=='production'; the browser's own X-Forwarded-Host sender deleted, making the origin-side pin safe to add"
  - phase: 07.5-production-deploy
    provides: "07.5-12: assets-only Cloudflare Workers for admin/qr-menu on path prefixes of the single apex; 07.5-13: PUBLIC_APEX_DOMAIN is the only apex parameter apps/api reads"
provides:
  - "infra/docker/Caddyfile serves three surfaces off two site blocks: {$API_HOST} and {$WEBSITE_HOST},*.{$WEBSITE_HOST}, with /internal* 404 first, /v1/* and /api/* to the api, everything else to the website"
  - "header_up X-Forwarded-Host {host} on every reverse_proxy in the Caddyfile, closing the measured client-forgeable-host defect (M-07/M-08)"
  - "apps/website's SSR fetchers (api-client.ts) call API_INTERNAL_ORIGIN, an optional server-only env var falling back to NEXT_PUBLIC_API_ORIGIN, keeping the tenant-selecting Host header off the public internet"
  - "infra/, docs/runbooks/, apps/website env templates and infra/scripts guards all name exactly one apex parameter (PUBLIC_APEX_DOMAIN) — ADMIN_APEX_DOMAIN and GUEST_APEX_DOMAIN are gone"
  - "local-prod-rehearsal.sh seeds two tenants, two locations, two tenant_domains rows, two menu_categories, three menu_items and three orders before calling backup-nightly.sh, and refuses to proceed if any of the four manifest-tracked counts is zero — the restore drill can now fail, and has been watched doing so"
  - "local-prod-rehearsal.sh proves the path split itself: a curl to /v1/menu on a wildcard tenant host through Caddy, watched failing with handle /v1/* removed and passing with it restored"
  - "infra/CLAUDE.md, CLAUDE.md, .planning/codebase/STACK.md and .planning/codebase/INTEGRATIONS.md describe the VPS + Docker Compose + Cloudflare + GHCR stack that exists, not AWS EKS/Terraform/Kubernetes; infra/terraform/ and infra/k8s/ are deleted"
affects: [07.5-08, 07.5-09, 07.5-10]

tech-stack:
  added: []
  patterns:
    - "Caddy handle-block path split inside one site block: mutually exclusive, evaluated in written order — /internal* 404 first (the client_ip gate only exists on {$API_HOST}, so ordering here is a real control, not style), then /v1/* and /api/* to the api, then the website fallback"
    - "header_up X-Forwarded-Host {host} pins the header to the request's own Host on every reverse_proxy — Caddy's own linter calls this 'unnecessary' (it doesn't model attacker-controlled input), but the measured behavior (M-07/M-08) is what matters, not the linter"
    - "API_INTERNAL_ORIGIN mirrors apiOrigin()'s shape in apps/website/lib/env.ts — optional, server-only, falls back to the public origin, same localhost-rejection guard in production — and is consumed only by api-client.ts, never checkout-api.ts (the browser module, which 07.4-02 already emptied of any origin/header logic)"
    - "Rehearsal seeding: direct psql INSERTs (not seed-demo, which refuses outside NODE_ENV=development) as the Postgres admin/superuser role, parent-then-child ordering respecting composite tenant FKs (ADR-0020 I-2), with an explicit non-zero-count assertion before the backup step ever runs"

key-files:
  created:
    - .planning/phases/07.5-production-deploy/07.5-14-SUMMARY.md
  modified:
    - infra/docker/Caddyfile
    - apps/website/lib/env.ts
    - apps/website/lib/api-client.ts
    - apps/website/test/env.spec.ts
    - apps/website/test/api-client.spec.ts
    - infra/docker/env/website.env.example
    - infra/docker/env/compose.env.example
    - infra/docker/env/api.env.example
    - infra/scripts/assert-no-domain-literals.sh
    - infra/scripts/assert-hostname-depth.sh
    - infra/scripts/local-prod-rehearsal.sh
    - infra/CLAUDE.md
    - CLAUDE.md
    - .planning/codebase/STACK.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/STRUCTURE.md
    - docs/runbooks/production-stack.md
    - docs/runbooks/menu-edge-caching.md
    - docs/runbooks/database-backup-restore.md
    - .planning/phases/07.5-production-deploy/deferred-items.md
  deleted:
    - infra/terraform/.gitkeep
    - infra/terraform/buckets-cors.tf
    - infra/k8s/.gitkeep
  renamed:
    - from: .planning/todos/pending/rehearsal-restore-drill-compares-zero-to-zero.md
      to: .planning/todos/done/rehearsal-restore-drill-compares-zero-to-zero.md

key-decisions:
  - "docker-compose.prod.yml already pinned caddy:2.11-alpine (not the floating caddy:2-alpine the plan expected) — confirmed and left unchanged, no-op, not added to this plan's file list"
  - "The Caddy compose-variable pin's own validation comment ('reverse_proxy overwrites...') was reworded to 'the proxy overwrites' — the literal word 'reverse_proxy' in that WHY comment was inflating the file's own reverse_proxy-directive grep count by one, a self-inflicted instance of the phase's own grep-hygiene lesson"
  - "The rehearsal's tampered-manifest proof was captured without re-running the full docker-compose stack twice: restore-drill.sh is self-contained (it boots its own throwaway scratch Postgres), so one preserved dump+manifest pair from a single full rehearsal run served both the real PASSED transcript and, after hand-tampering a copy of the manifest, the FAILED transcript"
  - "Chose a non-colliding dummy apex (zzz-plan14-verify.invalid) to re-run the plan's own literal assert-no-domain-literals.sh scan a second time, proving the four 'example.invalid' exact-match hits in the guards' own fixture defaults and the rehearsal's own PUBLIC_APEX_DOMAIN assignment are a verify-tooling artifact (the scan's own demo value collides with the codebase's standing test literal), not a real domain-literal leak introduced by this plan"
  - "Additionally fixed the same stale AWS-EKS/Kubernetes-Job/Vault-Secrets-Manager narrative in the root CLAUDE.md (its Platform Requirements section is a GSD:stack-managed block literally sourced from .planning/codebase/STACK.md), .planning/codebase/INTEGRATIONS.md's Hosting section and Secrets-location line, and .planning/codebase/STRUCTURE.md's directory tree — none were in this plan's file list, but all four are the same live, always-loaded instruction-file class the plan's own must_haves truth ('no instruction file... deploys to EKS with Terraform-managed RDS') is about"
  - "Appended a new row to docs/runbooks/database-backup-restore.md's Drill log documenting this plan's real seeded-and-restored counts and its tampered-manifest failure — that table exists specifically to track drill history, and this plan is what closed the zero-to-zero gap its only prior row called out"

requirements-completed: ['SC#1', 'SC#2', 'SC#4', 'NEW-3', 'APEX-1', 'G-07', 'D-08']

duration: ~3h
completed: 2026-09-06
---

# Phase 07.5 Plan 14: Caddy Path Split, X-Forwarded-Host Pin, One Apex, a Restore Drill That Can Fail Summary

**Caddy now routes `/v1/*` and `/api/*` to the api same-origin on every tenant host and pins `X-Forwarded-Host` on every proxy call; the website's SSR fetch moved off the public internet onto the Compose network; every template, guard and the local rehearsal collapsed to one apex parameter; the rehearsal's restore drill seeds real rows and has been watched both passing and failing; and every instruction file in the repo now describes the VPS/Cloudflare/GHCR stack that exists instead of the AWS EKS/Terraform stack that never shipped.**

## Performance

- **Duration:** ~3h
- **Completed:** 2026-09-06T01:20:00Z
- **Tasks:** 3/3
- **Files modified:** 20 (1 created, 19 modified/renamed, 3 deleted)

## Accomplishments

- `infra/docker/Caddyfile`: three `handle` blocks added to `{$WEBSITE_HOST}, *.{$WEBSITE_HOST}` in the required order (`/internal*` 404 first, then `/v1/*`, `/api/*`, fallback), plus `header_up X-Forwarded-Host {host}` on all four `reverse_proxy` directives in the file (the three new ones and the pre-existing `{$API_HOST}` one). `caddy validate` accepts the file (with a scratch self-signed cert mounted — see Verification below).
- `apps/website/lib/env.ts` gained `API_INTERNAL_ORIGIN` (optional, server-only, falls back to `NEXT_PUBLIC_API_ORIGIN`, subject to the same production localhost-rejection guard) and its own `internalApiOrigin()` accessor; `apps/website/lib/api-client.ts`'s three SSR fetchers (`fetchMenuPublic`, `fetchAvailabilityPublic`, `fetchOrderStatus`) now call it instead of `apiOrigin()`. `checkout-api.ts` (the browser module) is untouched.
- `infra/docker/env/compose.env.example` and `api.env.example` drop `ADMIN_APEX_DOMAIN`/`GUEST_APEX_DOMAIN`; the Stripe Connect return/refresh URLs now point at `${PUBLIC_APEX_DOMAIN}/admin/tenant/payouts?stripe=return|refresh`, matching 07.4-06's code defaults.
- `infra/scripts/assert-no-domain-literals.sh` and `assert-hostname-depth.sh` collapsed their three-apex logic (three `scan_exact` calls, a three-apex match loop, three self-test fixtures) to one; both `--self-test` modes pass and were each watched failing on a deliberately broken fixture, then restored.
- `infra/scripts/local-prod-rehearsal.sh` rewritten for one apex end to end (`example.invalid` alone), and now seeds two tenants (including `pizza`, used by a new `/v1/menu` wildcard-host curl proof), two locations, two `tenant_domains` rows, two `menu_categories`, three `menu_items` and three `orders` via direct `psql` before calling `backup-nightly.sh` — asserting all four manifest-tracked counts are non-zero first. `restore-drill.sh: PASSED elapsed=7s tenants=2 orders=3 menu_items=3 tenant_domains=2` on a real run; the same dump's manifest was hand-tampered afterward and correctly rejected.
- `infra/CLAUDE.md` rewritten in full for the Docker Compose + Caddy + GHCR stack, history confined to a trailing `## Superseded (historical)` section; `infra/terraform/` and `infra/k8s/` deleted. The same stale AWS/Kubernetes narrative was also found and fixed in the root `CLAUDE.md`, `.planning/codebase/STACK.md`, `.planning/codebase/INTEGRATIONS.md` and `.planning/codebase/STRUCTURE.md`.
- `docs/runbooks/production-stack.md` rewritten for one apex (depth-≤1 reasoning intact); `docs/runbooks/menu-edge-caching.md` rewritten for the zone cache + `cf-cache-status` + tenant (not brand) language, with the `Vary`/`Set-Cookie` interaction written down as an open question (research assumption A9) pointing at plan 07.5-10; `docs/runbooks/database-backup-restore.md`'s Drill log gained a new row for this plan's seeded run.
- `.planning/todos/pending/rehearsal-restore-drill-compares-zero-to-zero.md` closed into `.planning/todos/done/` with both sets of observed counts.

## Task Commits

1. **Task 1: Caddy routes by path and pins the host; the website SSR call stops leaving the box** — `6e58728d` (feat)
2. **Task 2: One apex in the templates, the guards and the rehearsal — and a restore drill that can fail** — `4661798b` + `8506bf65` (feat) — split into two commits because an initial `git add` with a stale pathspec (the already-`git mv`'d pending-todo path) silently failed to stage the five other modified files; the todo rename landed in `4661798b`, the guard/template/rehearsal edits (including the todo's own content edit, restaged) landed in `8506bf65`. Both commits are Task 2's work; nothing was lost or duplicated.
3. **Task 3: Retire the AWS-era infrastructure instructions (G-07)** — `d023be72` + `594c087e` (docs) — same pathspec mishap (`infra/k8s` no longer existed as a path once its deletion was already staged from an earlier `git rm -r`), split into the directory-deletion commit and the doc-rewrite commit. Both commits are Task 3's work.

## Files Created/Modified

See `key-files` in frontmatter for the full list. Notable:

- `infra/docker/Caddyfile` — the path split + host pin, the mechanism the rest of the phase's Worker unwind rests on
- `apps/website/lib/env.ts`, `apps/website/lib/api-client.ts` — `API_INTERNAL_ORIGIN`
- `infra/scripts/local-prod-rehearsal.sh` — one apex, seeded rows, the `/v1/menu` wildcard-host proof
- `infra/CLAUDE.md`, `CLAUDE.md`, `.planning/codebase/STACK.md`, `.planning/codebase/INTEGRATIONS.md` — the AWS-era retirement

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, self-inflicted grep-hygiene] The Caddyfile's own compose-variable WHY comment inflated its `reverse_proxy` grep count**
- **Found during:** Task 1, running the Task 1 verify script literally
- **Issue:** The pre-existing global-block comment explaining `trusted_proxies` said "without this, `reverse_proxy` overwrites X-Forwarded-Host..." — once every real `reverse_proxy` directive in the file gained a `header_up` pin, the verify's `grep -c 'header_up ...' == grep -c 'reverse_proxy'` count mismatched by exactly one, because the comment's prose use of the literal word `reverse_proxy` counted as a fifth "reverse_proxy" occurrence with no corresponding `header_up` line. This is the same class of mistake the plan's own standing rule warns about (07.4-01/07.4-04's WHY-comment-matches-its-own-grep defect), self-inflicted this time.
- **Fix:** Reworded the comment to "the proxy overwrites X-Forwarded-Host" — same meaning, no longer reproducing the directive name literally.
- **Files modified:** `infra/docker/Caddyfile`
- **Verification:** `test "$(grep -c 'header_up X-Forwarded-Host {host}' infra/docker/Caddyfile)" = "$(grep -c 'reverse_proxy' infra/docker/Caddyfile)"` passes (4 = 4).
- **Committed in:** `6e58728d` (Task 1 commit)

**2. [Rule 1 - Bug, same class] The rehearsal restore-drill self-test's own fixture comments repeated the plan's grep-hygiene mistake**
- **Found during:** Task 2, running `assert-no-domain-literals.sh --self-test` and the real tree scan
- **Issue:** My first draft of the new `pp-ua.txt` self-test fixture (added to prove the TLD net's `ua` entry works, per the plan's own instruction to check this by running the self-test rather than reading the regex) had a WHY comment reading "pp.ua-shaped fixture..." and a failure message reading "...(restos.pp.ua-shaped)..." — both literally contain a `pp.ua`-shaped substring, which the guard's own TLD net then flagged when scanning its own source file.
- **Fix:** Reworded both to describe the fixture ("a second-level-ccTLD-shaped fixture") without reproducing a domain-shaped substring, matching the existing `tldhit.txt` fixture's own established pattern (the `!`-for-`.` trick, applied here to the prose rather than the data, which already used the trick correctly).
- **Files modified:** `infra/scripts/assert-no-domain-literals.sh`
- **Verification:** `assert-no-domain-literals.sh --self-test` still `PASSED`; the real tree scan no longer flags the guard's own source file for this reason.
- **Committed in:** `4661798b`/`8506bf65` (Task 2 commits)

**3. [Rule 2 - Missing critical, census gap] The same stale AWS/Kubernetes narrative existed in four live instruction files this plan's own census didn't list**
- **Found during:** Task 3, sweeping for the plan's own must_haves truth ("no instruction file... deploys to EKS with Terraform-managed RDS")
- **Issue:** The plan's Task 3 file list named `infra/CLAUDE.md` and `.planning/codebase/STACK.md` only. Grepping for `EKS|ElastiCache|Terraform|Fargate|Secrets Manager|Kubernetes` across the repo's live (non-phase-artifact) docs found the identical stale claim in three more files: the root `CLAUDE.md` (whose "Platform Requirements" section is a `<!-- GSD:stack-start source:codebase/STACK.md -->`-managed block literally sourced from the file I was already editing — leaving it unsynced would have meant the single most-loaded instruction file in the whole repo still told every future agent this project deploys to AWS EKS), `.planning/codebase/INTEGRATIONS.md` (a "Hosting" section with the same EKS/Terraform/Kubernetes-Job claim, plus a separate "Vault / AWS Secrets Manager; injected at pod startup" line), and `.planning/codebase/STRUCTURE.md` (a directory-tree diagram still listing `infra/k8s/` and `infra/terraform/` as extant stub directories, which this plan's own Task 3 deletes).
- **Fix:** Mirrored the same VPS + Docker Compose + Cloudflare + GHCR description into all three additional files, matching what `infra/CLAUDE.md` and `STACK.md` now say. `STRUCTURE.md`'s tree entry for `infra/k8s`/`infra/terraform` was replaced with the actual current contents of `infra/scripts/` and a one-line note that the AWS direction was abandoned in this plan.
- **Files modified:** `CLAUDE.md`, `.planning/codebase/INTEGRATIONS.md`, `.planning/codebase/STRUCTURE.md`
- **Verification:** `grep -inE 'EKS|ElastiCache|Fargate|Secrets Manager|Kubernetes Job|pod startup' CLAUDE.md .planning/codebase/INTEGRATIONS.md .planning/codebase/STRUCTURE.md` returns nothing.
- **Committed in:** `d023be72`/`594c087e` (Task 3 commits)

**4. [Rule 2 - Missing critical] The drill log runbook this plan's own fix directly extends was not in the file list**
- **Found during:** Task 3, sweeping `docs/runbooks/` for stale "brand" terminology
- **Issue:** `docs/runbooks/database-backup-restore.md` carries a "Drill log" table whose only row (from plan 07.5-06) explicitly documents the all-zero-counts state this plan's Task 2 exists to fix, ending with "the counting mechanism's discriminating power was verified separately." Leaving that table without a follow-up row after the standing zero-to-zero defect was actually fixed would make the runbook internally inconsistent with the very todo this plan just closed.
- **Fix:** Appended a row for this plan's real rehearsal run, with the seeded/backed-up/restored counts and the tampered-manifest failure transcript.
- **Files modified:** `docs/runbooks/database-backup-restore.md`
- **Verification:** `PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-no-domain-literals.sh docs/runbooks/database-backup-restore.md` — `PASSED`.
- **Committed in:** `d023be72`/`594c087e` (Task 3 commits)

**5. [Verification-methodology note, not a source change] The plan's own `caddy validate` verify command has no cert directory to mount**
- **Found during:** Task 1, running the literal verify command
- **Issue:** `docker run ... caddy:2.11-alpine caddy validate --config ...` (no `-v` for `/etc/caddy/certs`) fails with `open /etc/caddy/certs/origin.crt: no such file or directory`, because both site blocks declare `tls /etc/caddy/certs/origin.crt /etc/caddy/certs/origin.key` and Caddy's `validate` subcommand provisions the TLS app (which requires loading the referenced certificate files), not just parsing JSON. `infra/docker/certs/` is gitignored and only ever populated by the rehearsal script or a real deploy — it does not exist in a fresh checkout.
- **Resolution:** Ran the literal command as written (shown failing below), then re-ran with a scratch self-signed cert generated on the fly and mounted at `/etc/caddy/certs` — `caddy validate` then reports `Valid configuration`. Did not modify the Caddyfile or the compose file to work around this; the gitignored cert directory is the correct, intentional shape (plan 08 places the real Origin CA pair there).
- **Committed in:** N/A — verification-methodology note, no source change.

**6. [Verification-methodology note, not a source change] The plan's own literal domain-literal tree scan self-flags its own demo apex value**
- **Found during:** Task 2, running `PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-no-domain-literals.sh infra docs/runbooks`
- **Issue:** `assert-no-domain-literals.sh`'s own self-test fallback (`local pub="${PUBLIC_APEX_DOMAIN:-example.invalid}"`), `assert-hostname-depth.sh`'s identical fallback, and `local-prod-rehearsal.sh`'s own `PUBLIC_APEX_DOMAIN="example.invalid"` assignment all legitimately contain the literal `example.invalid` — the same value the plan's own verify command chooses as its demo scan target. Confirmed pre-existing (not introduced by this plan) by running the identical scan against the pre-Task-2 committed version of `assert-no-domain-literals.sh`, which already self-flagged.
- **Resolution:** Ran the literal command as written (shown failing below), then re-ran with a non-colliding dummy apex (`zzz-plan14-verify.invalid`) to prove this plan introduces zero new domain-literal residue — only pre-existing, out-of-scope findings remain (`infra/runbooks/2fa-recovery.md`, `infra/runbooks/spf-dkim-dmarc-checklist.md`, `docs/runbooks/spa-workers.md`, and the repo-wide `docker-compose.dev`/`.test` filename family tripping the TLD net's `.dev` entry), logged to `deferred-items.md`.
- **Committed in:** N/A — verification-methodology note, no source change.

---

**Total deviations:** 6 (2 self-inflicted grep-hygiene bugs fixed inline, 2 census-gap additions serving this plan's own stated goal, 2 verification-methodology notes)
**Impact on plan:** All narrow, all directly caused by or required to complete this plan's own stated behavior. No scope creep, no architectural change.

## Issues Encountered

None beyond the deviations above.

## Verification Transcripts

### `caddy validate` (with a scratch self-signed cert mounted — see Deviation 5)

```
$ SCRATCH=$(mktemp -d)
$ openssl req -x509 -newkey rsa:2048 -keyout "$SCRATCH/origin.key" -out "$SCRATCH/origin.crt" \
    -days 1 -nodes -subj "/CN=api.example.invalid"
$ docker run --rm -v "$PWD/infra/docker/Caddyfile:/etc/caddy/Caddyfile:ro" \
    -v "$SCRATCH:/etc/caddy/certs:ro" \
    -e API_HOST=api.example.invalid -e WEBSITE_HOST=example.invalid \
    -e INTERNAL_ALLOWED_IPS=192.0.2.1/32 caddy:2.11-alpine caddy validate --config /etc/caddy/Caddyfile
...
Valid configuration
```
(Literal command with no cert mount fails with `open /etc/caddy/certs/origin.crt: no such file or directory` — see Deviation 5.)

### Website RED transcripts (Task 1)

`test/env.spec.ts`, RED against the unmodified `env.ts` (3 new cases, run before implementing `API_INTERNAL_ORIGIN`):

```
FAIL test/env.spec.ts > env loader > internalApiOrigin() returns API_INTERNAL_ORIGIN when set (07.5-14)
  TypeError: internalApiOrigin is not a function
FAIL test/env.spec.ts > env loader > internalApiOrigin() falls back to NEXT_PUBLIC_API_ORIGIN when unset (07.5-14)
  TypeError: internalApiOrigin is not a function
FAIL test/env.spec.ts > env loader > throws WebsiteEnvValidationError in production when API_INTERNAL_ORIGIN is localhost (G-05, 07.5-14)
  AssertionError: promise resolved instead of rejecting
 Test Files  1 failed (1)
      Tests  3 failed | 7 passed (10)
```
GREEN after implementing: `Test Files 1 passed (1)`, `Tests 10 passed (10)`.

`test/api-client.spec.ts`, RED against the unmodified `api-client.ts` (mock updated to export `internalApiOrigin` returning a distinct origin from `apiOrigin`):

```
FAIL test/api-client.spec.ts > fetchMenuPublic > targets internalApiOrigin(), not the public apiOrigin(), and forwards the incoming host as x-forwarded-host (07.5-14)
  AssertionError: expected 'http://api.test/v1/menu' to be 'http://api.internal.test/v1/menu'
 Test Files  1 failed (1)
      Tests  1 failed | 1 passed (2)
```
GREEN after switching `api-client.ts` to `internalApiOrigin()`: `Test Files 1 passed (1)`, `Tests 2 passed (2)`.

Full website suite after both fixes: `pnpm --filter website exec vitest run` → `Test Files 11 passed (11)`, `Tests 87 passed (87)` (includes `no-client-tenant-header.spec.ts`, unaffected). `tsc --noEmit` clean. `eslint .` clean (after fixing one `prefer-nullish-coalescing` violation in the new `env.ts` code — Rule 1 self-fix, same commit).

### Guard self-test failure transcripts (Task 2)

`assert-no-domain-literals.sh`, broken by removing `ua` from `TLD_REGEX`, then restored:

```
$ sed -i '' "s/xyz|me|cloud|shop|ua|uk/xyz|me|cloud|shop|uk/" infra/scripts/assert-no-domain-literals.sh
$ PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-no-domain-literals.sh --self-test
SELF-TEST FAIL: pp-ua.txt (second-level ccTLD fixture) should have been rejected by TLD net
assert-no-domain-literals.sh --self-test: FAILED
$ # restored:
$ PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-no-domain-literals.sh --self-test
assert-no-domain-literals.sh --self-test: PASSED
```

`assert-hostname-depth.sh`, broken by widening the depth check from `-gt 1` to `-gt 2`, then restored:

```
$ sed -i '' 's/if \[ "\$depth" -gt 1 \]; then/if [ "$depth" -gt 2 ]; then/' infra/scripts/assert-hostname-depth.sh
$ PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-hostname-depth.sh --self-test
SELF-TEST FAIL: pizza.menu.example.invalid (depth 2) should have been rejected
assert-hostname-depth.sh --self-test: FAILED
$ # restored:
$ PUBLIC_APEX_DOMAIN=example.invalid bash infra/scripts/assert-hostname-depth.sh --self-test
assert-hostname-depth.sh --self-test: PASSED
```

### Rehearsal: the path split's own proof, watched failing then passing (Task 2)

Negative control — `handle /v1/*` removed from the Caddyfile, full rehearsal run:

```
=== local-prod-rehearsal.sh: seeding rows so the restore drill can fail (07.5-14) ===
seeded: tenants=2 orders=3 menu_items=3 tenant_domains=2
=== local-prod-rehearsal.sh: Caddy Host-routed /healthz and /readyz through TLS ===
/healthz via Caddy Host routing + TLS: 200
/readyz reports outbox_leader state: {"status":"ok",...}
=== local-prod-rehearsal.sh: /v1/menu on a wildcard tenant host through Caddy ===
local-prod-rehearsal.sh: FAILED — /v1/menu on pizza.example.invalid through Caddy did not return the pizza tenant's menu:
local-prod-rehearsal.sh: tearing down (project resto-rehearsal)
```

Caddyfile restored, full rehearsal run again — everything passes:

```
=== local-prod-rehearsal.sh: seeding rows so the restore drill can fail (07.5-14) ===
seeded: tenants=2 orders=3 menu_items=3 tenant_domains=2
=== local-prod-rehearsal.sh: /v1/menu on a wildcard tenant host through Caddy ===
/v1/menu via Caddy's wildcard tenant host routing: pizza tenant's menu returned
=== local-prod-rehearsal.sh: backup (BACKUP_LOCAL_ONLY=1) ===
backup-nightly.sh: object=backups/resto-20260905T230639Z.dump.gz bytes=28740 (local only, not uploaded)
=== local-prod-rehearsal.sh: restore drill ===
restore-drill.sh: PASSED elapsed=7s tenants=2 orders=3 menu_items=3 tenant_domains=2 observed_rto=7s
=== local-prod-rehearsal.sh: guard re-check over its own deliverables ===
assert-no-destructive-compose.sh: PASSED (12 files scanned)
local-prod-rehearsal.sh: PASSED
```

Manifest, both sides:

```json
{
  "dumpKey": "resto-20260905T230639Z.dump.gz",
  "counts": { "tenants": 2, "orders": 3, "menu_items": 3, "tenant_domains": 2 }
}
```

### Restore drill watched failing on a tampered manifest (Task 2)

`restore-drill.sh` is self-contained (its own throwaway scratch Postgres), so the preserved dump+manifest from the run above was reused directly — no second full rehearsal needed:

```
$ cp resto-20260905T230639Z.dump.gz{,.manifest.json} /tmp/rehearsal-tamper-test/
$ python3 -c "... bump manifest tenants: 2 -> 3 ..."
$ APP_ROLE_PASSWORD=... AUTH_ROLE_PASSWORD=... bash infra/scripts/restore-drill.sh \
    --dump-file /tmp/rehearsal-tamper-test/resto-20260905T230639Z.dump.gz
...
restore-drill.sh: FAILED — tenants expected=3 actual(resto_app)=2
```

## Known Stubs

None.

## Threat Flags

None — every file touched maps onto a `mitigate`/`accept` disposition already named in the plan's own `<threat_model>` (T-07.5-14-01 through -07, T-07.5-14-SC). No new network endpoints, auth paths, or schema changes were introduced.

## User Setup Required

None — no external service configuration required. Route binding against the real Cloudflare zone and settling research assumption A7 (does Cloudflare deliver a client-supplied `X-Forwarded-Host`?) are plan 07.5-08's job, not this plan's.

## Next Phase Readiness

This closes Wave 1 of Phase 07.5. Outstanding for Wave 2 (plan 08) and beyond:

- **A7** (does Cloudflare deliver a client-supplied `X-Forwarded-Host` to the origin?) — this plan's pin is applied regardless, but the live negative control against the real edge is plan 08's `verify-prod-origin.sh --stage full`, not this plan's.
- **A9** (does a Cloudflare Cache Rule on `/v1/menu*` actually produce MISS→HIT, and how does it interact with `Vary`/`Set-Cookie`?) — written down as an explicit open question in `docs/runbooks/menu-edge-caching.md`, closed by plan 07.5-10's live two-tenant smoke.
- The real Cloudflare zone still needs: DNS records, Origin CA issuance, Full-strict mode, the Worker Routes for `<apex>/admin*` and `*.<apex>/qr*`, and the repository variables collapsed to `PUBLIC_APEX_DOMAIN` alone — all plan 08 provisioning steps, not touched here.
- `infra/runbooks/2fa-recovery.md`, `infra/runbooks/spf-dkim-dmarc-checklist.md`, and `docs/runbooks/spa-workers.md` carry pre-existing domain-literal-guard findings unrelated to this plan — logged to `deferred-items.md`, not fixed.
- No blockers for 07.5-08, 07.5-09 or 07.5-10.

---
*Phase: 07.5-production-deploy*
*Completed: 2026-09-06*

## Self-Check: PASSED

All 21 created/modified files listed above (plus the SUMMARY itself) found on disk; all 4
deleted paths confirmed gone; all 5 task commits (`6e58728d`, `4661798b`, `8506bf65`, `d023be72`,
`594c087e`) found in `git log`.
