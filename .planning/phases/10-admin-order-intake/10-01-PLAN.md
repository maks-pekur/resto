---
phase: 10-admin-order-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/ordering.ts
  - packages/db/migrations/0073_orders_intake.sql
  - packages/db/migrations/0074_tenancy_erase_order_sequences.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/test/integration/erase-includes-ordering.spec.ts
autonomous: true
requirements: [ORDINT-04, ORDINT-05, ORDINT-07, ORDINT-08]

must_haves:
  truths:
    - "Every order field this phase needs exists in Postgres before any real order is written (D-04)"
    - "A per-location, per-business-date counter table exists and increments atomically under concurrent checkout"
    - "The feed query has a covering composite index"
    - "Erasing a tenant removes its order_daily_sequences rows (GDPR — the 08.4-08 precedent)"
    - "The migration is APPLIED to the dev database, not merely authored"
  artifacts:
    - path: "packages/db/migrations/0073_orders_intake.sql"
      provides: "orders columns + order_daily_sequences + feed index"
      contains: "order_daily_sequences"
    - path: "packages/db/migrations/0074_tenancy_erase_order_sequences.sql"
      provides: "tenancy_erase_tenant covers the new counter table"
      contains: "CREATE OR REPLACE FUNCTION tenancy_erase_tenant"
    - path: "packages/db/src/schema/ordering.ts"
      provides: "Drizzle definitions for every new column and the counter table"
      contains: "orderDailySequences"
    - path: "packages/db/migrations/meta/_journal.json"
      provides: "journal entries idx 73 and 74"
      contains: "0074_tenancy_erase_order_sequences"
  key_links:
    - from: "order_daily_sequences"
      to: "locations(id, tenant_id)"
      via: "composite FK"
      pattern: "FOREIGN KEY \\(\"location_id\",\"tenant_id\"\\) REFERENCES"
    - from: "order_daily_sequences"
      to: "RLS"
      via: "baseline PERMISSIVE tenant policy + RESTRICTIVE location policy"
      pattern: "order_daily_sequences_iso"
---

<objective>
Land the single order-schema migration that D-04 requires: short daily order number, `channel`, per-state timestamps, cancel reason + `canceled_from_status` + actors, operator ETA, and marketing consent — plus the per-location daily-counter table that generates the short number, the composite index that makes the feed query fast, and the GDPR erasure update the new table requires.

Purpose: every one of these fields is permanently unrecoverable if added after the first real orders exist. This plan is a hard gate on every other backend plan in the phase.

Output: migrations `0073` + `0074` authored, journalled, and **applied** to the local dev database, with the applied state asserted by querying the live catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-admin-order-intake/10-CONTEXT.md
@.planning/phases/10-admin-order-intake/10-RESEARCH.md
@.planning/phases/10-admin-order-intake/10-PATTERNS.md
@packages/db/CLAUDE.md

<interfaces>
Existing `orders` table columns (packages/db/src/schema/ordering.ts) — do NOT rename or drop any of these:
`id, tenant_id, brand_id, location_id, idempotency_key, order_number, status, fulfillment_mode,
 table_identifier, customer_name, customer_phone, customer_email, subtotal, delivery_fee,
 service_fee, discount, total, currency, scheduled_for, created_at, updated_at`

Existing CHECK constraints on `orders`:
- `orders_status_chk`   — status IN ('created','requires_action','paid','accepted','preparing','ready','completed','canceled','refunded','failed')
- `orders_fulfillment_mode_chk` — fulfillment_mode IN ('dine_in','pickup','delivery')

Existing helpers available to migration SQL (already installed by earlier migrations):
- `is_system_session()`   — 0026 / 0036
- `current_tenant_id()`   — baseline tenant GUC reader
- `current_location_id()` — 0065_location_guc.sql

Drizzle helper imports already present in packages/db/src/schema/ordering.ts:
`money, compositeTenantFk, pkUuid, tenantIdColumn, tenantParentUniqueIndex`
plus from drizzle-orm/pg-core: `check, foreignKey, integer, pgTable, smallint, text, timestamp, uniqueIndex, uuid`
(`date`, `boolean`, `index`, `primaryKey` will need adding to that import list.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the Drizzle ordering schema with every new order column and the daily-counter table</name>
  <files>packages/db/src/schema/ordering.ts</files>
  <read_first>
    - packages/db/src/schema/ordering.ts (the file being modified — read the whole `orders` table definition and the `paymentRefunds` definition at the bottom)
    - packages/db/src/schema/brands.ts (the `locations` table — `timezone` is `text('timezone')`, nullable; needed for the composite FK target)
    - packages/db/src/schema/_columns.ts (`tenantIdColumn`, `compositeTenantFk`, `pkUuid`, `tenantParentUniqueIndex` signatures)
    - .planning/phases/10-admin-order-intake/10-PATTERNS.md (section "Migration + schema" — the schema column style block)
    - .planning/phases/10-admin-order-intake/10-RESEARCH.md (section A.2 and A.3 — the counter-table shape and the full column table)
  </read_first>
  <action>
Add the following columns to the existing `orders` pgTable definition, placed after `scheduledFor` and before `createdAt`. Every timestamp uses the exact `timestamp('<snake_name>', { withTimezone: true, mode: 'date' })` shape already used by `scheduledFor`.

  - `shortNumber: integer('short_number')` — **nullable at this stage, deliberately.** The generator that fills it does not exist yet (it ships in plan 10-04, which also tightens this to `.notNull()` via migration `0075`). Adding it `NOT NULL` now would make `$inferInsert` require a value in `OrderDrizzleRepository.save()` and in five test/seed fixtures before any code can supply one. Add a WHY-comment on this line naming plan 10-04 as the tightening step.
  - `channel: text('channel').notNull().default('site')`
  - `acceptedAt`, `preparingAt`, `readyAt`, `completedAt`, `canceledAt` — all nullable timestamps as above.
  - `acceptedByUserId: text('accepted_by_user_id')` — nullable. Better Auth user ids are `text`, not uuid; do NOT add an FK to the BA-owned `user` table.
  - `canceledByUserId: text('canceled_by_user_id')` — nullable, same reasoning.
  - `cancelReason: text('cancel_reason')` — nullable.
  - `cancelNote: text('cancel_note')` — nullable; free text used only when `cancel_reason = 'other'`.
  - `canceledFromStatus: text('canceled_from_status')` — nullable; the `orders.status` value immediately before `cancel()` fired (D-09's reject-vs-cancel intent discriminator).
  - `etaAt: timestamp('eta_at', ...)` — nullable. Do NOT reuse `scheduledFor`; that column already means "the guest's requested time" and conflating them is a bug this phase fixes.
  - `marketingConsent: boolean('marketing_consent').notNull().default(false)`
  - `marketingConsentAt: timestamp('marketing_consent_at', ...)` — nullable.

Add to the `orders` table's second-argument array:
  - `check('orders_channel_chk', sql\`${table.channel} IN ('site','qr-menu')\`)`
  - `check('orders_cancel_reason_chk', ...)` allowing NULL plus exactly these seven values: `guest_no_show`, `kitchen_out_of_stock`, `kitchen_too_busy`, `guest_requested`, `payment_issue`, `duplicate_order`, `other`. These strings are canonical — they are pinned identically in `10-RESEARCH.md` §A.3 and `10-UI-SPEC.md` §5 / §12, and the admin chips and the guest-facing phrase map both read them. Do not invent variants.
  - `check('orders_canceled_from_status_chk', ...)` allowing NULL plus the same value set as `orders_status_chk`.
  - `index('orders_feed_idx').on(table.tenantId, table.locationId, table.status, desc(table.createdAt))` — the feed's covering index (RESEARCH A.4). Import `index` from `drizzle-orm/pg-core` and `desc` from `drizzle-orm`.

Add a new exported table `orderDailySequences` (`order_daily_sequences`) below `orders`:
  - `tenantId: tenantIdColumn()`
  - `locationId: uuid('location_id').notNull()`
  - `businessDate: date('business_date').notNull()` (import `date` from `drizzle-orm/pg-core`)
  - `counter: integer('counter').notNull().default(0)`
  - second-argument array: `primaryKey({ name: 'order_daily_sequences_pk', columns: [table.tenantId, table.locationId, table.businessDate] })`, a `foreignKey` named `order_daily_sequences_tenant_fk` on `tenantId` → `tenants.id` with `.onDelete('cascade')` (mirror `orders_tenant_fk`), and `compositeTenantFk({ name: 'order_daily_sequences_location_fk', child: { id: table.locationId, tenantId: table.tenantId }, parent: { id: locations.id, tenantId: locations.tenantId } }).onDelete('restrict')` (mirror `orders_location_fk`).

Do NOT touch `paymentRefunds` in this plan — the `pending`-row support D-11 needs is owned by plan 10-05 (migration `0076`), because changing `stripe_refund_id` to nullable also changes `PaymentRefundRow`'s TypeScript type and its consumers.
  </action>
  <verify>
    <automated>pnpm --filter @resto/db exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "short_number\|marketing_consent\|canceled_from_status\|eta_at" packages/db/src/schema/ordering.ts` returns ≥ 4.
    - `grep -n "orderDailySequences" packages/db/src/schema/ordering.ts` shows an exported `pgTable('order_daily_sequences', ...)`.
    - `grep -n "orders_cancel_reason_chk" packages/db/src/schema/ordering.ts` shows a CHECK containing all seven canonical strings `guest_no_show`, `kitchen_out_of_stock`, `kitchen_too_busy`, `guest_requested`, `payment_issue`, `duplicate_order`, `other`.
    - `grep -n "orders_feed_idx" packages/db/src/schema/ordering.ts` shows an index on `(tenantId, locationId, status, createdAt desc)`.
    - `git diff --stat packages/db/src/schema/ordering.ts` shows no lines removed from the existing `paymentRefunds` definition.
    - Typecheck of `@resto/db` passes with zero errors.
  </acceptance_criteria>
  <done>The Drizzle schema declares every new order column, the counter table with both FKs and the composite PK, and the feed index; `@resto/db` typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author migrations 0073 and 0074 plus their journal entries</name>
  <files>packages/db/migrations/0073_orders_intake.sql, packages/db/migrations/0074_tenancy_erase_order_sequences.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/migrations/0070_orders_location_id.sql (header-comment house style, composite-FK statement shape, the row-count-check precedent)
    - packages/db/migrations/0071_orders_location_rls.sql (RESTRICTIVE policy shape, `DO $$ ... DROP POLICY IF EXISTS ... $$` guard)
    - packages/db/migrations/0063_locations.sql (full new-table migration: CREATE TABLE, both FKs, ENABLE + FORCE ROW LEVEL SECURITY, RESTRICTIVE policy)
    - packages/db/migrations/0067_location_tenant_iso.sql (**critical landmine**: 0063/0064 shipped ENABLE+FORCE RLS with no baseline PERMISSIVE tenant policy, which denies ALL rows; every new table needs `<table>_iso` PERMISSIVE alongside any RESTRICTIVE policy)
    - packages/db/migrations/0072_tenancy_erase_locations.sql (the most recent `CREATE OR REPLACE FUNCTION tenancy_erase_tenant` body — copy it verbatim and add one DELETE)
    - packages/db/migrations/meta/_journal.json (tail — idx 72, `when` 1783987200000)
    - packages/db/sql/roles.sql (ALTER DEFAULT PRIVILEGES — explains why a new table normally needs no explicit GRANT)
  </read_first>
  <action>
**Confirm the next free migration number first:** run `ls packages/db/migrations/*.sql | tail -1`. It should be `0072_tenancy_erase_locations.sql`. If another migration has landed since planning, shift `0073`/`0074` (and the journal `idx`/`when` values) accordingly and note it in the SUMMARY.

Do NOT run `pnpm db:generate` / `drizzle-kit generate` under any circumstance — the snapshot baseline never moved past `0018_snapshot.json`, so a generate produces 300+ lines of destructive `DROP TABLE ... CASCADE` (documented in `08.4-01-SUMMARY.md`). Everything below is hand-written SQL.

**`0073_orders_intake.sql`** — header comment in the `0070` style, citing `Phase 10 Plan 01 (D-04)` and stating explicitly:
  (a) `short_number` is added nullable here and tightened to NOT NULL by `0075` in plan 10-04, because the generator that fills it does not exist until that plan;
  (b) the recorded invariant from Growth MED-18: **orders are INSERTed at `status='created'` before payment, so `count(orders)` is the correct ANL-04 conversion denominator — a future stale-order sweep must not delete unpaid `created` rows** (this comment is the durable record of that invariant);
  (c) `channel` has exactly one producible value (`'site'`) until QR ordering ships — `apps/qr-menu` has no order-submission path.

Statements, each separated by `--> statement-breakpoint`:
  1. `ALTER TABLE orders ADD COLUMN` for every column added in Task 1, matching Task 1's names and types exactly. `channel text NOT NULL DEFAULT 'site'` and `marketing_consent boolean NOT NULL DEFAULT false` carry their defaults; every other new column is nullable.
  2. `ALTER TABLE orders ADD CONSTRAINT orders_channel_chk CHECK (channel IN ('site','qr-menu'))`.
  3. `ALTER TABLE orders ADD CONSTRAINT orders_cancel_reason_chk CHECK (cancel_reason IS NULL OR cancel_reason IN ('guest_no_show','kitchen_out_of_stock','kitchen_too_busy','guest_requested','payment_issue','duplicate_order','other'))`.
  4. `ALTER TABLE orders ADD CONSTRAINT orders_canceled_from_status_chk CHECK (canceled_from_status IS NULL OR canceled_from_status IN (<the same ten values as orders_status_chk>))`.
  5. `CREATE INDEX orders_feed_idx ON orders USING btree (tenant_id, location_id, status, created_at DESC)`.
  6. `CREATE TABLE order_daily_sequences (...)` with the columns from Task 1 and `CONSTRAINT order_daily_sequences_pk PRIMARY KEY (tenant_id, location_id, business_date)`.
  7. `ALTER TABLE order_daily_sequences ADD CONSTRAINT order_daily_sequences_tenant_fk FOREIGN KEY (tenant_id) REFERENCES public.tenants(id) ON DELETE cascade`.
  8. `ALTER TABLE order_daily_sequences ADD CONSTRAINT order_daily_sequences_location_fk FOREIGN KEY (location_id, tenant_id) REFERENCES public.locations(id, tenant_id) ON DELETE restrict`.
  9. `ALTER TABLE order_daily_sequences ENABLE ROW LEVEL SECURITY;` then `... FORCE ROW LEVEL SECURITY;`.
  10. `CREATE POLICY order_daily_sequences_iso ON order_daily_sequences USING (is_system_session() OR tenant_id = current_tenant_id()) WITH CHECK (is_system_session() OR tenant_id = current_tenant_id());` — **this baseline PERMISSIVE policy is mandatory.** Omitting it is the exact bug `0067_location_tenant_iso.sql` had to retrofit: with FORCE RLS and zero applicable PERMISSIVE policies, Postgres denies every row regardless of any RESTRICTIVE policy.
  11. `CREATE POLICY order_daily_sequences_location_iso ON order_daily_sequences AS RESTRICTIVE USING (is_system_session() OR current_location_id() IS NULL OR location_id = current_location_id()) WITH CHECK (...same...);` — mirrors `0071_orders_location_rls.sql` exactly.

Before adding `short_number`, include a commented row-count guidance line mirroring `0070`'s precedent: existing dev-only order rows are left as-is here (the column is nullable at this stage); plan 10-04's `0075` is where stale dev rows are cleared before `SET NOT NULL`.

**`0074_tenancy_erase_order_sequences.sql`** — header comment citing `Phase 10 Plan 01`, stating that a new tenant-scoped table silently breaks GDPR erasure unless the erase function is extended, and naming the 08.4-08 precedent where exactly this was missed. Body: `DROP FUNCTION IF EXISTS tenancy_erase_tenant(uuid, text, text);` then `CREATE OR REPLACE FUNCTION tenancy_erase_tenant(...)` copied **verbatim** from `0072_tenancy_erase_locations.sql`, with one added statement — `DELETE FROM order_daily_sequences WHERE tenant_id = p_tenant_id;` — placed immediately before the existing `DELETE FROM orders WHERE tenant_id = p_tenant_id;` line. Change nothing else in the function body.

**`meta/_journal.json`** — append two entries after idx 72, keeping the existing `+86400000` per-entry `when` convention (do not use `Date.now()`):
  `{"idx": 73, "version": "7", "when": 1784073600000, "tag": "0073_orders_intake", "breakpoints": true}`
  `{"idx": 74, "version": "7", "when": 1784160000000, "tag": "0074_tenancy_erase_order_sequences", "breakpoints": true}`
  </action>
  <verify>
    <automated>node -e "const j=require('./packages/db/migrations/meta/_journal.json');const t=j.entries.slice(-2).map(e=>e.tag);if(t[0]!=='0073_orders_intake'||t[1]!=='0074_tenancy_erase_order_sequences')throw new Error('journal tail wrong: '+t);console.log('journal ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both `.sql` files exist and each begins with a `-- 00NN_<tag>.sql` header comment naming `Phase 10 Plan 01` and the decision id.
    - `grep -c "statement-breakpoint" packages/db/migrations/0073_orders_intake.sql` returns ≥ 10.
    - `grep -v '^--' packages/db/migrations/0073_orders_intake.sql | grep -c "order_daily_sequences_iso"` returns ≥ 1 (baseline PERMISSIVE policy present, comment lines excluded).
    - `grep -v '^--' packages/db/migrations/0073_orders_intake.sql | grep -c "AS RESTRICTIVE"` returns ≥ 1.
    - `grep -n "REFERENCES public.locations(id, tenant_id)" packages/db/migrations/0073_orders_intake.sql` matches the composite FK.
    - `grep -n "DELETE FROM order_daily_sequences" packages/db/migrations/0074_tenancy_erase_order_sequences.sql` matches, and it appears at a lower line number than `DELETE FROM orders`.
    - `diff <(grep -v order_daily_sequences packages/db/migrations/0074_tenancy_erase_order_sequences.sql) <(cat packages/db/migrations/0072_tenancy_erase_locations.sql)` shows only header-comment differences — the function body is otherwise unchanged.
    - The journal-tail node check prints `journal ok`.
  </acceptance_criteria>
  <done>Migrations 0073 and 0074 are authored in the repo's hand-written style with correct statement breakpoints, both RLS policies, the composite FK, and matching journal entries.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the migrations and assert the live database state</name>
  <files>packages/db/test/integration/erase-includes-ordering.spec.ts</files>
  <read_first>
    - packages/db/src/cli/migrate.ts (how `pnpm db:migrate` resolves its connection and which env var it needs)
    - packages/db/test/integration/erase-includes-ordering.spec.ts (the file being modified — the existing erase assertions to extend)
    - infra/docker/docker-compose.dev.yml (the dev Postgres service, in case `pnpm dev:up` is needed first)
    - .planning/phases/10-admin-order-intake/10-RESEARCH.md (section A.1, step 4 — the migrate invocation)
  </read_first>
  <action>
**This task is BLOCKING for every downstream plan in this phase.** TypeScript types come from the Drizzle schema file, so `build` and `typecheck` pass whether or not the migration was ever applied — asserting the SQL file's contents proves nothing. Prove the applied state by querying the live catalog.

1. Ensure the dev stack is up (`pnpm dev:up` if the Postgres container is not running).
2. Run `pnpm --filter @resto/db db:migrate`.
3. Assert the applied state with `psql` (or `node` + the `postgres` driver) against the dev database, and paste the raw output into the plan SUMMARY:
   - `SELECT column_name, is_nullable, data_type FROM information_schema.columns WHERE table_name='orders' AND column_name IN ('short_number','channel','accepted_at','preparing_at','ready_at','completed_at','canceled_at','cancel_reason','cancel_note','canceled_from_status','canceled_by_user_id','accepted_by_user_id','eta_at','marketing_consent','marketing_consent_at')` — must return 15 rows; `channel` and `marketing_consent` must be `NO` for `is_nullable`, every other new column `YES`.
   - `SELECT to_regclass('public.order_daily_sequences')` — must be non-null.
   - `SELECT policyname, permissive FROM pg_policies WHERE tablename='order_daily_sequences'` — must return exactly two rows: `order_daily_sequences_iso` (PERMISSIVE) and `order_daily_sequences_location_iso` (RESTRICTIVE).
   - `SELECT indexname FROM pg_indexes WHERE tablename='orders' AND indexname='orders_feed_idx'` — must return one row.
   - `SELECT conname FROM pg_constraint WHERE conname IN ('orders_channel_chk','orders_cancel_reason_chk','orders_canceled_from_status_chk','order_daily_sequences_location_fk','order_daily_sequences_tenant_fk')` — must return 5 rows.
   - `SELECT has_table_privilege('resto_app','order_daily_sequences','INSERT') AND has_table_privilege('resto_app','order_daily_sequences','SELECT') AND has_table_privilege('resto_app','order_daily_sequences','UPDATE')` — must be `true`. If it is `false`, add an explicit `GRANT SELECT, INSERT, UPDATE ON order_daily_sequences TO resto_app;` statement to `0073` (guarded by a `DO $$ IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname='resto_app') ... $$` block, mirroring `packages/db/sql/roles.sql` lines 60-73), re-run the migration on a fresh database, and re-assert.
   - `SELECT prosrc LIKE '%order_daily_sequences%' FROM pg_proc WHERE proname='tenancy_erase_tenant'` — must be `true`.
4. Extend `packages/db/test/integration/erase-includes-ordering.spec.ts`: seed an `order_daily_sequences` row for the tenant under test alongside the existing order fixtures, and assert the row count is `0` after `tenancy_erase_tenant` runs — mirroring how the existing spec asserts the ordering tables.
  </action>
  <verify>
    <automated>pnpm --filter @resto/db test -- erase-includes-ordering</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @resto/db db:migrate` exits 0 and its output names `0073_orders_intake` and `0074_tenancy_erase_order_sequences` as applied.
    - The `information_schema.columns` query returns 15 rows with the nullability pattern above; the raw output is pasted in the SUMMARY.
    - `pg_policies` for `order_daily_sequences` returns exactly the two named policies with the stated `permissive` values.
    - `orders_feed_idx` is present in `pg_indexes`.
    - `has_table_privilege('resto_app','order_daily_sequences', ...)` is `true` for SELECT, INSERT and UPDATE.
    - `pg_proc.prosrc` for `tenancy_erase_tenant` contains `order_daily_sequences`.
    - `erase-includes-ordering.spec.ts` passes and now asserts `order_daily_sequences` is emptied by erasure.
  </acceptance_criteria>
  <done>Both migrations are applied to the dev database and the applied state — columns, table, both RLS policies, the index, the constraints, the role grant, and the erase-function body — is proven by live catalog queries, not by reading the SQL file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → Postgres | Every tenant-scoped read/write crosses here; RLS is the last line of defence when `ScopedTx` is bypassed |
| tenant → tenant (same database) | Multi-tenant row isolation on the new counter table |
| location → location (same tenant) | 08.4's location grain must extend to any new order-adjacent table |
| data subject → retained PII | GDPR erasure must reach every table holding tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | `order_daily_sequences` cross-tenant read | mitigate | Baseline PERMISSIVE `order_daily_sequences_iso` policy on `tenant_id = current_tenant_id()`, plus ENABLE + FORCE RLS; asserted live via `pg_policies` in Task 3 |
| T-10-01-02 | Elevation of Privilege | `order_daily_sequences` cross-location access within a tenant | mitigate | RESTRICTIVE `order_daily_sequences_location_iso` policy mirroring `0071_orders_location_rls.sql` |
| T-10-01-03 | Denial of Service | FORCE RLS with no PERMISSIVE policy denies all rows, breaking order creation entirely | mitigate | Task 2 mandates the baseline `_iso` policy; Task 3's `pg_policies` assertion requires exactly two policies with the correct `permissive` values (the 0067 retrofit landmine) |
| T-10-01-04 | Repudiation | New GDPR-relevant table survives tenant erasure | mitigate | `0074` extends `tenancy_erase_tenant` with `DELETE FROM order_daily_sequences`; proven by `pg_proc.prosrc` and by the extended integration spec |
| T-10-01-05 | Tampering | Orphaned counter rows pointing at a deleted location | mitigate | Composite FK `(location_id, tenant_id) → locations(id, tenant_id) ON DELETE RESTRICT`, matching `orders_location_fk` |
| T-10-01-06 | Information Disclosure | New PII-adjacent columns (`marketing_consent_at`, `cancel_note`) escaping erasure | accept | They live on `orders`, which `tenancy_erase_tenant` already deletes wholesale (`0051`); no new surface |
| T-10-01-07 | Tampering | Unconstrained `cancel_reason` / `channel` free text corrupting analytics | mitigate | CHECK constraints pinning the canonical seven reason codes and the two channel values, asserted via `pg_constraint` |

**Package legitimacy:** this plan installs no npm/pip/cargo packages. No legitimacy gate applies.
</threat_model>

<verification>
- `pnpm --filter @resto/db exec tsc --noEmit` — clean.
- `pnpm --filter @resto/db db:migrate` — applied, exit 0.
- All Task 3 catalog queries return the asserted shapes, raw output pasted in the SUMMARY.
- `pnpm --filter @resto/db test -- erase-includes-ordering` — green.
- `git diff` touches only the five files in `files_modified`.
</verification>

<success_criteria>
- Fifteen new columns exist on `orders` in the live dev database with the specified nullability.
- `order_daily_sequences` exists with a composite PK, both FKs, ENABLE + FORCE RLS, and exactly two policies (one PERMISSIVE tenant, one RESTRICTIVE location).
- `orders_feed_idx` exists on `(tenant_id, location_id, status, created_at DESC)`.
- `tenancy_erase_tenant` deletes `order_daily_sequences` rows, proven by an integration test.
- The `status='created'`-before-payment invariant (Growth MED-18) is recorded in the `0073` header comment.
</success_criteria>

<output>
Create `.planning/phases/10-admin-order-intake/10-01-SUMMARY.md` when done. It MUST include the raw output of every Task 3 catalog query.
</output>
