---
phase: 10.2-brand-pinned-sessions
plan: 22
type: execute
wave: 7
depends_on: ['10.2-11']
files_modified:
  - apps/api/src/contexts/identity/application/archive-role.service.ts
  - apps/api/src/contexts/identity/application/assign-role.service.ts
  - apps/api/src/contexts/identity/application/list-active-custom-roles.ts
  - apps/api/src/contexts/identity/application/list-members.service.ts
  - apps/api/src/contexts/identity/application/seed-preset-roles.service.ts
  - apps/api/src/contexts/identity/application/sync-preset-roles.service.ts
  - apps/api/src/contexts/identity/infrastructure/ba-session-drizzle.revoker.ts
  - apps/api/src/contexts/identity/infrastructure/ba-user-drizzle.reader.ts
  - apps/api/src/contexts/identity/infrastructure/tenant-provisioning.adapter.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts
  - apps/api/src/contexts/identity/interfaces/http/me.controller.ts
autonomous: true
requirements: []
decisions: [D-41]

must_haves:
  truths:
    - '`apps/api` compiles: zero typecheck errors under `apps/api/src/`'
    - 'The API boots against the merged dev database, so every remaining checkpoint in this phase can verify against a running server instead of only against source'
    - 'No `organization`-named identifier remains in the identity application, infrastructure or interfaces layers outside `infrastructure/better-auth/`'
  artifacts:
    - path: 'apps/api/src/contexts/identity'
      provides: 'an identity context that compiles against the tenant-named Better Auth schema'
  key_links:
    - from: 'apps/api/src/contexts/identity'
      to: 'packages/db/src/schema/auth.ts'
      via: 'tenantRole / member.tenantId / session.activeTenantId'
      pattern: 'tenantRole'
---

<objective>
Make `apps/api` compile again by finishing the identity half of D-41's code sweep.

Purpose: this is scheduling repair, not new scope. Plan 05 Task 2b renamed the Drizzle exports and fields
(`organizationRole` → `tenantRole`, `member.organizationId` → `.tenantId`,
`session.activeOrganizationId` → `.activeTenantId`) and told Better Auth about it through its `schema`
option. Eleven identity files still read the old names; none of them were in plan 05's `files_modified`,
and the orchestrator parked their repair in plan 21 at wave 14.

That parking was a mistake and this plan undoes it. With `apps/api` uncompilable, **no checkpoint between
wave 6 and wave 14 can verify anything against a running server** — not plan 11's route probes, not plan
12's session rebind, not plan 13's signup flow. Live verification is the entire reason those gates exist;
this phase has already produced one endpoint that was dead in production while every static gate stayed
green (`10.2-FINDINGS.md` F-27).

Output: `npx tsc -p apps/api/tsconfig.json --noEmit` reports zero errors under `apps/api/src/`, and the
API boots.
</objective>

<context>
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-05-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the identity context at the tenant-named Better Auth schema</name>
  <files>all eleven files in files_modified</files>
  <read_first>
    - packages/db/src/schema/auth.ts (the authority: `export const tenantRole` on table `tenant_role`, `member.tenantId`, `invitation.tenantId`, `session.activeTenantId`)
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (the `schema` mapping that tells BA where those live — do NOT change it, and note it uses JS property names, not SQL column names; getting that backwards already cost this phase one runtime bug, see F-12)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md D-41
  </read_first>
  <action>
Get the exact work list first, and record the starting count:

```
npx tsc -p apps/api/tsconfig.json --noEmit 2>&1 | grep "error TS" | grep '^apps/api/src/'
```

Anchor that grep to the start of the line. An unanchored `grep 'contexts/identity'` also matches import
paths quoted inside other files' error text, which inflates the count with other files' problems — the
orchestrator got this wrong twice.

Then repoint each reference:

- `organizationRole` (the Drizzle export) → `tenantRole`
- `organizationRole.organizationId` → `tenantRole.tenantId`
- `member.organizationId` → `member.tenantId`
- `session.activeOrganizationId` → `session.activeTenantId`
- any local variable, parameter or destructured name carrying those values → the tenant spelling

`tenant-provisioning.adapter.ts`'s error is an object literal with a stale key, not an import — read what
the target type wants rather than pattern-matching the rename.

**The boundary that stays.** Better Auth's own API surface keeps saying `organization` —
`OrganizationOptions`, `organizationHooks`, `activeOrganizationId` as BA's _logical_ field name,
`POST /api/auth/organization/set-active`. That vocabulary is legitimate **only** inside
`apps/api/src/contexts/identity/infrastructure/better-auth/`. None of your eleven files are in that
directory, so none of them should end up saying `organization`. If a file needs something from BA, it goes
through the adapter.

This is a rename against a schema that already exists. Do not restructure, do not change a query's
predicates, and do not "improve" a service while you are in it. If a rename appears to require a behaviour
change, stop and say so — that is a finding, not a task.
</action>
<verify>
<automated>npx tsc -p apps/api/tsconfig.json --noEmit 2>&1 | grep "error TS" | grep -c '^apps/api/src/'</automated>
</verify>
<acceptance_criteria> - The `<automated>` count is 0. - `rg -c "organizationRole|organizationId|activeOrganizationId" apps/api/src/contexts/identity --glob '!**/better-auth/**'` returns 0. - `rg -c "organization" apps/api/src/contexts/identity --glob '!**/better-auth/**'` returns 0, or every remaining hit is named in the SUMMARY with a reason. - The starting error count and the finishing count are both recorded in the SUMMARY. - No query's WHERE clause changed: for each file touched, state in the SUMMARY whether the edit was purely a name change, and name any exception.
</acceptance_criteria>
<done>The identity context compiles against the tenant-named schema.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the API actually boots</name>
  <files>none — this task produces evidence</files>
  <read_first>
    - apps/api/src/main.ts (the preflight assertions that run before any controller mounts — `assertNoRlsBypass`, `assertTenantLockInstalled`, `assertSetConfigRevoked`, `assertNoBaCredentialAccess`, `assertInboxProcessedDeletable`, `assertWithoutTenantCallsiteRegistered`, `assertSystemRolesPresent`; plan 05 deleted `assertBrandRlsInstalled`)
    - .env (the dev connection strings; the CLI reads `process.env` directly with no dotenv, so source it: `set -a; . ./.env; set +a`)
  </read_first>
  <action>
A green typecheck is not a booting server. This phase has already shipped an endpoint that compiled,
passed its tests and failed on every request (F-27), so prove the process starts.

The dev stack is up (`resto-postgres` healthy) and migration 0079 is applied. Start the API, wait for it
to report listening, hit `GET /healthz`, and stop it. Capture the boot log.

What to watch for beyond "it started":

- every preflight assertion passes — these are the tenancy invariants, and one of them was deleted this
  phase, so confirm the surviving seven all run;
- no Better Auth error mentioning a model or field it cannot find. `getSchema` throws
  `The model "..." was not found in the schema object` when the `schema` mapping points at a name that
  does not exist. That is the exact failure F-12 describes, and a typecheck cannot see it.

Paste the boot log's assertion lines and the `/healthz` response into the SUMMARY. If the API does not
boot, that is the finding — report it rather than working around it.
</action>
<verify>
<automated>set -a; . ./.env; set +a; NODE_ENV=development timeout 90 pnpm --filter api start > /tmp/api-boot.log 2>&1 & sleep 45; curl -sf http://localhost:3000/healthz && echo BOOT_OK</automated>
</verify>
<acceptance_criteria> - `GET /healthz` returns success. - The SUMMARY contains the boot log's preflight assertion lines, all seven passing. - The SUMMARY confirms no Better Auth schema-mapping error appeared at boot. - If the port differs from 3000 in this environment, the SUMMARY records the real one.
</acceptance_criteria>
<done>The API compiles AND runs, so the remaining checkpoints in this phase can verify against a live server.</done>
</task>

</tasks>

<verification>
`RESTO_SPINE_MIGRATION=1` should NOT be needed for this plan's commits — closing the last `apps/api/src/`
errors is exactly the point. If a commit trips the affected-typecheck hook, that means the work is not
finished; fix it rather than reaching for the flag, and record in the SUMMARY that the flag was not used.

`apps/api` tests are expected to remain red — 246 test-file errors reference deleted brand modules and
belong to plan 19. This plan changes nothing about that and must not try to.
</verification>
