---
phase: 10.2-brand-pinned-sessions
plan: 07
type: execute
wave: 5
depends_on: ['10.2-06']
files_modified:
  - apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts
  - apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts
  - apps/api/src/contexts/tenancy/application/brand-queries.service.ts
  - apps/api/src/contexts/tenancy/application/tenant-queries.service.ts
  - apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts
  - apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts
  - apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts
  - apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts
  - apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/tenancy/tenancy.module.ts
  - apps/api/src/shared/tenant-context.middleware.ts
  - apps/api/src/contexts/tenancy/application/list-locations.service.ts
  - apps/api/src/contexts/tenancy/application/provision-location.service.ts
  - apps/api/src/contexts/tenancy/application/archive-location.service.ts
  - apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts
autonomous: true
requirements: []
decisions: [D-22, D-23, D-39, D-40]

must_haves:
  truths:
    - 'A guest hitting `<slug>.menu.resto.app` resolves to exactly one restaurant, with no second entity to look up'
    - 'D-22: the three host branches are distinct — `.menu.` for guests, `.admin.` for staff, bare for the future public site — and the API resolves the guest branch only'
    - 'D-39: exactly one Stripe onboarding path exists; the dead tenant-level stub and its route are deleted'
    - 'D-40: no tenancy service, controller or route path contains the word `brand`'
    - 'The `x-brand-slug` request header is no longer read anywhere'
  artifacts:
    - path: 'apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts'
      provides: 'single host->tenant resolution'
    - path: 'apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts'
      provides: 'the one surviving Stripe Connect onboarding path'
    - path: 'apps/api/src/shared/tenant-context.middleware.ts'
      provides: 'tenant + location context resolution with no brand dimension'
  key_links:
    - from: 'apps/api/src/shared/tenant-context.middleware.ts'
      to: 'apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts'
      via: 'injected resolver for customer-host lookup'
      pattern: 'TenantResolverService'
    - from: 'apps/api/src/contexts/tenancy/tenancy.module.ts'
      to: 'apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts'
      via: 'NestJS provider registration'
      pattern: 'StartTenantOnboardingService'
---

<objective>
Collapse the tenancy context's HTTP and resolution surface onto the merged entity: one host resolver, one
queries service, one Stripe onboarding path, tenant-named controllers, and a middleware that no longer
reads `x-brand-slug`.

Purpose: the middleware is the entry point for every tenant-scoped request in the system. Until its brand
branch is gone, the `TenantContext` shape plan 04 defined cannot be satisfied at runtime.
Output: `apps/api/src/contexts/tenancy/` and `apps/api/src/shared/tenant-context.middleware.ts` contain no
brand vocabulary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Absorb the location cluster — four files no plan in this phase owned</name>
  <files>apps/api/src/contexts/tenancy/application/list-locations.service.ts, apps/api/src/contexts/tenancy/application/provision-location.service.ts, apps/api/src/contexts/tenancy/application/archive-location.service.ts, apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts</files>
  <read_first>
    - .planning/phases/10.2-brand-pinned-sessions/deferred-items.md ("From plan 06" — the entry that found this gap)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-FINDINGS.md F-20
    - apps/api/src/contexts/tenancy/domain/ports.ts (`LocationRepository.listForTenant(tenantId)` — plan 06 renamed it from `listForBrand(brandId, tenantId)`)
    - apps/api/src/contexts/tenancy/domain/location.aggregate.ts (plan 06 removed `LocationSnapshot.brandId` and `LocationArchivedEvent.brandId`)
    - packages/db/src/schema/locations.ts (the table has no `brand_id`; plan 03 relocated it out of the deleted `brands.ts`)
  </read_first>
  <action>
**Added to this plan on 2026-08-20 by the orchestrator.** A `grep -l` across every `*-PLAN.md` in this
phase returns no owner for `list-locations.service.ts`, `provision-location.service.ts` or
`archive-location.service.ts`; `location-drizzle.repository.ts` appears in plan 08 only as a "do NOT
delete the similarly-named file" warning, never as work. All four are broken, and
`locations.controller.ts` — which this plan does own — depends on all of them. Without this task, Task 3
cannot compile.

Repair the four files against the merged model:

- `location-drizzle.repository.ts` — currently `implements LocationRepository` while still defining
  `listForBrand(brandId, tenantId)`, which is a hard `TS2420` error. Replace it with
  `listForTenant(tenantId)` matching the port, drop every `brands` join and `locations.brandId`
  reference, and keep the query going through `ScopedTx` — the tenant filter is now the ONLY filter,
  so losing it is a cross-tenant leak, not a cosmetic regression.
- `list-locations.service.ts` — repoint at `listForTenant`, drop the `BrandId` import and any brand
  parameter threading from its caller signature.
- `provision-location.service.ts` and `archive-location.service.ts` — drop `brandId` from the snapshot
  and event payloads (plan 06 already removed those fields from `LocationSnapshot` and
  `LocationArchivedEvent`), and drop `requireBrandContext`/`withBrand` usage (removed by plan 04).

Their two spec files (`test/unit/tenancy/archive-location.service.spec.ts`,
`test/unit/tenancy/provision-location.service.spec.ts`) are broken for the same reasons — repair them
alongside. If an assertion tested brand-grain behaviour that no longer exists, delete that assertion and
name it in the SUMMARY with the reason; do not weaken a surviving tenant-grain or location-grain
assertion to make a file pass.
</action>
<verify>
<automated>npx tsc -p apps/api/tsconfig.json --noEmit 2>&1 | grep -cE "tenancy/(application/(list-locations|provision-location|archive-location)|infrastructure/location-drizzle)"</automated>
</verify>
<acceptance_criteria> - The `<automated>` count is 0. - `rg -n "listForBrand" apps/api/src` returns no matches. - `rg -n "brandId|BrandId" apps/api/src/contexts/tenancy/application/list-locations.service.ts apps/api/src/contexts/tenancy/application/provision-location.service.ts apps/api/src/contexts/tenancy/application/archive-location.service.ts apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts` returns no matches. - `location-drizzle.repository.ts` still routes every read and write through `ScopedTx` — quote the relevant lines in the SUMMARY, since the tenant filter is now the only one standing. - Every deleted test assertion named in the SUMMARY with its reason.
</acceptance_criteria>
<done>The location cluster compiles against the merged model and `locations.controller.ts` has something to depend on.</done>
</task>

<task type="auto">
  <name>Task 1: Collapse host resolution and queries onto one service, and drop the x-brand-slug header</name>
  <files>apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts, apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts, apps/api/src/contexts/tenancy/application/brand-queries.service.ts, apps/api/src/contexts/tenancy/application/tenant-queries.service.ts, apps/api/src/shared/tenant-context.middleware.ts</files>
  <read_first>
    - apps/api/src/shared/tenant-context.middleware.ts (147 lines; `resolveContext` at ~69-93 has three branches: `resolveByCustomerHost` returning a tenant+brand pair, `resolveTenantOnly` plus the `x-brand-slug` header lookup at ~86-90, and the tenant-only fallback; also `HEADER_BRAND`, `HEADER_LOCATION`, `effectiveHost`, `runInTenantContext`)
    - apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts (`resolveByCustomerHost`, `resolveBrandBySlug` — the service injected into the middleware)
    - apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts (the tenant-only sibling that survives and absorbs the above)
    - apps/api/src/contexts/tenancy/application/brand-queries.service.ts and tenant-queries.service.ts (the read-side pair being merged)
    - packages/db/src/context.ts (post-plan-04 `TenantContext` — no `brandId` field, so any middleware branch returning one is now a compile error)
    - CLAUDE.md §"Architectural Constraints" (`runInTenantContext` is HTTP-middleware-only, ADR-0020 I-6)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md §4 "TenantContextMiddleware"
  </read_first>
  <action>
Delete `tenant-and-brand-resolver.service.ts`, moving its surviving behaviour into
`tenant-resolver.service.ts`: `resolveByCustomerHost(host)` now returns a tenant only (the guest-menu host
`<slug>.menu.resto.app` maps to one restaurant after the merge), and `resolveBrandBySlug` is deleted with no
replacement — there is no second entity to resolve inside a tenant.

Merge `brand-queries.service.ts` into `tenant-queries.service.ts` and delete the brand file. Keep the
surviving read methods under tenant-flavoured names (D-40).

`apps/api/src/shared/tenant-context.middleware.ts`:

- delete the whole `x-brand-slug` block (the `HEADER_BRAND` constant, the header read, and the
  `resolveBrandBySlug` call that follows it). There is no brand to look up separately from the tenant, and
  leaving a header the server ignores invites a client to believe it still works;
- the `resolveByCustomerHost` branch returns `{ tenantId, ...(locationId ? { locationId } : {}) }`;
- keep the `x-location-id` handling exactly as it is, including its comment that the header is a
  client-echoed hint and `LocationScopeGuard` is the real gate (T-084-09);
- keep `runInTenantContext` usage where it is — it is legitimately inside HTTP middleware, which is the
  only place ADR-0020 I-6 permits it. Do not move it or introduce a second binding site.

D-22 boundary to preserve while editing host resolution: `.menu.` is the guest branch this middleware
resolves; `.admin.` is the operator branch, whose context comes from the session's `activeOrganizationId`
via the `x-tenant-id` header, not from the host; the bare `<slug>.resto.app` is reserved for the future
public website and must NOT be resolved by this middleware. If the current customer-host matcher would
accept a bare `<slug>.resto.app`, tighten it to the `.menu.` label and say so in the SUMMARY.
</action>
<verify>
<automated>cd apps/api && npx vitest run test/unit/shared/tenant-context.middleware.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `test ! -f apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts` and `test ! -f apps/api/src/contexts/tenancy/application/brand-queries.service.ts` both succeed. - `rg -in "brand" apps/api/src/shared/tenant-context.middleware.ts` returns no matches. - `rg -c "x-brand-slug" apps/api/src` returns 0. - `rg -n "x-location-id|HEADER_LOCATION" apps/api/src/shared/tenant-context.middleware.ts` still matches. - `rg -n "runInTenantContext" apps/api/src` matches ONLY in `apps/api/src/shared/tenant-context.middleware.ts` (and its spec), per ADR-0020 I-6. - The SUMMARY states whether the customer-host matcher was tightened to the `.menu.` label and shows the matcher's current form.
</acceptance_criteria>
<done>One resolver maps a guest host to one restaurant; no request header can introduce a second context dimension.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead Stripe onboarding path and rename the live one</name>
  <files>apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts, apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts, apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts, apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts (the DEAD path: reads the tenant's `displayName`, sends it to Stripe as `business_profile.name`, calls the no-op `tenant.linkStripeAccount`)
    - apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts (the LIVE path: real `stripe_charges_enabled` / `stripe_payouts_enabled` / `stripe_onboarding_status` / `stripe_requirements_due` columns updated by the `account.updated` webhook per PAY-05; also handles Connect Standard OAuth from Phase 8.1)
    - apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts (route `POST /v1/tenancy/stripe-onboarding` — being deleted)
    - apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts (the surviving route surface being renamed)
    - apps/api/src/contexts/tenancy/domain/oauth-state.ts (the Connect Standard OAuth state value object the live path uses)
    - apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts (already accepts optional `country` and `default_currency` that nothing supplies — this is the wire D-35's derivation finally feeds)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md Pitfall 8
  </read_first>
  <action>
Delete `start-stripe-onboarding.service.ts` and `stripe-onboarding.controller.ts`, including the
`POST /v1/tenancy/stripe-onboarding` route registration in `tenancy.module.ts`. RESEARCH Pitfall 8 confirms
this path is fully dead, not merely duplicated: it calls `tenant.linkStripeAccount(_accountId)` which was a
literal no-op, and its `getStatus()` returned a hardcoded `not_started` shape forever. Nothing at runtime
signals the breakage, which is exactly why D-39 names it.

Rename `start-brand-onboarding.service.ts` -> `start-tenant-onboarding.service.ts` and
`StartBrandOnboardingService` -> `StartTenantOnboardingService`; rename `brand-onboarding.controller.ts` ->
`tenant-onboarding.controller.ts` with class `TenantOnboardingController`. Change its route paths so no
segment says `brand` (D-40) and record the exact old->new route strings in the SUMMARY — plan 14 updates
the admin client to match, and `docs/api/openapi.yaml` regenerates from these decorators.

While in the live path, wire the two Stripe inputs that exist but were never supplied: pass the tenant's
`country` and `defaultCurrency` (both now columns on the merged table, per plan 03) into the
`stripe-provider.adapter.ts` account-creation call. This is D-35's payoff — a Ukrainian restaurant's Connect
account is created with `country: 'UA'` and `default_currency: 'UAH'` instead of Stripe's platform default.

Keep the `wrapWith(mapError)` try/catch shape and the domain-error -> `HttpException` mapping in
`interfaces/http/error-mapping.ts`, updating the mapped error class names to whatever plan 06 renamed them
to. Never map `unknown` — return it unchanged (CLAUDE.md).
</action>
<verify>
<automated>cd apps/api && npx vitest run test/unit/tenancy/start-brand-onboarding-url.spec.ts test/unit/tenancy/brand-onboarding-oauth.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `test ! -f apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts` and `test ! -f apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts` both succeed. - `rg -c "StartStripeOnboardingService|stripe-onboarding" apps/api/src` returns 0. - `test -f apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts` succeeds and `rg -n "StartTenantOnboardingService" apps/api/src/contexts/tenancy/tenancy.module.ts` matches. - `rg -n "country" apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts` matches and the value reaches the Stripe adapter call. - `rg -in "brand" apps/api/src/contexts/tenancy/application/` returns no matches. - The SUMMARY lists old->new route paths for the onboarding controller.
</acceptance_criteria>
<done>One Stripe onboarding path remains, it is the one with live columns behind it, and it now tells Stripe the restaurant's country and currency.</done>
</task>

<task type="auto">
  <name>Task 3: Rename the remaining tenancy controllers, response shapes and module wiring</name>
  <files>apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts, apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts, apps/api/src/contexts/tenancy/tenancy.module.ts</files>
  <read_first>
    - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts (surviving operator routes; note which handlers carry `@BrandNeutral()` — plan 11 owns that decorator, do not remove it here)
    - apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts (internal-token-guarded provisioning route consumed by the seed CLI)
    - apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts (locations survive per D-19; check whether it reads a brand from context)
    - apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts (the wire shape that gains the merged columns)
    - apps/api/src/contexts/tenancy/interfaces/http/error-mapping.ts (`mapDomainError` for this context)
    - apps/api/src/contexts/tenancy/tenancy.module.ts (provider and controller registration for everything renamed or deleted in Tasks 1 and 2)
    - apps/api/src/shared/api/wrap.ts (the `wrapWith(mapper)` helper every controller uses)
  </read_first>
  <action>
Update the tenancy HTTP surface to the merged model.

`tenant-response.ts`: extend the wire shape with the columns that moved onto the tenant — `country`,
`theme`, `legalName`, `legalForm`, `taxId`, `paymentProvider`, `accountType`, the four `stripe*` fields —
so the admin can render payouts, theme and domains without a second fetch. Use unbranded Zod value types at
this boundary (`TenantSlugValue`, `CountryCodeValue`, `CurrencyValue`) so `nestjs-zod` emits `type: string`
rather than `unknown` in the OpenAPI output (ADR-0020 I-7). Delete any brand-shaped response type.

`tenants.controller.ts` and `internal-tenants.controller.ts`: remove any brand parameter, brand path segment
or brand response field; keep `@Permissions({...})` decorators and the `RestoZodValidationPipe` per-parameter
validation pattern (`@Body(new RestoZodValidationPipe(SomeDto)) input: SomeDto`) unchanged — the pipe is
applied per-parameter deliberately because the transpiler does not emit `design:paramtypes` for a global pipe.

`locations.controller.ts`: locations survive the merge unchanged in meaning (D-19). Remove only the brand
dimension from any query or response, and confirm the `LocationScopeGuard`-relevant decorators are untouched
— plan 11 owns the decorator sweep.

`error-mapping.ts`: map the renamed domain errors from plan 06 (e.g. `TenantSlugConflictError`) and delete
mappings for deleted errors. Do not map `unknown`.

`tenancy.module.ts`: remove providers for every deleted service/repository, register the renamed ones, and
remove the deleted controllers. Providers are injected by Symbol token, never by class (CLAUDE.md).
</action>
<verify>
<automated>cd apps/api && npx tsc -p tsconfig.json --noEmit 2>&1 | rg -c "contexts/tenancy" || true</automated>
</verify>
<acceptance_criteria> - `rg -in "brand" apps/api/src/contexts/tenancy/` returns no matches across the whole context directory. - `rg -n "country" apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts` matches. - `rg -n "RestoZodValidationPipe" apps/api/src/contexts/tenancy/interfaces/http/` still matches (per-parameter validation preserved). - `cd apps/api && npx tsc -p tsconfig.json --noEmit` reports zero errors originating in `apps/api/src/contexts/tenancy/`. - `rg -n "BRAND_REPOSITORY|BrandRepository|BrandQueriesService|TenantAndBrandResolverService" apps/api/src` returns 0.
</acceptance_criteria>
<done>The tenancy context compiles clean with no brand vocabulary in any file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                      | Description                                                                |
| ----------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Request `Host` header -> tenant resolution                                    | Untrusted host string selects which restaurant's data is served to a guest |
| Request headers (`x-tenant-id`, `x-location-id`) -> AsyncLocalStorage context | Client-echoed hints; guards are the real fence                             |
| Operator request -> Stripe Connect account creation                           | Country and currency now flow from our data into a payment provider        |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                      |
| ------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-07-01 | Spoofing               | Guest-host matcher accepting a bare `<slug>.resto.app` and serving menu data on the reserved public-site host | mitigate    | Task 1 tightens `resolveByCustomerHost` to the `.menu.` label per D-22 and records the matcher form in the SUMMARY                                                                                                                                                                                                   |
| T-10.2-07-02 | Elevation of Privilege | `x-brand-slug` left readable and used as a context selector                                                   | mitigate    | Header read deleted; `rg -c "x-brand-slug" apps/api/src` returns 0                                                                                                                                                                                                                                                   |
| T-10.2-07-03 | Information Disclosure | Merged `tenant-response.ts` exposing legal/Stripe fields on a public route                                    | mitigate    | The extended response type belongs to operator-authenticated routes only; verify no public controller (`public-menu.controller.ts`, checkout) imports it — assert with `rg -l "tenant-response" apps/api/src/contexts/catalog apps/api/src/contexts/payments` returning no public-route match, and record the result |
| T-10.2-07-04 | Tampering              | `runInTenantContext` called outside HTTP middleware while refactoring                                         | mitigate    | Acceptance criterion restricts its occurrences to the middleware file and its spec (ADR-0020 I-6)                                                                                                                                                                                                                    |
| T-10.2-07-05 | Repudiation            | Deleting the wrong Stripe path and losing live webhook-driven state                                           | mitigate    | RESEARCH Pitfall 8 identifies the dead path by its no-op `linkStripeAccount` and hardcoded `getStatus`; the surviving path is the one with the four live `stripe_*` columns behind it, asserted by the onboarding-URL and OAuth unit specs still passing                                                             |
| T-10.2-07-SC | Tampering              | npm/pip/cargo installs                                                                                        | accept      | No packages installed                                                                                                                                                                                                                                                                                                |

</threat_model>

<verification>
- `cd apps/api && npx tsc -p tsconfig.json --noEmit` reports zero errors from `apps/api/src/contexts/tenancy/` and
  `apps/api/src/shared/tenant-context.middleware.ts`.
- `rg -in "brand" apps/api/src/contexts/tenancy/ apps/api/src/shared/tenant-context.middleware.ts` returns 0.
- `rg -c "x-brand-slug" apps/api/src` returns 0.
- Targeted specs (run individually, never the whole suite in one process — known false-429 gotcha):
  `cd apps/api && npx vitest run test/unit/shared/tenant-context.middleware.spec.ts`
  and `npx vitest run test/unit/tenancy/tenant-resolver.service.spec.ts`. Brand-specific assertions in these
  specs are rewritten in plan 19; record which failures are brand-assertion-only.
</verification>

<success_criteria>

- One host resolver, one queries service, one Stripe onboarding path.
- No brand header, brand route segment or brand response field anywhere in the tenancy context.
- Stripe Connect accounts are created with the restaurant's real country and currency.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md` when done.
</output>
