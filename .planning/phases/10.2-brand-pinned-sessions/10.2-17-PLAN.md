---
phase: 10.2-brand-pinned-sessions
plan: 17
type: execute
wave: 12
depends_on: ['10.2-02', '10.2-13', '10.2-15']
files_modified:
  - apps/admin/src/routes/(auth)/signup.tsx
  - apps/admin/src/routes/(auth)/login.tsx
  - apps/admin/src/routes/(auth)/pick-organization.tsx
  - apps/admin/src/routes/(protected)/onboarding/index.tsx
  - apps/admin/src/routes/(protected)/onboarding/brand.tsx
  - apps/admin/src/components/nav-user.tsx
  - apps/admin/src/lib/admin-host.ts
  - apps/admin/src/main.tsx
  - apps/admin/src/env.ts
  - apps/admin/vite.config.ts
  - .env.example
autonomous: false
requirements: []
decisions:
  [
    D-02,
    D-06,
    D-15,
    D-16,
    D-17,
    D-18,
    D-20,
    D-21,
    D-22,
    D-24,
    D-25,
    D-26,
    D-27,
    D-28,
    D-30,
    D-31,
    D-36,
  ]

must_haves:
  truths:
    - 'D-25/D-26/D-28: signup asks for name, email, password and country, posts to `/v1/signup`, and no currency field exists'
    - 'D-17/D-18: an owner of two or more organizations picks one at every sign-in, on a screen that mirrors the staff pick-location interstitial; an owner of exactly one goes straight through'
    - "D-15/D-16/D-21: picking an organization revokes and re-issues the session server-side and lands the operator on that organization's host, with no password re-prompt"
    - "D-21: opening a host that disagrees with the bound session redirects to the bound organization's host, checked once at bootstrap"
    - "D-30/D-06: onboarding asks for the restaurant's name only and shows the host it will be reachable at"
    - 'D-31: an owner who abandoned onboarding resumes at onboarding on their next sign-in'
    - "D-36: the interface language is seeded from the signup country's default locale"
  artifacts:
    - path: 'apps/admin/src/routes/(auth)/pick-organization.tsx'
      provides: 'the sign-in organization picker'
    - path: 'apps/admin/src/lib/admin-host.ts'
      provides: 'host parsing and per-organization URL construction'
      contains: 'hostname'
  key_links:
    - from: 'apps/admin/src/routes/(auth)/pick-organization.tsx'
      to: 'apps/api/src/contexts/identity/infrastructure/better-auth/organization-switch.plugin.ts'
      via: 'POST /api/auth/switch-organization then a hard navigation'
      pattern: 'switch-organization'
    - from: 'apps/admin/src/main.tsx'
      to: 'apps/admin/src/lib/admin-host.ts'
      via: 'bootstrap host-vs-session reconciliation'
      pattern: 'admin-host'
---

<objective>
Build the account-creation and organization-entry journey in the admin: a four-field signup that calls
`/v1/signup`, a sign-in picker mirroring the staff pick-location screen, the switch-and-navigate flow across
per-organization hosts, a one-field onboarding screen, and the single bootstrap check that reconciles the
host against the session.

Purpose: this is the phase's user-visible half. Everything before it made the model correct; this makes it
reachable.
Output: a new owner can go from "create account" to a named, active restaurant, and an owner of several can
choose which one they are entering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@apps/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-12-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-15-SUMMARY.md

<interfaces>
Contract this plan CREATES for host handling. Keep it in one module so there is one parser.

apps/admin/src/lib/admin-host.ts:
export const ADMIN_HOST_SUFFIX: string // from VITE_ADMIN_HOST_SUFFIX, e.g. admin.resto.app
export const parseOrgSlugFromHost: (hostname: string) => string | null
// splits window.location.hostname on '.', matches the suffix LABEL BY LABEL, returns the leftmost
// label or null. Never uses endsWith() — `evil-admin.resto.app.attacker.com` must return null.
export const adminUrlForOrg: (slug: string, path?: string) => string
// https://<slug>.<ADMIN_HOST_SUFFIX><path> (dev: http://<slug>.admin.localhost:4000<path>)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the signup form and the post-sign-in branching</name>
  <files>apps/admin/src/routes/(auth)/signup.tsx, apps/admin/src/routes/(auth)/login.tsx</files>
  <read_first>
    - apps/admin/src/routes/(auth)/signup.tsx (119 lines, read in full: `SignUpSchema` with `displayName`/`email`/`password`/`defaultCurrency`; the "Restaurant name" label at ~63 on a field whose value goes to Better Auth as the PERSON's name at ~46; the hand-styled raw `<select>` at ~88-100 that is collected and never sent; the `authClient.signUp.email(...)` call at ~43-47; the `role="alert"` error paragraph at ~101-105)
    - apps/admin/src/routes/(auth)/login.tsx (the `authClient.organization.list()` + blind `setActive(orgs.data[0])` at ~55-58; the staff pick-location branch at ~60-71 that must survive; the `expired === '1'` banner using `tAuth('sessionExpired')`)
    - apps/admin/src/components/ui/select.tsx (the installed shadcn `Select` replacing the raw `<select>` — a reuse, not a new install)
    - packages/domain/src/country.ts (plan 01: `SUPPORTED_COUNTRIES`, `CountryCodeValue`, `defaultLocaleForCountry`)
    - apps/admin/src/lib/i18n/messages/en.json (plan 02: the `auth.signup.*` keys, including `countryOption.ua|gb|es`)
    - apps/admin/src/lib/queries/identity.ts (plan 14: `meTenantsQuery` returning `slug` and `status`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S1 and §S2
  </read_first>
  <action>
`signup.tsx`:
- `SignUpSchema` becomes `{ name, email, password, country }`. `country` is a `z.enum` built from
  `SUPPORTED_COUNTRIES` so the form and the API validate against one source (D-33).
- Field 1's label changes from "Restaurant name" to `auth.signup.nameLabel` ("Your name"). This is the fix,
  not a rename of the same wrong thing — the value always went to Better Auth as the person's name (D-27).
- Delete the currency `<select>` entirely (D-28: collected and never sent). Replace it with a shadcn
  `Select` bound to `country`, three `SelectItem`s in D-33's order (UA, GB, ES), each labelled with the
  country and its derived currency ("Ukraine · UAH") — transparency about a consequence, not a second
  question (D-35).
- Submit calls `POST /v1/signup` via `apiFetch`, not `authClient.signUp.email` (D-26). The direct call is
  what produces users with zero memberships, verified live 2026-08-19.
- On success, seed the interface language from the chosen country: `i18n.changeLanguage(defaultLocaleForCountry(country))`
  (D-36). This is a user-level choice living in the existing i18next cookie/localStorage detection — do not
  add a database column or a second locale mechanism.
- On success, navigate to `/onboarding` (D-30). A brand-new account has exactly one organization, in
  `pending_setup`, so it never goes through the picker.
- Move the card title, all four field labels, both button states, the error text and the footer link behind
  `useTranslation('translation', { keyPrefix: 'auth.signup' })`, matching `login.tsx`'s existing pattern.
- On a 4xx/5xx, show `auth.signup.errorGeneric` in the existing `role="alert"` paragraph and leave the form
  filled in.
- Layout is otherwise unchanged: same Card shell, same `space-y-4` stack, same submit-then-footer order.

`login.tsx` — replace the blind first-pick at lines 55-58:

- after a successful sign-in, read `meTenantsQuery`'s list;
- 0 organizations -> navigate to `/onboarding` (defensive; signup guarantees at least one);
- exactly 1 -> bind it via `POST /api/auth/switch-organization`, then hard-navigate to
  `adminUrlForOrg(slug, '/dashboard')` — no picker (D-17);
- 2 or more -> navigate to `/pick-organization` (D-17);
- then, unchanged, the existing staff pick-location branch at 60-71 for non-owners with no active location.
  The current code activates whichever organization sorts first, which is the exact bug UI-SPEC S2 names.
  Keep the `expired === '1'` banner and its `tAuth('sessionExpired')` copy — a stale second tab lands there
  after another tab switches organization, and no new UI is needed for that.
  </action>
  <verify>
  <automated>pnpm --filter admin typecheck && pnpm --filter admin build</automated>
  </verify>
  <acceptance_criteria> - `rg -n "authClient.signUp.email" apps/admin/src` returns no matches. - `rg -n "/v1/signup" apps/admin/src/routes/\(auth\)/signup.tsx` matches. - `rg -in "currency" apps/admin/src/routes/\(auth\)/signup.tsx` matches only inside country-option label keys, never as a form field name. - `rg -n "<select" apps/admin/src/routes/\(auth\)/signup.tsx` returns no matches (raw select replaced by the shadcn `Select`). - `rg -n "useTranslation" apps/admin/src/routes/\(auth\)/signup.tsx` matches and `rg -n "Restaurant name" apps/admin/src/routes/\(auth\)/signup.tsx` returns no matches. - `rg -n "defaultLocaleForCountry" apps/admin/src/routes/\(auth\)/signup.tsx` matches. - `rg -n "orgs.data\?\.\[0\]|orgs.data\[0\]" apps/admin/src/routes/\(auth\)/login.tsx` returns no matches (blind first-pick removed). - `rg -n "pick-location" apps/admin/src/routes/\(auth\)/login.tsx` still matches (staff branch preserved). - `pnpm --filter admin build` exits 0.
  </acceptance_criteria>
  <done>Signup asks four honest questions and creates a real account; sign-in routes an owner to their restaurant, the picker, or onboarding — never to whichever organization sorted first.</done>
  </task>

<task type="auto">
  <name>Task 2: Build the organization picker, the switch entry point and the host module</name>
  <files>apps/admin/src/routes/(auth)/pick-organization.tsx, apps/admin/src/lib/admin-host.ts, apps/admin/src/components/nav-user.tsx, apps/admin/src/main.tsx, apps/admin/src/env.ts, apps/admin/vite.config.ts, .env.example</files>
  <read_first>
    - apps/admin/src/routes/(auth)/pick-location.tsx (84 lines, read in full — the picker is a structural port of this: Card shell, `grid gap-2` outline-button list, `pendingId` state disabling the rest, the three loading/empty/populated states, the `role="alert"` error paragraph)
    - apps/admin/src/routes/(auth)/_layout.tsx (18 lines — purely presentational, NO `beforeLoad` guard today, so an authenticated-but-unbound session already passes through; confirm this by reading rather than assuming, and record it in the SUMMARY)
    - apps/admin/src/components/nav-user.tsx (post-plan-15; the dropdown where the "Switch organization" item goes, above the Account item)
    - apps/admin/src/lib/queries/identity.ts (plan 14: `meTenantsQuery`)
    - apps/admin/src/lib/i18n/messages/en.json (plan 02: the `pickOrganization.*` keys including `enteringState` with `{{name}}`, and `nav.user.switchOrganizationItem`)
    - apps/admin/src/env.ts (the `getEnv` helper with its PROD localhost rejection — the new host-suffix var goes through it)
    - apps/admin/vite.config.ts (currently has NO `server.allowedHosts` key; `server.port` is 4000 and there is no `host: true`)
    - apps/admin/src/main.tsx (post-plan-15 route-tree assembly and app bootstrap — the single reconciliation check goes here)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S2, §S3, §S5
    - .planning/phases/10.2-brand-pinned-sessions/10.2-12-SUMMARY.md (the switch endpoint's response shape)
  </read_first>
  <action>
Create `apps/admin/src/lib/admin-host.ts` FIRST — everything else in this task imports it, and it is the
security-sensitive part.

`parseOrgSlugFromHost(hostname)` splits the hostname on `.` and compares the trailing labels to
`ADMIN_HOST_SUFFIX`'s labels one by one, returning the single leftmost remaining label or `null`. It must
NEVER use `endsWith()`: `evil-admin.resto.app.attacker.com` ends with nothing useful but a naive suffix test
on `.admin.resto.app` against a crafted host is the origin-spoofing hazard D-24 names, and the same class of
bug is being guarded against on the server in plan 12. Return `null` for the bare apex (`admin.resto.app`)
— that host serves sign-in and signup, where no organization is bound yet.
`adminUrlForOrg(slug, path)` builds `https://<slug>.<suffix><path>`, or the dev `http://` form on
`admin.localhost:4000`.
Add `VITE_ADMIN_HOST_SUFFIX` to `apps/admin/src/env.ts` through the existing `getEnv` helper so its PROD
localhost rejection applies, and document it in `.env.example`.

Create `apps/admin/src/routes/(auth)/pick-organization.tsx` as a structural port of `pick-location.tsx`:
same Card shell, same `grid gap-2` outline-button list at equal visual weight (no logo, no subtitle, no
"recommended" accent), same `pendingId` disabling behaviour, same three states, same `role="alert"` error
position. Data comes from `meTenantsQuery`. All copy goes through `useTranslation` against the
`pickOrganization.*` keys plan 02 authored — this screen is localized from birth even though
`pick-location.tsx` is not retroactively localized (UI-SPEC's explicit scope note).

Three genuine divergences from the mirror:

1. On selection, `POST /api/auth/switch-organization` with the chosen id, then on success
   `window.location.assign(adminUrlForOrg(slug, '/dashboard'))` — a HARD navigation. TanStack Router's
   `navigate()` cannot cross an origin, and D-21 puts the organization in the host.
2. The pending button label interpolates the organization's name (`pickOrganization.enteringState` =
   "Entering {{name}}…") because a host change takes a perceptibly longer beat than the location picker's
   same-origin redirect.
3. On failure, re-enable every button, revert the pending label to the plain name and show
   `pickOrganization.error` — a real retry, not a dead-end message.

`nav-user.tsx`: add one `DropdownMenuItem` labelled `nav.user.switchOrganizationItem`, in its own group
directly above the Account item, rendered ONLY when `operator.baseRole === 'owner'` AND the owner belongs to
2+ organizations — the same conditional-render shape `location-switcher.tsx` already uses. Staff and
single-organization owners must not see a disabled item; they must see no item. It navigates client-side to
`/pick-organization`; no host change happens until a selection is made. One picker screen, two entry paths.

`main.tsx`: add the single bootstrap reconciliation (D-21's "one check at bootstrap"). Read
`parseOrgSlugFromHost(window.location.hostname)` and the session's bound organization. If a host slug is
present and disagrees with the bound organization's slug, `window.location.replace(adminUrlForOrg(boundSlug, currentPathAndSearch))`.
If no host slug is present (apex), do nothing — the login/signup screens live there. The session decides;
the host is display and routing. Do not add this check to any route file — one check at bootstrap is the
entire reason D-21 chose a host over a path segment.

`vite.config.ts`: add `server.host: true` and `server.allowedHosts: ['.admin.localhost']` so
`<slug>.admin.localhost:4000` resolves in dev. The installed Vite is 5.4.21, well above the 5.4.12 floor
where `allowedHosts` was introduced. Modern Chrome/Edge/Firefox resolve any `*.localhost` to loopback per
RFC 6761 with no `/etc/hosts` editing; Safari has a documented history of gaps here, so if the founder uses
Safari, note the `/etc/hosts` fallback in the SUMMARY. Leave the existing `/api` and `/v1` proxy entries
untouched.
</action>
<verify>
<automated>pnpm --filter admin typecheck && pnpm --filter admin test -- --run src/lib/admin-host</automated>
</verify>
<acceptance_criteria> - `rg -n "endsWith\(" apps/admin/src/lib/admin-host.ts` returns no matches. - A unit spec for `parseOrgSlugFromHost` asserts: `acme.admin.resto.app` -> `'acme'`; `admin.resto.app` -> `null`; `evil-admin.resto.app.attacker.com` -> `null`; `admin.resto.app.evil.com` -> `null`; `a.b.admin.resto.app` -> `null`. `pnpm --filter admin test -- --run src/lib/admin-host` exits 0. - `rg -n "window.location.assign" apps/admin/src/routes/\(auth\)/pick-organization.tsx` matches and `rg -n "navigate\(\{ to:" apps/admin/src/routes/\(auth\)/pick-organization.tsx` shows no same-origin navigation on the success path. - `rg -n "switch-organization" apps/admin/src/routes/\(auth\)/pick-organization.tsx` matches. - `rg -n "useTranslation" apps/admin/src/routes/\(auth\)/pick-organization.tsx` matches and no user-visible English string literal remains in that file. - `rg -n "baseRole === 'owner'" apps/admin/src/components/nav-user.tsx` matches on the switch item's render condition. - `rg -n "allowedHosts" apps/admin/vite.config.ts` and `rg -n "host: true" apps/admin/vite.config.ts` both match; the `/api` and `/v1` proxy entries are still present. - `rg -c "parseOrgSlugFromHost" apps/admin/src` equals 2 (the module and exactly one bootstrap call site) plus its spec — state the exact count in the SUMMARY. - `rg -n "VITE_ADMIN_HOST_SUFFIX" apps/admin/src/env.ts .env.example` matches in both.
</acceptance_criteria>
<done>An owner of several restaurants picks one at sign-in and lands on its host with a fresh session, and opening the wrong host bounces them to the right one in a single check.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite onboarding down to one field</name>
  <files>apps/admin/src/routes/(protected)/onboarding/index.tsx, apps/admin/src/routes/(protected)/onboarding/brand.tsx</files>
  <read_first>
    - apps/admin/src/routes/(protected)/onboarding/brand.tsx (260 lines, read in full: the two-field form with an editable slug, the 300ms-debounced `SlugAvailabilityHint` with its five states, the "Take suggestion" collision action, and the post-create navigation)
    - apps/admin/src/lib/slugify-tenant.ts (plan 14 — the display-only slug derivation for the host preview)
    - apps/admin/src/lib/i18n/messages/en.json (plan 02: `onboarding.*` including `hostPreview` with `{{host}}` and `createdToast` with `{{name}}`)
    - apps/admin/src/lib/admin-host.ts (Task 2 — `adminUrlForOrg` for both the preview string and the post-submit navigation)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md (the `POST /v1/me/tenants/onboarding` contract and its 409 behaviour)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S4
  </read_first>
  <action>
Replace `onboarding/brand.tsx` with `onboarding/index.tsx` at route path `/onboarding`. The nested `/brand`
segment described a sub-entity that no longer exists.

This screen SHRINKS. One field: "Restaurant name". No slug input, no availability hint, no "Take suggestion"
action, no five-state indicator. D-30 is explicit that onboarding asks the name only, and the server resolves
slug collisions silently by suffix (plan 13) — nothing here needs to expose that.

Beneath the input, a read-only host preview updating on the same ~300ms debounce, but now driving a preview
string rather than a network check: `onboarding.hostPreview` -> "You'll be reachable at {{host}}", where
`{{host}}` is `adminUrlForOrg(slugifyTenant(name))`'s hostname. Render it visually distinct from body text
(`font-mono text-muted-foreground`) so it reads as a system-generated value rather than another editable
field. Add a WHY-comment that the preview is advisory: the server may append a suffix on collision and its
returned slug wins.

Submit calls `POST /v1/me/tenants/onboarding` with `{ displayName }`. On success:
`toast.success(t('onboarding.createdToast', { name }))`, then navigate to the dashboard on the organization's
own host using the slug the SERVER returned — `window.location.assign(adminUrlForOrg(res.data.slug, '/dashboard'))`
— not the locally-previewed slug, which may differ after collision resolution. On failure show
`onboarding.errorGeneric` in a `role="alert"` paragraph.

Button copy is `onboarding.submitIdle` ("Continue") / `onboarding.submitPending` ("Creating your
restaurant…") — "Continue", not "Create restaurant", because D-30 frames this as one step in an
account-creation journey rather than a repeatable creation action, and it avoids the word "brand" entirely.

D-31's resume is NOT a state on this screen. The routing that sends an owner here lives in the protected
layout (plan 15) and derives from `status === 'pending_setup'`. This screen has one field and one submit;
there is no multi-step progress to visually resume.

All copy goes through `useTranslation`.
</action>
<verify>
<automated>pnpm --filter admin typecheck && pnpm --filter admin build</automated>
</verify>
<acceptance_criteria> - `test ! -f "apps/admin/src/routes/(protected)/onboarding/brand.tsx"` succeeds and `test -f "apps/admin/src/routes/(protected)/onboarding/index.tsx"` succeeds. - `rg -c "SlugAvailabilityHint|slug-availability|Take suggestion" apps/admin/src` returns 0. - `rg -c "<Input" "apps/admin/src/routes/(protected)/onboarding/index.tsx"` equals 1 (one field only). - `rg -n "onboarding.hostPreview" "apps/admin/src/routes/(protected)/onboarding/index.tsx"` matches. - `rg -n "res.data.slug|result.data.slug" "apps/admin/src/routes/(protected)/onboarding/index.tsx"` matches on the navigation path (server slug wins, not the local preview). - `rg -in "brand" "apps/admin/src/routes/(protected)/onboarding/"` returns no matches. - `pnpm --filter admin build` exits 0.
</acceptance_criteria>
<done>Naming the restaurant is one field and one button, the owner sees the host they will get, and they land on that exact host afterwards.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Walk the whole journey in a browser</name>
  <files>apps/admin/src/routes/(auth)/signup.tsx, apps/admin/src/routes/(auth)/pick-organization.tsx, apps/admin/src/routes/(protected)/onboarding/index.tsx</files>
  <read_first>
    - apps/admin/src/lib/admin-host.ts (the dev host form the walkthrough uses)
    - apps/admin/vite.config.ts (post-Task-2 `allowedHosts` / `host: true`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S1-S6 (the visual contract being confirmed)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md (what the server does at each step)
  </read_first>
  <what-built>
The complete journey is wired: signup -> onboarding -> dashboard, and sign-in -> picker -> a different
organization's host with a fresh session. Every part is covered by typecheck, unit tests and API e2e, but
none of that proves the browser experience — project memory is explicit that `tsc` + `vitest` green has
previously coexisted with a broken live path, and this phase's riskiest behaviours (a cross-origin hard
navigation, a cookie scoped to `.admin.*`, wildcard dev hosts, `*.localhost` resolution) are only observable
in a real browser.

Claude runs the API and the admin dev server and drives the flow; the founder confirms what they see.
</what-built>
<action>
Start the API and `pnpm --filter admin dev`, then walk:

1. Open `http://admin.localhost:4000/signup`. Confirm four fields (Your name, Email, Password, Country) and
   NO currency field. Create an account with country ES.
2. Confirm the interface switched to Spanish (D-36 seeding) and that no raw i18n key is visible anywhere.
3. Confirm you land on `/onboarding` with ONE field and a host preview line.
4. Submit a restaurant name. Confirm the toast, and confirm the browser ends up on
   `http://<slug>.admin.localhost:4000/dashboard`.
5. Confirm the sidebar shows the restaurant name as a static, non-clickable block, with the location
   switcher below it and NO brand switcher and NO "add restaurant" entry.
6. Sign out. Create a second account, or add a second organization membership directly in the dev database,
   so one user owns two organizations. Sign in as that user and confirm the picker appears.
7. Pick the second organization. Confirm the browser hard-navigates to that organization's host and the
   dashboard shows its data.
8. With a second tab still open on the first organization's host, perform an action. Confirm it redirects to
   `/login?expired=1` with the session-expired banner (the old token is dead, D-15).
9. Open `http://<otherOrgSlug>.admin.localhost:4000/orders` while bound to the first organization. Confirm
   the bootstrap check redirects to the bound organization's host.
10. Confirm the language switcher offers Русский, English and Español, and that selecting Español renders
    Spanish across the dashboard chrome.
    </action>
    <how-to-verify>
    Walk steps 1-10 and confirm each. The two that matter most: step 8 (the pre-switch token really is dead, not
    merely pointed elsewhere) and step 9 (a mismatched host bounces rather than serving the wrong organization).
    If your browser is Safari and `*.admin.localhost` does not resolve, use Chrome or Firefox for this walk and
    say so — Safari's `*.localhost` handling is unverified.
    </how-to-verify>
    <resume-signal>Type "approved" if all ten steps behave as described, or list the step numbers that did not.</resume-signal>
    <verify>
    <automated>pnpm --filter admin build</automated>
    </verify>
    <acceptance_criteria> - Step 1: the signup form shows exactly four fields and no currency control. - Step 2: the UI renders in Spanish with no raw i18n keys and no Russian strings mixed in. - Step 4: the final URL is `http://<slug>.admin.localhost:4000/dashboard` with the server-derived slug. - Step 5: the sidebar organization block is not clickable and there is no add-restaurant affordance. - Step 7: the picker's selection produces a cross-origin navigation, not a same-origin route change. - Step 8: the stale tab reaches `/login?expired=1`. - Step 9: the mismatched host redirects to the bound organization's host. - All ten observations are recorded in the SUMMARY, with the browser used.
    </acceptance_criteria>
    <done>The founder has seen a new owner go from nothing to a working restaurant, and has seen an old session die on switch.</done>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                                 |
| ---------------------------------------------------- | --------------------------------------------------------------------------- |
| `window.location.hostname` -> organization inference | Attacker-controllable if the parser is naive                                |
| Picker selection -> switch endpoint                  | Client asserts which organization it wants; the server validates membership |
| Cross-origin navigation target                       | A slug flowing into a URL the browser will follow                           |
| Anonymous internet -> signup form                    | The only account-creation surface                                           |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| ------------ | ---------------------- | --------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-17-01 | Spoofing               | Host parsed with a suffix check, letting `evil-admin.resto.app.attacker.com` masquerade as an organization host | mitigate    | `parseOrgSlugFromHost` matches labels, never `endsWith()`; a unit spec asserts four hostile hostnames return `null`; grep asserts no `endsWith(` in the module                                                        |
| T-10.2-17-02 | Elevation of Privilege | Client choosing an organization it is not a member of                                                           | mitigate    | The host and the picker are display and intent only; plan 12's endpoint verifies membership server-side before issuing a session, and the API's `auth.tenant_mismatch` cross-check is the standing fence              |
| T-10.2-17-03 | Tampering              | Open redirect via a server- or user-supplied slug flowing into `window.location.assign`                         | mitigate    | `adminUrlForOrg` composes a URL from a fixed scheme and a fixed suffix with the slug as the leftmost label only; the slug itself is constrained by `TenantSlugValue`'s regex, which admits no dots, slashes or colons |
| T-10.2-17-04 | Information Disclosure | Operator session cookie reaching a guest or public-site host during the cross-origin navigation                 | mitigate    | Cookie domain is `.admin.<domain>` (plan 12); step 8 of the walkthrough exercises the cross-subdomain path and step 9 exercises the mismatch path                                                                     |
| T-10.2-17-05 | Spoofing               | Stale pre-switch session still usable in another tab                                                            | mitigate    | Proven live in walkthrough step 8 and by plan 12's e2e; the old row is hard-deleted                                                                                                                                   |
| T-10.2-17-06 | Denial of Service      | Dev `allowedHosts` wildcard shipped into a production build                                                     | mitigate    | `server.*` config applies to the Vite dev server only and does not affect the static build; the SUMMARY must confirm `pnpm --filter admin build` output contains no dev-host references                               |
| T-10.2-17-SC | Tampering              | npm/pip/cargo installs                                                                                          | accept      | No packages installed; UI-SPEC confirms zero new shadcn primitives — the `Select` is already present                                                                                                                  |

</threat_model>

<verification>
- `pnpm --filter admin typecheck` and `pnpm --filter admin build` both exit 0.
- `pnpm --filter admin test -- --run src/lib/admin-host` passes, including the four hostile-hostname cases.
- `rg -c "endsWith\(" apps/admin/src/lib/admin-host.ts` returns 0.
- `rg -c "brandSlug|brand-switcher|meBrandsQuery|authClient.signUp.email" apps/admin/src` returns 0.
- The ten-step browser walkthrough is recorded in the SUMMARY with the browser used.
</verification>

<success_criteria>

- A new owner can create an account and a named restaurant and reach its dashboard on its own host.
- An owner of several restaurants picks one at every sign-in and lands on the right host with a new session.
- A mismatched host redirects once, at bootstrap.
- No hostile hostname parses as an organization.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-17-SUMMARY.md` when done.
</output>
