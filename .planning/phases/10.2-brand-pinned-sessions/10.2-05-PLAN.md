---
phase: 10.2-brand-pinned-sessions
plan: 05
type: execute
wave: 3
depends_on: ['10.2-03', '10.2-04']
files_modified:
  - packages/db/migrations/0079_organization_merge.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/sql/roles.sql
  - apps/api/src/shared/preflight/assert-brand-rls.ts
  - apps/api/src/main.ts
  - packages/db/src/cli/reset.ts
autonomous: false
requirements: []
decisions: [D-07, D-08, D-09, D-12, D-23, D-31]

must_haves:
  truths:
    - 'D-12: the live dev database matches the merged schema after a reset + migrate, with no data-preserving migration written'
    - 'D-09: `app_bind_brand(text)` and `current_brand_id()` are not callable in the database, and no `*_brand_iso` policy exists'
    - 'D-09: tenant-level RLS and location-level RLS are untouched and still enforce isolation'
    - 'D-07: `brands`, `brand_domains` and `member_brand_scope` do not exist as tables'
    - 'The API boots against the merged database — the brand-RLS boot preflight is gone, not merely satisfied'
  artifacts:
    - path: 'packages/db/migrations/0079_organization_merge.sql'
      provides: 'hand-written merge DDL + RLS teardown'
      contains: 'DROP FUNCTION IF EXISTS app_bind_brand'
    - path: 'packages/db/migrations/meta/_journal.json'
      provides: 'journal entry idx 79 for the hand-written migration'
      contains: '0079_organization_merge'
  key_links:
    - from: 'apps/api/src/main.ts'
      to: 'apps/api/src/shared/preflight/assert-brand-rls.ts'
      via: 'boot preflight import + call — BOTH removed'
      pattern: 'assertBrandRlsInstalled'
    - from: 'packages/db/migrations/0079_organization_merge.sql'
      to: 'packages/db/src/schema/tenants.ts'
      via: 'DDL matching the Drizzle schema defined in plan 03'
      pattern: 'ALTER TABLE tenants'
---

<objective>
Write the hand-written merge migration, tear down brand RLS, delete the boot preflight that would otherwise
refuse to start the API, and apply everything to the live dev database.

Purpose: plans 03 and 04 changed TypeScript files only. `pnpm typecheck` and `pnpm build` would both pass
without this plan while the live database still had `brands`, `brand_id` columns and brand RLS policies —
a false-positive verification state. This plan makes the database match the schema.
Output: a fresh dev database on the merged shape, `db:audit-fks` clean, API booting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@packages/db/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-03-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write migration 0079 — merge DDL and brand RLS teardown</name>
  <files>packages/db/migrations/0079_organization_merge.sql, packages/db/migrations/meta/_journal.json, packages/db/sql/roles.sql</files>
  <read_first>
    - packages/db/migrations/0058_brand_rls.sql (the original brand policy + function definitions being undone; also the `DO $$ ... IF EXISTS ... END $$` migration style used throughout this repo)
    - packages/db/migrations/0060_brand_rls_restrictive.sql (recreated the 7 surviving policies as RESTRICTIVE — this is the live shape being dropped, not 0058's PERMISSIVE shape)
    - packages/db/migrations/0068_stop_list_location_regrain.sql (lines 16 and 18 ALREADY dropped `menu_stop_list_brand_iso` and `catalog_brand_stop_version_brand_iso` — do NOT try to drop them again)
    - packages/db/migrations/0071_orders_location_rls.sql (adds `orders_location_iso` as a SIBLING of `orders_brand_iso` — `orders_location_iso` must survive)
    - packages/db/migrations/0061_drop_app_bind_brand_is_system_param.sql (the current definition of `app_bind_brand(text)`)
    - packages/db/migrations/0065_location_guc.sql (the location GUC lineage that must NOT be touched — use it as the template for what "leave alone" looks like)
    - packages/db/migrations/meta/_journal.json (entry shape: `idx`, `version: "7"`, `when`, `tag`, `breakpoints: true`; latest on disk is idx 78 / `0078_invitation_created_at`)
    - packages/db/src/schema/tenants.ts and packages/db/src/schema/locations.ts and packages/db/src/schema/menu.ts (post plan 03/04 — the DDL must reproduce exactly these shapes)
    - packages/db/sql/roles.sql (the `resto_app` grant surface — dropped tables must not leave dangling grants, and no DELETE privilege may be introduced)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md Pitfalls 1 and 3
  </read_first>
  <action>
Write `packages/db/migrations/0079_organization_merge.sql` BY HAND. Do NOT run `pnpm db:generate` —
`drizzle-kit generate` diffs against the last committed snapshot, and `packages/db/migrations/meta/` only
contains snapshots for `0000`–`0018` while 60 later migrations are hand-written. It will emit a plausible
but wrong file (RESEARCH Pitfall 3).

Statement order (separate each with `--> statement-breakpoint`, matching every existing migration):

1. RLS teardown — drop exactly these 7 policies, no more and no fewer:
   `menu_categories_brand_iso`, `menu_items_brand_iso`, `menu_item_sizes_brand_iso`,
   `menu_modifier_groups_brand_iso`, `menu_modifier_options_brand_iso`,
   `menu_item_modifier_groups_brand_iso`, `orders_brand_iso`.
   Use the `DROP POLICY IF EXISTS <name> ON <table>;` form (the precedent set by `0068`).
   Do NOT include `menu_stop_list_brand_iso` or `catalog_brand_stop_version_brand_iso` — `0068` already
   dropped them in Phase 08.4 and a re-drop is a live signal that the CONTEXT.md citation was trusted
   uncritically. Do NOT touch `orders_location_iso`, any `*_tenant_iso` policy, or any `*_location_iso` policy.
   CONTEXT.md D-09 says nine policies; the verified live count is seven plus two functions — say so in the
   migration's header comment and in the SUMMARY.
2. `DROP FUNCTION IF EXISTS current_brand_id();` and `DROP FUNCTION IF EXISTS app_bind_brand(text);`
3. Column drops: `ALTER TABLE ... DROP COLUMN IF EXISTS brand_id` on `menu_categories`, `menu_items`,
   `menu_item_sizes`, `menu_modifier_groups`, `menu_modifier_options`, `menu_item_modifier_groups`,
   `orders`, `customer_profiles`, `outbox`, and `locations` (the tenth column CONTEXT.md's D-08 omits —
   plan 03 removed it from the Drizzle schema). Their composite FKs and brand-keyed unique indexes go with
   them; drop the indexes and constraints explicitly by name before the column where Postgres would not
   cascade cleanly.
4. Recreate the four collapsed unique indexes named in plan 04's SUMMARY on their `(tenant_id, ...)` keys,
   preserving the `WHERE code IS NOT NULL` predicates.
5. Merge the domain rows before dropping the table (D-23): `INSERT INTO tenant_domains (...) SELECT ...
FROM brand_domains ON CONFLICT (domain) DO NOTHING`, then drop `brand_domains`. Even under D-12's reset
   this keeps the migration honest if it is ever replayed against a populated database.
6. `ALTER TABLE tenants ADD COLUMN` for every column plan 03 moved over, plus `country`. Add `country` as
   nullable first, backfill any existing rows to a chosen supported value, then `SET NOT NULL` — a single
   `ADD COLUMN ... NOT NULL` without a default fails on a non-empty table. Add the five `tenants_*_chk`
   constraints (`legal_form`, `payment_provider`, `account_type`, `stripe_onboarding_status`, `country`),
   and REPLACE `tenants_status_chk` so it also admits `'pending_setup'` — plan 03's reconciliation note
   explains why this one enum value is what makes D-31's flag-free onboarding resume derivable. Drop the
   old constraint by name before adding the new one; Postgres will not widen a CHECK in place.
7. `DROP TABLE IF EXISTS member_brand_scope;` then `DROP TABLE IF EXISTS brands;` (children first).
8. Session: `ALTER TABLE session DROP COLUMN IF EXISTS active_brand_id;`
9. Check `packages/db/sql/roles.sql` for grants naming `brands`, `brand_domains` or `member_brand_scope`
   and remove them. Do not add a DELETE grant to `resto_app` under any circumstance — hard deletes are
   forbidden project-wide (CLAUDE.md), and the `DROP TABLE` statements above run as the migration role,
   not as `resto_app`.

Append the journal entry to `packages/db/migrations/meta/_journal.json` by hand:
`{ "idx": 79, "version": "7", "when": <epoch ms>, "tag": "0079_organization_merge", "breakpoints": true }`.
A migration file without a journal entry is silently never applied — this exact omission was a blocker
recorded in `07.6-REVIEWS.md`.
</action>
<verify>
<automated>node -e "const j=require('./packages/db/migrations/meta/\_journal.json');const e=j.entries[j.entries.length-1];if(e.idx!==79||e.tag!=='0079_organization_merge'||e.version!=='7'||e.breakpoints!==true){console.error('journal entry wrong:',e);process.exit(1)}console.log('journal ok',e)" && rg -q "DROP FUNCTION IF EXISTS app_bind_brand" packages/db/migrations/0079_organization_merge.sql</automated>
</verify>
<acceptance_criteria> - `rg -c "statement-breakpoint" packages/db/migrations/0079_organization_merge.sql` is greater than 10. - `rg -c "_brand_iso" packages/db/migrations/0079_organization_merge.sql` equals 7. - `rg -n "menu_stop_list_brand_iso|catalog_brand_stop_version_brand_iso|orders_location_iso" packages/db/migrations/0079_organization_merge.sql` returns no matches. - `rg -n "DROP FUNCTION IF EXISTS current_brand_id" packages/db/migrations/0079_organization_merge.sql` and `rg -n "DROP FUNCTION IF EXISTS app_bind_brand" packages/db/migrations/0079_organization_merge.sql` both match. - `rg -c "DROP COLUMN IF EXISTS brand_id" packages/db/migrations/0079_organization_merge.sql` equals 10. - `rg -n "DROP TABLE IF EXISTS brands" packages/db/migrations/0079_organization_merge.sql` and `rg -n "DROP TABLE IF EXISTS member_brand_scope" packages/db/migrations/0079_organization_merge.sql` and `rg -n "DROP TABLE IF EXISTS brand_domains" packages/db/migrations/0079_organization_merge.sql` all match. - `rg -n "tenants_country_chk" packages/db/migrations/0079_organization_merge.sql` matches. - `rg -n "pending_setup" packages/db/migrations/0079_organization_merge.sql` matches inside the recreated `tenants_status_chk`. - `rg -in "grant delete" packages/db/sql/roles.sql` returns no matches. - The journal check in `<automated>` exits 0.
</acceptance_criteria>
<done>A hand-written, journal-registered migration expresses the whole merge, dropping exactly the verified 7 policies and 2 functions and nothing that belongs to tenant or location isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the brand-RLS boot preflight</name>
  <files>apps/api/src/shared/preflight/assert-brand-rls.ts, apps/api/src/main.ts</files>
  <read_first>
    - apps/api/src/shared/preflight/assert-brand-rls.ts (39 lines: checks `to_regprocedure('public.app_bind_brand(text)')` and the `orders_brand_iso` policy, throws `BrandRlsNotInstalledError`)
    - apps/api/src/main.ts (the import at ~line 25 and the `await assertBrandRlsInstalled(env.DATABASE_URL)` call at ~line 64, sitting among assertNoRlsBypass / assertTenantLockInstalled / assertSetConfigRevoked / assertNoBaCredentialAccess / assertInboxProcessedDeletable)
    - apps/api/src/shared/preflight/ (the sibling preflight files and their barrel/index — check whether `assertBrandRlsInstalled` is re-exported anywhere)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md Pitfall 2
  </read_first>
  <action>
Delete `apps/api/src/shared/preflight/assert-brand-rls.ts` and both of its call sites in
`apps/api/src/main.ts` (the `import { assertBrandRlsInstalled } from './shared/preflight/assert-brand-rls'`
line and the `await assertBrandRlsInstalled(env.DATABASE_URL)` line). Also remove `BrandRlsNotInstalledError`
and any re-export of `assertBrandRlsInstalled` from a preflight barrel.

This is a hard boot blocker, not a cleanup: after migration 0079 runs, this preflight throws
`BrandRlsNotInstalledError` with a message telling the operator to "run migration 0058" — advice that is
actively wrong once 0058's objects are intentionally dropped. It is invisible to a `grep brand` sweep scoped
to `apps/api/src/contexts/`, which is why CONTEXT.md's D-10 deletion list misses it.

Leave every other preflight assertion in `main.ts` in place and in the same order — `assertNoRlsBypass`,
`assertTenantLockInstalled`, `assertSetConfigRevoked`, `assertNoBaCredentialAccess`,
`assertInboxProcessedDeletable`, `assertWithoutTenantCallsiteRegistered`, `assertSystemRolesPresent`.
They guard the tenant dimension, which this phase does not weaken.
</action>
<verify>
<automated>test ! -f apps/api/src/shared/preflight/assert-brand-rls.ts && test "$(rg -c 'assertBrandRlsInstalled|BrandRlsNotInstalledError' apps/api/src | wc -l)" -eq 0</automated>
</verify>
<acceptance_criteria> - `test ! -f apps/api/src/shared/preflight/assert-brand-rls.ts` succeeds. - `rg -c "assertBrandRlsInstalled|BrandRlsNotInstalledError" apps/api/src` returns 0 matches. - `rg -n "assertNoRlsBypass" apps/api/src/main.ts` and `rg -n "assertTenantLockInstalled" apps/api/src/main.ts` and `rg -n "assertSetConfigRevoked" apps/api/src/main.ts` all still match. - `rg -c "await assert" apps/api/src/main.ts` is one lower than before the edit (record both numbers in the SUMMARY).
</acceptance_criteria>
<done>Nothing at boot asserts the existence of brand RLS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply the merged schema to the live dev database</name>
  <files>packages/db/migrations/0079_organization_merge.sql</files>
  <read_first>
    - packages/db/src/cli/reset.ts (what `db:reset` actually does — confirm it drops and recreates rather than truncating, and whether it re-runs `sql/roles.sql`)
    - packages/db/src/cli/migrate.ts (how the journal drives application order)
    - packages/db/src/cli/audit-fks.ts (what `db:audit-fks` asserts — this is the composite-FK invariant check)
    - packages/db/migrations/0079_organization_merge.sql (the file being applied)
  </read_first>
  <what-built>
Migration 0079 exists and the brand-RLS boot preflight is deleted, but the running dev database still has
`brands`, ten `brand_id` columns, seven `*_brand_iso` policies and the `app_bind_brand` function. Until this
is applied, `pnpm typecheck` and `pnpm build` pass against a database that does not match the schema — the
exact false-positive state this task exists to prevent.

Claude runs the commands; the human confirms the dev stack is up and that resetting the dev database is
acceptable right now (D-12 explicitly ships this as a schema rewrite WITH a database reset — the assumption
recorded there is that no real tenant data exists. If that has changed since 2026-08-19, stop and revisit
D-12 before running anything).
</what-built>
<action>

1. Confirm the dev stack is running: `pnpm dev:up` (idempotent; Postgres 16 on the compose stack).
2. Reset the dev database: `pnpm --filter @resto/db db:reset`. D-12 ships this phase as a schema rewrite
   with a dev reset, not a data-preserving migration.
3. Apply all migrations including 0079: `pnpm --filter @resto/db db:migrate`.
4. Prove the teardown landed with direct SQL against the dev database:
   - `SELECT to_regprocedure('public.app_bind_brand(text)') IS NULL` returns true
   - `SELECT to_regprocedure('public.current_brand_id()') IS NULL` returns true
   - `SELECT count(*) FROM pg_policies WHERE policyname LIKE '%_brand_iso'` returns 0
   - `SELECT count(*) FROM pg_policies WHERE policyname = 'orders_location_iso'` returns 1 (location RLS survived)
   - `SELECT count(*) FROM information_schema.columns WHERE column_name = 'brand_id'` returns 0
   - `SELECT to_regclass('public.brands') IS NULL AND to_regclass('public.brand_domains') IS NULL AND to_regclass('public.member_brand_scope') IS NULL` returns true
   - `SELECT count(*) FROM information_schema.columns WHERE table_name='tenants' AND column_name='country'` returns 1
   - `INSERT INTO tenants (... status ...) VALUES (..., 'pending_setup', ...)` succeeds and the same insert with `'nonsense'` is rejected by `tenants_status_chk`
5. Run the composite-FK invariant audit: `pnpm --filter @resto/db db:audit-fks` — must exit 0.
   </action>
   <how-to-verify>
6. Confirm Docker/colima is running and `pnpm dev:up` reports the Postgres container healthy.
7. Confirm you are willing to lose the current dev database contents (D-12).
8. After Claude runs the steps, confirm `pnpm --filter @resto/db db:migrate` printed `0079_organization_merge`
   as applied and `pnpm --filter @resto/db db:audit-fks` exited 0.
   </how-to-verify>
   <resume-signal>Type "approved" once the dev stack is up and a database reset is acceptable, or describe what should happen instead.</resume-signal>
   <verify>
   <automated>pnpm --filter @resto/db db:migrate && pnpm --filter @resto/db db:audit-fks</automated>
   </verify>
   <acceptance_criteria> - `pnpm --filter @resto/db db:migrate` exits 0 and its output names `0079_organization_merge`. - `pnpm --filter @resto/db db:audit-fks` exits 0. - Every SQL probe in the action list returns the stated value; paste the actual outputs into the SUMMARY. - `SELECT count(*) FROM pg_policies WHERE policyname LIKE '%_tenant_iso'` is unchanged from before the migration (record both numbers in the SUMMARY) — tenant RLS was not collaterally dropped.
   </acceptance_criteria>
   <done>The live dev database has the merged shape: no brands table, no brand_id column anywhere, no brand policy or function, tenant and location RLS intact, and the FK audit clean.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                      |
| ----------------------------------- | -------------------------------------------------------------------------------- |
| Migration role -> production schema | DDL runs with elevated privilege; a mis-scoped DROP removes an isolation control |
| `resto_app` role -> table grants    | The application role's privilege surface changes when tables are dropped         |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                      |
| ------------ | ---------------------- | --------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-05-01 | Information Disclosure | Over-broad RLS teardown dropping a tenant or location policy                | mitigate    | The migration drops exactly seven named `*_brand_iso` policies; acceptance criteria assert `rg -c "_brand_iso"` equals 7, that no `*_tenant_iso`/`*_location_iso` name appears in the file, and that `orders_location_iso` still exists in `pg_policies` after apply |
| T-10.2-05-02 | Elevation of Privilege | A DELETE grant slipping into `roles.sql` while editing grants               | mitigate    | Acceptance criterion `rg -in "grant delete" packages/db/sql/roles.sql` returns no matches; hard deletes are forbidden project-wide (CLAUDE.md)                                                                                                                       |
| T-10.2-05-03 | Tampering              | Composite-FK invariant (ADR-0020 I-2) silently weakened by the column drops | mitigate    | `pnpm --filter @resto/db db:audit-fks` exits 0 as a blocking acceptance criterion                                                                                                                                                                                    |
| T-10.2-05-04 | Denial of Service      | API refusing to boot after teardown via `assertBrandRlsInstalled`           | mitigate    | Preflight deleted in Task 2 before the migration is applied in Task 3; grep-zero criterion                                                                                                                                                                           |
| T-10.2-05-05 | Tampering              | Migration file present but never applied (missing journal entry)            | mitigate    | Journal shape asserted programmatically in Task 1's `<automated>`; `db:migrate` output naming `0079` asserted in Task 3                                                                                                                                              |
| T-10.2-05-SC | Tampering              | npm/pip/cargo installs                                                      | accept      | No packages installed                                                                                                                                                                                                                                                |

</threat_model>

<verification>
- `pnpm --filter @resto/db db:migrate` exits 0 with `0079_organization_merge` applied.
- `pnpm --filter @resto/db db:audit-fks` exits 0.
- The seven SQL probes in Task 3 return their stated values, pasted verbatim into the SUMMARY.
- `rg -c "assertBrandRlsInstalled" apps/api/src` returns 0.
- `packages/db` integration specs still reference dropped objects and are EXPECTED-RED until plan 18
  rewrites them; do not gate on `cd packages/db && npx vitest run` here.
</verification>

<success_criteria>

- The live dev database matches the merged Drizzle schema.
- Brand RLS is provably gone; tenant and location RLS are provably present.
- The FK audit passes, so the composite-FK invariant survived the merge.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-05-SUMMARY.md` when done.
</output>
