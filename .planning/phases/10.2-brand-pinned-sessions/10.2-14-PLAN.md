---
phase: 10.2-brand-pinned-sessions
plan: 14
type: execute
wave: 9
depends_on: ["10.2-08", "10.2-13"]
files_modified:
  - apps/admin/src/lib/api-client.ts
  - apps/admin/src/lib/queries/identity.ts
  - apps/admin/src/lib/queries/tenancy.ts
  - apps/admin/src/lib/queries/catalog.ts
  - apps/admin/src/lib/queries/locations.ts
  - apps/admin/src/lib/queries/orders.ts
  - apps/admin/src/lib/queries/roles.ts
  - apps/admin/src/lib/queries/team.ts
  - apps/admin/src/lib/hooks/use-effective-location.ts
  - apps/admin/src/lib/brand-payments-api.ts
  - apps/admin/src/lib/tenant-payments-api.ts
  - apps/admin/src/lib/slugify-brand.ts
  - apps/admin/src/lib/slugify-tenant.ts
  - apps/admin/src/lib/menu/catalog-errors.ts
autonomous: true
requirements: []
decisions: [D-10, D-11, D-19, D-39, D-40]

must_haves:
  truths:
    - "D-40: no admin data-access module sends, receives or names a brand"
    - "The admin client stops sending the `x-brand-slug` header, which the API no longer reads"
    - "Every query key and endpoint path matches the renamed API routes, so no admin screen 404s"
    - "D-19: the effective-location hook and the location query surface are unchanged in behaviour"
  artifacts:
    - path: "apps/admin/src/lib/api-client.ts"
      provides: "apiFetch with x-tenant-id and x-location-id only"
    - path: "apps/admin/src/lib/queries/identity.ts"
      provides: "meTenantsQuery replacing meBrandsQuery"
  key_links:
    - from: "apps/admin/src/lib/api-client.ts"
      to: "apps/api/src/shared/tenant-context.middleware.ts"
      via: "x-tenant-id header derived from session.activeOrganizationId"
      pattern: "x-tenant-id"
    - from: "apps/admin/src/lib/queries/identity.ts"
      to: "apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts"
      via: "GET /v1/me/tenants"
      pattern: "/v1/me/tenants"
---

<objective>
Rewrite the admin's data-access layer — the fetch wrapper, the seven query modules, the payments and slug
helpers — onto the merged API contract, before the routes and components that consume it are touched.

Purpose: interface-first ordering. Plans 15, 16 and 17 rewrite ~50 route and component files that all import
from here; giving them a settled contract avoids each of them rediscovering it.
Output: `apps/admin/src/lib/` contains no brand vocabulary and every endpoint path matches the API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@apps/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-08-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md

<interfaces>
Contracts this plan LOCKS for plans 15/16/17. Import these; do not re-derive them.

apps/admin/src/lib/api-client.ts:
  apiFetch<T>(path, opts?: {
    method?: 'GET'|'POST'|'PUT'|'PATCH'|'DELETE';
    body?: unknown;
    locationId?: string;      // 'all' or a uuid; 'all'/absent -> header omitted
    signal?: AbortSignal;
  }): Promise<{ status: number; ok: boolean; data: T | null }>
  // `brandSlug` option REMOVED — the API no longer reads x-brand-slug

apps/admin/src/lib/queries/identity.ts:
  meQuery()          -> MeResponse
  meTenantsQuery()   -> { tenants: Array<{ id; slug; displayName; status }> }
  // meBrandsQuery REMOVED; `status` is what plan 17 reads to decide onboarding vs dashboard (D-31)

Renamed modules: brand-payments-api.ts -> tenant-payments-api.ts; slugify-brand.ts -> slugify-tenant.ts
Deleted: friendlyCatalogError in apps/admin/src/lib/menu/catalog-errors.ts (D-39)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip brandSlug from apiFetch and rewrite the identity/tenancy queries</name>
  <files>apps/admin/src/lib/api-client.ts, apps/admin/src/lib/queries/identity.ts, apps/admin/src/lib/queries/tenancy.ts</files>
  <read_first>
    - apps/admin/src/lib/api-client.ts (full file: the `SessionLookupResult` reading `session.activeOrganizationId`, the 2s session cache, the `brandSlug?: string` option and its `'x-brand-slug'` header, the `locationId` passthrough with its D-12 comment, the 401 -> `window.location.href = '/login?expired=1'` branch, and the GET retry/backoff)
    - apps/admin/src/lib/queries/identity.ts (`meQuery`, `meBrandsQuery` and the `MeResponse` type)
    - apps/admin/src/lib/queries/tenancy.ts (tenant-level queries; check for brand-shaped endpoints)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-08-SUMMARY.md (the recorded old->new route strings)
    - apps/CLAUDE.md §"Auth + tenancy at the web layer" (no context cookie may be reintroduced; no static identity placeholders)
  </read_first>
  <action>
`api-client.ts`: delete the `brandSlug?: string` option and the `'x-brand-slug'` header it produced. The API
stopped reading that header in plan 07, so every remaining sender is dead weight that misleads the next
reader into thinking brand context still flows.

Everything else in this file stays: `x-tenant-id` continues to come from `session.activeOrganizationId` (the
session IS the context, D-14 — do not add a cookie or a module-level variable to carry it, per
`apps/CLAUDE.md`); the `locationId` passthrough keeps its dumb-passthrough contract and its D-12 comment;
the 401 -> `/login?expired=1` redirect stays, and it is what a stale second tab hits after another tab
switches organization (UI-SPEC S3 relies on this existing behaviour rather than new UI).

`queries/identity.ts`: replace `meBrandsQuery` with `meTenantsQuery` calling `GET /v1/me/tenants`, typed to
`{ tenants: Array<{ id: string; slug: string; displayName: string; status: string }> }`. `slug` and `status`
are both load-bearing: plan 17's picker builds a cross-origin URL from `slug`, and its routing decision
reads `status === 'pending_setup'` (D-31). Update the TanStack Query key from any brand-flavoured key to a
tenant one and record the old/new keys in the SUMMARY — a stale key in an unmigrated consumer silently
serves a cached brand-shaped payload.

`queries/tenancy.ts`: repoint every endpoint whose path changed in plans 07/08, using the recorded old->new
route table rather than guessing. Extend the tenant response type with the merged columns the admin renders
(`country`, `theme`, the `stripe*` fields, `legalName` etc.) so the payouts, theme and domains screens keep
working without a second entity fetch.
  </action>
  <verify>
    <automated>pnpm --filter admin typecheck 2>&1 | rg -c "src/lib/(api-client|queries)" || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "brandSlug|x-brand-slug" apps/admin/src/lib/api-client.ts` returns 0.
    - `rg -n "x-tenant-id" apps/admin/src/lib/api-client.ts` and `rg -n "activeOrganizationId" apps/admin/src/lib/api-client.ts` and `rg -n "/login\?expired=1" apps/admin/src/lib/api-client.ts` all still match.
    - `rg -n "meTenantsQuery" apps/admin/src/lib/queries/identity.ts` matches; `rg -c "meBrandsQuery" apps/admin/src` returns 0.
    - `rg -n "slug" apps/admin/src/lib/queries/identity.ts` and `rg -n "status" apps/admin/src/lib/queries/identity.ts` both match inside the tenants response type.
    - `rg -n "document.cookie|localStorage.setItem\('resto" apps/admin/src/lib/api-client.ts` returns no matches (no context cookie reintroduced).
    - The SUMMARY records old->new TanStack Query keys.
  </acceptance_criteria>
  <done>`apiFetch` carries tenant and location context only, and the admin can list the organizations a user belongs to with their slug and setup status.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining query modules and the effective-location hook</name>
  <files>apps/admin/src/lib/queries/catalog.ts, apps/admin/src/lib/queries/locations.ts, apps/admin/src/lib/queries/orders.ts, apps/admin/src/lib/queries/roles.ts, apps/admin/src/lib/queries/team.ts, apps/admin/src/lib/hooks/use-effective-location.ts</files>
  <read_first>
    - apps/admin/src/lib/queries/catalog.ts (every query passing `brandSlug` into `apiFetch` and every brand-flavoured query key)
    - apps/admin/src/lib/queries/locations.ts and orders.ts (the same, plus the location parameters that must survive)
    - apps/admin/src/lib/queries/roles.ts and team.ts (check for brand-scoped role/member endpoints)
    - apps/admin/src/lib/hooks/use-effective-location.ts (the D-12 hook that resolves the per-role location value; its LOCATION logic is Phase 08.5 work that survives)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-09-SUMMARY.md and 10.2-10-SUMMARY.md (the recorded catalog and ordering route changes)
  </read_first>
  <action>
Remove every `brandSlug` argument, every brand-flavoured query key segment and every brand field from the
response types across the five query modules. Repoint any endpoint whose path changed, using the route
tables recorded in the plan 07/08/09/10 summaries.

`use-effective-location.ts`: touch ONLY the brand dimension. Its location resolution — owner sees `'all'`,
staff are pinned to their scoped location — is Phase 08.5 behaviour that D-19 preserves. If the hook reads a
brand slug purely to build a query key, remove that segment; do not restructure the hook.

Query keys are the silent-failure surface here. A component still keying on `['brand', slug, 'items']` while
the query function fetches the tenant-scoped path will serve a stale cached payload with no error. After the
sweep, grep the whole `apps/admin/src` for any remaining query key literal containing `brand` and list them
in the SUMMARY — plans 15 and 16 must fix the consumers to match.

Do not add a compatibility alias export for a renamed query function. A stale import should be a compile
error, not a silently different query (D-39).
  </action>
  <verify>
    <automated>pnpm --filter admin typecheck 2>&1 | rg -c "src/lib/(queries|hooks)" || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg -in "brand" apps/admin/src/lib/queries/ apps/admin/src/lib/hooks/` returns no matches.
    - `rg -n "'all'" apps/admin/src/lib/hooks/use-effective-location.ts` still matches (owner aggregate behaviour preserved).
    - `rg -n "locationId" apps/admin/src/lib/queries/orders.ts` still matches.
    - The SUMMARY lists every remaining `brand`-containing query key literal found elsewhere in `apps/admin/src`, as the worklist for plans 15/16.
    - `pnpm --filter admin typecheck` reports no errors originating in `apps/admin/src/lib/queries/` or `apps/admin/src/lib/hooks/`.
  </acceptance_criteria>
  <done>Every admin query targets a real endpoint under a tenant-flavoured key, with location behaviour untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Rename the payments and slug helpers, delete the dead catalog error helper</name>
  <files>apps/admin/src/lib/brand-payments-api.ts, apps/admin/src/lib/tenant-payments-api.ts, apps/admin/src/lib/slugify-brand.ts, apps/admin/src/lib/slugify-tenant.ts, apps/admin/src/lib/menu/catalog-errors.ts</files>
  <read_first>
    - apps/admin/src/lib/brand-payments-api.ts (the Stripe onboarding/payouts calls; their endpoints changed in plan 07)
    - apps/admin/src/lib/payments-api.ts (the sibling that already exists — check whether the two should merge rather than both survive)
    - apps/admin/src/lib/slugify-brand.ts (client-side slug derivation used by the old onboarding screen's live preview)
    - apps/admin/src/lib/menu/catalog-errors.ts (`friendlyCatalogError` — D-39 names it as dead and English-only; confirm with `rg -n "friendlyCatalogError" apps/admin/src` before deleting)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md (the onboarding controller's old->new route strings)
    - packages/domain/src/tenant-slug.ts (plan 01 — the server-side slug rules the client preview should mirror)
  </read_first>
  <action>
Rename `brand-payments-api.ts` -> `tenant-payments-api.ts` and repoint its calls at the routes plan 07
recorded. Read `payments-api.ts` first: if the two files now cover the same surface, merge them into one and
say so in the SUMMARY — two payment client modules for one entity is exactly the duplication D-39 targets.

Rename `slugify-brand.ts` -> `slugify-tenant.ts`. Its remaining job is the read-only host preview on the
onboarding screen (UI-SPEC S4: "You'll be reachable at {{host}}"), NOT an availability check — collisions
are resolved silently on the server (plan 13). Keep the derivation aligned with `TenantSlugValue`'s rules
(lowercase, alphanumeric + hyphen, no edge hyphens) so the preview does not promise a host the server will
not grant; add a WHY-comment that this is display-only and the server is authoritative.

Delete `friendlyCatalogError` from `apps/admin/src/lib/menu/catalog-errors.ts` (D-39). Confirm zero callers
first with `rg -n "friendlyCatalogError" apps/admin/src`; if callers exist, this is not the dead code D-39
described — report that instead of deleting, and route the error through the same i18n path the rest of the
app uses.
  </action>
  <verify>
    <automated>pnpm --filter admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/admin/src/lib -iname "*brand*"` returns no results.
    - `rg -c "friendlyCatalogError" apps/admin/src` returns 0, OR the SUMMARY explains why it was kept and shows its callers.
    - `rg -n "slugify" apps/admin/src/lib/slugify-tenant.ts` matches and the file carries a WHY-comment marking it display-only.
    - The SUMMARY states whether `tenant-payments-api.ts` and `payments-api.ts` were merged, and why.
    - `pnpm --filter admin typecheck` reports no errors originating in `apps/admin/src/lib/`.
  </acceptance_criteria>
  <done>`apps/admin/src/lib` has no brand-named file, one payments client, and no dead error helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser SPA -> API | Every request's tenant context is derived from the session, not from client state |
| Session cookie -> `apiFetch` | The SPA reads `activeOrganizationId` from the session to label requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.2-14-01 | Spoofing | Client-supplied context header treated as authoritative | accept (unchanged posture) | `x-tenant-id` is a client-echoed hint; `AuthGuard`'s `auth.tenant_mismatch` cross-check against the session is the real fence and is untouched by this plan. Removing `x-brand-slug` strictly reduces the client-influenced surface |
| T-10.2-14-02 | Information Disclosure | Stale query cache serving a previous organization's data after a switch | mitigate | Query keys are renamed away from brand flavouring and the surviving `brand`-keyed literals are enumerated for plans 15/16. The organization switch is a hard navigation to a new origin (D-21), which discards the in-memory cache entirely |
| T-10.2-14-03 | Information Disclosure | Reintroducing a context cookie or module-level context variable | mitigate | Forbidden by `apps/CLAUDE.md`; asserted by a grep for `document.cookie` / `localStorage.setItem('resto` in `api-client.ts` |
| T-10.2-14-04 | Tampering | Client-side slug preview diverging from the server's rules and promising a host that is never granted | mitigate | `slugify-tenant.ts` mirrors `TenantSlugValue`'s rules and is marked display-only; the server resolves collisions silently and returns the authoritative slug |
| T-10.2-14-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `pnpm --filter admin typecheck` reports no errors from `apps/admin/src/lib/`.
- `rg -in "brand" apps/admin/src/lib/` returns 0.
- `find apps/admin/src/lib -iname "*brand*"` returns nothing.
- The rest of `apps/admin/src` still fails typecheck — EXPECTED; plans 15, 16 and 17 sweep the consumers.
</verification>

<success_criteria>
- `apiFetch` sends tenant and location context only.
- `meTenantsQuery` returns id, slug, displayName and status.
- No brand-named file, function or query key remains in `apps/admin/src/lib`.
</success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-14-SUMMARY.md` when done.
</output>
