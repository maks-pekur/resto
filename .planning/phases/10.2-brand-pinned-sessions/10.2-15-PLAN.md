---
phase: 10.2-brand-pinned-sessions
plan: 15
type: execute
wave: 10
depends_on: ["10.2-14"]
files_modified:
  - apps/admin/src/main.tsx
  - apps/admin/src/routes/(protected)/_layout.tsx
  - apps/admin/src/routes/(protected)/index.tsx
  - apps/admin/src/routes/(protected)/locations.tsx
  - apps/admin/src/routes/(protected)/orders.tsx
  - apps/admin/src/routes/(protected)/settings.tsx
  - apps/admin/src/routes/(protected)/team.tsx
  - apps/admin/src/routes/(protected)/roles.tsx
  - apps/admin/src/routes/(protected)/roles.$roleId.tsx
  - apps/admin/src/routes/(protected)/tenant.domains.tsx
  - apps/admin/src/routes/(protected)/tenant.payouts.tsx
  - apps/admin/src/routes/(protected)/tenant.theme.tsx
  - apps/admin/src/routes/(protected)/menu/_layout.tsx
  - apps/admin/src/routes/(protected)/menu/categories.tsx
  - apps/admin/src/routes/(protected)/menu/items.tsx
  - apps/admin/src/routes/(protected)/menu/items.$id.tsx
  - apps/admin/src/routes/(protected)/menu/modifier-groups.tsx
  - apps/admin/src/routes/(protected)/menu/modifier-groups.$id.tsx
  - apps/admin/src/routes/(protected)/menu/stop-list.tsx
  - apps/admin/src/routes/(protected)/dashboard-redirect.$.tsx
  - apps/admin/src/components/app-sidebar.tsx
  - apps/admin/src/components/nav-main.tsx
  - apps/admin/src/components/nav-user.tsx
  - apps/admin/src/components/site-header.tsx
  - apps/admin/src/components/brand-switcher.tsx
  - apps/admin/src/components/tenant-identity.tsx
  - apps/admin/src/components/location-switcher.tsx
autonomous: true
requirements: []
decisions: [D-10, D-19, D-20, D-31, D-39, D-40]

must_haves:
  truths:
    - "D-20: every admin path is `/orders`, `/menu/items`, `/settings` — no `/{brandSlug}` segment exists anywhere in the route tree"
    - "D-10: the brand switcher is gone; nothing in the sidebar offers to change or add a restaurant"
    - "D-19: the location switcher is the only interactive context control in the sidebar and behaves exactly as before"
    - "An operator can always see which restaurant they are in without looking at the address bar"
    - "D-31: a signed-in owner whose organization is still `pending_setup` is routed to onboarding by the protected layout"
  artifacts:
    - path: "apps/admin/src/routes/(protected)/_layout.tsx"
      provides: "the single protected layout holding sidebar chrome and the onboarding-resume redirect"
    - path: "apps/admin/src/components/tenant-identity.tsx"
      provides: "static, non-interactive organization label replacing the switcher"
  key_links:
    - from: "apps/admin/src/routes/(protected)/_layout.tsx"
      to: "apps/admin/src/lib/queries/identity.ts"
      via: "meTenantsQuery status read driving the onboarding redirect"
      pattern: "meTenantsQuery"
    - from: "apps/admin/src/components/app-sidebar.tsx"
      to: "apps/admin/src/components/tenant-identity.tsx"
      via: "SidebarHeader composition, replacing BrandSwitcher"
      pattern: "TenantIdentity"
---

<objective>
Collapse the admin route tree: delete the `/$brandSlug` segment and its layout, relocate the 18 route files
beneath it, move the onboarding-resume redirect to the surviving protected layout, delete the brand
switcher, and replace it with a static organization label.

Purpose: D-20 removes the second source of truth for context. The session decides; the host displays. Every
`params: { brandSlug }` and `useParams().brandSlug` in the tree has to go with it.
Output: a route tree with no dynamic organization segment and a sidebar whose only interactive context
control is the location switcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@apps/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the 18 route files out of $brandSlug and rewrite their paths</name>
  <files>apps/admin/src/main.tsx, apps/admin/src/routes/(protected)/_layout.tsx, apps/admin/src/routes/(protected)/index.tsx, apps/admin/src/routes/(protected)/locations.tsx, apps/admin/src/routes/(protected)/orders.tsx, apps/admin/src/routes/(protected)/settings.tsx, apps/admin/src/routes/(protected)/team.tsx, apps/admin/src/routes/(protected)/roles.tsx, apps/admin/src/routes/(protected)/roles.$roleId.tsx, apps/admin/src/routes/(protected)/tenant.domains.tsx, apps/admin/src/routes/(protected)/tenant.payouts.tsx, apps/admin/src/routes/(protected)/tenant.theme.tsx, apps/admin/src/routes/(protected)/menu/_layout.tsx, apps/admin/src/routes/(protected)/menu/categories.tsx, apps/admin/src/routes/(protected)/menu/items.tsx, apps/admin/src/routes/(protected)/menu/items.$id.tsx, apps/admin/src/routes/(protected)/menu/modifier-groups.tsx, apps/admin/src/routes/(protected)/menu/modifier-groups.$id.tsx, apps/admin/src/routes/(protected)/menu/stop-list.tsx, apps/admin/src/routes/(protected)/dashboard-redirect.$.tsx</files>
  <read_first>
    - apps/admin/src/routes/(protected)/$brandSlug/_layout.tsx (65 lines, read in full: `path: '/$brandSlug'`, `validateSearch: locationSearchSchema`, `meQuery` + `meBrandsQuery` suspense reads, the `brands.length === 0 -> navigate('/onboarding/brand')` redirect at ~48-51 which is the resume precedent D-31 reuses, and the `SidebarProvider` / `AppSidebar` / `SidebarInset` / `SiteHeader` / `Outlet` composition)
    - apps/admin/src/routes/(protected)/_layout.tsx (the surviving parent: `id: '/(protected)'`, `beforeLoad` redirecting to `/login` when there is no session, and a bare `<Outlet/>` component — this is where the chrome and the redirect move to)
    - apps/admin/src/main.tsx (the route-tree assembly — every `getParentRoute` wiring and the `$brandSlug` layout registration)
    - apps/admin/src/routes/(protected)/dashboard-redirect.$.tsx (`resolveLegacyDashboardRedirect` and its `path: '/dashboard/$'` splat)
    - All 18 files under apps/admin/src/routes/(protected)/$brandSlug/ (each one's `path`, its `useParams`/`params` usage, and its `getParentRoute`)
    - apps/admin/src/lib/queries/identity.ts (post-plan-14 `meTenantsQuery` returning `status`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md §F5 (the canonical before/after for the layout and the "17 files are a mechanical path-prefix removal" finding)
  </read_first>
  <action>
Move all 18 files from `apps/admin/src/routes/(protected)/$brandSlug/` to
`apps/admin/src/routes/(protected)/`, delete the `$brandSlug` directory, and for each moved file:
- shorten its route `path` from `/$brandSlug/...` to `/...`;
- delete every `Route.useParams()` / `useParams({ strict: false })` read of `brandSlug`;
- delete every `params: { brandSlug }` argument on `<Link>` and `navigate()` calls;
- repoint `getParentRoute` at the surviving `(protected)/_layout.tsx`.

Rename the three brand-settings routes (D-40): `brands.$slug.domains.tsx` -> `tenant.domains.tsx`,
`brands.$slug.payouts.tsx` -> `tenant.payouts.tsx`, `brands.$slug.theme.tsx` -> `tenant.theme.tsx`, with
paths `/tenant/domains`, `/tenant/payouts`, `/tenant/theme`. The `$slug` param disappears — there is exactly
one organization per session, so the settings screens operate on it implicitly.

Fold `$brandSlug/_layout.tsx` into `(protected)/_layout.tsx`:
- keep the existing `beforeLoad` session check and its `/login` redirect with `safeNext`;
- move over the `SidebarProvider` / `AppSidebar` / `SidebarInset` / `SiteHeader` / `Outlet` composition and
  the `validateSearch: locationSearchSchema`;
- replace `meBrandsQuery` with `meTenantsQuery` and rewrite the redirect that implements D-31's resume:
  zero organizations -> `/onboarding`; exactly one with `status === 'pending_setup'` -> `/onboarding`;
  two or more -> the picker is a sign-in-time concern (plan 17), so here just render;
  otherwise render the dashboard. Derive all of it from the `status` field — do not introduce a progress
  flag, a cookie or a local storage key (D-31, and `apps/CLAUDE.md` forbids a context cookie).

`dashboard-redirect.$.tsx`: its `resolveLegacyDashboardRedirect` maps legacy `/dashboard/...` URLs onto real
paths. Re-read it and decide whether it still has a job now that no path carries an organization segment;
if it does, keep it and make sure its output never reintroduces a `$brandSlug` prefix. If it does not, delete
it (D-39) and say so. Keep its existing `/^\/[/\\]/` protocol-relative guard either way — that guard stops an
open redirect and must not be lost.

`main.tsx`: rebuild the route-tree assembly for the new shape and remove the `$brandSlug` layout import.

No compatibility redirect from old `/{brandSlug}/...` URLs is built. CONTEXT.md is explicit (D-20 supersedes
phase 02-03's D-03; no shim), and the organization has moved to the host anyway, so an old path carries an
organization slug that means nothing on the new host.
  </action>
  <verify>
    <automated>pnpm --filter admin typecheck && pnpm --filter admin build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d "apps/admin/src/routes/(protected)/\$brandSlug"` succeeds.
    - `rg -c "brandSlug" apps/admin/src/routes/` returns 0.
    - `rg -c "\\\$brandSlug" apps/admin/src` returns 0.
    - `ls "apps/admin/src/routes/(protected)/" | wc -l` accounts for all 18 relocated files plus `_layout.tsx`, `onboarding/` and `dashboard-redirect.$.tsx` (or its deletion) — enumerate the final listing in the SUMMARY.
    - `rg -n "meTenantsQuery" "apps/admin/src/routes/(protected)/_layout.tsx"` and `rg -n "pending_setup" "apps/admin/src/routes/(protected)/_layout.tsx"` both match.
    - `rg -n "document.cookie|localStorage" "apps/admin/src/routes/(protected)/_layout.tsx"` returns no matches.
    - `rg -n "safeNext" "apps/admin/src/routes/(protected)/_layout.tsx"` still matches (the login redirect keeps its open-redirect guard).
    - `pnpm --filter admin typecheck` and `pnpm --filter admin build` both exit 0.
  </acceptance_criteria>
  <done>Admin URLs are `/orders` and `/menu/items`; an owner mid-onboarding is sent to onboarding by the layout, derived from status alone.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the brand switcher with a static organization label</name>
  <files>apps/admin/src/components/app-sidebar.tsx, apps/admin/src/components/brand-switcher.tsx, apps/admin/src/components/tenant-identity.tsx, apps/admin/src/components/location-switcher.tsx, apps/admin/src/components/site-header.tsx</files>
  <read_first>
    - apps/admin/src/components/brand-switcher.tsx (101 lines: its `switchTo()` POSTs `/v1/me/set-active-brand` then navigates to `/$brandSlug`; its "Add brand" `DropdownMenuItem` at ~76-82 links to `/onboarding/brand`; its `SidebarMenuButton size="lg"` trigger with avatar-initials + two-line name/slug)
    - apps/admin/src/components/app-sidebar.tsx (146 lines: `SidebarHeader` stacking `<BrandSwitcher>` then `<LocationSwitcher>` at ~132-135; the `brandPrefix = activeBrandSlug ? '/' + activeBrandSlug : ''` variable and every `${brandPrefix}/orders`-style URL; the `activeBrandSlug` prop threading)
    - apps/admin/src/components/location-switcher.tsx (imports `Route as brandSlugLayoutRoute from '@/routes/(protected)/$brandSlug/_layout'` at ~line 3 purely for `useNavigate({ from: ... })` type narrowing at ~32; its own logic at 36-89 is untouched by the merge)
    - apps/admin/src/components/site-header.tsx (referenced from app-sidebar; check for brand props)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S5 (the static identity block: same visual footprint, no DropdownMenu, no chevron, no hover affordance, must not be focusable — and the tablet-in-the-kitchen rationale for keeping it at all)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §"Accessibility Contract" (the block must not be a `button`, must have no `tabIndex`, must have no `role="button"`)
  </read_first>
  <action>
Delete `apps/admin/src/components/brand-switcher.tsx`.

Create `apps/admin/src/components/tenant-identity.tsx`: a static, non-interactive block with the same visual
footprint as the switcher's old trigger — avatar-initials square plus a two-line name/slug stack, styled to
match `SidebarMenuButton size="lg"` — but with NO `DropdownMenu` wrapper, NO chevron, and NO hover or active
affordance. It must read as inert rather than as a broken or disabled control. Per the accessibility
contract it is a plain `div`: no `button` element, no `tabIndex`, no `role="button"`. A focusable-but-inert
control would be a worse outcome than a truthful label.

It is kept, rather than dropped in favour of the address bar, because the admin targets tablet-in-the-kitchen
use where browser chrome is often minimised or covered — leaving "which restaurant is this" discoverable
only in a URL bar is a real regression for that user, not a neutral cleanup.

`app-sidebar.tsx`:
- swap `<BrandSwitcher .../>` for `<TenantIdentity .../>` in `SidebarHeader`, keeping `<LocationSwitcher/>`
  second in the stack;
- delete the `brandPrefix` variable and rewrite every nav URL from `` `${brandPrefix}/orders` `` to a bare
  `/orders`;
- delete the `activeBrandSlug` prop and its threading through `AppSidebar` and `NavMain`;
- the `NavMainItem.scope: 'brand' | 'tenant'` distinction is now meaningless — every item is tenant-scoped.
  Read how `scope` is consumed before deciding: if it only gated brand-vs-tenant visibility, delete the field
  and its consumers; if it is also doing permission-based visibility gating, keep that behaviour under a name
  that describes what it actually does. State which case applied in the SUMMARY.

`location-switcher.tsx`: change ONLY the import of the deleted `$brandSlug/_layout` route object to the
surviving `(protected)/_layout` route, so `useNavigate({ from: ... })` keeps its type narrowing. Its logic at
lines 36-89 — including the `if (!isOwner || locations.length === 0) return null` conditional render — is
unchanged (D-19).

`site-header.tsx`: remove any brand prop or brand-derived title.

No "+ Add restaurant" entry point is built. Deleting the switcher removes the only organization-creation
entry point in the app, and CONTEXT.md's deferred GAP is explicit that no replacement ships this phase —
restate that in the SUMMARY so it is not silently absorbed. UI-SPEC S5 notes the picker screen is the natural
future home for it.
  </action>
  <verify>
    <automated>pnpm --filter admin typecheck && pnpm --filter admin build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/admin/src/components/brand-switcher.tsx` succeeds.
    - `test -f apps/admin/src/components/tenant-identity.tsx` succeeds and `rg -n "DropdownMenu|ChevronsUpDown|ChevronDown|tabIndex|role=\"button\"|<button" apps/admin/src/components/tenant-identity.tsx` returns no matches.
    - `rg -c "brandPrefix|activeBrandSlug" apps/admin/src/components/` returns 0.
    - `rg -n "TenantIdentity" apps/admin/src/components/app-sidebar.tsx` and `rg -n "LocationSwitcher" apps/admin/src/components/app-sidebar.tsx` both match, with `TenantIdentity` at a lower line number (first in the header stack).
    - `rg -n "isOwner|locations.length === 0" apps/admin/src/components/location-switcher.tsx` still matches (D-19 behaviour preserved).
    - `rg -n "Add brand|Add restaurant|onboarding" apps/admin/src/components/` returns no matches (no create entry point rebuilt).
    - `pnpm --filter admin build` exits 0.
    - The SUMMARY restates the create-organization GAP and records the `NavMainItem.scope` decision.
  </acceptance_criteria>
  <done>The sidebar shows which restaurant the operator is in, offers no way to change or add one, and still switches locations exactly as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path -> rendered context | Previously a path segment could disagree with the session; after D-20 it cannot |
| `next` search param -> post-login redirect | An attacker-controlled redirect target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.2-15-01 | Spoofing | A URL segment claiming an organization the session is not bound to | mitigate | The segment is deleted entirely (D-20); the session is the only source of context and the API's `auth.tenant_mismatch` cross-check remains the server-side fence |
| T-10.2-15-02 | Tampering | Open redirect via `safeNext` or `resolveLegacyDashboardRedirect` lost during the layout merge | mitigate | Acceptance criteria assert `safeNext` survives in the protected layout and the `/^\/[/\\]/` protocol-relative guard survives in the redirect helper (or the helper is deleted outright) |
| T-10.2-15-03 | Information Disclosure | The static identity block rendering a placeholder or a previous organization's name | mitigate | It renders from `meTenantsQuery` data bound to the session; `apps/CLAUDE.md` forbids static identity placeholders, and a grep for hardcoded names belongs in plan 20's final sweep |
| T-10.2-15-04 | Elevation of Privilege | An inert-looking label that is actually focusable and actionable | mitigate | Acceptance criterion greps for `<button`, `tabIndex` and `role="button"` returning zero in `tenant-identity.tsx` |
| T-10.2-15-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; UI-SPEC confirms zero new shadcn primitives |
</threat_model>

<verification>
- `pnpm --filter admin typecheck` and `pnpm --filter admin build` both exit 0.
- `rg -c "brandSlug|\$brandSlug|brandPrefix|activeBrandSlug" apps/admin/src/routes/ apps/admin/src/components/` returns 0.
- `test ! -d "apps/admin/src/routes/(protected)/\$brandSlug"` succeeds.
- Manual verification of the rendered chrome is deferred to plan 17's checkpoint, which walks the whole
  sign-in journey once the auth surfaces exist.
</verification>

<success_criteria>
- No admin route carries an organization segment.
- The onboarding-resume redirect lives in the protected layout and reads `status`.
- The brand switcher is deleted and replaced by a static, non-focusable label.
- The location switcher is unchanged.
</success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-15-SUMMARY.md` when done.
</output>
