---
phase: 10.2-brand-pinned-sessions
plan: 01
subsystem: domain
tags: [zod, packages/domain, rbac, i18n, value-objects]

# Dependency graph
requires: []
provides:
  - Country/currency/locale registry (`packages/domain/src/country.ts`) — UA/GB/ES mapped to UAH/GBP/EUR and ru/en/es
  - TenantSlugValue/TenantSlug consolidation (`packages/domain/src/tenant-slug.ts`) — BrandSlug/BrandSlugValue deleted, folded in
  - TenantTheme (`packages/domain/src/tenant-theme.ts`) — BrandTheme renamed and repointed at `tenants.theme`
  - Brand RBAC resource removed from `PERMISSIONS_STATEMENT`/`SYSTEM_ROLES`/`PRESET_ROLES` — dropped, not renamed
  - `'pick-organization'` reserved slug (admin route shadow protection for plan 16)
affects:
  [
    10.2-02,
    10.2-03,
    10.2-06,
    10.2-08,
    10.2-09,
    10.2-10,
    10.2-12,
    10.2-13,
    10.2-16,
    10.2-19,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Country registry as a plain Readonly<Record<...>> catalogue (mirrors reserved-slugs.ts/rbac/permissions.ts), zero infra imports, zero Zod wrapper on the registry object itself'
    - 'RESTO_SPINE_MIGRATION=1 pre-commit opt-out for renames that cross packages/domain and cannot leave the repo type-green until every downstream call site is fixed by later plans in the same phase'

key-files:
  created:
    - packages/domain/src/country.ts
    - packages/domain/src/tenant-theme.ts
  modified:
    - packages/domain/src/index.ts
    - packages/domain/src/tenant-slug.ts
    - packages/domain/src/ids.ts
    - packages/domain/src/reserved-slugs.ts
    - packages/domain/src/rbac/permissions.ts
    - packages/domain/src/rbac/system-roles.ts
    - packages/domain/src/rbac/preset-roles.ts
    - packages/domain/CLAUDE.md

key-decisions:
  - 'D-32/D-33/D-35/D-38 implemented as written: UA/GB/ES only, UAH/GBP/EUR, ru/en/es default locales, UA pinned to ru (not uk) per the langSwitcher-mismatch WHY-comment'
  - 'D-40 implemented: BrandSlug/BrandSlugValue folded into TenantSlugValue (new unbranded HTTP-boundary export) with TenantSlug kept as an alias; BrandTheme renamed to TenantTheme; BrandId deleted from ids.ts'
  - 'D-39 implemented: the brand RBAC resource is deleted outright from PERMISSIONS_STATEMENT, SYSTEM_ROLES and PRESET_ROLES — not folded into tenant:*, not aliased'
  - 'Tasks 2 and 3 committed with RESTO_SPINE_MIGRATION=1 (see Deviations) — the documented, repo-policy opt-out for exactly this situation, not a hook bypass'

patterns-established:
  - 'Country config seam: currencyForCountry()/defaultLocaleForCountry() as total functions over an exhaustive Record — no fallback branch needed'

requirements-completed: []

# Metrics
duration: ~12min (Task 1, prior session) + ~25min (Tasks 2-3 landing, this session)
completed: 2026-08-20
---

# Phase 10.2 Plan 01: Shared registries (country, tenant-slug, RBAC) Summary

**Country/currency/locale registry, TenantSlugValue/TenantTheme consolidation, and brand RBAC resource removal — all three tasks complete in `packages/domain`, which typechecks and tests clean in isolation; the repo-wide `apps/api`/`apps/admin`/`packages/events` typecheck break is the expected, planned state until plans 06/08/09/10/13 land.**

## Performance

- **Started:** ~2026-08-20T07:31:00Z (Task 1, prior session)
- **Completed:** 2026-08-20 (Tasks 2-3 landed this session)
- **Tasks:** 3 of 3 committed
- **Files modified:** 14 (2 Task 1, 10 Task 2, 4 Task 3, with 2 files touched by both Task 1 and Task 2's `index.ts` hunks counted once)

## Accomplishments

- `packages/domain/src/country.ts` — `CountryCodeValue` (`UA`/`GB`/`ES`), `SUPPORTED_COUNTRIES`, `CountryConfig`, `COUNTRY_REGISTRY`, `currencyForCountry`, `defaultLocaleForCountry` — implemented exactly to the plan's `<interfaces>` contract, barrel-exported from `packages/domain/src/index.ts`.
- `packages/domain/src/tenant-slug.ts` — `TenantSlugValue` is now the unbranded HTTP-boundary schema (32 external call sites keep using `TenantSlug`, which is now an alias of `TenantSlugValue`); `brand-slug.ts` deleted outright.
- `packages/domain/src/tenant-theme.ts` — `BrandTheme` renamed to `TenantTheme`, doc-comment updated to reflect it persists on `tenants.theme` post-merge; `brand-theme.ts` deleted.
- `BrandId` removed from `packages/domain/src/ids.ts` and the barrel.
- `'pick-organization'` added to `ADMIN_ROOT_ROUTE_SEGMENTS` and `RESERVED_SLUGS`, asserted rejected by `TenantSlugValue` in the value-objects spec — closes the admin-route-shadow threat (T-10.2-01-03).
- The `brand` RBAC resource is gone from `PERMISSIONS_STATEMENT`, every `SYSTEM_ROLES` grant, and every `PRESET_ROLES` grant — closes the stale-grantable-resource threat (T-10.2-01-02).
- `packages/domain`: `npx tsc -p tsconfig.json --noEmit` exits 0; `npx vitest run` — 131/131 tests pass (8 test files).

## Task Commits

1. **Task 1: Create the country registry in packages/domain** — `dfab518d` (feat)
2. **Task 2: Fold BrandSlug and BrandTheme into tenant-named equivalents** — `a44da912` (feat)
3. **Task 3: Drop the brand RBAC resource** — `596f81ca` (feat)

**Plan metadata:** this commit (docs: SUMMARY rewrite + patch cleanup)

## Files Created/Modified

### Task 1 (prior session)

- `packages/domain/src/country.ts` — country/currency/locale registry
- `packages/domain/src/index.ts` — barrel export for the country registry (country-only hunk)

### Task 2

- `packages/domain/src/tenant-slug.ts` — adds `TenantSlugValue` (unbranded HTTP-boundary form), `TenantSlug` becomes an alias
- `packages/domain/src/brand-slug.ts` — deleted
- `packages/domain/src/brand-theme.ts` → renamed to `packages/domain/src/tenant-theme.ts` (`BrandTheme` → `TenantTheme`)
- `packages/domain/src/ids.ts` — `BrandId` removed
- `packages/domain/src/index.ts` — remaining hunk: `BrandId`/`BrandSlug`/`BrandSlugValue`/`BrandTheme` exports removed, `TenantSlugValue`/`TenantTheme` added
- `packages/domain/src/reserved-slugs.ts` — `pick-organization` added to `ADMIN_ROOT_ROUTE_SEGMENTS` and `RESERVED_SLUGS`; stale "brand" wording removed from doc comments
- `packages/domain/src/reserved-slugs.spec.ts` — `BrandSlugValue` import/assertions replaced with `TenantSlugValue` (Rule 3 auto-fix — this file imported `./brand-slug`, which Task 2 deletes)
- `packages/domain/test/brand-theme.spec.ts` → renamed to `packages/domain/test/tenant-theme.spec.ts`, retargeted at `TenantTheme` (Rule 3 auto-fix — imports `../src/brand-theme`, deleted by Task 2)
- `packages/domain/test/value-objects.spec.ts` — `BrandSlug` describe block replaced with a `TenantSlugValue` describe block (reserved-slug rejection, punycode rejection, normal-slug acceptance, plus a new `'pick-organization'` rejection assertion per the plan's explicit instruction)
- `packages/domain/CLAUDE.md` — Layout/Rules sections corrected to drop references to the now-deleted `brand-slug`/`brand-theme`/`BrandId` (Rule 2 auto-fix, this session — the file directly documents what Task 2 renamed and was left stale by the patch)

### Task 3

- `packages/domain/src/rbac/permissions.ts` — `brand` resource removed from `PERMISSIONS_STATEMENT`
- `packages/domain/src/rbac/system-roles.ts` — `brand` grants removed from `owner`/`admin`/`staff`
- `packages/domain/src/rbac/preset-roles.ts` — `brand` grants removed from `manager`/`cashier-foh`/`kitchen` (Rule 3 auto-fix — required for the Task 3 acceptance criterion `rg -in "brand" packages/domain/src/rbac/` returning zero matches; not in the plan's `files_modified` list but the file lives inside the directory that criterion scans)
- `packages/domain/test/rbac.spec.ts` — `brand` assertions removed from the resource-list expectation, the "admin can create/update/delete brands" test deleted, "staff can only read tenant + brand + location" rewritten as "... tenant + location" (Rule 3 auto-fix — this IS the RBAC regression spec Task 3 instructs to update; the plan's task text says it lives in `packages/domain/src/rbac/`, but it actually lives in `packages/domain/test/`)

## Decisions Made

- Implemented Task 1 exactly per the plan's `<interfaces>` contract (exact export names, D-33 country order, D-38 WHY-comment on `UA -> 'ru'`).
- Split the single `index.ts` diff (which the plan describes as one file touched by both Task 1 and Task 2) into two isolated commits — Task 1's country-registry hunk landed in the prior session, Task 2's Brand→Tenant rename hunk lands here — preserving true per-task atomicity instead of bundling both tasks' `index.ts` changes into either commit.
- Extended Task 3's file scope to `packages/domain/src/rbac/preset-roles.ts` and `packages/domain/test/rbac.spec.ts`, per Rule 3 (blocking-issue auto-fix): the plan's own acceptance criteria (`rg -in "brand" packages/domain/src/rbac/` returns zero; `npx vitest run` exits 0) cannot both be satisfied without touching these files, even though they aren't in the plan's `files_modified` frontmatter.
- Committed Tasks 2 and 3 with `RESTO_SPINE_MIGRATION=1` (see Deviations below) rather than leaving them uncommitted or bypassing the hook entirely.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - blocking issue] `preset-roles.ts` retained `brand` grants after Task 3's `permissions.ts`/`system-roles.ts` edits**

- **Found during:** Task 3
- **Issue:** Task 3's acceptance criterion `rg -in "brand" packages/domain/src/rbac/` requires zero matches in the whole directory; `preset-roles.ts` (in that directory) had `brand: [...]` grants in all three preset roles.
- **Fix:** Removed the `brand` line from `manager`, `cashier-foh`, `kitchen` preset permission objects.
- **Files modified:** `packages/domain/src/rbac/preset-roles.ts`
- **Verification:** `rg -in "brand" packages/domain/src/rbac/` returns zero matches; no spec in the domain package imports `PRESET_ROLES`, so nothing else broke.
- **Committed in:** `596f81ca` (Task 3 commit)

**2. [Rule 3 - blocking issue] `reserved-slugs.spec.ts` imports the file Task 2 deletes**

- **Found during:** Task 2
- **Issue:** `packages/domain/src/reserved-slugs.spec.ts` imports `BrandSlugValue` from `./brand-slug`, which Task 2 deletes outright. Not listed in the plan's `files_modified`.
- **Fix:** Re-pointed the import and both describe-block assertions at `TenantSlugValue` from `./tenant-slug`.
- **Files modified:** `packages/domain/src/reserved-slugs.spec.ts`
- **Verification:** `npx vitest run` — file passes (6/6).
- **Committed in:** `a44da912` (Task 2 commit)

**3. [Rule 3 - blocking issue] `test/brand-theme.spec.ts` imports the file Task 2 deletes**

- **Found during:** Task 2
- **Issue:** Same class of issue as #2 — imports `../src/brand-theme`, deleted by Task 2.
- **Fix:** Renamed to `test/tenant-theme.spec.ts`, retargeted at `TenantTheme` from `../src/tenant-theme`.
- **Files modified:** `packages/domain/test/brand-theme.spec.ts` → `packages/domain/test/tenant-theme.spec.ts`
- **Verification:** `npx vitest run` — file passes (6/6).
- **Committed in:** `a44da912` (Task 2 commit)

**4. [Rule 3 - blocking issue] `test/rbac.spec.ts` asserts the `brand` resource the plan requires deleted**

- **Found during:** Task 3
- **Issue:** This file (not `src/rbac/`, despite the plan's task text pointing there) is the actual RBAC regression spec: it asserts `brand` is in `PERMISSIONS_STATEMENT`'s resource list, and asserts `SYSTEM_ROLES.admin.brand`/`SYSTEM_ROLES.staff.brand` shapes. Left unchanged, `npx vitest run` fails after `permissions.ts`/`system-roles.ts` drop `brand`.
- **Fix:** Removed `'brand'` from the expected resource-key list; deleted the "admin can create/update/delete brands" test; rewrote "staff can only read tenant + brand + location" to "... tenant + location" without the brand assertions.
- **Files modified:** `packages/domain/test/rbac.spec.ts`
- **Verification:** `npx vitest run` — 23/23 in this file, 131/131 overall.
- **Committed in:** `596f81ca` (Task 3 commit)

**5. [Rule 2 - missing critical / documentation drift] `packages/domain/CLAUDE.md` referenced files Task 2 deletes**

- **Found during:** landing Tasks 2-3, this session
- **Issue:** The package's own CLAUDE.md Layout section listed `brand-slug`, `brand-theme`, and `BrandId` as part of the package surface, and a Schema-discipline rule referenced `BrandTheme.font` by name — all stale the moment Task 2's rename lands, and immediately misleading to the next person (or agent) reading this package's docs.
- **Fix:** Updated the Layout bullet to list `tenant-slug`/`tenant-theme` instead of `brand-slug`/`brand-theme`, dropped `BrandId` from the ids.ts bullet, and repointed the CSS-injection rule from `BrandTheme.font` to `TenantTheme.font`.
- **Files modified:** `packages/domain/CLAUDE.md`
- **Verification:** `rg -c "brand-slug|brand-theme" packages/domain/CLAUDE.md` returns 0.
- **Committed in:** `a44da912` (Task 2 commit)

**6. [Repo-policy opt-out, not a rule-1/2/3 auto-fix] Tasks 2 and 3 committed with `RESTO_SPINE_MIGRATION=1`**

- **Found during:** committing Task 2
- **Issue:** `.husky/pre-commit` runs `nx affected -t typecheck --uncommitted` on every commit. `packages/domain`'s Task 2/3 changes are correctly flagged as affecting `apps/api`, `apps/admin`, and `packages/events`, all of which still import `BrandId`/`BrandSlug`/`BrandSlugValue`/`BrandTheme` from `@resto/domain` — 34 compile errors across 22 files. This plan's own `<verification>` section states this is expected and explicitly forbids restoring green here (D-39: no compatibility alias).
- **Why this is the correct resolution, not a bypass:** `.husky/pre-commit` (committed at `3403657e`, prior to this plan, by the repo owner) documents exactly this situation as the intended use of `RESTO_SPINE_MIGRATION=1`: _"a rename that crosses packages/domain or packages/db and cannot leave the repo type-green until every call site is fixed... The phase that sets it owes a green gate before it closes."_ `lint-staged` (prettier + eslint) still ran normally on both commits — only the affected-typecheck step was skipped, and only via the documented, narrowly-scoped flag. This is distinct from `--no-verify` or `HUSKY=0`, both of which remain forbidden and were not used.
- **Files/commits affected:** `a44da912` (Task 2), `596f81ca` (Task 3) — both committed as `RESTO_SPINE_MIGRATION=1 git commit -m "..."`.
- **Verification:** `packages/domain` typechecks and tests clean in isolation (`cd packages/domain && npx tsc -p tsconfig.json --noEmit` exits 0; `npx vitest run` — 131/131). Repo-wide `pnpm exec nx run-many -t typecheck --projects=api,admin,events,seed-cli` shows 34 errors across 22 files, entirely `BrandId`/`BrandSlug`/`BrandTheme`/`brand` import failures in `apps/api/src/contexts/{tenancy,identity,catalog,ordering,payments,notifications}`, `apps/admin` (cache hit — not independently re-verified this session, see below), and `packages/events/src/contracts/tenancy.ts` — matches the plan's own prediction ("roughly 32 errors across 22 files in events/api/admin/seed-cli") almost exactly.
- **What's left red for later plans:** call sites of `BrandId`/`BrandSlug`/`BrandTheme` in `apps/api` (tenancy, identity, catalog, ordering, payments, notifications contexts) and `packages/events/src/contracts/tenancy.ts` — removed by plans 06, 08, 09, 10 and 13 per the plan's own `<interfaces>` note. The green gate across the whole repo is plan 19 (`10.2-19-PLAN.md`); do not re-add a `BrandSlug`/`BrandId`/`BrandTheme` alias to force interim green (D-39).
- **Caveat on `apps/admin`:** the `nx run-many` invocation reported `admin:typecheck` as a local-cache hit ("existing outputs match the cache, left as is") rather than an executed check. This session did not force a cache bust to independently confirm `apps/admin`'s error count; the plan's own text states `apps/admin` also imports `BrandSlug`/`BrandId`/`BrandTheme`, so it should be red too once actually run. Whoever picks up plans 06/08/09/10/13 should not assume `apps/admin` is clean on the strength of this cache hit alone.

---

**Total deviations:** 6 (4 Rule 3 blocking-issue auto-fixes carried over from the verified patch, 1 Rule 2 documentation-drift auto-fix this session, 1 repo-policy opt-out for committing Tasks 2-3)
**Impact on plan:** All auto-fixes were necessary for `packages/domain` to typecheck/test green in isolation, matching the plan's own acceptance criteria. No scope creep into plans 06/08/09/10/13's territory — zero downstream (`apps/api`/`apps/admin`/`packages/events`) call sites were touched. No compatibility alias was added (D-39 upheld).

## Issues Encountered

- This worktree had no `node_modules` at session start (fresh worktree checkout); `pnpm install --frozen-lockfile` was run first (12.3s, reused the shared pnpm store) before any typecheck/test verification was possible.
- The worktree's branch had been created from an older point in `admin-vite-spa`'s history (before Task 1 and the `RESTO_SPINE_MIGRATION` hook opt-out landed) and needed `git reset --hard` to the intended base commit before any of this session's work could be applied — resolved per the orchestrator-provided base commit, no data loss (working tree was clean at that point).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `packages/domain` fully delivers this plan's contract: `COUNTRY_REGISTRY`/`TenantSlugValue`/`TenantTheme` are live and importable from `@resto/domain`; the `brand` RBAC resource and `BrandId`/`BrandSlug`/`BrandTheme` are gone from the package.
- Plans 06, 08, 09, 10 and 13 must update their `apps/api`/`packages/events` call sites from `BrandId`/`BrandSlug`/`BrandTheme` to the tenant-named equivalents before their own commits will pass the pre-commit affected-typecheck gate without needing `RESTO_SPINE_MIGRATION=1` themselves.
- Plan 19 owes the repo-wide green gate this plan's `<verification>` section explicitly defers to it.
- Plan 20 (cleanup) can proceed without special-casing this plan's patch file — `10.2-01-tasks-2-3-verified.patch` no longer exists; its content lives entirely in commits `a44da912` and `596f81ca`.

## Self-Check

- `packages/domain/src/country.ts` — FOUND (committed in `dfab518d`)
- `packages/domain/src/tenant-theme.ts` — FOUND (committed in `a44da912`)
- `packages/domain/src/brand-slug.ts` — CONFIRMED ABSENT (deleted in `a44da912`)
- `packages/domain/src/brand-theme.ts` — CONFIRMED ABSENT (deleted in `a44da912`, renamed to `tenant-theme.ts`)
- Commit `dfab518d` — FOUND
- Commit `a44da912` — FOUND
- Commit `596f81ca` — FOUND
- `.planning/phases/10.2-brand-pinned-sessions/10.2-01-tasks-2-3-verified.patch` — CONFIRMED ABSENT (removed in this commit, per plan cleanup requirement)

## Self-Check: PASSED
