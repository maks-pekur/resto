---
phase: 10.2-brand-pinned-sessions
plan: 10
type: execute
wave: 5
depends_on: ['10.2-05', '10.2-06']
files_modified:
  - apps/api/src/contexts/ordering/domain/order.aggregate.ts
  - apps/api/src/contexts/ordering/domain/order.aggregate.spec.ts
  - apps/api/src/contexts/ordering/domain/ports.ts
  - apps/api/src/contexts/ordering/domain/events.ts
  - apps/api/src/contexts/ordering/application/create-order.service.ts
  - apps/api/src/contexts/ordering/application/get-order-detail.service.ts
  - apps/api/src/contexts/ordering/application/list-orders.service.ts
  - apps/api/src/contexts/ordering/infrastructure/order-drizzle.repository.ts
  - apps/api/src/contexts/ordering/infrastructure/catalog-menu-pricing.adapter.ts
  - apps/api/src/contexts/ordering/interfaces/http/operator-orders.dto.ts
  - apps/api/src/contexts/ordering/interfaces/http/orders.controller.spec.ts
  - apps/api/src/contexts/payments/domain/ports.ts
  - apps/api/src/contexts/payments/application/create-checkout-payment.service.ts
  - apps/api/src/contexts/payments/application/create-checkout-payment.service.spec.ts
  - apps/api/src/contexts/payments/application/cancel-order.service.spec.ts
  - apps/api/src/contexts/payments/application/handle-stripe-event.service.spec.ts
  - apps/api/src/contexts/payments/application/refund-order.service.spec.ts
  - apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts
  - apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.spec.ts
  - apps/api/src/contexts/notifications/application/send-guest-notification.service.ts
  - apps/api/src/contexts/notifications/application/send-guest-notification.service.spec.ts
  - apps/api/src/contexts/notifications/infrastructure/notification-order-drizzle.repository.ts
  - packages/events/src/contracts/ordering.ts
autonomous: true
requirements: []
decisions: [D-08, D-11, D-35, D-39, D-40]

must_haves:
  truths:
    - 'D-08: an order carries a tenant and a location and no brand'
    - "D-35: a Stripe checkout is created against the restaurant's own connected account with its own country-derived currency"
    - 'D-40: the ordering event contract carries no brandId and publishes on no brand-named subject'
    - 'Money paths keep their existing behaviour: server-side price authority, refund and cancel flows, guest notifications'
  artifacts:
    - path: 'apps/api/src/contexts/ordering/domain/order.aggregate.ts'
      provides: 'order aggregate with tenant + location scoping only'
    - path: 'packages/events/src/contracts/ordering.ts'
      provides: 'ordering event contracts without brandId'
  key_links:
    - from: 'apps/api/src/contexts/payments/application/create-checkout-payment.service.ts'
      to: 'apps/api/src/contexts/tenancy'
      via: 'connected-account lookup now reads the merged tenant, not a brand'
      pattern: 'stripeAccountId'
    - from: 'apps/api/src/contexts/ordering/domain/events.ts'
      to: 'packages/events/src/contracts/ordering.ts'
      via: 'emitted payload matching the contract'
      pattern: "ordering\\."
---

<objective>
Sweep the brand dimension out of ordering, payments and notifications — 22 files plus the ordering event
contract — and repoint the Stripe connected-account lookup at the merged tenant.

Purpose: this is the money path. Project memory records that Phase 8 shipped six runtime bugs past a green
suite because tests asserted call shape against mocks instead of observable side-effects on
production-shaped data. Every co-located spec touched here must be repaired in the same way.
Output: `apps/api/src/contexts/{ordering,payments,notifications}` and
`packages/events/src/contracts/ordering.ts` carry no brand identifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@apps/api/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-05-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the ordering context and its event contract</name>
  <files>apps/api/src/contexts/ordering/domain/order.aggregate.ts, apps/api/src/contexts/ordering/domain/order.aggregate.spec.ts, apps/api/src/contexts/ordering/domain/ports.ts, apps/api/src/contexts/ordering/domain/events.ts, apps/api/src/contexts/ordering/application/create-order.service.ts, apps/api/src/contexts/ordering/application/get-order-detail.service.ts, apps/api/src/contexts/ordering/application/list-orders.service.ts, apps/api/src/contexts/ordering/infrastructure/order-drizzle.repository.ts, apps/api/src/contexts/ordering/infrastructure/catalog-menu-pricing.adapter.ts, apps/api/src/contexts/ordering/interfaces/http/operator-orders.dto.ts, apps/api/src/contexts/ordering/interfaces/http/orders.controller.spec.ts, packages/events/src/contracts/ordering.ts</files>
  <read_first>
    - apps/api/src/contexts/ordering/domain/order.aggregate.ts (the `OrderSnapshot`, the state machine, `pullEvents()`; every `brandId` field)
    - apps/api/src/contexts/ordering/domain/events.ts (emitted domain events and the payload shapes that must match the contract)
    - packages/events/src/contracts/ordering.ts (~line 8: `brandId: z.string().uuid()` in an order-event payload; also the subject strings)
    - packages/events/src/envelope.ts and packages/events/src/outbox/ (envelope construction via `buildEnvelope` — ADR-0020 I-4 forbids direct literals)
    - apps/api/src/contexts/ordering/infrastructure/order-drizzle.repository.ts (inserts supplying `brand_id`, which no longer exists as a column after plan 05)
    - apps/api/src/contexts/ordering/application/list-orders.service.ts and get-order-detail.service.ts (`l.status === 'active'` location filtering from Phase 10 — must survive)
    - packages/db/src/schema/ordering.ts (post-plan-04 column list)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-04-SUMMARY.md (the events-package references plan 04 reported as still open)
  </read_first>
  <action>
Remove `brandId` from the ordering aggregate, ports, events, services, repository and operator DTO.

`packages/events/src/contracts/ordering.ts`: delete the `brandId: z.string().uuid()` field. If the subject
string contains `brand`, rename it to a tenant equivalent. Do NOT bump to `.v2` — D-12 establishes there is
no production deployment and no live consumer, so a compatibility version would be dead on arrival (D-39).
The contract and its emitter change in the same commit so the two never disagree.

`order-drizzle.repository.ts`: remove `brandId` from every insert payload and read projection. Do not add a
manual tenant predicate — `ScopedTx` applies it. Keep every location predicate; the order feed is strictly
single-location per the founder's 2026-08-18 decision and Phase 10 shipped that behaviour.

`create-order.service.ts`: the server is the price authority. Verify while editing that removing `brandId`
does not change which menu snapshot is priced against — `catalog-menu-pricing.adapter.ts` must still resolve
the published menu for the same restaurant. State in the SUMMARY which lookup key it uses after the sweep.

`operator-orders.dto.ts`: remove brand fields from the operator wire shape; keep location fields.

Co-located specs (`order.aggregate.spec.ts`, `orders.controller.spec.ts`): update them to the new shape, and
where an assertion only checked that a mock was called with a `brandId`, replace it with an assertion on an
observable side-effect (the persisted row, the emitted envelope payload, the HTTP response body) rather than
deleting the case. Project memory: mock-only call-shape assertions are exactly what let six Phase 8 runtime
bugs past a green suite.
</action>
<verify>
<automated>(cd packages/events && npx tsc -p tsconfig.json --noEmit) && (cd apps/api && npx vitest run src/contexts/ordering 2>&1 | tail -30)</automated>
</verify>
<acceptance_criteria> - `rg -in "brand" apps/api/src/contexts/ordering/ packages/events/src/contracts/ordering.ts` returns matches ONLY for `@BrandNeutral()`/`@RequireBrand()` decorators in `interfaces/http/` (plan 11's scope); list them in the SUMMARY. - `rg -n "\.v2" packages/events/src/contracts/ordering.ts` returns no matches. - `rg -c "buildEnvelope" apps/api/src/contexts/ordering` is unchanged or higher (record both numbers) and `rg -n "randomUUID\(\)" apps/api/src/contexts/ordering` shows no use as a `correlationId` (ADR-0020 I-4). - `rg -n "locationId" apps/api/src/contexts/ordering/application/list-orders.service.ts` still matches (single-location feed preserved). - `cd apps/api && npx vitest run src/contexts/ordering` exits 0. - `cd packages/events && npx tsc -p tsconfig.json --noEmit` exits 0.
</acceptance_criteria>
<done>An order is scoped by tenant and location, and its published event carries no brand identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint payments at the merged tenant and pass the derived currency</name>
  <files>apps/api/src/contexts/payments/domain/ports.ts, apps/api/src/contexts/payments/application/create-checkout-payment.service.ts, apps/api/src/contexts/payments/application/create-checkout-payment.service.spec.ts, apps/api/src/contexts/payments/application/cancel-order.service.spec.ts, apps/api/src/contexts/payments/application/handle-stripe-event.service.spec.ts, apps/api/src/contexts/payments/application/refund-order.service.spec.ts, apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts, apps/api/src/contexts/payments/infrastructure/stub/stub-provider.adapter.spec.ts</files>
  <read_first>
    - apps/api/src/contexts/payments/domain/ports.ts (`PAYMENT_PROVIDER_PORT` / `STRIPE_CONNECT_PORT` signatures carrying `brandId`)
    - apps/api/src/contexts/payments/application/create-checkout-payment.service.ts (how it resolves the connected account — today via the brand's `stripe_account_id`; after the merge that column is on `tenants`)
    - apps/api/src/contexts/payments/infrastructure/stripe/stripe-provider.adapter.ts (already accepts optional `country` and `default_currency` that nothing supplies — plan 07 wired account CREATION; this task wires the CHECKOUT path)
    - apps/api/src/contexts/payments/application/handle-stripe-event.service.spec.ts (the `account.updated` webhook that keeps `stripe_charges_enabled` / `stripe_payouts_enabled` / `stripe_onboarding_status` / `stripe_requirements_due` current — PAY-05)
    - packages/db/src/schema/tenants.ts (post-plan-03: the four `stripe_*` columns and `default_currency` now live here)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-06-SUMMARY.md (the renamed tenancy payment events these services emit)
    - CLAUDE.md and project memory on Phase 8: server is the price authority; mock-only tests let six runtime bugs ship
  </read_first>
  <action>
Remove `brandId` from the payments ports, services and adapters, and repoint every Stripe connected-account
lookup at the merged `tenants` row.

`create-checkout-payment.service.ts`: the connected account, the payment provider and the account type now
all come from the tenant. Pass the tenant's `defaultCurrency` (derived from country per D-35 at
provisioning) as the checkout currency instead of whatever the previous brand-level or hardcoded source was.
Record in the SUMMARY exactly where the currency came from before and where it comes from now — a silent
currency-source change on the money path is the highest-consequence thing in this plan.

`handle-stripe-event.service.*`: the `account.updated` webhook now updates the four `stripe_*` columns on
`tenants`. This is the live path RESEARCH Pitfall 8 identified; it must keep working.

Update the emitted event names to the tenancy contracts plan 06 renamed
(`tenancy.tenant_payment_account_linked.v1`, `tenancy.tenant_payment_capabilities_applied.v1`) and drop the
`brandId` field from their payloads. Build envelopes with `buildEnvelope`, never a literal (ADR-0020 I-4).

Co-located specs (`create-checkout-payment.service.spec.ts`, `cancel-order.service.spec.ts`,
`handle-stripe-event.service.spec.ts`, `refund-order.service.spec.ts`,
`stub-provider.adapter.spec.ts`): repair them to the new shape and strengthen rather than delete. For each
spec that only asserted a mock was called with certain arguments, add or convert one assertion to an
observable outcome — the persisted row's `stripe_onboarding_status`, the emitted envelope's payload, the
returned checkout session's currency. Use production-shape fixtures (real column sets, real currency codes),
not `{}` stubs.
</action>
<verify>
<automated>cd apps/api && npx vitest run src/contexts/payments 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `rg -in "brand" apps/api/src/contexts/payments/` returns matches ONLY for `@BrandNeutral()`/`@RequireBrand()` decorators in `interfaces/http/` (plan 11's scope). - `rg -n "defaultCurrency|default_currency" apps/api/src/contexts/payments/application/create-checkout-payment.service.ts` matches, sourced from the tenant. - `rg -n "tenant_payment_account_linked|tenant_payment_capabilities_applied" apps/api/src/contexts/payments/` matches (renamed contracts wired). - `rg -n "stripeChargesEnabled|stripePayoutsEnabled|stripeOnboardingStatus|stripeRequirementsDue" apps/api/src/contexts/payments/` still matches (PAY-05 webhook path intact). - `cd apps/api && npx vitest run src/contexts/payments` exits 0, and at least one spec per money-path file asserts a persisted value or an emitted payload rather than only a mock call. - The SUMMARY states the checkout currency's source before and after.
</acceptance_criteria>
<done>Checkout, refund, cancel and the `account.updated` webhook all operate on the merged tenant with its own currency, proven by assertions on observable outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep notifications</name>
  <files>apps/api/src/contexts/notifications/application/send-guest-notification.service.ts, apps/api/src/contexts/notifications/application/send-guest-notification.service.spec.ts, apps/api/src/contexts/notifications/infrastructure/notification-order-drizzle.repository.ts</files>
  <read_first>
    - apps/api/src/contexts/notifications/application/send-guest-notification.service.ts (which entity supplies the sender identity / display name / locale in the guest email — today likely the brand)
    - apps/api/src/contexts/notifications/infrastructure/notification-order-drizzle.repository.ts (the order+entity join that reads `brand_id`)
    - apps/api/src/contexts/notifications/application/send-guest-notification.service.spec.ts (existing assertions)
    - packages/db/src/schema/tenants.ts (post-plan-03: `display_name`, `locale`, `theme` now on the tenant)
    - packages/domain/src/country.ts (plan 01 — the country->locale mapping if the notification locale is derived rather than stored)
  </read_first>
  <action>
Remove `brandId` from the notifications context. The guest-facing sender identity — restaurant display name,
locale, and any theme-derived branding in the email — now comes from the merged `tenants` row rather than a
brand row. Update `notification-order-drizzle.repository.ts`'s join accordingly.

Guest notification locale (D-37 covers interface AND communication languages): the notification's language
should resolve from the tenant's `locale` column, which provisioning seeds from the signup country's default
locale (plan 06). Do not add a new locale-resolution mechanism; state in the SUMMARY which field the
notification reads.

Repair the co-located spec to the new shape. Where it asserted a mock email adapter was called, add an
assertion on the rendered subject/body containing the tenant's display name — an observable outcome on
production-shaped data, per the Phase 8 lesson.
</action>
<verify>
<automated>cd apps/api && npx vitest run src/contexts/notifications 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `rg -in "brand" apps/api/src/contexts/notifications/` returns no matches. - `rg -n "displayName|locale" apps/api/src/contexts/notifications/application/send-guest-notification.service.ts` matches, sourced from the tenant. - `cd apps/api && npx vitest run src/contexts/notifications` exits 0 with at least one assertion on rendered email content. - The SUMMARY names the field the notification locale reads.
</acceptance_criteria>
<done>Guest notifications carry the restaurant's own name and language, read from the merged entity.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                              |
| ------------------------------------------- | -------------------------------------------------------- |
| Guest checkout request -> price computation | Client-supplied cart; the server is the price authority  |
| API -> Stripe Connect                       | Connected-account id and currency selected from our data |
| Stripe webhook -> `handle-stripe-event`     | Untrusted inbound POST, signature-verified               |
| Domain event -> NATS JetStream              | Payload shape is a published contract                    |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                          |
| ------------ | ---------------------- | -------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-10-01 | Tampering              | Checkout charged in the wrong currency after the currency source changes         | mitigate    | Currency is read from the tenant's `default_currency`, which provisioning derives from the validated country (D-35). The SUMMARY must state the before/after source, and a spec asserts the returned checkout session's currency rather than a mock call |
| T-10.2-10-02 | Elevation of Privilege | Payment created against another restaurant's connected account                   | mitigate    | `stripe_account_id` is read from the tenant row resolved by the request's tenant context, which `ScopedTx` + RLS already fence; no brand-level indirection remains to be confused                                                                        |
| T-10.2-10-03 | Tampering              | Client-supplied price surviving the sweep                                        | mitigate    | `catalog-menu-pricing.adapter.ts` remains the pricing source; the SUMMARY must state its lookup key after the sweep, so a silent switch to a client-supplied value is visible                                                                            |
| T-10.2-10-04 | Repudiation            | Payment-linkage events published on a stale subject or with a dangling `brandId` | mitigate    | Contracts renamed in plan 06 and wired here in the same phase; `buildEnvelope` usage asserted, direct `randomUUID()` correlation ids grepped for (ADR-0020 I-4)                                                                                          |
| T-10.2-10-05 | Information Disclosure | Guest notification rendering another restaurant's name after the join change     | mitigate    | The repository join is rewritten to the tenant and a spec asserts the rendered email contains the expected display name                                                                                                                                  |
| T-10.2-10-SC | Tampering              | npm/pip/cargo installs                                                           | accept      | No packages installed                                                                                                                                                                                                                                    |

</threat_model>

<verification>
- `rg -in "brand" apps/api/src/contexts/ordering/ apps/api/src/contexts/payments/ apps/api/src/contexts/notifications/`
  returns matches only for `@BrandNeutral()`/`@RequireBrand()` decorators (plan 11's scope).
- `rg -in "brand" packages/events/src/` returns 0.
- `cd apps/api && npx vitest run src/contexts/ordering` then `src/contexts/payments` then
  `src/contexts/notifications` — three separate invocations. Never run the whole suite in one process;
  it produces false 429/timeout failures (project memory).
- `cd packages/events && npx tsc -p tsconfig.json --noEmit` exits 0.
</verification>

<success_criteria>

- Ordering, payments and notifications carry no brand identifier.
- The ordering event contract is brand-free with no compatibility version.
- Checkout currency and connected account both come from the merged tenant, and the change of source is
  documented rather than silent.
- Every repaired spec asserts at least one observable outcome, not only a mock call.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-10-SUMMARY.md` when done.
</output>
