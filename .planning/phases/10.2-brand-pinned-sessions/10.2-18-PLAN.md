---
phase: 10.2-brand-pinned-sessions
plan: 18
type: execute
wave: 11
depends_on: ["10.2-13"]
files_modified:
  - tools/scripts/seed/cli.ts
  - tools/scripts/seed/commands/seed-demo.ts
  - tools/scripts/seed/commands/provision-tenant.ts
  - tools/scripts/seed/commands/bootstrap-owner.ts
  - tools/scripts/seed/commands/seed-menu.ts
  - tools/scripts/seed/lib/demo-orders.ts
  - tools/scripts/seed/lib/api-client.ts
  - tools/scripts/seed/lib/operator-http.ts
  - tools/scripts/seed/README.md
  - tools/scripts/erase-tenant/cli.ts
autonomous: false
requirements: []
decisions: [D-13, D-33, D-34, D-35, D-40]

must_haves:
  truths:
    - "D-13: `pnpm resto:seed` produces a working demo environment on the merged model — no seed path creates a tenant+brand pair"
    - "A seeded restaurant is payment-ready enough to exercise guest checkout, closing the gap that parked Phase 10 plan 13"
    - "D-34/D-35: every seeded organization has a country from the supported set and a currency derived from it"
    - "The erase-tenant CLI still covers every PII-bearing column after the merge, including the ones that moved from brands"
  artifacts:
    - path: "tools/scripts/seed/commands/seed-demo.ts"
      provides: "single-tier demo seeding: organizations, locations, catalog, orders"
    - path: "tools/scripts/erase-tenant/cli.ts"
      provides: "erasure covering the merged table's PII columns"
  key_links:
    - from: "tools/scripts/seed/commands/seed-demo.ts"
      to: "apps/api/src/contexts/identity/interfaces/http/signup.controller.ts"
      via: "organization creation through the real provisioning path"
      pattern: "signup|provision"
---

<objective>
Rewrite the seed and erasure tooling onto the merged model so the dev environment cannot keep the old shape
alive, and make a seeded restaurant payment-ready.

Purpose: D-13 is explicit that seeds are rewritten in-phase, because a seed that still creates tenant+brand
pairs silently preserves the model this phase deleted. There is also a standing blocker worth closing here:
Phase 10 plan 13 is parked because `seed-demo` never makes a restaurant payment-ready, so guest checkout
returns `payments.not_enabled`.
Output: `pnpm resto:seed` builds a demo environment on the merged model, and erasure still covers every PII
column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the seed CLI's two-tier structure to one</name>
  <files>tools/scripts/seed/cli.ts, tools/scripts/seed/commands/seed-demo.ts, tools/scripts/seed/commands/provision-tenant.ts, tools/scripts/seed/commands/bootstrap-owner.ts, tools/scripts/seed/commands/seed-menu.ts, tools/scripts/seed/lib/demo-orders.ts, tools/scripts/seed/lib/api-client.ts, tools/scripts/seed/lib/operator-http.ts, tools/scripts/seed/README.md</files>
  <read_first>
    - tools/scripts/seed/commands/seed-demo.ts (the `BRANDS[]` array and the two-tier `BRANDS -> assignments -> locations` structure at ~110-146; `ensureBrand()` at ~140; `ensureLocations(brand: BrandDef)` at ~152; `ensureCatalog(brandSlug)` at ~175; every `op.withHeaders({ 'x-brand-slug': brand.slug })`)
    - tools/scripts/seed/lib/demo-orders.ts (~20 `readonly brandSlug: string`, ~46 `'x-brand-slug': input.brandSlug`)
    - tools/scripts/seed/commands/provision-tenant.ts and bootstrap-owner.ts (the tenant-level commands that survive and absorb the brand-level ones; they use `TenantSlug`)
    - tools/scripts/seed/lib/api-client.ts and operator-http.ts (the header plumbing that carries `x-brand-slug` today)
    - tools/scripts/seed/cli.ts (the command registry — brand-named subcommands are renamed or dropped here)
    - apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts (post-plan-07: the internal provisioning route the seed calls, and its new input shape including `country`)
    - packages/domain/src/country.ts (plan 01: valid country values for seeded organizations)
    - tools/scripts/seed/README.md (documented commands and flags that must match reality afterwards)
  </read_first>
  <action>
Collapse `seed-demo.ts`'s two-tier structure. Today each `BRANDS[]` entry becomes a brand under a tenant,
with locations and catalog hung off the brand and every operator call carrying an `x-brand-slug` header.
After the merge, each entry IS an organization: one provisioning call creates it, and its locations, catalog
and orders attach directly.

Concretely:
- rename `BRANDS[]` and `BrandDef` to organization-flavoured names (D-40);
- give each entry a `country` from the supported set — vary them (one `UA`, one `GB`, one `ES`) so the demo
  environment exercises all three currency derivations and both non-Russian locales rather than a single
  happy path;
- do NOT pass a currency: it is derived from country (D-35). If the seed currently sets one, delete it;
- delete `ensureBrand()`; fold its work into the organization-provisioning call;
- change `ensureLocations` and `ensureCatalog` to take the organization instead of a brand;
- delete every `'x-brand-slug'` header from `seed-demo.ts`, `demo-orders.ts`, `api-client.ts` and
  `operator-http.ts` — the API stopped reading it in plan 07;
- repoint every endpoint whose path changed, using the route tables recorded in the plan 07 and 08 summaries.

`cli.ts`: rename or remove brand-named subcommands and update `README.md` so the documented commands match.
A README describing a `seed brand` command that no longer exists is the kind of stale instruction that is
worse than none.

Watch the unique-index change from plan 04: menu category and item slugs are now unique per TENANT, where
they were previously unique per (tenant, brand). If the fixture data reuses the same category slugs across
what used to be sibling brands of one tenant, that is now a conflict — since each entry is its own
organization, the collision should not arise, but verify by running the seed rather than by reasoning.
  </action>
  <verify>
    <automated>pnpm --filter @resto/db db:reset && pnpm --filter @resto/db db:migrate && pnpm resto:seed demo 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `rg -in "brand" tools/scripts/seed/` returns no matches.
    - `rg -c "x-brand-slug" tools/` returns 0.
    - `pnpm resto:seed demo` exits 0 against a freshly reset and migrated database.
    - After seeding, a SQL probe shows at least three `tenants` rows with distinct `country` values covering `UA`, `GB` and `ES`, and each row's `default_currency` matches the registry (`UAH`, `GBP`, `EUR`) — paste the query output into the SUMMARY.
    - After seeding, `SELECT count(*) FROM information_schema.tables WHERE table_name IN ('brands','brand_domains','member_brand_scope')` returns 0.
    - `tools/scripts/seed/README.md` lists only commands that exist in `cli.ts`.
  </acceptance_criteria>
  <done>One seed command builds organizations directly, in three markets, with no brand tier anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Keep erasure complete across the merged columns</name>
  <files>tools/scripts/erase-tenant/cli.ts</files>
  <read_first>
    - tools/scripts/erase-tenant/cli.ts (the erasure pipeline: which tables and columns it anonymizes, how it uses `AUDIT_ERASURE_SALT`, and the audit row it writes)
    - packages/db/src/schema/tenants.ts (post-plan-03: `legal_name`, `legal_form`, `tax_id`, `stripe_account_id` now live here — these came from `brands` and are PII or PII-adjacent)
    - packages/db/src/schema/locations.ts (post-plan-03: `contacts` jsonb and `address`)
    - packages/db/test/integration/erase-includes-brands.spec.ts (the existing erasure-completeness assertion for the brands table — plan 19 rewrites the spec, but this task must make the CLI correct first)
    - CLAUDE.md §Constraints (GDPR: full erasure pipeline, 30-day cool-off, anonymization via `AUDIT_ERASURE_SALT`, audit log of all PII touches; hard deletes forbidden)
  </read_first>
  <action>
Update `tools/scripts/erase-tenant/cli.ts` so erasure still covers every PII-bearing column after the merge.

The columns that used to live on `brands` — `legal_name`, `legal_form`, `tax_id`, `stripe_account_id` — now
live on `tenants`. If the erasure pipeline enumerated tables explicitly, `brands` is gone from that list and
its columns must appear under `tenants` instead, or a real GDPR erasure would silently leave a legal name and
a tax id behind. Remove the `brands` / `brand_domains` / `member_brand_scope` entries and confirm every
column they carried is now covered on its new home table.

Keep the mechanism unchanged: anonymize via `AUDIT_ERASURE_SALT` hashing, write the audit row with the
hashed identifier, preserve aggregate stats, and never hard-delete (the `resto_app` role has no DELETE
privilege and that must not change).

If the pipeline discovers tables dynamically rather than from a list, say so in the SUMMARY and verify the
discovery still reaches the merged columns rather than assuming it does.
  </action>
  <verify>
    <automated>pnpm resto:erase-tenant --help 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `rg -in "brand" tools/scripts/erase-tenant/` returns no matches.
    - `rg -n "legal_name|legalName" tools/scripts/erase-tenant/cli.ts` and `rg -n "tax_id|taxId" tools/scripts/erase-tenant/cli.ts` both match under the tenants coverage.
    - `rg -n "AUDIT_ERASURE_SALT" tools/scripts/erase-tenant/cli.ts` still matches.
    - `rg -in "DELETE FROM" tools/scripts/erase-tenant/cli.ts` returns no matches (soft anonymization only).
    - The SUMMARY lists every PII column the pipeline covers on `tenants` and `locations` after the change.
  </acceptance_criteria>
  <done>Erasing a restaurant still removes its legal name, tax id and Stripe account reference, now from the table they moved to.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Make a seeded restaurant payment-ready</name>
  <files>tools/scripts/seed/commands/seed-demo.ts</files>
  <read_first>
    - .planning/phases/10-admin-order-intake/10-13-CHECKPOINT.md (the parked Phase 10 checkpoint and exactly what it was blocked on)
    - tools/scripts/seed/commands/seed-demo.ts (post-Task-1)
    - apps/api/src/contexts/payments/application/create-checkout-payment.service.ts (post-plan-10: what it requires before a checkout can be created — which is what `payments.not_enabled` is reporting)
    - packages/db/src/schema/tenants.ts (the four `stripe_*` columns that gate payment readiness)
    - apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts (post-plan-07: the real Connect onboarding path, and whether a seed can shortcut it safely)
  </read_first>
  <what-built>
Phase 10 plan 13 has been parked since 2026-08-18 because `seed-demo` never makes a restaurant
payment-ready, so guest checkout returns `payments.not_enabled` and the founder cannot complete a
two-screen walkthrough that needs a real Stripe-test payment. The seed rewrite is the natural moment to fix
that: the `stripe_*` columns now live on the merged table this plan already writes.

This needs the founder's call because there are two honest options with different fidelity, and picking the
wrong one silently weakens what a demo proves.
  </what-built>
  <action>
Present both options with their tradeoffs and implement the chosen one:

Option A — seed the Stripe columns directly. Set `stripe_account_id` to a Stripe test-mode connected account
id, `stripe_charges_enabled = true`, `stripe_payouts_enabled = true`,
`stripe_onboarding_status = 'complete'` on one seeded organization. Fast and offline; guest checkout stops
returning `payments.not_enabled` immediately. Fidelity cost: nothing exercises the real Connect onboarding
path, so a regression there stays invisible to the demo.

Option B — drive the real Connect onboarding path with Stripe test mode via the Stripe CLI. Higher fidelity
and exercises `start-tenant-onboarding.service.ts` end to end. Cost: the seed needs Stripe test credentials
and network access, so it stops being reproducible offline.

Whichever is chosen, gate it behind an explicit flag (for example `--payments-ready`) rather than making it
the default, and make the flag refuse to run outside `NODE_ENV=development`/`test` — the existing
`assertProdGuardrails` family (ADR-0020 I-3) is the precedent for that shape. A seed that can fabricate
payment readiness in a non-dev environment is a real hazard, not a convenience.

Never place a real Stripe secret in the seed source or in a committed file; read it from the environment.
  </action>
  <how-to-verify>
1. Choose Option A or Option B, weighing offline reproducibility against exercising the real onboarding path.
2. After implementation, confirm that seeding with the flag and then completing a guest checkout no longer
   returns `payments.not_enabled`.
  </how-to-verify>
  <resume-signal>Reply "option-a" or "option-b", or describe a different approach.</resume-signal>
  <verify>
    <automated>pnpm resto:seed demo --payments-ready 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm resto:seed demo --payments-ready` exits 0 in development.
    - The same command refuses to run with `NODE_ENV=production`, exiting non-zero with an explicit guardrail message — paste the output into the SUMMARY.
    - After seeding, a SQL probe shows one organization with `stripe_charges_enabled = true` and `stripe_onboarding_status = 'complete'`.
    - A guest checkout request against that organization no longer returns `payments.not_enabled` — paste the response into the SUMMARY.
    - `rg -n "sk_test|sk_live|rk_" tools/scripts/seed/` returns no matches (no secret in source).
  </acceptance_criteria>
  <done>A seeded restaurant can take a test payment, which unblocks the walkthrough Phase 10 has been parked on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed CLI -> API internal routes | Uses `INTERNAL_API_TOKEN`; a server-only credential |
| Seed CLI -> database | Can fabricate state that bypasses application invariants |
| Erasure CLI -> PII columns | The GDPR control; incompleteness is a compliance failure, not a bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.2-18-01 | Information Disclosure | Erasure silently missing `legal_name` / `tax_id` / `stripe_account_id` after they moved to `tenants` | mitigate | Task 2 re-points coverage at the new home table and lists every covered column in the SUMMARY; plan 19 rewrites `erase-includes-brands.spec.ts` into the merged assertion |
| T-10.2-18-02 | Elevation of Privilege | A seed flag fabricating payment readiness in a non-development environment | mitigate | The `--payments-ready` flag refuses to run outside development/test, following the `assertProdGuardrails` precedent (ADR-0020 I-3); asserted by a live probe with `NODE_ENV=production` |
| T-10.2-18-03 | Information Disclosure | A Stripe secret committed in seed source | mitigate | Secrets read from the environment only; grep asserts no `sk_test`/`sk_live`/`rk_` literal in `tools/scripts/seed/` |
| T-10.2-18-04 | Tampering | `INTERNAL_API_TOKEN` leaking into a client bundle while repointing seed endpoints | mitigate | The token stays confined to `tools/` and server code; CLAUDE.md forbids it in any client component or `NEXT_PUBLIC_*`/`VITE_*` var — verify no `VITE_` var is introduced by this plan |
| T-10.2-18-05 | Tampering | Seed writing rows that violate the new per-tenant slug uniqueness, leaving a half-seeded database | mitigate | The acceptance criterion runs the seed against a freshly reset database and requires a clean exit, so a conflict fails loudly rather than partially |
| T-10.2-18-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `pnpm --filter @resto/db db:reset && pnpm --filter @resto/db db:migrate && pnpm resto:seed demo` exits 0.
- `rg -in "brand" tools/` returns 0.
- The three-country SQL probe output is pasted into the SUMMARY.
- The `--payments-ready` guardrail probe output is pasted into the SUMMARY.
- A guest checkout against the seeded payment-ready organization does not return `payments.not_enabled`.
</verification>

<success_criteria>
- Seeds build organizations directly, in three markets, with derived currencies.
- No seed or erasure path references a brand.
- Erasure still covers every PII column, on its new home table.
- A seeded restaurant can take a test payment behind a development-only flag.
</success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-18-SUMMARY.md` when done.
</output>
