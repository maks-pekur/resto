---
phase: 10.2-brand-pinned-sessions
plan: 20
type: execute
wave: 13
depends_on: ["10.2-19"]
files_modified:
  - apps/admin/app/
  - apps/api/src/config/env.schema.ts
  - .env.example
  - docs/api/openapi.yaml
  - apps/website/next-env.d.ts
autonomous: false
requirements: []
decisions: [D-05, D-11, D-39, D-40]

must_haves:
  truths:
    - "D-05: the whole merge landed inside phase 10.2 — no brand-shaped code, schema, test, seed or document is left for a follow-up phase"
    - "D-11: a repo-wide grep for `brandId`, `brandSlug`, `brand_id` and `brand_slug` returns zero matches outside historical migration SQL"
    - "D-40: the word `brand` survives only inside Better Auth's own vocabulary boundary and in migration history, both of which are documented exceptions"
    - "D-39: the dead code the founder named — the leftover Next.js admin directory, dead env vars, superseded docs — is deleted, not left dormant"
    - "The whole monorepo typechecks, builds, lints and passes its tests, and the committed OpenAPI artifact matches the live decorators"
  artifacts:
    - path: "docs/api/openapi.yaml"
      provides: "regenerated API contract matching the merged routes"
  key_links:
    - from: "docs/api/openapi.yaml"
      to: "apps/api/src/contexts"
      via: "openapi:check drift gate"
      pattern: "openapi"
---

<objective>
Close the phase: delete the last named dead code, prove the rename is complete with repo-wide greps,
regenerate the API contract, and run every gate green.

Purpose: D-05 locked the whole merge inside this phase. This plan is where that claim is proven rather than
asserted — a residual `brandSlug` in a file no earlier plan owned would mean the merge leaked into the next
phase.
Output: zero brand identifiers outside documented exceptions, and a fully green monorepo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-19-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-18-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the last named dead code and dead configuration</name>
  <files>apps/admin/app/, apps/api/src/config/env.schema.ts, .env.example, apps/website/next-env.d.ts</files>
  <read_first>
    - apps/admin/app/ (the leftover Next.js directory sitting beside the real `src/` tree — D-39 names it explicitly; `git status` shows it as untracked, so confirm nothing in it is referenced before removing)
    - apps/admin/components.json (UI-SPEC notes it still reads `"rsc": true` and points at `app/globals.css`, both pre-Vite leftovers — decide whether removing `apps/admin/app/` makes that path dangling)
    - apps/api/src/config/env.schema.ts (the vars plan 13 flagged as possibly dead after closing public signup, and the renamed `RATE_LIMIT_TENANT_SLUG_CHECK_PER_MIN` from plan 08)
    - .env.example (must match the schema exactly after the changes)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md (which env vars plan 13 reported as newly dead)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-14-SUMMARY.md and 10.2-16-SUMMARY.md (whether `friendlyCatalogError` and the auto-save model were actually deleted or blocked)
  </read_first>
  <action>
Delete `apps/admin/app/` — the leftover Next.js directory D-39 names. Before deleting, run
`rg -l "admin/app/" apps/admin` and check `apps/admin/components.json`'s `tailwind.css` path: if it still
points at `app/globals.css`, repoint it at the real entry `apps/admin/src/styles.css` in the same commit, so
the next `npx shadcn add` does not fail on a dangling path. UI-SPEC flags this staleness as pre-existing;
removing the directory is what makes it worth fixing now.

Reconcile the env surface with reality:
- confirm plans 08 and 12 renamed `RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN` and added
  `ADMIN_WEB_ORIGIN_WILDCARD` / `VITE_ADMIN_HOST_SUFFIX` in both `env.schema.ts` and `.env.example`;
- remove any env var plan 13 reported as dead after closing public Better Auth signup, but only after
  grepping for readers — a var with a live reader is not dead;
- confirm `AUTH_COOKIE_DOMAIN`'s documented value in `.env.example` is `.admin.resto.app` and not the apex.

Read the two earlier summaries for `friendlyCatalogError` (plan 14) and the auto-save model (plan 16). If
either was blocked by a live consumer, that is a real finding — record it here as the phase's one accepted
D-39 exception with its reason, rather than forcing the deletion now.

`apps/website/next-env.d.ts` shows as modified in the working tree and is a generated file — restore or
regenerate it rather than committing incidental churn.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d apps/admin/app` succeeds.
    - `rg -n "app/globals.css" apps/admin/components.json` returns no matches, or the SUMMARY explains why it was left.
    - Every key in `.env.example` exists in `apps/api/src/config/env.schema.ts` and vice versa for required vars — paste the comparison into the SUMMARY.
    - `rg -n "AUTH_COOKIE_DOMAIN=" .env.example` shows `.admin.` and not a bare apex value.
    - `rg -c "RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN" .` returns 0.
    - `pnpm typecheck` and `pnpm build` both exit 0.
  </acceptance_criteria>
  <done>The leftover Next.js directory is gone, the env surface matches the code, and any D-39 exception is recorded with a reason.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the rename is complete and regenerate the API contract</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - docs/api/openapi.yaml (the committed contract artifact)
    - tools/openapi-check.ts (what `pnpm openapi:check` compares and how the artifact is regenerated)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md, 10.2-08-SUMMARY.md, 10.2-09-SUMMARY.md, 10.2-13-SUMMARY.md (the recorded old->new route tables — every one of these must be reflected in the regenerated artifact)
    - packages/api-client/src/generated/api.ts (generated from the artifact by `openapi-typescript` — regenerating the spec means regenerating this too)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md D-40 (the one documented exception: Better Auth's own `organization` vocabulary stays)
  </read_first>
  <action>
Run the repo-wide completeness greps and record every result verbatim in the SUMMARY. These are the proof of
D-05 and D-11, so a non-zero count needs an explanation, not a quiet fix:

- `rg -c "brandId|brandSlug|brand_id|brand_slug" apps packages tools` — expected 0
- `rg -li "brand" apps packages tools` — expected empty; any hit must be justified in the SUMMARY
- `find apps packages tools -iname "*brand*"` — expected empty
- `rg -c "brand" packages/db/migrations/` — expected NON-zero and correct: historical migrations are
  immutable history and are never rewritten. Say so explicitly so the count is not mistaken for a miss.
- `rg -c "activeOrganizationId|setActiveOrganization|organizationHooks|organization_role" apps packages` —
  expected non-zero: this is Better Auth's namespace, the one boundary D-40 keeps as-is.

Regenerate `docs/api/openapi.yaml` from the live decorators and re-run `openapi-typescript` for
`packages/api-client/src/generated/api.ts`. Then run `pnpm openapi:check`. Two failure modes to watch:
1. A route rename an earlier plan made but did not record — the diff will show a path that no summary
   mentions. Trace it to its plan and record it.
2. A `type: unknown` on any slug or country field — that is a branded Zod type leaking into an HTTP DTO
   (ADR-0020 I-7). Fix it by switching the DTO to the unbranded `TenantSlugValue` / `CountryCodeValue`, not
   by editing the YAML.

Grep the regenerated artifact for `brand`: the API contract is the most visible surface of the rename, and a
residual `brand` in a path or a schema name means a decorator was missed.
  </action>
  <verify>
    <automated>pnpm openapi:check && test "$(rg -ci brand docs/api/openapi.yaml | tail -1)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "brandId|brandSlug|brand_id|brand_slug" apps packages tools` returns 0.
    - `rg -li "brand" apps packages tools` returns empty, or every hit is justified in the SUMMARY.
    - `find apps packages tools -iname "*brand*"` returns empty.
    - `rg -ci "brand" docs/api/openapi.yaml` returns 0.
    - `rg -n "unknown" docs/api/openapi.yaml` shows no slug or country field typed as `unknown`.
    - `pnpm openapi:check` exits 0.
    - The SUMMARY contains all five grep commands with their verbatim output, including the deliberately non-zero migration and Better Auth counts with their justification.
  </acceptance_criteria>
  <done>The rename is provably complete, with the two intentional exceptions named, and the published contract matches the code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Full green gate and phase sign-off</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md (all 40 decisions — the coverage audit below is against this list)
    - Every `10.2-*-SUMMARY.md` produced by plans 01-19 (the evidence base for the audit)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md (the reconciliation decision on D-25/D-30 and the invitation-path decision — both need the founder's confirmation to be settled)
    - CLAUDE.md and project memory (running the whole apps/api suite in one process gives false 429/timeout failures — batch the e2e run)
  </read_first>
  <what-built>
Nineteen plans have landed: the schema merge and its migration, the RLS teardown, the API sweep across five
bounded contexts, the session revoke-and-reissue endpoint, the wildcard host handling, signup and onboarding,
the admin route-tree collapse and auth surfaces, the seeds, and the rebuilt test net.

What remains is the gate: every check green in one pass, and a decision-coverage audit confirming all 40
locked decisions are actually implemented rather than merely planned. Two decisions were resolved by the
executor during the phase and need the founder's explicit sign-off: the D-25/D-30 reconciliation (signup
creates a `pending_setup` organization that onboarding names) and the D-29 invitation-path call.
  </what-built>
  <action>
Run the full gate, in this order, and paste each result into the SUMMARY:
1. `pnpm typecheck`
2. `pnpm lint`
3. `pnpm build`
4. `pnpm test`
5. `pnpm --filter @resto/db db:audit-fks`
6. `pnpm openapi:check`
7. `cd apps/api && npx vitest run test/e2e` in SMALL BATCHES by file glob — never the whole directory in one
   process. Record which batches were run and their results.
8. `pnpm --filter admin e2e`

Then produce the decision-coverage audit: a 40-row table, one per D-01..D-40, each with the plan(s) that
implemented it and a one-line piece of evidence (a file path, a grep result, a test name). Any row without
evidence is a gap and must be named as such, not smoothed over.

Finally, restate for the founder the two executor-resolved decisions:
- the D-25/D-30/D-31 reconciliation implemented in plan 13 (signup creates the organization in
  `pending_setup`; onboarding names it and activates it; resume is derived from `status`);
- the D-29 invitation-path decision recorded in plan 13's checkpoint.
  </action>
  <how-to-verify>
1. Confirm all eight gate commands passed.
2. Read the 40-row decision-coverage table and confirm no row lacks evidence.
3. Confirm you are content with the D-25/D-30 reconciliation as implemented, and with the D-29 invitation
   decision.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or name the decision rows and gate failures to address.</resume-signal>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build && pnpm test && pnpm openapi:check && pnpm --filter @resto/db db:audit-fks</automated>
  </verify>
  <acceptance_criteria>
    - All eight gate commands exit 0; their output is pasted into the SUMMARY.
    - The SUMMARY contains a 40-row decision-coverage table with a plan reference and concrete evidence per row.
    - The SUMMARY names any decision without evidence explicitly as a gap.
    - The founder's response to the D-25/D-30 reconciliation and the D-29 invitation decision is recorded verbatim.
    - `rg -c "brandId|brandSlug|brand_id|brand_slug" apps packages tools` returns 0 at the moment the gate is run.
  </acceptance_criteria>
  <done>Every gate is green, all 40 decisions have evidence, and the two executor-resolved decisions are confirmed by the founder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed OpenAPI artifact -> generated client | Consumers type against the artifact; drift is a silent contract break |
| `.env.example` -> operator deployment | Documented values become production configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.2-20-01 | Information Disclosure | `.env.example` documenting `AUTH_COOKIE_DOMAIN` at the apex, sending operator cookies to guest and public-site hosts | mitigate | Acceptance criterion asserts the documented value carries the `.admin.` label; the schema's leading-dot refine alone cannot catch a wrong-but-well-formed value |
| T-10.2-20-02 | Tampering | Contract drift between the committed OpenAPI artifact and the live decorators | mitigate | `pnpm openapi:check` is a blocking gate command, and the artifact is regenerated rather than hand-edited |
| T-10.2-20-03 | Information Disclosure | A branded Zod type leaking into an HTTP DTO and publishing `type: unknown` for a slug or country | mitigate | Acceptance criterion greps the regenerated artifact for `unknown` on those fields; the fix is at the DTO, never in the YAML |
| T-10.2-20-04 | Repudiation | Declaring the phase complete while a decision is unimplemented | mitigate | The 40-row coverage audit requires concrete evidence per row and forbids smoothing over a missing one; the checkpoint blocks on the founder reading it |
| T-10.2-20-05 | Denial of Service | Removing an env var that still has a live reader, breaking boot | mitigate | Task 1 requires grepping for readers before removing any var, and `pnpm build` plus the API boot in the gate would surface a missing required var |
| T-10.2-20-SC | Tampering | npm/pip/cargo installs | accept | No packages installed across the entire phase — RESEARCH.md's Package Legitimacy Audit records zero new dependencies, so no `[ASSUMED]`/`[SUS]` package checkpoint was ever required |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm test`, `pnpm openapi:check` and
  `pnpm --filter @resto/db db:audit-fks` all exit 0.
- `cd apps/api && npx vitest run test/e2e` in small batches — all green; batches recorded.
- `pnpm --filter admin e2e` exits 0.
- `rg -c "brandId|brandSlug|brand_id|brand_slug" apps packages tools` returns 0.
- `rg -ci "brand" docs/api/openapi.yaml` returns 0.
- The 40-row decision-coverage audit is complete with evidence.
</verification>

<success_criteria>
- The monorepo typechecks, lints, builds and passes every suite.
- The brand vocabulary survives only in immutable migration history and inside Better Auth's own namespace.
- All 40 locked decisions have implementation evidence.
- The founder has confirmed the two decisions the executor resolved mid-phase.
</success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-20-SUMMARY.md` when done.
</output>
