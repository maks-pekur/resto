---
phase: 10.2-brand-pinned-sessions
plan: 11
type: execute
wave: 6
depends_on: ['10.2-07', '10.2-08', '10.2-09', '10.2-10']
files_modified:
  - apps/api/src/contexts/identity/domain/ports.ts
  - apps/api/src/contexts/identity/infrastructure/email/mailhog-smtp.adapter.ts
  - apps/api/src/contexts/identity/infrastructure/email/resend.adapter.ts
  - apps/api/src/contexts/notifications/application/send-guest-notification.service.ts
  - apps/api/src/contexts/notifications/infrastructure/guest-email-templates.ts
  - apps/api/src/shared/auth/require-brand.decorator.ts
  - apps/api/src/shared/auth/brand-neutral.decorator.ts
  - apps/api/src/shared/auth/index.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts
  - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/internal-bootstrap.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/me.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/member-location-roles.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/member-roles.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/roles.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/set-active-location.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/signup.controller.ts
  - apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts
  - apps/api/src/contexts/ordering/interfaces/http/operator-orders.controller.ts
  - apps/api/src/contexts/payments/interfaces/http/checkout.controller.ts
  - apps/api/src/contexts/payments/interfaces/http/stripe-webhook.controller.ts
  - apps/api/src/contexts/payments/interfaces/http/order-cancel.controller.ts
  - apps/api/src/contexts/payments/interfaces/http/refunds.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts
  - apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts
autonomous: false
requirements: []
decisions: [D-10, D-19, D-39, D-40]

must_haves:
  truths:
    - 'D-10: `@RequireBrand()` and `REQUIRE_BRAND_KEY` no longer exist — they were read by no guard and enforced nothing'
    - 'D-39: `@BrandNeutral()` and `BRAND_NEUTRAL_KEY` no longer exist; no inert-but-live dead code survives'
    - 'D-19: every route that was reachable without a location context before this plan is still reachable after it — the guard-bypass semantics are preserved by an explicit marker, not by an accident of a deleted decorator'
    - '`LocationScopeGuard` reads exactly one neutrality marker'
  artifacts:
    - path: 'apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts'
      provides: 'location scoping gated on LOCATION_NEUTRAL_KEY alone'
    - path: 'apps/api/src/shared/auth/index.ts'
      provides: 'auth decorator barrel with no brand exports'
  key_links:
    - from: 'apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts'
      to: 'apps/api/src/shared/auth/index.ts'
      via: 'LOCATION_NEUTRAL_KEY import (BRAND_NEUTRAL_KEY import removed)'
      pattern: 'LOCATION_NEUTRAL_KEY'
---

<objective>
Delete the two brand decorators and repair the guard-bypass semantics that one of them silently carried.

Purpose: `@RequireBrand()` is verified dead — no guard reads `REQUIRE_BRAND_KEY` — so it is a pure deletion
across 33 usages in 6 files. `@BrandNeutral()` is the opposite: `LocationScopeGuard` reads
`BRAND_NEUTRAL_KEY` as a second bypass marker, so 13 controllers currently escape location scoping through
it alone. Deleting it naively would make those routes start throwing `location.context_required` at runtime
— a regression `pnpm typecheck` cannot see.
Output: one neutrality marker, zero brand decorators, and every previously-bypassing route still bypassing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-07-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-08-SUMMARY.md

<interfaces>
Measured decorator inventory (grep-verified 2026-08-19, before plans 07/08 renames):

@RequireBrand() — 33 usages in 6 files, read by ZERO guards:
catalog/interfaces/http/catalog.controller.ts (19)
ordering/interfaces/http/operator-orders.controller.ts (4)
payments/interfaces/http/order-cancel.controller.ts (2)
payments/interfaces/http/refunds.controller.ts (1)
tenancy/interfaces/http/brand-onboarding.controller.ts -> tenant-onboarding.controller.ts (4)
tenancy/interfaces/http/locations.controller.ts (3)

@BrandNeutral() — 1 usage in each of 17 files, READ by LocationScopeGuard as a bypass marker.
Files that ALSO carry @LocationNeutral() (bypass survives without action) — 4:
catalog/public-menu.controller.ts, identity/me-brands.controller.ts -> me-tenants.controller.ts,
tenancy/internal-tenants.controller.ts, tenancy/locations.controller.ts
Files that carry @BrandNeutral() ALONE (must gain @LocationNeutral()) — 12, after
identity/set-active-brand.controller.ts is deleted by plan 08:
health/health.controller.ts, identity/internal-bootstrap.controller.ts, identity/me.controller.ts,
identity/member-location-roles.controller.ts, identity/member-roles.controller.ts,
identity/roles.controller.ts, identity/set-active-location.controller.ts,
identity/signup.controller.ts, ordering/orders.controller.ts, payments/checkout.controller.ts,
payments/stripe-webhook.controller.ts, tenancy/tenants.controller.ts,
tenancy/brand-onboarding.controller.ts -> tenant-onboarding.controller.ts

Re-run the grep before editing — plans 07-10 renamed three of these files and deleted one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Rename the last brand-named fields on the guest-notification port</name>
  <files>apps/api/src/contexts/identity/domain/ports.ts, apps/api/src/contexts/identity/infrastructure/email/mailhog-smtp.adapter.ts, apps/api/src/contexts/identity/infrastructure/email/resend.adapter.ts, apps/api/src/contexts/notifications/application/send-guest-notification.service.ts, apps/api/src/contexts/notifications/infrastructure/guest-email-templates.ts</files>
  <read_first>
    - .planning/phases/10.2-brand-pinned-sessions/10.2-FINDINGS.md F-22 (why this landed here)
    - apps/api/src/contexts/identity/domain/ports.ts lines 50-60 (`SendGuestNotificationInput`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-10-SUMMARY.md (plan 10 made the data behind these fields tenant-scoped; only the names are stale)
  </read_first>
  <action>
**Added to this plan on 2026-08-20 by the orchestrator.** `SendGuestNotificationInput.brandTheme`
and `.brandName` are the last `brand`-named identifiers outside migration history. `grep -l` returns
no owner for `identity/domain/ports.ts` anywhere in this phase, and plan 08 did not absorb it while
rewriting the identity context. Plan 10 proposed plan 21, but plan 21 is the `organization` → `tenant`
sweep, and **plan 20 asserts `brand` is already at zero before plan 21 runs** — so leaving it there
would fail plan 20's own completeness proof.

Purely a rename; plan 10 already made the data tenant-scoped:

- `brandTheme: GuestBrandTheme | null` → `tenantTheme: GuestTenantTheme | null`
- `brandName: string` → `tenantName: string`
- rename the `GuestBrandTheme` type itself to `GuestTenantTheme`
- update the five consumers: both email adapters, `send-guest-notification.service.ts`,
  `guest-email-templates.ts`, and their two spec files

Guest-facing email **copy** is not in scope — if a template renders the words "brand" or "бренд" to a
guest, that is product wording, not an identifier. Leave it and note it in the SUMMARY.
</action>
<verify>
<automated>rg -c "brandTheme|brandName|GuestBrandTheme" apps/api/src packages tools</automated>
</verify>
<acceptance_criteria> - The `<automated>` count is 0. - `npx tsc -p apps/api/tsconfig.json --noEmit 2>&1 | grep -c "contexts/notifications"` is 0. - `pnpm --filter api exec vitest run src/contexts/notifications` passes with the same test count as before the rename — record both numbers in the SUMMARY. - Any guest-facing email copy left containing the word "brand" is named in the SUMMARY as deliberate.
</acceptance_criteria>
<done>No identifier named `brand` survives outside migration history.</done>
</task>

<task type="auto">
  <name>Task 1: Preserve the location-bypass semantics that @BrandNeutral silently carried</name>
  <files>apps/api/src/health/health.controller.ts, apps/api/src/contexts/identity/interfaces/http/internal-bootstrap.controller.ts, apps/api/src/contexts/identity/interfaces/http/me.controller.ts, apps/api/src/contexts/identity/interfaces/http/member-location-roles.controller.ts, apps/api/src/contexts/identity/interfaces/http/member-roles.controller.ts, apps/api/src/contexts/identity/interfaces/http/roles.controller.ts, apps/api/src/contexts/identity/interfaces/http/set-active-location.controller.ts, apps/api/src/contexts/identity/interfaces/http/signup.controller.ts, apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts, apps/api/src/contexts/payments/interfaces/http/checkout.controller.ts, apps/api/src/contexts/payments/interfaces/http/stripe-webhook.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts (lines 27-37: `LOCATION_NEUTRAL_KEY` checked first, then `BRAND_NEUTRAL_KEY` — BOTH return `true` early; then `getLocationId()` absence throws `location.context_required`)
    - apps/api/src/shared/auth/brand-neutral.decorator.ts and the location-neutral decorator beside it (the `SetMetadata` shape and the exact decorator names/placement)
    - apps/api/src/shared/auth/index.ts (the barrel that re-exports both keys and both decorators)
    - Each of the 13 controllers listed in `<files>` — read the decorator placement (class-level vs handler-level) before editing; a class-level `@BrandNeutral()` must be replaced by a class-level `@LocationNeutral()`, not a handler-level one
    - apps/api/src/contexts/payments/interfaces/http/stripe-webhook.controller.ts specifically — it is an unauthenticated inbound webhook and MUST keep bypassing location scoping
    - apps/api/src/health/health.controller.ts — liveness/readiness probes must never require a location context
  </read_first>
  <action>
Before touching anything, re-run the inventory against the current tree, because plans 07-10 renamed three
files and deleted one:
`rg -l "@BrandNeutral\(\)" apps/api/src` and `rg -l "@LocationNeutral\(\)" apps/api/src`, then compute the
set difference. Record both lists and the difference in the SUMMARY.

For every file in the difference (carries `@BrandNeutral()` but not `@LocationNeutral()`), add
`@LocationNeutral()` at the SAME scope and position the `@BrandNeutral()` occupies — class-level stays
class-level. Do not remove `@BrandNeutral()` yet; Task 2 does that, so this task's diff is purely additive
and its effect is provably a no-op on routing behaviour.

Why this is not optional: `LocationScopeGuard.canActivate` returns `true` early on EITHER marker. Thirteen
controllers — including the Stripe webhook, the health probes, `POST /v1/signup` and the roles surface —
currently bypass location scoping through `BRAND_NEUTRAL_KEY` alone. Delete the marker without this step and
those routes begin throwing `ForbiddenException { code: 'location.context_required' }` for every caller.
TypeScript cannot see this; only a runtime request can.

Files that already carry both markers need no change in this task.

Do not add `@LocationNeutral()` to any controller that does NOT currently carry `@BrandNeutral()` — that
would newly weaken location scoping (D-19) rather than preserve it.
</action>
<verify>
<automated>test "$(comm -23 <(rg -l '@BrandNeutral\(\)' apps/api/src | sort) <(rg -l '@LocationNeutral\(\)' apps/api/src | sort) | wc -l)" -eq 0</automated>
</verify>
<acceptance_criteria> - `comm -23 <(rg -l '@BrandNeutral\(\)' apps/api/src | sort) <(rg -l '@LocationNeutral\(\)' apps/api/src | sort)` produces empty output — every brand-neutral file is now also location-neutral. - `rg -l '@LocationNeutral\(\)' apps/api/src | wc -l` equals the pre-edit `@BrandNeutral()` file count plus the pre-edit location-neutral-only file count, with no third source of new usages (show the arithmetic in the SUMMARY). - `rg -n "@LocationNeutral\(\)" apps/api/src/contexts/payments/interfaces/http/stripe-webhook.controller.ts` and `rg -n "@LocationNeutral\(\)" apps/api/src/health/health.controller.ts` both match. - `cd apps/api && npx tsc -p tsconfig.json --noEmit` exits 0 for these files (additive decorators only).
</acceptance_criteria>
<done>Every route that bypassed location scoping via the brand marker now says so explicitly with the location marker.</done>
</task>

<task type="auto">
  <name>Task 2: Delete both brand decorators and their guard read</name>
  <files>apps/api/src/shared/auth/require-brand.decorator.ts, apps/api/src/shared/auth/brand-neutral.decorator.ts, apps/api/src/shared/auth/index.ts, apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts, apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts, apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts, apps/api/src/contexts/ordering/interfaces/http/operator-orders.controller.ts, apps/api/src/contexts/payments/interfaces/http/order-cancel.controller.ts, apps/api/src/contexts/payments/interfaces/http/refunds.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts, apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts, apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts</files>
  <read_first>
    - apps/api/src/shared/auth/require-brand.decorator.ts (8 lines, whole file: `REQUIRE_BRAND_KEY` + `RequireBrand` via `SetMetadata`)
    - apps/api/src/shared/auth/brand-neutral.decorator.ts (`BRAND_NEUTRAL_KEY` + `BrandNeutral`)
    - apps/api/src/shared/auth/index.ts (barrel re-exports)
    - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts (the `BRAND_NEUTRAL_KEY` import and the second `getAllAndOverride` block at lines 33-37)
    - The 13 files listed in `<files>` (every remaining `@RequireBrand()` / `@BrandNeutral()` usage plus their imports)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md §F3 (the verification that `REQUIRE_BRAND_KEY` is read by zero guards)
  </read_first>
  <action>
Delete `apps/api/src/shared/auth/require-brand.decorator.ts` and
`apps/api/src/shared/auth/brand-neutral.decorator.ts`, and remove both from
`apps/api/src/shared/auth/index.ts`.

Remove all 33 `@RequireBrand()` usages and their imports. This is a pure deletion:
`REQUIRE_BRAND_KEY` is read by zero guards in the repo — `BrandScopeGuard` read `BRAND_NEUTRAL_KEY`, not
`REQUIRE_BRAND_KEY`, and it is deleted anyway. Nothing was enforcing it, so nothing changes at runtime.
Do not create a `@RequireTenant()` replacement — there is nothing to port.

Remove all `@BrandNeutral()` usages and their imports. Task 1 has already added the equivalent
`@LocationNeutral()` wherever it was load-bearing.

`location-scope.guard.ts`: delete the `BRAND_NEUTRAL_KEY` import and the entire second
`reflector.getAllAndOverride(BRAND_NEUTRAL_KEY, ...)` block. Leave the `LOCATION_NEUTRAL_KEY` check, the
`getLocationId()` absence check, the operator/tenant principal checks with their exact
`location.context_required` / `location.operator_required` / `location.tenant_required` codes, the
`principal.baseRole === 'owner'` bypass, and the `NotFoundException` on mismatch — all untouched (D-19).
Leaving the brand check as never-true dead code is explicitly against D-39.

After deleting, `apps/api/src/shared/auth/` must contain no file or identifier with `brand` in it.
</action>
<verify>
<automated>cd apps/api && npx vitest run test/unit/identity/location-scope.guard.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `test ! -f apps/api/src/shared/auth/require-brand.decorator.ts` and `test ! -f apps/api/src/shared/auth/brand-neutral.decorator.ts` both succeed. - `rg -c "RequireBrand|REQUIRE_BRAND_KEY|BrandNeutral|BRAND_NEUTRAL_KEY" apps/api/src` returns 0. - `rg -in "brand" apps/api/src/shared/` returns no matches. - `rg -c "getAllAndOverride" apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts` equals 1. - `rg -n "location.context_required" apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts` and `rg -n "baseRole === 'owner'" apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts` both still match. - `cd apps/api && npx vitest run test/unit/identity/location-scope.guard.spec.ts` exits 0. - `cd apps/api && npx tsc -p tsconfig.json --noEmit` exits 0 (this is the first plan at which the whole `apps/api` package should compile).
</acceptance_criteria>
<done>No brand decorator exists, `LocationScopeGuard` reads exactly one neutrality marker, and `apps/api` compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prove the guard-bypass rewiring did not close a route</name>
  <files>apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts</files>
  <read_first>
    - apps/api/src/main.ts (boot sequence and the port the dev API listens on)
    - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts (post-Task-2)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-11-PLAN.md `<interfaces>` (the 13-file bypass list)
  </read_first>
  <what-built>
Thirteen controllers had their location-scoping bypass moved from the deleted `@BrandNeutral()` marker to
an explicit `@LocationNeutral()`. TypeScript cannot verify that this preserved behaviour — a missed file
turns into a `403 { code: 'location.context_required' }` on a route that used to work, and the four most
consequential of those routes are the Stripe webhook, the health probes, `POST /v1/signup` and
`GET /v1/me`.

Claude boots the API against the merged dev database and probes the four routes without any
`x-location-id` header. The human confirms the dev stack is up.
</what-built>
<action>

1. Start the API against the merged dev database (`pnpm dev:up` first if the stack is down).
2. Probe each route WITHOUT an `x-location-id` header and record the status code:
   - `GET /healthz` (and `/readyz` if it exists) — expect 200, never 403
   - `POST /api/v1/... stripe webhook path` with a deliberately invalid signature — expect the
     signature-verification failure (400/401), NOT `403 location.context_required`. A 403 here means the
     bypass was lost and Stripe retries would black-hole
   - `POST /v1/signup` with an invalid body — expect a 400 validation error, NOT 403
   - `GET /v1/me` with a valid operator session and no location header — expect 200
3. For any 403 whose body contains `location.context_required`, add `@LocationNeutral()` to that controller
   and re-probe.
   </action>
   <how-to-verify>
4. Confirm the dev stack is running and the API booted without a preflight error.
5. Review the four recorded status codes: none may be a 403 carrying `location.context_required`.
   </how-to-verify>
   <resume-signal>Type "approved" once the four probe results are shown and none is a location-context 403, or name the route that regressed.</resume-signal>
   <verify>
   <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:5001/healthz</automated>
   </verify>
   <acceptance_criteria> - `curl -s -o /dev/null -w "%{http_code}" http://localhost:5001/healthz` returns 200. - The Stripe webhook probe returns a signature-verification status (400 or 401), and its response body does not contain `location.context_required`. - `POST /v1/signup` with an invalid body returns 400 with a `validation.failed` code, not 403. - `GET /v1/me` with a valid session and no location header returns 200. - All four status codes and response bodies are pasted into the SUMMARY.
   </acceptance_criteria>
   <done>Every route that bypassed location scoping before the decorator deletion still bypasses it, proven by live requests rather than by reading the diff.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                      |
| ------------------------------------- | -------------------------------------------------------------------------------- |
| Inbound request -> global guard chain | Authorization decisions; a neutrality marker is a deliberate bypass of one guard |
| Stripe -> webhook endpoint            | Unauthenticated inbound POST, verified by signature not by session               |
| Load balancer -> health probes        | Must be reachable with no tenant or location context                             |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                             | Disposition                 | Mitigation Plan                                                                                                                                                                                         |
| ------------ | ---------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-11-01 | Denial of Service      | Losing a bypass and 403-ing the Stripe webhook or the health probes                                   | mitigate                    | Task 1 is an additive-only diff computed from a measured set difference; Task 3 probes the four highest-consequence routes live and blocks on the result                                                |
| T-10.2-11-02 | Elevation of Privilege | Adding `@LocationNeutral()` too broadly and disabling location scoping on a route that should have it | mitigate                    | The action forbids adding the marker to any controller that did not already carry `@BrandNeutral()`; the acceptance criterion shows the arithmetic so a surplus usage is visible                        |
| T-10.2-11-03 | Elevation of Privilege | `@RequireBrand()` removal weakening an authorization check                                            | accept (verified non-issue) | `REQUIRE_BRAND_KEY` is read by zero guards — verified by grep in PATTERNS.md §F3 and re-verified by the criterion that `getAllAndOverride` count in `LocationScopeGuard` is 1. Nothing was enforcing it |
| T-10.2-11-04 | Tampering              | Location scoping semantics silently changed while editing the guard                                   | mitigate                    | The specific error codes, the owner bypass and the `NotFoundException` mismatch branch are asserted by grep and by the guard's own unit spec                                                            |
| T-10.2-11-SC | Tampering              | npm/pip/cargo installs                                                                                | accept                      | No packages installed                                                                                                                                                                                   |

</threat_model>

<verification>
- `rg -c "RequireBrand|REQUIRE_BRAND_KEY|BrandNeutral|BRAND_NEUTRAL_KEY" apps/api/src` returns 0.
- `rg -in "brand" apps/api/src/shared/` returns 0.
- `cd apps/api && npx tsc -p tsconfig.json --noEmit` exits 0 — this is the first plan at which the whole API package compiles.
- `cd apps/api && npx vitest run test/unit/identity/location-scope.guard.spec.ts` passes.
- The four live probes in Task 3 return non-403 statuses, pasted into the SUMMARY.
</verification>

<success_criteria>

- Both brand decorators are deleted; the location guard reads one marker.
- Every previously-bypassing route still bypasses, proven by live requests.
- `apps/api` typechecks end to end.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-11-SUMMARY.md` when done.
</output>
