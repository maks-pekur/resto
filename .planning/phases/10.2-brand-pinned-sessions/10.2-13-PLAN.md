---
phase: 10.2-brand-pinned-sessions
plan: 13
type: execute
wave: 8
depends_on: ['10.2-06', '10.2-12']
files_modified:
  - apps/api/src/contexts/identity/application/dto.ts
  - apps/api/src/contexts/identity/application/signup.service.ts
  - apps/api/src/contexts/identity/application/bootstrap-owner.service.ts
  - apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts
  - apps/api/src/contexts/identity/application/ports/tenant-provisioning.port.ts
  - apps/api/src/contexts/identity/infrastructure/tenant-provisioning.adapter.ts
  - apps/api/src/contexts/identity/interfaces/http/signup.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/identity/domain/signup-errors.ts
  - apps/api/src/contexts/identity/identity-http.module.ts
  - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts
autonomous: false
requirements: []
decisions: [D-06, D-25, D-26, D-27, D-28, D-29, D-30, D-31, D-34, D-35, D-36]

must_haves:
  truths:
    - 'D-25/D-26: `POST /v1/signup` accepts name, email, password and country, and creates a user who is a member of an organization from the first request — no stranded accounts'
    - "D-27: the person's name goes to the Better Auth user; the restaurant's name and slug are set at onboarding, not derived from the person's name at signup"
    - 'D-34/D-35: the signup country is applied to the organization and its currency is derived, never asked'
    - "D-30/D-06: onboarding asks for the restaurant's name and nothing else, and the slug is derived silently"
    - 'D-31: an owner who abandons onboarding and signs back in lands on onboarding again, derived from existing data'
    - 'D-29: `POST /api/auth/sign-up/email` no longer creates accounts from the public internet, and invitation redemption still works'
  artifacts:
    - path: 'apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts'
      provides: 'onboarding: name the restaurant, derive the slug, activate the organization'
    - path: 'apps/api/src/contexts/identity/application/signup.service.ts'
      provides: 'user + provisional organization created atomically from a country'
  key_links:
    - from: 'apps/api/src/contexts/identity/application/signup.service.ts'
      to: 'packages/domain/src/country.ts'
      via: 'currency and locale derivation from the signup country'
      pattern: 'currencyForCountry|defaultLocaleForCountry'
    - from: 'apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts'
      to: 'apps/api/src/contexts/identity/application/check-tenant-slug-availability.service.ts'
      via: 'silent server-side collision resolution'
      pattern: 'SlugAvailability|CheckTenantSlug'
---

<objective>
Make the account-creation journey work end to end on the server: signup creates a user and their first
organization from a country, onboarding names the restaurant and activates it, and the publicly-open Better
Auth signup route stops creating accounts.

> RECONCILIATION — read before implementing, and re-read the note in plan 03 Task 1.
> CONTEXT.md's locked decisions conflict on when the organization is created. D-25 says signup "creates the
> user and their first organization" and D-26 frames the current bug as a user with zero memberships. D-30
> says "onboarding is creating the first organization". D-34 says country is "collected at signup, applied
> to the first organization at onboarding". D-06 says the restaurant's name is asked once, in onboarding.
> D-31 requires the resume point to come from existing data, not a progress flag.
> The reading implemented here satisfies all six: `/v1/signup` creates the user AND the organization
> atomically — so membership exists from request one and there is no stranded account — with the signup
> country applied, a provisional display name and slug, and `status = 'pending_setup'`. Onboarding is where
> the owner names the restaurant: it sets `display_name`, derives the real `slug`, and flips the status to
> `'active'`. The resume point is `status = 'pending_setup'`, a value on the lifecycle column that already
> models exactly this kind of state.
> If the founder prefers signup to leave the user at zero organizations instead, the changes are confined to
> this plan's Task 1 and the one enum value added in plan 03.

Purpose: today the admin signup form calls Better Auth directly and produces a user with zero memberships
who cannot create anything — verified live on 2026-08-19.
Output: a working server-side path from "create account" to "named, active restaurant".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-06-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-08-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-12-SUMMARY.md

<interfaces>
Endpoints this plan LOCKS. Plan 17's signup form and onboarding screen call them.

POST /v1/signup
body: { name: string; email: string; password: string; country: 'UA'|'GB'|'ES' }
creates: BA user (name = the PERSON's name) + organization (status 'pending_setup', country applied,
default_currency derived, locale derived, provisional display_name + slug) + owner membership
returns: the existing enumeration-safe equalized response shape — do NOT widen it (D-06 phase 03)

POST /v1/me/tenants/onboarding
body: { displayName: string }
requires: an authenticated session bound to an organization whose status is 'pending_setup'
behaviour: set display_name, derive a slug from it, resolve collisions silently by suffix,
write the `<slug>.menu.<suffix>` domain row, flip status to 'active'
200 -> { id, slug, displayName, status: 'active' }
409 -> the caller's organization is already 'active' (idempotent-safe, not an error state to surface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild signup around name, email, password and country</name>
  <files>apps/api/src/contexts/identity/application/dto.ts, apps/api/src/contexts/identity/application/signup.service.ts, apps/api/src/contexts/identity/application/bootstrap-owner.service.ts, apps/api/src/contexts/identity/application/ports/tenant-provisioning.port.ts, apps/api/src/contexts/identity/infrastructure/tenant-provisioning.adapter.ts, apps/api/src/contexts/identity/interfaces/http/signup.controller.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/application/dto.ts (~36-48: `SignUpInputSchema` with `email`, `password`, `displayName`, `defaultCurrency: CurrencyValue`, `locale` with a `.default('en')`; plus `SignUpInputDto extends createZodDto(...)`)
    - apps/api/src/contexts/identity/application/signup.service.ts (full file: `slugify`, `MAX_SLUG_CANDIDATES`, `findFreeSlug`, `PARITY_FLOOR_MS` and the `executeOrTimeEqualize` enumeration defence from Phase 03 D-06, the three-way `displayName` overload at ~137-152, `bootstrap.execute({ tenantSlug, email, password, name })`, and `signInAndActivate` at ~229 calling `setActiveOrganization`)
    - apps/api/src/contexts/identity/application/bootstrap-owner.service.ts (~line 47 documents why BA's `createOrganization` must NOT be called; ~line 124 calls `auth.api.signUpEmail` SERVER-SIDE — this internal call must keep working after Task 3 closes the public route)
    - apps/api/src/contexts/identity/application/ports/tenant-provisioning.port.ts (the identity->tenancy seam; its `provision(...)` signature changes with plan 06's service)
    - apps/api/src/contexts/tenancy/application/provision-tenant.service.ts (post-plan-06: takes `{ slug, displayName, country, locale? }`, derives currency)
    - packages/domain/src/country.ts (plan 01: `CountryCodeValue`, `currencyForCountry`, `defaultLocaleForCountry`)
    - apps/api/src/contexts/identity/domain/signup-errors.ts (`SlugUnavailableError`, `SignupEmailAlreadyExistsError`, `SignupBetterAuthFailureError`)
  </read_first>
  <action>
`dto.ts` — replace `SignUpInputSchema`'s fields with: `name` (the PERSON's name, min 2 max 120, trimmed),
`email`, `password` (keep the existing min 12 / max 128), and `country: CountryCodeValue`. DELETE
`defaultCurrency` (D-28/D-35: currency is derived, never asked) and DELETE the `locale` input (it is derived
from country via `defaultLocaleForCountry`). Use the unbranded `CountryCodeValue` at this HTTP boundary so
`nestjs-zod` emits `type: string` for OpenAPI (ADR-0020 I-7).

`signup.service.ts` — split the three-way `displayName` overload (D-27):

- the PERSON's name goes to Better Auth as the user's `name`, via `bootstrap.execute({ ..., name: input.name })`;
- the ORGANIZATION is provisioned with a PROVISIONAL `displayName` and slug. Derive the provisional slug from
  `slugify(input.name)` plus a short random suffix so two people with the same name never collide, and reuse
  the existing `findFreeSlug` loop as the final guard. The provisional display name is never shown to the
  owner — onboarding replaces it before they see any dashboard;
- provision with `status: 'pending_setup'`, `country: input.country`,
  `defaultCurrency: currencyForCountry(input.country)`, `locale: defaultLocaleForCountry(input.country)`.

Do NOT change the enumeration defence. `PARITY_FLOOR_MS`, `executeOrTimeEqualize` and the
`{ status: 'pending_verification' }` collapsed response are Phase 03 D-06 controls: both branches must keep
padding to the same floor and the response must keep leaking neither tenant id, user id nor `Set-Cookie`.
Adding a country to the input does not change that; widening the response would silently undo it.

`tenant-provisioning.port.ts` and its adapter — update the seam's `provision(...)` signature to match plan
06's service: `{ slug, displayName, country, locale? }`, no `defaultCurrency`.

`signup.controller.ts` — keep the per-parameter `@Body(new RestoZodValidationPipe(SignUpInputDto))` usage,
the existing rate limiting and the `@LocationNeutral()` marker plan 11 added. Only the DTO shape changes.

`bootstrap-owner.service.ts` — unchanged in behaviour. It must keep calling `auth.api.signUpEmail`
server-side; note that fact prominently for Task 3, which closes the PUBLIC HTTP route and must not break
this internal call.
</action>
<verify>
<automated>cd apps/api && npx vitest run test/e2e/signup-enumeration.e2e.spec.ts 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `rg -n "country: CountryCodeValue" apps/api/src/contexts/identity/application/dto.ts` matches and `rg -n "defaultCurrency" apps/api/src/contexts/identity/application/dto.ts` returns no match inside `SignUpInputSchema`. - `rg -n "currencyForCountry" apps/api/src/contexts/identity/application/signup.service.ts` and `rg -n "defaultLocaleForCountry" apps/api/src/contexts/identity/application/signup.service.ts` both match. - `rg -n "pending_setup" apps/api/src/contexts/identity/application/signup.service.ts` matches. - `rg -n "PARITY_FLOOR_MS" apps/api/src/contexts/identity/application/signup.service.ts` still matches and the equalized response still returns only `{ status: 'pending_verification' }`. - A `POST /v1/signup` with `country: 'ES'` creates an organization whose `default_currency` is `EUR` and `locale` is `es` — assert this against the persisted row in an e2e, not against a mock. - A `POST /v1/signup` with `country: 'XX'` returns 400 `validation.failed`. - `cd apps/api && npx vitest run test/e2e/signup-enumeration.e2e.spec.ts` exits 0 (the enumeration defence still holds).
</acceptance_criteria>
<done>One request creates a person, their first restaurant and their ownership of it, with the market's currency and language already right.</done>
</task>

<task type="auto">
  <name>Task 2: Build the onboarding finalize path</name>
  <files>apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts, apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts, apps/api/src/contexts/identity/interfaces/http/error-mapping.ts, apps/api/src/contexts/identity/domain/signup-errors.ts, apps/api/src/contexts/identity/identity-http.module.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/application/check-tenant-slug-availability.service.ts (post-plan-08 — the availability check that backs silent collision resolution)
    - apps/api/src/contexts/identity/application/signup.service.ts (`slugify` and `findFreeSlug` — reuse them rather than writing a second slug derivation; a second one WILL drift)
    - apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts (post-plan-08: `GET /v1/me/tenants` and `GET /v1/me/tenants/slug-availability`; the `@Permissions({...})` and `RestoZodValidationPipe` conventions)
    - apps/api/src/contexts/tenancy/application/provision-tenant.service.ts and the tenant repository (post-plan-06 — how a tenant row and its domain row are written, so onboarding updates them the same way)
    - packages/db/src/schema/tenants.ts (post-plan-03: `status` accepts `'pending_setup'`; `tenant_domains` holds the menu hostname)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-UI-SPEC.md §S4 (one field, silent collision resolution, host preview, "Continue")
    - apps/api/src/shared/api/wrap.ts and the identity `error-mapping.ts`
  </read_first>
  <action>
Create `apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts` with the project's
single-public-`execute(input)` shape, and mount it as `POST /v1/me/tenants/onboarding` on
`me-tenants.controller.ts`.

Behaviour:

1. Resolve the caller's organization from the session's `activeOrganizationId` — not from a body parameter.
   A body-supplied organization id would let one owner rename another owner's restaurant.
2. If that organization's `status` is not `'pending_setup'`, return 409. Onboarding is not a rename tool;
   renaming an active restaurant is a settings concern this phase does not ship.
3. Set `display_name` to the submitted name.
4. Derive the slug with the SAME `slugify` + `findFreeSlug` pair signup uses — import them, do not
   reimplement. Collisions are resolved silently by suffix and are never surfaced to the user (UI-SPEC S4:
   the availability check is a server-side mechanism, not a UI hint).
5. Write/replace the `<slug>.menu.<PRIMARY_DOMAIN_SUFFIX>` row in `tenant_domains`, respecting the global
   unique index. Because signup already created a provisional domain row, this is an update-or-replace, not
   a blind insert — read the tenant repository's domain-write method from plan 06 and use it.
6. Flip `status` to `'active'`.
7. Return `{ id, slug, displayName, status }`. The caller needs `slug` to build its post-onboarding
   navigation to `<slug>.admin.<domain>`.

D-31's resume point is a read, not a write: `GET /v1/me/tenants` already returns each organization's
`status` after plan 08, so the admin derives "resume at onboarding" from `status === 'pending_setup'` with
no additional endpoint and no progress flag. Confirm `status` is in that response's projection; if plan 08
omitted it, add it here and note the change.

Add whatever domain error the 409 maps to in `domain/signup-errors.ts` (plain `Error` subclass, explicit
`this.name`, no code and no HTTP status inside the domain), and map it in
`interfaces/http/error-mapping.ts`. Register the service in `identity-http.module.ts`.
</action>
<verify>
<automated>cd apps/api && npx tsc -p tsconfig.json --noEmit && npx vitest run test/e2e/identity-bootstrap.e2e.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `rg -n "activeOrganizationId" apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts` matches and `rg -n "organizationId" apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts` shows no organization id accepted in the onboarding request body. - `rg -n "slugify|findFreeSlug" apps/api/src/contexts/identity/application/finalize-tenant-setup.service.ts` matches (reused, not reimplemented). - `rg -n "status" apps/api/src/contexts/identity/application/list-my-tenants.service.ts` matches in the returned projection (D-31's resume signal is readable). - An e2e asserts: signup -> `GET /v1/me/tenants` shows one organization with `status: 'pending_setup'` -> `POST /v1/me/tenants/onboarding` with a name -> the persisted row has the submitted `display_name`, a derived slug, `status: 'active'`, and exactly one `tenant_domains` row for `<slug>.menu.<suffix>`. - A second `POST /v1/me/tenants/onboarding` on the same organization returns 409. - Two owners submitting the same restaurant name both succeed, with distinct slugs and no error surfaced to either.
</acceptance_criteria>
<done>An owner names their restaurant once and it becomes active, reachable at its own hostname, with collisions resolved without them ever seeing an error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Close public Better Auth signup without breaking invitations</name>
  <files>apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts (~line 201 `emailAndPassword: { ... }`, the `hooks.before` surface, and the plugins array)
    - apps/api/src/contexts/identity/application/bootstrap-owner.service.ts (~line 124: `auth.api.signUpEmail(...)` — the SERVER-SIDE call that `/v1/signup` depends on and that must keep working)
    - apps/admin/src/routes/(auth)/accept-invitation.$id.tsx (the invitee flow: it calls `authClient.organization.acceptInvitation({ invitationId })` and NOTHING else — meaning the invitee must already have an account and a session when they land here)
    - node_modules/.pnpm/better-auth@1.4.22*/node_modules/better-auth/dist/plugins/organization/routes/*.mjs (grep for `signUpEmail` and `createUser` — a 2026-08-19 grep found NO match, i.e. acceptInvitation does not create accounts)
    - apps/api/src/config/env.schema.ts (~line 187: the per-IP rate limit for `POST /api/auth/sign-up/email`, RES-137 — record whether it becomes dead config)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md Open Question 3
  </read_first>
  <what-built>
`/v1/signup` is now the intended account-creation path. `POST /api/auth/sign-up/email` is still publicly
open and is how the stranded accounts appeared. D-29 says close it — and says to check invitation acceptance
first, because blindly disabling it would break the invitations repaired on 2026-08-19.

Two findings from this planning pass that the executor must re-verify rather than trust:

1. Better Auth's `acceptInvitation` does NOT call `signUpEmail`/`createUser` — a grep of the organization
   plugin's route bundle returned nothing. So invitation ACCEPTANCE does not share the signup path.
2. But `accept-invitation.$id.tsx` calls only `acceptInvitation`, which requires an existing session. So an
   invited staff member who has no account yet has NO account-creation path once the public route closes.
   Whether that is a real gap depends on how the invitation email flow actually works today.

This is the phase's single most safety-critical unresolved item, and it needs the founder's call on the
invitee-without-an-account case before the route is closed.
</what-built>
<action>

1. Re-verify finding 1 yourself:
   `rg -n "signUpEmail|createUser" node_modules/.pnpm/better-auth@1.4.22*/node_modules/better-auth/dist/plugins/organization/routes/*.mjs`
   Paste the result (including "no matches") into the SUMMARY.
2. Trace the invitee journey end to end: read the invitation email template in
   `apps/api/src/contexts/identity/infrastructure/email/`, follow the link target, and determine whether an
   invitee without an existing account can reach a session today. Report exactly what happens.
3. Implement the closure as a Better Auth `hooks.before` that rejects `/sign-up/email` for EXTERNAL HTTP
   requests only. The distinguishing test must be a property of the request, not a header a client can
   forge — BA populates `ctx.request` for real HTTP requests routed through the handler and leaves it
   undefined for server-side `auth.api.*` calls. Verify that distinction empirically before relying on it:
   add a temporary log in the hook, call `/v1/signup`, and confirm the internal `signUpEmail` still
   succeeds. If the distinction does not hold, the fallback is to gate on the `INTERNAL_API_TOKEN` the
   project already uses for server-to-server calls — never on a client-settable header alone.
4. Return a 403 with a stable problem code (e.g. `signup.direct_disabled`) pointing at `/v1/signup`.
5. Report whether `RATE_LIMIT_SIGNUP_PER_MIN` (or whatever `env.schema.ts:187` names) is now dead config; do
   not delete it in this task — plan 20 owns dead-config removal.
   </action>
   <how-to-verify>
6. Read the executor's report on the invitee-without-an-account journey.
7. Decide: if an invitee genuinely cannot create an account after the closure, either accept that
   invitations only work for people who already have accounts (and confirm that is the intended MVP
   behaviour), or direct that the closure be scoped to admit a request carrying a valid pending invitation
   for that email.
8. Confirm the two probes below returned the stated results.
   </how-to-verify>
   <resume-signal>Type "approved" to close the route as implemented, or describe the invitation carve-out to add.</resume-signal>
   <verify>
   <automated>cd apps/api && npx vitest run test/e2e/identity-invitation.e2e.spec.ts 2>&1 | tail -30</automated>
   </verify>
   <acceptance_criteria> - A direct `curl -X POST http://localhost:5001/api/auth/sign-up/email -d '{"email":"x@y.z","password":"...","name":"X"}'` returns 403 with the chosen problem code; paste the response into the SUMMARY. - `POST /v1/signup` still succeeds end to end after the closure — paste the created organization row. - `cd apps/api && npx vitest run test/e2e/identity-invitation.e2e.spec.ts` exits 0. - The SUMMARY contains the verbatim grep output for `signUpEmail|createUser` in BA's organization routes. - The SUMMARY describes what an invitee without an existing account experiences, and records the founder's decision. - `rg -n "x-internal|headers\[" apps/api/src/contexts/identity/infrastructure/better-auth/auth.config.ts` shows no closure gated on a client-settable header alone.
   </acceptance_criteria>
   <done>The public signup route no longer creates accounts, `/v1/signup` still does, and the invitee case is either proven fine or explicitly decided by the founder.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                                                        |
| --------------------------------------------------------- | ------------------------------------------------------------------ |
| Anonymous internet -> `POST /v1/signup`                   | The only remaining account-creation surface; fully unauthenticated |
| Authenticated session -> `POST /v1/me/tenants/onboarding` | Names and activates a restaurant                                   |
| Server-side `auth.api.signUpEmail` -> BA pipeline         | Must keep working while the public HTTP route is closed            |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                          |
| ------------ | ---------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-13-01 | Information Disclosure | Email enumeration via signup response or timing                                                            | mitigate    | Phase 03 D-06 controls are preserved verbatim: `PARITY_FLOOR_MS` padding on both branches and the collapsed `{ status: 'pending_verification' }` response. Acceptance criteria assert both, and the existing enumeration e2e is the gate |
| T-10.2-13-02 | Elevation of Privilege | Renaming another owner's restaurant via a body-supplied organization id                                    | mitigate    | The onboarding service resolves the organization from the session's `activeOrganizationId` only; grep asserts no organization id is accepted in the request body                                                                         |
| T-10.2-13-03 | Denial of Service      | Unbounded organization creation from the open signup surface                                               | mitigate    | The existing per-IP signup rate limit stays applied; `findFreeSlug` is bounded at `MAX_SLUG_CANDIDATES`. Plan-based limits on organization count are explicitly deferred (CONTEXT.md), so this is a rate control, not a quota            |
| T-10.2-13-04 | Spoofing               | Slug collision letting one restaurant take another's hostname                                              | mitigate    | `findFreeSlug` plus the global unique index on `tenant_domains.domain` (one table after D-23); an acceptance criterion has two owners submit the same name and asserts distinct slugs                                                    |
| T-10.2-13-05 | Denial of Service      | Closing `/sign-up/email` also blocking the internal `auth.api.signUpEmail`, breaking `/v1/signup` entirely | mitigate    | Task 3 requires empirically verifying the internal-vs-external distinction before relying on it, and a live probe that `/v1/signup` still succeeds after the closure                                                                     |
| T-10.2-13-06 | Elevation of Privilege | The closure bypassed by a forged header                                                                    | mitigate    | The action forbids gating on a client-settable header alone; the fallback is the existing `INTERNAL_API_TOKEN` server-to-server control                                                                                                  |
| T-10.2-13-07 | Tampering              | A `pending_setup` organization serving a public menu or taking orders before it is named                   | mitigate    | `SERVABLE_STATUSES` is `new Set(['active'])` and host resolvers test `status === 'active'` — verified in plan 03; add an e2e asserting a `pending_setup` organization's menu host returns 404/410                                        |
| T-10.2-13-SC | Tampering              | npm/pip/cargo installs                                                                                     | accept      | No packages installed                                                                                                                                                                                                                    |

</threat_model>

<verification>
- `cd apps/api && npx tsc -p tsconfig.json --noEmit` exits 0.
- Run each of these alone (never the whole e2e directory in one process):
  `cd apps/api && npx vitest run test/e2e/signup-enumeration.e2e.spec.ts`
  `cd apps/api && npx vitest run test/e2e/identity-bootstrap.e2e.spec.ts`
  `cd apps/api && npx vitest run test/e2e/identity-invitation.e2e.spec.ts`
- The live probes in Task 3 (403 on the direct route, success on `/v1/signup`) are pasted into the SUMMARY.
- An e2e proves a `pending_setup` organization does not serve its public menu.
</verification>

<success_criteria>

- Signup produces a person, a restaurant and an ownership in one request, with derived currency and locale.
- Onboarding names the restaurant, derives its slug silently and activates it.
- Abandoning onboarding and returning lands the owner back on onboarding, derived from `status`.
- Public Better Auth signup is closed and invitations still work — or the founder has explicitly decided
  what happens to an invitee without an account.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-13-SUMMARY.md` when done.
</output>
