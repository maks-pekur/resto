---
phase: 10.2-brand-pinned-sessions
plan: 19
type: execute
wave: 12
depends_on: ['10.2-17', '10.2-18']
files_modified:
  - apps/api/test/e2e/brand-isolation.e2e.spec.ts
  - apps/api/test/e2e/set-active-brand.e2e.spec.ts
  - apps/api/test/e2e/brand-host-resolution.e2e.spec.ts
  - apps/api/test/e2e/menu-brand-response.e2e.spec.ts
  - apps/api/test/e2e/me-brands.e2e.spec.ts
  - apps/api/test/e2e/catalog-brand-isolation.e2e.spec.ts
  - apps/api/test/e2e/roles-brand-scope-orthogonality.e2e.spec.ts
  - apps/api/test/e2e/catalog-brand-scope.e2e.spec.ts
  - apps/api/test/e2e/brand-scope-guard.e2e.spec.ts
  - apps/api/test/e2e/brand-slug-lookup.e2e.spec.ts
  - apps/api/test/e2e/brand-payout-isolation.e2e.spec.ts
  - apps/api/test/e2e/catalog-brand-read-isolation.e2e.spec.ts
  - apps/api/test/e2e/tenant-isolation.e2e.spec.ts
  - apps/api/test/e2e/order-routes-authz.e2e.spec.ts
  - apps/api/test/unit/identity/
  - apps/api/test/unit/tenancy/
  - apps/api/test/unit/shared/tenant-context.middleware.spec.ts
  - packages/db/test/integration/tenant-isolation.spec.ts
  - packages/db/test/integration/brands-rls.spec.ts
  - packages/db/test/integration/brand-id-not-null.spec.ts
  - packages/db/test/integration/erase-includes-brands.spec.ts
  - packages/db/test/integration/composite-tenant-fk.spec.ts
  - packages/db/test/integration/scoped-tx.spec.ts
  - packages/db/test/unit/context.spec.ts
  - packages/domain/test/value-objects.spec.ts
  - apps/admin/e2e/adm-00-smoke-walk.spec.ts
  - apps/admin/e2e/fixtures/
autonomous: true
requirements: []
decisions: [D-09, D-11, D-13, D-15, D-17, D-21, D-24, D-38]

must_haves:
  truths:
    - 'The brand-era regression net is either deleted or rewritten into its tenant-only equivalent — nothing is left failing or skipped'
    - 'D-09: an integration test proves `app_bind_brand` and `current_brand_id` are not callable, and that tenant RLS still blocks cross-tenant reads'
    - 'Cross-tenant isolation coverage is at least as strong after the merge as before it'
    - 'D-17: a regression test proves a fresh session has no organization bound until one is chosen'
    - 'The `adm-00` scenarios parked in `test.fixme` awaiting this phase are repaired or deleted, not left parked'
  artifacts:
    - path: 'packages/db/test/integration/tenant-isolation.spec.ts'
      provides: 'the canonical RLS regression net, extended to every table that lost brand_id'
    - path: 'apps/admin/e2e/adm-00-smoke-walk.spec.ts'
      provides: 'admin smoke walk with no brand-switcher scenarios'
  key_links:
    - from: 'packages/db/test/integration/tenant-isolation.spec.ts'
      to: 'packages/db/migrations/0079_organization_merge.sql'
      via: 'negative-existence assertions on the dropped functions'
      pattern: 'app_bind_brand'
---

<objective>
Rebuild the regression net the merge invalidated: make a per-file delete-or-rewrite decision on 12 brand e2e
specs, 11 brand unit specs and the `packages/db` integration suite, extend the canonical RLS net to the
merged shape, and repair the four parked `adm-00` scenarios.

Purpose: this phase deletes an entire dimension of isolation testing. Left as-is, the suite would be green
because the tests were removed, not because isolation still holds. Cross-tenant coverage must come out of
this at least as strong as it went in.
Output: no spec references a brand, no spec is left skipped, and RLS teardown correctness is asserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@packages/db/CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-05-SUMMARY.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-17-SUMMARY.md

<interfaces>
Per-file disposition for the 12 API e2e specs. Decide from the file's own assertions, but these are the
starting positions derived from PATTERNS.md §F7 and RESEARCH.md's validation architecture:

DELETE outright (every assertion is about brand-scope logic that no longer exists):
set-active-brand.e2e.spec.ts (597 lines, 11 cases — tamper-resistance, owner re-pin, non-owner scope,
deterministic initial pin; superseded by organization-switch.e2e.spec.ts)
brand-scope-guard.e2e.spec.ts
catalog-brand-scope.e2e.spec.ts
roles-brand-scope-orthogonality.e2e.spec.ts

REWRITE to the tenant-only equivalent (the isolation property survives, only the dimension changes):
brand-isolation.e2e.spec.ts, catalog-brand-isolation.e2e.spec.ts,
catalog-brand-read-isolation.e2e.spec.ts, brand-payout-isolation.e2e.spec.ts

REWRITE for a specific surviving reason:
menu-brand-response.e2e.spec.ts — also asserts the no-`Set-Cookie`-on-public-menu-read invariant that
`apps/CLAUDE.md` depends on. Keep and extend that assertion.
brand-host-resolution.e2e.spec.ts — host resolution survives, collapsed to one entity; also the natural
home for the D-22 three-branch assertions.
me-brands.e2e.spec.ts -> me-tenants.e2e.spec.ts — the endpoint survives renamed and now backs the picker.
brand-slug-lookup.e2e.spec.ts — the rate-limited slug check survives renamed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the twelve brand e2e specs and strengthen tenant isolation coverage</name>
  <files>apps/api/test/e2e/brand-isolation.e2e.spec.ts, apps/api/test/e2e/set-active-brand.e2e.spec.ts, apps/api/test/e2e/brand-host-resolution.e2e.spec.ts, apps/api/test/e2e/menu-brand-response.e2e.spec.ts, apps/api/test/e2e/me-brands.e2e.spec.ts, apps/api/test/e2e/catalog-brand-isolation.e2e.spec.ts, apps/api/test/e2e/roles-brand-scope-orthogonality.e2e.spec.ts, apps/api/test/e2e/catalog-brand-scope.e2e.spec.ts, apps/api/test/e2e/brand-scope-guard.e2e.spec.ts, apps/api/test/e2e/brand-slug-lookup.e2e.spec.ts, apps/api/test/e2e/brand-payout-isolation.e2e.spec.ts, apps/api/test/e2e/catalog-brand-read-isolation.e2e.spec.ts, apps/api/test/e2e/tenant-isolation.e2e.spec.ts, apps/api/test/e2e/order-routes-authz.e2e.spec.ts</files>
  <read_first>
    - Every file in `<files>` — read each one's assertions before deciding its fate; the `<interfaces>` block gives a starting position, not a verdict
    - apps/api/test/e2e/organization-switch.e2e.spec.ts (written in plan 12 — the replacement for `set-active-brand.e2e.spec.ts`; do not duplicate its cases)
    - apps/api/test/e2e/ fixture and harness helpers (post-merge tenant creation; `set-active-brand.e2e.spec.ts` is the richest example of harness usage — mine it before deleting it)
    - apps/CLAUDE.md (the no-`Set-Cookie`-on-public-menu-read invariant `menu-brand-response.e2e.spec.ts` protects)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md D-22 (the three host branches to assert)
    - CLAUDE.md and project memory: running the whole `apps/api` e2e directory in one process yields false 429/timeout failures — every command here targets one file
  </read_first>
  <action>
For each of the twelve specs, make and record an explicit decision — deleted, rewritten, or renamed — with a
one-line reason. A spec left failing or marked `.skip`/`.fixme` is not a decision.

Deletions: before deleting a file, check whether any assertion in it covers a property that survives under a
different name. `set-active-brand.e2e.spec.ts`'s tamper-resistance cases are the ones most likely to have a
surviving analogue — if plan 12's `organization-switch.e2e.spec.ts` does not already cover an equivalent,
port it there rather than losing it.

Rewrites: keep the isolation SHAPE and change only the dimension. A test that asserted brand A cannot read
brand B's menu becomes tenant A cannot read tenant B's menu — and that assertion must be present and passing,
not merely removed. Coverage must not shrink: record in the SUMMARY the count of cross-isolation assertions
before and after.

`menu-brand-response.e2e.spec.ts` -> rename to `menu-response.e2e.spec.ts`, strip the brand assertions, and
KEEP and strengthen the no-`Set-Cookie` assertion. Add the D-24 companion: a request to
`<slug>.menu.<domain>` must never receive an operator session cookie.

`brand-host-resolution.e2e.spec.ts` -> rename to `host-resolution.e2e.spec.ts` and assert D-22's three
branches explicitly: `<slug>.menu.<domain>` resolves the guest menu; the bare `<slug>.<domain>` does NOT
resolve as a guest menu host (it is reserved for the future public site); an unknown host resolves nothing.

`me-brands.e2e.spec.ts` -> `me-tenants.e2e.spec.ts`, asserting a user who belongs to two organizations sees
exactly two and a user who belongs to one sees exactly one — the picker depends on that being right, and it
is also the leak boundary.

Add one new case to whichever spec covers sign-in: a fresh session has no `activeOrganizationId` bound
before any switch call (D-17). RESEARCH verified this against Better Auth's source; this turns the
verification into a regression net.

Assert on observable outcomes — status codes, response bodies, persisted rows, cookie headers — never on a
mock having been called.
</action>
<verify>
<automated>cd apps/api && for f in test/e2e/tenant-isolation.e2e.spec.ts test/e2e/menu-response.e2e.spec.ts test/e2e/host-resolution.e2e.spec.ts test/e2e/me-tenants.e2e.spec.ts; do echo "== $f"; npx vitest run "$f" 2>&1 | tail -8; done</automated>
</verify>
<acceptance_criteria> - `ls apps/api/test/e2e/ | rg -i brand` returns no results. - `rg -c "\.skip\(|\.fixme\(|xit\(|xdescribe\(" apps/api/test/e2e/` returns 0. - `rg -in "brand" apps/api/test/e2e/` returns no matches. - Each of the four commands in `<automated>` exits 0 when run individually. - The SUMMARY contains a 12-row table: file, disposition (deleted / rewritten / renamed), one-line reason. - The SUMMARY records the count of cross-isolation assertions before and after, and the after count is not lower. - A spec asserts a fresh session has no `activeOrganizationId`. - A spec asserts a request to the guest menu host receives no operator session cookie.
</acceptance_criteria>
<done>Every brand-era e2e has an explicit fate, cross-tenant coverage did not shrink, and the guest-host cookie invariant is still guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the unit and packages/db integration suites</name>
  <files>apps/api/test/unit/identity/, apps/api/test/unit/tenancy/, apps/api/test/unit/shared/tenant-context.middleware.spec.ts, packages/db/test/integration/tenant-isolation.spec.ts, packages/db/test/integration/brands-rls.spec.ts, packages/db/test/integration/brand-id-not-null.spec.ts, packages/db/test/integration/erase-includes-brands.spec.ts, packages/db/test/integration/composite-tenant-fk.spec.ts, packages/db/test/integration/scoped-tx.spec.ts, packages/db/test/unit/context.spec.ts, packages/domain/test/value-objects.spec.ts</files>
  <read_first>
    - apps/api/test/unit/identity/ (the 7 brand specs: brand-provisioning.adapter, brand-scope-guard, brand-slug-rate-limit.guard, check-brand-slug-availability.service, create-my-brand.service, list-my-brands.service, set-active-brand.service — each dies or is renamed with its production counterpart from plan 08)
    - apps/api/test/unit/tenancy/ (the 5 brand specs: brand-aggregate, brand-onboarding-oauth, provision-brand.service, start-brand-onboarding-url, tenant-and-brand-resolver.service — same rule against plans 06 and 07)
    - packages/db/test/integration/tenant-isolation.spec.ts (per `packages/db/CLAUDE.md` this is THE canonical RLS regression net: "every new tenant-scoped table needs an entry here")
    - packages/db/test/integration/brands-rls.spec.ts and brand-id-not-null.spec.ts (assert brand-dimension RLS and NOT NULL constraints that no longer exist)
    - packages/db/test/integration/erase-includes-brands.spec.ts (asserts erasure completeness for the brands table; the property survives, the table does not)
    - packages/db/test/integration/composite-tenant-fk.spec.ts and scoped-tx.spec.ts (the ADR-0020 I-2 and ScopedTx nets that must survive intact minus the brand dimension)
    - packages/db/test/unit/context.spec.ts (asserts `TenantContext` shape including `brandId`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-05-SUMMARY.md (the exact list of dropped policies, functions and columns to assert negatively)
  </read_first>
  <action>
Unit specs (12 files across identity and tenancy): each follows its production counterpart. Deleted service
-> deleted spec. Renamed service -> renamed spec with brand assertions rewritten to the tenant equivalent.
`brand-slug-rate-limit.guard.spec.ts` renames to `tenant-slug-rate-limit.guard.spec.ts` alongside its guard.
`tenant-and-brand-resolver.service.spec.ts` folds into `tenant-resolver.service.spec.ts`.

`packages/db/test/integration/tenant-isolation.spec.ts` — the canonical RLS net. Extend it:

- add or update an entry for every table that lost `brand_id` (`menu_categories`, `menu_items`,
  `menu_item_sizes`, `menu_modifier_groups`, `menu_modifier_options`, `menu_item_modifier_groups`, `orders`,
  `customer_profiles`, `outbox`, `locations`), asserting tenant-level RLS alone blocks a cross-tenant read;
- add negative-existence assertions proving the teardown: `to_regprocedure('public.app_bind_brand(text)')`
  IS NULL, `to_regprocedure('public.current_brand_id()')` IS NULL, and
  `SELECT count(*) FROM pg_policies WHERE policyname LIKE '%_brand_iso'` is 0;
- add a positive assertion that `orders_location_iso` still exists, so a future over-broad teardown fails here.

`brands-rls.spec.ts` and `brand-id-not-null.spec.ts` — delete outright; they assert constraints that no
longer exist.

`erase-includes-brands.spec.ts` — rewrite, do not delete. The erasure-completeness property ("every
PII-bearing column is covered") survives; only its subject moves. Rename it to reflect the merged table and
assert coverage of `legal_name`, `legal_form`, `tax_id` and `stripe_account_id` on `tenants`, matching what
plan 18 wired into the erasure CLI.

`composite-tenant-fk.spec.ts` — keep every assertion about `tenant_id` composite FKs; remove only the
brand-parented rows. If the spec enumerates expected FKs, update the list to the post-merge set and
cross-check it against `pnpm --filter @resto/db db:audit-fks`.

`scoped-tx.spec.ts` and `packages/db/test/unit/context.spec.ts` — remove `brandId` from the `TenantContext`
shape assertions and any `withBrand` case. Keep every tenant and location case.

`packages/domain/test/value-objects.spec.ts` — plan 01 already rewrote its slug cases; verify rather than
re-edit, and add a country-registry case asserting `currencyForCountry` and `defaultLocaleForCountry` return
the locked mapping for all three countries.
</action>
<verify>
<automated>pnpm --filter @resto/db test && pnpm --filter @resto/domain test && cd apps/api && npx vitest run test/unit 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `find apps/api/test/unit packages/db/test packages/domain/test -iname "*brand*"` returns no results. - `rg -in "brand" apps/api/test/unit/ packages/db/test/ packages/domain/test/` returns no matches. - `rg -n "app_bind_brand" packages/db/test/integration/tenant-isolation.spec.ts` matches inside a negative-existence assertion. - `rg -n "orders_location_iso" packages/db/test/integration/tenant-isolation.spec.ts` matches inside a positive-existence assertion. - `rg -n "legal_name|tax_id" packages/db/test/integration/` matches in the rewritten erasure spec. - `pnpm --filter @resto/db test` exits 0. - `pnpm --filter @resto/domain test` exits 0. - `cd apps/api && npx vitest run test/unit` exits 0.
</acceptance_criteria>
<done>The RLS net covers every table that lost a dimension, proves the dropped functions are gone, and proves the surviving location policy is not.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the parked adm-00 Playwright scenarios</name>
  <files>apps/admin/e2e/adm-00-smoke-walk.spec.ts, apps/admin/e2e/fixtures/</files>
  <read_first>
    - apps/admin/e2e/adm-00-smoke-walk.spec.ts (scenarios 3, 6, 7a and 7b sit in `test.fixme` awaiting this phase: brand switcher, cross-tab brand sync, add-brand-from-switcher)
    - apps/admin/e2e/README.md (how the suite is run and what it assumes about seeded data)
    - apps/admin/e2e/fixtures/ (the fixture data and any brand-shaped setup)
    - apps/admin/e2e/adm-01-all-mode-smoke.spec.ts, adm-02-orders-workflow-smoke.spec.ts, adm-03-guest-status-loop.spec.ts (check whether they also assume a `/{brandSlug}` URL)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-17-SUMMARY.md (the new routes, hosts and picker the scenarios must target)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-18-SUMMARY.md (what the rewritten seed provides)
  </read_first>
  <action>
Resolve the four parked scenarios explicitly — CONTEXT.md says they must be repaired or deleted here, not
left parked:
- Scenario 3 (brand switcher): the switcher is deleted (D-10). Delete this scenario and, in its place, assert
  the static organization-identity block renders the restaurant's name and is NOT clickable (UI-SPEC S5).
- Scenario 6 (cross-tab brand sync): rewrite as the D-15 cross-tab consequence — with two tabs open, switching
  organization in one causes the other's next action to land on `/login?expired=1`. This is a higher-value
  test than the one it replaces because it proves the old token is genuinely dead.
- Scenario 7a/7b (add brand from switcher): the create-organization entry point is deliberately not rebuilt
  (CONTEXT.md deferred GAP). Delete both and add a comment in the spec file naming the GAP, so the next
  reader knows the coverage is absent by decision rather than by oversight.

Sweep the whole `apps/admin/e2e/` directory for `/{brandSlug}` URL assumptions and rewrite them to the flat
paths from plan 15. Update fixtures to the merged seed shape from plan 18.

Every scenario must end enabled. `rg "test.fixme|test.skip" apps/admin/e2e/` returning matches means the
work is not done.

Do not leave a scenario passing because its assertion was weakened to nothing — if a scenario's subject is
gone, delete the scenario rather than keeping an empty shell.
</action>
<verify>
<automated>pnpm --filter admin e2e 2>&1 | tail -40</automated>
</verify>
<acceptance_criteria> - `rg -c "test.fixme|test.skip|test.only" apps/admin/e2e/` returns 0. - `rg -in "brand" apps/admin/e2e/` returns no matches. - A scenario asserts the sidebar organization block renders a name and has no click handler. - A scenario asserts a stale second tab reaches `/login?expired=1` after an organization switch in the first. - The spec file contains a comment naming the deferred create-organization GAP where scenarios 7a/7b were. - `pnpm --filter admin e2e` exits 0. - The SUMMARY lists each of the four scenarios with its resolution.
</acceptance_criteria>
<done>No admin e2e scenario is parked, and the cross-tab test now proves the session really dies on switch.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                       |
| ------------------------------------- | --------------------------------------------------------------------------------- |
| Test suite -> merge correctness claim | The suite is what stands between the merge and an undetected isolation regression |
| Tenant A session -> Tenant B data     | The property every isolation spec exists to guard                                 |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                |
| ------------ | ---------------------- | ------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | --------------------------------------------------------------------------------------- |
| T-10.2-19-01 | Information Disclosure | Cross-tenant isolation regressing while the suite goes green because the tests were deleted | mitigate    | Every deletion requires a recorded reason; the SUMMARY reports cross-isolation assertion counts before and after and the after count may not be lower; rewrites keep the isolation shape and change only the dimension         |
| T-10.2-19-02 | Tampering              | RLS teardown removing more than intended, undetected                                        | mitigate    | `tenant-isolation.spec.ts` gains negative-existence assertions for the two dropped functions and all `*_brand_iso` policies, plus a POSITIVE assertion that `orders_location_iso` survives — an over-broad teardown fails here |
| T-10.2-19-03 | Information Disclosure | GDPR erasure incompleteness after PII columns moved tables                                  | mitigate    | `erase-includes-brands.spec.ts` is rewritten rather than deleted, asserting coverage of `legal_name`, `legal_form`, `tax_id` and `stripe_account_id` on `tenants`                                                              |
| T-10.2-19-04 | Information Disclosure | Operator session cookie reaching the guest menu host                                        | mitigate    | The renamed `menu-response.e2e.spec.ts` keeps and extends the no-`Set-Cookie` assertion, plus the D-24 companion for the `.menu.` host                                                                                         |
| T-10.2-19-05 | Repudiation            | Scenarios left skipped, creating an illusion of coverage                                    | mitigate    | `rg -c "test.fixme                                                                                                                                                                                                             | test.skip | test.only"` returning 0 is a hard acceptance criterion in both the API and admin suites |
| T-10.2-19-SC | Tampering              | npm/pip/cargo installs                                                                      | accept      | No packages installed                                                                                                                                                                                                          |

</threat_model>

<verification>
- `find apps/api/test packages/db/test packages/domain/test apps/admin/e2e -iname "*brand*"` returns nothing.
- `rg -in "brand" apps/api/test/ packages/db/test/ packages/domain/test/ apps/admin/e2e/` returns 0.
- `rg -c "\.skip\(|\.fixme\(|\.only\(" apps/api/test/ apps/admin/e2e/` returns 0.
- `pnpm --filter @resto/db test` and `pnpm --filter @resto/domain test` both exit 0.
- `cd apps/api && npx vitest run test/unit` exits 0.
- API e2e specs are run in small batches or individually — never the whole directory in one process
  (project memory: false 429/timeout failures).
- `pnpm --filter admin e2e` exits 0.
</verification>

<success_criteria>

- No test file references a brand and none is skipped.
- Cross-tenant isolation coverage did not shrink; the counts are recorded.
- The RLS teardown is asserted both negatively (brand objects gone) and positively (location policy intact).
- The four parked `adm-00` scenarios are resolved, and the cross-tab one now proves session death.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-19-SUMMARY.md` when done.
</output>
