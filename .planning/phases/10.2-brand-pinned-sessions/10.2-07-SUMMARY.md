---
phase: 10.2-brand-pinned-sessions
plan: 07
subsystem: tenancy
tags: [nestjs, drizzle, zod, ddd, stripe-connect, oauth]

# Dependency graph
requires:
  - phase: 10.2-brand-pinned-sessions (plan 06)
    provides: Tenant aggregate carrying brand-provenance state, TenantRepository.find* returning TenantSnapshot, tenancy event contracts with no brandId
  - phase: 10.2-brand-pinned-sessions (plan 04)
    provides: TenantContext with no brandId field, requireBrandContext/withBrand removed
provides:
  - Location cluster (list/provision/archive services + LocationDrizzleRepository) repaired against the tenant-only model — no plan in the phase owned this before now (F-20)
  - One host resolver (TenantResolverService.resolveByCustomerHost) with no x-brand-slug header path; tenant-and-brand-resolver.service.ts and brand-queries.service.ts deleted outright
  - One surviving Stripe Connect onboarding path (StartTenantOnboardingService) with country/defaultCurrency wired into account creation; the dead no-op tenant-level stub deleted
  - tenancy.module.ts fully clean of brand vocabulary (all providers/controllers/imports) — pulled forward from Task 3's own scope by an eslint gate, not by choice
  - TenantResponseDto extended with country/theme/legal*/stripe* fields for the operator-authenticated /v1/tenants/me surface
affects: [10.2-08, 10.2-10, 10.2-11, 10.2-13, 10.2-14, 10.2-19, 10.2-21]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'ScopedTx.selectFrom(table) with no extraWhere is the correct shape once a query has no second filter dimension left — the tenant filter is auto-applied from the ALS-bound context, never written manually (location-drizzle.repository.ts)'
    - 'RESTO_SPINE_MIGRATION=1 pre-commit opt-out used for all four task commits'

key-files:
  created:
    - apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts
    - apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts
    - apps/api/test/unit/tenancy/start-tenant-onboarding-url.spec.ts
    - apps/api/test/unit/tenancy/tenant-onboarding-oauth.spec.ts
  modified:
    - apps/api/src/contexts/tenancy/application/list-locations.service.ts
    - apps/api/src/contexts/tenancy/application/provision-location.service.ts
    - apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts
    - apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts
    - apps/api/src/contexts/tenancy/application/tenant-queries.service.ts
    - apps/api/src/shared/tenant-context.middleware.ts
    - apps/api/src/contexts/tenancy/domain/oauth-state.ts
    - apps/api/src/contexts/tenancy/tenancy.module.ts
    - apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts
    - apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts
    - apps/api/test/unit/tenancy/archive-location.service.spec.ts
    - apps/api/test/unit/tenancy/provision-location.service.spec.ts
    - apps/api/test/unit/shared/tenant-context.middleware.spec.ts
    - apps/api/test/unit/tenancy/tenant-resolver.service.spec.ts
    - apps/api/test/unit/tenancy/tenant-queries.service.spec.ts
  deleted:
    - apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts
    - apps/api/src/contexts/tenancy/application/brand-queries.service.ts
    - apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts
    - apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts (renamed into start-tenant-onboarding.service.ts)
    - apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts
    - apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts (renamed into tenant-onboarding.controller.ts)
    - apps/api/test/unit/tenancy/tenant-and-brand-resolver.service.spec.ts
    - apps/api/test/unit/tenancy/start-brand-onboarding-url.spec.ts (renamed into start-tenant-onboarding-url.spec.ts)
    - apps/api/test/unit/tenancy/brand-onboarding-oauth.spec.ts (renamed into tenant-onboarding-oauth.spec.ts)

key-decisions:
  - "Task 3's tenancy.module.ts cleanup (removing ProvisionBrandService, BrandQueriesService, TenantAndBrandResolverService, BRAND_REPOSITORY, BrandDrizzleRepository) landed inside Task 2's commit, not Task 3's, forced by @typescript-eslint/no-unsafe-assignment poisoning the whole providers/exports array literal once any one element resolves to an unresolvable import. See Deviations."
  - "oauth-state.ts's OAuthStatePayload.brandId renamed to tenantId — unowned by any plan (grep -l confirms only this plan cites it), and renaming closed 5 of 6 pre-existing brand matches inside contexts/tenancy/application/. The 6th (CreateOnboardingAccountInput.brandId in payments/domain/ports.ts) is plan 10's field name, left alone per the no-shimming-across-a-concurrent-plan's-file rule."
  - 'Stripe onboarding redirect URL after OAuth callback changed from `${ADMIN_WEB_URL}/${slug}/payouts` to `${ADMIN_WEB_URL}/payouts` — D-20 removes the `/{brandSlug}` path segment from admin paths entirely; the merged model has no second slug to embed once brand IS tenant.'
  - '@RequireBrand()/@BrandNeutral() decorators and their imports were left untouched in every file this plan touched (locations.controller.ts, tenants.controller.ts, tenant-onboarding.controller.ts, internal-tenants.controller.ts) — explicitly plan 11''s ownership per this plan''s own read_first notes ("plan 11 owns that decorator, do not remove it here"), repeated twice in the plan text. This is why `rg -in "brand" apps/api/src/contexts/tenancy/` does not return zero — see Self-Check.'
  - 'D-22 host tightening: TenantResolverService.resolveByCustomerHost''s subdomain-slug branch now requires the second label to be exactly "menu" (labels[1] === "menu"), not just ">2 labels + not reserved". The pre-merge matcher (TenantAndBrandResolverService.resolveByCustomerHost) would have accepted a bare `<slug>.resto.app` as a guest-menu host — the exact host D-22 reserves for the future public website. The custom-domain branch (tenant_domains lookup) is unchanged — those rows are explicitly registered, not pattern-matched.'

patterns-established:
  - 'A repository method whose sole remaining filter dimension is the tenant should call scoped.selectFrom(table) with no extraWhere — ScopedTx already applies eq(table.tenantId, ctx.tenantId) from the ALS-bound context (packages/db/src/client.ts:80). Writing it manually is redundant, not defense-in-depth, once there is no second predicate to AND against it.'

requirements-completed: []

# Metrics
duration: ~50min
completed: 2026-08-20
---

# Phase 10.2 Plan 07: Collapse Tenancy HTTP/Resolution Surface onto Tenant Summary

**One host resolver (`TenantResolverService.resolveByCustomerHost`, tightened to the `.menu.` label per D-22), one Stripe Connect onboarding path (`StartTenantOnboardingService`, now wiring `country`/`defaultCurrency` into account creation), and a fully brand-vocabulary-free `tenancy.module.ts` — plus the four-file location cluster that no other plan in this phase owned, repaired and routed entirely through `ScopedTx`'s auto-applied tenant filter.**

## Performance

- **Duration:** ~50 min
- **Started:** 2026-08-20T12:10:00Z (approx.)
- **Completed:** 2026-08-20T12:54:24Z
- **Tasks:** 4 of 4 committed (Task 0 through Task 3, plan's stated order)
- **Files modified:** 25 (9 deleted/renamed-away, 4 created, 15 modified — 3 files counted twice across delete+create for renames)

## Accomplishments

- **Task 0 — absorbed the unowned location cluster (F-20).** `location-drizzle.repository.ts` no longer defines `listForBrand`; it implements the port's `listForTenant(tenantId)` and routes every read/write through `ScopedTx`, whose tenant filter is now the _only_ filter on the query (see Threat Note below). `list-locations.service.ts`/`provision-location.service.ts` drop `requireBrandContext`/`BrandId` entirely.
- **Task 1 — one host resolver, no `x-brand-slug`.** `tenant-and-brand-resolver.service.ts` and `brand-queries.service.ts` deleted outright (D-40); `TenantResolverService` absorbs `resolveByCustomerHost`, tightened to the `.menu.` label so a bare `<slug>.resto.app` no longer resolves a guest-menu tenant (D-22). `tenant-context.middleware.ts` no longer reads `x-brand-slug` in any form.
- **Task 2 — one Stripe onboarding path.** `start-stripe-onboarding.service.ts` (confirmed dead: no-op `linkStripeAccount`, hardcoded `not_started` status) and its controller deleted. `start-brand-onboarding.service.ts` → `start-tenant-onboarding.service.ts` (`StartTenantOnboardingService`), rewired off `Tenant`/`TENANT_REPOSITORY` instead of the deleted `Brand`/`BrandRepository`, and now passes `country`/`defaultCurrency` from the tenant snapshot into `PaymentProviderPort.ensureOnboardingAccount` — the wire D-35 left unconnected.
- **Task 3 — HTTP surface cleanup.** `tenant-response.ts` gained `country`, `theme`, `legalName`, `legalForm`, `taxId`, `paymentProvider`, `accountType`, and the four `stripe*` fields, using unbranded `CountryCodeValue`/`CurrencyValue`/`TenantSlugValue`/`TenantTheme` at the boundary. `locations.controller.ts` no longer exposes `brandId` in its response schema. `tenants.controller.ts`, `internal-tenants.controller.ts`, `error-mapping.ts` needed zero changes — already fully tenant-named (confirmed by direct `rg`, not assumed).
- **`apps/api/src/contexts/tenancy/` typechecks with zero errors** and `apps/api/src/shared/tenant-context.middleware.ts` typechecks with zero errors (both confirmed via `npx tsc -p apps/api/tsconfig.json --noEmit | grep -c "^src/contexts/tenancy"` → 0, and the middleware-path grep → 0).
- **55/55 tests pass** across the seven specs this plan owns: `tenant-context.middleware.spec.ts` (10), `tenant-resolver.service.spec.ts` (17), `tenant-queries.service.spec.ts` (8), `archive-location.service.spec.ts` (4), `provision-location.service.spec.ts` (3), `start-tenant-onboarding-url.spec.ts` (2), `tenant-onboarding-oauth.spec.ts` (11).

## Task Commits

1. **Task 0: Absorb the location cluster** - `3717225a` (fix)
2. **Task 1: Collapse host resolution, drop x-brand-slug** - `eccb708a` (feat)
   - follow-up doc commit - `62c3afef` (docs) — logged the `brand-queries.service.ts` deletion's ripple into plans 08/10's files
3. **Task 2: Delete dead Stripe path, rename live path** - `b9bd4b9d` (feat) — includes the forced `tenancy.module.ts` full cleanup, see Deviations
4. **Task 3: Rename remaining controllers, response shapes** - `8ed0703f` (feat)

All four task commits used `RESTO_SPINE_MIGRATION=1` — verified genuine tracked content in `.husky/pre-commit` (`git log --oneline -- .husky/pre-commit` → `3403657e build: allow spine migrations to opt out of affected typecheck`) before relying on it. `lint-staged` (prettier + eslint) ran on every commit attempt, unskipped — two attempts failed on real eslint errors (`no-unsafe-assignment` on `tenancy.module.ts`, `no-non-null-assertion` on a since-rewritten test helper) and were fixed and re-committed.

## Files Created/Modified

### Task 0 (`3717225a`)

- `apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts` — `listForBrand(brandId, tenantId)` → `listForTenant(_tenantId)`; every `ROW_TO_SNAPSHOT`/`save`/insert no longer reads or writes `brandId`
- `apps/api/src/contexts/tenancy/application/list-locations.service.ts` — drops `requireBrandContext`/`BrandId`, calls `listForTenant`
- `apps/api/src/contexts/tenancy/application/provision-location.service.ts` — drops `requireBrandContext`/`BrandId` from the snapshot construction and log line
- `apps/api/test/unit/tenancy/archive-location.service.spec.ts` — fixture cleanup (`BRAND_ID`/`brandId` removed, `listForBrand` mock → `listForTenant`)
- `apps/api/test/unit/tenancy/provision-location.service.spec.ts` — rewritten; see Deviations for the deleted brand-context test

### Task 1 (`eccb708a`, docs follow-up `62c3afef`)

- `apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts` — deleted
- `apps/api/src/contexts/tenancy/application/brand-queries.service.ts` — deleted
- `apps/api/src/contexts/tenancy/application/tenant-resolver.service.ts` — gains `resolveByCustomerHost`, `resolveByHost`/`resolveBySlug`/`resolveById` no longer call `.toSnapshot()` (repo now returns `TenantSnapshot` directly, per plan 06)
- `apps/api/src/contexts/tenancy/application/tenant-queries.service.ts` — same `.toSnapshot()` fix on `findBySlug`/`findById`
- `apps/api/src/shared/tenant-context.middleware.ts` — `HEADER_BRAND`/`x-brand-slug` block deleted; `resolveByCustomerHost` returns `{ tenantId, ...(locationId ? {...} : {}) }`; `x-location-id` handling and `runInTenantContext` usage untouched
- `apps/api/test/unit/tenancy/tenant-and-brand-resolver.service.spec.ts` — deleted (see Deviations)
- `apps/api/test/unit/tenancy/tenant-resolver.service.spec.ts` — gained a `resolveByCustomerHost` describe block porting the surviving coverage
- `apps/api/test/unit/tenancy/tenant-queries.service.spec.ts` — fixture fixes (`country` not `defaultCurrency`, `findByStripeAccountId` mock added, `.toSnapshot()` calls on mocked results)
- `apps/api/test/unit/shared/tenant-context.middleware.spec.ts` — `BrandRepository`/`TenantAndBrandResolverService` removed from setup; one test's host changed from a `.menu.` host (which now resolves via the customer path) to an `.admin.` host to keep exercising the intended `resolveByHost` fallback path
- `.planning/phases/10.2-brand-pinned-sessions/deferred-items.md` — logged the `brand-queries.service.ts` deletion's effect on plan 08's/10's already-broken files

### Task 2 (`b9bd4b9d`)

- `apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts` — deleted
- `apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts` — deleted
- `apps/api/src/contexts/tenancy/application/start-brand-onboarding.service.ts` → `start-tenant-onboarding.service.ts` — rewritten off `Tenant`/`TENANT_REPOSITORY`, drops the `slug` parameter from every method (resolves via `requireTenantContext()` + `findCurrentTenant()`/`findById`), wraps the account-creation call in `StripeOnboardingFailedError`, passes `country`/`defaultCurrency` to `ensureOnboardingAccount`
- `apps/api/src/contexts/tenancy/interfaces/http/brand-onboarding.controller.ts` → `tenant-onboarding.controller.ts` — `TenantOnboardingController`/`TenantOAuthCallbackController`, routes converge onto `v1/tenancy/onboarding/*` (both controllers share the prefix, distinct suffixes)
- `apps/api/src/contexts/tenancy/domain/oauth-state.ts` — `OAuthStatePayload.brandId` → `tenantId`
- `apps/api/src/contexts/tenancy/tenancy.module.ts` — full brand-vocabulary cleanup (see Deviations)
- `apps/api/test/unit/tenancy/start-brand-onboarding-url.spec.ts` → `start-tenant-onboarding-url.spec.ts` — rewritten against `TenantSnapshot`; the arbitrary-slug parametrized test dropped (see Deviations)
- `apps/api/test/unit/tenancy/brand-onboarding-oauth.spec.ts` → `tenant-onboarding-oauth.spec.ts` — rewritten against `TenantSnapshot`/`TenantRepository`; `slugFromPath` param dropped from every call (service no longer takes one)

### Task 3 (`8ed0703f`)

- `apps/api/src/contexts/tenancy/interfaces/http/tenant-response.ts` — extended with `country`, `theme`, `legalName`, `legalForm`, `taxId`, `paymentProvider`, `accountType`, `stripeChargesEnabled`, `stripePayoutsEnabled`, `stripeOnboardingStatus`, `stripeRequirementsDue`
- `apps/api/src/contexts/tenancy/interfaces/http/locations.controller.ts` — `brandId` removed from `LocationResponseSchema` and `toResponse`

## Decisions Made

- See `key-decisions` in frontmatter for the five load-bearing ones. Restated briefly: (1) `tenancy.module.ts`'s full cleanup moved from Task 3 to Task 2, forced by tooling; (2) `oauth-state.ts`'s field renamed since unowned and directly touched; (3) OAuth redirect URL drops its slug segment per D-20; (4) `@RequireBrand()`/`@BrandNeutral()` left alone, plan 11's territory; (5) D-22 host-matcher tightened to `.menu.` only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `tenancy.module.ts`'s full brand-vocabulary cleanup could not wait for Task 3**

- **Found during:** Task 2, first commit attempt
- **Issue:** Task 2's own acceptance criteria require `rg -c "StartStripeOnboardingService|stripe-onboarding" apps/api/src` to return 0 and `rg -n "StartTenantOnboardingService" apps/api/src/contexts/tenancy/tenancy.module.ts` to match — both require editing `tenancy.module.ts`, which the plan's own `<files>` tags list under Task 3, not Task 2. A first, minimal edit (swapping only the four Stripe-onboarding-specific lines) could not be **committed**: `@typescript-eslint/no-unsafe-assignment` failed on the whole `providers`/`exports` array literals, because `BrandDrizzleRepository` (imported from a file plan 06 already deleted) resolves to `any` and ESLint's contextual-typing check poisons the entire array, not just that one element. Removing only that pair left the array-level `any[]` errors still firing — the three OTHER already-dead imports (`ProvisionBrandService`, `BrandQueriesService`, `TenantAndBrandResolverService`) had to go too before `npx eslint apps/api/src/contexts/tenancy/tenancy.module.ts --max-warnings=0` passed clean.
- **Fix:** Removed all five dead/duplicate registrations (`BRAND_REPOSITORY`/`BrandDrizzleRepository`, `ProvisionBrandService`, `BrandQueriesService`, `TenantAndBrandResolverService`) from imports, `providers`, and `exports` — effectively completing Task 3's module-wiring scope inside Task 2's commit.
- **Files modified:** `apps/api/src/contexts/tenancy/tenancy.module.ts`
- **Verification:** `npx eslint apps/api/src/contexts/tenancy/tenancy.module.ts --max-warnings=0` — clean. `npx tsc -p apps/api/tsconfig.json --noEmit | grep "^src/contexts/tenancy"` — zero matches, including after Task 3's own edits landed (confirms nothing left for Task 3 to fix here).
- **Verified safe** (no new breakage in a previously-working file): every outside consumer of the five removed classes was already broken before this edit — `notifications/application/send-guest-notification.service.ts` (+spec, plan 10, mid-rewrite), `identity/infrastructure/brand-provisioning.adapter.ts` (+port, +spec, plan 08, deleted outright by its own Task 1), and the unowned `test/unit/tenancy/provision-brand.service.spec.ts`. None of these files could resolve `BRAND_REPOSITORY`/`BrandRepository` from `tenancy/domain/ports.ts` regardless of what `tenancy.module.ts` does — plan 06 already removed those exports.
- **Committed in:** `b9bd4b9d` (Task 2 commit)

**2. [Rule 1 - Bug] `@typescript-eslint/no-non-null-assertion` blocked a commit twice due to a staging mistake, not a real code issue**

- **Found during:** Task 2, second and third commit attempts
- **Issue:** `tenant-onboarding-oauth.spec.ts` originally cast `(repo.save as ReturnType<typeof vi.fn>).mock.calls.at(0)?.at(0) as Tenant | undefined` then re-cast `(savedTenant as Tenant)` two lines later — ESLint's rule flagged this double-cast pattern. Fixed once, but the fix wasn't re-staged before the next `git commit` attempt (the working-tree edit existed, the git index still held the pre-fix content), so the exact same error recurred on the next attempt.
- **Fix:** Replaced the double-cast with a captured closure variable (`let savedTenant: Tenant | undefined` set inside `save: vi.fn().mockImplementation(...)`), then `git add` the file before retrying.
- **Files modified:** `apps/api/test/unit/tenancy/tenant-onboarding-oauth.spec.ts`
- **Verification:** `npx vitest run test/unit/tenancy/tenant-onboarding-oauth.spec.ts` — 11/11 pass, both before and after; `npx eslint` clean.
- **Committed in:** `b9bd4b9d` (Task 2 commit)

**3. [Rule 1 - Bug] `tenant-context.middleware.spec.ts`'s host-fallback test collided with the new customer-host-first resolution order**

- **Found during:** Task 1, running the middleware spec after rewriting `resolveContext`
- **Issue:** "still resolves via host when no header is present" configured `repo.findByDomainHost` to resolve a tenant for ANY host and used `cafe-a.menu.resto.app` — a `.menu.` host. Pre-merge, the customer/brand path used a SEPARATE `BrandRepository` mock (always null in this test), so it fell through to the operator-host fallback the test meant to exercise. Post-merge, `resolveByCustomerHost` and `resolveByHost` share the same `TenantRepository` mock, so the customer path now resolves first and the test's own assertion (`resolveByHost` was called) went unmet — not a product regression, a test whose mock shape predates the merge.
- **Fix:** Changed the test's host to `cafe-a.admin.resto.app` (operator/admin host — doesn't match `.menu.`, exercises the intended fallback) and its repo mock to `findBySlug` (matching the subdomain-extraction path `resolveByHost` actually takes for a non-domain-row host).
- **Files modified:** `apps/api/test/unit/shared/tenant-context.middleware.spec.ts`
- **Verification:** `npx vitest run test/unit/shared/tenant-context.middleware.spec.ts` — 10/10 pass.
- **Committed in:** `eccb708a` (Task 1 commit)

---

**Total deviations:** 3 (1 Rule 3 blocking-issue fix that pulled Task 3 scope into Task 2, 2 Rule 1 bug fixes). No scope creep beyond what the eslint gate and pre-existing test/mock shape mismatches forced.
**Impact on plan:** Task 3's remaining files (`tenants.controller.ts`, `internal-tenants.controller.ts`, `error-mapping.ts`) needed zero further changes at Task 3's own commit — confirmed by direct `rg`, not assumed — because Task 2's forced module cleanup already covered everything Task 3 would have touched in `tenancy.module.ts`.

## Known Stubs

None — no hardcoded empty values, placeholder text, or unwired data sources introduced by this plan.

## Threat Flags

None beyond the plan's own threat register (all five threats — T-10.2-07-01 through -05 — were the plan's own stated mitigations, executed as specified: D-22 host tightening, `x-brand-slug` deletion, `tenant-response.ts` confirmed unreachable from public routes, `runInTenantContext` confirmed middleware-only, and the correct live Stripe path confirmed by its real webhook-driven columns vs. the dead path's hardcoded `not_started`).

**Surviving `ScopedTx` tenant-scoping — quoted per the amendment's explicit request:**

```ts
// apps/api/src/contexts/tenancy/infrastructure/location-drizzle.repository.ts
async listForTenant(_tenantId: TenantId): Promise<readonly LocationSnapshot[]> {
  // ADR-0020 I-1: `scoped.selectFrom` auto-applies `eq(table.tenantId, ...)`
  // from the ALS-bound context — this is now the ONLY filter on this query,
  // not a redundant one alongside a dropped brand predicate.
  return this.withTenant(async (scoped) => {
    const rows = await scoped
      .selectFrom(schema.locations)
      .orderBy(asc(schema.locations.createdAt));
    return rows.map(ROW_TO_SNAPSHOT);
  });
}
```

`findById`, `save`, and `countScopedMembers` in the same file all route through `this.withTenant(...)` too (`selectOne` base-class helper for `findById`; explicit `scoped.insertInto`/`onConflictDoUpdate` for `save`; `scoped.selectFrom` for `countScopedMembers`). `ScopedTx.selectFrom` (`packages/db/src/client.ts:73-86`) auto-applies `eq(table.tenantId, this.tenantId)` from the ALS-bound context before any caller-supplied `extraWhere` — with the `brandId` predicate gone, `listForTenant` now supplies no `extraWhere` at all, relying entirely on that auto-applied filter. This was verified, not assumed: `ScopedTx`'s implementation was read directly (not cited from a comment) before writing this method.

## Issues Encountered

- **`rg -in "brand" apps/api/src/contexts/tenancy/` does not return zero** (Task 3's literal acceptance criterion). Full list of remaining matches, all accounted for:
  - `@RequireBrand()`/`RequireBrand` import — `locations.controller.ts`, `tenant-onboarding.controller.ts` (3 usages + 1 import each)
  - `@BrandNeutral()`/`BrandNeutral` import — `tenants.controller.ts`, `tenant-onboarding.controller.ts`, `internal-tenants.controller.ts`
  - Both decorators are explicitly plan 11's ownership per this plan's own read_first notes, repeated in both Task 1's context (`@BrandNeutral()`, Task 3 read_first) and confirmed again for `locations.controller.ts` — left untouched deliberately, not an oversight.
  - `location-drizzle.repository.ts:49` — a WHY-comment explaining the _historical_ brand predicate that was dropped (see Threat Flags quote above) — prose, not vocabulary surviving in code.
  - `start-tenant-onboarding.service.ts:72` — `brandId: snapshot.id` supplied into `PaymentProviderPort.ensureOnboardingAccount`'s `CreateOnboardingAccountInput.brandId` field, a port owned by plan 10 (concurrent worktree) — logged to `deferred-items.md` for whoever renames that field.
- **`rg -n "listForBrand" apps/api/src` returns 2 matches, not 0** — both in `apps/api/src/contexts/ordering/application/{list-orders,get-order-detail}.service.ts`, owned by plan 10 (`grep -l` confirmed). Zero matches inside `apps/api/src/contexts/tenancy/` — the scope this plan actually owns.
- **`rg -n "BRAND_REPOSITORY|BrandRepository|BrandQueriesService|TenantAndBrandResolverService" apps/api/src` returns 13 matches, not 0** — all in `payments`/`notifications`/`identity`, all owned by plans 08/10 (concurrent). Zero matches inside `apps/api/src/contexts/tenancy/`.
- None of the above are silent gaps — every one is either a documented, deliberate exclusion (decorators, WHY-comment) or logged to `deferred-items.md` with the owning plan named via mechanical `grep -l`, per this phase's established norm.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `apps/api/src/contexts/tenancy/` and `apps/api/src/shared/tenant-context.middleware.ts` typecheck clean (0 errors), confirmed via direct `tsc` run, not inferred.
- `tenancy.module.ts` needs no further brand-vocabulary cleanup — fully done, ahead of its originally-planned task boundary.
- Plans 08 and 10 can proceed independently: neither had a live dependency on anything this plan deleted (`BrandQueriesService`, `TenantAndBrandResolverService`) that wasn't already broken before this plan touched it.
- Plan 10 (`payments/domain/ports.ts`'s `CreateOnboardingAccountInput.brandId` field) and plan 19 (green-gate sweep of the still-dead `test/unit/tenancy/{brand-aggregate,provision-brand.service}.spec.ts`, unowned by any plan, plus the two `listForBrand` call sites in `ordering/`) both have concrete, named follow-ups in `deferred-items.md`.
- Plan 11's decorator sweep will find `@RequireBrand()`/`@BrandNeutral()` exactly where it left them across this plan's files — no collision expected.

## Self-Check

- `apps/api/src/contexts/tenancy/application/tenant-and-brand-resolver.service.ts` — CONFIRMED ABSENT
- `apps/api/src/contexts/tenancy/application/brand-queries.service.ts` — CONFIRMED ABSENT
- `apps/api/src/contexts/tenancy/application/start-stripe-onboarding.service.ts` — CONFIRMED ABSENT
- `apps/api/src/contexts/tenancy/interfaces/http/stripe-onboarding.controller.ts` — CONFIRMED ABSENT
- `apps/api/src/contexts/tenancy/application/start-tenant-onboarding.service.ts` — FOUND, contains `country`/`defaultCurrency` wiring
- `apps/api/src/contexts/tenancy/interfaces/http/tenant-onboarding.controller.ts` — FOUND, `TenantOnboardingController`/`TenantOAuthCallbackController` both present
- `.planning/phases/10.2-brand-pinned-sessions/deferred-items.md` — FOUND, updated with plan-07 entries
- Commit `3717225a` — FOUND in `git log --oneline`
- Commit `eccb708a` — FOUND in `git log --oneline`
- Commit `62c3afef` — FOUND in `git log --oneline`
- Commit `b9bd4b9d` — FOUND in `git log --oneline`
- Commit `8ed0703f` — FOUND in `git log --oneline`
- `cd apps/api && npx tsc -p tsconfig.json --noEmit | grep -c "^src/contexts/tenancy"` — 0
- `cd apps/api && npx tsc -p tsconfig.json --noEmit | grep -c "shared/tenant-context.middleware"` — 0
- `cd apps/api && npx vitest run test/unit/shared/tenant-context.middleware.spec.ts test/unit/tenancy/tenant-resolver.service.spec.ts test/unit/tenancy/tenant-queries.service.spec.ts test/unit/tenancy/archive-location.service.spec.ts test/unit/tenancy/provision-location.service.spec.ts test/unit/tenancy/start-tenant-onboarding-url.spec.ts test/unit/tenancy/tenant-onboarding-oauth.spec.ts` — 55/55 pass
- `git status --short` — clean, nothing uncommitted

## Self-Check: PASSED

---

_Phase: 10.2-brand-pinned-sessions_
_Completed: 2026-08-20_
