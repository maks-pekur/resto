---
phase: 10.2-brand-pinned-sessions
plan: 08
type: execute
wave: 5
depends_on: ['10.2-06']
files_modified:
  - apps/api/src/contexts/identity/application/set-active-brand.service.ts
  - apps/api/src/contexts/identity/application/create-my-brand.service.ts
  - apps/api/src/contexts/identity/application/list-my-brands.service.ts
  - apps/api/src/contexts/identity/application/list-my-tenants.service.ts
  - apps/api/src/contexts/identity/application/check-brand-slug-availability.service.ts
  - apps/api/src/contexts/identity/application/check-tenant-slug-availability.service.ts
  - apps/api/src/contexts/identity/application/ports/brand-provisioning.port.ts
  - apps/api/src/contexts/identity/application/ports/member-brand-scope-reader.port.ts
  - apps/api/src/contexts/identity/application/ports/session-active-brand-writer.port.ts
  - apps/api/src/contexts/identity/infrastructure/brand-provisioning.adapter.ts
  - apps/api/src/contexts/identity/infrastructure/initial-brand-drizzle.repository.ts
  - apps/api/src/contexts/identity/infrastructure/member-brand-scope-drizzle.reader.ts
  - apps/api/src/contexts/identity/infrastructure/better-auth/session-active-brand.adapter.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/brand-slug-rate-limit.guard.ts
  - apps/api/src/contexts/identity/interfaces/http/guards/tenant-slug-rate-limit.guard.ts
  - apps/api/src/contexts/identity/interfaces/http/me-brands.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts
  - apps/api/src/contexts/identity/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/identity/interfaces/http/index.ts
  - apps/api/src/contexts/identity/domain/brand-errors.ts
  - apps/api/src/contexts/identity/domain/principal.ts
  - apps/api/src/contexts/identity/identity-http.module.ts
  - apps/api/src/contexts/identity/identity.tokens.ts
autonomous: true
requirements: []
decisions: [D-02, D-07, D-10, D-14, D-19, D-39, D-40]

must_haves:
  truths:
    - 'D-10: `BrandScopeGuard`, `SetActiveBrandService`, its controller, the member-brand-scope reader and the initial-brand repository do not exist'
    - "D-14: no application code writes a brand pin onto a session; Better Auth's `activeOrganizationId` is the only context pin"
    - 'D-02: an owner belonging to several organizations can list them from the API, which is what the sign-in picker renders'
    - 'D-19: `LocationScopeGuard` and location scoping survive the guard-chain change intact'
    - 'The slug-availability check survives renamed, still rate-limited — it is what onboarding needs to resolve slug collisions server-side'
  artifacts:
    - path: 'apps/api/src/contexts/identity/application/list-my-tenants.service.ts'
      provides: 'the organizations a signed-in user belongs to'
    - path: 'apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts'
      provides: 'GET /v1/me/tenants and GET /v1/me/tenants/slug-availability'
    - path: 'apps/api/src/contexts/identity/identity-http.module.ts'
      provides: 'four global APP_GUARDs, BrandScopeGuard removed'
  key_links:
    - from: 'apps/api/src/contexts/identity/identity-http.module.ts'
      to: 'apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts'
      via: 'APP_GUARD registration order preserved'
      pattern: 'LocationScopeGuard'
    - from: 'apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts'
      to: 'apps/api/src/contexts/identity/application/list-my-tenants.service.ts'
      via: 'constructor injection'
      pattern: 'ListMyTenantsService'
---

<objective>
Delete the identity context's brand machinery — the scope guard, the active-brand pin and its controller,
the member-brand-scope reader, the initial-brand repository — and rename what survives: the "my
organizations" list the sign-in picker needs, and the rate-limited slug-availability check onboarding needs.

Purpose: this removes the parallel context pin D-14 forbids, and produces the two endpoints the admin's new
auth surfaces (plan 17) call.
Output: `apps/api/src/contexts/identity` has no brand vocabulary and registers four global guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-CONTEXT.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md
@CLAUDE.md
@.planning/phases/10.2-brand-pinned-sessions/10.2-06-SUMMARY.md

<interfaces>
Endpoints this plan LOCKS. Plan 14 (admin lib) and plan 17 (pick-organization, onboarding) call them.

GET /v1/me/tenants
-> { tenants: Array<{ id: string; slug: string; displayName: string; status: string }> }
The organizations the signed-in user is a member of. Renders the sign-in picker (D-02/D-17) and the
static organization-identity block in the sidebar. `slug` is required by the picker because the success
path is a hard navigation to `<slug>.admin.<domain>`.

GET /v1/me/tenants/slug-availability?slug=<candidate>
-> { available: boolean; suggestion?: string }
Rate-limited by TenantSlugRateLimitGuard. Backs the SERVER-SIDE silent collision resolution at
onboarding; the UI never renders a "taken" error (UI-SPEC S4).

DELETED with no replacement in this phase:
POST /v1/me/brands (create a second organization — CONTEXT.md deferred GAP)
POST /v1/me/set-active-brand (superseded by the BA switch-organization endpoint in plan 12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the brand scope guard, the active-brand pin and their supporting ports and adapters</name>
  <files>apps/api/src/contexts/identity/application/set-active-brand.service.ts, apps/api/src/contexts/identity/application/create-my-brand.service.ts, apps/api/src/contexts/identity/application/ports/brand-provisioning.port.ts, apps/api/src/contexts/identity/application/ports/member-brand-scope-reader.port.ts, apps/api/src/contexts/identity/application/ports/session-active-brand-writer.port.ts, apps/api/src/contexts/identity/infrastructure/brand-provisioning.adapter.ts, apps/api/src/contexts/identity/infrastructure/initial-brand-drizzle.repository.ts, apps/api/src/contexts/identity/infrastructure/member-brand-scope-drizzle.reader.ts, apps/api/src/contexts/identity/infrastructure/better-auth/session-active-brand.adapter.ts, apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts, apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts, apps/api/src/contexts/identity/domain/brand-errors.ts, apps/api/src/contexts/identity/identity-http.module.ts, apps/api/src/contexts/identity/identity.tokens.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/interfaces/http/guards/brand-scope.guard.ts (reads `getBrandId()` from `@resto/db` — already removed in plan 04 — plus `MEMBER_BRAND_SCOPE_READER` and `BRAND_NEUTRAL_KEY`)
    - apps/api/src/contexts/identity/interfaces/http/guards/location-scope.guard.ts (the structurally identical sibling that SURVIVES — its `ForbiddenException` code shape `{resource}.context_required` / `.operator_required` / `.tenant_required` / `.access_denied` and its `principal.baseRole === 'owner'` bypass are the template for what "unchanged" looks like)
    - apps/api/src/contexts/identity/application/set-active-brand.service.ts (depends on `BRAND_PROVISIONING_PORT`, `SESSION_ACTIVE_BRAND_WRITER`, `SESSION_ACTIVE_LOCATION_WRITER`, `InitialLocationDrizzleRepository` — note the LOCATION writer and repository SURVIVE)
    - apps/api/src/contexts/identity/interfaces/http/set-active-brand.controller.ts (87 lines: `@Post('set-active-brand')` under `v1/me`, `@BrandNeutral()`, `@Permissions({ tenant: ['read'] })`, and a local `mapError` composing `BrandOutOfScopeError` / `NonOwnerBrandSwitchForbiddenError` with `mapIdentityError`)
    - apps/api/src/contexts/identity/identity-http.module.ts (~lines 95-100: five `provide: APP_GUARD` registrations plus the `MEMBER_BRAND_SCOPE_READER` token binding)
    - apps/api/src/contexts/identity/identity.tokens.ts (Symbol DI tokens to remove)
    - apps/api/src/contexts/identity/domain/brand-errors.ts and domain/principal.ts (the error classes being deleted, and the `Principal` shape which must keep `tenantId`/`baseRole`)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-PATTERNS.md §3 (deletion targets with grep-verified dependents) and §F4 (the guard chain is FIVE global guards, not six — `RateLimitGuard` is never registered as `APP_GUARD`)
  </read_first>
  <action>
Delete these files outright (D-10, D-39 — no shims, no `@deprecated` aliases):
`set-active-brand.service.ts`, `set-active-brand.controller.ts`, `create-my-brand.service.ts`,
`ports/brand-provisioning.port.ts`, `ports/member-brand-scope-reader.port.ts`,
`ports/session-active-brand-writer.port.ts`, `infrastructure/brand-provisioning.adapter.ts`,
`infrastructure/initial-brand-drizzle.repository.ts`, `infrastructure/member-brand-scope-drizzle.reader.ts`,
`infrastructure/better-auth/session-active-brand.adapter.ts`,
`interfaces/http/guards/brand-scope.guard.ts`, `domain/brand-errors.ts`.

`create-my-brand.service.ts` and its `POST /v1/me/brands` route are deleted with NO replacement this phase.
CONTEXT.md's deferred GAP is explicit: after this phase an owner cannot create a SECOND restaurant. The
first one still has a path — plan 13's onboarding finalises the organization `/v1/signup` created. Do not
invent a create-organization surface here.

`identity-http.module.ts`:

- remove the `{ provide: APP_GUARD, useClass: BrandScopeGuard }` registration. The chain becomes FOUR global
  guards in this exact order: `AuthGuard -> PermissionsGuard -> LocationScopeGuard -> OwnerOnlyGuard`.
  Note the correction to CONTEXT.md's "six global guards": there were five, and `RateLimitGuard` is not one
  of them — it is never registered as `APP_GUARD` anywhere in the repo. Record the before/after list in the
  SUMMARY;
- remove the `MEMBER_BRAND_SCOPE_READER` token binding and every provider for a deleted class;
- leave `LocationScopeGuard`, `OwnerOnlyGuard`, `MEMBER_LOCATION_SCOPE_READER`,
  `SESSION_ACTIVE_LOCATION_WRITER` and `InitialLocationDrizzleRepository` untouched (D-19).

`identity.tokens.ts`: remove `BRAND_PROVISIONING_PORT`, `MEMBER_BRAND_SCOPE_READER`,
`SESSION_ACTIVE_BRAND_WRITER` and any sibling brand token. Keep every location token.

`interfaces/http/error-mapping.ts` and `interfaces/http/index.ts`: drop mappings and re-exports for the
deleted error classes and the deleted controller.

`domain/principal.ts`: verify it carries no brand field; if it does, remove it. `tenantId` and `baseRole`
stay — `AuthGuard`'s `auth.tenant_mismatch` cross-check is the surviving fence and this plan must not touch it.

Do NOT delete `initial-location-drizzle.repository.ts` — its name is one character away from the file that
IS deleted, and it is live location machinery.
</action>
<verify>
<automated>cd apps/api && npx vitest run test/unit/identity/location-scope.guard.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - All twelve named files are absent: `for f in set-active-brand.service.ts set-active-brand.controller.ts create-my-brand.service.ts; do test ! -f "apps/api/src/contexts/identity/application/$f" -o ! -f "apps/api/src/contexts/identity/interfaces/http/$f"; done` and a `find apps/api/src/contexts/identity -iname '*brand*'` listing only `list-my-brands.service.ts`, `check-brand-slug-availability.service.ts`, `brand-slug-rate-limit.guard.ts` and `me-brands.controller.ts` (the four Task 2 renames). - `rg -c "BrandScopeGuard|MEMBER_BRAND_SCOPE_READER|SESSION_ACTIVE_BRAND_WRITER|BRAND_PROVISIONING_PORT|InitialBrandDrizzleRepository" apps/api/src` returns 0. - `rg -c "APP_GUARD" apps/api/src/contexts/identity/identity-http.module.ts` equals 4. - `rg -n "LocationScopeGuard" apps/api/src/contexts/identity/identity-http.module.ts` and `rg -n "OwnerOnlyGuard" apps/api/src/contexts/identity/identity-http.module.ts` both still match. - `test -f apps/api/src/contexts/identity/infrastructure/initial-location-drizzle.repository.ts` succeeds (the similarly-named survivor was not collaterally deleted). - `rg -n "auth.tenant_mismatch" apps/api/src/contexts/identity/interfaces/http/guards/auth.guard.ts` still matches. - The SUMMARY records the guard chain before (5) and after (4) with names in order.
</acceptance_criteria>
<done>Nothing in the identity context can pin, read or enforce a brand; the location fence and the tenant cross-check are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the surviving list and slug-availability surfaces to tenant vocabulary</name>
  <files>apps/api/src/contexts/identity/application/list-my-brands.service.ts, apps/api/src/contexts/identity/application/list-my-tenants.service.ts, apps/api/src/contexts/identity/application/check-brand-slug-availability.service.ts, apps/api/src/contexts/identity/application/check-tenant-slug-availability.service.ts, apps/api/src/contexts/identity/interfaces/http/me-brands.controller.ts, apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts, apps/api/src/contexts/identity/interfaces/http/guards/brand-slug-rate-limit.guard.ts, apps/api/src/contexts/identity/interfaces/http/guards/tenant-slug-rate-limit.guard.ts, apps/api/src/contexts/identity/interfaces/http/error-mapping.ts, apps/api/src/contexts/identity/identity-http.module.ts</files>
  <read_first>
    - apps/api/src/contexts/identity/application/list-my-brands.service.ts (the query that becomes "list my organizations" — check what it returns today and whether it reads `member` or the deleted `member_brand_scope`)
    - apps/api/src/contexts/identity/application/check-brand-slug-availability.service.ts (the availability logic that survives)
    - apps/api/src/contexts/identity/interfaces/http/me-brands.controller.ts (~line 112 `@Body(new RestoZodValidationPipe(...))`, ~line 148 `@UseGuards(BrandSlugRateLimitGuard)` on `GET /v1/me/brands/slug-availability`; also the `POST /v1/me/brands` handler being deleted with Task 1's service)
    - apps/api/src/contexts/identity/interfaces/http/guards/brand-slug-rate-limit.guard.ts (per-route guard, NOT one of the global APP_GUARDs, gated by `RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN`)
    - apps/api/src/config/env.schema.ts (the `RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN` env var that renames with the guard)
    - packages/db/src/schema/auth.ts (`member` table — the post-merge source of "which organizations does this user belong to")
    - packages/domain/src/tenant-slug.ts (post-plan-01 `TenantSlugValue` for the HTTP boundary)
    - .planning/phases/10.2-brand-pinned-sessions/10.2-RESEARCH.md Pitfall 5
  </read_first>
  <action>
Rename, do not delete (RESEARCH Pitfall 5 — the function is still needed):

`list-my-brands.service.ts` -> `list-my-tenants.service.ts`, class `ListMyBrandsService` ->
`ListMyTenantsService`. Its query source must now be the BA `member` table joined to `tenants` — after the
merge, "the organizations I belong to" IS `member.organization_id -> tenants.id`, and the schema already
documents `export const organization = tenants` in `packages/db/src/schema/auth.ts`. If the current
implementation reads the deleted `member_brand_scope`, rewrite it against `member`. The response must
include `slug` as well as `id` and `displayName` — the picker's success path is a hard navigation to
`<slug>.admin.<domain>`, so a response without slug makes the picker unimplementable.

`check-brand-slug-availability.service.ts` -> `check-tenant-slug-availability.service.ts`, class renamed
correspondingly, validating with `TenantSlugValue`.

`brand-slug-rate-limit.guard.ts` -> `tenant-slug-rate-limit.guard.ts`, class `BrandSlugRateLimitGuard` ->
`TenantSlugRateLimitGuard`. It stays a PER-ROUTE guard applied with `@UseGuards(...)`; do not promote it to
`APP_GUARD`. Rename `RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN` to `RATE_LIMIT_TENANT_SLUG_CHECK_PER_MIN` in
`apps/api/src/config/env.schema.ts` and every reader, and update `.env.example` if it names the old var.

`me-brands.controller.ts` -> `me-tenants.controller.ts`, class `MeTenantsController`, routes:
`GET /v1/me/brands` -> `GET /v1/me/tenants`; `GET /v1/me/brands/slug-availability` ->
`GET /v1/me/tenants/slug-availability`; `POST /v1/me/brands` DELETED (Task 1). Keep the existing
`@Permissions({...})` decorators, the per-parameter `RestoZodValidationPipe` usage, and the
`@BrandNeutral()` decorators exactly as they are — plan 11 owns the decorator sweep and changing them here
would collide with it.

Register the renamed providers and controller in `identity-http.module.ts` and update
`interfaces/http/error-mapping.ts` for renamed error classes.

Record every old->new route string in the SUMMARY; plan 14 rewrites the admin client against it and
`docs/api/openapi.yaml` regenerates from these decorators.
</action>
<verify>
<automated>cd apps/api && npx vitest run test/unit/identity/check-brand-slug-availability.service.spec.ts test/unit/identity/list-my-brands.service.spec.ts 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - `find apps/api/src/contexts/identity -iname "*brand*"` returns no results. - `rg -n "GET|@Get" apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts` shows routes `tenants` and `tenants/slug-availability`; `rg -n "@Post" apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts` returns no create-organization handler. - `rg -n "slug" apps/api/src/contexts/identity/application/list-my-tenants.service.ts` matches in the returned projection. - `rg -c "RATE_LIMIT_BRAND_SLUG_CHECK_PER_MIN" apps/api/src .env.example` returns 0 and `rg -n "RATE_LIMIT_TENANT_SLUG_CHECK_PER_MIN" apps/api/src/config/env.schema.ts` matches. - `rg -n "@UseGuards\(TenantSlugRateLimitGuard\)" apps/api/src/contexts/identity/interfaces/http/me-tenants.controller.ts` matches (still per-route, still rate-limited). - `rg -c "member_brand_scope|memberBrandScope" apps/api/src` returns 0. - The SUMMARY lists old->new route strings.
</acceptance_criteria>
<done>`GET /v1/me/tenants` returns the organizations a user belongs to with their slugs, and the rate-limited slug check is available for onboarding under a tenant-named route.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                          |
| -------------------------------------------- | -------------------------------------------------------------------- |
| Authenticated request -> global guard chain  | Where authorization is decided; this plan removes one guard from it  |
| Signed-in user -> `GET /v1/me/tenants`       | Must return only the organizations that user is actually a member of |
| Anonymous/authenticated -> slug-availability | An unrated endpoint enumerating slugs is a reconnaissance surface    |

## STRIDE Threat Register

| Threat ID    | Category               | Component                                                                | Disposition | Mitigation Plan                                                                                                                                                                                                                     |
| ------------ | ---------------------- | ------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10.2-08-01 | Elevation of Privilege | Removing `BrandScopeGuard` weakening tenant isolation                    | mitigate    | The tenant fence was never `BrandScopeGuard` — it is `AuthGuard`'s `auth.tenant_mismatch` cross-check plus `ScopedTx` + RLS. Acceptance criteria assert `auth.tenant_mismatch` and the four surviving guards are intact and ordered |
| T-10.2-08-02 | Information Disclosure | `GET /v1/me/tenants` leaking organizations the caller is not a member of | mitigate    | The query is anchored on `member.user_id = <session user>` joined to `tenants`; plan 19 adds an e2e asserting a two-organization user sees exactly two and a one-organization user sees exactly one                                 |
| T-10.2-08-03 | Denial of Service      | Slug-availability endpoint left unrated after the rename                 | mitigate    | `TenantSlugRateLimitGuard` stays applied per-route; acceptance criterion greps for the `@UseGuards` decorator on the renamed handler and for the renamed env var                                                                    |
| T-10.2-08-04 | Information Disclosure | Slug enumeration via the availability endpoint                           | accept      | The endpoint answers a boolean about a namespace that is public by construction (every slug is a live hostname). Rate limiting bounds the enumeration rate; this is unchanged from the pre-merge posture                            |
| T-10.2-08-05 | Elevation of Privilege | Location scoping collaterally weakened while deleting the brand sibling  | mitigate    | `LocationScopeGuard`, `MEMBER_LOCATION_SCOPE_READER`, `SESSION_ACTIVE_LOCATION_WRITER` and `initial-location-drizzle.repository.ts` are named as must-survive; the location-scope guard spec is the task's `<automated>` gate       |
| T-10.2-08-SC | Tampering              | npm/pip/cargo installs                                                   | accept      | No packages installed                                                                                                                                                                                                               |

</threat_model>

<verification>
- `find apps/api/src/contexts/identity -iname "*brand*"` returns nothing.
- `rg -c "BrandScopeGuard|MEMBER_BRAND_SCOPE_READER|BRAND_PROVISIONING_PORT|SESSION_ACTIVE_BRAND_WRITER" apps/api/src` returns 0.
- `rg -c "APP_GUARD" apps/api/src/contexts/identity/identity-http.module.ts` equals 4.
- `cd apps/api && npx vitest run test/unit/identity/location-scope.guard.spec.ts` passes (run this file alone —
  the whole `apps/api` suite in one process produces false 429/timeout failures).
- `pnpm --filter api typecheck` still reports errors from `contexts/catalog`, `contexts/ordering`,
  `contexts/payments`, `shared/auth` and `identity-core.module.ts` — EXPECTED, swept by plans 09–12.
</verification>

<success_criteria>

- The brand pin, its guard, its ports and its adapters are deleted with no replacement.
- The guard chain is four guards, correctly ordered, with location scoping intact.
- `GET /v1/me/tenants` and the rate-limited `GET /v1/me/tenants/slug-availability` exist.
  </success_criteria>

<output>
Create `.planning/phases/10.2-brand-pinned-sessions/10.2-08-SUMMARY.md` when done.
</output>
