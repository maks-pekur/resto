---
phase: 05-customer-site
plan: 06
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - apps/website/app/about/page.tsx
  - apps/website/app/delivery/page.tsx
  - apps/website/app/contact/page.tsx
  - apps/website/app/faq/page.tsx
  - apps/website/components/content-page.tsx
  - apps/website/lib/content.ts
  - apps/website/test/content-page.spec.tsx
autonomous: true
requirements: [SITE-10]
must_haves:
  truths:
    - 'About / Delivery / Contact / FAQ pages are reachable and render seeded per-tenant plain-text content'
    - 'Content renders as split-on-newline paragraphs (no markdown, no WYSIWYG)'
    - 'Each content page uses the TenantHeader and no CategoryNav/CartDrawer'
    - 'Each content page has its own SEO title and is index-able'
  artifacts:
    - path: 'apps/website/components/content-page.tsx'
      provides: 'Minimal static content renderer (split-on-newline paragraphs)'
      min_lines: 15
    - path: 'apps/website/lib/content.ts'
      provides: 'Seeded per-tenant content lookup with defaults'
      contains: 'ContentPageKey'
  key_links:
    - from: 'apps/website/app/about/page.tsx'
      to: 'apps/website/components/content-page.tsx'
      via: 'renders ContentPage with seeded content'
      pattern: 'ContentPage'
---

<objective>
Ship the four minimal content pages (About / Delivery / Contact / FAQ) from seeded per-tenant plain-text content (D-08), rendered as split-on-newline paragraphs under the tenant header. A full operator WYSIWYG editor is explicitly Phase 15/MVP-2.

Purpose: SITE-10 — operator-editable content pages are accessible from the site (Phase 5 delivers seeded/static; the editor is deferred).
Output: `/about`, `/delivery`, `/contact`, `/faq` each render the tenant's content with their own SEO title.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-customer-site/05-RESEARCH.md
@.planning/phases/05-customer-site/05-PATTERNS.md
@.planning/phases/05-customer-site/05-UI-SPEC.md
@.planning/phases/05-customer-site/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded content source + ContentPage renderer</name>
  <files>apps/website/lib/content.ts, apps/website/components/content-page.tsx, apps/website/test/content-page.spec.tsx</files>
  <read_first>
    - .planning/phases/05-customer-site/05-UI-SPEC.md (Interaction § Content Pages — routes, seeded plain text, split-on-newline, centered max-720, TenantHeader only; Copywriting content-page headings)
    - .planning/phases/05-customer-site/05-RESEARCH.md (§ Architectural Responsibility Map — content pages RSC; Implementation Note 6 — no markdown parsing)
    - .planning/phases/05-customer-site/05-CONTEXT.md (D-08 — minimal static/seeded; full editor is Phase 15)
    - apps/website/lib/i18n/localized.ts (localized helper for any LocalizedText, from 05-02)
  </read_first>
  <action>
    Create `apps/website/lib/content.ts`: define `type ContentPageKey = 'about' | 'delivery' | 'contact' | 'faq'` and a `getSeededContent(key, brandName)` function returning a heading + plain-text body for the page. Since the `/v1/menu` `MenuDto` carries NO tenant content field (verified — content editor is Phase 15), Phase 5 sources content from a local seeded default keyed by page (generic per-tenant copy interpolating `{restaurantName}` from `MenuBrandDto.displayName`); structure the function so Phase 15 can swap the default for a real API-backed content field without changing call sites. Build `apps/website/components/content-page.tsx` (RSC, no `'use client'` needed): props `{ heading: string, body: string }`; render centered single-column max-width 720px, the heading (Heading role), and the body split on `\n` into `<p>` elements (skip empty lines); NO markdown parsing, NO `dangerouslySetInnerHTML` (Implementation Note 6 + XSS mitigation).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm nx test website -- content-page.spec</automated>
  </verify>
  <acceptance_criteria>
    - `ContentPage` splits a multi-line body into multiple `<p>` elements and drops blank lines.
    - `ContentPage` does NOT use `dangerouslySetInnerHTML`.
    - `getSeededContent` returns heading+body for all four keys and interpolates the restaurant name.
  </acceptance_criteria>
  <done>A seeded-content source and a safe split-on-newline content renderer exist, structured for a Phase 15 API swap.</done>
</task>

<task type="auto">
  <name>Task 2: The four content routes with TenantHeader + SEO metadata</name>
  <files>apps/website/app/about/page.tsx, apps/website/app/delivery/page.tsx, apps/website/app/contact/page.tsx, apps/website/app/faq/page.tsx</files>
  <read_first>
    - apps/website/components/content-page.tsx + apps/website/lib/content.ts (from Task 1)
    - apps/website/app/page.tsx (RSC tenant-gating + getTenantSlugFromHeaders + fetchMenuPublic pattern from 05-03)
    - apps/website/components/layout/tenant-header.tsx (header to reuse)
    - .planning/phases/05-customer-site/05-UI-SPEC.md (SEO & Meta Contract — content pages get own title "{PageHeading} — {restaurantName}"; index,follow)
  </read_first>
  <action>
    Create `apps/website/app/about/page.tsx`, `delivery/page.tsx`, `contact/page.tsx`, `faq/page.tsx` as RSC pages. Each: resolve the tenant via `getTenantSlugFromHeaders()` + `fetchMenuPublic()` (reuse the 05-03 gating: `notFound()` on missing/not-found, suspended state on 403, re-throw transient to `error.tsx`); render the `TenantHeader` (brand from the menu) and `<ContentPage heading body />` from `getSeededContent(key, brandName)`. No CategoryNav, no CartDrawer (UI-SPEC content-page layout). Add `generateMetadata()` per page: title from the page heading + restaurant name (e.g. `About — {restaurantName}`), `robots: { index: true, follow: true }` (content pages are an SEO surface per D-03). Use the UI-SPEC copy for the four headings ("About {restaurantName}", "Delivery Information", "Contact Us", "Frequently Asked Questions").
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm nx build website && pnpm nx lint website && test -f apps/website/app/about/page.tsx && test -f apps/website/app/faq/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four routes exist and render the TenantHeader + ContentPage.
    - Each page's `generateMetadata` sets a page-specific title and `index,follow`.
    - Routes reuse the same tenant-gating (not-found/suspended/error) as the menu page.
    - build + lint pass.
  </acceptance_criteria>
  <done>About / Delivery / Contact / FAQ are reachable, index-able, and render seeded content under the tenant header.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                               |
| --------------------------- | ----------------------------------------- |
| Seeded/tenant content → DOM | Plain-text content rendered to all guests |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-06-X | Tampering (XSS)        | content-page body render | mitigate    | Body is split into React `<p>` text children (auto-escaped); NO `dangerouslySetInnerHTML`, NO markdown/HTML parsing (Implementation Note 6). When Phase 15 wires a real content field, the same plain-text-only invariant must hold |
| T-05-06-I | Information Disclosure | content source           | accept      | Phase 5 content is generic seeded copy, no PII; no secret data path                                                                                                                                                                 |

</threat_model>

<verification>
- `pnpm nx test website` (content-page spec) green.
- `pnpm nx build website` + `pnpm nx lint website` pass.
- Manual (executor optional): `/about`, `/delivery`, `/contact`, `/faq` render for a resolved tenant.
</verification>

<success_criteria>

- SITE-10: operator-editable content pages accessible from the site — Phase 5 delivers seeded/static (D-08); full editor is Phase 15.
- Content renders safely (plain-text, no HTML injection) and is index-able.
  </success_criteria>

<output>
Create `.planning/phases/05-customer-site/05-06-SUMMARY.md` when done.
</output>
