---
phase: 05-customer-site
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - apps/website/app/checkout/page.tsx
  - apps/website/components/checkout/checkout-form.tsx
  - apps/website/components/checkout/address-input.tsx
  - apps/website/components/checkout/order-time-selector.tsx
  - apps/website/components/checkout/order-summary.tsx
  - apps/website/lib/checkout-schema.ts
  - apps/website/components/ui/form.tsx
  - apps/website/components/ui/input.tsx
  - apps/website/components/ui/label.tsx
  - apps/website/components/ui/radio-group.tsx
  - apps/website/components/ui/tooltip.tsx
  - apps/website/test/checkout-form.spec.tsx
autonomous: true
requirements: [SITE-04, SITE-06, SITE-07]
must_haves:
  truths:
    - 'Checkout is a single page (no stepper): address (delivery only) + contact + order time + summary + disabled pay button'
    - "Guest enters a delivery address and sees inline zone-validity feedback (stub always 'We deliver to this area')"
    - 'Guest provides name + phone with client-side validation'
    - 'Guest chooses ASAP or schedules a time'
    - "The pay button renders with accent weight but is disabled with a 'Payment processing coming soon' tooltip; no payment is initiated"
    - 'Order summary shows items + subtotal + stub delivery fee read from the cart store'
  artifacts:
    - path: 'apps/website/app/checkout/page.tsx'
      provides: 'Checkout route (RSC shell)'
      contains: 'CheckoutForm'
    - path: 'apps/website/components/checkout/checkout-form.tsx'
      provides: 'RHF contact + address + time form with disabled pay button'
      min_lines: 40
    - path: 'apps/website/components/checkout/address-input.tsx'
      provides: 'Address field + stub zone validity badge'
      min_lines: 20
  key_links:
    - from: 'apps/website/components/checkout/checkout-form.tsx'
      to: 'apps/website/store/cart.ts'
      via: 'reads mode + items + subtotal for summary'
      pattern: 'useCartStore'
    - from: 'apps/website/components/checkout/checkout-form.tsx'
      to: 'apps/website/lib/checkout-schema.ts'
      via: 'zodResolver'
      pattern: 'zodResolver'
---

<objective>
Build the single-page checkout (D-07, no stepper): delivery address with stub inline zone-validity feedback (SITE-04), contact info name+phone with optional-account hint (SITE-06), ASAP/scheduled order-time selector (SITE-07), a read-only order summary from the cart, and a disabled "Place order" pay button with a "coming soon" tooltip — no payment flow is initiated in Phase 5.

Purpose: SITE-04 (address + zone stub) + SITE-06 (contact info + disabled checkout) + SITE-07 (order time).
Output: `/checkout` renders the one-page form; the pay button is visibly weighted but disabled until Phase 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-customer-site/05-RESEARCH.md
@.planning/phases/05-customer-site/05-PATTERNS.md
@.planning/phases/05-customer-site/05-UI-SPEC.md

<interfaces>
<!-- Cart store API consumed read-only here (from 05-04). -->
useCartStore: { mode: 'delivery'|'pickup'|null, items: CartLineItem[] }
selectSubtotal(state) → decimal string
<!-- Checkout form values (build the zod schema in lib/checkout-schema.ts). -->
CheckoutForm: { name: string (min 1), phone: string (validated), address?: string (required iff mode==='delivery'), orderTime: { kind: 'asap' } | { kind: 'scheduled', at: string } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Checkout zod schema + AddressInput stub + OrderTimeSelector</name>
  <files>apps/website/lib/checkout-schema.ts, apps/website/components/checkout/address-input.tsx, apps/website/components/checkout/order-time-selector.tsx, apps/website/components/ui/form.tsx, apps/website/components/ui/input.tsx, apps/website/components/ui/label.tsx, apps/website/components/ui/radio-group.tsx, apps/website/test/checkout-form.spec.tsx</files>
  <read_first>
    - .planning/phases/05-customer-site/05-UI-SPEC.md (Checkout Page layout sections 1-6; Interaction Contracts § Address Input stub + § Checkout Button; Copywriting checkout rows; Accessibility)
    - apps/admin/app/dashboard/(workspace)/menu/items/[id]/item-detail-form-client.tsx (RHF + zodResolver pattern, lines 1-113)
    - .planning/phases/05-customer-site/05-RESEARCH.md (§ Pattern — address stub always green in Phase 5; § No Analog — order-time uses RadioGroup)
  </read_first>
  <behavior>
    - schema requires name (min 1) and phone (non-empty, basic phone shape)
    - schema requires address when mode==='delivery', allows absent address when mode==='pickup'
    - orderTime defaults to { kind: 'asap' }; scheduled requires a non-empty time
  </behavior>
  <action>
    Install shadcn `form`, `input`, `label`, `radio-group`, `tooltip` in `apps/website`. Create `apps/website/lib/checkout-schema.ts` exporting `CheckoutFormSchema` (zod) + `CheckoutForm` type per the interface block; model the mode-conditional address requirement with a `superRefine` (mode passed into a schema factory or validated as a discriminated field). Build `apps/website/components/checkout/address-input.tsx` (`'use client'`): shadcn `Input` (placeholder "Enter your delivery address") + an on-blur stub validation that ALWAYS returns valid in Phase 5 → green `Badge` with check icon + copy "We deliver to this area"; the component MUST also support a yellow/"outside zone" state ("We don't deliver to this address yet. Try pickup instead.") even though the Phase 5 stub never triggers it (Phase 9 wires the real check). Build `apps/website/components/checkout/order-time-selector.tsx` (`'use client'`): shadcn `RadioGroup` with "As soon as possible" / "Schedule for later"; selecting scheduled reveals a time input.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm nx test website -- checkout-form.spec</automated>
  </verify>
  <acceptance_criteria>
    - Schema tests cover all three behaviors (name/phone required; address conditional on mode; order-time default/scheduled), RED before GREEN.
    - AddressInput renders the green valid badge on blur and exposes the yellow out-of-zone state for Phase 9.
    - OrderTimeSelector reveals the time input only when scheduled is chosen.
  </acceptance_criteria>
  <done>The checkout schema, address stub (forward-compatible with Phase 9), and order-time selector exist and are tested.</done>
</task>

<task type="auto">
  <name>Task 2: CheckoutForm (RHF) + OrderSummary with disabled pay button</name>
  <files>apps/website/components/checkout/order-summary.tsx, apps/website/components/checkout/checkout-form.tsx, apps/website/components/ui/tooltip.tsx</files>
  <read_first>
    - apps/admin/app/dashboard/(workspace)/menu/items/[id]/item-detail-form-client.tsx (FormProvider + useForm + FormField, lines 1-113)
    - apps/website/lib/checkout-schema.ts (from Task 1)
    - apps/website/store/cart.ts (mode + items + selectSubtotal from 05-04)
    - .planning/phases/05-customer-site/05-UI-SPEC.md (Checkout Page single-column max-640; Interaction § Checkout Button disabled + tooltip + aria-disabled + aria-describedby; Copywriting "Place order" disabled, "Payment processing coming soon")
  </read_first>
  <action>
    Build `apps/website/components/checkout/order-summary.tsx` (`'use client'`): read `items` + `selectSubtotal` from the cart store; render a read-only list (item name, quantity, line total, `tabular-nums`), a subtotal row, and a stub delivery-fee row shown only when `mode==='delivery'` (labelled, value is a Phase 5 placeholder — modifiers/discounts wire in Phase 7/8/11). Build `apps/website/components/checkout/checkout-form.tsx` (`'use client'`): `useForm<CheckoutForm>({ resolver: zodResolver(CheckoutFormSchema), mode: 'onChange' })`; render in single-column max-width-640 sections per UI-SPEC: (1) `AddressInput` shown only when `mode==='delivery'`, (2) contact name+phone via `FormField`+`Input` with the optional-account hint as static non-functional text, (3) `OrderTimeSelector`, (4) `OrderSummary`, (5) a disabled "Place order" `Button` with full accent weight, `disabled`, `aria-disabled="true"`, wrapped in a shadcn `Tooltip` ("Payment processing coming soon") with `aria-describedby` pointing at the tooltip text. The button has NO onClick / no submit handler that initiates payment (D-07 — no payment flow in Phase 5); the form's `onSubmit` is a no-op or simply prevents default. If the cart is empty, show an empty-cart message and a link back to the menu instead of the form.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm nx typecheck website && test -f apps/website/components/checkout/order-summary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The pay button is `disabled` with `aria-disabled="true"`, accent weight, and a "Payment processing coming soon" tooltip referenced via `aria-describedby`.
    - AddressInput renders only in delivery mode.
    - OrderSummary reads items + subtotal from the cart store; delivery-fee stub row appears only for delivery.
    - No code path initiates a payment.
    - typecheck passes.
  </acceptance_criteria>
  <done>The one-page checkout form renders all required sections with a visibly-weighted but disabled, non-functional pay button.</done>
</task>

<task type="auto">
  <name>Task 3: Checkout route (RSC shell)</name>
  <files>apps/website/app/checkout/page.tsx</files>
  <read_first>
    - apps/website/components/checkout/checkout-form.tsx (from Task 2)
    - apps/website/components/layout/tenant-header.tsx (header to reuse, no CategoryNav/CartDrawer on checkout per UI-SPEC)
    - .planning/phases/05-customer-site/05-UI-SPEC.md (Checkout Page — TenantHeader present, no CategoryNav/CartDrawer)
    - apps/website/app/page.tsx (RSC tenant-gating pattern from 05-03 to mirror)
  </read_first>
  <action>
    Create `apps/website/app/checkout/page.tsx` as an RSC shell: render the `TenantHeader` (no CategoryNav, no CartDrawer per UI-SPEC) and mount `<CheckoutForm/>` (client). The page needs the resolved tenant for the header brand — reuse the `getTenantSlugFromHeaders()` + `fetchMenuPublic()` pattern from 05-03 (or pass brand via the header if cheaper); on not-found `notFound()`, on suspended render the suspended state, transient errors re-throw to `error.tsx`. Add `generateMetadata()` with title `Checkout — {brandName}` and `robots: { index: false }` (a transactional page need not be indexed; D-03 SEO is for menu + content pages).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm nx build website && pnpm nx lint website</automated>
  </verify>
  <acceptance_criteria>
    - `/checkout` renders the TenantHeader + CheckoutForm and no CategoryNav/CartDrawer.
    - generateMetadata sets `robots index:false` for checkout.
    - build + lint pass.
  </acceptance_criteria>
  <done>The `/checkout` route renders the single-page checkout under the tenant header.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                   |
| ------------------------------------ | --------------------------------------------- |
| Guest form input → client validation | Untrusted name/phone/address entered by guest |
| (future) checkout submit → API       | No submit path exists in Phase 5              |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | ---------------------- | ----------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-05-T | Tampering              | checkout form fields                | mitigate    | Zod-validated client-side; values are never submitted anywhere in Phase 5 (no server action) — no injection sink exists yet. Phase 7 MUST re-validate server-side at order creation      |
| T-05-05-E | Elevation of Privilege | disabled pay button bypass          | mitigate    | The button has no handler that initiates payment and no submit-to-API path; `disabled` + `aria-disabled` + absent onClick means there is nothing to bypass in Phase 5 (D-07 scope fence) |
| T-05-05-X | Tampering (XSS)        | rendered cart item names in summary | mitigate    | Item names are rendered as React text children (auto-escaped); snapshots originate from the API menu, not free guest input                                                               |
| T-05-05-I | Information Disclosure | address field stub                  | accept      | Address is held client-side only, never sent anywhere in Phase 5; no PII leaves the browser                                                                                              |

</threat_model>

<verification>
- `pnpm nx test website` (checkout-form spec) green.
- `pnpm nx build website` + `pnpm nx lint website` pass.
- Manual (executor optional): with items in cart, `/checkout` shows summary; pay button disabled with tooltip; delivery mode reveals address with green badge.
</verification>

<success_criteria>

- SITE-04: delivery address entry with inline zone-validity stub (forward-compatible with Phase 9).
- SITE-06: contact name+phone + disabled "coming soon" checkout button; no payment initiated.
- SITE-07: ASAP / scheduled order-time selection.
- Single page, no stepper (D-07).
  </success_criteria>

<output>
Create `.planning/phases/05-customer-site/05-05-SUMMARY.md` when done.
</output>
