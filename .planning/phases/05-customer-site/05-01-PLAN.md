---
phase: 05-customer-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-client/src/menu-types.ts
  - packages/api-client/src/public.ts
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - apps/website/package.json
  - apps/website/project.json
  - apps/website/tsconfig.json
  - apps/website/eslint.config.mjs
  - apps/website/postcss.config.mjs
  - apps/website/next.config.mjs
  - apps/website/vitest.config.ts
  - apps/website/components.json
  - apps/website/app/globals.css
  - apps/website/app/layout.tsx
  - apps/website/app/page.tsx
  - apps/website/lib/utils.ts
  - apps/website/lib/i18n/request.ts
  - apps/website/test/setup.ts
autonomous: false
requirements: [SITE-01]
must_haves:
  truths:
    - 'apps/website builds and serves a placeholder page on port 3002'
    - 'packages/api-client/public re-exports the full MenuDto wire types'
    - 'packages/ui is an initialized importable package (no longer an empty placeholder)'
  artifacts:
    - path: 'apps/website/project.json'
      provides: 'Nx project config (build/lint/typecheck/test/serve on 3002)'
      contains: '"name": "website"'
    - path: 'packages/api-client/src/menu-types.ts'
      provides: 'Canonical MenuDto wire types'
      contains: 'export interface MenuDto'
    - path: 'packages/ui/package.json'
      provides: 'Initialized @resto/ui package'
      contains: '@resto/ui'
  key_links:
    - from: 'packages/api-client/src/public.ts'
      to: 'packages/api-client/src/menu-types.ts'
      via: 're-export'
      pattern: "export type .* from './menu-types'"
---

<objective>
Scaffold `apps/website` from `.gitkeep` into a buildable Next.js App Router + RSC app matching the `apps/admin` stack, initialize the `@resto/ui` package, and establish the canonical `MenuDto` wire types in `@resto/api-client`. This is the foundation every other Phase 5 plan builds on.

Purpose: SITE-01 — a working Next.js scaffold and the shared type layer (D-02) that the website and (Phase 6) qr-menu both consume.
Output: A `pnpm nx build website` that produces a `.next` directory; `@resto/api-client/public` exporting `MenuDto`; `@resto/ui` initialized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-customer-site/05-RESEARCH.md
@.planning/phases/05-customer-site/05-PATTERNS.md
@.planning/phases/05-customer-site/05-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Canonical menu wire types to copy verbatim into packages/api-client/src/menu-types.ts. -->
<!-- Source: apps/qr-menu/src/api/types.ts (lines 1-115). Types-only, zero runtime cost. -->

The full set to copy: LocalizedText, MenuPhotoDto, MenuItemSizeDto, MenuModifierOptionDto,
MenuModifierGroupDto, MenuItemDto, MenuCategoryDto, MenuBrandThemeDto, MenuBrandDto, MenuDto.

Key fields the website depends on downstream:

- MenuDto: tenantId, version, currency, brand (MenuBrandDto | null), categories[], items[], modifierGroups[]
- MenuBrandDto: id, slug, displayName, theme (MenuBrandThemeDto | null)
- MenuBrandThemeDto: logoUrl, primaryColor, font (all string | null)
- MenuItemDto: id, slug, categoryId, name (LocalizedText), description, basePrice (string), currency, imageUrl, photos[], allergens[], sizes[], modifierGroupIds[]
- MenuItemSizeDto: id, name, price (string), isDefault, sortOrder
- MenuModifierGroupDto: id, name, minSelectable, maxSelectable, isRequired, options[]
- MenuModifierOptionDto: id, name, priceDelta (string), defaultAmount, freeAmount, sortOrder
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for [ASSUMED] dependencies</name>
  <files>apps/website/package.json</files>
  <read_first>
    - .planning/phases/05-customer-site/05-RESEARCH.md (§ Package Legitimacy Audit — all packages [ASSUMED], slopcheck unavailable)
  </read_first>
  <action>Pause before any package install (Task 3). Present the legitimacy evidence below to the human and obtain explicit approval. Do not run `pnpm add` / `pnpm i` for any net-new package until approved. This gate is NOT auto-approvable (workflow.auto_advance ignored per security gate).</action>
  <what-built>Nothing yet — this gate precedes the install task. RESEARCH.md flagged every Phase 5 dependency as [ASSUMED] because slopcheck was unavailable. Per the package legitimacy gate, a human must verify the packages NOT already in the workspace before install: zustand, next-intl (already in admin but confirm version), next-themes, and confirm next/react/sonner/react-hook-form/@hookform/resolvers/server-only/zod are the existing workspace versions.</what-built>
  <how-to-verify>
    1. Confirm `zustand` at https://npmjs.com/package/zustand — repo github.com/pmndrs/zustand, ~10M weekly downloads, no postinstall script (`npm view zustand scripts`).
    2. Confirm `next-intl` at https://npmjs.com/package/next-intl — repo github.com/amannn/next-intl; verify it matches the version already in `apps/admin/package.json`.
    3. Confirm `next-themes` at https://npmjs.com/package/next-themes — already present in admin; verify version match.
    4. Confirm the remaining packages (next, react, react-dom, sonner, react-hook-form, @hookform/resolvers, server-only, zod, lucide-react, radix-ui, class-variance-authority, clsx, tailwind-merge) are the SAME versions already declared in `apps/admin/package.json` — no new untrusted package is introduced. `next` is pinned to `^16.2.9` (latest stable, per D-04); admin's `^16.2.6` already semver-resolves to the same 16.2.9, so no workspace divergence is introduced.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the non-admin-pinned packages are legitimate, OR rejects with a named replacement.
    - This checkpoint is NOT auto-approvable.
  </acceptance_criteria>
  <verify><human-check>Human approves package legitimacy before install.</human-check></verify>
  <done>Human has approved the dependency set (or named replacements) before Task 3 installs anything.</done>
  <resume-signal>Type "approved" or name packages to reject/replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add canonical MenuDto types to @resto/api-client and init @resto/ui</name>
  <files>packages/api-client/src/menu-types.ts, packages/api-client/src/public.ts, packages/ui/package.json, packages/ui/tsconfig.json, packages/ui/src/index.ts</files>
  <read_first>
    - apps/qr-menu/src/api/types.ts (the full MenuDto wire shape to copy — canonical source)
    - packages/api-client/src/public.ts (current 3-line export to extend)
    - packages/api-client/package.json (exports map — `./public` already declared)
    - packages/domain/package.json (template for a minimal TS-source package with no bundler — mirror for packages/ui)
  </read_first>
  <action>
    Create `packages/api-client/src/menu-types.ts` containing all 10 menu wire interfaces copied verbatim from `apps/qr-menu/src/api/types.ts` (LocalizedText, MenuPhotoDto, MenuItemSizeDto, MenuModifierOptionDto, MenuModifierGroupDto, MenuItemDto, MenuCategoryDto, MenuBrandThemeDto, MenuBrandDto, MenuDto). Keep the doc comment noting these mirror `apps/api/src/contexts/catalog/domain/published-menu.ts`. In `packages/api-client/src/public.ts`, add a re-export line: `export type { LocalizedText, MenuPhotoDto, MenuItemSizeDto, MenuModifierOptionDto, MenuModifierGroupDto, MenuItemDto, MenuCategoryDto, MenuBrandThemeDto, MenuBrandDto, MenuDto } from './menu-types';` (per PATTERNS.md `packages/api-client/src/public.ts` section). Do not remove the existing generated-paths exports.

    Initialize `packages/ui` (currently `.gitkeep` only) as a minimal TS-source React package mirroring `packages/domain`'s no-bundler convention: `package.json` (name `@resto/ui`, private, type module, `main`/`types` → `./src/index.ts`, exports map `.` → `./src/index.ts`, React + react-dom as peerDependencies, `@resto/config-typescript` + `@resto/config-eslint` + typescript + eslint as devDependencies), `tsconfig.json` extending `@resto/config-typescript`, and `src/index.ts` with a placeholder export comment (components land in Phase 6 per D-02 — Phase 5 only initializes the package so it is importable). Per RESEARCH Open Question 1 + § Pattern 5: do NOT place MenuItemCard here in Phase 5 — build that in apps/website/components; extraction is Phase 6.

  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm --filter @resto/api-client exec tsc -p tsconfig.json --noEmit && grep -q "menu-types" packages/api-client/src/public.ts && grep -q '"@resto/ui"' packages/ui/package.json && grep -q 'src/index.ts' packages/ui/package.json && test -f packages/ui/src/index.ts && pnpm --filter @resto/ui exec tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/menu-types.ts` exports MenuDto and all 9 supporting interfaces.
    - `packages/api-client/src/public.ts` re-exports the menu types from `./menu-types`.
    - `packages/ui/package.json` exists with name `@resto/ui` and a `.` export pointing at `./src/index.ts` (both asserted by grep: `"@resto/ui"` AND `src/index.ts` present in the file).
    - `packages/ui/src/index.ts` exists and the package compiles: `pnpm --filter @resto/ui exec tsc -p tsconfig.json --noEmit` exits 0 (the package is importable, not just present on disk).
    - api-client typecheck passes.
  </acceptance_criteria>
  <done>`@resto/api-client/public` exports MenuDto; `@resto/ui` is an initialized importable package that compiles under its own tsconfig.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold apps/website config + minimal RSC shell</name>
  <files>apps/website/package.json, apps/website/project.json, apps/website/tsconfig.json, apps/website/eslint.config.mjs, apps/website/postcss.config.mjs, apps/website/next.config.mjs, apps/website/vitest.config.ts, apps/website/components.json, apps/website/app/globals.css, apps/website/app/layout.tsx, apps/website/app/page.tsx, apps/website/lib/utils.ts, apps/website/lib/i18n/request.ts, apps/website/test/setup.ts</files>
  <read_first>
    - apps/admin/project.json (exact analog — copy, swap name/scope/port/cwd)
    - apps/admin/tsconfig.json (copy verbatim, keep exactOptionalPropertyTypes:false)
    - apps/admin/eslint.config.mjs (copy verbatim incl. shadcn relaxation block)
    - apps/admin/postcss.config.mjs (copy verbatim — 2-line Tailwind 4 config)
    - apps/admin/next.config.mjs (mirror; REMOVE the /api/:path* rewrite, keep /v1/:path*, keep typedRoutes + withNextIntl)
    - apps/admin/vitest.config.ts (copy verbatim)
    - apps/admin/components.json (reference preset; do NOT copy — see Task action)
    - apps/admin/app/globals.css (copy the Tailwind 4 + shadcn token block; light-mode tokens only)
    - apps/admin/app/layout.tsx (analog for the root layout; Phase 5 layout is built fully in 05-02 — here stub it)
    - apps/admin/lib/utils.ts (cn() helper — copy verbatim)
    - .planning/phases/05-customer-site/05-PATTERNS.md (config sections with per-file change notes)
  </read_first>
  <action>
    Create the `apps/website` scaffold by copying admin's config files with documented substitutions (PATTERNS.md). `package.json`: name `website`, scripts mirroring admin (`dev` → `next dev --port 3002`, build, lint, typecheck, test), and the dependency set from RESEARCH § Standard Stack. Pin `next` to `^16.2.9` — the latest stable per locked decision D-04 (the website uses the LATEST Next.js). DEVIATION NOTE: admin's `package.json` declares `^16.2.6`, but `^16.2.6` semver-resolves to the same 16.2.9 the registry serves today, so there is no workspace-wide Next divergence — pinning `^16.2.9` simply makes the latest-version intent explicit per D-04. Pin the remaining shared deps (react, react-dom, next-intl, next-themes, sonner, react-hook-form, @hookform/resolvers, server-only, zod, lucide-react, radix-ui, class-variance-authority, clsx, tailwind-merge) to admin's existing versions, plus zustand from the approved gate. `project.json`: copy admin's, set name `website`, tag `scope:website`, port `3002`, all `cwd` → `apps/website`. `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`, `vitest.config.ts`: copy verbatim. `next.config.mjs`: mirror admin but drop the `/api/:path*` rewrite (website has no BA auth flow), keep `/v1/:path*` rewrite + `typedRoutes: true` + `withNextIntl('./lib/i18n/request.ts')`.

    Because `next.config.mjs` references `withNextIntl('./lib/i18n/request.ts')`, create a MINIMAL stub `apps/website/lib/i18n/request.ts` now so the `next build` verify does not fail (the full implementation lands in 05-02 Task 3). The stub: `import { getRequestConfig } from 'next-intl/server'; export default getRequestConfig(async () => ({ locale: 'en', messages: {} }));` — single placeholder `getRequestConfig` default export, no message-file imports yet. 05-02 Task 3 replaces this file with the real `resolveLocale()` + messages implementation.

    Run `npx shadcn init` in `apps/website` matching admin's preset (new-york / neutral / cssVariables / lucide / rsc / tsx) — do NOT copy admin's components.json (init fresh per UI-SPEC shadcn gate). Per RESEARCH Pitfall 7: pass explicit flags and do NOT let the CLI overwrite `postcss.config.mjs` (Tailwind 4 two-liner stays as copied). Add `apps/website/app/globals.css` with the shadcn neutral token set + `--radius: 0.75rem` (light mode only — Phase 5 is light-only per UI-SPEC; no dark block needed). Copy `lib/utils.ts` (`cn`). Create `test/setup.ts` (jsdom matchers, mirror admin's). Create a MINIMAL placeholder `app/layout.tsx` (Inter font + globals import + plain `<html><body>{children}</body></html>`) and `app/page.tsx` returning a static "RestOS Website" heading — both are fully replaced in 05-02/05-03; they exist now only so `next build` succeeds. Run `pnpm install` from the repo root with `pnpm i` (workspace install — this is an Nx pnpm workspace, NOT the black-builder --ignore-workspace convention).

  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/restos && pnpm i && pnpm nx build website && test -d apps/website/.next && test -f apps/website/lib/i18n/request.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/website/package.json` pins `next` at `^16.2.9` (latest stable, per D-04); no `do NOT bump` instruction remains.
    - `apps/website/lib/i18n/request.ts` exists with a `getRequestConfig` default export (stub) so the build resolves `withNextIntl`.
    - `pnpm nx build website` exits 0 and produces `apps/website/.next`.
    - `apps/website/next.config.mjs` contains a `/v1/:path*` rewrite and NO `/api/:path*` rewrite.
    - `apps/website/components.json` exists with style new-york, baseColor neutral, cssVariables true, iconLibrary lucide, rsc true.
    - `apps/website/project.json` serve target uses port 3002.
    - `pnpm nx lint website` and `pnpm nx typecheck website` exit 0.
  </acceptance_criteria>
  <done>The website app builds, lints, and typechecks on the latest Next (^16.2.9, D-04); the i18n request stub lets `withNextIntl` resolve; serve runs on 3002.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                             |
| ------------------------ | ------------------------------------------------------- |
| npm registry → workspace | Untrusted package code enters the build at install      |
| developer machine → repo | Scaffold config could leak secrets if copied carelessly |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-SC   | Tampering              | npm installs (zustand/next-intl/next-themes + workspace-pinned set) | mitigate    | Task 1 blocking-human legitimacy gate before install; reuse admin-pinned versions for all but the net-new packages                                                                                                     |
| T-05-01-E | Elevation of Privilege | apps/website env config                                             | mitigate    | No secrets in scaffold; `INTERNAL_API_TOKEN` is NOT added to website env (website is anonymous public consumer — `/v1/menu` is `@Public()`). Env schema (05-02) only declares `NEXT_PUBLIC_API_ORIGIN` + `WEBSITE_URL` |
| T-05-01-I | Information Disclosure | shadcn init / config copy                                           | accept      | Config files contain no secrets; admin's eslint/tsconfig/postcss are public patterns                                                                                                                                   |

</threat_model>

<verification>
- `pnpm i` succeeds at repo root (workspace resolves the new app + ui package).
- `pnpm nx build website` produces `.next`.
- `pnpm --filter @resto/api-client exec tsc --noEmit` passes with menu-types added.
- `packages/ui` resolves as `@resto/ui` and compiles under its own tsconfig (importable).
</verification>

<success_criteria>

- SITE-01: `apps/website` scaffolded on the admin stack (App Router + RSC) at the latest Next (^16.2.9, D-04), builds clean.
- Shared layer foundation: `@resto/api-client/public` exports MenuDto; `@resto/ui` initialized (D-02).
- No new untrusted package introduced beyond the human-approved gate.
  </success_criteria>

<output>
Create `.planning/phases/05-customer-site/05-01-SUMMARY.md` when done.
</output>
