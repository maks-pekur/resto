---
phase: 07.6-admin-vite-spa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/vite.config.ts
  - apps/admin/index.html
  - apps/admin/tsconfig.json
  - apps/admin/project.json
  - apps/admin/eslint.config.mjs
  - apps/admin/postcss.config.mjs
  - apps/admin/next.config.mjs
  - apps/admin/next-env.d.ts
  - apps/admin/instrumentation.ts
  - apps/admin/proxy.ts
  - apps/admin/src/main.tsx
  - apps/admin/src/styles.css
  - apps/admin/src/env.ts
  - apps/admin/src/lib/auth-client.ts
  - apps/admin/src/lib/api-client.ts
  - apps/admin/src/lib/query-client.ts
  - apps/admin/src/lib/i18n/index.ts
  - apps/admin/src/lib/i18n/messages/ru.json
  - apps/admin/src/lib/i18n/messages/en.json
  - apps/admin/src/hooks/use-theme.ts
  - apps/admin/src/routes/__root.tsx
autonomous: false
requirements: [SC-1, SC-2, SC-3]
user_setup: []
must_haves:
  truths:
    - 'apps/admin builds and serves via Vite (vite build produces dist/, vite dev serves on :3001) with no Next.js'
    - 'VITE_API_ORIGIN fails loudly in prod when missing; has a dev default only'
    - "The auth-client talks to the API over CORS with credentials: 'include'"
    - 'apiFetch forwards x-tenant-id (from live session) + x-brand-slug, retries idempotent GET 5xx once, redirects to /login?expired=1 on 401, applies AbortSignal.timeout'
    - 'react-i18next initialises with ru default; ru+en messages ported with {{var}} interpolation'
    - 'use-theme toggles a dark class on documentElement from localStorage'
  artifacts:
    - path: 'apps/admin/vite.config.ts'
      provides: 'Vite + React + Tailwind4 build config mirroring qr-menu'
      contains: '@tailwindcss/vite'
    - path: 'apps/admin/src/lib/api-client.ts'
      provides: 'client fetch wrapper (credentials, timeout, retry, 401 handling)'
      contains: "credentials: 'include'"
    - path: 'apps/admin/src/lib/auth-client.ts'
      provides: 'better-auth/react SPA client with organizationClient plugin'
      contains: 'createAuthClient'
    - path: 'apps/admin/src/lib/i18n/messages/ru.json'
      provides: 'ported ru messages, double-brace interpolation'
      contains: '{{'
  key_links:
    - from: 'src/lib/api-client.ts'
      to: 'VITE_API_ORIGIN'
      via: 'env.ts getEnv'
      pattern: 'VITE_API_ORIGIN'
    - from: 'src/lib/auth-client.ts'
      to: 'API /api/auth/*'
      via: 'createAuthClient baseURL + credentials include'
      pattern: 'createAuthClient'
    - from: 'src/main.tsx'
      to: 'QueryClient + RouterProvider'
      via: 'provider nesting'
      pattern: 'QueryClientProvider|RouterProvider'
---

<objective>
Stand up the fresh Vite + React 19 + Tailwind 4 + shadcn SPA foundation in `apps/admin`, big-bang replacing the Next.js scaffold (D-07). This plan delivers everything the surface-port plans (04/05/06) depend on but NO feature routes yet: build config, the data/auth/i18n/theme plumbing, and the TanStack Router root. It retires the Next config + server-only wrappers and installs the new stack behind a package-legitimacy gate.

Purpose: The surface port (Waves 2) cannot begin until the Vite app boots, `apiFetch`/`authClient`/`queryClient`/i18n/theme exist, and the route root is in place. This is the interface-first foundation — contracts the rest of the app builds against.
Output: bootable Vite admin shell (root route renders, providers wired), new client libs, ported i18n messages, retired Next config files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md
@apps/CLAUDE.md

<interfaces>
Reference configs to mirror (qr-menu is the in-repo Vite analog):
  apps/qr-menu/vite.config.ts, apps/qr-menu/project.json, apps/qr-menu/tsconfig.json, apps/qr-menu/src/main.tsx

Stack deltas vs qr-menu (PATTERNS.md + RESEARCH.md):

- add @tailwindcss/vite plugin (qr-menu uses no Tailwind plugin), add `@` → /src alias, server port 3001
- dev proxy: '/api' and '/v1' → http://localhost:3000, changeOrigin:false
- manualChunks: react, router (@tanstack/react-router), query (@tanstack/react-query)
- sourcemap: 'hidden', target: 'es2022' (apps/CLAUDE.md)

Decision traceability: TanStack Router (D-05), TanStack Query (D-06), react-i18next default-ru (D-09),
lightweight localStorage+class theme (D-10) are all delivered by this foundation plan.

New deps (ALL [ASSUMED] in RESEARCH Package Legitimacy Audit — gate before install):
@tanstack/react-router ^1.170.16, @tanstack/react-query ^5.101.0, react-i18next ^17.0.8,
i18next ^26.3.1, i18next-browser-languagedetector, @tailwindcss/vite ^4.3.1,
@vitejs/plugin-react ^6.0.2 (dev), @tanstack/router-devtools (dev), @tanstack/react-query-devtools (dev)
better-auth ~1.4.22 already in workspace (sub-path ./react + ./client/plugins confirmed)
Remove: next, next-intl, next-themes, server-only, eslint-config-next, @tailwindcss/postcss

env.ts (client rewrite of lib/env.ts): getEnv(key) throws in import.meta.env.PROD when missing; dev default
devDefaults = { VITE_API_ORIGIN: 'http://localhost:3000' }; drop INTERNAL_API_TOKEN + ACTIVE_BRAND_COOKIE_SECRET entirely.

api-client.ts (PATTERNS.md lines 224-294): credentials:'include', x-tenant-id from authClient live session
(session?.data?.session?.activeOrganizationId), x-brand-slug from opts.brandSlug, AbortSignal.timeout
(10s GET / 30s mutation), one retry on idempotent GET 5xx (500ms backoff), 401 → signOut + location='/login?expired=1'.
NO cookie-header forwarding, NO Set-Cookie parsing (browser handles httpOnly cookie).

auth-client.ts (PATTERNS.md lines 298-335): createAuthClient({ baseURL: VITE_API_ORIGIN,
fetchOptions: { credentials: 'include' }, plugins: [organizationClient()] }).

i18n: react-i18next init, fallbackLng 'ru', detection order [cookie, localStorage, navigator],
caches [cookie, localStorage], interpolation.escapeValue false. Transform messages:
{var} → {{var}} (22+ occurrences in ru.json); rewrite the one ICU plural
menu.publishBar.unpublishedChanges to i18next native \_one/\_few/\_other keys.

use-theme.ts (PATTERNS.md lines 533-563): localStorage 'resto-theme', toggle 'dark' class on documentElement,
default 'system' honoring prefers-color-scheme.

\_\_root.tsx: createRootRouteWithContext<{ queryClient }>; component wraps <Outlet/> in <I18nextProvider i18n={i18n}>.
CSS import order in main.tsx: '@resto/config-tailwind/tokens.css' then './styles.css'.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate for new SPA dependencies</name>
  <action>PAUSE for human verification. Do not run any pnpm install until the human approves each new package against its official registry page (links in how-to-verify). Halt on any typosquat/unofficial-publisher report.</action>
  <what-built>Package legitimacy gate for the new SPA dependencies. The researcher tagged every new package [ASSUMED] (slopcheck unavailable). Before any install runs, a human must confirm each package is the genuine official package.</what-built>
  <how-to-verify>
    Verify each package resolves to its official org on the registry (open each link, confirm publisher + repo):
    1. https://www.npmjs.com/package/@tanstack/react-router — TanStack org, repo github.com/TanStack/router
    2. https://www.npmjs.com/package/@tanstack/react-query — TanStack org, repo github.com/TanStack/query
    3. https://www.npmjs.com/package/react-i18next — i18next org, repo github.com/i18next/react-i18next
    4. https://www.npmjs.com/package/i18next — i18next org
    5. https://www.npmjs.com/package/i18next-browser-languagedetector — i18next org
    6. https://www.npmjs.com/package/@tailwindcss/vite — tailwindlabs org
    7. https://www.npmjs.com/package/@vitejs/plugin-react — vitejs org
    Confirm none introduces a postinstall script (RESEARCH verified none for react-router/better-auth; spot-check the rest).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or name any package that looks wrong (typosquat / unofficial publisher) to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Vite scaffold — package.json, configs, retire Next, install deps</name>
  <read_first>
    - apps/admin/package.json (current — deps to keep: dnd-kit, hookform/resolvers, radix-ui, react-hook-form, sonner, lucide-react, zod, class-variance-authority, clsx, tailwind-merge, tw-animate-css, @resto/domain)
    - apps/qr-menu/vite.config.ts, apps/qr-menu/project.json, apps/qr-menu/tsconfig.json, apps/qr-menu/package.json (analogs)
    - apps/admin/eslint.config.mjs (keep the shadcn-relaxed overrides; drop eslint-config-next)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (vite.config / project.json / tsconfig / index.html sections, lines 42-160; Notes #5 line 879)
    - apps/CLAUDE.md (sourcemap hidden, VITE_* no prod fallback)
  </read_first>
  <action>
    Rewrite apps/admin/package.json: change scripts to `"dev": "vite --port 3001"`, `"build": "vite build"` (keep e2e/playwright scripts). Remove next, next-intl, next-themes, server-only, eslint-config-next, @tailwindcss/postcss; add the gated deps (router, query, react-i18next, i18next, i18next-browser-languagedetector, @tailwindcss/vite; dev: @vitejs/plugin-react ^6, @tanstack/router-devtools, @tanstack/react-query-devtools). Add `react-dom` if absent. Create vite.config.ts mirroring qr-menu plus the deltas (tailwindcss() plugin, @ alias, port 3001, /api+/v1 proxy to :3000, manualChunks react/router/query, sourcemap hidden, target es2022). Create index.html (qr-menu shape, title "Resto Admin", lang ru, mount #root → /src/main.tsx). Rewrite tsconfig.json to extend @resto/config-typescript/vite.json with @/* → ./src/* path. Rewrite project.json to qr-menu shape (sourceRoot apps/admin/src, build output dist, add e2e target). Delete next.config.mjs, next-env.d.ts, instrumentation.ts, proxy.ts, postcss.config.mjs, and the stale `.next/` build dir. Update eslint.config.mjs to drop the next config while keeping the shadcn ui-dir relaxed overrides. Run the install via pnpm with `--filter @resto/admin` (only after the gate above is approved). Create src/styles.css (Tailwind 4 entry — `@import 'tailwindcss';` plus any current globals.css app-level overrides). Do NOT yet port feature components/routes — only the foundation.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && test ! -f apps/admin/next.config.mjs && test ! -f apps/admin/proxy.ts && echo "NEXT-RETIRED"; grep -c '"next"' apps/admin/package.json; grep -c "@tailwindcss/vite" apps/admin/vite.config.ts; pnpm exec nx run admin:lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - apps/admin/package.json has no `next`, `next-intl`, `next-themes`, `server-only`, `eslint-config-next` (grep count 0 for `"next"`)
    - apps/admin/{next.config.mjs,proxy.ts,next-env.d.ts,instrumentation.ts,postcss.config.mjs} do not exist
    - vite.config.ts contains `@tailwindcss/vite`, port 3001, and the /api+/v1 proxy
    - project.json sourceRoot is apps/admin/src with build output dist
    - `admin:lint` runs (config valid)
  </acceptance_criteria>
  <done>apps/admin is a Vite project shell; Next is fully retired; deps installed; lint config valid.</done>
</task>

<task type="auto">
  <name>Task 2: Client plumbing — env, auth-client, api-client, query-client, theme</name>
  <read_first>
    - apps/admin/lib/env.ts (analog to rewrite for import.meta.env)
    - apps/admin/lib/api-server.ts + api-server-internal.ts (timeout/retry/401 logic to extract; cookie-forwarding to DROP)
    - apps/admin/lib/actions/sign-in-and-bind-org.ts (BA 3-call contract: sign-in → list orgs → set-active)
    - apps/admin/components/theme-provider.tsx + theme-toggle.tsx (current theme behavior)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (env.ts/api-client/auth-client/query-client/use-theme sections, lines 192-360, 533-563)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Patterns 1, 4, 9; Pitfall 1 — read activeOrganizationId fresh from authClient)
    - apps/CLAUDE.md (AbortSignal.timeout, one-retry-on-GET, VITE_* no prod fallback)
  </read_first>
  <action>
    Create src/env.ts: `getEnv(key)` reads import.meta.env, throws when missing in PROD, falls back to devDefaults only in DEV; export VITE_API_ORIGIN. Do NOT include INTERNAL_API_TOKEN or ACTIVE_BRAND_COOKIE_SECRET. Create src/lib/auth-client.ts: `createAuthClient({ baseURL: VITE_API_ORIGIN, fetchOptions: { credentials: 'include' }, plugins: [organizationClient()] })` from better-auth/react + better-auth/client/plugins. Create src/lib/api-client.ts per PATTERNS.md lines 235-291: typed `apiFetch<T>(path, opts)` returning `{ status, ok, data }`; reads tenantId fresh from `authClient.getSession()` live value (Pitfall 1), sets accept + content-type + x-tenant-id + x-brand-slug headers, `credentials: 'include'`, `AbortSignal.timeout` (10s GET / 30s mutation), one retry on idempotent GET 500-504 with 500ms backoff, on 401 calls `authClient.signOut()` then sets `window.location.href = '/login?expired=1'` and returns a 401 result. Create src/lib/query-client.ts: QueryClient with queries staleTime 30_000, retry 1, retryDelay 500; mutations retry 0. Create src/hooks/use-theme.ts: localStorage 'resto-theme', toggle 'dark' class on documentElement, default 'system'. Strip every `server-only`/`next/*` import — none may remain in src/.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "credentials: 'include'" apps/admin/src/lib/api-client.ts; grep -c "createAuthClient" apps/admin/src/lib/auth-client.ts; grep -rn "server-only\|next/headers\|next/navigation\|'use server'" apps/admin/src/ | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/api-client.ts contains `credentials: 'include'`, `AbortSignal.timeout`, `x-tenant-id`, `x-brand-slug`, and `/login?expired=1`
    - src/lib/auth-client.ts contains `createAuthClient` with `organizationClient()` plugin and `credentials: 'include'`
    - src/env.ts throws in PROD on missing var and has no INTERNAL_API_TOKEN reference
    - Zero `server-only` / `next/*` / `'use server'` imports anywhere under apps/admin/src
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>The SPA data/auth/theme plumbing exists, server-only-free, with timeout/retry/401 parity and credentialed cross-origin session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: i18n port + Vite entry + TanStack Router root</name>
  <read_first>
    - apps/admin/lib/i18n/messages/ru.json + en.json (port + transform source)
    - apps/admin/lib/i18n/request.ts + messages-index.ts + locales.ts (next-intl config being retired)
    - apps/qr-menu/src/i18n/index.ts (detection priority analog) + apps/qr-menu/src/main.tsx (entry analog)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (i18n index + messages + main.tsx + __root.tsx sections, lines 162-188, 363-451)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 7 — interpolation + plural rewrite)
  </read_first>
  <behavior>
    - i18n.t('nav.dashboard') resolves to the ported ru string by default (fallbackLng 'ru')
    - i18n.t with an interpolation key (e.g. menu.items.withCurrency, { price: '100' }) renders the value substituted (double-brace works)
    - menu.publishBar.unpublishedChanges pluralizes correctly for count 1 / 2 / 5 via i18next native _one/_few/_other keys
    - switching i18n.changeLanguage('en') resolves the en string for the same key
  </behavior>
  <action>
    Copy ru.json + en.json into src/lib/i18n/messages/ and transform: replace every `{var}` with `{{var}}` (grep `{[a-z]` across both files to find all ~22 sites); rewrite the single ICU plural menu.publishBar.unpublishedChanges into i18next native `unpublishedChanges_one/_few/_other` keys with `{{count}}`. Create src/lib/i18n/index.ts: i18next + initReactI18next + LanguageDetector, resources { ru: { translation: ru }, en: { translation: en } }, fallbackLng 'ru', detection order [cookie, localStorage, navigator] caches [cookie, localStorage], interpolation.escapeValue false; export the i18n instance. Create src/routes/__root.tsx using `createRootRouteWithContext<{ queryClient: QueryClient }>()` whose component wraps `<Outlet/>` in `<I18nextProvider i18n={i18n}>`; export a `router` (createRouter with the root route tree — a minimal index child rendering a placeholder is fine; real routes arrive in Wave 2). Create src/main.tsx mirroring qr-menu: import tokens.css then styles.css, mount #root with `<StrictMode><QueryClientProvider client={queryClient}><RouterProvider router={router} context={{ queryClient }} /></QueryClientProvider></StrictMode>`. Add a vitest spec asserting the four <behavior> cases. Run `vite build` to prove the foundation compiles to static dist/.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "{{" apps/admin/src/lib/i18n/messages/ru.json; grep -c "unpublishedChanges_" apps/admin/src/lib/i18n/messages/ru.json; pnpm exec nx run admin:test 2>&1 | tail -8; pnpm exec nx run admin:build 2>&1 | tail -5 && test -d apps/admin/dist && echo "DIST-OK"</automated>
  </verify>
  <acceptance_criteria>
    - ru.json contains `{{` interpolation (≥20 occurrences) and `unpublishedChanges_one/_few/_other` keys (3)
    - The i18n vitest spec asserting default-ru, interpolation, plural, and en-switch passes
    - `vite build` produces apps/admin/dist/ (DIST-OK printed)
    - `admin:test` and `admin:build` exit 0
  </acceptance_criteria>
  <done>react-i18next is wired with ported ru+en messages; the Vite entry + TanStack Router root build to static dist/.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                          |
| -------------------------- | -------------------------------------------------------------------- |
| Vite build → static bundle | Anything in import.meta.env.VITE\_\* is baked into the public bundle |
| SPA browser → API          | credentialed cross-origin requests cross here                        |

## STRIDE Threat Register

| Threat ID | Category        | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                        |
| --------- | --------------- | ----------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-11  | Info Disclosure | secrets baked into VITE\_\* bundle        | mitigate    | env.ts exposes only VITE_API_ORIGIN (a public URL); INTERNAL_API_TOKEN + ACTIVE_BRAND_COOKIE_SECRET removed from src entirely; acceptance grep asserts no INTERNAL_API_TOKEN reference |
| T-076-12  | Info Disclosure | source maps shipped to prod               | mitigate    | vite.config sourcemap: 'hidden' (apps/CLAUDE.md), not 'true'                                                                                                                           |
| T-076-13  | Tampering       | stale activeOrganizationId in x-tenant-id | mitigate    | api-client reads activeOrganizationId fresh from authClient.getSession() per request (Pitfall 1), not a cached prop                                                                    |
| T-076-14  | Elevation       | XSS exfiltrating session                  | mitigate    | session cookie is httpOnly — JS cannot read it; api-client never stores a token; only credentials:'include' sends the browser-managed cookie                                           |
| T-076-SC  | Tampering       | npm installs of [ASSUMED] packages        | mitigate    | blocking-human package-legitimacy checkpoint before install (this plan, Task 0 gate); all 7 new packages verified against official orgs                                                |

</threat_model>

<verification>
- `admin:lint`, `admin:typecheck`, `admin:test`, `admin:build` all exit 0
- `vite build` emits apps/admin/dist/
- Zero server-only / next/* imports under apps/admin/src
- Package legitimacy gate approved before any install ran
</verification>

<success_criteria>

- SC-1 (foundation): apps/admin is a Vite + React + shadcn SPA shell (no Next.js) that builds to static dist/
- SC-2 (foundation): the client data layer is credentialed-session-only; no INTERNAL_API_TOKEN in the bundle
- SC-3 (foundation): auth-client + api-client establish the cookie-session + 401/expired client behavior
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-03-SUMMARY.md` when done
</output>
