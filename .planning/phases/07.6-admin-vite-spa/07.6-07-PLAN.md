---
phase: 07.6-admin-vite-spa
plan: 07
type: execute
wave: 3
depends_on: [07.6-04, 07.6-05, 07.6-06]
files_modified:
  - .github/workflows/ci.yml
  - apps/admin/e2e/adm-00-smoke-walk.spec.ts
  - apps/admin/playwright.config.ts
  - apps/admin/.env.example
  - docs/runbooks/admin-static-deploy.md
autonomous: false
requirements: [SC-4, SC-5]
user_setup:
  - service: cloudflare
    why: 'Static admin hosting (Pages) + admin.resto.app DNS/TLS, mirroring the qr-menu static deploy (D-11)'
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: 'Cloudflare Dashboard → My Profile → API Tokens → Create Token (scoped to Pages:Edit + the resto.app zone DNS:Edit)'
      - name: CLOUDFLARE_ACCOUNT_ID
        source: 'Cloudflare Dashboard → account home (right sidebar Account ID)'
      - name: VITE_API_ORIGIN
        source: 'Set in the Cloudflare Pages project env (production = https://api.resto.app) — baked into the bundle at build'
    dashboard_config:
      - task: "Create the Pages project 'resto-admin' (direct-upload / wrangler), no framework preset, build output apps/admin/dist"
        location: 'Cloudflare Dashboard → Workers & Pages → Create → Pages'
      - task: 'Add the custom domain admin.resto.app to the Pages project (auto-provisions TLS) and confirm the CNAME in the resto.app zone'
        location: 'Cloudflare Dashboard → resto-admin Pages project → Custom domains'
must_haves:
  truths:
    - 'apps/admin builds to static assets and deploys to Cloudflare Pages at admin.resto.app over HTTPS with auto TLS'
    - 'Deep links (e.g. /dashboard/<brand>/menu/items) resolve via SPA fallback to index.html (no _redirects loop)'
    - 'CD publishes the admin static bundle on merge to main, alongside the existing four-surface CI/CD'
    - 'The ADM-00 Playwright smoke-walk runs against the SPA and asserts login → dashboard → brand list → menu CRUD parity'
    - 'The Phase 7.5 four-surface external smoke (api /healthz, admin login at admin.resto.app, website tenant menu, qr-menu SPA) is retargeted to admin.resto.app and passes over HTTPS (this is SC-5, distinct from the ADM-00 SPA walk)'
  artifacts:
    - path: '.github/workflows/ci.yml'
      provides: 'admin static build + wrangler pages deploy on merge to main'
      contains: 'wrangler pages deploy'
    - path: 'apps/admin/e2e/adm-00-smoke-walk.spec.ts'
      provides: 'ported SPA smoke-walk (no Next webServer)'
      contains: 'brand-switcher-trigger'
    - path: 'docs/runbooks/admin-static-deploy.md'
      provides: 'admin static deploy + SPA-fallback + rollback runbook'
      contains: 'SPA fallback'
  key_links:
    - from: '.github/workflows/ci.yml (deploy job)'
      to: 'wrangler pages deploy apps/admin/dist'
      via: 'CLOUDFLARE_API_TOKEN + project resto-admin'
      pattern: 'wrangler pages deploy'
    - from: 'admin.resto.app'
      to: 'Cloudflare Pages project'
      via: 'custom-domain CNAME + auto TLS'
      pattern: 'admin.resto.app'
---

<objective>
Deploy the new Vite admin SPA as a static site on Cloudflare Pages at `admin.resto.app` with auto TLS and SPA fallback, wire CD to publish on merge to main alongside the existing pipeline, and prove parity + reachability via the ported ADM-00 Playwright smoke-walk and the four-surface HTTPS smoke (Phase 7.5 SC#5). This folds the admin static-deploy slice deferred from Phase 7.5 (D-11) and is the final acceptance gate for the phase (SC-4, SC-5).

Purpose: A built SPA that never ships is not done. This closes SC-4 (static deploy + CD + admin host/TLS) and SC-5 (four-surface smoke passes with the new static admin).
Output: CD deploy step, ported Playwright config + smoke-walk, env example, deploy runbook, and a verified live admin host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.5-production-deploy/07.5-CONTEXT.md

<interfaces>
Depends on the full SPA surface (04/05/06) building to apps/admin/dist (admin:build green).
Cloudflare Pages SPA behavior (RESEARCH Pattern 10 + Pitfall 7): NO 404.html in dist → native SPA fallback to index.html;
  DO NOT add a _redirects `/* /index.html 200` (triggers Cloudflare loop-detection, silently ignored).
Build: `pnpm exec nx run admin:build` → apps/admin/dist; project root for Pages = apps/admin/dist; no framework preset.
CD: `wrangler pages deploy apps/admin/dist --project-name resto-admin` (install wrangler in CI; Env availability table
  notes wrangler not yet verified → add the install step). Gate behind merge-to-main only (not PRs).
VITE_API_ORIGIN is build-time baked (Cloudflare Pages env) → must be present in the build job env; env.ts throws if missing in PROD.
Existing CI (.github/workflows/ci.yml): jobs install/affected/format/commitlint/secret-scan/audit/openapi-drift/docker-api;
  add the admin static build+deploy as a new job gated on `github.ref == refs/heads/main` and the affected/build success.
ADM-00 smoke-walk (apps/admin/e2e/adm-00-smoke-walk.spec.ts): scenarios assert data-testid="brand-switcher-trigger",
  the zero-brand EmptyState copy ("your tenant has no brands yet"), the 3-brand dropdown switcher, login → /dashboard.
  Port playwright.config.ts: replace the Next `next dev`/`next start` webServer with `vite preview --port 3001`
  (or vite dev), keep ADMIN_E2E_API_ORIGIN. The operator-session fixture + seed-tenants stay (they hit the API).
Four-surface smoke (07.5 SC#5): api /healthz, admin login page, website tenant menu, qr-menu SPA — extend/reuse the
  07.5 external smoke to point admin at admin.resto.app (the new static host).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the ADM-00 Playwright smoke-walk to the Vite SPA</name>
  <read_first>
    - apps/admin/e2e/adm-00-smoke-walk.spec.ts (the scenarios + testids to preserve)
    - apps/admin/playwright.config.ts (current Next webServer config to replace)
    - apps/admin/e2e/fixtures/ (operator-session + seed-tenants — keep)
    - apps/admin/src/routes/(protected)/dashboard/_layout.tsx (the ported shell — confirm testids/copy match)
  </read_first>
  <action>
    Update apps/admin/playwright.config.ts: replace the Next `webServer` command (`next dev`/`next start`) with the Vite equivalent — `pnpm exec nx run admin:build && pnpm exec vite preview --port 3001` (cwd apps/admin), reusing the existing port 3001, baseURL, and ADMIN_E2E_API_ORIGIN. Keep the operator-session fixture and seed-tenants beforeAll unchanged (they exercise the live API). Update adm-00-smoke-walk.spec.ts only where selectors changed in the port (the data-testid="brand-switcher-trigger" and the EmptyState copy were preserved in 04, so most scenarios are unchanged); adjust any route paths to the new brand-in-URL shape (/dashboard → /dashboard or /dashboard/$brandSlug) so the smoke-walk reaches login → dashboard → brand list → a menu CRUD round-trip. Run the e2e suite locally against a running API to confirm green.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "next dev\|next start" apps/admin/playwright.config.ts; grep -c "vite preview\|vite\b" apps/admin/playwright.config.ts; grep -c "brand-switcher-trigger" apps/admin/e2e/adm-00-smoke-walk.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts has no `next dev`/`next start` webServer; uses a Vite serve command
    - The smoke-walk still asserts data-testid="brand-switcher-trigger" and the zero-brand EmptyState copy
    - The spec reaches login → dashboard → brand list → menu CRUD on the SPA
  </acceptance_criteria>
  <done>The ADM-00 parity smoke-walk runs against the Vite SPA via a Vite webServer; brand + menu scenarios pass.</done>
</task>

<task type="auto">
  <name>Task 2: CD deploy job (wrangler pages) + env example + runbook</name>
  <read_first>
    - .github/workflows/ci.yml (job structure, the affected/build matrix, secret usage)
    - apps/qr-menu/project.json + the 07.5 qr-menu static deploy approach (mirror the static-hosting pattern)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 10 + Pitfall 7 — Cloudflare SPA fallback, no _redirects)
    - apps/admin/src/env.ts (VITE_API_ORIGIN is build-time, fails loud in PROD)
  </read_first>
  <action>
    Add a `deploy-admin` job to .github/workflows/ci.yml gated on `if: github.ref == 'refs/heads/main' && github.event_name == 'push'` and depending on the install/affected build success. Steps: checkout, setup node/pnpm, install, `pnpm exec nx run admin:build` with `VITE_API_ORIGIN` from the job env (set to https://api.resto.app), install wrangler (`pnpm dlx wrangler` or a pinned version), then `wrangler pages deploy apps/admin/dist --project-name resto-admin` using `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` GitHub secrets. Do NOT create a `_redirects` file (Pitfall 7 — Cloudflare native SPA fallback handles routing; ensure no 404.html is emitted). Create apps/admin/.env.example documenting `VITE_API_ORIGIN` (no prod fallback) and dev default. Create docs/runbooks/admin-static-deploy.md covering: the Pages project + custom-domain/TLS setup (founder steps), the CD flow, the SPA-fallback behavior (no _redirects), VITE_API_ORIGIN build-time injection, and app-only rollback (redeploy a prior Pages deployment / `wrangler pages deployment` list+rollback). Mirror the qr-menu static-deploy conventions where they exist.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "wrangler pages deploy" .github/workflows/ci.yml; grep -c "resto-admin" .github/workflows/ci.yml; test ! -f apps/admin/public/_redirects && echo "NO-REDIRECTS-OK"; grep -c "VITE_API_ORIGIN" apps/admin/.env.example; test -f docs/runbooks/admin-static-deploy.md && echo "RUNBOOK-OK"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml has a `deploy-admin` job running `wrangler pages deploy apps/admin/dist --project-name resto-admin`, gated to main pushes only
    - No `_redirects` file is added (NO-REDIRECTS-OK printed)
    - apps/admin/.env.example documents VITE_API_ORIGIN with no prod fallback
    - docs/runbooks/admin-static-deploy.md exists (RUNBOOK-OK)
  </acceptance_criteria>
  <done>CD publishes the static admin to Cloudflare Pages on merge to main; SPA fallback is native; runbook documents host/TLS/rollback.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Cloudflare Pages project + admin.resto.app DNS/TLS + prod CORS</name>
  <action>PAUSE for human action. The founder must complete the Cloudflare account-side setup (Pages project, API token GitHub secrets, custom domain + TLS, prod CORS/trusted-origins) per how-to-verify before the live four-surface smoke can run. Resume only on the "live" signal.</action>
  <what-built>The CD job, build, and SPA are ready. Cloudflare account-side setup that no CLI can do without founder credentials must be completed before the live host exists.</what-built>
  <how-to-verify>
    1. Create a Cloudflare API token (Pages:Edit + resto.app zone DNS:Edit) and add it as the GitHub secret `CLOUDFLARE_API_TOKEN`; add `CLOUDFLARE_ACCOUNT_ID` as a secret.
    2. Create the Pages project `resto-admin` (Workers & Pages → Create → Pages → direct upload, no framework preset).
    3. In the Pages project env, set `VITE_API_ORIGIN=https://api.resto.app` (production).
    4. Add the custom domain `admin.resto.app` to the project (auto-provisions TLS) and confirm the CNAME in the resto.app zone.
    5. Add `https://admin.resto.app` to the API's production `CORS_ALLOWED_ORIGINS` and `BETTER_AUTH_TRUSTED_ORIGINS` (secret store) and confirm `AUTH_COOKIE_DOMAIN=.resto.app` — per the .env.example updated in 07.6-02.
    Confirm: visiting https://admin.resto.app returns the SPA login page over HTTPS, and a deep link (e.g. /login?next=/dashboard) loads (SPA fallback).
  </how-to-verify>
  <resume-signal>Type "live" once admin.resto.app serves the SPA over HTTPS and the API CORS/trusted-origins include the admin host, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Four-surface HTTPS smoke against the live static admin</name>
  <read_first>
    - the Phase 7.5 external four-surface smoke (07.5-10 plan / any e2e smoke that hits api/admin/website/qr-menu) — reuse + retarget admin
    - apps/admin/e2e/adm-00-smoke-walk.spec.ts (the ported parity walk)
    - .planning/phases/07.5-production-deploy/07.5-CONTEXT.md (SC#5 four-surface smoke definition)
  </read_first>
  <action>
    Two DISTINCT verifications run here — keep them separate:
    (A) FOUR-SURFACE EXTERNAL SMOKE (SC-5, from Phase 7.5 SC#5): extend/retarget the Phase 7.5 four-surface external smoke script/spec so the ADMIN surface points at https://admin.resto.app (the new static host) instead of the retired admin ECS service. The four surfaces it asserts: api `/healthz` 200, admin login page reachable over HTTPS at admin.resto.app (and SPA deep-link fallback works), website tenant menu renders, qr-menu SPA loads. Locate the existing Phase 7.5 external smoke (07.5-10 plan / the e2e or script that hits api+admin+website+qr-menu) and change the admin target host to admin.resto.app; ensure it is wired into CI. This script — NOT the ADM-00 walk — is what SC-5 requires to pass.
    (B) ADM-00 SPA PARITY WALK (separate, SC-1 regression guard): run the ported ADM-00 Playwright smoke-walk (apps/admin/e2e/adm-00-smoke-walk.spec.ts, via `admin:e2e`) against the live (or preview) admin to confirm login → dashboard → brand → menu CRUD works end-to-end with the production cookie-session + CORS. This is the SPA functional walk, distinct from the four-surface external liveness smoke.
    Record both results (and any CORS/cookie adjustments) in docs/runbooks/admin-static-deploy.md. The four-surface smoke (A) passing with admin at admin.resto.app is the phase's final SC-5 acceptance gate.
  </action>
  <verify>
    <automated>curl -fsS -o /dev/null -w "admin liveness HTTP %{http_code}\n" https://admin.resto.app/ ; grep -rl "admin.resto.app" .github/workflows e2e tools 2>/dev/null | head; SMOKE=$(grep -rl "healthz" e2e tools .github/workflows 2>/dev/null | head -1); echo "smoke spec: $SMOKE"; grep -c "admin.resto.app" "$SMOKE" 2>/dev/null; pnpm exec nx run admin:e2e 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - (SC-5) The Phase 7.5 four-surface external smoke script/spec is updated to target the admin surface at admin.resto.app and PASSES over HTTPS (api /healthz + admin login + website tenant menu + qr-menu SPA)
    - The four-surface smoke spec/CI references `admin.resto.app` (grep non-zero on the smoke spec that also references /healthz) — proving the admin target was retargeted off the retired ECS host
    - Liveness: `curl -f https://admin.resto.app/` returns 2xx (the static SPA is reachable over HTTPS)
    - (distinct) The ADM-00 SPA parity walk (`admin:e2e`) passes against the deployed/preview SPA: login → dashboard → brand → menu CRUD — this is the SPA functional regression guard, NOT the four-surface external smoke
    - `admin:e2e` exits 0
    - The runbook records both the four-surface smoke result and the ADM-00 walk result
  </acceptance_criteria>
  <done>The static admin is live over HTTPS at admin.resto.app; the four-surface external smoke (7.5 SC#5, retargeted to admin.resto.app) passes — and, separately, the ADM-00 SPA parity walk passes against the deployed SPA.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                        |
| ------------------------------- | -------------------------------------------------- |
| static bundle on CDN            | Public assets; anything baked in is world-readable |
| CD secrets (Cloudflare token)   | Deploy credentials cross the CI boundary           |
| admin.resto.app ↔ api.resto.app | Cross-subdomain credentialed session in production |

## STRIDE Threat Register

| Threat ID | Category        | Component                                           | Disposition | Mitigation Plan                                                                                                                                                    |
| --------- | --------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-076-30  | Info Disclosure | secrets in the static bundle                        | mitigate    | only VITE_API_ORIGIN (public URL) is build-injected; env.ts has no secret keys; source maps are 'hidden' (07.6-03); no INTERNAL_API_TOKEN reachable (07.6-06 gate) |
| T-076-31  | Tampering       | SPA-fallback misconfig leaks a 404.html / loop      | mitigate    | no \_redirects file (Pitfall 7); no 404.html emitted → native Cloudflare SPA fallback; acceptance asserts NO-REDIRECTS-OK                                          |
| T-076-32  | Spoofing        | production CORS allows the admin origin too broadly | mitigate    | CORS_ALLOWED_ORIGINS is an explicit allowlist incl. admin.resto.app (never `*` with credentials); confirmed in the human-action gate + 07.6-02 docs                |
| T-076-33  | Elevation       | leaked Cloudflare CD token                          | mitigate    | token scoped to Pages:Edit + the resto.app zone only (least privilege); stored as a GitHub secret, never in the repo; deploy job gated to main pushes              |
| T-076-34  | Tampering       | wrong VITE_API_ORIGIN ships → traffic to bad API    | mitigate    | env.ts throws in PROD when VITE_API_ORIGIN is missing; build job sets it explicitly; four-surface smoke catches a wrong origin (login would fail)                  |

</threat_model>

<verification>
- `admin:build` → dist/, `admin:e2e` exits 0
- ci.yml `deploy-admin` job present, gated to main, runs wrangler pages deploy
- No _redirects / 404.html in the deploy artifact
- admin.resto.app serves the SPA over HTTPS (curl liveness 2xx)
- (SC-5) the Phase 7.5 four-surface external smoke is retargeted to admin.resto.app and passes — verified separately from the ADM-00 SPA parity walk (admin:e2e)
</verification>

<success_criteria>

- SC-4: admin builds to static + deploys via Cloudflare Pages with admin host + TLS; CD publishes on merge to main
- SC-5: the four-surface smoke (7.5 SC#5) passes with the new static admin reachable over HTTPS
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-07-SUMMARY.md` when done
</output>
