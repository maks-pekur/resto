---
phase: 07.6-admin-vite-spa
plan: 09
type: execute
deferred: true # DEFERRED 2026-06-26 (split decision — see 07.6-REVIEWS.md): per-brand publish rework moved to its own phase before Phase 8. Do NOT execute under Phase 7.6. Retained as reference (re-plan against 07.6-08-RESEARCH.md + this review). The 3 cross-brand read leaks are fixed now via a quick task instead.
wave: 2
depends_on: [07.6-08]
files_modified:
  - packages/events/src/contracts/catalog.ts
  - packages/events/src/index.ts
  - apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts
  - apps/api/src/contexts/catalog/application/get-published-menu.service.ts
  - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts
  - apps/api/test/e2e/catalog.e2e.spec.ts
  - apps/api/test/e2e/menu-brand-response.e2e.spec.ts
autonomous: true
requirements: [CR-04]
must_haves:
  truths:
    - 'finalizeMenuPublish UPSERTs the per-brand catalog_menu_version row (target (brand_id, tenant_id)), bumping only the publishing brand'
    - 'menu_first_published / menu_republished events carry brandId (v2 subject catalog.*.v2); a clean cut-over (no v1 emission), no subscriber edit (catalog has no NATS consumer)'
    - 'The public /v1/menu and /v1/menu/items/:id ETag derives from current(tenantId, brandId) — per-brand version; ETag value shape "<int>" unchanged; qr-menu/website 304 still works per brand'
    - "Brand A publish bumps Brand A's menu_version and leaves Brand B's unchanged (e2e-proven)"
    - "Brand A's /v1/menu ETag changes after Brand A publish; Brand B's ETag is unchanged (e2e-proven)"
    - 'POST /v1/catalog/publish and GET /v1/catalog/draft-diff without x-brand-slug return 403 (e2e-proven)'
  artifacts:
    - path: 'packages/events/src/contracts/catalog.ts'
      provides: 'MenuFirstPublishedV2 + MenuRepublishedV2 with brandId, subject catalog.*.v2'
      contains: 'menu_first_published.v2'
    - path: 'apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts'
      provides: 'finalizeMenuPublish per-brand UPSERT + v2 envelopes'
      contains: 'catalogMenuVersion.brandId'
    - path: 'apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts'
      provides: 'ETag from current(tenantId, brandId)'
      contains: 'menuVersions.current('
    - path: 'apps/api/test/e2e/catalog.e2e.spec.ts'
      provides: 'cross-brand version-isolation + @RequireBrand 403 tests'
      contains: 'brand'
    - path: 'apps/api/test/e2e/menu-brand-response.e2e.spec.ts'
      provides: 'per-brand ETag isolation test'
      contains: 'ETag'
  key_links:
    - from: 'apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts'
      to: 'MenuVersionPort.current'
      via: 'current(tenantId, brandId) from ALS brand'
      pattern: 'menuVersions\\.current\\([^)]*brand'
    - from: 'apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts'
      to: 'catalog_menu_version (brand_id, tenant_id)'
      via: 'onConflictDoUpdate target [brandId, tenantId]'
      pattern: 'onConflictDoUpdate'
---
> **BUILT ON AN ENTITY THAT NO LONGER EXISTS — do not execute as written.** It completes the
> per-brand publish rework begun in 07.6-08, and the Brand level was removed by phase 10.2. See
> 07.6-08's banner.


<objective>
Complete the per-brand publish rework (CR-04) on top of the data path from 07.6-08: rewrite `finalizeMenuPublish` to UPSERT the per-brand `catalog_menu_version` row and emit brandId-carrying v2 events, repoint the public ETag derivation to the per-brand version, and add the cross-brand isolation e2e tests that prove the whole guarantee (Brand A publish does not touch Brand B's version, ETag, or draft; the three routes 403 without a brand).

This plan depends on 07.6-08 (the brand-keyed schema + migration + brand-aware port/adapter + brand-scoped services/controller MUST already be in place). Executing this plan closes the 08→09 compile seam (the `current(tenantId, brandId)` callers).

Purpose: This is the half of CR-04 that touches the wire (events) and the CDN-visible public read (ETag), plus the regression net. Without per-brand finalize + ETag, the schema change is inert; without the tests, the isolation guarantee is unproven.
Output: v2 event contracts, brand-scoped finalize UPSERT, per-brand public ETag, and passing cross-brand isolation e2e tests.

NOTE TO REVIEWER (founder gate): review this plan together with 07.6-08 before execution — they are one feature split only for context budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.6-admin-vite-spa/07.6-08-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-08-PLAN.md
@CLAUDE.md
@packages/events/CLAUDE.md
@apps/CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from source — no exploration required. -->

Current catalog event contracts (packages/events/src/contracts/catalog.ts:5-25):

```typescript
export const MenuFirstPublishedV1Payload = z.object({
  tenantId: TenantId,
  version: z.number().int().positive(),
});
export const MenuFirstPublishedV1 = defineEventContract({
  type: 'catalog.menu_first_published.v1',
  payload: MenuFirstPublishedV1Payload,
});
export const MenuRepublishedV1Payload = z.object({
  tenantId: TenantId,
  version: z.number().int().positive(),
});
export const MenuRepublishedV1 = defineEventContract({
  type: 'catalog.menu_republished.v1',
  payload: MenuRepublishedV1Payload,
});
```

`defineEventContract` and `buildEnvelope` come from '../envelope' / @resto/events. `buildEnvelope(Contract, payload, { tenantId })` is the only sanctioned envelope constructor (ADR-0020 I-4 — correlationId from OTel span; never randomUUID()).

Current finalizeMenuPublish (catalog-drizzle.repository.ts:836-889): UPSERTs `catalogMenuVersion` with `target: [catalogMenuVersion.tenantId]`, set `menu_version + 1`; reads `tenants.menuFirstPublishedAt`; on first publish stamps it + appends `buildEnvelope(MenuFirstPublishedV1, { tenantId, version }, { tenantId })`, else `MenuRepublishedV1`. Runs under `withTenant` (ALS path) or `withTenantId` (background path) — the `hasAls` branch. NOTE 07.6-08 already widened the input type to `{ tenantId: TenantId; brandId: string }` and the call sites pass brandId, but the UPSERT body + events were left tenant-keyed behind a marked seam.

menuFirstPublishedAt decision (locked in 07.6-08): stays TENANT-WIDE (Option A). The first-publish/republish branch keeps reading/stamping `tenants.menuFirstPublishedAt` per tenant — do NOT add a brands column. Only the version-row UPSERT and the event payloads become per-brand.

Audit subscriber (RESOLVED — do not edit): nats-audit-subscriber.ts subscribes only to `tenancy.>`, `identity.>`, `ordering.>`. No `catalog.>` subscription exists, so v2 has no consumer to update — clean cut-over, emit v2 only.

Public ETag callers to repoint:

- public-menu.controller.ts:174 (`menu`) and :196 (`item`): `const version = await wrap(() => this.menuVersions.current(TenantId.parse(ctx.tenantId)))`. After 07.6-08 the port is `current(tenantId, brandId)`. Read brandId from ALS via `getBrandId()` (the route already called `requireBrandOr404()` so a brand is bound) or `requireBrandContext()`. ETag value shape `'"' + version + '"'` UNCHANGED; Cache-Control headers UNCHANGED (`s-maxage=300, stale-while-revalidate=60`); no Set-Cookie added.
- get-published-menu.service.ts:21: `const version = await this.versions.current(tenantId)` then `const brandId = requireBrandContext()`. Reorder so brandId is read first, then `current(tenantId, brandId)`.

e2e fixtures (already exist — reuse, do not reinvent):

- apps/api/test/e2e/catalog.e2e.spec.ts has setup giving `{ slug, brandSlug, authed: { cookie, 'x-tenant-id', 'x-brand-slug' } }` per cafe; a `readMenuVersion()` helper (~line 758) queries the DB; publish via `inject({ method:'POST', url:'/v1/catalog/publish', headers: authed })`.
- apps/api/test/e2e/catalog-brand-isolation.e2e.spec.ts shows the pattern for a SECOND brand under the SAME tenant: `createBrand(brandBSlug, 'Brand B')` + `hdr(brandBSlug)`. Use this to build the two-brand, one-tenant fixture for cross-brand isolation.
- apps/api/test/e2e/menu-brand-response.e2e.spec.ts is the ETag/304 + no-Set-Cookie regression net.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2 catalog publish event contracts (brandId) + rewrite finalizeMenuPublish to per-brand UPSERT + v2 events</name>
  <files>packages/events/src/contracts/catalog.ts, packages/events/src/index.ts, apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts</files>
  <read_first>
    - packages/events/src/contracts/catalog.ts (v1 contracts to mirror as v2)
    - packages/events/src/index.ts (export surface — v2 must be exported)
    - apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts:836-889 (finalizeMenuPublish to rewrite)
    - apps/api/src/contexts/catalog/infrastructure/postgres-menu-version.adapter.ts (the per-brand UPSERT pattern reference from 07.6-08; and currentStop as the read pattern)
    - packages/events/CLAUDE.md (buildEnvelope / ADR-0020 I-4 rules)
  </read_first>
  <action>
    packages/events/src/contracts/catalog.ts: add `MenuFirstPublishedV2Payload` = `z.object({ tenantId: TenantId, brandId: z.string().uuid(), version: z.number().int().positive() })` and `MenuFirstPublishedV2 = defineEventContract({ type: 'catalog.menu_first_published.v2', payload: ... })`; likewise `MenuRepublishedV2Payload` + `MenuRepublishedV2` (`type: 'catalog.menu_republished.v2'`). Keep the v1 contracts in the file (do not delete — they remain valid wire definitions even though no longer emitted). Export the four new symbols from packages/events/src/index.ts alongside the existing v1 exports.

    catalog-drizzle.repository.ts finalizeMenuPublish: it already accepts `{ tenantId, brandId }` (from 07.6-08). Rewrite the body:
    - Change the UPSERT to per-brand: `scoped.insertInto(schema.catalogMenuVersion, { brandId, menuVersion: 2 }).onConflictDoUpdate({ target: [schema.catalogMenuVersion.brandId, schema.catalogMenuVersion.tenantId], set: { menuVersion: sql\`${schema.catalogMenuVersion.menuVersion} + 1\` } }).returning({ menuVersion: schema.catalogMenuVersion.menuVersion })`. ScopedTx auto-injects tenant_id on INSERT; brandId is supplied explicitly (mirrors catalog_brand_stop_version usage). Initial value 2 on first insert matches the existing convention (first publish → version 2; backfilled rows start at the inherited integer).
    - Keep the `tenants.menuFirstPublishedAt` read + first-publish stamp TENANT-WIDE (decision Option A): unchanged `select menuFirstPublishedAt where tenants.id = tenantId`, and on first publish `update tenants set menuFirstPublishedAt = now()`.
    - Swap the outbox envelopes to v2: first publish → `buildEnvelope(MenuFirstPublishedV2, { tenantId, brandId, version }, { tenantId })`; republish → `buildEnvelope(MenuRepublishedV2, { tenantId, brandId, version }, { tenantId })`. Update the imports (drop the v1 import only if it is now unused in this file; otherwise leave). Do NOT emit v1 (clean cut-over; no consumer — see interfaces).
    - Both withTenant (ALS) and withTenantId (background, from DelayedPublishService.doPublish(tenantId, brandId)) branches are preserved; the `op` closure now closes over `brandId` too.
    - Remove the now-stale 07.6-08 seam comment.

  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -q 'menu_first_published.v2' packages/events/src/contracts/catalog.ts && grep -q "brandId: z.string().uuid()" packages/events/src/contracts/catalog.ts && grep -q 'catalogMenuVersion.brandId, schema.catalogMenuVersion.tenantId' apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts && grep -q 'MenuFirstPublishedV2' apps/api/src/contexts/catalog/infrastructure/catalog-drizzle.repository.ts && pnpm --filter @resto/events typecheck</automated>
  </verify>
  <acceptance_criteria>
    - contracts/catalog.ts contains `menu_first_published.v2` and `menu_republished.v2` and both v2 payloads include `brandId: z.string().uuid()`; v1 contracts still present.
    - packages/events/src/index.ts exports MenuFirstPublishedV2 + MenuRepublishedV2.
    - finalizeMenuPublish UPSERT target is `[schema.catalogMenuVersion.brandId, schema.catalogMenuVersion.tenantId]` and inserts `{ brandId, ... }`.
    - finalize emits `MenuFirstPublishedV2` / `MenuRepublishedV2` via `buildEnvelope(...)` with `{ tenantId, brandId, version }`; no `MenuFirstPublishedV1`/`MenuRepublishedV1` buildEnvelope call remains in finalizeMenuPublish.
    - menuFirstPublishedAt stays tenant-keyed (grep: `tenants.menuFirstPublishedAt` still read/updated by tenantId; no `brands.menuFirstPublishedAt` introduced).
    - `pnpm --filter @resto/events typecheck && pnpm --filter @resto/api typecheck` pass.
  </acceptance_criteria>
  <done>v2 contracts carry brandId and are exported; finalizeMenuPublish UPSERTs the per-brand version row and emits v2 events; first-publish flag stays tenant-wide; events + api typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the public ETag + GetPublishedMenuService to the per-brand version (closes the 08→09 seam)</name>
  <files>apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts, apps/api/src/contexts/catalog/application/get-published-menu.service.ts</files>
  <read_first>
    - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts:164-209 (menu + item ETag derivation, both call current(tenantId))
    - apps/api/src/contexts/catalog/application/get-published-menu.service.ts (current(tenantId) then requireBrandContext())
    - apps/CLAUDE.md (the public-read caching contract: menu ETag = menuVersion, Set-Cookie-free, never Cache-Control: private/no-store; a stop changes only the availability ETag, never the menu ETag)
  </read_first>
  <action>
    public-menu.controller.ts: in both `menu` (line ~174) and `item` (line ~196), after `requireBrandOr404()`, obtain the bound brandId — use `requireBrandContext()` (a brand is guaranteed bound because requireBrandOr404 already passed) — and call `this.menuVersions.current(TenantId.parse(ctx.tenantId), brandId)`. Keep `const etag = '"' + version.toString() + '"'`, the If-None-Match 304 branch, the `Cache-Control` headers, and the absence of Set-Cookie EXACTLY as-is — only the version source changes. Import `requireBrandContext` from '@resto/db' if not already imported (requireBrandOr404 helper is already used).

    get-published-menu.service.ts: reorder so `const brandId = requireBrandContext()` is read before the version, then `const version = await this.versions.current(tenantId, brandId)`. Leave `loadPublishedMenu(tenantId, version, brandId)` unchanged.

    Do NOT change the availability path (`/v1/menu/availability` uses stopVersion via currentStop — already brand-keyed).

  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm --filter @resto/api typecheck && grep -c 'menuVersions.current(' apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts | awk '{exit ($1>=2)?0:1}' && grep -q 'this.versions.current(tenantId, brandId)' apps/api/src/contexts/catalog/application/get-published-menu.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - public-menu.controller.ts: both ETag derivations call `this.menuVersions.current(` with a brandId argument (grep `menuVersions.current(` lines each have a second arg / brand var).
    - ETag value still `'"' + version.toString() + '"'`; Cache-Control lines unchanged (`s-maxage=300, stale-while-revalidate=60`); no `Set-Cookie` and no `Cache-Control: private`/`no-store` added.
    - get-published-menu.service.ts: `requireBrandContext()` read before `this.versions.current(tenantId, brandId)`.
    - `pnpm --filter @resto/api typecheck` passes with NO residual `current()` arity errors anywhere (the 08→09 seam is now fully closed).
  </acceptance_criteria>
  <done>The public /v1/menu and /v1/menu/items/:id ETag derives from the per-brand version; the ETag shape, 304 handling, Cache-Control, and Set-Cookie-free contract are unchanged; the full api typecheck passes (no arity seam left).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-brand isolation e2e tests (version, ETag, @RequireBrand 403, draft-diff)</name>
  <files>apps/api/test/e2e/catalog.e2e.spec.ts, apps/api/test/e2e/menu-brand-response.e2e.spec.ts</files>
  <read_first>
    - apps/api/test/e2e/catalog.e2e.spec.ts (setup fixtures, readMenuVersion ~line 758, publish via inject)
    - apps/api/test/e2e/catalog-brand-isolation.e2e.spec.ts (the createBrand(slug,name) + hdr(brandSlug) two-brand-one-tenant pattern)
    - apps/api/test/e2e/menu-brand-response.e2e.spec.ts (ETag/304/no-Set-Cookie net to extend)
  </read_first>
  <behavior>
    - Brand A publish bumps Brand A's catalog_menu_version and leaves Brand B's row unchanged (two brands under ONE tenant; assert via DB read of each brand's row).
    - Two distinct brand publishes under one tenant produce independent version sequences (A at v_A, B at v_B, no coupling).
    - GET /v1/menu for Brand A's host returns an ETag that changes after Brand A publishes; Brand B's /v1/menu ETag is unchanged by Brand A's publish.
    - POST /v1/catalog/publish WITHOUT x-brand-slug → 403 (BrandScopeGuard via @RequireBrand).
    - DELETE /v1/catalog/publish WITHOUT x-brand-slug → 403.
    - GET /v1/catalog/draft-diff WITHOUT x-brand-slug → 403.
    - draft-diff for Brand A does not include Brand B's draft items (cross-brand draft leak closed).
  </behavior>
  <action>
    Extend apps/api/test/e2e/catalog.e2e.spec.ts: add a describe block that provisions ONE tenant with TWO brands (reuse the createBrand pattern from catalog-brand-isolation.e2e). Add the version-isolation test (read each brand's catalog_menu_version row before/after an A-only publish; assert A bumped, B unchanged) and the independent-sequence test. Add the three @RequireBrand 403 tests (publish POST, publish DELETE, draft-diff GET) by injecting WITHOUT the x-brand-slug header but WITH a valid session + x-tenant-id, asserting statusCode 403. Add the draft-diff brand-scoping test (create a draft item in each brand; assert Brand A's draft-diff lists only A's item).

    Extend apps/api/test/e2e/menu-brand-response.e2e.spec.ts: add a per-brand ETag-isolation test — two brand hosts under one tenant, capture each /v1/menu ETag, publish Brand A, assert A's ETag changed and B's ETag is identical to before; assert no Set-Cookie on either response (the existing net) still holds.

    Reuse existing helpers; do not add new infra. If the readMenuVersion helper is scoped to a single brand today, parameterise it by brandId for the per-brand assertions.

  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm --filter @resto/api vitest run test/e2e/catalog.e2e.spec.ts test/e2e/menu-brand-response.e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - catalog.e2e: new tests assert cross-brand version isolation (A bumps, B unchanged), and 403 on publish POST / publish DELETE / draft-diff GET without x-brand-slug, and brand-scoped draft-diff.
    - menu-brand-response.e2e: new test asserts per-brand /v1/menu ETag isolation + no Set-Cookie.
    - `pnpm --filter @resto/api test:e2e` (or the targeted `nx run api:e2e`) runs catalog.e2e and menu-brand-response.e2e GREEN for the new + existing cases. (Known: ~16 pre-existing NATS connect-timing failures at app boot may be present — they are NOT in catalog.e2e/menu-brand-response.e2e and are NOT regressions per 07.6-REVIEW; the new tests and these two suites must pass.)
  </acceptance_criteria>
  <done>The cross-brand isolation guarantee is proven by e2e: Brand A publish does not move Brand B's version or ETag; the three publish/cancel/draft-diff routes 403 without a bound brand; draft-diff is brand-scoped. The two affected suites are green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                               |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| operator (admin SPA) → publish/cancel/draft-diff | Brand asserted via x-brand-slug → BrandScopeGuard (added in 07.6-08); verified here by the 403 e2e tests. |
| CDN / customer → /v1/menu (per-brand host)       | Public, edge-cached read; ETag must discriminate brands and stay Set-Cookie-free.                         |
| outbox → NATS (catalog.\*.v2)                    | Event payload now carries brandId; no consumer exists, so no downstream trust boundary is widened.        |

## STRIDE Threat Register

| Threat ID   | Category               | Component                         | Disposition       | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------- | ---------------------- | --------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-09-01 | Tampering              | finalizeMenuPublish UPSERT        | mitigate          | UPSERT target `(brand_id, tenant_id)` (Task 1) bumps only the publishing brand's row; the cross-brand version-isolation e2e (Task 3) proves Brand A publish leaves Brand B's row unchanged.                                                                                                                                                                                                     |
| T-076-09-02 | Information Disclosure | public /v1/menu ETag              | mitigate          | ETag from `current(tenantId, brandId)` (Task 2). The per-brand host already discriminates the CDN cache key `(brand host, /v1/menu)`; the value is now per-brand so Brand A's publish cannot invalidate Brand B's cached menu. ETag shape unchanged → no cache poisoning on first deploy (backfill kept the integer). Set-Cookie-free contract re-verified by menu-brand-response.e2e (Task 3). |
| T-076-09-03 | Information Disclosure | menu\_\*\_published event payload | accept            | v2 adds brandId to the payload. No external/cross-tenant consumer (audit does not subscribe catalog; RESOLVED). brandId is not sensitive PII; envelope already carries tenantId. Accepted.                                                                                                                                                                                                      |
| T-076-09-04 | Elevation              | publish/cancel/draft-diff guard   | mitigate (verify) | @RequireBrand() was added in 07.6-08; THIS plan's 403 e2e tests (Task 3) are the regression net proving the guard actually rejects brand-less requests.                                                                                                                                                                                                                                         |
| T-076-09-05 | Tampering              | npm/pip/cargo installs            | mitigate          | No new package installs in this plan. Package Legitimacy Gate N/A.                                                                                                                                                                                                                                                                                                                              |

No HIGH-severity threat is left unmitigated: per-brand UPSERT + per-brand ETag are mitigated and e2e-proven; the v2 payload disclosure is accepted (no consumer, non-sensitive); the guard is verified by tests.
</threat_model>

<verification>
- v2 contracts exist with brandId and are exported; v1 retained but no longer emitted.
- finalizeMenuPublish UPSERTs the per-brand row and emits v2; menuFirstPublishedAt stays tenant-wide.
- Public ETag (menu + item) derives from current(tenantId, brandId); shape/Cache-Control/no-Set-Cookie unchanged; full api typecheck passes (08→09 seam closed).
- catalog.e2e + menu-brand-response.e2e green for the new cross-brand isolation cases (version, ETag, 403 ×3, draft-diff scoping).
- No raw SQL outside packages/db; buildEnvelope (not literal envelopes) used for events (ADR-0020 I-4).
</verification>

<success_criteria>

- finalizeMenuPublish bumps only the publishing brand's version and emits brandId-carrying v2 events (clean cut-over, no subscriber edit).
- The public /v1/menu + item ETag is per-brand; qr-menu/website 304 still works per brand; CDN contract unchanged.
- Cross-brand isolation (version, ETag, draft-diff) and the @RequireBrand 403 behaviour are proven by passing e2e tests.
- CR-04 is fully delivered: the publish cycle is brand-scoped end to end.
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-09-SUMMARY.md` when done.
</output>
