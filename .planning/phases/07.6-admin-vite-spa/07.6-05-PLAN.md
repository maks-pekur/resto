---
phase: 07.6-admin-vite-spa
plan: 05
type: execute
wave: 2
depends_on: [07.6-03]
files_modified:
  - apps/admin/src/lib/queries/catalog.ts
  - apps/admin/src/lib/menu/
  - apps/admin/src/components/menu/
  - apps/admin/src/routes/(protected)/dashboard/index.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/_layout.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/categories.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/items.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/items.$id.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/modifier-groups.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/modifier-groups.$id.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/stop-list.tsx
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - 'An operator can list, create, and edit menu items, categories, and modifier groups from the SPA'
    - 'Item sizes and the stop-list are editable; toggling stop-list reflects immediately'
    - 'The publish flow shows the countdown toast and supports cancel-publish; auto-save persists edits debounced via a Query mutation'
    - 'Photo upload uses the presigned-URL flow'
    - 'The draft-diff sticky publish bar shows unpublished-change counts'
    - 'All catalog reads/writes go through operator v1/catalog (no internal token)'
    - 'src/lib/queries/catalog.ts is created here and dashboard/index.tsx is (re)wired to the real catalog stop-list query'
  artifacts:
    - path: 'apps/admin/src/lib/queries/catalog.ts'
      provides: 'TanStack Query factories + mutations for catalog reads/writes via operator v1/catalog (OWNED by this plan)'
      contains: '/v1/catalog'
    - path: 'apps/admin/src/routes/(protected)/dashboard/$brandSlug/menu/items.tsx'
      provides: 'item list route with loader prefetch'
      contains: 'ensureQueryData'
    - path: 'apps/admin/src/components/menu/sticky-publish-bar.tsx'
      provides: 'ported publish bar wired to draft-diff query'
      contains: 'unpublishedCount'
  key_links:
    - from: 'src/lib/queries/catalog.ts'
      to: 'GET/POST /v1/catalog/*'
      via: 'apiFetch with brandSlug → x-brand-slug'
      pattern: '/v1/catalog'
    - from: 'menu route loaders'
      to: 'queryClient.ensureQueryData'
      via: 'TanStack Router loader prefetch'
      pattern: 'ensureQueryData'
    - from: 'auto-save (use-auto-save)'
      to: 'upsert mutation'
      via: 'debounced mutateAsync onPersist'
      pattern: 'useDebouncedAutosave|mutateAsync'
    - from: 'dashboard/index.tsx today-86 widget'
      to: 'catalogStopListQuery (src/lib/queries/catalog.ts)'
      via: 'real query replacing the 04 inline placeholder'
      pattern: 'stopListQuery|catalog'
---

<objective>
Port the catalog-admin UI surface — menu item/category/modifier-group editors, item-sizes, stop-list, the delayed-publish flow (countdown toast + cancel), photo upload, and the draft-diff sticky publish bar — from Next RSC + server-actions to TanStack Query + the operator `v1/catalog` endpoints (D-08). This is the core operator surface and the heart of SC-1 (UI preserved) and SC-2 (all catalog data through operator auth). This plan OWNS src/lib/queries/catalog.ts and re-wires the dashboard/index.tsx today's-86 widget (which 07.6-04 shipped as a self-contained placeholder) onto the real catalog stop-list query.

Purpose: This is the largest UI surface and the one the audit HIGH-7 finding centres on. Every read/write here must run through the relocated operator endpoints (07.6-01), with brand passed as x-brand-slug from the brand-in-URL param.
Output: src/lib/queries/catalog.ts (queries + mutations), ported menu components + lib, the menu route group under brand-in-URL, and the rewired dashboard/index.tsx today's-86 widget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md
@apps/CLAUDE.md

<interfaces>
Depends on 07.6-03 foundation: apiFetch, queryClient, i18n. Depends on 07.6-01 operator catalog GETs being live
  (categories, items, items/:id, modifier-groups, modifier-groups/:id, stop-list, draft-diff).
Coordinates with 07.6-04 (also Wave 2, no ordering guarantee): the menu routes nest under (protected)/dashboard/$brandSlug —
  04 owns the $brandSlug/dashboard _layout; this plan owns the menu/* subtree + src/lib/queries/catalog.ts + src/lib/menu +
  src/components/menu. THIS PLAN OWNS src/lib/queries/catalog.ts (04 never imports it). This plan ALSO re-wires
  dashboard/index.tsx: 04 ships it self-contained with an inline today's-86 placeholder; this plan overwrites that one widget
  to consume the real catalog stop-list query. Because Wave 2 has no inter-plan ordering, this overwrite must be safe
  regardless of which plan's Task runs last — see Task 3.

Operator catalog endpoints (all under v1/catalog, brand via x-brand-slug; draft-diff is tenant-only):
GET categories|items|items/:id|modifier-groups|modifier-groups/:id|stop-list|draft-diff
POST categories|items|modifier-groups|modifier-options|item-sizes|stop-list|publish|photo-upload-url
DELETE stop-list|publish ; PATCH archive (existing operator writes)

Query factory pattern (PATTERNS.md lines 654-661, RESEARCH Code Example 3):
categoriesQuery(brandSlug) = { queryKey: ['catalog','categories',brandSlug], queryFn: () => apiFetch('/v1/catalog/categories',{brandSlug}), staleTime }
draftDiffQuery(brandSlug) staleTime 10_000

Auto-save → mutation (RESEARCH Code Example 4): useDebouncedAutosave(form, async v => { const r = await upsertMutation.mutateAsync(v); return { ok: r.ok }; }, onState).

Menu components to port (PATTERNS.md menu/\_layout section lines 621-661 + STATE decisions):
Behavior-critical (Task 2a): sticky-publish-bar(-client).tsx, publish-countdown-toast.tsx (Sonner id 'publish-countdown',
Date.now() baseline, ref-guarded onElapse at 5s), status-badge.tsx (draft outline / modified amber / published default /
paused secondary / archived ghost), category-select.tsx (parent-picker depth ≤ 2; refineCategoryDepth),
auto-save-indicator.tsx.
Editor/table/widget (Task 2b): bju-row.tsx, todays-86-widget.tsx + todays-86-reset-button-client.tsx, items-table,
items-filter-bar, item-detail-form, item-aside, item-sizes-card, item-modifier-groups-card, photo-upload,
categories-table, category-form, modifier-group-form, modifier-options-list, group-editor-shell, stop-list-table.
lib/menu to port (mostly clean): zod-schemas.ts, use-auto-save.ts, catalog-errors.ts, localized.ts (DEFAULT_LOCALE='ru'),
format-age.ts, pluralize-changes.ts, types.ts. Rewrite the \*-action.ts files (cancel-publish-action, schedule-publish-action)
as Query mutations (they currently import next).

Reorder: two sequential upsert POSTs (no batch endpoint — 04b decision). Item status filter default 'active'.
ResizeObserver polyfill needed in test setup (JSDOM lacks it; Radix popper) — replicate apps/admin/test/setup.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog Query factories + mutations + ported menu lib</name>
  <read_first>
    - apps/admin/lib/menu/{zod-schemas,use-auto-save,catalog-errors,localized,format-age,pluralize-changes,types}.ts (clean port)
    - apps/admin/lib/menu/{cancel-publish-action,schedule-publish-action}.ts (rewrite — currently import next)
    - apps/admin/app/dashboard/(workspace)/menu/**/*-action.ts (server actions → mutations: upsert-item, upsert-category, reorder-category, archive-item, archive-category, toggle-stop-list, reset-stop-list, upsert-modifier-group, upsert-modifier-option, upsert-item-size, upsert-item-modifier-groups, photo-upload-url)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (query factory + auto-save sections, lines 654-661, 806-831)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 5, Code Examples 3-4)
  </read_first>
  <action>
    Create src/lib/queries/catalog.ts exporting query factories (categoriesQuery, itemsQuery(filters), itemQuery(id), modifierGroupsQuery, modifierGroupQuery(id), stopListQuery, draftDiffQuery) — each keyed by ['catalog', resource, brandSlug, ...filters] calling apiFetch on the corresponding GET with `{ brandSlug }`; staleTime 30_000 for stable reads, 10_000 for draft-diff. Export mutation helpers (or thin functions consumed by useMutation in components) for every write: upsertItem, upsertCategory, reorderCategories (two sequential upsert POSTs), archiveItem, archiveCategory, toggleStopList, resetStopList, upsertModifierGroup, upsertModifierOption, upsertItemSize, upsertItemModifierGroups, getPhotoUploadUrl, schedulePublish, cancelPublish — each via apiFetch with method + body + brandSlug; each mutation invalidates the relevant query keys (and draft-diff) on success. Port the clean lib/menu/* files into src/lib/menu/ verbatim (adjust @/ paths). Rewrite cancel-publish-action.ts + schedule-publish-action.ts as the schedulePublish/cancelPublish mutation functions (drop the next imports). Replicate the test/setup.ts ResizeObserver polyfill under the Vite test setup.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "/v1/catalog" apps/admin/src/lib/queries/catalog.ts; grep -rn "next/\|server-only\|'use server'\|internal/v1/catalog" apps/admin/src/lib/menu apps/admin/src/lib/queries | wc -l; grep -c "DEFAULT_LOCALE = 'ru'" apps/admin/src/lib/menu/localized.ts; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/queries/catalog.ts is created by this plan and references `/v1/catalog` for all reads and writes (no `internal/v1/catalog`)
    - It exports a stop-list query factory (stopListQuery) that dashboard/index.tsx (Task 3) will consume
    - Zero next/* / server-only / 'use server' imports across src/lib/menu + src/lib/queries
    - DEFAULT_LOCALE='ru' preserved in localized.ts
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>Catalog read queries + write mutations route through operator v1/catalog; menu lib is ported and server-action-free; src/lib/queries/catalog.ts exists with a stop-list factory for the dashboard widget.</done>
</task>

<task type="auto">
  <name>Task 2a: Port behavior-critical menu components (publish bar, countdown toast, status badge, category-select, auto-save indicator)</name>
  <read_first>
    - apps/admin/components/menu/{sticky-publish-bar,sticky-publish-bar-client,publish-countdown-toast,status-badge,category-select,auto-save-indicator}.tsx
    - apps/admin/lib/ui/toast-helpers.ts (Sonner helpers — port)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (menu component sections + STATE decisions on publish-countdown / status-badge variants)
  </read_first>
  <action>
    Port the five behavior-critical menu components into src/components/menu, swapping `useTranslations`→`useTranslation` (keyPrefix), next/link→TanStack Link, and wiring server-action callbacks to the Query mutations from Task 1 (via useMutation). Port lib/ui/toast-helpers.ts. Preserve each component's exact behavior contract (see acceptance criteria). Keep StickyPublishBar mounted only in the menu route-group layout (not global) — it reads the draft-diff query.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "publish-countdown" apps/admin/src/components/menu/publish-countdown-toast.tsx; grep -c "unpublishedCount" apps/admin/src/components/menu/sticky-publish-bar.tsx; grep -rn "useTranslations\b\|next-intl\|next/link\|'use server'" apps/admin/src/components/menu/{sticky-publish-bar,sticky-publish-bar-client,publish-countdown-toast,status-badge,category-select,auto-save-indicator}.tsx 2>/dev/null | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - sticky-publish-bar.tsx exposes the unpublished-change count from the draft-diff query (grep `unpublishedCount`) and mounts only in the menu layout
    - publish-countdown-toast.tsx preserves the constant Sonner id 'publish-countdown' and threads success/info/error replace-in-place; the Date.now() baseline + ref-guarded onElapse fires exactly once at the 5s boundary
    - status-badge.tsx preserves the variant mapping: draft → outline, modified → outline+amber, published → default, paused → secondary, archived → ghost+muted
    - category-select.tsx disables already-child options to enforce depth ≤ 2 and applies refineCategoryDepth
    - auto-save-indicator.tsx reflects the use-auto-save state machine (idle/saving/saved/error)
    - Zero next-intl / next/link / useTranslations(next-intl) / 'use server' imports across these five files
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>The behavior-critical menu components are ported with their exact contracts (publish-countdown id, status-badge variants, category depth ≤ 2, draft-diff count) preserved and wired to Task 1 mutations.</done>
</task>

<task type="auto">
  <name>Task 2b: Port menu editor / table / form components</name>
  <read_first>
    - apps/admin/components/menu/{bju-row,todays-86-widget,todays-86-reset-button-client}.tsx
    - apps/admin/app/dashboard/(workspace)/menu/**/*-client.tsx (editor shells, forms, tables — items-table, items-filter-bar, item-detail-form, item-aside, item-sizes-card, item-modifier-groups-card, photo-upload, categories-table, category-form, modifier-group-form, modifier-options-list, group-editor-shell, stop-list-table)
    - apps/admin/src/lib/queries/catalog.ts (the mutations from Task 1 to wire auto-save + writes onto)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (menu editor sections)
  </read_first>
  <action>
    Port the editor/table/form/widget menu components into src/components/menu, swapping `useTranslations`→`useTranslation` (keyPrefix), next/link→TanStack Link, and wiring server-action callbacks to the Task 1 Query mutations via useMutation + useDebouncedAutosave. Components: bju-row, todays-86-widget + todays-86-reset-button-client, items-table, items-filter-bar (default status filter 'active'), item-detail-form, item-aside, item-sizes-card, item-modifier-groups-card, photo-upload, categories-table, category-form, modifier-group-form, modifier-options-list, group-editor-shell, stop-list-table. Auto-save: wire item/category forms through useDebouncedAutosave + the upsert mutation (onPersist returns { ok }). Photo upload: call getPhotoUploadUrl mutation then PUT the file to the presigned URL (keep AbortSignal.timeout). Reorder via two sequential upsert POSTs (no batch endpoint).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -rn "useTranslations\b\|next-intl\|next/link\|'use server'" apps/admin/src/components/menu | wc -l; ls apps/admin/src/components/menu | wc -l; grep -rc "useDebouncedAutosave\|mutateAsync" apps/admin/src/components/menu | grep -v ':0' | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The full menu component set is ported (≥18 files in src/components/menu across 2a+2b)
    - Item/category forms wire auto-save through useDebouncedAutosave + the Task 1 upsert mutation
    - photo-upload uses getPhotoUploadUrl then a presigned PUT with AbortSignal.timeout
    - items-filter-bar defaults the status filter to 'active'
    - Zero next-intl / next/link / useTranslations(next-intl) / 'use server' imports across src/components/menu
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>The editor/table/form menu surface is ported, auto-save + writes wired to Task 1 mutations, no server actions remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Menu route group (brand-in-URL) + dashboard/index.tsx rewire + publish-flow test</name>
  <read_first>
    - apps/admin/app/dashboard/(workspace)/menu/layout.tsx (draft-diff RSC → loader analog)
    - apps/admin/app/dashboard/(workspace)/menu/**/page.tsx (the 6 menu pages: categories, items, items/[id], modifier-groups, modifier-groups/[id], stop-list)
    - apps/admin/src/routes/(protected)/dashboard/index.tsx (the 04-shipped self-contained placeholder to rewire)
    - apps/admin/src/lib/queries/catalog.ts (stopListQuery — Task 1)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (menu/_layout section, lines 621-661)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 5 — ensureQueryData loader; Pitfall 2 — brand-in-URL 403 if slug missing)
  </read_first>
  <behavior>
    - The menu/_layout loader prefetches draft-diff for the brandSlug and the StickyPublishBar shows unpublishedCount
    - Navigating to /dashboard/$brandSlug/menu/items renders the item list from itemsQuery (loader-prefetched, no waterfall)
    - Triggering schedulePublish shows the countdown toast; cancelPublish dismisses it and reverts
    - A menu route entered without a brandSlug redirects to brand selection (never sends a brand-scoped read with no x-brand-slug)
    - dashboard/index.tsx today's-86 widget renders from the real catalog stop-list query (no inline placeholder remains)
  </behavior>
  <action>
    Create the menu route subtree under src/routes/(protected)/dashboard/$brandSlug/menu/: _layout.tsx (loader ensureQueryData(draftDiffQuery(brandSlug)); component renders <Outlet/> + StickyPublishBar from useSuspenseQuery(draftDiffQuery)), categories.tsx, items.tsx (loader prefetches itemsQuery + categoriesQuery), items.$id.tsx (loader prefetches itemQuery + categoriesQuery + modifierGroupsQuery), modifier-groups.tsx, modifier-groups.$id.tsx, stop-list.tsx. Each route reads brandSlug from `Route.useParams()` and passes it through the query factories (→ x-brand-slug). Guard against missing brand: if brandSlug is absent, redirect to the dashboard brand-select (Pitfall 2). REWIRE dashboard/index.tsx (which 04 shipped self-contained with an inline today's-86 placeholder): replace the inline placeholder with the real catalog stop-list widget consuming stopListQuery from src/lib/queries/catalog.ts; keep the rest of the landing (setup-checklist) intact. This is a one-widget swap — keep the file otherwise identical so it works no matter which Wave-2 plan's Task ran first. Add a vitest spec covering the five <behavior> cases (mock apiFetch; assert loader prefetch keys, list render, publish-toast id appears then clears on cancel, the no-brand redirect, and the dashboard widget rendering from stopListQuery). Register the subtree in the router.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -rc "ensureQueryData" apps/admin/src/routes/\(protected\)/dashboard/\$brandSlug/menu/ | grep -v ':0' | wc -l; grep -rc "x-brand-slug\|brandSlug" apps/admin/src/routes/\(protected\)/dashboard/\$brandSlug/menu/items.tsx; grep -c "stopListQuery\|lib/queries/catalog" apps/admin/src/routes/\(protected\)/dashboard/index.tsx; pnpm exec nx run admin:test 2>&1 | tail -10; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The 6 menu routes + _layout exist under $brandSlug/menu and use loader `ensureQueryData`
    - items.tsx threads brandSlug into its queries (→ x-brand-slug)
    - dashboard/index.tsx now imports from src/lib/queries/catalog.ts and renders the today's-86 widget from stopListQuery (the 04 inline placeholder is replaced)
    - The publish-flow vitest spec (countdown shows, cancel clears, no-brand redirect, loader prefetch, dashboard widget) passes
    - `admin:test` and `admin:typecheck` exit 0
  </acceptance_criteria>
  <done>The full menu surface is reachable under brand-in-URL with loader prefetch + publish flow; dashboard/index.tsx is rewired to the real stop-list query; behavior test passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                      |
| ------------------------------------ | ------------------------------------------------ |
| SPA menu writes → v1/catalog         | Untrusted operator input + brand slug cross here |
| presigned photo PUT → object storage | Direct browser upload to a signed URL            |

## STRIDE Threat Register

| Threat ID | Category        | Component                                                | Disposition | Mitigation Plan                                                                                                                              |
| --------- | --------------- | -------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-20  | Elevation       | brand-scoped write with forged x-brand-slug              | mitigate    | every catalog read/write passes brandSlug → x-brand-slug; API @RequireBrand()+BrandScopeGuard authorize from DB (07.6-01); forged slug → 403 |
| T-076-21  | Tampering       | menu mutation bypassing operator auth                    | mitigate    | all writes go through apiFetch → v1/catalog (operator session); zero internal/v1/catalog references (Task 1 grep gate)                       |
| T-076-22  | Info Disclosure | brand-scoped read sent with no brand → cross-tenant leak | mitigate    | Pitfall 2 guard: missing brandSlug redirects to brand-select before any brand-scoped query fires                                             |
| T-076-23  | Tampering       | presigned-URL photo upload to arbitrary path             | accept      | API issues the presigned URL scoped to the tenant/brand path; browser PUT cannot choose the key — server-controlled (existing 04b mechanism) |
| T-076-24  | DoS             | unbounded auto-save mutation storms                      | mitigate    | useDebouncedAutosave debounces persistence; mutations retry 0 (no retry storm); AbortSignal.timeout caps each call                           |

</threat_model>

<verification>
- `admin:typecheck` + `admin:test` exit 0
- Zero internal/v1/catalog references in src/lib/queries or src/lib/menu
- Menu routes use loader prefetch + brand-in-URL; publish-flow behavior test green
- dashboard/index.tsx consumes the real stopListQuery (placeholder replaced)
</verification>

<success_criteria>

- SC-1: the catalog-admin UI surface (item list, editors, stop-list, publish flow, photo upload) is preserved
- SC-2: all catalog reads/writes go through operator v1/catalog — no internal token in the menu surface
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-05-SUMMARY.md` when done
</output>
