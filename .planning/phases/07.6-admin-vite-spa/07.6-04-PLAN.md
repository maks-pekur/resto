---
phase: 07.6-admin-vite-spa
plan: 04
type: execute
wave: 2
depends_on: [07.6-03]
files_modified:
  - apps/admin/src/routes/(auth)/_layout.tsx
  - apps/admin/src/routes/(auth)/login.tsx
  - apps/admin/src/routes/(auth)/signup.tsx
  - apps/admin/src/routes/(auth)/forgot-password.tsx
  - apps/admin/src/routes/(auth)/reset-password.tsx
  - apps/admin/src/routes/(auth)/accept-invitation.$id.tsx
  - apps/admin/src/routes/(protected)/_layout.tsx
  - apps/admin/src/routes/(protected)/dashboard/_layout.tsx
  - apps/admin/src/routes/(protected)/dashboard/index.tsx
  - apps/admin/src/routes/(protected)/onboarding/brand.tsx
  - apps/admin/src/components/app-sidebar.tsx
  - apps/admin/src/components/nav-main.tsx
  - apps/admin/src/components/nav-user.tsx
  - apps/admin/src/components/brand-switcher.tsx
  - apps/admin/src/components/site-header.tsx
  - apps/admin/src/components/tenant-breadcrumb.tsx
  - apps/admin/src/components/empty-state.tsx
  - apps/admin/src/components/theme-toggle.tsx
  - apps/admin/src/components/locale-switcher-items.tsx
  - apps/admin/src/components/setup-checklist-card.tsx
  - apps/admin/src/components/ui/
  - apps/admin/src/hooks/use-mobile.ts
  - apps/admin/src/lib/utils.ts
  - apps/admin/src/lib/slugify-brand.ts
  - apps/admin/src/lib/queries/identity.ts
  - apps/admin/src/lib/auth/safe-next.ts
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - 'An operator can sign in from the SPA login route; the better-auth session is established and the active org is set'
    - 'Visiting a protected route while unauthenticated redirects to /login with a safe (same-origin) next param'
    - 'The login next param rejects protocol-relative (//evil.com) redirects, falling back to /dashboard'
    - 'The dashboard shell renders sidebar + nav-user + brand-switcher; theme + locale toggles work client-side'
    - 'Brand switching sets the active org and reflects the brand in the URL (brand-in-URL)'
    - 'A zero-brand tenant renders the EmptyState onboarding prompt'
  artifacts:
    - path: 'apps/admin/src/routes/(protected)/_layout.tsx'
      provides: 'client route guard via beforeLoad session check'
      contains: 'beforeLoad'
    - path: 'apps/admin/src/routes/(auth)/login.tsx'
      provides: 'SPA login with better-auth client + open-redirect-safe next'
      contains: 'signIn.email'
    - path: 'apps/admin/src/lib/auth/safe-next.ts'
      provides: 'reusable open-redirect guard for next params'
      contains: "startsWith('/')"
    - path: 'apps/admin/src/components/app-sidebar.tsx'
      provides: 'ported sidebar with react-i18next + brand-in-URL links'
      contains: 'useTranslation'
  key_links:
    - from: '(protected)/_layout.tsx beforeLoad'
      to: 'authClient.getSession()'
      via: 'redirect to /login on no session'
      pattern: 'authClient.getSession'
    - from: 'login.tsx'
      to: 'authClient.signIn.email + organization.setActive'
      via: '3-call sign-in fan-out'
      pattern: 'organization.setActive'
    - from: 'brand-switcher.tsx'
      to: 'authClient.organization.setActive + navigate brand-in-URL'
      via: 'active org + route param'
      pattern: 'organization.setActive'
---

<objective>
Port the auth surface, the client route guard, and the dashboard shell (sidebar, nav-user, brand-switcher, header, breadcrumb, empty-state, theme/locale toggles) plus the onboarding/brand route to the Vite SPA. This re-establishes — client-side — the open-redirect safety and 401→/login behavior previously handled in Next server actions and `proxy.ts` (D-04), and replaces the server-action sign-in fan-out with the better-auth/react client (D-02). Establishes brand-in-URL active-brand state (D-03).

Purpose: Without auth + shell, no protected feature route is reachable. This is the entry path and chrome every other Wave-2 plan renders inside.
Output: working login/signup/forgot/reset/accept-invite routes, a beforeLoad-guarded protected layout, the dashboard shell with ported chrome, onboarding/brand, and the shadcn ui/ primitives regenerated under src/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md
@apps/CLAUDE.md

<interfaces>
Depends on 07.6-03 foundation: src/lib/auth-client.ts (authClient), src/lib/api-client.ts (apiFetch),
  src/lib/query-client.ts, src/lib/i18n (i18n + useTranslation), src/hooks/use-theme.ts, src/routes/__root.tsx (router + context).

better-auth/react client usage (PATTERNS.md lines 320-335, RESEARCH Pattern 1):
authClient.useSession() → { data: { session: { activeOrganizationId }, user } }
authClient.signIn.email({ email, password }); authClient.organization.list(); authClient.organization.setActive({ organizationId }); authClient.signOut()
authClient.useListOrganizations(); authClient.useActiveOrganization()

Open-redirect rule (apps/CLAUDE.md + proxy.ts:26-29 + actions.ts:22-24):
safe = raw.startsWith('/') && !/^\/[\\/]/.test(raw) ? raw : '/dashboard' → extract to src/lib/auth/safe-next.ts

Route guard (PATTERNS.md (protected)/\_layout section, lines 457-484; RESEARCH Pattern 3):
beforeLoad: await authClient.getSession(); if no session.data.session → throw redirect({ to: '/login', search: { next: safeNext } })

Components to port (PATTERNS.md exact analogs):
app-sidebar.tsx (lines 593-605): keep navMain + JSX; swap useTranslations→useTranslation keyPrefix 'nav'; add /$brandSlug prefix to nav URLs
nav-user.tsx (lines 607-618): keep JSX; swap next-themes→@/hooks/use-theme, next-intl→react-i18next; <form action={signOutAction}> → onClick authClient.signOut()
brand-switcher.tsx (lines 569-589): keep JSX + initialsOf; next/link→@tanstack/react-router Link, useRouter→useNavigate, setActiveBrandAction→authClient.organization.setActive + navigate brand-in-URL; drop HMAC cookie + cross-tab sync
theme-toggle.tsx: port verbatim, useTheme from @/hooks/use-theme
empty-state.tsx, site-header.tsx, tenant-breadcrumb.tsx, page-heading.tsx, setup-checklist-card.tsx, nav-main.tsx, locale-switcher-items.tsx: strip next-intl/next imports, swap to react-i18next + TanStack Link

login.tsx (PATTERNS.md lines 488-529): react-hook-form + zod resolver (drop useActionState/useFormStatus),
authClient.signIn.email → on success organization.list → setActive(first org) → navigate(safe next).
data-testid="brand-switcher-trigger" and EmptyState "no brands yet" copy must be preserved (e2e fixtures assert them).

shadcn ui/: regenerate primitives under src/components/ui via shadcn (components.json already new-york/neutral),
OR copy existing components/ui/\* verbatim (framework-agnostic). Keep the relaxed-eslint dir convention.

Identity reads the shell needs: /v1/me (kind/email/baseRole/tenantId), /v1/me/brands (brand list) — wrap as
TanStack Query factories in src/lib/queries/identity.ts using apiFetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port shadcn ui/ primitives + framework-agnostic lib + shared safe-next guard</name>
  <read_first>
    - apps/admin/components/ui/ (all primitives — framework-agnostic, copy under src/)
    - apps/admin/lib/utils.ts, apps/admin/lib/slugify-brand.ts, apps/admin/hooks/use-mobile.ts (clean, port verbatim)
    - apps/admin/app/(auth)/login/actions.ts (open-redirect logic to extract, lines 22-24)
    - apps/admin/proxy.ts (open-redirect logic, lines 26-29)
    - apps/admin/components.json (shadcn config — new-york/neutral)
  </read_first>
  <action>
    Copy apps/admin/components/ui/* into apps/admin/src/components/ui/ verbatim (they import only radix-ui/cva/clsx/tailwind-merge — framework-agnostic). Copy lib/utils.ts → src/lib/utils.ts, lib/slugify-brand.ts → src/lib/slugify-brand.ts, hooks/use-mobile.ts → src/hooks/use-mobile.ts. Create src/lib/auth/safe-next.ts exporting `safeNext(raw: string): string` = `raw.startsWith('/') && !/^\/[\\/]/.test(raw) ? raw : '/dashboard'` (single source for the open-redirect rule). Adjust any `@/` import paths so they resolve under src/. Do NOT bring over any file importing next/* or server-only. Keep components.json pointed at the new src/components paths if shadcn add will be re-run.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && ls apps/admin/src/components/ui/ | wc -l; grep -c "startsWith('/')" apps/admin/src/lib/auth/safe-next.ts; grep -rn "next/\|server-only" apps/admin/src/components/ui apps/admin/src/lib/utils.ts | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ contains the shadcn primitives (≥30 files)
    - src/lib/auth/safe-next.ts exports the open-redirect guard
    - Zero next/* or server-only imports in the ported ui/ + utils
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>shadcn primitives + clean lib + the shared safe-next guard live under src/; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Auth routes + client route guard</name>
  <read_first>
    - apps/admin/app/(auth)/login/login-form-client.tsx + actions.ts (analog — fields, SubmitButton, error alert, next handling)
    - apps/admin/app/(auth)/{signup,forgot-password,reset-password}/*-form-client.tsx + actions.ts (analogs)
    - apps/admin/app/(auth)/accept-invitation/[id]/*-form-client.tsx + actions.ts (analog)
    - apps/admin/lib/actions/sign-in-and-bind-org.ts (BA 3-call contract)
    - apps/admin/proxy.ts (guard + open-redirect)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (login.tsx + (protected)/_layout sections, lines 457-529)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Patterns 2, 3; Code Examples 1, 2)
  </read_first>
  <action>
    Create src/routes/(auth)/_layout.tsx — pathless auth layout (no sidebar; centered card). Create login.tsx: react-hook-form + zod resolver (email + password), on submit `authClient.signIn.email`; on error set a `role="alert"` message (i18n key auth.signInError); on success `authClient.organization.list()` then `organization.setActive({ organizationId: first.id })` if any, then `navigate({ to: safeNext(search.next ?? '/dashboard') })`. Read `next` from validated TanStack Router search params; use src/lib/auth/safe-next.ts (never trust the raw value). Create signup.tsx, forgot-password.tsx, reset-password.tsx, accept-invitation.$id.tsx porting the corresponding form-client fields and the matching better-auth client calls (signUp.email / forgetPassword / resetPassword / organization.acceptInvitation). Use TanStack Router `Link` for cross-links (not next/link). Create src/routes/(protected)/_layout.tsx with `beforeLoad` that calls `authClient.getSession()` and `throw redirect({ to: '/login', search: { next: safeNext(`${location.pathname}${location.search}`) } })` when there is no session; component renders `<Outlet/>`. Register all routes in the router tree (__root from Wave 1).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "signIn.email" apps/admin/src/routes/\(auth\)/login.tsx; grep -c "beforeLoad" apps/admin/src/routes/\(protected\)/_layout.tsx; grep -rc "safeNext" apps/admin/src/routes/ | grep -v ':0'; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - login.tsx calls `authClient.signIn.email` and uses `safeNext` on the next param
    - (protected)/_layout.tsx has a `beforeLoad` that redirects to /login when `authClient.getSession()` returns no session
    - signup / forgot-password / reset-password / accept-invitation routes exist and call the matching better-auth client method
    - No next/link or next/navigation import in any auth route
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>Operator login (and the rest of the auth flow) works through the better-auth client; protected routes are guarded with open-redirect-safe next.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard shell + chrome components + onboarding/brand</name>
  <read_first>
    - apps/admin/app/dashboard/layout.tsx + (workspace)/layout.tsx + (workspace)/page.tsx (shell + dashboard index analogs)
    - apps/admin/components/{app-sidebar,nav-main,nav-user,brand-switcher,site-header,tenant-breadcrumb,empty-state,theme-toggle,locale-switcher-items,setup-checklist-card,page-heading}.tsx (port analogs)
    - apps/admin/app/(onboarding)/onboarding/brand/{page.tsx,brand-form-client.tsx} (onboarding analog)
    - apps/admin/lib/me.ts + me-brands.ts + lib/actions/create-brand.ts + check-brand-slug.ts (identity reads + brand create)
    - apps/admin/e2e/adm-00-smoke-walk.spec.ts (testids + copy the e2e asserts: brand-switcher-trigger, "your tenant has no brands yet")
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (app-sidebar/nav-user/brand-switcher sections, lines 569-618)
  </read_first>
  <action>
    Create src/lib/queries/identity.ts: TanStack Query factories `meQuery()` (apiFetch '/v1/me') and `meBrandsQuery()` (apiFetch '/v1/me/brands'). Port the chrome components into src/components, swapping every `useTranslations`→`useTranslation('translation', { keyPrefix: ... })`, `next/link`→TanStack `Link` (`to` not `href`), `useRouter`→`useNavigate`, `next-themes`→`@/hooks/use-theme`. In brand-switcher.tsx: replace the server action with `authClient.organization.setActive({ organizationId: brand.id })` then `navigate({ to: '/dashboard/$brandSlug', params: { brandSlug: brand.slug } })` (brand-in-URL, D-03); drop the HMAC active-brand cookie and the cross-tab sync. In nav-user.tsx: replace `<form action={signOutAction}>` with a button calling `authClient.signOut()` then navigating to /login. In app-sidebar.tsx: prefix nav URLs with the active `/$brandSlug`. Create src/routes/(protected)/dashboard/_layout.tsx (SidebarProvider + AppSidebar + SiteHeader + Outlet; reads meQuery/meBrandsQuery; if zero brands render EmptyState empty-variant with the "create your first brand" link to /onboarding/brand — preserve the exact e2e copy and data-testid="brand-switcher-trigger"). Create dashboard/index.tsx (workspace landing — port the setup-checklist + today's-86 widget shell using catalog stop-list query, which arrives in 07.6-05; render a safe placeholder if the query factory is not yet present, but wire it to the real query once 05 lands — coordinate by importing from src/lib/queries/catalog.ts which 05 owns). Create onboarding/brand.tsx porting brand-form-client with react-hook-form + create-brand via apiFetch to the brand-create operator endpoint + slug-availability check. Keep brand-in-URL params typed.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "organization.setActive" apps/admin/src/components/brand-switcher.tsx; grep -rc "useTranslation" apps/admin/src/components/app-sidebar.tsx; grep -rn "next-themes\|next-intl\|next/link\|next/navigation" apps/admin/src/components/ | wc -l; grep -rc "brand-switcher-trigger" apps/admin/src/routes/\(protected\)/dashboard/_layout.tsx; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - brand-switcher.tsx calls `organization.setActive` and navigates brand-in-URL; no HMAC cookie / set-active server action remains
    - app-sidebar + nav-user use `useTranslation` (react-i18next), zero next-themes/next-intl/next-link imports across src/components
    - dashboard/_layout.tsx renders the brand-switcher (data-testid preserved) and the zero-brand EmptyState with the create-first-brand link
    - onboarding/brand.tsx creates a brand via apiFetch (operator endpoint), not a server action
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>The dashboard shell renders with ported chrome, brand-in-URL switching, theme/locale toggles, and onboarding/brand — all client-side, server-action-free.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                      |
| ---------------------- | ------------------------------------------------ |
| login next param (URL) | Attacker-controlled redirect target crosses here |
| browser route guard    | Client-side gate; the API is the real authority  |
| SPA → /api/auth/\*     | Credentialed auth requests cross-origin          |

## STRIDE Threat Register

| Threat ID | Category        | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | --------------- | ---------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-15  | Spoofing        | open redirect via next param                               | mitigate    | safe-next.ts rejects non-`/` and protocol-relative `//`; used in login + route guard; single source (Task 1)                                                                            |
| T-076-16  | Elevation       | client guard is the only access control                    | accept      | beforeLoad is UX-only (flash prevention); the API AuthGuard/Permissions/BrandScope is the real boundary — a bypassed client guard still cannot read tenant data without a valid session |
| T-076-17  | Tampering       | brand-in-URL slug forged to access other brand             | mitigate    | brand slug only sets x-brand-slug; API BrandScopeGuard authorizes from DB membership (07.6-01); forged slug yields 403, not access                                                      |
| T-076-18  | Spoofing        | static operator placeholder (operator@example.com) shipped | mitigate    | nav-user renders real session user (apps/CLAUDE.md: no static identity placeholders); meQuery supplies real email                                                                       |
| T-076-19  | Info Disclosure | session-expiry not handled on protected route              | mitigate    | api-client 401 → signOut + /login?expired=1 (from 07.6-03); route guard re-checks session on navigation                                                                                 |

</threat_model>

<verification>
- `admin:typecheck` exits 0
- Zero next-themes / next-intl / next/link / next/navigation / server-only imports anywhere under apps/admin/src
- safe-next guard is used in both login and the protected route guard
- Dashboard shell preserves the e2e-asserted testids + copy
</verification>

<success_criteria>

- SC-1: the dashboard shell + sidebar nav surface is preserved by porting components
- SC-3: operator login via the better-auth session works from the SPA; open-redirect/cookie-security re-established client-side
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-04-SUMMARY.md` when done
</output>
