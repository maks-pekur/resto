---
phase: 07.6-admin-vite-spa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts
  - apps/api/.env.example
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - 'An owner-role operator can POST /v1/tenants/me/offboard and DELETE /v1/tenants/me/offboard with a session cookie (no internal token)'
    - 'Offboard tenant is derived from session ALS, never from a URL :id param'
    - 'Non-owner (admin/staff) operators are rejected from the offboard endpoints'
    - 'admin.resto.app is documented as an allowed CORS origin + better-auth trusted origin in env config'
  artifacts:
    - path: 'apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts'
      provides: 'POST/DELETE /v1/tenants/me/offboard operator handlers'
      contains: 'me/offboard'
    - path: 'apps/api/.env.example'
      provides: 'documented admin host in CORS + trusted origins'
      contains: 'admin.resto.app'
  key_links:
    - from: 'settings danger-zone (future SPA)'
      to: 'POST/DELETE /v1/tenants/me/offboard'
      via: "operator session cookie, tenant:['delete'] permission"
      pattern: 'v1/tenants/me/offboard'
    - from: 'OffboardTenantService'
      to: 'TenantsController constructor'
      via: 'DI (already exported from tenancy.module.ts)'
      pattern: 'OffboardTenantService'
---

<objective>
Add operator-authenticated tenant offboarding endpoints `POST/DELETE /v1/tenants/me/offboard` to the operator `TenantsController`, mirroring the schedule/cancel logic in `InternalTenantsController` but deriving the tenant from the session (ALS) instead of a URL `:id` param. This closes the last admin→internal-token data path (the settings danger-zone), required so the SPA drops its final `apiFetchInternal` call (SC-2). Also document `admin.resto.app` in the API's CORS allowed-origins and better-auth trusted-origins env example so the cross-subdomain cookie session works from the static admin host (SC-3 dependency).

Purpose: RESEARCH Open Question #1 / Pitfall 6 — settings offboard uses `internal/v1/tenants`, NOT catalog, so it is not covered by D-08. The recommended resolution is a small operator extension to the tenancy context. INTERNAL_API_TOKEN and the internal tenants endpoints STAY (other callers: bootstrap/provisioning).
Output: 2 new operator handlers on v1/tenants, env documentation for the admin origin, integration tests for owner-only access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md

<interfaces>
<!-- CRITICAL: PATTERNS.md suggested @Permissions({ tenant: ['manage'] }) but 'manage' does NOT exist. -->
From packages/domain/src/rbac/permissions.ts:
  PERMISSIONS_STATEMENT.tenant = ['read', 'delete', 'transfer']   ← NO 'manage'
  SYSTEM_ROLES.owner.tenant = ['read', 'delete', 'transfer']      ← owner has 'delete'
  SYSTEM_ROLES.admin.tenant = ['read']                            ← admin does NOT have 'delete'
  → Use @Permissions({ tenant: ['delete'] }) — offboard = scheduled tenant deletion; owner-only, matching the legacy requireOwner() gate.

OffboardTenantService (already exported from tenancy.module.ts, injected into operator TenantsController):
schedule(input: { tenantId: string; requestedBy: string }): Promise<TenantSnapshot>
cancel(input: { tenantId: string }): Promise<TenantSnapshot>

ScheduleOffboardingInputDto: { requestedBy: string (1..120) }

Existing operator TenantsController pattern (tenants.controller.ts:24-40):
@Controller('v1/tenants')
@Get('me') @Permissions({ tenant: ['read'] }) @RequiresTenantContext()
uses try/catch + mapDomainError, toResponse(snapshot)
Tenant comes from ALS — use requireTenantContext() (already used across catalog.controller.ts)

Legacy gate being replaced (settings/actions.ts): requireOwner() asserts me.baseRole === 'owner', requestedBy = userId.
→ In the SPA, requestedBy will be the operator's userId; the new endpoint accepts requestedBy in the body for the schedule call.

env.schema.ts already validates CORS_ALLOWED_ORIGINS (line 132) and AUTH_COOKIE_DOMAIN must start with '.' (line 255).
auth.config.ts already enables crossSubDomainCookies + trustedOrigins. ONLY env values need the admin host — no schema/code change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST/DELETE /v1/tenants/me/offboard to operator TenantsController</name>
  <read_first>
    - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts (target — getMe pattern, lines 1-80)
    - apps/api/src/contexts/tenancy/interfaces/http/internal-tenants.controller.ts (analog — scheduleOffboarding/cancelOffboarding logic, lines 95-125)
    - apps/api/src/contexts/tenancy/tenancy.module.ts (confirm OffboardTenantService is exported + TenantsController registered)
    - packages/domain/src/rbac/permissions.ts (confirm tenant:['delete'], no 'manage')
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (tenants.controller.ts section, lines 737-781)
  </read_first>
  <action>
    Add to tenants.controller.ts: import `Body`, `Delete`, `HttpCode`, `HttpStatus`, `Post` from `@nestjs/common`; import `OffboardTenantService`, `ScheduleOffboardingInputDto`, `RestoZodValidationPipe`, and `requireTenantContext` from @resto/db. Inject `OffboardTenantService` into the constructor. Add `POST me/offboard` (HttpCode ACCEPTED) decorated `@Permissions({ tenant: ['delete'] })` + `@RequiresTenantContext()`, taking `@Body(new RestoZodValidationPipe(ScheduleOffboardingInputDto)) input`; derive `tenantId = requireTenantContext()` from ALS (NOT a :id param), call `offboarding.schedule({ tenantId, requestedBy: input.requestedBy })`, return `toResponse(snapshot)`. Add `DELETE me/offboard` (HttpCode OK) decorated `@Permissions({ tenant: ['delete'] })` + `@RequiresTenantContext()`, derive tenantId from ALS, call `offboarding.cancel({ tenantId })`, return `toResponse(snapshot)`. Wrap both handler bodies in the file's existing try/catch + `mapDomainError` idiom (matching getMe). Do NOT touch internal-tenants.controller.ts — the internal endpoints stay for other callers. Use `@Permissions({ tenant: ['delete'] })` (NOT 'manage' — it does not exist).
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "me/offboard" apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts; grep -c "tenant: \['delete'\]" apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts; pnpm exec nx run api:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tenants.controller.ts contains `@Post('me/offboard')` and `@Delete('me/offboard')`
    - Both handlers carry `@Permissions({ tenant: ['delete'] })` (2 occurrences); zero occurrences of `tenant: ['manage']`
    - Neither handler reads a `@Param('id')` — tenantId comes from `requireTenantContext()`
    - `api:typecheck` exits 0
  </acceptance_criteria>
  <done>Operator offboard schedule/cancel endpoints exist on v1/tenants, owner-gated, tenant-from-session; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Document admin.resto.app in CORS + trusted-origins env example</name>
  <read_first>
    - apps/api/.env.example (target — CORS_ALLOWED_ORIGINS / BETTER_AUTH_TRUSTED_ORIGINS lines)
    - apps/api/src/config/env.schema.ts (lines 132, 255 — confirm no schema change needed; the validators already accept the values)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 2 — exact env values, lines 316-325)
  </read_first>
  <action>
    In apps/api/.env.example, update the documented production-shape values: add `https://admin.resto.app` to `CORS_ALLOWED_ORIGINS` and to `BETTER_AUTH_TRUSTED_ORIGINS`; confirm the dev default for `CORS_ALLOWED_ORIGINS` already includes `http://localhost:3001` (the admin Vite dev port, same as current Next port — keep unchanged). Confirm `AUTH_COOKIE_DOMAIN` example value is `.resto.app` (leading dot, already validated by env.schema.ts line 255). Add a one-line comment noting the admin SPA host shares the registrable domain so the session cookie is same-site under SameSite=Lax (D-01). Do NOT modify env.schema.ts — the validators already accept these strings; this is documentation/example only so the deploy plan (07.6-07) and prod secret store inject the correct values.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "admin.resto.app" apps/api/.env.example; grep -c "localhost:3001" apps/api/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/.env.example references `admin.resto.app` in both CORS and trusted-origins documentation (≥2 occurrences)
    - The dev default still lists `localhost:3001` for admin
    - env.schema.ts is unchanged (no schema edit required — already validates these)
  </acceptance_criteria>
  <done>The admin host is documented for CORS + trusted origins; deploy plan can inject it into the prod secret store.</done>
</task>

<task type="auto">
  <name>Task 3: Owner-only integration test for the operator offboard endpoints</name>
  <read_first>
    - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts (the new handlers)
    - apps/api/ existing tenancy operator e2e/integration spec (grep for getMe / v1/tenants/me tests) — copy the operator session fixture + role setup
    - apps/api/src/contexts/tenancy/application/offboard-tenant.service.ts (schedule/cancel return shape + 30-day cool-off behavior)
  </read_first>
  <action>
    Add or extend a tenancy `*.e2e.spec.ts` that, using an operator session fixture: (1) as an OWNER, POST /v1/tenants/me/offboard with `{ requestedBy: <userId> }` returns 202 and the tenant snapshot shows offboardingScheduledAt set; (2) as an owner, DELETE /v1/tenants/me/offboard returns 200 and the schedule is cleared; (3) as a NON-owner (admin or staff role), both endpoints return 403 (proving the tenant:['delete'] gate); (4) unauthenticated request returns 401. Assert the tenant is the session's tenant (no :id leakage / cross-tenant offboard possible). Reuse the existing tenancy fixture rather than re-bootstrapping auth.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm exec nx run api:test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test: owner POST me/offboard → 202 + offboardingScheduledAt set
    - Test: owner DELETE me/offboard → 200 + schedule cleared
    - Test: non-owner (admin/staff) → 403 on both endpoints
    - Test: unauthenticated → 401
    - `api:test` exits 0
  </acceptance_criteria>
  <done>Owner-only, session-scoped offboard is proven by automated test; suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                |
| ------------------------------------------ | -------------------------------------------------------------------------- |
| SPA browser → API (v1/tenants/me/offboard) | Untrusted session cookie; destructive tenant-lifecycle action crosses here |
| CORS-with-credentials origin allowlist     | Cross-subdomain credentialed requests gated here                           |

## STRIDE Threat Register

| Threat ID | Category         | Component                                       | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | ---------------- | ----------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-06  | Elevation        | offboard endpoint reachable by non-owner        | mitigate    | @Permissions({ tenant: ['delete'] }) — only owner role holds tenant:delete (admin/staff do not); asserted 403 in Task 3                                                    |
| T-076-07  | Tampering        | cross-tenant offboard via forged :id            | mitigate    | No :id param exists — tenant derived from session ALS via requireTenantContext(); a request can only offboard its own tenant                                               |
| T-076-08  | Spoofing         | credentialed CORS request from arbitrary origin | mitigate    | CORS_ALLOWED_ORIGINS is an explicit allowlist (never `*` with credentials); only api/admin/menu hosts; documented in Task 2; buildOriginMatcher enforces exact/regex match |
| T-076-09  | Repudiation      | offboard not attributable to an operator        | mitigate    | schedule() records requestedBy (operator userId); session-derived principal makes the action attributable (closes HIGH-7 attribution for this path)                        |
| T-076-10  | Tampering (CSRF) | cross-site POST to me/offboard                  | mitigate    | SameSite=Lax cookie blocks cross-site state-changing POST; credentialed fetch only succeeds from CORS-allowed origin; API validates Origin                                 |

</threat_model>

<verification>
- `api:typecheck` and `api:test` both exit 0
- tenants.controller.ts exposes me/offboard POST+DELETE with `tenant: ['delete']` and no `:id` param
- .env.example documents admin.resto.app for CORS + trusted origins
</verification>

<success_criteria>

- SC-2: the settings danger-zone has an operator-auth target — no remaining admin reliance on internal/v1/tenants from the browser
- SC-3: the API config documents the admin origin for the cross-subdomain cookie session
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-02-SUMMARY.md` when done
</output>
