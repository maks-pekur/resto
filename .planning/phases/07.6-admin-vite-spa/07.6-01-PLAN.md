---
phase: 07.6-admin-vite-spa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts
  - apps/api/src/contexts/catalog/catalog.module.ts
  - apps/api/src/contexts/catalog/interfaces/http/internal-catalog.controller.ts
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - 'Operator with menu:read can GET all 7 catalog reads on v1/catalog with a session cookie (no internal token)'
    - 'internal/v1/catalog no longer exists in the running API'
    - 'The 7 relocated reads enforce tenant-from-session + brand-from-x-brand-slug via BrandScopeGuard'
  artifacts:
    - path: 'apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts'
      provides: '7 operator-guarded catalog GET handlers (categories, items, items/:id, modifier-groups, modifier-groups/:id, stop-list, draft-diff)'
      contains: "@Permissions({ menu: ['read'] })"
    - path: 'apps/api/src/contexts/catalog/catalog.module.ts'
      provides: 'CatalogModule controllers list without InternalCatalogController'
      contains: 'controllers: [PublicMenuController, CatalogController]'
  key_links:
    - from: 'apps/admin (future SPA)'
      to: 'GET /v1/catalog/*'
      via: 'operator session cookie + x-brand-slug header'
      pattern: 'v1/catalog/(categories|items|modifier-groups|stop-list|draft-diff)'
    - from: 'catalog.controller.ts handlers'
      to: 'List/Get catalog application services'
      via: 'constructor injection (already registered in CatalogModule)'
      pattern: 'ListCategoriesService|ListItemsService|GetItemService|GetDraftDiffService'
---

<objective>
Relocate the 7 catalog GET read handlers from the internal token-protected `InternalCatalogController` (`internal/v1/catalog`) onto the operator-authenticated `CatalogController` (`v1/catalog`), reusing the exact same application services, then delete `internal-catalog.controller.ts`. This closes review HIGH-7 (per-operator RBAC/attribution) by routing all admin catalog reads through better-auth session + Permissions + BrandScope instead of `INTERNAL_API_TOKEN` (D-08).

Purpose: The Vite SPA cannot hold `INTERNAL_API_TOKEN`. The 7 reads are the exact, closed parity gap (every catalog write is already operator-guarded). After this plan, no admin data path touches the internal surface for catalog.
Output: 7 new GET handlers on `v1/catalog`, updated `CatalogModule` controllers list, deleted `internal-catalog.controller.ts`, no dead callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md

<interfaces>
<!-- menu:read IS declared — Assumption A1 RESOLVED, no access-control change needed -->
From packages/domain/src/rbac/permissions.ts:
  PERMISSIONS_STATEMENT.menu = ['read', 'create', 'update', 'delete']
  SYSTEM_ROLES.owner/admin both grant menu: ['read', 'create', 'update', 'delete']

The 7 services to inject (ALL already registered as providers in CatalogModule):
ListCategoriesService, ListItemsService, GetItemService,
ListModifierGroupsService, GetModifierGroupService, GetStopListService, GetDraftDiffService

Existing operator-guard pattern in catalog.controller.ts (writes):
@Controller('v1/catalog') @RequiresTenantContext() (class-level; global AuthGuard applies)
per-write: @Permissions({ menu: ['update'] }) @RequireBrand()
const wrap = wrapWith(mapCatalogError); return wrap(() => service.execute(input));

Handler bodies to copy VERBATIM from internal-catalog.controller.ts:55-146
(parentId filter logic for listCategories; status/categoryId/q/limit/offset parsing for listItems)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify menu:read declared, then add 7 operator GET handlers to CatalogController</name>
  <read_first>
    - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts (target — existing writes show the operator-guard + wrap pattern, lines 1-90)
    - apps/api/src/contexts/catalog/interfaces/http/internal-catalog.controller.ts (analog — exact 7 handler bodies, lines 55-146)
    - packages/domain/src/rbac/permissions.ts (confirm menu:['read'] exists — A1)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (catalog.controller.ts section, lines 665-733 — decorator + RequireBrand table)
  </read_first>
  <action>
    First confirm `menu: ['read']` is present in `PERMISSIONS_STATEMENT` in packages/domain/src/rbac/permissions.ts (it is — do NOT modify access-control). Add to catalog.controller.ts: import `Get`, `Query` from `@nestjs/common`; import the 7 List/Get services and their response DTOs (CategoryListResponseDto, ItemListResponseDto, ItemDetailResponseDto, ModifierGroupListResponseDto, ModifierGroupDetailResponseDto, StopListResponseDto, DraftDiffResponseDto) plus `ItemStatusFilter` type. Inject all 7 services into the constructor alongside existing services. Add 7 GET handlers copying handler BODIES verbatim from internal-catalog.controller.ts (the parentId/status/categoryId/q/limit/offset parsing is identical). Decorate each with `@Permissions({ menu: ['read'] })`; add `@RequireBrand()` to the 6 brand-scoped reads (categories, items, items/:id, modifier-groups, modifier-groups/:id, stop-list) but NOT to draft-diff (tenant-only per PATTERNS.md Pattern 6 table). Swap `@ApiUnauthorizedResponse` to `@ApiForbiddenResponse` (operator guard returns 403, not 401). Use the existing `wrap`/`mapCatalogError` already in the file. Do not change any handler logic — only decorators and wiring differ from the internal controller.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm exec nx run api:typecheck 2>&1 | tail -5; grep -c "@Permissions({ menu: \['read'\] })" apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts</automated>
  </verify>
  <acceptance_criteria>
    - catalog.controller.ts contains exactly 7 occurrences of `@Permissions({ menu: ['read'] })`
    - catalog.controller.ts contains `@Get('categories')`, `@Get('items')`, `@Get('items/:id')`, `@Get('modifier-groups')`, `@Get('modifier-groups/:id')`, `@Get('stop-list')`, `@Get('draft-diff')`
    - `@RequireBrand()` appears on the 6 brand-scoped GETs but not on draft-diff
    - `api:typecheck` exits 0
  </acceptance_criteria>
  <done>All 7 catalog reads exist on v1/catalog as operator-guarded handlers; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete InternalCatalogController and drop it from CatalogModule</name>
  <read_first>
    - apps/api/src/contexts/catalog/catalog.module.ts (target — controllers array line ~40)
    - apps/api/src/contexts/catalog/interfaces/http/internal-catalog.controller.ts (file to delete)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (D-08 checklist, lines 860-874 — confirms only apps/admin calls internal/v1/catalog)
  </read_first>
  <action>
    Grep the whole repo for `internal/v1/catalog` to confirm only `apps/admin` (Next, being retired) references it — no website/qr-menu/api/test caller exists for the path besides admin. Delete the file apps/api/src/contexts/catalog/interfaces/http/internal-catalog.controller.ts. In catalog.module.ts, remove the `InternalCatalogController` import and remove it from the `controllers` array, leaving `controllers: [PublicMenuController, CatalogController]`. Leave all providers (the 7 services stay — now consumed by CatalogController) and the `InternalTokenGuard` import path untouched elsewhere in the repo (it stays for internal/v1/tenants). Run the catalog e2e/integration suite to confirm the relocated reads behave identically under operator auth.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && test ! -f apps/api/src/contexts/catalog/interfaces/http/internal-catalog.controller.ts && echo "DELETED OK"; grep -rn "InternalCatalogController\|internal/v1/catalog" apps/api/ packages/ | grep -v '^#' | grep -vc test; pnpm exec nx run api:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File internal-catalog.controller.ts does not exist
    - No reference to `InternalCatalogController` remains in apps/api/src or catalog.module.ts
    - No non-admin caller of `internal/v1/catalog` exists in apps/api or packages
    - catalog.module.ts `controllers` array equals `[PublicMenuController, CatalogController]`
    - `api:typecheck` exits 0
  </acceptance_criteria>
  <done>Internal catalog read surface is fully removed; CatalogModule wires only public + operator controllers; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: Operator-auth integration test for the 7 relocated reads</name>
  <read_first>
    - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts (the new handlers)
    - apps/api/ existing catalog e2e/integration spec for the operator write path (find via grep for v1/catalog POST tests) — copy its auth-fixture/session-bootstrap setup
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 6 — guard derivation; brand from x-brand-slug)
  </read_first>
  <action>
    Locate the existing catalog operator e2e spec (the one exercising POST /v1/catalog/* with an operator session + x-brand-slug). If none isolates the GET reads, add a `*.e2e.spec.ts` that, using the same operator session fixture: (1) calls each of the 7 GET reads on v1/catalog with a valid session + x-brand-slug and asserts 200 with the expected response DTO shape; (2) asserts a request WITHOUT a session returns 401/403 (not 200) — proving the internal-token bypass is gone; (3) asserts a brand-scoped read with an out-of-scope x-brand-slug is rejected by BrandScopeGuard (403). draft-diff is asserted tenant-only (no brand header required). If a catalog operator e2e spec already exists, extend it rather than duplicating the fixture.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && pnpm exec nx run api:test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts each of the 7 GETs returns 200 under operator session + x-brand-slug
    - A test asserts unauthenticated GET on v1/catalog/categories returns 401 or 403 (NOT 200)
    - A test asserts out-of-scope x-brand-slug is rejected (403) on a brand-scoped read
    - `api:test` exits 0
  </acceptance_criteria>
  <done>The relocated reads are proven operator-guarded and tenant/brand-isolated by automated test; suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                        |
| -------------------------------------------- | ---------------------------------------------------------------------------------- |
| SPA browser → API (v1/catalog)               | Untrusted session cookie + forgeable x-tenant-id / x-brand-slug headers cross here |
| internal token surface (retired for catalog) | Was a shared static secret; now removed from the catalog read path                 |

## STRIDE Threat Register

| Threat ID | Category        | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | --------------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-076-01  | Spoofing        | x-tenant-id header on relocated reads     | mitigate    | AuthGuard cross-checks x-tenant-id against the session's actual activeOrganizationId (existing tenant_mismatch check, RES-172); tenant is session-derived, not trusted from header alone |
| T-076-02  | Elevation       | x-brand-slug on brand-scoped reads        | mitigate    | BrandScopeGuard validates brandId ∈ member_brand_scope from DB; @RequireBrand() on all 6 brand reads; forged slug cannot reach out-of-scope brand (asserted in Task 3)                   |
| T-076-03  | Elevation       | Missing @Permissions on a relocated read  | mitigate    | Every GET carries @Permissions({ menu: ['read'] }); grep gate asserts exactly 7 occurrences (Task 1)                                                                                     |
| T-076-04  | Info Disclosure | Leftover internal/v1/catalog read surface | mitigate    | InternalCatalogController deleted; grep gate asserts no remaining non-admin caller (Task 2)                                                                                              |
| T-076-05  | Tampering       | draft-diff exposed cross-brand            | accept      | draft-diff is tenant-scoped (not brand-scoped) by existing service design; tenant isolation via AuthGuard suffices — matches internal behavior, no scope change                          |

</threat_model>

<verification>
- `api:typecheck` and `api:test` both exit 0
- `grep -rn "internal/v1/catalog" apps/api packages` returns no non-test, non-admin hits
- The 7 GET handlers are operator-guarded (`@Permissions({ menu: ['read'] })` present 7×)
</verification>

<success_criteria>

- SC-1: catalog-admin reads are served by an operator endpoint the SPA can consume
- SC-2: no internal-token catalog read path remains — admin browser will reach v1/catalog under session auth only
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-01-SUMMARY.md` when done
</output>
