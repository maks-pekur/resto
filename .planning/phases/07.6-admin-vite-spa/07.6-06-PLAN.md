---
phase: 07.6-admin-vite-spa
plan: 06
type: execute
wave: 2
depends_on: [07.6-02, 07.6-03]
files_modified:
  - apps/admin/src/lib/queries/tenancy.ts
  - apps/admin/src/lib/queries/team.ts
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.domains.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.theme.tsx
  - apps/admin/src/routes/(protected)/dashboard/$brandSlug/brands.$slug.payouts.tsx
  - apps/admin/src/routes/(protected)/dashboard/settings.tsx
  - apps/admin/src/routes/(protected)/dashboard/team.tsx
  - apps/admin/src/components/danger-zone-card.tsx
  - apps/admin/src/components/ai-preview-card.tsx
  - apps/admin/src/components/ai-preview-card-client.tsx
  - apps/admin/src/components/team/
  - apps/admin/src/components/settings/
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - 'An operator can view brand domains, edit brand theme, and view payouts from the SPA'
    - 'Settings renders; an owner can schedule and cancel offboarding via the operator v1/tenants/me/offboard endpoint (no internal token)'
    - 'Two-factor enable and team invite/management work via the better-auth client'
    - 'Settings shows the cool-off / not-authorized error states with friendly copy'
  artifacts:
    - path: 'apps/admin/src/routes/(protected)/dashboard/settings.tsx'
      provides: 'settings route incl. offboard danger-zone via operator endpoint'
      contains: 'me/offboard'
    - path: 'apps/admin/src/lib/queries/tenancy.ts'
      provides: 'tenancy queries + offboard mutations via operator v1/tenants'
      contains: '/v1/tenants/me/offboard'
  key_links:
    - from: 'settings danger-zone'
      to: 'POST/DELETE /v1/tenants/me/offboard'
      via: 'apiFetch operator session (no internal token)'
      pattern: '/v1/tenants/me/offboard'
    - from: 'team route'
      to: 'authClient.organization invite/list/remove'
      via: 'better-auth organization client'
      pattern: "organization\\."
---

<objective>
Port the remaining operator surface — brands/domains/payouts/theme, settings (including the offboarding danger-zone, now on the operator `POST/DELETE /v1/tenants/me/offboard` endpoint from 07.6-02), two-factor, and team management — to the Vite SPA. This eliminates the last `apiFetchInternal` call from the admin browser (SC-2) and preserves the remaining UI surface (SC-1).

Purpose: Closes the settings offboard internal-token gap (RESEARCH Open Question #1 / Pitfall 6) by retargeting the danger-zone at the operator endpoint, and ports the brand-config + team surfaces that complete the operator dashboard.
Output: tenancy + team Query factories, brands/domains/payouts/theme routes, settings + team routes, danger-zone + ai-preview + team/settings components — all server-action-free, operator-auth only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.6-admin-vite-spa/07.6-CONTEXT.md
@.planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md
@.planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md
@apps/CLAUDE.md

<interfaces>
Depends on 07.6-03 (apiFetch, authClient, queryClient, i18n) and 07.6-02 (operator offboard endpoints live).
Owns ONLY: brands.$slug.* routes, settings.tsx, team.tsx, danger-zone/ai-preview/team/settings components,
  and src/lib/queries/{tenancy,team}.ts — NO overlap with 04 (shell/auth) or 05 (menu).

Operator offboard (from 07.6-02): POST /v1/tenants/me/offboard { requestedBy } → 202; DELETE /v1/tenants/me/offboard → 200.
owner-only (tenant:['delete']); tenant from session. Replaces apiFetchInternal('/internal/v1/tenants/:id/offboard').
Legacy friendly-error mapping (settings/actions.ts:36-50): 404 not found / 401 not authorized /
409 cool-off expired / 409 cannot be offboarded — preserve these messages.
requireOwner gate (settings/actions.ts:53-60): /v1/me kind==='operator' && baseRole==='owner' — port as a client guard
that hides/disables the danger-zone for non-owners (the API still enforces 403).

Tenancy reads: /v1/me (kind/email/baseRole/tenantId), /v1/me/domains, /v1/tenants/me, /v1/tenants/me/domains.
Wrap as tenancyQuery / tenantDomainsQuery in src/lib/queries/tenancy.ts via apiFetch.
Brand theme/payouts: use the existing operator brand/payout endpoints the current Next pages call (confirm via the
current page.tsx server reads); wrap as Query factories.

Team + 2FA: better-auth organization client — authClient.organization.inviteMember / listInvitations / listMembers /
removeMember / cancelInvitation; two-factor enable via authClient.twoFactor.enable (port two-factor-enable-client.tsx).
invite/accept already have routes in 04 (accept-invitation); team.tsx manages the member/invite list.

Components to port: danger-zone-card.tsx, ai-preview-card.tsx + ai-preview-card-client.tsx, the team invite-form /
team-invite-button clients, the settings two-factor-enable client — strip next-intl/next imports, wire to mutations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenancy + team Query factories + brands/domains/payouts/theme routes</name>
  <read_first>
    - apps/admin/app/dashboard/(workspace)/brands/[slug]/{domains,theme,payouts}/page.tsx (analogs — what each reads)
    - apps/admin/lib/me.ts + me-brands.ts (identity reads)
    - apps/admin/components/{tenant-breadcrumb,page-heading}.tsx (already ported in 04 — reuse, do not duplicate)
    - .planning/phases/07.6-admin-vite-spa/07.6-PATTERNS.md (apiFetch query factory pattern, lines 823-831)
  </read_first>
  <action>
    Create src/lib/queries/tenancy.ts (tenancyQuery '/v1/tenants/me', tenantDomainsQuery '/v1/tenants/me/domains', brandThemeQuery, brandPayoutsQuery — using the same operator endpoints the current Next pages read; confirm exact paths from each page.tsx) and src/lib/queries/team.ts (memberListQuery, invitationListQuery via the better-auth organization client or operator endpoints as the current pages do). Create the three brand routes under (protected)/dashboard/$brandSlug/: brands.$slug.domains.tsx (loader prefetch tenantDomainsQuery; render the domains table), brands.$slug.theme.tsx (theme editor form → brand-theme update mutation), brands.$slug.payouts.tsx (payouts read + the Stripe Connect status display the current page shows). Swap useTranslations→useTranslation, next/link→TanStack Link. Each brand-scoped read threads brandSlug → x-brand-slug. No server actions; all writes are Query mutations via apiFetch.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -rc "/v1/tenants/me" apps/admin/src/lib/queries/tenancy.ts; ls apps/admin/src/routes/\(protected\)/dashboard/\$brandSlug/ | grep -c "brands\."; grep -rn "next/\|server-only\|'use server'" apps/admin/src/lib/queries apps/admin/src/routes/\(protected\)/dashboard/\$brandSlug/brands* | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tenancy.ts queries reference `/v1/tenants/me` operator endpoints
    - brands.$slug.{domains,theme,payouts}.tsx routes exist
    - Zero next/* / server-only / 'use server' imports in the new queries + brand routes
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>Brand config surface (domains/theme/payouts) ported with operator-auth queries; brand-in-URL threaded.</done>
</task>

<task type="auto">
  <name>Task 2: Settings route + offboard danger-zone via operator endpoint</name>
  <read_first>
    - apps/admin/app/dashboard/(workspace)/settings/{page.tsx,actions.ts,two-factor-enable-client.tsx,two-factor-actions.ts} (analogs)
    - apps/admin/components/danger-zone-card.tsx (port)
    - apps/api/src/contexts/tenancy/interfaces/http/tenants.controller.ts (the new me/offboard endpoints from 07.6-02 — request/response shapes)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pitfall 6 + Open Question #1 — settings offboard resolution)
  </read_first>
  <action>
    Create src/routes/(protected)/dashboard/settings.tsx porting the settings surface. Add offboard mutations to src/lib/queries/tenancy.ts: scheduleOffboard (POST /v1/tenants/me/offboard { requestedBy: userId } via apiFetch) and cancelOffboard (DELETE /v1/tenants/me/offboard) — NOT apiFetchInternal, NOT /internal/*. Port danger-zone-card.tsx wiring its schedule/cancel buttons to these mutations; preserve the friendly-error mapping (404/401/409 cool-off/409 cannot-be-offboarded) and the owner-only guard (read baseRole from tenancyQuery/meQuery; hide or disable the danger-zone for non-owners — the API enforces 403 regardless). Port two-factor-enable-client.tsx onto authClient.twoFactor.enable (or the operator 2FA endpoints the current actions call). Swap next-intl/next imports. After save, invalidate the tenancy query instead of revalidatePath. Confirm NO `apiFetchInternal` / `internal/v1/tenants` reference remains anywhere in apps/admin/src.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "me/offboard" apps/admin/src/routes/\(protected\)/dashboard/settings.tsx apps/admin/src/lib/queries/tenancy.ts 2>/dev/null | tail -1; grep -rn "apiFetchInternal\|internal/v1/tenants\|INTERNAL_API_TOKEN" apps/admin/src/ | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - settings/tenancy.ts reference `me/offboard` (operator endpoint)
    - Zero `apiFetchInternal`, `internal/v1/tenants`, or `INTERNAL_API_TOKEN` references anywhere in apps/admin/src (the decisive SC-2 gate)
    - The danger-zone preserves friendly-error copy and the owner-only client guard
    - `admin:typecheck` exits 0
  </acceptance_criteria>
  <done>Settings + offboard danger-zone run entirely on operator auth; the admin browser has zero internal-token surface left.</done>
</task>

<task type="auto">
  <name>Task 3: Team management route + components</name>
  <read_first>
    - apps/admin/app/dashboard/(workspace)/team/{page.tsx,invite-form-client.tsx,invite-action.ts,team-invite-button-client.tsx} (analogs)
    - apps/admin/components/ai-preview-card.tsx + ai-preview-card-client.tsx (port if rendered in team/settings)
    - .planning/phases/07.6-admin-vite-spa/07.6-RESEARCH.md (Pattern 1 — organization client methods)
  </read_first>
  <action>
    Create src/routes/(protected)/dashboard/team.tsx (loader prefetch memberListQuery + invitationListQuery; render members + pending invitations). Port invite-form-client + team-invite-button into src/components/team, wiring invite to `authClient.organization.inviteMember({ email, role })`, cancel-invite to `cancelInvitation`, remove-member to `removeMember` — each as a useMutation invalidating the team queries. Swap next-intl→react-i18next, next/link→TanStack Link, drop server actions. Port ai-preview-card if the team/settings pages render it. Register the team route in the router.
  </action>
  <verify>
    <automated>cd /Users/mp_dev/projects/RestOS && grep -c "organization.inviteMember\|inviteMember" apps/admin/src/components/team/*.tsx apps/admin/src/routes/\(protected\)/dashboard/team.tsx 2>/dev/null | tail -1; grep -rn "next-intl\|next/link\|'use server'" apps/admin/src/components/team apps/admin/src/routes/\(protected\)/dashboard/team.tsx | wc -l; pnpm exec nx run admin:typecheck 2>&1 | tail -3; pnpm exec nx run admin:build 2>&1 | tail -4 && test -d apps/admin/dist && echo "DIST-OK"</automated>
  </verify>
  <acceptance_criteria>
    - team.tsx + team components use the better-auth organization client (inviteMember/removeMember/cancelInvitation)
    - Zero next-intl / next/link / 'use server' imports in the team surface
    - `admin:typecheck` exits 0 and `admin:build` produces dist/ (DIST-OK) — the full surface compiles
  </acceptance_criteria>
  <done>Team management is ported on the organization client; the full admin SPA builds to static dist/.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                       |
| ---------------------------------------- | ------------------------------------------------- |
| settings danger-zone → me/offboard       | Destructive owner-only tenant action crosses here |
| team invite/remove → organization client | Membership changes cross here                     |

## STRIDE Threat Register

| Threat ID | Category        | Component                                           | Disposition | Mitigation Plan                                                                                                                     |
| --------- | --------------- | --------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-076-25  | Elevation       | non-owner triggers offboard                         | mitigate    | client guard hides/disables danger-zone for non-owners; API tenant:['delete'] (07.6-02) enforces 403 — defence in depth             |
| T-076-26  | Info Disclosure | residual internal-token path in browser             | mitigate    | Task 2 grep gate asserts zero apiFetchInternal/internal/v1/tenants/INTERNAL_API_TOKEN in apps/admin/src — the decisive SC-2 closure |
| T-076-27  | Tampering       | cross-tenant offboard via stale tenant              | mitigate    | offboard tenant is session-derived in the API (no :id); SPA cannot target another tenant                                            |
| T-076-28  | Elevation       | team member invited with elevated role by non-admin | mitigate    | better-auth organization plugin enforces inviter permission server-side; staff role cannot invite                                   |
| T-076-29  | Repudiation     | offboard not attributable                           | mitigate    | requestedBy = operator userId sent on schedule; session-bound principal makes it attributable                                       |

</threat_model>

<verification>
- `admin:typecheck` + `admin:build` exit 0 (full surface compiles to dist/)
- Zero apiFetchInternal / internal/v1/tenants / INTERNAL_API_TOKEN anywhere in apps/admin/src
- Settings offboard + team run on operator/organization auth only
</verification>

<success_criteria>

- SC-1: brands/domains/payouts/theme, settings, team surfaces preserved by porting
- SC-2: the admin browser has NO remaining internal-token path — settings offboard moved to the operator endpoint
  </success_criteria>

<output>
Create `.planning/phases/07.6-admin-vite-spa/07.6-06-SUMMARY.md` when done
</output>
