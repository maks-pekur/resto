---
phase: 07-ordering
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/ids.ts
  - packages/domain/src/index.ts
  - apps/api/src/contexts/ordering/domain/money-utils.ts
  - apps/api/src/contexts/ordering/domain/money-utils.spec.ts
  - apps/api/src/contexts/ordering/domain/discount.ts
  - apps/api/src/contexts/ordering/domain/discount.spec.ts
autonomous: true
requirements: [PROMO-06]
must_haves:
  truths:
    - "applyDiscount() computes percentage and fixed discounts at cart/category/item scope without any DB access"
    - "applyDiscount() never returns a negative number (clamped to 0)"
    - "Money is converted decimal-string <-> integer minor units with no float drift ('0.10' + '0.20' = '0.30')"
    - "OrderId and OrderItemId branded types are exported from @resto/domain"
  artifacts:
    - path: "apps/api/src/contexts/ordering/domain/discount.ts"
      provides: "DiscountSpec discriminated union + pure applyDiscount()"
      contains: "export function applyDiscount"
    - path: "apps/api/src/contexts/ordering/domain/money-utils.ts"
      provides: "toMinorUnits / fromMinorUnits (no cross-package @resto/cart import)"
      contains: "export function toMinorUnits"
    - path: "packages/domain/src/ids.ts"
      provides: "OrderId, OrderItemId branded zod schemas"
      contains: "OrderId"
  key_links:
    - from: "apps/api/src/contexts/ordering/domain/discount.ts"
      to: "OrderLineDraft minor-unit lineTotal"
      via: "pure-integer arithmetic"
      pattern: "Math.max\\(0,"
---

<objective>
Build the pure, dependency-free domain foundation for the ordering context: the branded `OrderId` / `OrderItemId` identity types, the money minor-unit conversion helpers (replicated into the ordering domain, NOT imported cross-package from `@resto/cart`), and the greenfield discount engine (PROMO-06). All three are pure TypeScript with zero infrastructure imports, so they are TDD'd in full before any aggregate, repository, or schema exists.

Purpose: PROMO-06 mandates a pure discount function (no DB calls) that Phase 8 checkout and Phase 11 promo codes feed without rewriting. The `DiscountSpec` discriminated union is built extensible so Phase 11 appends new union members. The money helpers prevent IEEE-754 float drift in all downstream totals.
Output: `OrderId`/`OrderItemId` in `@resto/domain`; `money-utils.ts` + `discount.ts` in `ordering/domain` with full spec coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ordering/07-CONTEXT.md
@.planning/phases/07-ordering/07-RESEARCH.md
@.planning/phases/07-ordering/07-PATTERNS.md

<interfaces>
<!-- Existing money helpers in @resto/cart to REPLICATE (not import) — packages/cart/src/cart.ts -->
parseMinorUnits(value: string): number   // '12.50' -> 1250
formatMinorUnits(minor: number): string  // 1250 -> '12.50'

<!-- Existing branded-id pattern — packages/domain/src/ids.ts -->
export const TenantId = z.string().uuid().brand<'TenantId'>();
export type TenantId = z.infer<typeof TenantId>;
export const BrandId = z.string().uuid().brand<'BrandId'>();
export const MenuItemId = z.string().uuid().brand<'MenuItemId'>();
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Branded OrderId / OrderItemId + ordering money-utils (TDD)</name>
  <files>packages/domain/src/ids.ts, packages/domain/src/index.ts, apps/api/src/contexts/ordering/domain/money-utils.ts, apps/api/src/contexts/ordering/domain/money-utils.spec.ts</files>
  <read_first>
    - packages/domain/src/ids.ts (existing branded-id pattern — TenantId/BrandId/MenuItemId at lines 15-35)
    - packages/domain/src/index.ts (barrel export — confirm ids re-export site)
    - packages/cart/src/cart.ts lines 34-50 (parseMinorUnits/formatMinorUnits to replicate verbatim)
    - 07-PATTERNS.md §"packages/domain/src/ids.ts" + §"Money: parseMinorUnits / formatMinorUnits"
  </read_first>
  <behavior>
    - toMinorUnits('12.50') === 1250; toMinorUnits('0.00') === 0; toMinorUnits('9999999999.99') === 999999999999
    - fromMinorUnits(1250) === '12.50'; fromMinorUnits(0) === '0.00'; fromMinorUnits(5) === '0.05'
    - Round-trip: fromMinorUnits(toMinorUnits('0.10') + toMinorUnits('0.20')) === '0.30' (no float drift — the Pitfall-1 guard)
    - toMinorUnits rejects/handles a value with >2 decimal places consistently with the cart helper (match cart.ts behavior exactly)
    - OrderId.parse(randomUUID()) succeeds; OrderId.parse('not-a-uuid') throws
  </behavior>
  <action>
    Add `OrderId` and `OrderItemId` to `packages/domain/src/ids.ts` mirroring the existing `MenuItemId` shape exactly: `export const OrderId = z.string().uuid().brand<'OrderId'>();` plus the `export type OrderId = z.infer<typeof OrderId>;` companion. Ensure both are re-exported through `packages/domain/src/index.ts` (follow how `MenuItemId` is surfaced).
    Create `apps/api/src/contexts/ordering/domain/money-utils.ts` exporting `toMinorUnits(value: string): number` and `fromMinorUnits(minor: number): string`, copied verbatim from `packages/cart/src/cart.ts` `parseMinorUnits`/`formatMinorUnits` (rename to `toMinorUnits`/`fromMinorUnits`). Do NOT import from `@resto/cart` — it is a UI-focused package and the module-boundary rule forbids `apps/api` depending on it (research assumption A7). Write the spec first (RED), then the implementation (GREEN). No code comments unless a genuine WHY.
  </action>
  <verify>
    <automated>pnpm vitest run apps/api/src/contexts/ordering/domain/money-utils.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "OrderId" packages/domain/src/ids.ts` and `OrderItemId` present
    - `node -e "const {OrderId}=require('@resto/domain'); OrderId.parse(require('crypto').randomUUID())"` exits 0 (or equivalent vitest assertion)
    - money-utils.spec.ts asserts the `'0.10' + '0.20' === '0.30'` round-trip and passes
    - No `from '@resto/cart'` import anywhere in `apps/api/src/contexts/ordering/`
    - `pnpm nx run domain:typecheck` passes
  </acceptance_criteria>
  <done>OrderId/OrderItemId exported from @resto/domain; toMinorUnits/fromMinorUnits implemented with passing float-drift spec; no @resto/cart import in apps/api.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Discount engine — DiscountSpec union + applyDiscount() (TDD, PROMO-06)</name>
  <files>apps/api/src/contexts/ordering/domain/discount.ts, apps/api/src/contexts/ordering/domain/discount.spec.ts</files>
  <read_first>
    - 07-PATTERNS.md §"apps/api/src/contexts/ordering/domain/discount.ts" (full DiscountSpec union + applyDiscount signature)
    - 07-RESEARCH.md §"Discount Engine (PROMO-06)" + §"Pitfall 6" (minor-units-only input)
    - 07-CONTEXT.md D-05 (extensible discriminated union; pure; no DB; Phase 11 extends without rewrite)
    - apps/api/src/contexts/ordering/domain/money-utils.ts (Task 1 output — minor-unit math)
  </read_first>
  <behavior>
    - applyDiscount(lines, null) === 0
    - percentage/cart pct=10 over lines summing 10000 minor units === 1000
    - percentage/item itemId='X' pct=20 discounts only line(s) with itemId 'X'
    - percentage/category categoryId='C' pct=15 discounts only lines in category 'C'
    - fixed/cart amountMinorUnits=500 === 500; if 500 > cart subtotal it clamps to subtotal (never returns more than the eligible base)
    - fixed/item and fixed/category apply only to matching lines and clamp to that scope's eligible total
    - A discount that would exceed the eligible base returns the base, never a negative or over-base value
    - applyDiscount returns an integer (minor units) for every variant — never a float, never a decimal string
  </behavior>
  <action>
    Create `discount.ts` with the `DiscountSpec` discriminated union EXACTLY as in 07-PATTERNS.md (six members: {percentage|fixed} × {cart|category|item}), the `OrderLineDraft` interface (`{ itemId: string; categoryId: string; lineTotal: number }` where `lineTotal` is integer minor units), and the pure `applyDiscount(lines, spec | null): number`. Use plain integer percentage for `pct` (e.g. `10` = 10%); percentage arms compute `Math.round(eligibleBase * pct / 100)` to stay integer (Pitfall 6 — input is already minor units, never decimal strings). All six arms clamp with `Math.max(0, Math.min(eligibleBase, computed))` so discount is `>= 0` and `<= eligible base` (tampering mitigation T-07-04). Also export a Zod `DiscountSpecSchema` (discriminatedUnion on `kind`+`scope`, or nested discriminated unions) so the Plan 04 DTO can validate `discountSpec` at the HTTP boundary — `export type DiscountSpec = z.infer<...>` to keep the type and schema in sync per the DTO/Schema convention. Spec first (RED) covering all six variants + null + clamp cases, then implement (GREEN). No code comments.
  </action>
  <verify>
    <automated>pnpm vitest run apps/api/src/contexts/ordering/domain/discount.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - discount.spec.ts has at least one assertion per DiscountSpec variant (6) plus null + over-base clamp + negative-clamp; all pass
    - `grep -c "kind:" apps/api/src/contexts/ordering/domain/discount.ts` (after `grep -v '^#'`) confirms 6 union members
    - `grep -q "Math.max(0," apps/api/src/contexts/ordering/domain/discount.ts` (clamp present)
    - No `import` from `@resto/db`, `drizzle-orm`, `@nestjs/*` in discount.ts (purity gate): `grep -E "@resto/db|drizzle-orm|@nestjs" apps/api/src/contexts/ordering/domain/discount.ts` returns nothing
    - `DiscountSpecSchema` exported and `z.infer` type derived from it
    - `pnpm nx run api:typecheck` passes
  </acceptance_criteria>
  <done>applyDiscount is a pure function (no DB/framework imports) covering all 6 DiscountSpec variants with full clamp behavior; DiscountSpecSchema exported for Plan 04 DTO reuse; spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → applyDiscount input | The discount spec ultimately originates from a client/promo source; the engine must never trust it to produce a negative or over-base discount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | applyDiscount() | mitigate | Clamp result with `Math.max(0, Math.min(eligibleBase, computed))` — discount is always `>= 0` and `<= eligible base`; spec asserts both bounds |
| T-07-FLOAT | Tampering | money-utils + discount | mitigate | All arithmetic in integer minor units; no `parseFloat`; spec asserts `'0.10'+'0.20'='0.30'` round-trip |
| T-07-PURE | Tampering | discount.ts | mitigate | Purity gate: grep asserts no `@resto/db`/`drizzle-orm`/`@nestjs` import — engine cannot reach a DB to be poisoned |
</threat_model>

<verification>
- `pnpm vitest run apps/api/src/contexts/ordering/domain/` (both specs green)
- `pnpm nx run domain:typecheck && pnpm nx run api:typecheck`
- Module-boundary: no `@resto/cart` import inside `apps/api/src/contexts/ordering/`
</verification>

<success_criteria>
- OrderId/OrderItemId branded types exported from @resto/domain and re-exported through the barrel
- money-utils round-trips with zero float drift
- applyDiscount covers all 6 DiscountSpec variants, clamps to [0, eligibleBase], is import-pure
- DiscountSpecSchema available for downstream DTO validation
</success_criteria>

<output>
Create `.planning/phases/07-ordering/07-01-SUMMARY.md` when done.
</output>
