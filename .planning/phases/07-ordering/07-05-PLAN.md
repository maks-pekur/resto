---
phase: 07-ordering
plan: 05
type: execute
wave: 4
depends_on: ['07-04']
files_modified:
  - apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts
  - apps/api/src/contexts/ordering/interfaces/http/error-mapping.ts
  - apps/api/src/contexts/ordering/ordering.module.ts
  - apps/api/src/app.module.ts
  - apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts
  - apps/api/src/contexts/audit/application/record-audit.service.ts
  - packages/db/test/integration/tenant-isolation.spec.ts
autonomous: true
requirements: [ORD-01, ORD-03, ORD-07, ORD-09]
must_haves:
  truths:
    - 'POST /v1/orders is anonymous (@Public) but still 404s when no tenant resolves from host (@RequireActiveTenant)'
    - 'OrderingModule is registered in app.module.ts and the api boots with it'
    - 'Domain errors map to the right HTTP status via error-mapping (409 duplicate, 422 unavailable, 404 not-found, 409 invalid-transition)'
    - 'audit context subscribes to ordering.> and produces audit rows for ordering.order_created (ORD-09)'
    - 'record-audit resolves targetId from payload.orderId for the 5 ordering.* events'
    - 'tenant-isolation spec covers orders/order_items cross-tenant denial (ORD-06 wired into the existing net)'
  artifacts:
    - path: 'apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts'
      provides: 'POST /v1/orders anonymous create endpoint'
      contains: '@Public'
    - path: 'apps/api/src/contexts/ordering/ordering.module.ts'
      provides: 'NestJS module wiring repo + services + controller'
      contains: 'OrderingModule'
  key_links:
    - from: 'app.module.ts'
      to: 'OrderingModule'
      via: 'imports array'
      pattern: 'OrderingModule'
    - from: 'nats-audit-subscriber'
      to: 'ordering.> NATS subject'
      via: 'audit-recorder-ordering durable consumer'
      pattern: 'audit-recorder-ordering'
    - from: 'record-audit.service ACTION_TARGET_KIND'
      to: "'order' targetId branch"
      via: 'payload.orderId resolver'
      pattern: 'ordering.order_created'
---

<objective>
Surface the ordering context over HTTP and close the audit loop: the anonymous `POST /v1/orders` controller + error-mapping, the `OrderingModule` wiring, its registration in `app.module.ts`, the two surgical audit edits (`ordering.>` subscription + `ACTION_TARGET_KIND`/targetId branch), and the cross-tenant isolation coverage for the new `orders`/`order_items` tables.

Purpose: ORD-01/ORD-03 (the anonymous cart→order HTTP entry, the only reachable surface in Phase 7), ORD-07/ORD-09 (the five contracts now flow outbox→NATS→audit and produce audit rows), and the ORD-06 isolation net is extended to the new tables. This is the integration wave — after it, an external request creates an order end-to-end and an audit row appears.
Output: controller + error-mapping + module + app.module wiring + audit edits + isolation rows; an e2e order-create test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ordering/07-CONTEXT.md
@.planning/phases/07-ordering/07-RESEARCH.md
@.planning/phases/07-ordering/07-PATTERNS.md

<interfaces>
<!-- Plan 04 outputs -->
CreateOrderService.execute(input): Promise<{ orderId; orderNumber }>
GetOrderService.execute({ orderId }): Promise<OrderSnapshot>
CreateOrderInputDto; OrderResponseSchema (application/dto.ts)
ORDER_REPOSITORY (Symbol); OrderDrizzleRepository
OrderNotFoundError, DuplicateOrderKeyError, InvalidOrderTransitionError, OrderItemUnavailableError
<!-- Existing shared HTTP infra -->
@Public(), @RequireActiveTenant()   // apps/api/src/shared/auth
wrapWith(mapper)                     // apps/api/src/shared/api/wrap
RestoZodValidationPipe              // apps/api/src/shared/api/zod-validation.pipe
<!-- Audit extension points — record-audit.service.ts:7 (ACTION_TARGET_KIND), :67 (targetId resolver) -->
const ACTION_TARGET_KIND: Record<string,string> = { 'tenancy.tenant_provisioned':'tenant', ..., 'identity.signed_in':'user', ... }
// targetId resolver branches: 'tenant'|'menu' -> payload.tenantId; 'menu_item' -> payload.itemId; 'user' -> payload.userId
<!-- Audit subscriber loop — nats-audit-subscriber.ts:~42 ( [{subject:TENANCY_SUBJECT,...},{subject:IDENTITY_SUBJECT,...}] ) -->
<!-- Audit projection entry point — RecordAuditService.execute/handle(envelope) called via runDeduped from the subscriber; projects an envelope into an audit_log row -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: orders.controller + error-mapping + OrderingModule + app.module registration</name>
  <files>apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts, apps/api/src/contexts/ordering/interfaces/http/error-mapping.ts, apps/api/src/contexts/ordering/ordering.module.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/contexts/catalog/interfaces/http/public-menu.controller.ts (@Public + @RequireActiveTenant + wrapWith pattern)
    - apps/api/src/contexts/catalog/interfaces/http/catalog.controller.ts (RestoZodValidationPipe per-parameter @Body)
    - apps/api/src/contexts/catalog/interfaces/http/error-mapping.ts (domain-error → HttpException mapper; returns err unchanged for unknown)
    - apps/api/src/contexts/catalog/catalog.module.ts (module wiring — provider Symbol + services + controller)
    - apps/api/src/app.module.ts (imports array — add OrderingModule near CatalogModule)
    - 07-PATTERNS.md §"orders.controller.ts", §"error-mapping.ts", §"ordering.module.ts", §"app.module.ts"
  </read_first>
  <action>
    Create `error-mapping.ts` exporting `mapOrderError(err): unknown` mapping `DuplicateOrderKeyError`→`ConflictException({code:'ordering.duplicate_idempotency_key'})`, `OrderItemUnavailableError`→`UnprocessableEntityException({code:'ordering.item_unavailable'})`, `OrderNotFoundError`→`NotFoundException({code:'ordering.order_not_found'})`, `InvalidOrderTransitionError`→`ConflictException({code:'ordering.invalid_transition'})`; return `err` unchanged for anything else (ProblemDetailsFilter handles it). Do NOT throw inside the mapper.
    Create `orders.controller.ts` (`@ApiTags('ordering')`, `@Public()`, `@Controller('v1/orders')`, `const wrap = wrapWith(mapOrderError)`): a `@Post()` `@HttpCode(CREATED)` `@RequireActiveTenant()` handler validating `@Body(new RestoZodValidationPipe(CreateOrderInputDto))` and returning `wrap(() => this.createOrder.execute(input))`. `@Public()` is mandatory (ORD-03 anonymous); `@RequireActiveTenant()` keeps the 404-on-no-tenant behavior. Per-parameter pipe (CONVENTIONS.md), not global.
    Create `ordering.module.ts` exactly as 07-PATTERNS.md: `controllers: [OrdersController]`, `providers: [{ provide: ORDER_REPOSITORY, useClass: OrderDrizzleRepository }, CreateOrderService, GetOrderService]`.
    Register `OrderingModule` in `app.module.ts` imports array (after `CatalogModule`, before/after `AuditModule` — order does not matter for DI). No code comments.
  </action>
  <verify>
    <automated>pnpm nx run api:build && pnpm nx run api:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@Public" apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts` AND `grep -q "@RequireActiveTenant" ...`
    - `grep -q "RestoZodValidationPipe(CreateOrderInputDto)" ...orders.controller.ts` (per-parameter pipe)
    - error-mapping returns `err` for unknown (`grep -q "return err" apps/api/src/contexts/ordering/interfaces/http/error-mapping.ts`)
    - OrderingModule wires ORDER_REPOSITORY→OrderDrizzleRepository + CreateOrderService + GetOrderService + OrdersController
    - `grep -q "OrderingModule" apps/api/src/app.module.ts` in the imports array
    - `pnpm nx run api:build` succeeds (Nest boots with the module wired)
  </acceptance_criteria>
  <done>Anonymous POST /v1/orders endpoint with per-param Zod validation + domain-error mapping; OrderingModule registered; api builds.</done>
</task>

<task type="auto">
  <name>Task 2: audit wiring — ordering.> subscription + ACTION_TARGET_KIND + 'order' targetId branch (ORD-09)</name>
  <files>apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts, apps/api/src/contexts/audit/application/record-audit.service.ts</files>
  <read_first>
    - apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts (the subscription loop at ~line 42 — TENANCY/IDENTITY entries)
    - apps/api/src/contexts/audit/application/record-audit.service.ts (ACTION_TARGET_KIND map at line 7; targetId resolver at lines 67-84 — 'tenant'/'menu'/'menu_item'/'user' branches)
    - 07-PATTERNS.md §"nats-audit-subscriber.ts (MODIFY)" + §"record-audit.service.ts (MODIFY)" (exact insertion points)
    - 07-RESEARCH.md §"Audit Wiring (ORD-09)"
  </read_first>
  <action>
    In `nats-audit-subscriber.ts`: add `const ORDERING_CONSUMER_NAME = 'audit-recorder-ordering';` and `const ORDERING_SUBJECT = 'ordering.>';` alongside the IDENTITY constants, and add `{ subject: ORDERING_SUBJECT, durableName: ORDERING_CONSUMER_NAME }` as a third entry in the subscription loop array (copy the TENANCY block exactly — same `runDeduped` path; NEVER `runInTenantContext`, Pitfall 4). No other change to this file.
    In `record-audit.service.ts`: add five entries to `ACTION_TARGET_KIND` — `'ordering.order_created':'order'`, `'ordering.order_paid':'order'`, `'ordering.order_canceled':'order'`, `'ordering.order_refunded':'order'`, `'ordering.order_status_changed':'order'`. In the `targetId` resolver IIFE, add an `if (targetType === 'order') return typeof payload.orderId === 'string' && payload.orderId.length > 0 ? payload.orderId : null;` branch alongside the existing 'tenant'/'menu_item'/'user' branches. No code comments.
  </action>
  <verify>
    <automated>pnpm nx run api:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "audit-recorder-ordering" apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts`
    - `grep -q "ordering.>" apps/api/src/contexts/audit/infrastructure/nats-audit-subscriber.ts`
    - `grep -cE "ordering\\.order_(created|paid|canceled|refunded|status_changed)" apps/api/src/contexts/audit/application/record-audit.service.ts` === 5
    - `grep -q "targetType === 'order'" apps/api/src/contexts/audit/application/record-audit.service.ts` with a `payload.orderId` resolver
    - no `runInTenantContext` added to the subscriber
    - `pnpm nx run api:typecheck` passes
  </acceptance_criteria>
  <done>audit context subscribes to ordering.> via runDeduped, maps the 5 ordering.* events to 'order', and resolves targetId from payload.orderId (ORD-09).</done>
</task>

<task type="auto">
  <name>Task 3: tenant-isolation rows for orders/order_items (ORD-06 net) + mandatory audit-projection test + advisory e2e (ORD-07/ORD-09)</name>
  <files>packages/db/test/integration/tenant-isolation.spec.ts, apps/api/src/contexts/ordering/interfaces/http/orders.controller.ts</files>
  <read_first>
    - packages/db/test/integration/tenant-isolation.spec.ts (existing cross-tenant matrix — how menu tables are exercised; add orders/order_items rows the same way)
    - apps/api/src/contexts/audit/application/record-audit.service.ts (the RecordAuditService projection entry point under test — how it reads an envelope and writes an audit_log row)
    - apps/api/test/ integration/e2e harness (existing spec that projects an envelope through RecordAuditService and/or drives an HTTP request with a resolved tenant + asserts an audit row — copy the pattern; e.g. identity-audit)
    - 07-RESEARCH.md §"Validation Architecture" (ORD-06 → tenant-isolation; ORD-09 audit-row assertion)
    - 07-PATTERNS.md §"Shared Patterns" (db.withTenant cross-tenant denial)
  </read_first>
  <action>
    Extend `packages/db/test/integration/tenant-isolation.spec.ts` to insert an `orders` row (+ one `order_items` row) under tenant A and assert tenant B's `db.withTenant` context cannot read it (RLS + ScopedTx denial), mirroring the existing menu-table cases — this folds the four new tables into the ORD-06 isolation net.
    ORD-09 audit projection — MANDATORY and NATS-timing-independent: add a focused integration test that feeds an `ordering.order_created.v1` envelope (with a known `orderId`) directly through the existing audit projection path — i.e. invoke `RecordAuditService` the same way `runDeduped` does in the subscriber — and assert it writes one `audit_log` row whose `target_id === orderId` and action maps to the `'order'` target kind. This test does NOT depend on the NATS broker round-trip; it proves the projection deterministically, so ORD-09 is actually verified rather than deferred to a flaky broker. (This replaces the previous disjunctive "audit row OR outbox row" fallback — the projection assertion is required, not optional.)
    Advisory e2e (may be skipped if the broker is flaky in CI, but write it): one end-to-end test that POSTs a valid cart payload to `/v1/orders` with a resolved tenant host, asserts a 201 with `{ orderId, orderNumber }`, asserts exactly one `orders` row + the expected `order_items`/`order_modifiers` rows, and asserts the `outbox_events` row for `ordering.order_created.v1` exists (ORD-07 contract). The full outbox→NATS→audit round-trip in this e2e is advisory; the REQUIRED audit proof is the projection integration test above. Keep the controller file edit minimal (it should already be complete from Task 1 — only touch it here if the e2e reveals a wiring gap). No code comments.
  </action>
  <verify>
    <automated>pnpm vitest run packages/db/test/integration/tenant-isolation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tenant-isolation.spec.ts has an `orders` (and `order_items`) cross-tenant denial case that passes
    - REQUIRED: a focused integration test feeds an `ordering.order_created.v1` envelope through `RecordAuditService` (the existing runDeduped projection path) and asserts ONE `audit_log` row with `target_id === orderId` — NATS-timing-independent, no disjunctive fallback (ORD-09 proven)
    - ADVISORY: the e2e POST→201 + single orders row + `outbox_events` row for `ordering.order_created.v1` (ORD-07); the full NATS round-trip in the e2e may be skipped if the broker is flaky, but the projection test above is NOT skippable
    - `pnpm vitest run packages/db/test/integration/tenant-isolation.spec.ts` green
    - `pnpm nx affected --target=test` green for the api + db projects (or the equivalent scoped run)
  </acceptance_criteria>
  <done>orders/order_items folded into the cross-tenant isolation net (ORD-06); ORD-09 proven by a mandatory NATS-independent RecordAuditService projection test (audit_log row, target_id === orderId); ORD-07 outbox row asserted; full e2e round-trip advisory.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                    |
| ------------------------------------ | ------------------------------------------------------------------------------ |
| anonymous internet → POST /v1/orders | Unauthenticated endpoint; the primary untrusted-input crossing for the phase   |
| host header → tenant resolution      | A spoofed/absent host must resolve to 404, never to another tenant             |
| outbox → NATS → audit                | Crosses a durable async boundary; tenant context must not leak across messages |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                               |
| --------- | ---------------------- | -------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-07-10   | Elevation of privilege | POST /v1/orders auth                   | mitigate    | `@Public()` is intentional (ORD-03 anonymous) BUT `@RequireActiveTenant()` enforces a resolved tenant → 404 on no/invalid tenant; no cross-tenant write possible (tenant from host, not body) |
| T-07-05   | Tampering              | request body price fields              | mitigate    | DTO (Plan 04) accepts no total/subtotal; RestoZodValidationPipe per-parameter validates the shape at the controller                                                                           |
| T-07-01   | Information disclosure | cross-tenant order read                | mitigate    | tenant-isolation spec now asserts orders/order_items RLS+ScopedTx denial (Task 3)                                                                                                             |
| T-07-11   | Tampering              | audit subscriber tenant-context mixing | mitigate    | ordering.> subscription uses `runDeduped` (db.withoutTenant internally), never `runInTenantContext` (Pitfall 4); grep gate in Task 2                                                          |
| T-07-SC   | Tampering              | npm installs                           | accept      | No new packages this phase (RESEARCH.md §Package Legitimacy Audit: N/A)                                                                                                                       |

</threat_model>

<verification>
- `pnpm nx run api:build` (Nest boots with OrderingModule)
- `pnpm vitest run packages/db/test/integration/tenant-isolation.spec.ts` (orders/order_items isolation)
- the RecordAuditService projection integration test asserts a single audit_log row for ordering.order_created with target_id === orderId (REQUIRED, NATS-independent); the order-create e2e asserts 201 + single order row + outbox row (full round-trip advisory)
- no `runInTenantContext` in the audit subscriber
</verification>

<success_criteria>

- Anonymous POST /v1/orders creates an order end-to-end (ORD-01/ORD-03)
- OrderingModule registered; api builds and boots
- audit context records ordering.order_created rows with target_id = orderId, proven by a NATS-independent projection test (ORD-07/ORD-09)
- orders/order_items cross-tenant isolation proven (ORD-06)
  </success_criteria>

<output>
Create `.planning/phases/07-ordering/07-05-SUMMARY.md` when done.
</output>
</content>
