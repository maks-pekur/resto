-- The guest was told the figures might be approximate; nobody wanted to say it, and no screen
-- set the flag. A column only a dead checkbox wrote is worse than no column.
ALTER TABLE menu_items DROP COLUMN nutrition_estimated;
