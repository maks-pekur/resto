{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "config-eslint",
  "projectType": "library",
  "sourceRoot": "packages/config-eslint",
  "tags": ["scope:shared", "type:lib", "layer:infrastructure"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/config-eslint"
      }
    }
  }
}
